---
phase: 20-clerk-foundation-setup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sign-in/[[...sign-in]]/page.tsx
autonomous: true
requirements: [AUTH-03]
gap_closure: true
gap_source: 20-UAT.md

must_haves:
  truths:
    - "Theme toggle is visible on sign-in page"
    - "User can switch between light, dark, and system themes on sign-in page"
    - "Clerk sign-in form colors update when theme changes"
  artifacts:
    - path: "src/app/sign-in/[[...sign-in]]/page.tsx"
      provides: "Sign-in page with ThemeToggle component"
      contains: "ThemeToggle"
  key_links:
    - from: "src/app/sign-in/[[...sign-in]]/page.tsx"
      to: "src/components/ui/ThemeToggle.tsx"
      via: "import and render"
      pattern: "import.*ThemeToggle"
---

<objective>
Add ThemeToggle component to the sign-in page so users can switch between light and dark themes.

Purpose: Close UAT gap from Test 4 - user reported "There is no theme toggle on the page."
Output: Sign-in page with functional theme toggle in corner position
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-clerk-foundation-setup/20-UAT.md
@src/components/ui/ThemeToggle.tsx

<interfaces>
<!-- ThemeToggle component - already exists, just needs to be imported -->

From src/components/ui/ThemeToggle.tsx:
```typescript
// Client component using next-themes
// Renders a 3-button radio group: Light | Dark | System
// Uses design tokens for styling (--primary, --divider, --text-*, --bg-*)
export default function ThemeToggle(): JSX.Element;
```

From src/app/sign-in/[[...sign-in]]/page.tsx (current state):
```typescript
import { SignIn } from "@clerk/nextjs";
import { clerkAppearance } from "@/lib/clerk-theme";

export default function SignInPage(): JSX.Element;
// Currently renders: <main> with branding div + <SignIn /> component
// Missing: ThemeToggle component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ThemeToggle to sign-in page</name>
  <files>src/app/sign-in/[[...sign-in]]/page.tsx</files>
  <action>
1. Add "use client" directive at top of file (required because ThemeToggle uses client hooks)
2. Import ThemeToggle: `import ThemeToggle from "@/components/ui/ThemeToggle";`
3. Add ThemeToggle component positioned in the top-right corner of the page, outside the centered content flow
4. Use absolute positioning with Tailwind: `absolute top-4 right-4`
5. Ensure the toggle is accessible and does not overlap with the centered sign-in form

The layout should be:
- ThemeToggle in top-right corner (absolute positioned)
- Branding + SignIn form centered (existing layout unchanged)
  </action>
  <verify>
    <automated>grep -q "ThemeToggle" src/app/sign-in/\[\[...sign-in\]\]/page.tsx && grep -q "use client" src/app/sign-in/\[\[...sign-in\]\]/page.tsx && echo "PASS: ThemeToggle imported and client directive present"</automated>
  </verify>
  <done>ThemeToggle component is imported and rendered on sign-in page with top-right positioning</done>
</task>

</tasks>

<verification>
1. `grep "ThemeToggle" src/app/sign-in/\[\[...sign-in\]\]/page.tsx` shows import and usage
2. `npm run build` completes without errors
3. Visual check: Navigate to /sign-in, theme toggle visible in top-right corner
4. Functional check: Clicking Light/Dark/System changes the Clerk form colors
</verification>

<success_criteria>
- ThemeToggle component visible on sign-in page
- Theme toggle positioned in top-right corner (does not interfere with sign-in form)
- Clicking theme options changes both page background and Clerk form colors
- UAT Test 4 gap is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/20-clerk-foundation-setup/20-04-SUMMARY.md`
</output>
