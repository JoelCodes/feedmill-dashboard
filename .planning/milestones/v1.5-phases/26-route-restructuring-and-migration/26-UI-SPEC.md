---
phase: 26
slug: route-restructuring-and-migration
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-11
---

# Phase 26 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (CSS variables in globals.css) |
| Preset | not applicable |
| Component library | none (custom components) |
| Icon library | Lucide React v0.577.0 |
| Font | Helvetica, Arial, sans-serif (system stack) |

**Note:** Phase 26 is a route restructuring phase with no new visual components. All UI elements reuse existing design system from globals.css and established components (DashboardLayout, Sidebar, Header). The only new page is a minimal Coming Soon placeholder using existing tokens.

---

## Spacing Scale

Declared values (reusing existing tokens from globals.css):

| Token | Value | Usage |
|-------|-------|-------|
| --space-1 | 4px | Icon gaps, inline padding |
| --space-2 | 8px | Compact element spacing |
| --space-4 | 16px | Default element spacing |
| --space-5 | 24px | Section padding |
| --space-6 | 32px | Layout gaps |
| --space-8 | 48px | Major section breaks |
| --space-10 | 64px | Page-level spacing |

Exceptions: none

**Source:** Pre-existing CSS variables in `src/app/globals.css` lines 72-81

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 12px (text-xs for nav labels) | 700 (font-bold) | 1.5 (default) |
| Label | 10px (--fs-10) | 700 (font-bold) | 1.5 (default) |
| Heading (Coming Soon) | 24px (text-2xl) | 700 (font-bold) | 1.2 (default) |
| Subtext (Coming Soon) | 14px (text-sm) | 400 (regular) | 1.5 (default) |

**Source:** Existing Sidebar component (lines 42-43, 51-52, 116-120) and standard Tailwind typography classes.

**Note:** Phase 26 reuses established typography from existing components. Coming Soon page uses text-2xl for heading and text-sm for body per CONTEXT.md D-05 (minimal placeholder).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #f8f9fa (--bg-page) | Page background |
| Secondary (30%) | #ffffff (--bg-card) | Sidebar, cards, header |
| Accent (10%) | #4fd1c5 (--primary) | Logo, active nav icons, links |
| Destructive | not applicable | No destructive actions in this phase |

Accent reserved for:
- Logo background (Sidebar line 41)
- Active navigation icon background (Sidebar line 108)
- Inactive navigation icon color (Sidebar line 112)

**Source:** Existing color tokens from `src/app/globals.css` lines 6-22 and current Sidebar implementation.

**Note:** Phase 26 uses existing color system. No new accent usage or destructive actions introduced.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | not applicable (no CTAs in this phase) |
| Coming Soon heading | "Coming Soon" |
| Coming Soon body | "Production features launching soon." |
| Empty state heading | not applicable (no empty states) |
| Empty state body | not applicable |
| Error state | not applicable (no error states) |
| Destructive confirmation | not applicable (no destructive actions) |

**Source:** CONTEXT.md D-05 specifies "minimal placeholder content — simple 'Coming Soon' heading with brief professional subtext."

**Note:** Phase 26 only introduces a Coming Soon homepage. All other pages (Orders, Customers, Mill Production) are being moved, not redesigned, so they retain their existing copywriting.

---

## Component Inventory

Phase 26 reuses existing components without modification:

| Component | Location | Purpose |
|-----------|----------|---------|
| DashboardLayout | src/components/DashboardLayout.tsx | Wraps Coming Soon page |
| Sidebar | src/components/Sidebar.tsx | Extended with context-aware navigation (demo vs production) |
| Header | src/components/Header.tsx | Page title display (may need update for /demo/* routes) |

**New elements:**
- Coming Soon page (`src/app/page.tsx`) - Simple centered text using existing layout

**Modified elements:**
- Sidebar navItems: Split into `demoNavItems` (Orders, Customers, Mill Production at /demo/* paths) and `productionNavItems` ("Coming Soon" placeholder) based on `pathname.startsWith('/demo')` check

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |

**Note:** Phase 26 uses no shadcn components or third-party registries. All UI elements are custom components from existing codebase or native HTML elements.

---

## Implementation Notes

### Context-Aware Sidebar Navigation

Sidebar component will conditionally render different navigation items based on current route:

**Demo context** (`pathname.startsWith('/demo')`):
- Section header: "DEMO"
- Links: Orders (/demo/orders), Customers (/demo/customers), Mill Production (/demo/mill-production)
- Settings section: Formulas (unchanged)

**Production context** (all other routes):
- Section header: "PRODUCTION"
- Links: Coming Soon (/) - single placeholder link
- Settings section: Formulas (unchanged)

**Source:** CONTEXT.md D-02 (route-based detection), D-03 (production placeholder), D-04 (demo navigation), RESEARCH.md lines 596-724 (conditional navigation code example)

### Coming Soon Page Layout

```typescript
<DashboardLayout>
  <div className="flex flex-1 items-center justify-center">
    <div className="text-center">
      <h1 className="text-2xl font-bold text-[var(--text-primary)]">
        Coming Soon
      </h1>
      <p className="text-text-secondary mt-2 text-sm">
        Production features launching soon.
      </p>
    </div>
  </div>
</DashboardLayout>
```

**Source:** RESEARCH.md lines 726-751 (Coming Soon homepage code example), CONTEXT.md D-05 (minimal content), D-06 (full layout)

---

## Accessibility Notes

No new accessibility requirements beyond existing implementation:
- Existing Sidebar uses semantic `<nav>` and `<Link>` elements (already WCAG compliant)
- Coming Soon page uses semantic heading hierarchy (h1)
- No interactive elements requiring keyboard navigation beyond existing patterns

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Sources

| Decision | Source |
|----------|--------|
| Spacing scale | `src/app/globals.css` lines 72-81 (existing CSS variables) |
| Typography | `src/components/Sidebar.tsx` + Tailwind defaults |
| Color tokens | `src/app/globals.css` lines 6-22 (existing color system) |
| Copywriting | CONTEXT.md D-05 (minimal placeholder specification) |
| Sidebar patterns | `src/components/Sidebar.tsx` lines 28-33, 35-36 (existing `usePathname()` usage) |
| Layout structure | `src/components/DashboardLayout.tsx` (existing component) |
| Coming Soon design | RESEARCH.md lines 726-751 (code example), CONTEXT.md D-05, D-06 |
| Navigation logic | RESEARCH.md lines 596-724 (conditional rendering pattern) |

---

*Generated: 2026-05-11*
*Phase 26 is a route reorganization phase with minimal new UI - all design decisions reference existing system*
