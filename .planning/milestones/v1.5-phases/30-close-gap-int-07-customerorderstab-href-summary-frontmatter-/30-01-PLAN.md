---
phase: 30
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/CustomerOrdersTab.tsx
  - src/components/__tests__/CustomerOrdersTab.test.tsx
autonomous: true
requirements:
  - ROUTE-01
tags:
  - cleanup
  - href-migration
  - tdd
  - regression-test
must_haves:
  decisions_covered:
    - "D-01: scope — INT-07 (sole v1.5 blocker) is in scope"
    - "D-02: mirroring Jest component test on CustomerOrdersTab is in scope"
    - "D-05: change CustomerOrdersTab.tsx:159 href from /orders to /demo/orders (sibling-component application of Phase 29 D-05)"
    - "D-06: new test file src/components/__tests__/CustomerOrdersTab.test.tsx asserts rendered href shape (mirrors Phase 29 D-06)"
    - "D-07: test location is src/components/__tests__/ (not co-located) — matches sibling-component convention"
    - "D-08: reuse jest.mock('next/link') MockLink pattern verbatim from Timeline.test.tsx lines 8–14"
    - "D-09: test scope is minimal — href shape only; no broader CustomerOrdersTab coverage"
    - "D-12: source edit + test file land in a single atomic commit"
  truths:
    - "CustomerOrdersTab order-row Link href navigates to /demo/orders?selected=<orderId> (the live route after Phase 26) [D-05]"
    - "An automated component test asserts the demo-prefixed href shape on CustomerOrdersTab, preventing this exact regression class on the sibling component Phase 29 missed [D-06]"
    - "FLOW-07 (demo user → /demo/customers/[id] → Orders tab → click row → /demo/orders?selected=<id>) is wired end-to-end (no 404)"
  artifacts:
    - path: src/components/CustomerOrdersTab.tsx
      provides: "Order-row Link that points to the live /demo/orders route at line 159"
      contains: "/demo/orders?selected="
    - path: src/components/__tests__/CustomerOrdersTab.test.tsx
      provides: "Component test asserting the demo-prefixed href shape on a rendered order row"
      contains: "/demo/orders?selected=order-1"
      min_lines: 20
  key_links:
    - from: src/components/CustomerOrdersTab.tsx
      to: /demo/orders route
      via: next/link href template literal at line 159
      pattern: "href=\\{`/demo/orders\\?selected="
    - from: src/components/__tests__/CustomerOrdersTab.test.tsx
      to: CustomerOrdersTab.tsx href shape
      via: toHaveAttribute assertion against single-order fixture
      pattern: "toHaveAttribute\\('href', '/demo/orders\\?selected=order-1'\\)"
---

<objective>
Close INT-07 (the sole remaining v1.5 milestone blocker): the `CustomerOrdersTab` client component still links to the deleted `/orders` path, breaking FLOW-07 (demo customer-detail → Orders tab → order detail). This is the sibling-component miss not covered by Phase 29's INT-01 scope.

Apply the one-line href fix at `CustomerOrdersTab.tsx:159` and pair it with a new Jest component test (`src/components/__tests__/CustomerOrdersTab.test.tsx`) that locks the corrected href shape — mirroring Phase 29 D-05/D-06 exactly.

Purpose: Restore the broken demo Orders-tab navigation and add a structural mechanism (a Jest component test) that prevents the same class of regression on this sibling component going forward.
Output: Updated `CustomerOrdersTab.tsx` href + new `CustomerOrdersTab.test.tsx`. Jest test for `CustomerOrdersTab` goes RED then GREEN as the TDD cycle completes. Single atomic commit per D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/v1.5-INTEGRATION-CHECK.md
@.planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-CONTEXT.md
@.planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-RESEARCH.md
@.planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-PATTERNS.md
@.planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-VALIDATION.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-01-PLAN.md

<interfaces>
<!-- All identifiers, imports, and verbatim patterns the executor needs. No codebase exploration required. -->

From src/components/CustomerOrdersTab.tsx (current state, the file being modified):
- "use client" component (line 1)
- Imports at lines 3–8: useState/useMemo, Package/Search (lucide-react), StatusBadge, Order/OrderStatus, formatDeliveryDate, and Link from "next/link"
- Default export: `export default function CustomerOrdersTab({ orders }: { orders: Order[] })` (line 16)
- STATUS_FILTERS constant at line 14: ["Producing", "Ready", "Complete"]
- activeStatuses default at line 18: new Set<OrderStatus>() (empty → all orders render unfiltered)
- The render-tree: filteredOrders.map at lines 156–161 produces one <Link> per order
- LINE 159 (the bug — current value):
    href={`/orders?selected=${order.id}`}
- LINE 159 (target value after this plan):
    href={`/demo/orders?selected=${order.id}`}

From src/components/ui/Timeline.test.tsx (read-only reference; the verbatim MockLink template at lines 7–14):
The file mocks next/link as a plain anchor:
- jest.mock('next/link', () => { const MockLink = ({ children, href }: { children: React.ReactNode; href: string }) => (<a href={href}>{children}</a>); MockLink.displayName = 'MockLink'; return MockLink; });
This is why `toHaveAttribute('href', ...)` works directly on the rendered DOM. Copy verbatim per D-08.

From src/components/ui/Timeline.test.tsx (read-only reference; the assertion shape at line 82):
- expect(link).toHaveAttribute('href', '/demo/orders?selected=order-1');
This is the Phase 29 D-06 precedent that this plan's new assertion mirrors byte-for-byte except for the surrounding fixture context.

From src/components/__tests__/OrdersTable.test.tsx (read-only reference; the inline mock fixture template at lines 5–20):
Default export import form: `import OrdersTable from "../OrdersTable";`
Inline Order[] fixture shape — all 13 fields required by the Order interface:
  const mockOrders: Order[] = [
    {
      id: "ORD-001",
      documentNumber: "12345",
      customer: "Test Farm",
      customerId: "CUST-001",
      textureType: "Coarse",
      formulaType: "Grower",
      quantity: 10,
      location: "Springfield, IL",
      deliveryDate: new Date("2026-05-15"),
      status: "Producing",
      hasChanges: false,
      createdAt: new Date("2026-05-01"),
      updatedAt: new Date("2026-05-01"),
    },
  ];
For the new test, use the same shape but set id: "order-1" so the assertion literal mirrors Timeline.test.tsx:82.

From src/types/order.ts (read-only reference; the Order interface contract):
- Order requires all 13 fields: id, documentNumber, customer, customerId, textureType, formulaType, quantity, location, deliveryDate, status, hasChanges, createdAt, updatedAt
- OrderStatus = "Pending" | "Producing" | "Ready" | "In Transit" | "Complete"
- "Producing" is unfiltered through STATUS_FILTERS and renders by default — matches OrdersTable.test.tsx precedent

From .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md §D-05/D-06:
- D-05 precedent: one-line href fix from `/orders?selected=` to `/demo/orders?selected=`
- D-06 precedent: paired Jest component test asserting the demo-prefixed href shape
- Atomic commit: source edit + test land together (this plan applies the same pattern to the CustomerOrdersTab sibling)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — create CustomerOrdersTab.test.tsx with failing demo-prefixed href assertion</name>
  <files>src/components/__tests__/CustomerOrdersTab.test.tsx</files>
  <read_first>
    - src/components/ui/Timeline.test.tsx (entire file; MockLink pattern at lines 7–14; assertion shape at line 82 — D-08 locked verbatim reuse)
    - src/components/__tests__/OrdersTable.test.tsx (lines 1–51; inline `mockOrders: Order[]` fixture template and the default-export import form `import OrdersTable from "../OrdersTable";`)
    - src/types/order.ts (the Order interface — all 13 required fields and the OrderStatus union)
    - src/components/CustomerOrdersTab.tsx (entire file — confirm default-export shape `export default function CustomerOrdersTab({ orders }: { orders: Order[] })` at line 16, confirm line 159 still reads `` href={`/orders?selected=${order.id}`} ``, and confirm the filteredOrders.map render path renders one `<Link>` per order)
    - .planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-PATTERNS.md §"`src/components/__tests__/CustomerOrdersTab.test.tsx` (component test, request-response)" (the full copyable test scaffold)
    - .planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-CONTEXT.md §Decisions D-06/D-07/D-08/D-09 (test location, mock pattern, minimal scope)
  </read_first>
  <behavior>
    - A new file `src/components/__tests__/CustomerOrdersTab.test.tsx` exists with exactly one `it()` block named `'renders order row link with /demo/orders?selected=<id> href'`.
    - The test renders `<CustomerOrdersTab orders={mockOrders} />` where `mockOrders` is a single-element `Order[]` array with `id: 'order-1'` and `status: 'Producing'`.
    - The test mocks `next/link` using the verbatim MockLink pattern from Timeline.test.tsx lines 7–14 (no deviation; same `MockLink.displayName = 'MockLink'`).
    - The assertion is `expect(link).toHaveAttribute('href', '/demo/orders?selected=order-1')` against `screen.getByRole('link')` (no `name:` filter — single-order fixture means exactly one `<a>` after MockLink replacement).
    - Running `npm test -- --testPathPattern=CustomerOrdersTab` MUST exit non-zero with output mentioning both the expected `'/demo/orders?selected=order-1'` and the received `'/orders?selected=order-1'`. RED state proves the test is exercising line 159 of CustomerOrdersTab.tsx.
  </behavior>
  <action>
    Create `src/components/__tests__/CustomerOrdersTab.test.tsx` as a new file. Use the full copyable scaffold from 30-PATTERNS.md §"src/components/__tests__/CustomerOrdersTab.test.tsx" lines 124–164 — copy byte-for-byte. Identifiers required:
    - Default-export import: `import CustomerOrdersTab from '../CustomerOrdersTab';` (matches OrdersTable.test.tsx default-import form)
    - Order type import: `import { Order } from '@/types/order';` (uses the `@/` jest config alias)
    - Testing Library imports: `import { render, screen } from '@testing-library/react';`
    - MockLink jest.mock block verbatim from Timeline.test.tsx:7–14 (D-08; including `MockLink.displayName = 'MockLink';` and the `return MockLink;`). Mock placement immediately after imports, before fixtures (mirrors Timeline.test.tsx convention; Jest hoists jest.mock regardless).
    - Inline fixture: `const mockOrders: Order[] = [ { id: 'order-1', documentNumber: '12345', customer: 'Test Farm', customerId: 'CUST-001', textureType: 'Coarse', formulaType: 'Grower', quantity: 10, location: 'Springfield, IL', deliveryDate: new Date('2026-05-15'), status: 'Producing', hasChanges: false, createdAt: new Date('2026-05-01'), updatedAt: new Date('2026-05-01') } ];` (all 13 Order fields; `id: 'order-1'` mirrors Timeline.test.tsx:82 precedent; `status: 'Producing'` mirrors OrdersTable.test.tsx:16). Single element only — D-09 minimal scope; multi-order would force `getAllByRole` and break the unambiguous single-link query (Pitfall 1 in 30-RESEARCH.md).
    - Describe + single it() block: `describe('CustomerOrdersTab', () => { it('renders order row link with /demo/orders?selected=<id> href', () => { render(<CustomerOrdersTab orders={mockOrders} />); const link = screen.getByRole('link'); expect(link).toHaveAttribute('href', '/demo/orders?selected=order-1'); }); });`
    Do NOT add any of the following (D-09 scope discipline): search-input tests, filter-pill tests, empty-state tests, statusCounts memoization tests, hover-state tests, multi-order tests, accessibility / jest-axe tests, beforeEach/afterEach hooks, custom render wrappers. The test file contains exactly one `it()` block. This task implements D-02/D-06/D-07/D-08/D-09 per user decision and is the RED step of the TDD pair with Task 2 (D-05).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=CustomerOrdersTab 2>&1 | grep -E "(FAIL|/demo/orders\\?selected=order-1|/orders\\?selected=order-1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/__tests__/CustomerOrdersTab.test.tsx` exists: `test -f src/components/__tests__/CustomerOrdersTab.test.tsx && echo OK` prints `OK`.
    - File contains the demo-prefixed assertion literal exactly once: `grep -c "/demo/orders?selected=order-1" src/components/__tests__/CustomerOrdersTab.test.tsx` returns `1`.
    - File contains the verbatim MockLink mock: `grep -c "MockLink.displayName = 'MockLink'" src/components/__tests__/CustomerOrdersTab.test.tsx` returns `1`.
    - File contains exactly one `it(...)` block: `grep -c "  it(" src/components/__tests__/CustomerOrdersTab.test.tsx` returns `1` (D-09 minimal scope).
    - File uses the default-import form: `grep -c "^import CustomerOrdersTab from '../CustomerOrdersTab';" src/components/__tests__/CustomerOrdersTab.test.tsx` returns `1`.
    - Running `npm test -- --testPathPattern=CustomerOrdersTab` exits non-zero AND the output contains the substring `FAIL` AND mentions both `/demo/orders?selected=order-1` (expected) and `/orders?selected=order-1` (received) — proving the assertion fires against the still-stale href at CustomerOrdersTab.tsx:159.
  </acceptance_criteria>
  <done>The new component test file exists in `src/components/__tests__/` and FAILS against the current stale `CustomerOrdersTab.tsx:159` href. Do NOT commit yet — the source edit (Task 2) lands in the same atomic commit per D-12.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — change CustomerOrdersTab.tsx:159 href to /demo/orders</name>
  <files>src/components/CustomerOrdersTab.tsx</files>
  <read_first>
    - src/components/CustomerOrdersTab.tsx (the entire file — confirm the import block at lines 1–8 is untouched; confirm the `filteredOrders.map` block at lines 156–161; specifically the `href` template literal on line 159 currently reads `` `/orders?selected=${order.id}` ``)
    - .planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-PATTERNS.md §"`src/components/CustomerOrdersTab.tsx` (client component, request-response)" (current state vs target state at line 159; the 5-character `/demo` prefix insertion)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md §D-05 (one-line href fix precedent — Timeline.tsx:123)
  </read_first>
  <behavior>
    - Running `npm test -- --testPathPattern=CustomerOrdersTab` MUST exit zero and the previously-RED test `'renders order row link with /demo/orders?selected=<id> href'` MUST pass.
    - Running `npm test` full suite MUST exit zero (no other tests regressed by this 5-character source edit).
    - No other behavior in CustomerOrdersTab changes (search input, filter pills, status counts, empty state, formatDeliveryDate calls, StatusBadge rendering are all untouched).
  </behavior>
  <action>
    In `src/components/CustomerOrdersTab.tsx`, on line 159, change the href template literal from `` `/orders?selected=${order.id}` `` to `` `/demo/orders?selected=${order.id}` ``. The edit is the addition of the 5-character `/demo` prefix immediately before `/orders` inside the backtick template literal — preserving the leading indent (6 spaces + `href={\``), the `${order.id}` interpolation, and the trailing `\`}`. Do NOT touch the surrounding `<Link>` JSX, the `className` prop on the Link (lines 160–161), the import statements (lines 1–8), the STATUS_FILTERS constant (line 14), the activeStatuses default (line 18), the filteredOrders.map signature (line 156), or any other line in the file. This task implements D-05 per user decision and is the GREEN step of the TDD pair with Task 1 (D-06).

    After saving, run `npm test -- --testPathPattern=CustomerOrdersTab` and confirm the test passes (GREEN). Then run `npm test` to confirm full-suite green. Then stage both files together (`git add src/components/CustomerOrdersTab.tsx src/components/__tests__/CustomerOrdersTab.test.tsx`) and create the single atomic commit per D-12: `fix(30): INT-07 CustomerOrdersTab href + regression test`. Use explicit file paths in `git add` — do NOT use `git add -A` or `git add .` (Pitfall 5 in 30-RESEARCH.md; the 30-02 frontmatter edits belong to a separate commit).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=CustomerOrdersTab</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CustomerOrdersTab.tsx` contains exactly one demo-prefixed href template literal: `grep -c "href={\`/demo/orders?selected=\${order.id}\`}" src/components/CustomerOrdersTab.tsx` returns `1`.
    - File `src/components/CustomerOrdersTab.tsx` contains zero bare-prefixed `/orders?selected=` references: `grep -E "(^|[^/])/orders\\?selected=" src/components/CustomerOrdersTab.tsx` returns no output (exit code non-zero from grep is fine; empty stdout is the acceptance signal).
    - Running `npm test -- --testPathPattern=CustomerOrdersTab` exits 0; output contains `PASS` and no `FAIL` for the CustomerOrdersTab suite.
    - Running `npm test` full suite exits 0 (no other tests regressed).
    - Repo-wide stale-href sweep is clean: `grep -rE "(^|[^/])/orders\\?selected=" src/` returns no output (zero hits anywhere in src/) — this proves FLOW-07 has no remaining stale sibling.
    - A single atomic commit was created with message `fix(30): INT-07 CustomerOrdersTab href + regression test` containing exactly two files: `src/components/CustomerOrdersTab.tsx` and `src/components/__tests__/CustomerOrdersTab.test.tsx`. Verify via `git log -1 --name-only` showing exactly these two files staged.
  </acceptance_criteria>
  <done>The CustomerOrdersTab component test passes against the corrected `CustomerOrdersTab.tsx:159` href. Both files are committed atomically per D-12 commit #1. FLOW-07 is wired end-to-end. The repo-wide stale-href sweep returns zero hits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan changes the route prefix of an `href` template literal from `/orders` (404 dead route) to `/demo/orders` (existing route already guarded by the same `requireRole('demo')` middleware that protects the page rendering CustomerOrdersTab). No new auth/role surface; no user-input handling; no new data exposure. The 5-character edit lives entirely inside an already-trusted render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `CustomerOrdersTab.tsx:159` href template literal | accept | The only interpolated value is `order.id`, which is owned typed data (`Order.id: string` from the server-fetched orders prop), not user input. No external URL/scheme interpolation. Same risk profile as the equivalent Timeline.tsx:123 path that Phase 29 already shipped without incident. |
| T-30-02 | Information disclosure | Route enumeration via 404 → 200 transition | accept | Both `/orders` (404) and `/demo/orders` (auth-guarded 200) are gated by middleware that fires before page render. Fixing the 404 does not change what an unauthenticated probe learns. Verified in `.planning/v1.5-INTEGRATION-CHECK.md` §"requireRole — 4 Demo Pages Verified" — `/demo/orders` is behind the same `requireRole('demo')` guard as the page that hosts CustomerOrdersTab. |

**Rationale for empty mitigation surface:** Per 30-RESEARCH.md §"Security Domain", no ASVS category (V2–V6) applies. The phase makes a 5-character routing-prefix edit inside an already-protected client component. No new attack surface is introduced.
</threat_model>

<verification>
- `npm test -- --testPathPattern=CustomerOrdersTab` exits 0 (the new test passes).
- `npm test` full suite exits 0 (no regressions).
- `grep -c "/demo/orders?selected=" src/components/CustomerOrdersTab.tsx` returns `1`.
- `grep -c "/demo/orders?selected=order-1" src/components/__tests__/CustomerOrdersTab.test.tsx` returns `1`.
- `grep -E "(^|[^/])/orders\\?selected=" src/components/CustomerOrdersTab.tsx` returns no output.
- Repo-wide sweep: `grep -rE "(^|[^/])/orders\\?selected=" src/` returns no output (zero hits in src/).
- `git log -1 --pretty=format:'%s'` returns exactly `fix(30): INT-07 CustomerOrdersTab href + regression test`.
- `git log -1 --name-only --pretty=format:''` lists exactly two files: `src/components/CustomerOrdersTab.tsx` and `src/components/__tests__/CustomerOrdersTab.test.tsx`.
</verification>

<success_criteria>
- INT-07 (the sole v1.5 blocker) and FLOW-07 are closed: the CustomerOrdersTab order-row link points to `/demo/orders`.
- A component test now locks the href shape on the CustomerOrdersTab sibling — Phase 29 caught Timeline, Phase 30 catches CustomerOrdersTab, and the regression net is now structural across both sibling components in the `/demo/customers/[id]` render chain.
- No other tests regressed; full suite green.
- The CustomerOrdersTab source diff is exactly one line (line 159, 5-character prefix insertion). No collateral edits to imports, surrounding JSX, or other component logic.
- The new test file contains exactly one `it()` block per D-09 minimal scope; no broader CustomerOrdersTab coverage was added.
- A single atomic commit (D-12 commit #1) bundles the test with the fix.
</success_criteria>

<output>
After completion, create `.planning/phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-01-SUMMARY.md`.
</output>
