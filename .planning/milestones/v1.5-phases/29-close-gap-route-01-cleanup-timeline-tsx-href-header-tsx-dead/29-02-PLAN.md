---
phase: 29
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
  - src/components/__tests__/Header.test.tsx
autonomous: true
requirements:
  - ROUTE-01
tags:
  - cleanup
  - dead-code
  - tdd
must_haves:
  decisions_covered:
    - "D-01: scope — INT-06 (Header dead branches) is in scope"
    - "D-11: delete the 3 legacy startsWith branches from Header.getPageTitle"
  truths:
    - "Header.getPageTitle no longer contains the legacy /orders, /customers, /mill-production startsWith branches [D-11]"
    - "Header.getPageTitle('/orders') returns 'Dashboard' (falls through to the default), not 'Orders' [D-11]"
    - "An automated component test locks the new behavior, preventing accidental reintroduction of legacy fallback branches"
  artifacts:
    - path: src/components/Header.tsx
      provides: "getPageTitle without legacy fallback branches"
      min_lines: 30
    - path: src/components/__tests__/Header.test.tsx
      provides: "Component test asserting Header renders 'Dashboard' for /orders pathname"
  key_links:
    - from: src/components/Header.tsx
      to: getPageTitle default return
      via: removal of 3 legacy startsWith branches and the // Legacy routes comment
      pattern: "// Legacy routes \\(404 fallback\\)"
---

<objective>
Close INT-06: delete the 3 unreachable `startsWith` branches in `Header.getPageTitle` (legacy `/orders`, `/mill-production`, `/customers`) and the `// Legacy routes (404 fallback)` comment. Lock the new behavior with a Header component test that mocks `usePathname` as `'/orders'` and asserts the page title renders as `'Dashboard'`.

Purpose: Remove dead code left over from the Phase 26 route migration (the demo-prefixed branches above already win for `/demo/*` paths; the legacy branches are unreachable in normal navigation and only fire on direct stale links). After deletion, an unmatched path falls through to the existing default `return 'Dashboard';`. The new component test locks this behavior so the branches cannot be silently re-added.
Output: 4 lines deleted from `Header.tsx`. One new `it(...)` added to `Header.test.tsx`. Jest Header suite goes RED then GREEN as the TDD cycle completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md

<interfaces>
<!-- Existing Header.tsx structure + existing test mocks. -->

From src/components/Header.tsx (current getPageTitle body, lines 23–39):
- The function is a const declared inside the default-exported Header component.
- Lines 25–27: demo-prefixed startsWith branches (return 'Orders', 'Customers', 'Mill Production') — KEPT.
- Lines 30–31: '/' returns 'Coming Soon'; '/settings' startsWith returns 'Settings' — KEPT.
- Lines 33–36: the comment `// Legacy routes (404 fallback)` and 3 startsWith branches for `/orders`, `/mill-production`, `/customers` — TARGET FOR DELETION.
- Line 38: `return 'Dashboard';` default — KEPT (becomes the fallback for any path that isn't /, /demo/*, or /settings).

From src/components/Header.tsx (page title render, lines 101–103):
- Title is rendered as `<h1 className="text-text-primary text-sm font-bold">{title}</h1>` AND as the trailing breadcrumb span `<span className="text-text-primary">{title}</span>`. Either is queryable by `screen.getByRole('heading', { name: ... })` (the h1) or `screen.getAllByText(...)` (both elements).

From src/components/__tests__/Header.test.tsx (existing mock, lines 51–56):
- jest.mock("next/navigation", () => ({ usePathname: () => "/orders", useRouter: () => ({ push: jest.fn() }) }));
- usePathname is ALREADY mocked to "/orders" — the new test reuses this mock without modification.

From src/components/__tests__/Header.test.tsx (existing assertion shape, lines 81–84):
- `const userButton = screen.getByTestId("clerk-userbutton"); expect(loadedContent).toContainElement(userButton);`
- The file uses `@testing-library/react` `render` + `screen.getByTestId / getByRole`. No new imports needed for the new title-text assertion.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — add Header test asserting 'Dashboard' for /orders pathname</name>
  <files>src/components/__tests__/Header.test.tsx</files>
  <read_first>
    - src/components/__tests__/Header.test.tsx (the entire file; specifically the existing jest.mock("next/navigation", ...) block at lines 51–56 and the existing describe block structure)
    - src/components/Header.tsx (the `getPageTitle` function lines 23–39 and the page-title render at lines 101–103, to know which element to query)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/components/__tests__/Header.test.tsx — D-11 TDD"
  </read_first>
  <behavior>
    - Add a new `it(...)` test (inside the existing describe block) that renders `<Header />` against the existing `usePathname: () => "/orders"` mock and asserts the rendered heading text is `'Dashboard'`.
    - Running `npm test -- --testPathPattern=Header` MUST show this new test FAIL: the rendered heading is currently `'Orders'` (the legacy branch returns `'Orders'` for `/orders`). RED state proves the dead branch is still firing.
  </behavior>
  <action>
    Inside `src/components/__tests__/Header.test.tsx`, add a new `it(...)` inside the existing top-level describe block (after the last existing `it` block). Name it: `"renders 'Dashboard' title when pathname is the legacy /orders (dead branch removed)"`. Inside it: call `render(<Header />)`, then assert `expect(screen.getByRole('heading', { name: 'Dashboard' })).toBeInTheDocument()`. Do not modify the existing `jest.mock("next/navigation", ...)` block — it already returns `'/orders'` (per 29-PATTERNS.md, line 51–56). Do not add new imports beyond `screen` (already imported). Do not change any other test in the file. This task implements D-11 RED per user decision; the GREEN step is Task 2.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Header 2>&1 | grep -E "(FAIL|Dashboard|legacy /orders)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/__tests__/Header.test.tsx` contains exactly one match for the new test name: `grep -c "legacy /orders (dead branch removed)" src/components/__tests__/Header.test.tsx` returns `1`.
    - File contains the new assertion: `grep -c "getByRole('heading', { name: 'Dashboard' })" src/components/__tests__/Header.test.tsx` returns `1`.
    - Running `npm test -- --testPathPattern=Header` exits non-zero AND output contains `FAIL` for the new test name, with a diff showing `Dashboard` expected and `Orders` actually rendered.
  </acceptance_criteria>
  <done>The new Header test exists and FAILS against current Header.tsx. Commit before Task 2.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — delete the 3 legacy fallback branches from Header.tsx::getPageTitle</name>
  <files>src/components/Header.tsx</files>
  <read_first>
    - src/components/Header.tsx (the entire `getPageTitle` function, lines 23–39)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/components/Header.tsx — D-11: dead branch deletion"
  </read_first>
  <behavior>
    - After deletion, `getPageTitle('/orders')` returns `'Dashboard'` (falls through to the default).
    - After deletion, `getPageTitle('/customers')` returns `'Dashboard'`, `getPageTitle('/mill-production')` returns `'Dashboard'`.
    - The kept demo branches still return their correct titles: `getPageTitle('/demo/orders')` returns `'Orders'`, etc.
    - Running `npm test -- --testPathPattern=Header` exits zero and the new "legacy /orders (dead branch removed)" test passes.
  </behavior>
  <action>
    In `src/components/Header.tsx`, delete exactly these 4 lines (originally lines 33–36):
    - The line `  // Legacy routes (404 fallback)`
    - The line `  if (path.startsWith('/orders')) return 'Orders';`
    - The line `  if (path.startsWith('/mill-production')) return 'Production';`
    - The line `  if (path.startsWith('/customers')) return 'Customers';`
    Do not touch the demo-prefixed branches above (lines 25–27), the `/` and `/settings` branches, the `return 'Dashboard';` default, or any imports. Do not introduce a replacement fallback (a `'Page Not Found'` string would change behavior for `/sign-in` and other unmatched paths — per CONTEXT.md D-11). This task implements D-11 GREEN per user decision.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Header</automated>
  </verify>
  <acceptance_criteria>
    - Negative-existence: `grep -c "Legacy routes (404 fallback)" src/components/Header.tsx` returns `0`.
    - Negative-existence: `grep -cE "path\\.startsWith\\('/orders'\\)|path\\.startsWith\\('/customers'\\)|path\\.startsWith\\('/mill-production'\\)" src/components/Header.tsx` returns `0`.
    - Demo branches still present: `grep -c "path.startsWith('/demo/orders')" src/components/Header.tsx` returns `1`; same for `/demo/customers` and `/demo/mill-production`.
    - Default fallback still present: `grep -c "return 'Dashboard';" src/components/Header.tsx` returns `1`.
    - Running `npm test -- --testPathPattern=Header` exits 0; output contains `PASS` for the Header suite and shows the "legacy /orders (dead branch removed)" test passing.
  </acceptance_criteria>
  <done>Dead branches removed from Header.tsx; new Header test passes. Commit (atomically, or as RED/GREEN pair).</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=Header` exits 0.
- The 4 dead lines are absent from `src/components/Header.tsx` (grep gates above return 0).
- The new component test pins the post-deletion behavior.
</verification>

<success_criteria>
- INT-06 closed: 3 unreachable startsWith branches + the legacy comment are gone.
- `getPageTitle` now has 5 explicit branches (3 demo + `/` + `/settings`) and 1 default return.
- A new component test guards against accidental reintroduction of legacy fallback titles.
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-02-SUMMARY.md`.
</output>
