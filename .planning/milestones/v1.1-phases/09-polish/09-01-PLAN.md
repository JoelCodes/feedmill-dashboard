---
phase: 09-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/mill-production/page.tsx
autonomous: true
requirements: [POLSH-01, POLSH-02, POLSH-03]

must_haves:
  truths:
    - "No hardcoded hex colors remain in mill-production/page.tsx"
    - "No hardcoded text sizes (11px, 15px) remain in mill-production/page.tsx"
    - "No inline boxShadow style on ProductionCard"
    - "All status colors use design tokens from globals.css"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Status color tokens, typography tokens, text color tokens"
      contains: "--status-completed-border"
    - path: "src/app/mill-production/page.tsx"
      provides: "Token-based styling"
      min_lines: 250
  key_links:
    - from: "src/app/mill-production/page.tsx"
      to: "src/app/globals.css"
      via: "CSS variables"
      pattern: "var\\(--status-"
---

<objective>
Replace all hardcoded hex colors, text sizes, and inline styles in mill-production/page.tsx with design tokens defined in globals.css.

Purpose: Establish consistent design system usage for maintainability and future theming. Per CONTEXT.md decisions D-01 through D-11.

Output: globals.css with new tokens, mill-production/page.tsx with token-based classes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-PATTERNS.md

<interfaces>
<!-- Key patterns and tokens from globals.css and mill-production/page.tsx -->

From src/app/globals.css (existing token pattern):
```css
:root {
  --text-primary: #2d3748;
  --text-secondary: #a0aec0;
  --shadow-card: 0 3.5px 5px rgba(0, 0, 0, 0.03);
}

@theme inline {
  --color-text-primary: var(--text-primary);
  --color-success: var(--success);
}
```

From src/app/mill-production/page.tsx (values to replace):
```typescript
// STATE_COLORS - hardcoded hex values to replace with CSS variables
const STATE_COLORS: Record<ProductionState, { border: string; header: string }> = {
  Completed: { border: "#38a169", header: "#276749" },
  Mixing: { border: "#d69e2e", header: "#975a16" },
  Blocked: { border: "#e53e3e", header: "#c53030" },
  Pending: { border: "#a0aec0", header: "#4a5568" },
};

// PRODUCTION_STATE_PILL_CONFIG - countBg uses hardcoded hex with opacity
countBg: "bg-[#2f855a22]"  // Replace with bg-[var(--status-completed-bg-22)]

// ProductionCard - inline boxShadow to replace with shadow-card class
style={{ boxShadow: "0 2px 8px rgba(0, 0, 0, 0.05)" }}

// Text elements - hardcoded sizes and colors
text-[11px] font-semibold text-[#718096]   // Replace with text-card-label text-muted
text-[15px] font-bold text-[#2d3748]       // Replace with text-card-title text-primary
text-sm font-medium text-[#4a5568]         // Replace with text-sm text-medium
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current token definitions)
    - .planning/phases/09-polish/09-PATTERNS.md (exact token values)
  </read_first>
  <action>
Add new design tokens to globals.css following the existing pattern.

**In :root block, add after existing tokens (after --radius-xl line ~52):**

```css
/* Status-specific color tokens (D-01, D-02, D-03) */
--status-completed-border: #38a169;
--status-completed-header: #276749;
--status-completed-bg-22: #2f855a38;

--status-mixing-border: #d69e2e;
--status-mixing-header: #975a16;
--status-mixing-bg-22: #975a1638;

--status-blocked-border: #e53e3e;
--status-blocked-header: #c53030;
--status-blocked-bg-22: #c5303038;

--status-pending-border: #a0aec0;
--status-pending-header: #4a5568;
--status-pending-bg-22: #71809638;

/* Typography tokens (D-05, D-06) */
--text-11: 0.6875rem;
--text-15: 0.9375rem;

/* Text color tokens (D-10, D-11) */
--text-muted: #718096;
--text-medium: #4a5568;
```

**In @theme inline block, add after existing mappings (after --color-divider line ~77):**

```css
/* Status color utilities */
--color-status-completed-border: var(--status-completed-border);
--color-status-completed-header: var(--status-completed-header);
--color-status-mixing-border: var(--status-mixing-border);
--color-status-mixing-header: var(--status-mixing-header);
--color-status-blocked-border: var(--status-blocked-border);
--color-status-blocked-header: var(--status-blocked-header);
--color-status-pending-border: var(--status-pending-border);
--color-status-pending-header: var(--status-pending-header);

/* Typography utilities */
--text-card-label: var(--text-11);
--text-card-title: var(--text-15);

/* Text color utilities */
--color-text-muted: var(--text-muted);
--color-text-medium: var(--text-medium);
```

Preserve all existing tokens and their order.
  </action>
  <verify>
    <automated>cd /Users/joel/Desktop/Projects/cgm-dashboard && npm run build 2>&1 | grep -q 'Compiled successfully' && grep -q 'status-completed-border: #38a169' src/app/globals.css && echo 'PASS' || echo 'FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--status-completed-border: #38a169`
    - globals.css contains `--status-mixing-border: #d69e2e`
    - globals.css contains `--status-blocked-border: #e53e3e`
    - globals.css contains `--status-pending-border: #a0aec0`
    - globals.css contains `--status-completed-bg-22: #2f855a38`
    - globals.css contains `--text-11: 0.6875rem`
    - globals.css contains `--text-15: 0.9375rem`
    - globals.css contains `--text-muted: #718096`
    - globals.css contains `--text-medium: #4a5568`
    - @theme inline block contains `--color-status-completed-border: var(--status-completed-border)`
    - @theme inline block contains `--text-card-label: var(--text-11)`
    - @theme inline block contains `--color-text-muted: var(--text-muted)`
    - npm run build succeeds with new tokens
  </acceptance_criteria>
  <done>globals.css has 12 new :root tokens (4 status sets x 3 + 2 typography + 2 text color) and 12 new @theme inline mappings, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded values in mill-production page</name>
  <files>src/app/mill-production/page.tsx</files>
  <read_first>
    - src/app/mill-production/page.tsx (current implementation)
    - src/app/globals.css (after Task 1 tokens added)
    - .planning/phases/09-polish/09-PATTERNS.md (replacement mappings)
  </read_first>
  <action>
Replace all hardcoded hex colors, text sizes, and inline styles with design tokens.

**1. Update STATE_COLORS (lines 21-29):**

Replace:
```typescript
const STATE_COLORS: Record<
  ProductionState,
  { border: string; header: string }
> = {
  Completed: { border: "#38a169", header: "#276749" },
  Mixing: { border: "#d69e2e", header: "#975a16" },
  Blocked: { border: "#e53e3e", header: "#c53030" },
  Pending: { border: "#a0aec0", header: "#4a5568" },
};
```

With:
```typescript
const STATE_COLORS: Record<
  ProductionState,
  { border: string; header: string }
> = {
  Completed: {
    border: "var(--status-completed-border)",
    header: "var(--status-completed-header)",
  },
  Mixing: {
    border: "var(--status-mixing-border)",
    header: "var(--status-mixing-header)",
  },
  Blocked: {
    border: "var(--status-blocked-border)",
    header: "var(--status-blocked-header)",
  },
  Pending: {
    border: "var(--status-pending-border)",
    header: "var(--status-pending-header)",
  },
};
```

**2. Update PRODUCTION_STATE_PILL_CONFIG countBg values (lines 31-56):**

Replace:
- `countBg: "bg-[#2f855a22]"` with `countBg: "bg-[var(--status-completed-bg-22)]"`
- `countBg: "bg-[#975a1622]"` with `countBg: "bg-[var(--status-mixing-bg-22)]"`
- `countBg: "bg-[#c5303022]"` with `countBg: "bg-[var(--status-blocked-bg-22)]"`
- `countBg: "bg-[#71809622]"` with `countBg: "bg-[var(--status-pending-bg-22)]"`
- `text: "text-[#718096]"` (Pending) with `text: "text-muted"`

**3. Update ProductionCard component (lines 65-95):**

Replace inline boxShadow style:
```tsx
<div
  className="relative overflow-hidden rounded-r-xl bg-white"
  style={{
    boxShadow: "0 2px 8px rgba(0, 0, 0, 0.05)",
  }}
>
```

With shadow-card class (per D-07, D-08):
```tsx
<div className="relative overflow-hidden rounded-r-xl bg-white shadow-card">
```

Replace text styling:
- `text-[11px] font-semibold text-[#718096]` with `text-card-label font-semibold text-muted`
- `text-[15px] font-bold text-[#2d3748]` with `text-card-title font-bold text-primary`
- `text-sm font-medium text-[#4a5568]` with `text-sm font-medium text-medium`
- `text-xs font-medium text-[#718096]` with `text-xs font-medium text-muted`

**4. Update StateSection component (line 115):**

Replace:
```tsx
<span className="text-base font-medium text-[#718096]">
```

With:
```tsx
<span className="text-base font-medium text-muted">
```

**5. Update MillColumn component (lines 151-154):**

Replace:
```tsx
<h2 className="text-2xl font-bold text-[#2d3748]">{millLine}</h2>
<p className="mt-1 text-base font-semibold text-[#718096]">
```

With:
```tsx
<h2 className="text-2xl font-bold text-primary">{millLine}</h2>
<p className="mt-1 text-base font-semibold text-muted">
```
  </action>
  <verify>
    <automated>cd /Users/joel/Desktop/Projects/cgm-dashboard && ! grep -E 'border: "#[0-9a-fA-F]{6}"' src/app/mill-production/page.tsx && ! grep -E 'header: "#[0-9a-fA-F]{6}"' src/app/mill-production/page.tsx && grep -q 'shadow-card' src/app/mill-production/page.tsx && echo 'PASS' || echo 'FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - STATE_COLORS uses `var(--status-completed-border)` not `#38a169`
    - STATE_COLORS uses `var(--status-mixing-border)` not `#d69e2e`
    - STATE_COLORS uses `var(--status-blocked-border)` not `#e53e3e`
    - STATE_COLORS uses `var(--status-pending-border)` not `#a0aec0`
    - PRODUCTION_STATE_PILL_CONFIG countBg uses `var(--status-completed-bg-22)` not `#2f855a22`
    - Pending pill text uses `text-muted` not `text-[#718096]`
    - ProductionCard uses `shadow-card` class, no inline boxShadow style
    - ProductionCard uses `text-card-label` not `text-[11px]`
    - ProductionCard uses `text-card-title` not `text-[15px]`
    - ProductionCard uses `text-muted` not `text-[#718096]`
    - ProductionCard uses `text-primary` not `text-[#2d3748]`
    - ProductionCard uses `text-medium` not `text-[#4a5568]`
    - MillColumn h2 uses `text-primary` not `text-[#2d3748]`
    - MillColumn p uses `text-muted` not `text-[#718096]`
    - StateSection span uses `text-muted` not `text-[#718096]`
  </acceptance_criteria>
  <done>mill-production/page.tsx STATE_COLORS uses CSS variables not hardcoded hex, uses shadow-card class, uses typography tokens, uses text color tokens</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and visual consistency</name>
  <files>src/app/mill-production/page.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/mill-production/page.tsx (after Task 2 changes)
    - src/app/globals.css (after Task 1 changes)
  </read_first>
  <action>
Run build verification and confirm token application.

1. Run `npm run build` to verify TypeScript compilation succeeds
2. Run `npm run lint` to verify no ESLint errors
3. Verify CSS variable usage is correct by checking:
   - All STATE_COLORS values start with `var(--status-`
   - All countBg values contain `var(--status-`
   - No text-[#xxxxxx] patterns remain
   - No style={{ boxShadow patterns remain

Note: Visual appearance should be identical to before refactoring since the hex values in tokens match the original hardcoded values exactly.
  </action>
  <verify>
    <automated>cd /Users/joel/Desktop/Projects/cgm-dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript compilation errors
    - No ESLint errors related to the changed files
    - grep for `style={{ boxShadow` returns 0 matches in mill-production/page.tsx
    - grep for `text-\[#` returns 0 matches in mill-production/page.tsx
  </acceptance_criteria>
  <done>Build succeeds, lint passes, no hardcoded values remain, tokens properly applied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens | Design tokens are static values, no dynamic user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | CSS variables | accept | CSS variables are server-rendered, no user control over values |

This phase is purely presentational refactoring with no trust boundary changes.
</threat_model>

<verification>
## Phase Verification

After all tasks complete:

1. **Zero hardcoded values:**
   ```bash
   grep -E "#[0-9a-fA-F]{6}" src/app/mill-production/page.tsx
   # Should return no matches
   ```

2. **Token definitions exist:**
   ```bash
   grep -c "status-completed-border" src/app/globals.css
   # Should return 2 (one in :root, one in @theme inline)
   ```

3. **Build succeeds:**
   ```bash
   npm run build
   # Should complete without errors
   ```

4. **Visual parity:**
   - Run `npm run dev`
   - Navigate to /mill-production
   - Verify cards render with correct status border colors
   - Verify filter pill count badges have correct backgrounds
   - Verify text colors and sizes match original appearance
</verification>

<success_criteria>
- [ ] globals.css has 12 new status/typography/text tokens in :root
- [ ] globals.css has 12 new mappings in @theme inline
- [ ] mill-production/page.tsx has zero hardcoded hex colors
- [ ] mill-production/page.tsx uses shadow-card instead of inline boxShadow
- [ ] mill-production/page.tsx uses text-card-label and text-card-title tokens
- [ ] mill-production/page.tsx uses text-muted, text-medium, text-primary tokens
- [ ] npm run build succeeds
- [ ] Visual appearance unchanged from before refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-01-SUMMARY.md`
</output>
