---
phase: 19-documentation-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - jest.setup.ts
  - eslint.config.mjs
autonomous: true
requirements:
  - DOC-03
user_setup: []

must_haves:
  truths:
    - "jest-axe is installed and configured for accessibility testing"
    - "eslint-plugin-jsx-a11y is installed and enforcing accessibility rules"
    - "All existing tests pass after configuration changes"
  artifacts:
    - path: "package.json"
      provides: "jest-axe and eslint-plugin-jsx-a11y dependencies"
      contains: "jest-axe"
    - path: "jest.setup.ts"
      provides: "jest-axe matcher extension"
      contains: "toHaveNoViolations"
    - path: "eslint.config.mjs"
      provides: "jsx-a11y plugin configuration"
      contains: "jsxA11y"
  key_links:
    - from: "jest.setup.ts"
      to: "jest-axe"
      via: "import toHaveNoViolations"
      pattern: "import.*jest-axe"
    - from: "eslint.config.mjs"
      to: "eslint-plugin-jsx-a11y"
      via: "flat config recommended"
      pattern: "jsxA11y\\.flatConfigs\\.recommended"
---

<objective>
Set up accessibility testing infrastructure for Phase 19.

Purpose: Per D-06, the project needs both automated testing (eslint-plugin-jsx-a11y + jest-axe) and manual screen reader verification. This plan installs and configures the automated tooling that subsequent plans depend on.

Output: jest-axe and eslint-plugin-jsx-a11y installed and configured, ready for accessibility tests in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-documentation-accessibility/19-RESEARCH.md

<interfaces>
<!-- Current jest.setup.ts (1 line) -->
From jest.setup.ts:
```typescript
import '@testing-library/jest-dom'
```

<!-- Current eslint.config.mjs structure -->
From eslint.config.mjs:
```javascript
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";
import tailwindcss from "eslint-plugin-tailwindcss";
import noHardcodedValues from "./eslint-rules/no-hardcoded-values.js";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  { /* tailwindcss config */ },
  { /* custom rules config */ },
  globalIgnores([...]),
]);

export default eslintConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install accessibility testing dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/package.json
  </read_first>
  <action>
Install jest-axe@10.0.0 and eslint-plugin-jsx-a11y@6.10.2 as dev dependencies:

```bash
npm install --save-dev jest-axe@10.0.0 eslint-plugin-jsx-a11y@6.10.2
```

These versions are verified in 19-RESEARCH.md:
- jest-axe 10.0.0: Industry standard wrapper around axe-core with Jest matchers
- eslint-plugin-jsx-a11y 6.10.2: Official ESLint accessibility plugin for JSX
  </action>
  <verify>
    <automated>grep -q '"jest-axe"' package.json && grep -q '"eslint-plugin-jsx-a11y"' package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"jest-axe": "^10.0.0"` in devDependencies
    - package.json contains `"eslint-plugin-jsx-a11y": "^6.10.2"` in devDependencies
    - `npm ls jest-axe` shows 10.0.0 installed
    - `npm ls eslint-plugin-jsx-a11y` shows 6.10.2 installed
  </acceptance_criteria>
  <done>jest-axe@10.0.0 and eslint-plugin-jsx-a11y@6.10.2 installed</done>
</task>

<task type="auto">
  <name>Task 2: Extend jest.setup.ts with jest-axe matcher</name>
  <files>jest.setup.ts</files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/jest.setup.ts
  </read_first>
  <action>
Add jest-axe toHaveNoViolations matcher to jest.setup.ts. The file currently has only one line importing @testing-library/jest-dom. Add the jest-axe import and extend expect:

```typescript
import '@testing-library/jest-dom'
import { toHaveNoViolations } from 'jest-axe'

// Extend Jest expect with jest-axe matchers for accessibility testing
expect.extend(toHaveNoViolations)
```

This follows the jest-axe recommended setup pattern and makes toHaveNoViolations() available in all test files without per-file imports.
  </action>
  <verify>
    <automated>grep -q "toHaveNoViolations" /Users/joel/Desktop/Projects/cgm-dashboard/jest.setup.ts && grep -q "jest-axe" /Users/joel/Desktop/Projects/cgm-dashboard/jest.setup.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - jest.setup.ts contains `import { toHaveNoViolations } from 'jest-axe'`
    - jest.setup.ts contains `expect.extend(toHaveNoViolations)`
    - File has exactly 5 lines (import jest-dom, blank, import jest-axe, blank, extend)
  </acceptance_criteria>
  <done>jest.setup.ts extends expect with toHaveNoViolations matcher</done>
</task>

<task type="auto">
  <name>Task 3: Configure eslint-plugin-jsx-a11y in eslint.config.mjs</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/eslint.config.mjs
  </read_first>
  <action>
Add jsx-a11y plugin to eslint.config.mjs using flat config format. Insert after nextTs but before tailwindcss config:

1. Add import at top:
   ```javascript
   import jsxA11y from "eslint-plugin-jsx-a11y";
   ```

2. Add to defineConfig array (after `...nextTs`, before tailwindcss config block):
   ```javascript
   jsxA11y.flatConfigs.recommended,
   ```

The complete file should be:
```javascript
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";
import tailwindcss from "eslint-plugin-tailwindcss";
import jsxA11y from "eslint-plugin-jsx-a11y";
import noHardcodedValues from "./eslint-rules/no-hardcoded-values.js";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  jsxA11y.flatConfigs.recommended,
  {
    plugins: {
      tailwindcss,
    },
    rules: {
      "tailwindcss/classnames-order": "warn",
      "tailwindcss/no-contradicting-classname": "error",
      "tailwindcss/no-custom-classname": "off",
    },
    settings: {
      tailwindcss: {
        skipClassAttribute: false,
        callees: ["classnames", "clsx", "ctl", "cn"],
        config: null,
      },
    },
  },
  {
    plugins: {
      custom: {
        rules: {
          "no-hardcoded-values": noHardcodedValues,
        },
      },
    },
    rules: {
      "custom/no-hardcoded-values": "error",
    },
  },
  globalIgnores([
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
```
  </action>
  <verify>
    <automated>grep -q "eslint-plugin-jsx-a11y" /Users/joel/Desktop/Projects/cgm-dashboard/eslint.config.mjs && grep -q "jsxA11y.flatConfigs.recommended" /Users/joel/Desktop/Projects/cgm-dashboard/eslint.config.mjs && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.mjs contains `import jsxA11y from "eslint-plugin-jsx-a11y"`
    - eslint.config.mjs contains `jsxA11y.flatConfigs.recommended` in defineConfig array
    - `npm run lint` exits with 0 (no new errors introduced)
  </acceptance_criteria>
  <done>eslint.config.mjs includes jsx-a11y recommended rules</done>
</task>

<task type="auto">
  <name>Task 4: Verify all tests still pass</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full test suite to verify configuration changes don't break existing tests:

```bash
npm test
```

Expected: All 192+ tests pass. If any failures occur related to jest-axe setup, they would indicate a TypeScript type issue that needs addressing in jest.setup.ts.

Also run lint to verify jsx-a11y rules don't flag existing components:

```bash
npm run lint
```

Expected: Exit 0. If jsx-a11y rules flag existing accessibility issues, document them but don't fix in this plan (fixes happen in Plan 02).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` shows "Tests: X passed" with 0 failures
    - `npm run lint` exits with code 0
    - No TypeScript errors related to jest-axe types
  </acceptance_criteria>
  <done>All existing tests pass, lint passes with jsx-a11y rules active</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries — configuration changes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | — | — | — | No security implications for dev tooling configuration |
</threat_model>

<verification>
After all tasks complete:
1. `grep -c "jest-axe" package.json` returns 1
2. `grep -c "eslint-plugin-jsx-a11y" package.json` returns 1
3. `grep -c "toHaveNoViolations" jest.setup.ts` returns 1
4. `grep -c "jsxA11y" eslint.config.mjs` returns at least 2 (import + usage)
5. `npm test` passes all tests
6. `npm run lint` exits 0
</verification>

<success_criteria>
- jest-axe@10.0.0 installed and matcher extended globally
- eslint-plugin-jsx-a11y@6.10.2 installed with recommended flat config
- All 192+ existing tests continue to pass
- Lint passes with no new errors from jsx-a11y rules
</success_criteria>

<output>
After completion, create `.planning/phases/19-documentation-accessibility/19-01-SUMMARY.md`
</output>
