---
phase: 16-foundation-design-system-setup
plan: 05
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - designs/component-library.pen
  - .planning/docs/design-tokens.md
autonomous: true
requirements: [DES-01, DES-02, DES-03]

must_haves:
  truths:
    - "Component library .pen file exists as single source of truth"
    - "Token sync process is documented for future updates"
    - "Design file hierarchy is clear (library -> pages)"
  artifacts:
    - path: "designs/component-library.pen"
      provides: "Design system primitives and token definitions"
      contains: "Design System Tokens"
    - path: ".planning/docs/design-tokens.md"
      provides: "Token usage documentation and sync process"
      contains: "Token Sync Process"
  key_links:
    - from: "designs/*.pen page files"
      to: "designs/component-library.pen"
      via: "references library components"
      pattern: "library reference"
---

<objective>
Create component library design file and document token sync process.

Purpose: Establish the design source of truth that designers and developers reference, with clear documentation on how to keep CSS tokens synchronized.

Output: component-library.pen file with token definitions, design-tokens.md documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-foundation-design-system-setup/16-CONTEXT.md
@.planning/phases/16-foundation-design-system-setup/16-RESEARCH.md

<interfaces>
<!-- Existing .pen files to reference library -->

designs/
- order-dashboard.pen
- design-system.pen (existing - to be superseded by component-library.pen)
- page-layout.pen
- mill-production.pen
- customers.pen
- customer-detail.pen

<!-- Token structure from globals.css (to mirror in .pen) -->

Color tokens: --primary, --success, --warning, --error, --info, --purple
Interactive states: --*-hover, --*-active, --*-disabled
Backgrounds: --bg-page, --bg-card, --bg-sidebar
Text: --text-primary, --text-secondary, --text-muted, --text-medium
Spacing: --space-1 through --space-12
Radii: --radius-sm, --radius-md, --radius-lg, --radius-xl
Shadows: --shadow-sm, --shadow-card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create component-library.pen design file</name>
  <files>designs/component-library.pen</files>
  <read_first>
    - src/app/globals.css (token values to document)
    - designs/design-system.pen (existing design file structure if exists)
  </read_first>
  <action>
Create designs/component-library.pen with the following structure.

Note: .pen files are Pencil.dev format - essentially structured text/JSON for design tooling. Create as a text file with this content:

```pen
# Component Library - CGM Dashboard Design System
# Version: 1.0.0
# Created: 2026-05-07
# Source of truth for design tokens and component primitives

## Design System Tokens

### Color Palette

#### Primary
- primary: #4fd1c5
- primary-dark: #38b2ac
- primary-hover: #45b8ad
- primary-active: #3a9d94
- primary-disabled: #a8d5d0

#### Success
- success: #48bb78
- success-dark: #2f855a
- success-light: #c6f6d5
- success-hover: #38a169
- success-active: #2f855a
- success-disabled: #9ae6b4

#### Warning
- warning: #f59e0b
- warning-light: #fefcbf
- warning-hover: #d97706
- warning-active: #b45309
- warning-disabled: #fcd34d

#### Error
- error: #e53e3e
- error-dark: #c53030
- error-light: #fed7d7
- error-hover: #d53f3f
- error-active: #c53030
- error-disabled: #fc8181

#### Info
- info: #2b6cb0
- info-light: #bee3f8
- info-hover: #2563eb
- info-active: #1d4ed8
- info-disabled: #93c5fd

### Background Colors

#### Light Mode
- bg-page: #f8f9fa
- bg-card: #ffffff
- bg-sidebar: #ffffff

#### Dark Mode
- bg-page: #1a202c
- bg-card: #2d3748
- bg-sidebar: #2d3748

### Text Colors

#### Light Mode
- text-primary: #2d3748
- text-secondary: #a0aec0
- text-muted: #718096
- text-medium: #4a5568

#### Dark Mode
- text-primary: #e2e8f0
- text-secondary: #a0aec0
- text-muted: #a0aec0
- text-medium: #cbd5e0

### Spacing Scale

- space-1: 0.25rem (4px)
- space-2: 0.5rem (8px)
- space-3: 0.75rem (12px)
- space-4: 1rem (16px)
- space-5: 1.5rem (24px)
- space-6: 2rem (32px)
- space-8: 3rem (48px)
- space-10: 4rem (64px)
- space-12: 6rem (96px)

### Semantic Spacing

- spacing-xs: space-1 (4px)
- spacing-sm: space-2 (8px)
- spacing-md: space-4 (16px)
- spacing-lg: space-6 (32px)
- spacing-xl: space-8 (48px)

### Border Radius

- radius-sm: 6px
- radius-md: 8px
- radius-lg: 12px
- radius-xl: 15px

### Shadows

- shadow-sm: 0 3.5px 5px rgba(0, 0, 0, 0.02)
- shadow-card: 0 3.5px 5px rgba(0, 0, 0, 0.03)

## Component Primitives

### Button (Phase 17)
Variants: primary, secondary, ghost, destructive
Sizes: sm, md, lg
States: default, hover, active, disabled, loading

### Input (Phase 17)
Types: text, number, select, textarea
States: default, focus, error, disabled
Includes: label, helper text, error message

### Card (Phase 17)
Compound: Card.Header, Card.Content, Card.Footer
Variants: default, elevated

### Badge (Phase 17)
Refactored from: StatusBadge
Variants: status colors
Sizes: sm, md

### Theme Toggle (Phase 17)
Options: light, dark, system
Persists: localStorage

## Page References

This library is the source of truth. Page .pen files reference these tokens:
- order-dashboard.pen
- page-layout.pen
- mill-production.pen
- customers.pen
- customer-detail.pen

## Sync Process

1. Design changes start in this file
2. Update src/app/globals.css to match
3. Update .planning/docs/design-tokens.md
4. Run ESLint to verify token usage

---
# File: component-library.pen
# CSS Implementation: src/app/globals.css
# Documentation: .planning/docs/design-tokens.md
```
  </action>
  <verify>
    <automated>test -f designs/component-library.pen && grep -c "Design System Tokens" designs/component-library.pen && grep -c "primary: #4fd1c5" designs/component-library.pen</automated>
  </verify>
  <acceptance_criteria>
    - File exists at designs/component-library.pen
    - Contains "Design System Tokens" section
    - Contains all color tokens matching globals.css values
    - Contains spacing scale (space-1 through space-12)
    - Contains border radius values
    - Contains shadow values
    - Contains component primitive placeholders for Phase 17
    - Contains page reference list
    - Contains sync process instructions
  </acceptance_criteria>
  <done>Component library .pen file created as design source of truth (per D-05)</done>
</task>

<task type="auto">
  <name>Task 2: Create token usage documentation</name>
  <files>.planning/docs/design-tokens.md</files>
  <read_first>
    - designs/component-library.pen (from Task 1)
    - src/app/globals.css (token implementation)
    - .planning/phases/16-foundation-design-system-setup/16-RESEARCH.md (patterns)
  </read_first>
  <action>
Create .planning/docs directory if needed, then create .planning/docs/design-tokens.md:

```markdown
# Design Tokens Documentation

**Created:** 2026-05-07
**Last Updated:** 2026-05-07
**Source of Truth:** designs/component-library.pen
**Implementation:** src/app/globals.css

## Overview

This document describes the design token system for the CGM Dashboard. Tokens follow a two-tier naming pattern:

1. **Primitives** (in `:root`) - Raw values like `--primary: #4fd1c5`
2. **Semantic aliases** (in `@theme inline`) - Contextual names like `--color-primary: var(--primary)`

## Token Categories

### Colors

Use semantic color tokens for all color values:

| Token | Purpose | Example Usage |
|-------|---------|---------------|
| `--color-primary` | Primary actions, links | `bg-[var(--color-primary)]` |
| `--color-primary-hover` | Primary hover state | `hover:bg-[var(--color-primary-hover)]` |
| `--color-primary-active` | Primary active/pressed state | `active:bg-[var(--color-primary-active)]` |
| `--color-primary-disabled` | Primary disabled state | Conditionally applied |
| `--color-success` | Success states, confirmations | Status badges |
| `--color-warning` | Warning states, cautions | Alert messages |
| `--color-error` | Error states, destructive | Form validation |
| `--color-info` | Informational states | Tooltips |

### Backgrounds

| Token | Purpose | Example Usage |
|-------|---------|---------------|
| `--color-bg-page` | Page background | Applied to body |
| `--color-bg-card` | Card/panel backgrounds | Card components |

### Text

| Token | Purpose | Example Usage |
|-------|---------|---------------|
| `--color-text-primary` | Primary text | Headings, body |
| `--color-text-secondary` | Secondary text | Descriptions |
| `--color-text-muted` | Muted/subtle text | Timestamps, hints |

### Spacing

Use spacing tokens for all padding, margin, gap values:

| Token | Value | Tailwind Usage |
|-------|-------|----------------|
| `--space-1` | 0.25rem (4px) | `p-[var(--space-1)]` |
| `--space-2` | 0.5rem (8px) | `gap-[var(--space-2)]` |
| `--space-3` | 0.75rem (12px) | `m-[var(--space-3)]` |
| `--space-4` | 1rem (16px) | `p-[var(--space-4)]` |
| `--space-5` | 1.5rem (24px) | `py-[var(--space-5)]` |
| `--space-6` | 2rem (32px) | `px-[var(--space-6)]` |
| `--space-8` | 3rem (48px) | `gap-[var(--space-8)]` |
| `--space-10` | 4rem (64px) | `mt-[var(--space-10)]` |
| `--space-12` | 6rem (96px) | `mb-[var(--space-12)]` |

**Semantic spacing aliases:**

| Alias | Maps To | When to Use |
|-------|---------|-------------|
| `--spacing-xs` | space-1 | Tight spacing (icons, badges) |
| `--spacing-sm` | space-2 | Small gaps (inline elements) |
| `--spacing-md` | space-4 | Standard spacing (cards, sections) |
| `--spacing-lg` | space-6 | Large spacing (page sections) |
| `--spacing-xl` | space-8 | Extra large (hero sections) |

### Border Radius

| Token | Value | When to Use |
|-------|-------|-------------|
| `--radius-sm` | 6px | Small elements (badges, pills) |
| `--radius-md` | 8px | Standard elements (buttons, inputs) |
| `--radius-lg` | 12px | Large elements (cards, modals) |
| `--radius-xl` | 15px | Extra large (hero cards) |

### Shadows

| Token | When to Use |
|-------|-------------|
| `--shadow-sm` | Subtle elevation (buttons) |
| `--shadow-card` | Card elevation |

## Dark Mode

Dark mode is handled automatically via CSS variable overrides in the `.dark` class. Components using semantic tokens automatically adapt:

```css
/* In globals.css */
:root {
  --primary: #4fd1c5;  /* Light mode value */
}

.dark {
  --primary: #63b3ed;  /* Dark mode value */
}

/* Semantic alias references primitive */
@theme inline {
  --color-primary: var(--primary);  /* Auto-swaps based on .dark */
}
```

**Important:** Override primitives in `.dark`, not semantic aliases.

## Usage Patterns

### DO

```tsx
// Use semantic color tokens
<button className="bg-[var(--color-primary)] hover:bg-[var(--color-primary-hover)]">

// Use spacing tokens
<div className="p-[var(--space-4)] gap-[var(--space-2)]">

// Use radius tokens
<div className="rounded-[var(--radius-md)]">
```

### DON'T

```tsx
// Hardcoded hex colors (ESLint error)
<button className="bg-[#4fd1c5]">

// Hardcoded px values (ESLint error)
<div className="p-[16px]">

// Raw color names instead of tokens
<div className="bg-teal-400">  // Use --color-primary instead
```

## Token Sync Process (DES-03)

When updating design tokens:

1. **Start in Pencil.dev:** Update `designs/component-library.pen`
2. **Update CSS:** Mirror changes in `src/app/globals.css`
   - Add primitive to `:root`
   - Add dark mode override to `.dark`
   - Add semantic alias to `@theme inline`
3. **Update docs:** Add to this file if new category
4. **Verify:** Run `npm run lint` to check for violations
5. **Test:** Run `npm test` to ensure no regressions

### Adding a New Color

1. In component-library.pen, add to Color Palette section
2. In globals.css :root, add primitive: `--newcolor: #value;`
3. In globals.css .dark, add override: `--newcolor: #darkvalue;`
4. In globals.css @theme inline, add alias: `--color-newcolor: var(--newcolor);`
5. Add interactive states if needed: `--newcolor-hover`, etc.

### Adding New Spacing

1. In component-library.pen, add to Spacing Scale
2. In globals.css :root, add: `--space-N: Xrem;`
3. Optionally add semantic alias in @theme inline

## ESLint Enforcement

The `custom/no-hardcoded-values` rule prevents:

- Hex colors: `#fff`, `#abc123`, `bg-[#4fd1c5]`
- Pixel values: `w-[24px]`, `p-[16px]`

Allowed patterns:

- Token references: `bg-[var(--color-primary)]`
- Standard Tailwind: `p-4`, `text-red-500`

## File References

| File | Purpose |
|------|---------|
| `designs/component-library.pen` | Design source of truth |
| `src/app/globals.css` | CSS implementation |
| `eslint-rules/no-hardcoded-values.js` | Enforcement rule |
| `.planning/docs/design-tokens.md` | This documentation |

---

*Documentation created: 2026-05-07 for Phase 16*
```
  </action>
  <verify>
    <automated>test -f .planning/docs/design-tokens.md && grep -c "Token Sync Process" .planning/docs/design-tokens.md && grep -c "DON'T" .planning/docs/design-tokens.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/docs/design-tokens.md
    - Contains token category tables (colors, spacing, radius, shadows)
    - Contains DO/DON'T usage examples
    - Contains "Token Sync Process" section (per DES-03)
    - Contains dark mode explanation
    - Contains ESLint enforcement section
    - Contains file references table
  </acceptance_criteria>
  <done>Token usage documentation created with sync process (per DES-03)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Documentation and design files with no executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | N/A | Design files | accept | Static documentation; no security impact |
</threat_model>

<verification>
After all tasks complete:
1. `test -f designs/component-library.pen` returns 0
2. `test -f .planning/docs/design-tokens.md` returns 0
3. `grep "Token Sync Process" .planning/docs/design-tokens.md` finds section
4. Token values in component-library.pen match globals.css values
</verification>

<success_criteria>
- component-library.pen created with all token definitions (per D-05)
- Token values in .pen file match CSS implementation exactly
- Component primitives section outlines Phase 17 work
- Page reference section documents hierarchy (per D-06)
- Sync process documented with step-by-step instructions (per DES-03)
- Usage documentation includes DO/DON'T examples
- Dark mode behavior explained
- ESLint enforcement referenced
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-design-system-setup/16-05-SUMMARY.md`
</output>
