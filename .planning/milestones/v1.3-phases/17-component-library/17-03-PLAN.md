---
phase: 17-component-library
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/components/ui/Input.tsx
  - src/components/ui/Input.test.tsx
  - src/components/ui/Select.tsx
  - src/components/ui/Select.test.tsx
  - src/components/ui/Textarea.tsx
  - src/components/ui/Textarea.test.tsx
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "Input shows error state with red border and error icon when error prop provided"
    - "Input has aria-invalid=true when error present"
    - "Input links error message via aria-describedby"
    - "Select renders options correctly with chevron icon"
    - "Textarea supports vertical resize only"
    - "All inputs show focus ring on focus"
  artifacts:
    - path: "src/components/ui/Input.tsx"
      provides: "Text/number input with validation states and ARIA"
      exports: ["default"]
    - path: "src/components/ui/Select.tsx"
      provides: "Native select wrapper with validation states"
      exports: ["default"]
    - path: "src/components/ui/Textarea.tsx"
      provides: "Multi-line input with validation states"
      exports: ["default"]
    - path: "src/components/ui/Input.test.tsx"
      provides: "Input test coverage"
    - path: "src/components/ui/Select.test.tsx"
      provides: "Select test coverage"
    - path: "src/components/ui/Textarea.test.tsx"
      provides: "Textarea test coverage"
  key_links:
    - from: "src/components/ui/Input.tsx"
      to: "lucide-react"
      via: "AlertCircle icon import"
      pattern: "import.*AlertCircle.*from.*lucide-react"
---

<objective>
Create Input, Select, and Textarea components with validation states and accessibility attributes. All components follow the same visual pattern with error states showing red border plus error icon (D-07, D-08).

Purpose: Provide form input primitives with proper validation UX and accessibility.
Output: Three input components with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-component-library/17-RESEARCH.md
@.planning/phases/17-component-library/17-UI-SPEC.md
@.planning/phases/17-component-library/17-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Token references from globals.css:
- Error: `--error`
- Primary: `--primary`
- Text: `--text-primary`, `--text-secondary`
- Background: `--bg-card`, `--bg-page`
- Border: `--divider`, `--radius-md`

ARIA pattern for validation (from 17-RESEARCH.md):
```typescript
aria-invalid={!!error}
aria-describedby={error ? errorId : helperText ? helperId : undefined}
aria-required={props.required}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Input component with validation states and tests</name>
  <files>src/components/ui/Input.tsx, src/components/ui/Input.test.tsx</files>
  <read_first>
    - src/lib/utils.ts (cn utility)
    - src/app/globals.css (token values)
    - .planning/phases/17-component-library/17-UI-SPEC.md (Input specifications section 3.2)
    - .planning/phases/17-component-library/17-RESEARCH.md (ARIA validation pattern)
  </read_first>
  <behavior>
    - Test 1: Input renders with default styling (border-[var(--divider)])
    - Test 2: Input shows error border when error prop provided (border-[var(--error)])
    - Test 3: Input shows AlertCircle icon when error prop provided
    - Test 4: Input has aria-invalid="true" when error prop provided
    - Test 5: Input has aria-describedby linking to error message
    - Test 6: Input renders label when label prop provided
    - Test 7: Input renders helper text when helperText prop provided
    - Test 8: Error message replaces helper text when both provided
    - Test 9: Input shows focus ring on focus
    - Test 10: Input is disabled when disabled prop is true
  </behavior>
  <action>
1. Create test file first at `src/components/ui/Input.test.tsx`:
   - Import render, screen from @testing-library/react
   - Import userEvent from @testing-library/user-event
   - Write failing tests for all 10 behaviors

2. Create Input component at `src/components/ui/Input.tsx`:
   - Import { cn } from "@/lib/utils"
   - Import { AlertCircle } from "lucide-react"

3. Define InputProps interface:
   ```typescript
   interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
     label?: string;
     helperText?: string;
     error?: string;
   }
   ```

4. Implement Input function component:
   - Generate unique IDs for accessibility: inputId, helperId, errorId
   - Return wrapper div with flex flex-col gap-2
   - Render label if provided:
     - htmlFor={inputId}
     - className="text-sm font-semibold text-[var(--text-primary)]"
   - Render input container div with position relative:
     - input element with:
       - id={inputId}
       - className with conditional error state per D-07, D-08:
         - Base: "w-full h-10 px-4 rounded-[var(--radius-md)] border text-base bg-[var(--bg-card)] text-[var(--text-primary)] placeholder:text-[var(--text-secondary)]"
         - Focus: "focus:outline-none focus:ring-2 focus:ring-offset-0"
         - Error: "border-[var(--error)] border-2 focus:ring-[var(--error)] focus:ring-opacity-20 pr-10"
         - Normal: "border-[var(--divider)] focus:border-[var(--primary)] focus:ring-[var(--primary)] focus:ring-opacity-20"
         - Disabled: "disabled:bg-[var(--bg-page)] disabled:text-[var(--text-secondary)] disabled:cursor-not-allowed"
       - aria-invalid={!!error}
       - aria-describedby={error ? errorId : helperText ? helperId : undefined}
       - aria-required={props.required}
     - AlertCircle icon when error:
       - className="absolute right-3 top-1/2 -translate-y-1/2 h-4 w-4 text-[var(--error)]"
       - aria-hidden="true"
   - Render error message or helper text:
     - Error: id={errorId}, role="alert", aria-live="polite", text-xs text-[var(--error)]
     - Helper: id={helperId}, text-xs text-[var(--text-secondary)]

5. Export: export default Input
  </action>
  <verify>
    <automated>npm test -- Input.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "aria-invalid" src/components/ui/Input.tsx returns >= 1
    - grep -c "aria-describedby" src/components/ui/Input.tsx returns >= 1
    - grep -c "AlertCircle" src/components/ui/Input.tsx returns >= 1
    - grep -c 'role="alert"' src/components/ui/Input.tsx returns >= 1
    - grep -c "border-\[var(--error)\]" src/components/ui/Input.tsx returns >= 1
    - npm test -- Input.test.tsx passes with 0 failures
  </acceptance_criteria>
  <done>Input component shows error state with red border + icon per D-07, has proper ARIA attributes, all 10 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Select component with validation states and tests</name>
  <files>src/components/ui/Select.tsx, src/components/ui/Select.test.tsx</files>
  <read_first>
    - src/components/ui/Input.tsx (pattern from Task 1)
    - src/app/globals.css (token values)
    - .planning/phases/17-component-library/17-UI-SPEC.md (Select specifications section 3.3)
  </read_first>
  <behavior>
    - Test 1: Select renders options from options prop
    - Test 2: Select shows ChevronDown icon
    - Test 3: Select shows error border when error prop provided
    - Test 4: Select has aria-invalid="true" when error prop provided
    - Test 5: Select renders label when label prop provided
    - Test 6: Select renders error message when error prop provided
    - Test 7: Select is disabled when disabled prop is true
  </behavior>
  <action>
1. Create test file at `src/components/ui/Select.test.tsx`
   - Write failing tests for all 7 behaviors

2. Create Select component at `src/components/ui/Select.tsx`:
   - Import { cn } from "@/lib/utils"
   - Import { ChevronDown, AlertCircle } from "lucide-react"

3. Define SelectProps interface:
   ```typescript
   interface SelectProps extends React.SelectHTMLAttributes<HTMLSelectElement> {
     label?: string;
     helperText?: string;
     error?: string;
     options: Array<{ value: string; label: string }>;
   }
   ```

4. Implement Select component following Input pattern:
   - Same ID generation for accessibility
   - Same label, error, helper text pattern
   - Select element instead of input:
     - className similar to Input but with cursor-pointer and pr-10 for chevron
     - Map over options to render option elements
   - ChevronDown icon positioned right (instead of AlertCircle for normal state)
   - AlertCircle icon when error (same as Input)
   - Apply same ARIA attributes as Input

5. Export: export default Select
  </action>
  <verify>
    <automated>npm test -- Select.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "options.map" src/components/ui/Select.tsx returns >= 1
    - grep -c "ChevronDown" src/components/ui/Select.tsx returns >= 1
    - grep -c "aria-invalid" src/components/ui/Select.tsx returns >= 1
    - npm test -- Select.test.tsx passes with 0 failures
  </acceptance_criteria>
  <done>Select component renders options, shows chevron icon, has validation states, all 7 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Textarea component with validation states and tests</name>
  <files>src/components/ui/Textarea.tsx, src/components/ui/Textarea.test.tsx</files>
  <read_first>
    - src/components/ui/Input.tsx (pattern from Task 1)
    - src/app/globals.css (token values)
    - .planning/phases/17-component-library/17-UI-SPEC.md (Textarea specifications section 3.4)
  </read_first>
  <behavior>
    - Test 1: Textarea renders with vertical resize only (resize-y)
    - Test 2: Textarea has min-height of 96px
    - Test 3: Textarea shows error border when error prop provided
    - Test 4: Textarea has aria-invalid="true" when error prop provided
    - Test 5: Textarea renders label when label prop provided
    - Test 6: Textarea renders error message when error prop provided
    - Test 7: Textarea is disabled when disabled prop is true
  </behavior>
  <action>
1. Create test file at `src/components/ui/Textarea.test.tsx`
   - Write failing tests for all 7 behaviors

2. Create Textarea component at `src/components/ui/Textarea.tsx`:
   - Import { cn } from "@/lib/utils"
   - Import { AlertCircle } from "lucide-react"

3. Define TextareaProps interface:
   ```typescript
   interface TextareaProps extends React.TextareaHTMLAttributes<HTMLTextAreaElement> {
     label?: string;
     helperText?: string;
     error?: string;
   }
   ```

4. Implement Textarea component following Input pattern:
   - Same ID generation for accessibility
   - Same label, error, helper text pattern
   - Textarea element instead of input:
     - Add "min-h-[96px] resize-y leading-6" to className
     - Error icon positioned top-right at "right-3 top-3" (not centered)
     - Remove h-10 (textarea has variable height)
   - Apply same ARIA attributes as Input

5. Export: export default Textarea
  </action>
  <verify>
    <automated>npm test -- Textarea.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "resize-y" src/components/ui/Textarea.tsx returns >= 1
    - grep -c "min-h-\[96px\]" src/components/ui/Textarea.tsx returns >= 1
    - grep -c "aria-invalid" src/components/ui/Textarea.tsx returns >= 1
    - npm test -- Textarea.test.tsx passes with 0 failures
  </acceptance_criteria>
  <done>Textarea component has vertical resize only, min-height 96px, validation states, all 7 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input | Form values from user typing |
| Error messages | Strings passed as props from parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | Form values | accept | Components are presentational only, validation happens server-side |
| T-17-06 | Spoofing | aria-invalid | accept | ARIA attributes are UI feedback, not security controls |
</threat_model>

<verification>
- All Input tests pass (10)
- All Select tests pass (7)
- All Textarea tests pass (7)
- Error states show red border + AlertCircle icon
- ARIA attributes correctly applied
- Focus rings visible on focus
</verification>

<success_criteria>
- Input component exists at src/components/ui/Input.tsx
- Select component exists at src/components/ui/Select.tsx
- Textarea component exists at src/components/ui/Textarea.tsx
- All test files exist and pass
- Error state follows D-07, D-08 (red border + icon)
- ARIA validation pattern implemented
</success_criteria>

<output>
After completion, create `.planning/phases/17-component-library/17-03-SUMMARY.md`
</output>
