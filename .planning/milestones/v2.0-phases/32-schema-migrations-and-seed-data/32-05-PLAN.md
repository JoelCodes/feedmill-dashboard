---
phase: 32-schema-migrations-and-seed-data
plan: 05
type: tdd
wave: 3
depends_on: [02]
files_modified:
  - scripts/export-seed.ts
  - src/db/seed-data.json
  - src/db/__tests__/seed-data.test.ts
autonomous: true
requirements:
  - DATA-07
tags: [seed, json-export, transform, tdd]

must_haves:
  truths:
    - "`scripts/export-seed.ts` is a checked-in, re-runnable script that reads `mockOrders` from `src/services/millProduction.ts` and writes `src/db/seed-data.json`"
    - "The transform converts camelCase mock fields → snake_case DB column names, casts `weight_lbs` to a string (Drizzle numeric requirement), and injects `created_by: 'system-seed'` (D-19) and `version: 1` (D-11) on every row"
    - "`src/db/seed-data.json` exists, contains exactly 33 rows, and every row has 11 fields (order_number, customer, product, weight_lbs, delivery_time, state, mill_line, texture_type, line_code, created_by, version)"
    - "Every row's `created_by` is exactly `'system-seed'` (D-19)"
    - "Every row's `state` is one of {Pending, Mixing, Completed, Blocked}; every row's `mill_line` is one of {Premix, Excel, CGM}"
    - "`src/db/__tests__/seed-data.test.ts` asserts row count = 33, required fields present, enum values valid, all created_by = 'system-seed' — runs in <5s"
  artifacts:
    - path: "scripts/export-seed.ts"
      provides: "One-shot JSON exporter — re-runnable when the demo mock evolves"
      contains: "import { mockOrders } from"
    - path: "src/db/seed-data.json"
      provides: "Static 33-row JSON snapshot consumed by seed.ts in Plan 06"
      contains: "system-seed"
    - path: "src/db/__tests__/seed-data.test.ts"
      provides: "Shape assertions for the JSON snapshot (row count, required fields, enum validity, sentinel created_by)"
  key_links:
    - from: "scripts/export-seed.ts"
      to: "src/services/millProduction.ts"
      via: "import { mockOrders } from '../src/services/millProduction'"
      pattern: "mockOrders\\.map"
    - from: "src/db/seed.ts (Plan 06)"
      to: "src/db/seed-data.json"
      via: "JSON import via tsconfig resolveJsonModule"
      pattern: "import seedData from"
---

<objective>
Build the seed JSON exporter — a one-shot TypeScript script that reads the 33-order mock array from `src/services/millProduction.ts` (made exportable in Plan 02), transforms camelCase fields to snake_case DB columns, and writes `src/db/seed-data.json`. This is a `type: tdd` plan because the transform has a deterministic input/output contract: given the 33-row mock as input, the output JSON shape is fully predictable (11 fields per row, sentinel values for `created_by` + `version`, `null` for missing optional fields). RED→GREEN→REFACTOR cycle applies.

Purpose: Satisfies the BUILD half of DATA-07 (seed produces 33-row baseline). The RUNTIME half (loading JSON into the DB) is Plan 06. Splitting BUILD and RUNTIME lets the transform be unit-tested in isolation (no DB connection) and lets Plan 06 consume a stable JSON artifact that has already been validated.
Output: One executable script, one JSON file, one test file. NO modifications to dependencies or DB state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-schema-migrations-and-seed-data/32-CONTEXT.md
@.planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md
@.planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md
@.planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md
@src/services/millProduction.ts

<interfaces>
<!-- Concrete shapes the transform must produce. Full skeletons in 32-RESEARCH.md §4 "scripts/export-seed.ts" (lines 394-434) and 32-PATTERNS.md §"src/db/seed-data.json" (lines 204-242). -->

Input contract (from mockOrders in src/services/millProduction.ts after Plan 02):
  - 33 DemoOrder objects with camelCase fields: id, orderNumber, customer, product, weightLbs (number), deliveryTime, state, millLine, textureType? (optional string), lineCode? (optional string)

Output contract (per row in src/db/seed-data.json):
  - 11 snake_case fields:
    - order_number: string (verbatim from orderNumber, e.g. "ORD-255154")
    - customer: string (verbatim)
    - product: string (verbatim)
    - weight_lbs: string (cast from number; Drizzle numeric inserts expect string)
    - delivery_time: string (verbatim from deliveryTime)
    - state: string (one of Pending|Mixing|Completed|Blocked)
    - mill_line: string (one of Premix|Excel|CGM)
    - texture_type: string | null (null when source field is undefined)
    - line_code: string | null (null when source field is undefined)
    - created_by: literal "system-seed" (D-19)
    - version: literal 1 (D-11)
  - id is DROPPED — DB generates uuid via defaultRandom()
  - created_at / updated_at NOT in JSON — DB defaultNow() handles these
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD RED — write seed-data.test.ts asserting JSON shape contract</name>
  <files>src/db/__tests__/seed-data.test.ts</files>
  <read_first>
    .planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md,
    src/services/bins.test.ts
  </read_first>
  <behavior>
    - Test file uses `import seedData from '../seed-data.json'` (requires tsconfig.json `resolveJsonModule: true` — verified present per CONTEXT.md §"Build-time risks").
    - Initially RED: `Cannot find module '../seed-data.json'` because Task 2 has not yet generated the JSON.
    - Assertions: (1) `seedData` is an array, (2) `seedData.length === 33`, (3) every row has the 11 required fields listed in <interfaces>, (4) every row's `created_by === 'system-seed'`, (5) every row's `state` is in the productionStateEnum value set, (6) every row's `mill_line` is in the millLineEnum value set, (7) every row's `weight_lbs` is a string (not a number — Drizzle numeric requirement), (8) every row's `version === 1`, (9) every row's `texture_type` is either a string or `null` (never `undefined` — JSON round-trip), (10) every row's `line_code` is either a string or `null`.
    - One `describe('src/db/seed-data.json shape', ...)` block; one `it()` per assertion category.
  </behavior>
  <action>
    Write `src/db/__tests__/seed-data.test.ts` following the test analog in 32-PATTERNS.md §"src/db/__tests__/seed-data.test.ts" (lines 437-497). Use the exact `REQUIRED_FIELDS` array from the pattern: `['order_number', 'customer', 'product', 'weight_lbs', 'delivery_time', 'state', 'mill_line', 'created_by']`. Add additional `it()` blocks for the `version === 1`, `weight_lbs typeof === 'string'`, and `texture_type / line_code` (string-or-null) assertions noted in <behavior>. Confirm the test is RED by running the verify command — expect `Cannot find module '../seed-data.json'`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="src/db/__tests__/seed-data" --watchAll=false 2>&amp;1 | grep -qE "FAIL|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/db/__tests__/seed-data.test.ts contains `import seedData from '../seed-data.json'`
    - File exists: src/db/__tests__/seed-data.test.ts asserts `seedData.length` equals `33`
    - File exists: src/db/__tests__/seed-data.test.ts asserts `created_by === 'system-seed'` for every row
    - File exists: src/db/__tests__/seed-data.test.ts asserts `state` values are in `['Pending', 'Mixing', 'Completed', 'Blocked']`
    - File exists: src/db/__tests__/seed-data.test.ts asserts `mill_line` values are in `['Premix', 'Excel', 'CGM']`
    - File exists: src/db/__tests__/seed-data.test.ts asserts `typeof weight_lbs === 'string'` for every row
    - File exists: src/db/__tests__/seed-data.test.ts asserts `version === 1` for every row
    - Test: `npm test -- --testPathPattern="src/db/__tests__/seed-data" --watchAll=false` exits non-zero (RED — JSON file does not yet exist)
    - TDD commit: `test(32-05): add failing seed-data.json shape contract`
  </acceptance_criteria>
  <done>
    RED gate confirmed; test file committed with failing test for the JSON contract.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD GREEN — write scripts/export-seed.ts + run it to produce seed-data.json</name>
  <files>
    scripts/export-seed.ts,
    src/db/seed-data.json
  </files>
  <read_first>
    .planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md,
    src/services/millProduction.ts,
    src/db/__tests__/seed-data.test.ts
  </read_first>
  <behavior>
    - After this task, the RED test from Task 1 turns GREEN.
    - `scripts/export-seed.ts` is a pure Node script (no DB connection, no dotenv): import `mockOrders` from `../src/services/millProduction`, map each row to the snake_case shape per <interfaces>, write JSON to `src/db/seed-data.json` via `fs.writeFileSync` with 2-space indent.
    - The script is re-runnable: running it twice produces byte-identical output (the transform is deterministic — same input → same output).
    - Use `String(o.weightLbs)` to cast number to string (Drizzle numeric requirement — 32-RESEARCH.md Pitfall 2 + §4 line 414).
    - Use `o.textureType ?? null` and `o.lineCode ?? null` to convert optional `undefined` to JSON `null`.
    - Hardcode `created_by: 'system-seed'` (D-19) and `version: 1` (D-11) on every row.
    - DROP the source `id` field — DB generates UUID via `defaultRandom()`.
  </behavior>
  <action>
    Write `scripts/export-seed.ts` per the skeleton in 32-RESEARCH.md §4 "scripts/export-seed.ts" (lines 398-428) and 32-PATTERNS.md §"scripts/export-seed.ts" (lines 308-340). Imports: `writeFileSync` from 'fs', `path` from 'path', `mockOrders` from '../src/services/millProduction' (the named export added by Plan 02 Task 2). Use `path.resolve(__dirname, '../src/db/seed-data.json')` for the output path. After writing the script, run `npx tsx scripts/export-seed.ts` from the repo root — this requires tsx to be installed. Note: `tsx` is installed by Plan 06 Task 1. To break the dependency cycle, this plan can EITHER (a) run the script via `npx --yes tsx@4.21.0 scripts/export-seed.ts` (uses npm's on-demand fetch — no devDep install required), OR (b) one-time hand-translate (acceptable per CONTEXT.md "Claude's Discretion" line: "Whether the JSON export ... is generated by a checked-in `scripts/export-seed.ts` script ... or as a one-time hand transformation"). Prefer option (a) — the script remains checked-in and re-runnable, and `npx --yes tsx@VERSION` is a documented stable invocation that doesn't pollute devDependencies. Commit both the script AND the resulting `src/db/seed-data.json`.
  </action>
  <verify>
    <automated>test -f src/db/seed-data.json &amp;&amp; npm test -- --testPathPattern="src/db/__tests__/seed-data" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File exists: scripts/export-seed.ts contains `import { mockOrders } from '../src/services/millProduction'`
    - File exists: scripts/export-seed.ts contains `mockOrders.map`
    - File exists: scripts/export-seed.ts contains `created_by: 'system-seed'` AND `version: 1`
    - File exists: scripts/export-seed.ts contains `String(o.weightLbs)` (numeric column requires string)
    - File exists: scripts/export-seed.ts contains `o.textureType ?? null` AND `o.lineCode ?? null` (nullable handling)
    - File exists: src/db/seed-data.json is a valid JSON array
    - File exists: src/db/seed-data.json has exactly 33 entries (`jq 'length' src/db/seed-data.json` returns 33)
    - File exists: src/db/seed-data.json every entry contains `"created_by": "system-seed"` (`jq '[.[] | select(.created_by != "system-seed")] | length' src/db/seed-data.json` returns 0)
    - Test: `npm test -- --testPathPattern="src/db/__tests__/seed-data" --watchAll=false` exits 0 (GREEN — all shape assertions pass)
    - TDD commit: `feat(32-05): implement seed JSON exporter — produces 33-row baseline`
    - Re-runnable: running `npx --yes tsx@4.21.0 scripts/export-seed.ts` a second time produces the same JSON content (`git diff src/db/seed-data.json` is empty)
  </acceptance_criteria>
  <done>
    GREEN gate confirmed; script + JSON committed; test passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mockOrders` (trusted, hand-coded data in src/services/millProduction.ts) → transform → src/db/seed-data.json | Single-direction data flow, all sources are trusted and reviewed. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | src/db/seed-data.json — JSON could be hand-edited post-export to inject malicious data | accept | JSON is committed to git and reviewed in PR. Plan 06's seed runtime uses parameterized Drizzle inserts (no SQL string concat) so even if data were tampered, no SQL injection vector exists. The transform script is deterministic — repeated runs detect tampering by diff. |
| T-32-09 | Information Disclosure | scripts/export-seed.ts accidentally connects to a real production DB to fetch "real" mock data | accept | The script is pure data transformation — it does NOT import @neondatabase/serverless, does NOT load dotenv, does NOT read DATABASE_URL. Verified by acceptance grep (no `neon(`, no `process.env.DATABASE_URL`). |
</threat_model>

<verification>
- `npm test -- --testPathPattern="src/db/__tests__/seed-data" --watchAll=false` exits 0
- `jq 'length' src/db/seed-data.json` returns 33
- `jq '[.[] | .state] | unique | sort' src/db/seed-data.json` returns at most `["Blocked", "Completed", "Mixing", "Pending"]` (subset of the productionStateEnum)
- `jq '[.[] | .mill_line] | unique | sort' src/db/seed-data.json` returns `["CGM", "Excel", "Premix"]` (all three mill lines present; expected distribution: 11 each per 32-PATTERNS.md line 242)
- `! grep -q "neon(" scripts/export-seed.ts` — no DB connection in the export script (acceptance: pure data transform)
</verification>

<success_criteria>
- BUILD half of DATA-07 satisfied: 33-row JSON snapshot exists, validated, committed
- Transform is deterministic and re-runnable
- Seed JSON test is GREEN
- No DB connection in the export script (pure transform — verified by grep)
</success_criteria>

<output>
After completion, create `.planning/phases/32-schema-migrations-and-seed-data/32-05-SUMMARY.md` per the standard template.
</output>
