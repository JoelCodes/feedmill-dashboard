---
phase: 33-server-actions-queries-and-bulk-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
autonomous: true
requirements:
  - IMPORT-07
tags:
  - dependencies
  - config
  - next-config

must_haves:
  truths:
    - "`read-excel-file@9.0.9` is listed under `dependencies` in `package.json` (not devDependencies) and is resolvable from `node_modules/read-excel-file/node`."
    - "`zod` is listed under `dependencies` in `package.json` at `^4.3.6` (lock the existing transitive install so it cannot be hoisted away)."
    - "Server actions reject any upload whose request body exceeds 2 MB with an HTTP 413 BEFORE entering action code — enforced by Next.js via `experimental.serverActions.bodySizeLimit: '2mb'` in `next.config.ts`."
    - "`npx tsc --noEmit` passes after the config edit (NextConfig type still satisfied)."
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency declarations for read-excel-file and zod."
      contains: "\"read-excel-file\": \"9.0.9\""
    - path: "next.config.ts"
      provides: "Next.js 16 server-actions body-size limit (defense layer 3)."
      contains: "bodySizeLimit: '2mb'"
  key_links:
    - from: "next.config.ts"
      to: "Next.js framework runtime"
      via: "experimental.serverActions.bodySizeLimit"
      pattern: "experimental:\\s*\\{[^}]*serverActions:\\s*\\{[^}]*bodySizeLimit:\\s*['\"]2mb['\"]"
    - from: "package.json#dependencies.read-excel-file"
      to: "src/actions/import.ts (plan 33-05/33-06 consumers)"
      via: "import readXlsxFile from 'read-excel-file/node'"
      pattern: "\"read-excel-file\""
---

<objective>
Land the two foundational prerequisites that every later Phase 33 plan depends on:

1. The `read-excel-file@9.0.9` npm package is installed (it is currently absent from `dependencies` per `package.json` lines 19-31), and `zod` is explicitly pinned in `dependencies` (it is present as a transitive dependency at 4.3.6 but must not rely on hoisting).
2. `next.config.ts` declares `experimental.serverActions.bodySizeLimit: '2mb'` per IMPORT-07. RESEARCH.md §5 (line 399-431) confirms this is the correct nested key in Next.js 16.1.6 — top-level `serverActions:` has no effect.

Purpose: Wave 2 plans (33-05 and 33-06) cannot start XLSX work without `read-excel-file` installed. Wave 1 query/test plans need `zod` explicitly declared so that `npm ci` after a `node_modules` reset cannot lose it. The body-size limit is the third defense layer (D-09) — required by IMPORT-07 wording in REQUIREMENTS.md line 62.

Output: Updated `package.json` (and lock file), updated `next.config.ts`. No new source files. No tests required (pure config / dep manifest — Wave 0 of VALIDATION.md does not list test scaffolds for this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-CONTEXT.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-RESEARCH.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-PATTERNS.md
@next.config.ts
@package.json

<interfaces>
<!-- NextConfig shape (extracted from node_modules/next/dist/server/config-shared.d.ts per RESEARCH.md §5): -->

NextConfig.experimental?: ExperimentalConfig

ExperimentalConfig {
  serverActions?: {
    bodySizeLimit?: SizeLimit;   // SizeLimit = number | `${number}${'b'|'kb'|'mb'|'gb'}`
    allowedOrigins?: string[];
  };
  // ... other fields not used here
}

<!-- Banned: TOP-LEVEL `serverActions` is not a NextConfig key. Putting it at the top level passes the loose TS check but has no runtime effect (Pitfall 4). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add read-excel-file@9.0.9 and zod to package.json dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies block at lines 19-30 — confirm absence of read-excel-file and zod)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-RESEARCH.md §Standard Stack table (Standard Stack: `read-excel-file` 9.0.9 NOT INSTALLED; `zod` 4.3.6 INSTALLED transitively)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-PATTERNS.md §package.json section (lines 646-661)
  </read_first>
  <action>
    Run `npm install read-excel-file@9.0.9 zod@^4.3.6` from the repo root. This will pin `read-excel-file` exactly at 9.0.9 (per STATE.md "v2.0 implementation notes" lock + RESEARCH.md §Environment Availability) and add `zod` to the explicit `dependencies` block (do NOT rely on the existing transitive install at 4.3.6 — Pitfall: a future `npm dedupe` or unrelated Clerk version bump could hoist it away).

    Use the exact specifiers `read-excel-file@9.0.9` (pinned, no caret — CVE-conscious + STATE.md mandate) and `zod@^4.3.6` (caret — minor compatibility per RESEARCH.md §Standard Stack note "Zod v4.3.6 and v4.4.3 share the same classic API surface").

    After the install completes, do not run `npm audit fix` and do not modify other dependencies. Run `git diff package.json package-lock.json` and confirm the only change to `dependencies` is the addition of these two keys.

    Per RESEARCH.md §Pitfall 3, `read-excel-file` exposes four subpath exports (`/node`, `/browser`, `/universal`, `/web-worker`). The subpath used by Phase 33 is `read-excel-file/node`. Confirm it resolves via `node -e "console.log(require.resolve('read-excel-file/node'))"` after install.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies['read-excel-file']!=='9.0.9') process.exit(1); if(!p.dependencies['zod']) process.exit(1); console.log('ok');" && node -e "require.resolve('read-excel-file/node'); require.resolve('zod')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies['read-excel-file']` strictly equals `"9.0.9"` (no caret, no tilde).
    - `package.json` `dependencies['zod']` exists and matches `^4\\.\\d+\\.\\d+`.
    - `node -e "require.resolve('read-excel-file/node')"` exits 0.
    - `node -e "require.resolve('zod')"` exits 0 and resolves to the v4.x install in `node_modules/zod`.
    - No other entries in `dependencies` or `devDependencies` are altered (compared to git HEAD).
    - `package-lock.json` is regenerated and committed alongside `package.json`.
  </acceptance_criteria>
  <done>Both packages are listed in `package.json` `dependencies`, lockfile is updated, and both modules resolve at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Add experimental.serverActions.bodySizeLimit to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current 6-line file — comment placeholder only)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-RESEARCH.md §5 (lines 399-431 — the verified config-key shape from node_modules/next/dist/server/config-shared.d.ts)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-PATTERNS.md §next.config.ts (lines 612-642 — before/after diff)
    - .planning/REQUIREMENTS.md line 62 (IMPORT-07 wording)
  </read_first>
  <action>
    Edit `next.config.ts` so that `nextConfig` contains the literal property path `experimental.serverActions.bodySizeLimit: '2mb'`. Replace the `/* config options here */` placeholder; do NOT add other config keys (no `proxyClientMaxBodySize`, no `images`, no `eslint` overrides — strictly the bodySizeLimit per D-09 + IMPORT-07).

    The exact NextConfig key path (verified in `node_modules/next/dist/server/config-shared.d.ts` line ~532 per RESEARCH.md §5):

    `nextConfig.experimental.serverActions.bodySizeLimit = '2mb'`

    Do NOT use a top-level `nextConfig.serverActions` key (Pitfall 4 — TypeScript will not error due to loose object types but the setting has no runtime effect). Do NOT add an inline `/* @type-suppress */` or `// @ts-expect-error` comment — the typed shape resolves cleanly.

    Preserve the existing `import type { NextConfig } from "next";` and `export default nextConfig;` lines. Maintain the file's double-quote string style (current file uses `"next"` in the import — match it; use `'2mb'` single-quoted for the value to match project convention seen elsewhere — single quotes are acceptable inside object literals per repo style).
  </action>
  <verify>
    <automated>grep -E "experimental:\s*\{" next.config.ts && grep -E "serverActions:\s*\{" next.config.ts && grep -E "bodySizeLimit:\s*['\"]2mb['\"]" next.config.ts && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v '^$' | { ! grep -E 'next\.config\.ts'; }</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains the literal string `bodySizeLimit: '2mb'` (single quotes).
    - The `experimental:` key wraps `serverActions:` which wraps `bodySizeLimit`. Verified by grep: `grep -Pzo "experimental:\\s*\\{[^}]*serverActions:\\s*\\{[^}]*bodySizeLimit" next.config.ts` exits 0.
    - `next.config.ts` does NOT have a top-level `serverActions:` key (sibling to `experimental:`). Verified by: `grep -v '^#' next.config.ts | grep -cE '^\\s*serverActions:' | grep -E '^0$' >/dev/null`.
    - `npx tsc --noEmit` reports zero errors originating from `next.config.ts`.
    - File contains exactly one `nextConfig` const declaration and one `export default nextConfig;` line.
  </acceptance_criteria>
  <done>`next.config.ts` declares the 2 MB body-size limit under the verified `experimental.serverActions.bodySizeLimit` key path and the project type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js server | Operator uploads `.xlsx` payload; size validated at three layers (client, server action, framework). This plan installs the framework layer (`bodySizeLimit`). |
| node_modules → application code | A compromised npm dependency could execute on the server. Pinning `read-excel-file` exactly at `9.0.9` (no caret) reduces drift exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-DoS | Denial of Service | Server actions accepting file uploads | mitigate | `experimental.serverActions.bodySizeLimit: '2mb'` in `next.config.ts` returns HTTP 413 from Next.js before the action body runs. Layers 1 (client) and 2 (server-side `file.size` check in plan 33-05) are added by later plans; this plan is required for layer 3 per IMPORT-07. |
| T-33-XLSX | Tampering / Code Injection | XLSX parser dependency | mitigate | Pin `read-excel-file@9.0.9` exactly (no caret) so a transitive upgrade cannot silently introduce a vulnerable version. The library is a pure parser with no formula evaluation (RESEARCH.md §Security Domain, line 866) and is NOT `xlsx`/SheetJS (banned per STATE.md and CVE-2023-30533). |
| T-33-SupplyChain | Tampering | New transitive npm install | accept | Standard supply-chain risk shared with all v2.0 work. Mitigated indirectly by the lockfile commit and Vercel build reproducibility. No additional measure required for v2.0 scope. |
</threat_model>

<verification>
- `node -e "require.resolve('read-excel-file/node')"` succeeds.
- `node -e "require.resolve('zod')"` succeeds.
- `grep -E "bodySizeLimit:\s*['\"]2mb['\"]" next.config.ts` exits 0.
- `npx tsc --noEmit` exits 0 with no `next.config.ts` errors.
- `git diff package.json` shows only the addition of two `dependencies` keys; no other lines are modified.
</verification>

<success_criteria>
- IMPORT-07 framework-layer enforcement is in place: oversized requests get HTTP 413 from Next.js without entering action code.
- `read-excel-file/node` is importable in any future `src/actions/import.ts` written by plan 33-05.
- `zod` is no longer dependent on transitive hoisting; it is explicitly pinned for the project.
</success_criteria>

<output>
After completion, create `.planning/phases/33-server-actions-queries-and-bulk-import/33-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include the exact installed versions of `read-excel-file` and `zod`, and the `next.config.ts` diff.
</output>
