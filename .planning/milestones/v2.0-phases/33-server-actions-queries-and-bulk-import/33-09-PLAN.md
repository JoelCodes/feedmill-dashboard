---
phase: 33-server-actions-queries-and-bulk-import
plan: "09"
type: execute
wave: 4
depends_on:
  - "33-04"
  - "33-06"
files_modified:
  - .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md
  - .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md
autonomous: true
gap_closure: true
requirements: []
tags:
  - gap-closure
  - deferral
  - cache-invalidation
  - revalidate-tag
  - phase-34-handoff
  - docs-only

must_haves:
  truths:
    - "GAP-02 (revalidateTag cache invalidation observable end-to-end) is explicitly DEFERRED to Phase 34 with a concrete observable test step that the Phase 34 UAT will execute against the running RSC consumer."
    - "The deferral is recorded in TWO durable locations: (a) `33-HUMAN-UAT.md` Test #2 is rewritten to mark the gap as `deferred_to_phase_34` with the concrete test step the operator will execute when Phase 34 ships; (b) a Phase 34 inherited-UAT hand-off file `34-INHERITED-UAT.md` is created (inside the Phase 33 directory because Phase 34's directory does NOT exist yet — Phase 34 planning has not started; the orchestrator's plan-phase agent for Phase 34 will pick up this file when it runs)."
    - "The deferral rationale is explicit: `unstable_cache` invalidation is observable only through an RSC consumer. Phase 34 builds the first RSC consumer of `getProductionOrders` (the live dashboard at `/`). Building a parallel Node-script harness would require booting `next dev` in CI which is heavyweight AND would duplicate Phase 34's UAT work without adding signal."
    - "The deferral is NOT silent — operator and verifier agents will both encounter the explicit `deferred_to_phase_34` marker in `33-HUMAN-UAT.md` and the inherited test step in `34-INHERITED-UAT.md` will be surfaced to Phase 34's discuss/plan agents."
    - "No source code is modified. No tests are added. This is a doc-only plan closing the verification report's GAP-02 by classifying it as an inherited UAT item rather than a Phase 33 in-scope verification."
  artifacts:
    - path: ".planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md (modified)"
      provides: "Test #2 line rewritten with `result: deferred_to_phase_34` and an inline deferral note"
      contains: "deferred_to_phase_34"
    - path: ".planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md (created)"
      provides: "Phase 34 hand-off file naming the concrete test step the Phase 34 UAT must include — links back to GAP-02 in 33-VERIFICATION.md and 33-HUMAN-UAT.md"
      contains: "GAP-02"
      min_lines: 40
  key_links:
    - from: ".planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md Test #2"
      to: ".planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md"
      via: "Cross-reference text noting the deferral target"
      pattern: "34-INHERITED-UAT\\.md"
    - from: ".planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md"
      to: ".planning/phases/33-server-actions-queries-and-bulk-import/33-VERIFICATION.md GAP-02"
      via: "Inline citation of the gap ID and source"
      pattern: "GAP-02"
---

<objective>
Close VERIFICATION.md **GAP-02** (revalidateTag cache invalidation observable end-to-end) by formally **deferring** it to Phase 34 with two concrete artifacts:

1. **Update `33-HUMAN-UAT.md` Test #2** — rewrite the `result:` line from `[pending]` to `deferred_to_phase_34` and add an inline note describing the deferral rationale + the exact test step the Phase 34 UAT will execute. This makes the deferral visible to any verifier re-reading Phase 33.

2. **Create `34-INHERITED-UAT.md`** — a hand-off file inside the Phase 33 directory (Phase 34's directory doesn't exist yet — the orchestrator's plan-phase agent for Phase 34 will read inherited UAT items as part of its standard context-gathering). The file lists the concrete test step Phase 34 must perform during its UAT and cites GAP-02 + `33-VERIFICATION.md` as the source.

Why deferral instead of building a harness now (per operator decision in gap-closure inputs):

- `unstable_cache` invalidation is only observable to a consumer. The only consumer of `getProductionOrders` is the Phase 34 RSC dashboard. There is no Phase 33 consumer.
- Building a parallel harness would require booting `next dev` in a CI/scripted context. The test would import `getProductionOrders` (which has `import 'server-only'`) inside a Next.js worker, warm the cache, fire a transition, and re-call `getProductionOrders`. This is fragile (server-only-vs-tsx loader conflicts, ESM/CJS boundary issues, `next/cache` only emits effects inside the Next.js worker process) AND duplicates exactly what Phase 34's existence-of-the-RSC-consumer makes trivial to test.
- Unit tests (33-04, 33-06) already assert that `revalidateTag('production-orders', 'max')` is called with the correct tag string. Phase 33 query files (33-02) wrap `unstable_cache` with the matching tag. The CONTRACT between action and query is verified by grep + unit test. The OBSERVABLE BEHAVIOR (next RSC render returns fresh data) is what Phase 34 UAT will close.

Scope discipline:
- This plan touches ONLY two `.md` files inside `.planning/phases/33-server-actions-queries-and-bulk-import/`.
- No source code changes. No package.json changes. No test files. No scripts. No schema changes.
- `33-VERIFICATION.md` itself is NOT modified — the planner's verifier already escalated GAP-02 at operator request and the document is the historical record of that decision.

Output: One modified UAT file + one new inherited-UAT hand-off file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-CONTEXT.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-VERIFICATION.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-04-SUMMARY.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-06-SUMMARY.md

<interfaces>
<!-- No code interfaces. This is a doc-only plan.

     Existing 33-HUMAN-UAT.md Test #2 (lines 19-21):

         ### 2. revalidateTag cache invalidation observed in browser
         expected: After a successful transition, the Phase 34 dashboard reflects the new order state without a manual hard refresh
         result: [pending]

     Will be rewritten to:

         ### 2. revalidateTag cache invalidation observed in browser
         expected: After a successful transition, the Phase 34 dashboard reflects the new order state without a manual hard refresh
         result: deferred_to_phase_34
         deferral_rationale: |
           Cannot be observed in Phase 33 — requires an RSC consumer (Phase 34 dashboard) to call getProductionOrders.
           Action-side tag contract is verified by unit tests (33-04 Tests A7/B5/C8/D7) and by grep
           (revalidateTag('production-orders', 'max') in transitions.ts matches tags: ['production-orders']
           in src/db/queries/orders.ts and src/db/queries/events.ts). Inherited as a Phase 34 UAT item —
           see 34-INHERITED-UAT.md.

     New 34-INHERITED-UAT.md will contain:
     - Phase 34 inherited UAT step 1: end-to-end revalidateTag observation (GAP-02 closure).
     - Concrete steps (transition action via UI button → no manual refresh → card updates).
     - Pass/fail criteria.
     - Cross-reference to 33-VERIFICATION.md GAP-02 + 33-HUMAN-UAT.md Test #2.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 33-HUMAN-UAT.md Test #2 with deferral marker</name>
  <files>.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md (lines 1-36, full file — confirm current Test #2 shape; preserve frontmatter and other tests)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-VERIFICATION.md (lines 12-15 — GAP-02 statement; lines 28-33 — human_verification entry for GAP-02 with expected behavior)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-CONTEXT.md (decisions D-01 through D-13 — confirm revalidateTag('production-orders', 'max') is the mandated tag string)
  </read_first>
  <action>
    Open `.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md`. Make THREE targeted edits:

    Edit 1 — Frontmatter `status` field (line 2): change `status: gaps_flagged` to `status: gaps_closed`. Justification: with the deferral in place, gap-02 is no longer "flagged" — it is "deferred" with a concrete forwarding artifact. Gaps -01 and -03 are closed by plans 33-07 and 33-08 (harnesses). After all three closures land, the status accurately reflects the post-closure state.

    NOTE: If the verifier agent's convention is to leave UAT status untouched and only the orchestrator flips it, OR if 33-07/33-08 haven't merged yet, leave `status: gaps_flagged` unchanged. The decision rule: only flip to `gaps_closed` if `result: [pending]` is replaced for ALL three tests. Since this plan only updates Test #2's deferral and Tests #1/#3 will be updated by operator after running 33-07/33-08 harnesses (Task 3 checkpoints in those plans), the safer default is to keep `status: gaps_flagged` here and let the operator update it after the harness runs land.

    REVISED Edit 1 — leave `status: gaps_flagged` unchanged. The status flips when the operator records the harness-pass results.

    Edit 2 — Update Test #2 (line 19-21, the `result:` line for "revalidateTag cache invalidation observed in browser"):

    REPLACE the current single-line `result: [pending]` with a multi-line block:

    ```
    ### 2. revalidateTag cache invalidation observed in browser
    expected: After a successful transition, the Phase 34 dashboard reflects the new order state without a manual hard refresh
    result: deferred_to_phase_34
    deferral_rationale: |
      Cannot be observed in Phase 33 — observation requires an RSC consumer
      (the Phase 34 dashboard) to call getProductionOrders. Action-side tag
      contract is verified by unit tests (33-04 Tests A7/B5/C8/D7, 33-06 Tests
      11 + 22) and by grep: revalidateTag('production-orders', 'max') in
      src/actions/transitions.ts and src/actions/import.ts matches tags:
      ['production-orders'] in src/db/queries/orders.ts and src/db/queries/events.ts.
      End-to-end observable behavior inherits to Phase 34's UAT — see
      .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md
      and 33-VERIFICATION.md GAP-02 for the source statement.
    deferred_test_step: |
      Phase 34 UAT MUST include a concrete step asserting:
      (1) Sign in as mill_operator and visit /. Note the state of one Pending order.
      (2) In a second tab, find the same order and click the Mixing transition button
          (Phase 34's per-order action button).
      (3) In the first tab, WITHOUT manually refreshing, observe the order card
          transitions from Pending → Mixing within one render cycle (≤30s if relying
          on the polling interval; immediate if Next.js router.refresh() is wired).
      (4) Pass: card visibly updates. Fail: requires manual refresh.
    ```

    Preserve the rest of the file unchanged — frontmatter (other than the noted leave-alone), `## Current Test` section, Tests #1 and #3 (which will be updated by operator running the 33-07/33-08 harnesses), `## Summary` block, `## Gaps` block.

    Edit 3 — Update the `## Summary` block counts (line 28-34):
    - `total: 3` — unchanged
    - `passed: 0` — unchanged (will increment when operator runs the harnesses)
    - `issues: 0` — unchanged
    - `pending: 3` — change to `pending: 2` (Tests #1 and #3 still pending; Test #2 is now deferred, not pending)
    - `skipped: 0` — unchanged
    - `blocked: 0` — unchanged
    - ADD a new line: `deferred: 1` (Test #2 is now formally deferred).

    Resulting summary block:
    ```
    total: 3
    passed: 0
    issues: 0
    pending: 2
    skipped: 0
    blocked: 0
    deferred: 1
    ```

    Use exact YAML-block indentation matching the existing file. Do not introduce trailing whitespace. Preserve the file's existing UTF-8 / LF line-ending convention.
  </action>
  <verify>
    <automated>grep -F "deferred_to_phase_34" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -F "34-INHERITED-UAT.md" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -F "GAP-02" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -F "deferral_rationale:" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -F "deferred_test_step:" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -F "deferred: 1" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -E "^pending: 2$" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -E "^### 1\. Concurrent transition race" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && grep -E "^### 3\. End-to-end XLSX import" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md && { ! grep -F "result: [pending]" .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md | grep -c "."; } | head -1 || echo "OK: Test #2 [pending] replaced; Tests #1 and #3 still marked [pending]"</automated>
  </verify>
  <acceptance_criteria>
    - `33-HUMAN-UAT.md` contains the literal string `deferred_to_phase_34`.
    - `33-HUMAN-UAT.md` references `34-INHERITED-UAT.md`.
    - `33-HUMAN-UAT.md` cites `GAP-02`.
    - `33-HUMAN-UAT.md` contains `deferral_rationale:` AND `deferred_test_step:` block markers.
    - `33-HUMAN-UAT.md` summary block has `pending: 2` (was 3) and adds `deferred: 1`.
    - Tests #1 and #3 are NOT modified (they retain `[pending]` until the operator runs the harnesses from plans 33-07 and 33-08).
    - The frontmatter `status: gaps_flagged` is unchanged (operator flips when all three gaps resolve).
    - File is well-formed Markdown (no syntax-breaking edits).
  </acceptance_criteria>
  <done>Test #2 is formally deferred to Phase 34 with a concrete test step. Verifier agents re-reading the UAT file will see the deferral and not flag it as an open gap.</done>
</task>

<task type="auto">
  <name>Task 2: Create 34-INHERITED-UAT.md hand-off file</name>
  <files>.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md</files>
  <read_first>
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-VERIFICATION.md GAP-02 (lines 12-15 + 28-33 — the gap statement and human_verification block)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-CONTEXT.md (decisions D-01..D-13 — confirm `revalidateTag('production-orders', 'max')` is the canonical tag invocation)
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-04-SUMMARY.md (confirms transitions.ts uses the two-arg revalidateTag form per Next.js 16.1.6)
    - .planning/ROADMAP.md Phase 34 entry (lines 137-148 — Phase 34 success criteria; SC#1 explicitly references "live DB-backed mill dashboard at /")
    - .planning/REQUIREMENTS.md PROD-01..PROD-11 (Phase 34 requirements — the inherited UAT step folds naturally into PROD-01 + PROD-09 verification)
  </read_first>
  <action>
    Create `.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md` as a NEW file. The file lives inside Phase 33's directory because Phase 34's directory does not yet exist (Phase 34 has not been planned). When the orchestrator's plan-phase agent runs for Phase 34, its standard context discovery includes "read all `.md` files in the immediately-preceding completed phase directory" — this hand-off file will surface there. The Phase 34 discuss agent will fold the inherited step into Phase 34's UAT plan.

    File content (use this structure verbatim — frontmatter then body):

    ```
    ---
    phase: 33-server-actions-queries-and-bulk-import
    handoff_target_phase: 34-production-dashboard-ui-and-homepage-promotion
    handoff_type: inherited-uat
    source_gap: GAP-02
    source_document: 33-VERIFICATION.md
    created: <yyyy-mm-dd from current date>
    status: pending-phase-34-planning
    ---

    # Phase 34 Inherited UAT — From Phase 33 GAP-02

    ## Source

    - **Gap:** `GAP-02` in `.planning/phases/33-server-actions-queries-and-bulk-import/33-VERIFICATION.md`
    - **Truth:** "revalidateTag cache invalidation observable end-to-end"
    - **Why deferred (not closed in Phase 33):** `unstable_cache` invalidation is only observable to a consumer of the wrapped query. The only consumer of `getProductionOrders` and `getOrderEvents` is the Phase 34 RSC dashboard at `/`. Phase 33 has no RSC consumer; building a parallel `next dev` harness duplicates Phase 34 UAT work without adding signal.
    - **What IS already verified in Phase 33** (do not re-verify):
      - Action-side: `revalidateTag('production-orders', 'max')` is called on every successful mutation. Unit tests: 33-04 Tests A7/B5/C8/D7 (transitions), 33-06 Tests 11 + 22 (commitImportAction).
      - Query-side: both `src/db/queries/orders.ts` and `src/db/queries/events.ts` wrap their query bodies in `unstable_cache(..., ['production-orders'], { tags: ['production-orders'] })`. Tag string matches the action's invocation.
      - Cross-file grep gate: `grep -rn "'production-orders'" src/actions/ src/db/queries/ | wc -l` reports ≥6 matches (4 in transitions + 1 in import + 1 each in queries/orders + queries/events).

    ## Inherited Test Step (MUST appear in Phase 34's UAT)

    **Step name:** End-to-end revalidateTag observation (GAP-02 closure)

    **Preconditions:**
    - Phase 34 dashboard at `/` is implemented (PROD-01) and renders against live DB via `getProductionOrders`.
    - At least one `production_orders` row exists in `state: 'Pending'` (use the seed data from Phase 32 or the harness from Phase 33 plan 33-07).
    - Operator is signed in as `mill_operator`.

    **Steps:**
    1. Open Tab A: navigate to `/`. Note the state of one specific Pending order (record `orderNumber` and `version` for reference).
    2. Open Tab B: navigate to `/` in a SECOND tab/window (same browser session, same user).
    3. In Tab B: click the per-order Mixing transition button (Phase 34's UI element). Observe the optimistic UI response (the card moves to the Mixing column — or, in v2.0 without optimistic UI per PROD-FUT-01 deferral, the card updates after the action returns).
    4. In Tab A: WITHOUT pressing the browser refresh button, observe the order card. Within one render cycle, the card should reflect the new state.
       - **If polling is the only refresh mechanism (v2.0 design per PROD-09):** the card updates within the 30-second polling interval (`REFRESH_INTERVAL_MS = 30_000`). Time the update.
       - **If `router.refresh()` is wired into the action button's onSuccess handler (recommended):** the card updates near-instantly (within ~1s of the action's RSC response landing).

    **Pass criteria:**
    - Tab A's order card reflects the new state without a manual hard refresh (no F5, no Ctrl+R, no clicking the page's manual refresh control — that control exists per PROD-11 but is not what GAP-02 tests).
    - The state update lands within ≤30s (the polling interval upper bound).

    **Fail criteria:**
    - Tab A's card remains in the old state indefinitely (requires manual refresh) — `revalidateTag` is not firing OR the `unstable_cache` tag does not match OR the page is not re-rendering on tag invalidation.
    - The state update relies on a hard refresh — same diagnosis.

    **Diagnostic hints if the test fails:**
    - Run `grep -rn "'production-orders'" src/` — confirm the tag string appears in BOTH the action invocations and the query `unstable_cache` wrappers. A typo in either site silently breaks invalidation (RESEARCH.md §6 + Pitfall — tag-string drift).
    - Confirm `getProductionOrders` is called from the page RSC (not from a client component that bypasses the cache layer).
    - Confirm `export const dynamic = 'force-dynamic'` is on the page (PROD-01) so the RSC re-renders on each request.
    - Confirm `revalidateTag('production-orders', 'max')` (two-arg form for Next.js 16.1.6 — see 33-04-SUMMARY.md decision note) is called in EVERY mutating action path (success AND server-error fallback in transitions; success in commitImportAction).

    ## Where this folds into Phase 34's existing UAT

    - **PROD-01 verification:** the dashboard renders live DB data — same test setup as this GAP-02 step.
    - **PROD-09 verification:** the 30s polling refreshes data — the polling test naturally exercises GAP-02 (since each poll re-runs `getProductionOrders` which respects tag invalidation).
    - **Recommended:** Phase 34's planner adds this step as an EXPLICIT line item in its `34-PLAN.md` UAT section, citing `34-INHERITED-UAT.md` and `GAP-02`. Do NOT fold it silently into PROD-01 or PROD-09 verification — explicit is better than implicit for inherited gaps.

    ## Closure protocol

    When Phase 34's UAT runs (post-implementation):
    1. Execute the steps above against the running Phase 34 dashboard.
    2. Record the result in Phase 34's `34-HUMAN-UAT.md` under a dedicated test entry titled `Inherited: GAP-02 revalidateTag end-to-end (from Phase 33)`.
    3. After Phase 34 ships its verification report, AMEND `33-HUMAN-UAT.md` Test #2: change `result: deferred_to_phase_34` to `result: closed_in_phase_34 (pass/fail, <date>, <Phase 34 verification commit>)`.
    4. After amendment, flip `33-HUMAN-UAT.md` frontmatter `status: gaps_flagged` → `status: gaps_closed` (assuming GAP-01 and GAP-03 are also closed by then via plans 33-08 and 33-07).

    ## Cross-References

    - Source gap: `33-VERIFICATION.md` GAP-02
    - Source UAT entry: `33-HUMAN-UAT.md` Test #2 (marked `deferred_to_phase_34`)
    - Action-side unit test coverage: `33-04-SUMMARY.md` Tests A7/B5/C8/D7
    - Action-side unit test coverage (import): `33-06-SUMMARY.md` Tests 11 + 22
    - Tag-string contract: `src/actions/transitions.ts` line 59 (CONFLICT_MESSAGE constant), tag invocations throughout `src/actions/*.ts` and `src/db/queries/*.ts`
    - Phase 34 ROADMAP entry: `.planning/ROADMAP.md` Phase 34 (PROD-01..PROD-11 requirements)
    ```

    Replace the `<yyyy-mm-dd from current date>` placeholder with today's date (e.g., `2026-05-13`).

    The file MUST be >=40 lines and contain ALL of: `GAP-02`, `revalidateTag`, `34-INHERITED-UAT`, `PROD-01`, `PROD-09`, the literal string `production-orders`, AND `deferred_to_phase_34`.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && wc -l .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md | awk '{ if ($1 < 40) { print "FAIL: hand-off file too short ("$1" lines, need >=40)"; exit 1 } else { print "OK: hand-off file has "$1" lines" } }' && grep -F "GAP-02" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "revalidateTag" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "production-orders" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "PROD-01" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "PROD-09" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "33-VERIFICATION.md" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "33-HUMAN-UAT.md" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "handoff_target_phase:" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "source_gap: GAP-02" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md && grep -F "deferred_to_phase_34" .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md` exists and is >=40 lines.
    - File contains GAP-02, revalidateTag, production-orders, PROD-01, PROD-09, 33-VERIFICATION.md, 33-HUMAN-UAT.md, handoff_target_phase, source_gap: GAP-02, deferred_to_phase_34.
    - Frontmatter is well-formed YAML with the `handoff_type: inherited-uat` marker.
    - The body has a clearly delineated "Inherited Test Step" section with numbered steps, pass criteria, and fail criteria.
    - The body has a "Closure protocol" section describing the four-step closure path (execute → record in Phase 34 UAT → amend 33-HUMAN-UAT.md → flip status).
  </acceptance_criteria>
  <done>Phase 34's plan-phase agent will discover this file during its standard context discovery (read preceding-phase artifacts) and surface the inherited test step in Phase 34's UAT plan. No silent deferrals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan only modifies planning documentation. No source code, no DB, no external systems.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09-SilentDefer | Repudiation / Verification gap | Future Phase 34 planning ignores inherited UAT | mitigate | The hand-off file lives in the Phase 33 directory where Phase 34's plan-phase agent reads inherited context. The file's frontmatter `handoff_target_phase` makes it discoverable. The 33-HUMAN-UAT.md Test #2 entry references the hand-off file by path. The closure protocol explicitly names the four steps to fully close (including amending 33-HUMAN-UAT.md after Phase 34 UAT runs). |
| T-33-09-TagDrift | Tampering / Cache poisoning | Tag string drift between action and query | accept (verified pre-deferral) | This is the underlying risk the gap exists to surface. Phase 33 already verifies tag-string consistency via grep + unit tests. Phase 34 will provide the observable closure. No new surface in this plan. |
| T-33-09-DocOnly | n/a | No new surface | accept | This plan adds zero executable code. Zero new threat surface beyond the two doc files. |
</threat_model>

<verification>
- `.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md` Test #2 result line is `deferred_to_phase_34` and contains `deferral_rationale:` + `deferred_test_step:` blocks.
- `.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md` summary block: `pending: 2`, `deferred: 1`.
- `.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md` exists, is >=40 lines, contains all required substrings (GAP-02, PROD-01, PROD-09, production-orders, etc.), and has the closure-protocol section.
- No source code changes (verified by `git diff --name-only` returning only the two `.md` paths).
</verification>

<success_criteria>
- GAP-02 from `33-VERIFICATION.md` is formally deferred — not silently — with a concrete forwarding test step that Phase 34's UAT will execute.
- The deferral is recorded in TWO durable, discoverable locations (33-HUMAN-UAT.md inline + 34-INHERITED-UAT.md hand-off file).
- Closure protocol is explicit: amend 33-HUMAN-UAT.md after Phase 34 UAT runs; flip status only after all three Phase 33 gaps land closed.
- Zero code or test changes — pure doc-only plan as specified by the operator's gap-closure inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/33-server-actions-queries-and-bulk-import/33-09-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Note that this plan is doc-only (no code), cite GAP-02 as the source, list both modified files, and reference the closure protocol that Phase 34 will execute.
</output>
</content>
</invoke>