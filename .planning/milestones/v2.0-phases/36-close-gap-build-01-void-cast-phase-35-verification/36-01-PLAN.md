---
phase: 36-close-gap-build-01-void-cast-phase-35-verification
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/BlockedAlertBand.test.tsx
  - src/components/BlockedAlertBand.tsx
autonomous: true
requirements:
  - PROD-06
tags:
  - nuqs
  - startTransition
  - typescript
  - regression-test
user_setup: []

must_haves:
  truths:
    - "`npm run build` exits 0 — BlockedAlertBand.tsx:44 no longer leaks Promise<URLSearchParams> into startTransition"
    - "A regression test in BlockedAlertBand.test.tsx asserts the void cast is present at the source level so the bug cannot silently re-land"
    - "The fix pattern at BlockedAlertBand.tsx:44 matches the canonical reference at BlockedExceptionList.tsx:35"
  artifacts:
    - path: "src/components/BlockedAlertBand.test.tsx"
      provides: "Test 12 source-grep regression for void-cast inside startTransition"
      contains: "startTransition\\(\\(\\) => void setQuery"
    - path: "src/components/BlockedAlertBand.tsx"
      provides: "BlockedAlertBand component with corrected void cast on line 44"
      contains: "startTransition(() => void setQuery"
  key_links:
    - from: "src/components/BlockedAlertBand.tsx:44"
      to: "React startTransition callback type contract (() => void | Promise<void>)"
      via: "void operator coerces Promise<URLSearchParams> to undefined"
      pattern: "startTransition\\(\\(\\) => void setQuery\\(\\{ order: order\\.id \\}\\)\\)"
    - from: "src/components/BlockedAlertBand.test.tsx Test 12"
      to: "src/components/BlockedAlertBand.tsx source content"
      via: "fs.readFileSync(path.resolve(__dirname, 'BlockedAlertBand.tsx'))"
      pattern: "expect\\(content\\)\\.toMatch\\(/startTransition\\\\\\(\\\\\\(\\\\\\) => void setQuery/\\)"
---

<objective>
Close BUILD-01: fix the one-line TypeScript error at `src/components/BlockedAlertBand.tsx:44` that blocks `npm run build` from exiting 0. The `nuqs` `setQuery` setter on a non-shallow key returns `Promise<URLSearchParams>`, which leaks into the React `startTransition(callback)` brand `() => void | Promise<void>` (`VoidOrUndefinedOnly`). Add the canonical `void` operator cast (matching the pattern at `BlockedExceptionList.tsx:35`) so the callback's return is coerced to `undefined`.

Per TDD mode: write the source-grep regression test FIRST (Test 12 in `BlockedAlertBand.test.tsx`, modeled on `DrawerSkeleton.test.tsx` Test 7), confirm RED (test fails because current source lacks the `void` cast), then apply the one-character fix at `BlockedAlertBand.tsx:44` and confirm GREEN (regression test passes + `npm run build` exits 0).

Purpose: Unblock `npm run build` so the v2.0 milestone can ship. Belt-and-suspenders the fix so a future refactor cannot silently remove the `void` operator.

Output:
- Extended `BlockedAlertBand.test.tsx` with new Test 12 source-grep regression
- One-character diff at `BlockedAlertBand.tsx:44` adding `void` before `setQuery`
- Two atomic commits (RED test then GREEN fix) per TDD RED/GREEN gate sequence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-VALIDATION.md
@src/components/BlockedAlertBand.tsx
@src/components/BlockedExceptionList.tsx
@src/components/BlockedAlertBand.test.tsx
@src/components/DrawerSkeleton.test.tsx

<interfaces>
Canonical fix pattern reference (from src/components/BlockedExceptionList.tsx:35):

  const openDrawer = (id: string) => startTransition(() => void setQuery({ order: id }));

Current broken line (src/components/BlockedAlertBand.tsx:44):

  onClick={() => startTransition(() => setQuery({ order: order.id }))}

Required fix shape (one-line diff, add `void` after the inner arrow):

  onClick={() => startTransition(() => void setQuery({ order: order.id }))}

Source-grep regression test analog (from src/components/DrawerSkeleton.test.tsx:38-45):

  it('Test 7: component is purely presentational (no useState or onClick in source)', () => {
    const fs = require('fs');
    const path = require('path');
    const filePath = path.resolve(__dirname, 'DrawerSkeleton.tsx');
    const content = fs.readFileSync(filePath, 'utf-8');
    expect(content).not.toMatch(/useState|onClick/);
  });

Why fs.readFileSync over jest @/ alias: 35-LEARNINGS.md documents that in worktree mode the `@/` jest alias resolves to the main project root rather than the worktree, breaking source-grep tests. `path.resolve(__dirname, 'BlockedAlertBand.tsx')` resolves correctly because __dirname points to the test file's directory.

Existing test file shape (src/components/BlockedAlertBand.test.tsx): 5 RTL tests (Tests 7-11) + 1 T10b option-assertion test, all within a single `describe('BlockedAlertBand', ...)` block. Append the new Test 12 inside the existing describe block, after the T10b test at line 126.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD RED — Append source-grep regression Test 12 to BlockedAlertBand.test.tsx (failing test before fix)</name>
  <files>src/components/BlockedAlertBand.test.tsx</files>
  <read_first>
    - src/components/BlockedAlertBand.test.tsx (full file — to confirm existing test order and the closing brace of the describe block)
    - src/components/DrawerSkeleton.test.tsx (lines 38-45 — Test 7 is the canonical fs.readFileSync + path.resolve(__dirname, ...) source-grep regression pattern)
    - src/components/BlockedAlertBand.tsx (line 44 — confirms current source lacks the `void` cast, which is why the new test must FAIL on first run)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md (section 2 — adapted regression test snippet for BlockedAlertBand.test.tsx)
  </read_first>
  <behavior>
    - Test 12 reads `src/components/BlockedAlertBand.tsx` from disk via `fs.readFileSync` (path resolved with `path.resolve(__dirname, 'BlockedAlertBand.tsx')`).
    - Test 12 asserts the source content matches the regex `/startTransition\(\(\) => void setQuery/`.
    - Before the fix lands (current source), Test 12 MUST fail because the source contains `startTransition(() => setQuery` (no `void`), which does not satisfy the regex.
    - After the fix lands (Task 2), Test 12 MUST pass because the source contains `startTransition(() => void setQuery`.
    - The other 6 existing tests (Tests 7-11 + T10b) MUST continue to pass — do not modify them.
  </behavior>
  <action>
    Open `src/components/BlockedAlertBand.test.tsx`. Locate the closing brace of the existing `describe('BlockedAlertBand', ...)` block (currently at line 127 immediately after the T10b test). Insert a new test immediately before that closing brace:

    1. Test name: `'Test 12: startTransition callback uses `void setQuery` cast (BUILD-01 regression guard)'`
    2. Inside the test: use `const fs = require('fs')` and `const path = require('path')` (matching the CommonJS-style require pattern from DrawerSkeleton.test.tsx:40-41 — do NOT use ES imports inside the test body).
    3. Resolve the source file with `path.resolve(__dirname, 'BlockedAlertBand.tsx')`.
    4. Read content with `fs.readFileSync(filePath, 'utf-8')`.
    5. Assert with `expect(content).toMatch(/startTransition\(\(\) => void setQuery/)`.
    6. Add a leading comment block explaining the BUILD-01 regression rationale and citing `BlockedExceptionList.tsx:35` as the canonical reference fix pattern.

    Run the regression test to confirm RED. EXPECTED OUTCOME: Test 12 fails with the source-not-matching-regex error; Tests 7-11 + T10b still pass.

    Commit the failing test as `test(36-01): add source-grep regression for BlockedAlertBand void-cast (BUILD-01 RED)` (matches the project's TDD commit prefix discipline from 35-LEARNINGS.md).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='BlockedAlertBand' 2>&1 | grep -E '(Test 12|FAIL|PASS|Tests:)' | head -20</automated>
    Expected: Test 12 shows in the output as FAILING with a "received content did not match regex" error. Tests 7-11 + T10b show as passing.
  </verify>
  <acceptance_criteria>
    - `src/components/BlockedAlertBand.test.tsx` contains a new test named "Test 12: startTransition callback uses `void setQuery` cast (BUILD-01 regression guard)" within the existing `describe('BlockedAlertBand', ...)` block.
    - The new test uses `fs.readFileSync` + `path.resolve(__dirname, 'BlockedAlertBand.tsx')`, mirroring the pattern at `DrawerSkeleton.test.tsx:38-45`.
    - The new test asserts `expect(content).toMatch(/startTransition\(\(\) => void setQuery/)`.
    - Running `npm test -- --testPathPattern='BlockedAlertBand'` shows Test 12 FAILING and the other six tests PASSING (TDD RED gate).
    - A commit lands with the message `test(36-01): add source-grep regression for BlockedAlertBand void-cast (BUILD-01 RED)`.
  </acceptance_criteria>
  <done>Test 12 exists in `src/components/BlockedAlertBand.test.tsx`, runs, fails for the documented reason (source lacks `void`), and the failing-test commit is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD GREEN — Apply the one-character void-cast fix at BlockedAlertBand.tsx:44, confirm Test 12 passes and npm run build exits 0</name>
  <files>src/components/BlockedAlertBand.tsx</files>
  <read_first>
    - src/components/BlockedAlertBand.tsx (full file — confirms the current line 44 is `onClick={() => startTransition(() => setQuery({ order: order.id }))}` and no other lines need modification)
    - src/components/BlockedExceptionList.tsx (line 35 — `const openDrawer = (id: string) => startTransition(() => void setQuery({ order: id }))` is the canonical reference fix pattern)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md (section 1 — confirms the one-line diff and the expression+`void` shape is preferred over the block-form variants)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md (Investigation 1 — root cause + fix shape; Risk R7 — must use the expression+`void` form, not the block-form variant)
  </read_first>
  <behavior>
    - `src/components/BlockedAlertBand.tsx:44` reads `onClick={() => startTransition(() => void setQuery({ order: order.id }))}` after the fix (one new `void` token inserted between the inner arrow `=>` and `setQuery`).
    - No other lines of `BlockedAlertBand.tsx` change: imports remain, the hook call at line 32 remains, the JSX surrounding the button remains.
    - Test 12 from Task 1 transitions from RED to PASS (the source-grep regex now matches).
    - All other tests in `BlockedAlertBand.test.tsx` (Tests 7-11 + T10b) remain PASSING.
    - `npm run build` exits 0 — the TS2322 `Promise<URLSearchParams>` not assignable to `VoidOrUndefinedOnly` error no longer reproduces.
  </behavior>
  <action>
    Open `src/components/BlockedAlertBand.tsx`. Locate line 44:

      onClick={() => startTransition(() => setQuery({ order: order.id }))}

    Insert the `void` operator between the inner arrow's `=>` and `setQuery`. The corrected line reads:

      onClick={() => startTransition(() => void setQuery({ order: order.id }))}

    Do NOT change the imports (line 20), the hook call (line 32), the type definitions, or any surrounding JSX. The diff is exactly one whitespace-bounded insertion of the four-character token `void ` (lowercase, followed by a single space).

    Run `npm test -- --testPathPattern='BlockedAlertBand'` and confirm Test 12 passes alongside the other six tests (TDD GREEN gate). Then run `npm run build` and confirm exit code 0 (integration-level gate — closes the BUILD-01 ROADMAP success criterion #1).

    Commit as `fix(36-01): add void cast to BlockedAlertBand.tsx:44 startTransition callback (BUILD-01 GREEN)` matching the project's conventional fix-commit prefix and the four prior `fix(35-07): ...` commits from the post-phase SQL fix series.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='BlockedAlertBand' 2>&1 | tail -10 &amp;&amp; echo '--- BUILD ---' &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
    Expected: all 7 tests pass (Tests 7-11, T10b, Test 12); `npm run build` exits 0 with no TS2322 error against `BlockedAlertBand.tsx:44`.
  </verify>
  <acceptance_criteria>
    - `grep -n "startTransition(() => void setQuery" src/components/BlockedAlertBand.tsx` returns exactly one match at line 44.
    - `grep -n "startTransition(() => setQuery" src/components/BlockedAlertBand.tsx` returns NO matches (the pre-fix shape no longer exists).
    - The diff between the pre-fix and post-fix file is exactly one line, with exactly one inserted `void ` token (4 chars + 1 space) and no other content change.
    - `npm test -- --testPathPattern='BlockedAlertBand'` exits 0 with all 7 tests passing.
    - `npm run build` exits 0 — no TS2322 error on `src/components/BlockedAlertBand.tsx`.
    - A commit lands with the message `fix(36-01): add void cast to BlockedAlertBand.tsx:44 startTransition callback (BUILD-01 GREEN)`.
  </acceptance_criteria>
  <done>The void cast is in place at line 44, Test 12 passes, all sibling tests still pass, `npm run build` exits 0, and the GREEN fix commit is recorded. BUILD-01 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase touches only TypeScript syntax (a void operator). No new boundaries; no untrusted input handling change; no auth/authz change. The pre-existing client→nuqs URL-state surface is unchanged in shape and parameterization (already covered by Phase 34's T-34-03-02 threat assessment per the file's existing docblock at BlockedAlertBand.tsx:14-18). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | (no new threat) | src/components/BlockedAlertBand.tsx | accept | Type-only change. The `void` operator coerces a Promise to `undefined` at runtime; this does not change what data crosses any boundary. CWE catalogue does not apply — no security-relevant behavior changes. Existing T-34-03-02 (parseAsString URL-param safety) remains in effect unchanged. |

No high-severity threats introduced. `security_enforcement: true` satisfied via this minimal threat model per Phase 36 §security_threat_model note.
</threat_model>

<verification>
- `npm test -- --testPathPattern='BlockedAlertBand'` exits 0 with 7 passing tests (Tests 7, 8, 9, 10, 11, T10b, Test 12).
- `npm run build` exits 0.
- `grep -n "startTransition(() => void setQuery" src/components/BlockedAlertBand.tsx` returns exactly one match at line 44.
- Git log shows two new commits: the failing-test RED commit and the fix GREEN commit, in that order.
</verification>

<success_criteria>
- BUILD-01 closed: `npm run build` exits 0; the BlockedAlertBand.tsx:44 `Promise<URLSearchParams>` leak is fixed.
- Regression test in place: Test 12 source-grep asserts the `void` cast remains and will FAIL if a future contributor removes it.
- Pattern alignment: the fix shape at line 44 byte-matches the canonical reference at `BlockedExceptionList.tsx:35`.
- TDD discipline: two atomic commits (RED then GREEN), each verifiable independently.
- ROADMAP Phase 36 SC#1 satisfied (`npm run build` exits 0) and SC#2 satisfied (regression test covers void-cast path; `npm test -- BlockedAlertBand` exits 0).
</success_criteria>

<output>
Create `.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-01-SUMMARY.md` summarizing:
- BUILD-01 root cause + the one-line fix applied at BlockedAlertBand.tsx:44
- The new Test 12 source-grep regression and why it complements npm run build as the structural invariant guard
- The two atomic commits and their hashes
- Reference to BlockedExceptionList.tsx:35 as the canonical pattern that BlockedAlertBand.tsx:44 now matches
</output>
