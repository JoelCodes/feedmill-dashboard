---
phase: 36-close-gap-build-01-void-cast-phase-35-verification
plan: 02
type: execute
wave: 1
depends_on:
  - 36-01
files_modified:
  - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md
autonomous: true
requirements:
  - KPI-01
  - KPI-02
  - KPI-03
  - KPI-04
  - KPI-05
  - KPI-06
  - KPI-07
  - KPI-08
  - PROD-06
tags:
  - verification
  - documentation
  - kpi
  - phase-35
user_setup: []

must_haves:
  truths:
    - "`.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` exists with frontmatter `status: verified`"
    - "All 8 KPI-* requirements (KPI-01..KPI-08) have a row in Requirements Coverage with status SATISFIED and code-evidence file:line citations"
    - "All 7 Phase 35 plans (35-01..35-07) are referenced in at least one Observable Truth or Required Artifacts row"
    - "KPI-06 row includes an explicit retest reference to commits ba54b4a..4d61194 and final fix at src/db/queries/kpis.ts:313"
    - "KPI-03 row cites D-14/OQ-2 (intentional client-side computation via useMemo in ProductionDashboard.tsx:221-233)"
    - "Behavioral Spot-Checks section cites `npm run build` exits 0 as evidence of PROD-06 satisfaction (closing the BUILD-01 gap from Plan 01)"
  artifacts:
    - path: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md"
      provides: "Goal-backward verification artifact for Phase 35 mirroring 34-VERIFICATION.md structure"
      contains: "## Requirements Coverage"
  key_links:
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md §Requirements Coverage"
      to: "src/db/queries/kpis.ts (and the rendered KPI components)"
      via: "file:line and file:symbol citations in the evidence cells"
      pattern: "src/db/queries/kpis\\.ts"
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md §Data-Flow Trace"
      to: "src/db/queries/kpis.ts:313 sql.raw() tz inlining"
      via: "explicit citation of the load-bearing post-phase SQL fix"
      pattern: "kpis\\.ts:313|sql\\.raw"
---

<objective>
Author `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` — the goal-backward verification artifact for Phase 35 (KPI Sections and Role-Specific Metrics). The doc must mirror the structural analog at `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-VERIFICATION.md` and provide evidence rows for every Phase 35 ROADMAP success criterion (5 SCs) and every KPI-* requirement (KPI-01..KPI-08). Each evidence row carries a concrete file:line or file:symbol citation drawn from the existing Phase 35 codebase (already shipped — this plan only documents what's there).

This is a documentation task with no I/O contract — `type: execute` per the planner mode-routing rules. Verification is plan-checker pattern matching against the analog structure plus file-existence assertion plus grep gates for required tokens.

Purpose: Close ROADMAP Phase 36 SC#3 ("`35-VERIFICATION.md` exists ... each KPI-* row has a `satisfied` verdict with code-evidence citations"). Provides the documentation foundation for the VALIDATION.md re-classification in Plan 04 and the audit re-run in Plan 05.

Output:
- New file `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` with the section layout and frontmatter described below
- One atomic commit `docs(36-02): author 35-VERIFICATION.md (goal-backward, 8/8 KPI satisfied)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/integration-check-v2.0.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-VERIFICATION.md

<interfaces>
Phase 35 deliverables to cite (per 36-RESEARCH.md Investigation 2 and 36-PATTERNS.md section 3):

- src/db/queries/kpis.ts — exports getKpiStrip, getSevenDayTrend, getBlockedWithDwell, sanitizeIanaTimezone
  - kpis.ts:313 is the load-bearing `sql.raw('...')` tz inlining (fix from commits ba54b4a..24b34bf)
  - kpis.ts:1 has `import 'server-only'` (D-14 + Pitfall 6 from 35-RESEARCH.md)
  - All three queries use `unstable_cache(..., { tags: ['production-orders'] })` per D-14
- src/lib/formula-mix.ts — exports bucketTexture() (D-11/D-12)
- src/lib/format-dwell.ts — exports formatDwell() (UI-SPEC dwell format)
- src/lib/timezone.ts — exports sanitizeIanaTimezone() (Pitfall 2 IANA allowlist)
- src/components/KpiCard.tsx, KpiStrip.tsx, TzBootstrap.tsx — Plan 35-05 (legacy KPICard.tsx deleted per D-08)
- src/components/SevenDayTrendChart.tsx, BlockedExceptionList.tsx, KpiSection.tsx — Plan 35-06 (D-07 zone 3, D-10, D-13)
- drizzle/0001_mute_champions.sql + src/db/schema/orders.ts (earlyDeliveryDate column, PgDateString mode) — Plan 35-01
- src/db/seed.ts + src/db/seed-data.json + scripts/export-seed.ts — Plan 35-01 backfill
- src/components/ProductionDashboard.tsx:221-233 — KPI-03 useMemo (intentionally client-side per D-14/OQ-2)
- src/components/BlockedExceptionList.tsx:93-100 — KPI-08 overdue badge render
- src/app/page.tsx — RSC reading `tz` cookie, fanning out KPI queries (Plan 35-07)

Phase 35 plan-to-KPI mapping (per 36-RESEARCH.md §Plan-by-plan coverage, table at lines 244-254):

| Plan  | Deliverables                                                            | KPI ID(s)                           |
| ----- | ----------------------------------------------------------------------- | ----------------------------------- |
| 35-01 | earlyDeliveryDate column + Drizzle migration + seed backfill            | KPI-08                              |
| 35-02 | productionOrderImportSchema extension + commitImportAction              | KPI-08 (data ingress)               |
| 35-03 | bucketTexture (formula-mix) + formatDwell (TDD pure helpers)            | KPI-05, KPI-07                      |
| 35-04 | getKpiStrip + getSevenDayTrend + getBlockedWithDwell + sanitizeIanaTz   | KPI-01, KPI-02, KPI-04..07          |
| 35-05 | KpiCard + KpiStrip + TzBootstrap (presentational primitives)            | KPI-01, KPI-02, KPI-04, KPI-05      |
| 35-06 | SevenDayTrendChart + BlockedExceptionList + KpiSection layout           | KPI-06, KPI-07, KPI-08              |
| 35-07 | RSC integration in page.tsx + MillColumn summary prop + UAT             | KPI-03 (header strip), all KPIs wired |

Post-phase SQL fix commits to cite explicitly in the KPI-06 evidence row (per 36-RESEARCH.md Investigation 5):

  ba54b4a fix(35-07): remove invalid GROUP BY 1 from pure-aggregate KPI queries
  ca707c9 fix(35-07): align getSevenDayTrend GROUP BY expression with SELECT cast chain
  d792924 fix(35-07): inline qualified column ref in getSevenDayTrend to fix GROUP BY mismatch
  24b34bf fix(35-07): inline sanitized tz as SQL literal in getSevenDayTrend
  4d61194 docs(35): mark Phase 35 complete — v2.0 milestone feature-complete

Frontmatter shape for 35-VERIFICATION.md (per 36-PATTERNS.md section 3 and 36-RESEARCH.md §Required frontmatter):

  ---
  phase: 35-kpi-sections-and-role-specific-metrics
  verified: 2026-05-15T<HH:MM>:00Z
  status: verified
  score: 8/8 KPI requirements verified + 5/5 ROADMAP success criteria verified
  gaps: []
  retest_outcome:
    date: 2026-05-15
    source: 35-UAT.md (status: complete)
    results:
      - <one bullet per UAT scenario summary — Plan 03 populates this after UAT execution>
  human_verification: []
  ---

NOTE on retest_outcome.results: at the time Plan 02 runs, Plan 03's UAT has not yet executed. Plan 02 emits placeholder bullets ("UAT-1..UAT-10 — pending Plan 03 execution"); Plan 03 (after UAT pass) updates the bullets in place. This avoids a circular dependency between artifacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 35-VERIFICATION.md with frontmatter and the 8 required sections, populated with Phase 35 evidence</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-VERIFICATION.md (the structural analog — read full file; copy section order and table headers)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md (Investigations 2 and 5 — section requirements and post-phase SQL fix details)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md (section 3 — adapted frontmatter, table-row examples for Goal Achievement, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Requirements Coverage)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md (Decisions + Surprises sections — informs Anti-Patterns Found inventory and the KPI-06 retest rationale)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md (D-01..D-17 — the locked decisions that the verification doc must explicitly cite for KPI-03 and KPI-05)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md (lines 60-99 — typography + color contracts for the visual rendering claims in Observable Truths)
    - .planning/REQUIREMENTS.md (lines 66-73 — KPI-01..KPI-08 definitions for the Requirements Coverage section)
    - .planning/ROADMAP.md (Phase 35 entry — the 5 ROADMAP success criteria that drive Observable Truths)
    - src/db/queries/kpis.ts (read lines 1-50 for `import 'server-only'` + function exports, and lines 299-339 for getSevenDayTrend including the line 313 sql.raw() fix)
    - src/components/ProductionDashboard.tsx (lines 221-233 — KPI-03 useMemo client-side computation citation)
    - src/components/BlockedExceptionList.tsx (lines 93-100 — KPI-08 overdue badge render citation)
  </read_first>
  <action>
    Create the file `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` with the following structure (mirroring `34-VERIFICATION.md` section-for-section):

    1. **YAML frontmatter** with fields: `phase: 35-kpi-sections-and-role-specific-metrics`, `verified: 2026-05-15T<HH:MM>:00Z` (use the current UTC time at write moment), `status: verified`, `score: 8/8 KPI requirements verified + 5/5 ROADMAP success criteria verified`, `gaps: []`, `retest_outcome` with `date: 2026-05-15`, `source: 35-UAT.md (status: complete)`, and `results:` as a placeholder bulleted list ("UAT-1..UAT-10 — pending Plan 03 execution" — Plan 03 will update these in place after UAT pass), `human_verification: []`. Do NOT include `overrides_applied` or `override_notes` (no review-fix cycle for Phase 35).

    2. **H1 title and intro paragraph** stating the Phase 35 goal (verbatim from ROADMAP §Phase 35 goal line) and the verification date.

    3. **`## Goal Achievement` → `### Observable Truths`** — table with columns `# | Truth | Status | Evidence`. Populate 5 rows mapping the 5 ROADMAP success criteria (SC#1: mill-wide + per-line tons today; SC#2: per-column header strip; SC#3: pending backlog + formula mix; SC#4: 7-day trend with empty state; SC#5: cross-column exception list + overdue badge) to ✓ VERIFIED status with file:line evidence. Close with `**Score:** 5/5 truths verified`.

    4. **`### Gaps Summary`** — single paragraph stating "No remaining gaps." (Phase 35 code is complete and Plan 01 has closed BUILD-01).

    5. **`### Required Artifacts`** — table with columns `Artifact | Expected | Status | Details`. Populate one row per Phase 35 NEW file enumerated in the <interfaces> block above (kpis.ts, formula-mix.ts, format-dwell.ts, timezone.ts, KpiCard.tsx, KpiStrip.tsx, KpiSection.tsx, SevenDayTrendChart.tsx, BlockedExceptionList.tsx, TzBootstrap.tsx, drizzle/0001_mute_champions.sql, src/db/seed.ts updates).

    6. **`### Key Link Verification`** — table `From | To | Via | Status | Details`. Populate rows for: `page.tsx → kpis.ts queries`, `KpiStrip → ProductionDashboard slot`, `MillColumn summary prop → header strip data`, `KpiSection wrapping zone-3 bottom layout per D-07`, `revalidateTag('production-orders') → KPI cache invalidation (D-14 cache tag invariant)`.

    7. **`### Data-Flow Trace (Level 4)`** — table `Artifact | Data Variable | Source | Produces Real Data | Status`. Three rows:
       - `KpiStrip / KpiCard` ← `getKpiStrip(tz)` from `page.tsx` RSC after `cookies().get('tz')` lookup ← SQL AT TIME ZONE on production_orders
       - `SevenDayTrendChart` ← `getSevenDayTrend(tz)` — **MUST explicitly cite `src/db/queries/kpis.ts:313` `sql.raw()` tz inlining** and reference commits ba54b4a..24b34bf as the load-bearing fix for the Drizzle GROUP BY parameter-slot mismatch (42803). Include a brief parenthetical note that this fix was retest-validated by UAT-3 in 35-UAT.md.
       - `BlockedExceptionList` ← `getBlockedWithDwell()` ← row navigation via `useOrderQuery` drawer route

    8. **`### Behavioral Spot-Checks`** — table `Behavior | Command | Result | Status`. Include at minimum these rows (per 36-RESEARCH.md Investigation 2):
       - `KPI test suite green` — `npm test -- --testPathPattern='kpis|formula-mix|format-dwell'` — `passing`
       - `KPI queries tagged production-orders` — `grep -n "'production-orders'" src/db/queries/kpis.ts` — 3 matches (one per query)
       - `server-only import gate on kpis.ts` — `grep -n "import 'server-only'" src/db/queries/kpis.ts` — 1 match at line 1
       - `Build green post BUILD-01 fix` — `npm run build` — exit 0 (cite Plan 36-01 as the closure)
       - `Legacy KPICard.tsx deleted per D-08` — `grep -rn 'KPICard' src/` — exit 1 / no matches
       - `KPI-06 sql.raw tz inlining present` — `grep -n "sql.raw" src/db/queries/kpis.ts` — at least one match at line 313

    9. **`### Requirements Coverage`** — table `Requirement | Phase | Description | Status | Evidence`. Populate 8 rows (KPI-01..KPI-08) AND 1 row for PROD-06. Use the per-KPI citations from 36-PATTERNS.md section 3 (the table at lines 227-234). Each row:
       - KPI-01: ✓ SATISFIED, evidence cites `getKpiStrip.completedTodayLbs` → KpiStrip "Completed Today" card
       - KPI-02: ✓ SATISFIED, evidence cites `getKpiStrip.{premixLbs, excelLbs, cgmLbs}` → 3 per-line KpiCards
       - KPI-03: ✓ SATISFIED, evidence cites `computeColumnWeights` useMemo in `src/components/ProductionDashboard.tsx:221-233` and includes parenthetical citation "intentional client-side per D-14/OQ-2; no DB query"
       - KPI-04: ✓ SATISFIED, evidence cites `getKpiStrip.{pendingCount, pendingLbs}` → "Pending Backlog" KpiCard
       - KPI-05: ✓ SATISFIED, evidence cites `getKpiStrip.{pelletPct, mashPct, crumblePct}` and includes "em-dash null-state per D-12 NULLIF semantics"
       - KPI-06: ✓ SATISFIED, evidence cites `getSevenDayTrend(tz)` → `SevenDayTrendChart`, and **MUST include explicit retest reference**: "Post-phase SQL fix sequence ba54b4a..4d61194 closed Drizzle param-slot GROUP BY (42803). Final fix `sql.raw()` tz inlining at src/db/queries/kpis.ts:313. UAT-3 in 35-UAT.md retest-validated."
       - KPI-07: ✓ SATISFIED, evidence cites `getBlockedWithDwell()` → `BlockedExceptionList`; "server-sorted by dwell ASC (ORDER BY MAX(changedAt) ASC)"
       - KPI-08: ✓ SATISFIED, evidence cites `isOverdue` server-computed in kpis.ts; rendered at `src/components/BlockedExceptionList.tsx:93-100`
       - PROD-06: ✓ SATISFIED, evidence cites `src/components/BlockedAlertBand.tsx` with parenthetical "BUILD-01 closed by Phase 36 Plan 01 (commit `fix(36-01): add void cast to BlockedAlertBand.tsx:44 startTransition callback (BUILD-01 GREEN)`)"

    10. **`### Anti-Patterns Found`** — table `File | Line | Pattern | Severity | Impact`. Inventory carried-forward pre-existing issues per 36-RESEARCH.md §Open Question #3:
        - Drizzle `IndexedColumn` TS errors in test files (pre-existing, predates v2.0)
        - 14 ClerkProvider mock failures in `src/app/settings/__tests__/page.test.tsx` (D-04 deferred from Phase 27)
        - **Deferred backlog (out of scope per ROADMAP Phase 36 goal):** SUMMARY frontmatter backfill (~22 partial entries) — cite `v2.0-MILESTONE-AUDIT.md` audit verdict item 4
        - **Deferred backlog (out of scope per ROADMAP Phase 36 goal):** INT-02 `33-HUMAN-UAT.md` Test #2 amendment — cite `v2.0-MILESTONE-AUDIT.md` audit verdict item 5
        - **Deferred backlog (captured by commit 4d61194):** KPI SQL integration smoke tests — defer to v2.1 hardening

    11. **`### Human Verification — pending Plan 03 UAT execution`** — single paragraph stating "See `35-UAT.md` for full UAT scenarios and observed results. Plan 36-03 executes the 10 UAT scenarios (UAT-1..UAT-10); upon completion, the `retest_outcome.results` bullets in this file's frontmatter are populated and the §Gaps Summary above remains 'No remaining gaps.' if all UATs pass."

    Trailing line: `_Verified: <UTC timestamp>_` and `_Verifier: Claude (gsd-planner via Plan 36-02)_` matching the trailing convention from `34-VERIFICATION.md:201-202`.

    Commit as `docs(36-02): author 35-VERIFICATION.md (goal-backward, 8/8 KPI satisfied)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md &amp;&amp; echo "FILE EXISTS" &amp;&amp; grep -c -E "^## (Goal Achievement|Required Artifacts|Key Link Verification|Data-Flow Trace|Behavioral Spot-Checks|Requirements Coverage|Anti-Patterns Found|Human Verification)|^### (Goal Achievement|Observable Truths|Gaps Summary|Required Artifacts|Key Link Verification|Data-Flow Trace|Behavioral Spot-Checks|Requirements Coverage|Anti-Patterns Found|Human Verification)" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md &amp;&amp; grep -E "KPI-0[1-8]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | wc -l &amp;&amp; grep -E "kpis\.ts:313|sql\.raw" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | head -3</automated>
    Expected: file exists; section count >= 8 (the major H2/H3 headers); KPI-0[1-8] tokens >= 8 (each KPI must appear in at least the Requirements Coverage row); at least 1 match for kpis.ts:313 OR sql.raw cite in the Data-Flow Trace and KPI-06 evidence.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` exists.
    - Frontmatter contains: `phase: 35-kpi-sections-and-role-specific-metrics`, `status: verified`, `score: 8/8 KPI requirements verified + 5/5 ROADMAP success criteria verified`, `gaps: []`. Run `grep -E "^(phase|status|score|gaps):" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` and verify each token is present.
    - All 8 KPI-* requirement IDs (KPI-01..KPI-08) appear in the Requirements Coverage table with ✓ SATISFIED status. Run `grep -c "^| KPI-0[1-8] " .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` and verify count is 8.
    - PROD-06 appears with a citation to Plan 36-01 (BUILD-01 closure). Run `grep "PROD-06" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns a line that also contains the string "BUILD-01" or "36-01".
    - KPI-06 row explicitly cites `kpis.ts:313` OR the `sql.raw` token in the evidence cell. Run `grep "KPI-06" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` and verify the cited line contains either `kpis.ts:313` or `sql.raw`.
    - All 7 Phase 35 plans (35-01..35-07) appear as references somewhere in the document. Run `grep -cE "35-0[1-7]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` and verify count >= 7.
    - The section structure matches `34-VERIFICATION.md`: H2 "Goal Achievement" and H3 sections "Observable Truths", "Gaps Summary", "Required Artifacts", "Key Link Verification", "Data-Flow Trace (Level 4)", "Behavioral Spot-Checks", "Requirements Coverage", "Anti-Patterns Found", "Human Verification".
    - Behavioral Spot-Checks row for `npm run build` cites Plan 36-01 as the BUILD-01 closure.
    - A commit lands with message `docs(36-02): author 35-VERIFICATION.md (goal-backward, 8/8 KPI satisfied)`.
  </acceptance_criteria>
  <done>The 35-VERIFICATION.md artifact exists with all 8 KPI rows SATISFIED, all 5 ROADMAP success criteria covered in Observable Truths, all 7 Phase 35 plans referenced, the KPI-06 SQL-fix retest explicitly cited, and PROD-06 evidence pointing to Plan 36-01's BUILD-01 closure. The commit is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase task authors a Markdown documentation artifact. No code paths, no data flows, no external surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | (no new threat) | .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | accept | Documentation-only artifact. No CWE applies — no security-relevant behavior is created or modified. |

No high-severity threats introduced. `security_enforcement: true` satisfied via this minimal threat model per Phase 36 §security_threat_model note.
</threat_model>

<verification>
- `test -f .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` exits 0.
- `grep -c "^| KPI-0[1-8] " .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns 8.
- `grep "PROD-06" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns a line citing Plan 36-01 / BUILD-01.
- `grep -E "kpis\.ts:313|sql\.raw" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns at least one match (KPI-06 SQL-fix citation).
- `grep -cE "35-0[1-7]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns >= 7 (all Phase 35 plans referenced).
- Git log contains the new docs commit.
</verification>

<success_criteria>
- ROADMAP Phase 36 SC#3 satisfied: `35-VERIFICATION.md` exists with goal-backward analysis covering all 7 plans (35-01..35-07) and all 8 KPI-* requirements; each KPI-* row has a `satisfied` verdict with code-evidence citations.
- The structural template matches `34-VERIFICATION.md` (section-for-section).
- KPI-06 retest scope is explicit — the load-bearing post-phase SQL fix at `src/db/queries/kpis.ts:313` is cited with reference to commits ba54b4a..4d61194.
- PROD-06 is included to close the audit's BLOCKER-01 (BUILD-01) traceability loop via the Plan 36-01 commit reference.
- Carried-forward anti-patterns and deferred backlog items are documented (not silently dropped) per 36-RESEARCH.md Open Question #3.
- The `retest_outcome.results` placeholder bullets signal to Plan 03 where to insert post-UAT results without rewriting the file.
</success_criteria>

<output>
Create `.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-02-SUMMARY.md` summarizing:
- The `35-VERIFICATION.md` artifact created and its section count
- Confirmation that all 8 KPI-* requirements have SATISFIED rows with file:line citations
- The deferred-backlog items captured in the Anti-Patterns section (audit verdict items 4 and 5, KPI SQL integration smoke tests)
- The handoff to Plan 03: retest_outcome.results bullets are placeholders that Plan 03 will populate post-UAT
</output>
