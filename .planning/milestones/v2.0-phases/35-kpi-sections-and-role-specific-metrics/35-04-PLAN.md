---
phase: 35
plan: 04
type: tdd
wave: 2
depends_on: [35-01, 35-03]
files_modified:
  - src/db/queries/kpis.ts
  - src/db/queries/__tests__/kpis.test.ts
  - src/lib/timezone.ts
  - src/lib/__tests__/timezone.test.ts
autonomous: true
requirements: [KPI-01, KPI-02, KPI-04, KPI-05, KPI-06, KPI-07, KPI-08]
tags: [tdd, kpis, drizzle, sql, aggregation, timezone, security]
must_haves:
  truths:
    - "D-14: every KPI query is wrapped in `unstable_cache` with cache tag `['production-orders']` — invalidates alongside existing order queries when any mutating action calls `revalidateTag('production-orders')`"
    - "D-01 / D-02: every time-windowed query (KPI-01, KPI-02, KPI-05, KPI-06) uses `date_trunc('day', updatedAt AT TIME ZONE $tz)` with the IANA string sanitized via `Intl.supportedValuesOf('timeZone')` before SQL composition (Pitfall 2 mitigated)"
    - "D-12: KPI-05 SQL CASE expression for texture bucketing produces identical bucket assignments to the `bucketTexture()` helper from Plan 35-03 for every known input — agreement enforced by a Jest test"
    - "Pitfall 1: every `sum()` aggregate is wrapped in `COALESCE(..., '0')` to prevent null propagation when no rows match"
    - "Pitfall 5: KPI-01/02/05/06 'today' boundaries use `date_trunc('day', NOW() AT TIME ZONE $tz)` — never UTC date_trunc; operators on different timezones see their own local 'today'"
    - "D-03: KPI-07 dwell time = `NOW() - MAX(changed_at)` joined from `order_events` (most-recent Block event) where `to_state = 'Blocked'` AND order's current `state = 'Blocked'`; dwell value is exposed as epoch seconds via `EXTRACT(EPOCH FROM ...)` and formatted via `formatDwell()` before being returned to the UI"
    - "KPI-08 query payload includes a computed `isOverdue: earlyDeliveryDate IS NOT NULL AND earlyDeliveryDate < CURRENT_DATE AND state <> 'Completed'` boolean per row — UI conditionally renders the overdue badge"
  artifacts:
    - path: "src/db/queries/kpis.ts"
      provides: "Server-only KPI aggregation queries: getKpiStrip(tz), getSevenDayTrend(tz), getBlockedWithDwell()"
      exports: ["getKpiStrip", "getSevenDayTrend", "getBlockedWithDwell", "type KpiStripData", "type TrendDay", "type BlockedOrderWithDwell"]
      min_lines: 120
    - path: "src/db/queries/__tests__/kpis.test.ts"
      provides: "Mock-`db` unit tests for KPI-01 ‥ KPI-08 covering SQL structure, COALESCE wrapping, cache-tag invariant, and SQL-CASE / bucketTexture agreement"
    - path: "src/lib/timezone.ts"
      provides: "sanitizeIanaTimezone(raw: string | null | undefined): string — validates via Intl.supportedValuesOf('timeZone'); falls back to 'America/Chicago'"
      exports: ["sanitizeIanaTimezone", "DEFAULT_TIMEZONE"]
    - path: "src/lib/__tests__/timezone.test.ts"
      provides: "Pitfall 2 mitigation tests: valid IANA passes through, invalid input falls back, SQL-injection probe string ('UTC; DROP TABLE x') falls back"
  key_links:
    - from: "src/db/queries/kpis.ts"
      to: "src/lib/timezone.ts"
      via: "sanitizeIanaTimezone called on every tz parameter before SQL composition"
      pattern: "sanitizeIanaTimezone"
    - from: "src/db/queries/kpis.ts"
      to: "src/lib/formula-mix.ts"
      via: "FormulaBucket type import + agreement test in __tests__/kpis.test.ts"
      pattern: "FormulaBucket\\|bucketTexture"
    - from: "src/db/queries/kpis.ts"
      to: "src/lib/format-dwell.ts"
      via: "formatDwell called server-side to pre-format dwell strings in getBlockedWithDwell"
      pattern: "formatDwell"
    - from: "src/db/queries/kpis.ts"
      to: "next/cache"
      via: "unstable_cache with tags: ['production-orders']"
      pattern: "tags:\\s*\\['production-orders'\\]"
    - from: "src/db/queries/kpis.ts"
      to: "drizzle-orm"
      via: "sum, count, sql template imports + AT TIME ZONE clause"
      pattern: "AT TIME ZONE"
---

<objective>
Build the server-side KPI aggregation query layer in `src/db/queries/kpis.ts` per CONTEXT.md D-14 (shared cache tag) + RESEARCH.md Patterns 1-5. Every KPI from KPI-01 through KPI-08 (except KPI-03 which is computed client-side from the unfiltered orders prop per RESEARCH.md Open Question #2 + Pattern 10 recommendation) is implemented as an `unstable_cache`-wrapped query function that returns a typed payload directly consumable by the new UI components in Plans 35-05 / 35-06 / 35-07.

Purpose: this plan is the architectural keystone. Every UI component in Wave 3 receives props sourced from these queries. The TDD discipline here (RED → GREEN with mocked Drizzle queries) ensures:
1. Cache-tag invariant is wired correctly (D-14): one mistake here breaks the entire dashboard's 30-second freshness story.
2. Timezone sanitization (Pitfall 2): the only V5 Input Validation security control in Phase 35 lives in this layer.
3. SQL / JS agreement for KPI-05 bucketing (D-11/D-12): without an explicit agreement test, the CASE expression and `bucketTexture()` will drift silently.

Output: three exported query functions (`getKpiStrip`, `getSevenDayTrend`, `getBlockedWithDwell`) + a pure timezone-sanitization helper + comprehensive Jest coverage of the SQL structure, cache wiring, security gate, and JS/SQL agreement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-03-PLAN.md
@.planning/phases/33-server-actions-queries-and-bulk-import/33-02-PLAN.md
@src/db/queries/orders.ts
@src/db/queries/events.ts
@src/db/queries/__tests__/orders.test.ts
@src/db/schema/orders.ts
@src/db/schema/events.ts

<interfaces>
<!-- Type definitions executors will rely on. -->

From src/db/queries/orders.ts (canonical pattern, lines 32-49):
```typescript
export const getProductionOrders = unstable_cache(
  async (filters?: ProductionOrderFilters): Promise<ProductionOrder[]> => { ... },
  ['production-orders'],           // unique cache KEY
  { tags: ['production-orders'] }  // shared cache TAG — D-14
);
```

From src/db/queries/events.ts (cache key vs. tag split, lines 26-36):
```typescript
export const getOrderEvents = unstable_cache(
  async (orderId: string): Promise<OrderEvent[]> => { ... },
  ['order-events'],                 // different KEY
  { tags: ['production-orders'] }   // SAME TAG — D-14
);
```

From src/db/schema/orders.ts (post-Plan 35-01):
- `productionOrders` Drizzle table with: `state`, `millLine`, `weightLbs (numeric → string)`, `textureType (string | null)`, `updatedAt (timestamptz)`, `earlyDeliveryDate (string | null)`, `customer`, `orderNumber`, `id (uuid)`.
- Enum: `productionStateEnum.enumValues` = `['Pending','Mixing','Completed','Blocked']`.
- Enum: `millLineEnum.enumValues` = `['Premix','Excel','CGM']`.

From src/db/schema/events.ts:
- `orderEvents` table with: `orderId (uuid → productionOrders.id)`, `fromState`, `toState (productionStateEnum)`, `changedAt (timestamptz)`, `changedBy`, `note`.
- Composite index on `(orderId, changedAt DESC)` — RESEARCH.md verified.

From src/lib/formula-mix.ts (Plan 35-03):
- `export type FormulaBucket = 'Pellet' | 'Mash' | 'Crumble';`
- `export function bucketTexture(raw: string | null): FormulaBucket | null;`

From src/lib/format-dwell.ts (Plan 35-03):
- `export function formatDwell(epochSeconds: number): string;`

Locked return-payload types (this plan creates them — downstream UI plans 35-05/06/07 import them):

```typescript
// src/db/queries/kpis.ts — exported types

export type KpiStripData = {
  // KPI-01: mill-wide tons completed today (formatted string preserves numeric precision)
  completedTodayLbs: string;          // e.g., "18400" — UI prepends commas/units
  // KPI-02: per-line tons completed today
  premixLbs: string;
  excelLbs: string;
  cgmLbs: string;
  // KPI-04: pending backlog
  pendingCount: number;
  pendingLbs: string;
  // KPI-05: formula mix percentages over today's Completed (uncategorized excluded)
  pelletPct: number | null;           // null when denominator = 0 (no categorized completions today)
  mashPct: number | null;
  crumblePct: number | null;
  uncategorizedCount: number;          // for KPI-05 D-12 footnote
};

export type TrendDay = {
  date: string;                        // YYYY-MM-DD from date_trunc::date
  completedLbs: number;                // sum SUM(weightLbs)::float8 server-side
};

export type BlockedOrderWithDwell = {
  orderId: string;
  orderNumber: string;
  customer: string;
  millLine: 'Premix' | 'Excel' | 'CGM';
  dwellSeconds: number;                // EXTRACT(EPOCH FROM ...) — raw for sort/test stability
  dwellFormatted: string;              // server-side formatDwell(dwellSeconds) — Plan 35-03 helper
  earlyDeliveryDate: string | null;
  isOverdue: boolean;                  // earlyDeliveryDate !== null AND earlyDeliveryDate < CURRENT_DATE AND state !== 'Completed'
};
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD sanitizeIanaTimezone helper (Pitfall 2 security gate) + DEFAULT_TIMEZONE constant</name>
  <files>
    src/lib/timezone.ts,
    src/lib/__tests__/timezone.test.ts
  </files>
  <read_first>
    - src/lib/production-derivations.ts (pure-lib shape — match its style)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pitfall 2: IANA Timezone String Injection via Cookie" + § "Anti-Patterns to Avoid" (timezone allowlist rule)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "IANA timezone validation before SQL"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-01, D-02 (fallback is 'America/Chicago')
  </read_first>
  <behavior>
    - Test 1 (valid IANA): `sanitizeIanaTimezone('America/New_York')` returns `'America/New_York'`.
    - Test 2 (mill's TZ): `sanitizeIanaTimezone('America/Chicago')` returns `'America/Chicago'`.
    - Test 3 (Pacific): `sanitizeIanaTimezone('America/Los_Angeles')` returns `'America/Los_Angeles'` (the "remote manager" use case in CONTEXT.md).
    - Test 4 (UTC): `sanitizeIanaTimezone('UTC')` returns `'UTC'` — valid IANA per `Intl.supportedValuesOf`.
    - Test 5 (null): `sanitizeIanaTimezone(null)` returns `DEFAULT_TIMEZONE` (which is `'America/Chicago'`).
    - Test 6 (undefined): `sanitizeIanaTimezone(undefined)` returns `'America/Chicago'`.
    - Test 7 (empty string): `sanitizeIanaTimezone('')` returns `'America/Chicago'`.
    - Test 8 (Pitfall 2 SQL-injection probe): `sanitizeIanaTimezone("America/Chicago'; DROP TABLE production_orders; --")` returns `'America/Chicago'` (the injection string is NOT in `Intl.supportedValuesOf('timeZone')` so the validator falls back).
    - Test 9 (case mismatch): `sanitizeIanaTimezone('america/chicago')` returns `'America/Chicago'` — IANA names are case-sensitive in the `Intl` allowlist, so lowercase is invalid and falls back. (This is the secure default — better to silently fall back than to attempt normalization which would mask injection attempts.)
    - Test 10 (whitespace probe): `sanitizeIanaTimezone(' America/Chicago ')` returns `'America/Chicago'` (whitespace not trimmed; the trimmed-with-space string is not a member of the allowlist, so it falls back).
    - Test 11 (random garbage): `sanitizeIanaTimezone('not-a-timezone')` returns `'America/Chicago'`.
    - Test 12 (DEFAULT_TIMEZONE export): `DEFAULT_TIMEZONE === 'America/Chicago'` is asserted directly (locks the fallback value).
  </behavior>
  <action>
    Step 1 (RED). Create `src/lib/__tests__/timezone.test.ts` with the 12 tests above. Imports: `import { sanitizeIanaTimezone, DEFAULT_TIMEZONE } from '@/lib/timezone';`. Run `npm test -- --testPathPattern="lib/__tests__/timezone\.test"` — confirm all 12 fail. Commit: `test(35-04): RED — assert sanitizeIanaTimezone rejects non-IANA inputs and SQL-injection probes`.

    Step 2 (GREEN). Create `src/lib/timezone.ts`:
    - JSDoc block at file top citing Pitfall 2 and D-02 fallback rationale.
    - `export const DEFAULT_TIMEZONE = 'America/Chicago' as const;` — UI-SPEC + D-02 fallback.
    - `export function sanitizeIanaTimezone(raw: string | null | undefined): string` implementation:
        ```
        if (raw === null || raw === undefined || raw === '') return DEFAULT_TIMEZONE;
        const allowlist = Intl.supportedValuesOf('timeZone');
        return allowlist.includes(raw) ? raw : DEFAULT_TIMEZONE;
        ```
    - Do NOT cache `Intl.supportedValuesOf('timeZone')` at module scope. Node's V8 implementation lazy-loads the ICU data — first-call cost is tolerable and module-load caching introduces test-isolation surprises in Jest worker reuse. RESEARCH.md Assumption A2 confirms `Intl.supportedValuesOf` is available in Node 24+ (verified in Environment Availability table).
    - Do NOT do regex pre-filtering. The Intl allowlist IS the validation surface; a regex would either be redundant or wrong (IANA names contain `/`, `_`, digits — regex temptation leads to subtle bugs).
    - Add NO `'use server'` / `'use client'` directives — pure helper, importable from any context.

    Run tests — confirm all 12 GREEN. Commit: `feat(35-04): GREEN — sanitizeIanaTimezone via Intl.supportedValuesOf allowlist`.

    Step 3 (REFACTOR). Skip unless a clear improvement emerges. The implementation is already minimal.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="lib/__tests__/timezone\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "^export function sanitizeIanaTimezone" src/lib/timezone.ts` returns exactly 1.
    - source-assertion: `grep -c "^export const DEFAULT_TIMEZONE" src/lib/timezone.ts` returns exactly 1.
    - source-assertion: `grep -c "'America/Chicago'" src/lib/timezone.ts` returns ≥ 1.
    - source-assertion: `grep -c "Intl.supportedValuesOf" src/lib/timezone.ts` returns exactly 1.
    - source-assertion: `grep -c "'use server'\|'use client'" src/lib/timezone.ts` returns 0 (no directives — pure helper).
    - behavior-assertion: `npm test -- --testPathPattern="lib/__tests__/timezone\.test"` shows 12 tests passing (including SQL-injection probe + case mismatch).
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `sanitizeIanaTimezone` is the V5 Input Validation gate for the entire KPI query layer; the SQL-injection probe test (Test 8) is the security canary that catches any future regression. `DEFAULT_TIMEZONE` is the locked fallback for first-render scenarios per D-02.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD getKpiStrip — KPI-01, KPI-02, KPI-04, KPI-05 in a single query payload</name>
  <files>
    src/db/queries/kpis.ts,
    src/db/queries/__tests__/kpis.test.ts
  </files>
  <read_first>
    - src/db/queries/orders.ts (canonical `unstable_cache` pattern + Drizzle query shape)
    - src/db/queries/__tests__/orders.test.ts (mock-`db` pattern: how the existing tests mock `db.select().from().where()` chains — Phase 33-02 established this)
    - src/db/schema/orders.ts (post-Plan 35-01 column shape including earlyDeliveryDate)
    - src/lib/formula-mix.ts (Plan 35-03 — `bucketTexture` + `FormulaBucket` for the agreement test)
    - src/lib/timezone.ts (Task 1 of this plan)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 1: Drizzle SQL Aggregation with Timezone-Aware Date Filter" + § "Pattern 2: Formula Mix Bucketing — SQL CASE Expression" + § "Pattern 5: `unstable_cache` Wrapping" + § "Pitfall 1: `sum()` Returns `null` When No Rows Match" + § "Pitfall 5: Date Boundary Off-by-One Across Midnight"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/db/queries/kpis.ts"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-01, D-02, D-11, D-12, D-14, D-15
  </read_first>
  <behavior>
    For `getKpiStrip(tz: string): Promise<KpiStripData>` — KPI-01/02/04/05 in one cached payload:

    Cache-tag invariant tests (D-14 — MUST pass before any SQL-shape work):
    - Test 1: `getKpiStrip` is wrapped in `unstable_cache` with `tags: ['production-orders']` and a unique cache KEY array (different from `'production-orders'`/`'order-events'`/`'import-batches'`). Assertion: inspect via `Object.getOwnPropertyDescriptor` or read the source — alternatively, mock `next/cache.unstable_cache` and assert the third argument's `tags` field equals `['production-orders']` AND the second argument is a unique key (e.g., `['kpi-strip']`).
    - Test 2: per RESEARCH.md Pattern 5, the cache KEY is distinct from `getProductionOrders` ('production-orders'). Use `['kpi-strip']`.

    SQL-structure tests (mock the `db` object; assert calls but do NOT execute against a real DB):
    - Test 3 (KPI-01 / KPI-02 — completed today with tz-aware boundary): the query passed to `db.select(...).from(productionOrders).where(...).groupBy(...)` includes an `AT TIME ZONE` SQL fragment carrying the sanitized tz string. Use a spy / `sql` template inspector — RESEARCH.md Code Examples show the verbatim SQL shape.
    - Test 4 (Pitfall 1): every `sum()` aggregate is wrapped in a `COALESCE(..., '0')` fragment. Assert by searching the captured SQL string for `COALESCE` near each `SUM`.
    - Test 5 (Pitfall 2 wiring): `getKpiStrip(' ; DROP TABLE x')` — the `where` clause's tz value is `'America/Chicago'` (sanitized fallback), NOT the raw injection string.

    Return-shape tests (mock `db` to return canned rows and assert payload mapping):
    - Test 6 (KPI-01 happy path): when the mock returns one row `{ totalLbs: '18400' }` for the mill-wide sum, the payload's `completedTodayLbs === '18400'`.
    - Test 7 (KPI-02 per-line happy path): when the mock returns 3 grouped rows `[{ millLine: 'Premix', lbs: '6000' }, { millLine: 'Excel', lbs: '8000' }, { millLine: 'CGM', lbs: '4400' }]`, the payload has `premixLbs === '6000'`, `excelLbs === '8000'`, `cgmLbs === '4400'`.
    - Test 8 (KPI-02 missing line): when the mock returns only Premix and Excel (CGM had no completions today), `cgmLbs === '0'` — payload guarantees all three lines are present (server-side default-fill, NOT undefined).
    - Test 9 (KPI-04 pending backlog): when the mock returns `[{ count: 5, totalLbs: '47200' }]`, payload has `pendingCount === 5` AND `pendingLbs === '47200'`.
    - Test 10 (KPI-04 empty): when the mock returns `[{ count: 0, totalLbs: null }]` (no pending orders), payload has `pendingCount === 0` AND `pendingLbs === '0'` (COALESCE applied).
    - Test 11 (KPI-05 happy path): when the mock returns `{ pelletPct: 58.0, mashPct: 32.0, crumblePct: 10.0, uncategorizedCount: 3 }`, payload preserves those numbers as `pelletPct: 58.0`, `mashPct: 32.0`, `crumblePct: 10.0`, `uncategorizedCount: 3`.
    - Test 12 (KPI-05 NULLIF guard D-12): when the mock returns `{ pelletPct: null, mashPct: null, crumblePct: null, uncategorizedCount: 0 }` (zero categorized completions today → SQL `NULLIF(0)` → percentages NULL), payload's `pelletPct === null`, `mashPct === null`, `crumblePct === null`, `uncategorizedCount === 0`. UI decides display ("—" or "0%"); query does NOT coerce to 0.

    SQL/JS-agreement test (D-11 / D-12 — the critical SQL-CASE vs. bucketTexture agreement):
    - Test 13: for each of the 7 known DB texture values `['PELLET', 'SH PELLET', 'MASH', 'FINE CR', 'C. CRUMBLE', null, 'UNKNOWN_FUTURE_VALUE']`, the SQL CASE expression produces the same output that `bucketTexture()` (Plan 35-03) does. Implementation strategy: extract the JS-side bucket assignments by calling `bucketTexture(value)` for each test value. Then read the actual `kpis.ts` source file and either (a) parse the CASE expression's WHEN/THEN pairs, or (b) inline-evaluate the WHEN strings against each test value to derive the expected bucket. Assert both methods produce identical bucket assignments. PATTERNS.md § "src/db/queries/kpis.ts" shows the literal CASE template that this test verifies.
  </behavior>
  <action>
    Step 1 (RED — security + cache invariants FIRST). Create `src/db/queries/__tests__/kpis.test.ts` with the test scaffolding from `orders.test.ts` (mock for `next/cache.unstable_cache`, mock for `@/db` exporting a chainable `db` object with `.select().from().where().groupBy()` returning configurable promises). Write Tests 1, 2, 5, and 13 FIRST — these are the cache invariant, security gate, and SQL/JS-agreement contracts. Run — confirm all fail. Commit: `test(35-04): RED — getKpiStrip cache invariant + Pitfall 2 sanitization + bucketTexture agreement`.

    Step 2 (Add SQL structure + return shape tests). Add Tests 3, 4, 6-12 to the same file. Run — confirm all fail. Commit: `test(35-04): RED — getKpiStrip SQL aggregation + COALESCE + return-shape assertions`.

    Step 3 (GREEN — implement `getKpiStrip`). Create `src/db/queries/kpis.ts`:
    - Line 1: `import 'server-only';` (server-only discipline — RESEARCH.md).
    - Imports: `unstable_cache` from `next/cache`, `db` from `@/db`, `productionOrders` from `@/db/schema/orders`, `sum`, `count`, `eq`, `and`, `inArray`, `sql` from `drizzle-orm`, `sanitizeIanaTimezone` from `@/lib/timezone`, `type FormulaBucket` from `@/lib/formula-mix` (type-only; do NOT call the JS helper from server-query code per RESEARCH.md anti-pattern), `type MillLine`, `type ProductionState` from `@/db/schema/orders`.
    - Define and export `type KpiStripData` per the <interfaces> block (verbatim).
    - Implement `export const getKpiStrip = unstable_cache(async (rawTz: string): Promise<KpiStripData> => { ... }, ['kpi-strip'], { tags: ['production-orders'] });`. Inside the inner function:
      a) First line: `const tz = sanitizeIanaTimezone(rawTz);` (Pitfall 2 — sanitize before SQL composition).
      b) Issue 4 queries in parallel via `Promise.all`:
         i) Mill-wide KPI-01: `SELECT COALESCE(SUM(weight_lbs)::text, '0') AS total FROM production_orders WHERE state='Completed' AND date_trunc('day', updated_at AT TIME ZONE $tz) = date_trunc('day', NOW() AT TIME ZONE $tz)`. Use the `sql` template literal pattern from RESEARCH.md Pattern 1; the `$tz` placeholder uses Drizzle's tagged-template substitution (the IANA string is post-sanitization).
         ii) Per-line KPI-02: same as KPI-01 + `GROUP BY mill_line`. Map the 3 returned rows into a `Record<MillLine, string>` and default-fill `'0'` for any missing line (Test 8).
         iii) KPI-04 pending backlog: `SELECT COUNT(*) AS cnt, COALESCE(SUM(weight_lbs)::text, '0') AS total FROM production_orders WHERE state='Pending'` (no date filter — KPI-04 is "all pending", not "today").
         iv) KPI-05 formula mix: per RESEARCH.md Pattern 2, use a CASE expression matching `bucketTexture` rules verbatim AND `NULLIF(COUNT(*) FILTER (WHERE bucket IS NOT NULL), 0)` for the percent denominators (D-12). Each percentage is computed in SQL via `ROUND(... * 100.0 / NULLIF(...), 1)` and returned typed `number | null`. Date filter: same `AT TIME ZONE $tz` as KPI-01/02 because KPI-05 is "today's Completed orders" per REQUIREMENTS.md KPI-05.
      c) Compose the return object per `KpiStripData` exactly.
    - Do NOT call `bucketTexture` server-side. The CASE expression and the JS helper are PARALLEL implementations whose agreement is enforced by Test 13. RESEARCH.md anti-pattern: "Never call bucketTexture() from the SQL query."
    - Inline-comment every CASE WHEN line with a `// → 'Pellet'` / `// → 'Mash'` / `// → 'Crumble'` to make drift visible to PR reviewers AND to give the agreement test (Test 13) a parseable structure.

    Run tests — confirm Tests 1-13 GREEN. Commit: `feat(35-04): GREEN — implement getKpiStrip with COALESCE-guarded SQL + IANA sanitization + KPI-05 NULLIF denominator (D-12)`.

    Step 4 (REFACTOR — only if clean). If parallelizing the 4 sub-queries with Promise.all created hard-to-read code, extract them into named helper functions (`fetchMillWideToday`, `fetchPerLineToday`, `fetchPendingBacklog`, `fetchFormulaMix`) WITHIN `kpis.ts`. The top-level export remains `getKpiStrip`. Confirm all 13 tests stay green. Commit only if a change was made.

    Do NOT add `getSevenDayTrend` or `getBlockedWithDwell` in this task — they get their own task to keep context budget under control.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="db/queries/__tests__/kpis\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `head -n 2 src/db/queries/kpis.ts | grep -c "import 'server-only'"` returns 1 (server-only discipline — must be line 1 or 2 per RESEARCH.md).
    - source-assertion: `grep -c "^export const getKpiStrip = unstable_cache" src/db/queries/kpis.ts` returns exactly 1.
    - source-assertion: `grep -c "tags: \['production-orders'\]" src/db/queries/kpis.ts` returns ≥ 1 (D-14).
    - source-assertion: `grep -c "'kpi-strip'" src/db/queries/kpis.ts` returns ≥ 1 (unique cache KEY).
    - source-assertion: `grep -c "sanitizeIanaTimezone" src/db/queries/kpis.ts` returns ≥ 1 (Pitfall 2 mitigation wired in).
    - source-assertion: `grep -c "AT TIME ZONE" src/db/queries/kpis.ts` returns ≥ 3 (KPI-01, KPI-02, KPI-05 — every "today" query).
    - source-assertion: `grep -c "COALESCE" src/db/queries/kpis.ts` returns ≥ 3 (every sum() guarded — Pitfall 1).
    - source-assertion: `grep -c "NULLIF" src/db/queries/kpis.ts` returns ≥ 1 (KPI-05 denominator guard — D-12).
    - source-assertion: `grep -c "WHEN 'PELLET'" src/db/queries/kpis.ts` returns ≥ 1, similarly for `'SH PELLET'`, `'MASH'`, `'FINE CR'`, `'C. CRUMBLE'`.
    - source-assertion: `grep -c "bucketTexture(" src/db/queries/kpis.ts` returns 0 (anti-pattern: JS helper NOT called from server-query code; agreement enforced by test).
    - behavior-assertion: `npm test -- --testPathPattern="db/queries/__tests__/kpis\.test"` shows ≥ 13 tests passing including Test 13 (the SQL/JS agreement test).
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `getKpiStrip` implemented; cache tag invariant, IANA sanitization, COALESCE guards, NULLIF guard, and SQL/JS bucket agreement all enforced by Jest; payload type `KpiStripData` exported for Plan 35-05 to consume.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: TDD getSevenDayTrend (KPI-06) + getBlockedWithDwell (KPI-07 + KPI-08)</name>
  <files>
    src/db/queries/kpis.ts,
    src/db/queries/__tests__/kpis.test.ts
  </files>
  <read_first>
    - src/db/queries/kpis.ts (the file created in Task 2; this task appends two more exports)
    - src/db/queries/events.ts (existing pattern using both schema imports — order_events join model)
    - src/db/schema/events.ts (orderEvents shape, especially `changedAt timestamptz`, `toState productionStateEnum`)
    - src/lib/format-dwell.ts (Plan 35-03 — server-side dwell formatting)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 3: Dwell-Time Query for KPI-07" + § "Pattern 4: 7-Day Trend Bucketing (KPI-06)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/db/queries/kpis.ts" — dwell-time JOIN+GROUP BY pattern (lines 100-115)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-03 (dwell time semantics — "this block, not ever blocked")
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md (BlockedExceptionList row format + overdue badge condition)
  </read_first>
  <behavior>
    For `getSevenDayTrend(tz: string): Promise<TrendDay[]>`:
    - Test 14 (cache + tag): wrapped in `unstable_cache` with unique key `['kpi-seven-day-trend']` and tag `['production-orders']`.
    - Test 15 (sanitization): bad tz input falls back to `'America/Chicago'` (Pitfall 2).
    - Test 16 (SQL shape): query uses `date_trunc('day', updated_at AT TIME ZONE $tz)` for both the GROUP BY and the `INTERVAL '6 days'` lower bound.
    - Test 17 (return shape): when mock returns 7 rows `[{ date: '2026-05-08', completedLbs: '5000' }, ...]`, payload is `TrendDay[]` with `completedLbs` parsed to `number` (NOT string — per `<interfaces>` contract; UI receives numeric for SVG height math).
    - Test 18 (empty DB): when mock returns 0 rows (no completed orders in last 7 days), payload is `[]` (empty array, NOT null, NOT undefined). UI renders empty state when `data.length < 7`.
    - Test 19 (partial — 3 days of data): when mock returns 3 rows, payload has 3 entries. The query does NOT zero-fill missing dates server-side; UI handles `data.length < 7` empty state (per D-13 + Plan 35-06 contract).
    - Test 20 (ordering): payload is ordered oldest-first by date (matches `ORDER BY ... ASC` in Pattern 4) — UI renders bars left-to-right as time-ascending.

    For `getBlockedWithDwell(): Promise<BlockedOrderWithDwell[]>` (no tz parameter — dwell is wallclock-relative):
    - Test 21 (cache + tag): wrapped in `unstable_cache` with unique key `['kpi-blocked-dwell']` and tag `['production-orders']`. NO tz argument — Pattern 3 verified, dwell is not tz-aware (it's a duration, not a calendar date).
    - Test 22 (SQL shape — JOIN): query is `db.select(...).from(orderEvents).innerJoin(productionOrders, ...).where(and(eq(orderEvents.toState, 'Blocked'), eq(productionOrders.state, 'Blocked'))).groupBy(...).orderBy(...)`. Assert the JOIN happens and BOTH the event toState AND the order's current state are filtered to 'Blocked' (D-03 + Pattern 3 — "only CURRENTLY blocked orders").
    - Test 23 (dwell formula): the SELECT includes `EXTRACT(EPOCH FROM (NOW() - MAX(changedAt)))` as the dwell seconds value. RESEARCH.md Pattern 3 + Assumption A4 confirms this is the canonical SQL.
    - Test 24 (sort): ORDER BY clause sorts by `MAX(changedAt) ASC` — oldest block = longest dwell = first row in UI per UI-SPEC default sort.
    - Test 25 (return mapping): when mock returns one row `{ orderId: '...', orderNumber: 'ORD-001', customer: 'Acme', millLine: 'Premix', dwellSeconds: 8040, earlyDeliveryDate: '2026-05-10' }` AND today is `'2026-05-14'` (mock current date), payload row has:
        - `dwellSeconds: 8040`
        - `dwellFormatted: '2h 14m'` (server-side `formatDwell` call — Plan 35-03 dependency)
        - `isOverdue: true` (earlyDeliveryDate '2026-05-10' < today '2026-05-14' AND state is 'Blocked' which is != 'Completed')
    - Test 26 (isOverdue null guard — KPI-08 edge): when mock returns a row with `earlyDeliveryDate: null`, payload row has `isOverdue: false`. Null date never overdue.
    - Test 27 (isOverdue completed guard): when mock returns a row with `earlyDeliveryDate: '2026-05-01'` (past) AND `state: 'Completed'` — wait, this row shouldn't appear because the WHERE clause filters to `state='Blocked'`. Re-frame: blocked orders cannot be completed by definition. Test 27 instead covers: when `earlyDeliveryDate` is today exactly (`= CURRENT_DATE`), `isOverdue: false` (strict `<` per CONTEXT.md D-04 SQL skeleton). Today is NOT overdue.
    - Test 28 (formatDwell agreement): for each of `[0, 60, 3599, 3600, 86400]`, the server's `dwellFormatted` equals what `formatDwell(seconds)` produces in Plan 35-03 — a JS-side parallel agreement. This is a Pitfall-3-style drift guard.
  </behavior>
  <action>
    Step 1 (RED — append 15 tests). Open `src/db/queries/__tests__/kpis.test.ts` and append Tests 14-28 in two describe blocks: `describe('getSevenDayTrend')` (Tests 14-20) and `describe('getBlockedWithDwell')` (Tests 21-28). Mock the chainable `db.select().from().innerJoin().where().groupBy().orderBy()` chain — the existing scaffolding from Task 2 should be extensible. For Test 25 / 26 / 27, the mock `db` must also expose a way to control the current-date the SQL `CURRENT_DATE` macro returns. Approach: instead of mocking SQL's CURRENT_DATE (impossible without a real DB), include the comparison as a JS-side step after the query returns. I.e., the SELECT returns `earlyDeliveryDate` raw, and `getBlockedWithDwell`'s mapping function computes `isOverdue: earlyDeliveryDate !== null && earlyDeliveryDate < todayIso` where `todayIso` is computed via a single `await db.execute(sql\`SELECT CURRENT_DATE::text AS today\`)` call (or pass `now` via the SELECT). The simplest implementation: `SELECT ..., (early_delivery_date < CURRENT_DATE) AS is_overdue FROM ...` — let Postgres compute the boolean. Then the mock returns `is_overdue` directly and the JS mapping is a pass-through. Tests 25-27 then mock the `is_overdue` value the SELECT returns.

    Run — confirm all 15 fail. Commit: `test(35-04): RED — getSevenDayTrend + getBlockedWithDwell (KPI-06/07/08) behavior + cache + formatDwell agreement`.

    Step 2 (GREEN — append two query exports to `src/db/queries/kpis.ts`):

    A) `export const getSevenDayTrend = unstable_cache(async (rawTz: string): Promise<TrendDay[]> => { ... }, ['kpi-seven-day-trend'], { tags: ['production-orders'] });`
       Inside:
       - `const tz = sanitizeIanaTimezone(rawTz);`
       - Query per RESEARCH.md Pattern 4: SELECT `date_trunc('day', updated_at AT TIME ZONE $tz)::date AS date, COALESCE(SUM(weight_lbs), '0')::float8 AS completed_lbs` FROM productionOrders WHERE state='Completed' AND `updated_at AT TIME ZONE $tz >= date_trunc('day', NOW() AT TIME ZONE $tz) - INTERVAL '6 days'` GROUP BY 1 ORDER BY 1 ASC.
       - Note: the cast to `::float8` lets the JS-side receive `completedLbs` as `number` (Test 17 contract). Alternatively, use `::text` and `parseFloat` JS-side — pick `::float8` to keep the JS shape clean.
       - Map result to `TrendDay[]`.

    B) `export const getBlockedWithDwell = unstable_cache(async (): Promise<BlockedOrderWithDwell[]> => { ... }, ['kpi-blocked-dwell'], { tags: ['production-orders'] });`
       Inside:
       - NO tz parameter (D-03 — dwell is wallclock-relative, not calendar-aware).
       - Query per RESEARCH.md Pattern 3 + PATTERNS.md lines 100-115. Add `(early_delivery_date < CURRENT_DATE) AS is_overdue` to the SELECT so Postgres returns the boolean directly. Include `early_delivery_date` in the SELECT and GROUP BY.
       - Map each row to `BlockedOrderWithDwell`:
         `dwellSeconds: row.dwellSeconds` (already a number from EXTRACT EPOCH),
         `dwellFormatted: formatDwell(row.dwellSeconds)` — imports `formatDwell` from `@/lib/format-dwell`,
         `isOverdue: row.earlyDeliveryDate !== null && row.isOverdue === true` — defensive JS-side double-guard in case Postgres returns null for the boolean when earlyDeliveryDate is null. The simpler form `row.isOverdue ?? false` also works but the explicit null guard documents intent.

    - Export both new types: `TrendDay`, `BlockedOrderWithDwell` per `<interfaces>` contract.

    Run tests — confirm Tests 14-28 GREEN. Commit: `feat(35-04): GREEN — implement getSevenDayTrend + getBlockedWithDwell with EXTRACT(EPOCH) dwell + isOverdue boolean`.

    Step 3 (REFACTOR). If the two query bodies have significant duplication (sanitize → select → map), extract a tiny `withSanitizedTz` wrapper. Skip if not. Confirm all 28 tests green.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="db/queries/__tests__/kpis\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "^export const getSevenDayTrend" src/db/queries/kpis.ts` returns exactly 1.
    - source-assertion: `grep -c "^export const getBlockedWithDwell" src/db/queries/kpis.ts` returns exactly 1.
    - source-assertion: `grep -c "'kpi-seven-day-trend'" src/db/queries/kpis.ts` returns ≥ 1.
    - source-assertion: `grep -c "'kpi-blocked-dwell'" src/db/queries/kpis.ts` returns ≥ 1.
    - source-assertion: `grep -c "EXTRACT(EPOCH" src/db/queries/kpis.ts` returns ≥ 1 (dwell seconds).
    - source-assertion: `grep -c "INTERVAL '6 days'" src/db/queries/kpis.ts` returns ≥ 1 (KPI-06 7-day window).
    - source-assertion: `grep -c "innerJoin" src/db/queries/kpis.ts` returns ≥ 1 (orderEvents JOIN productionOrders for KPI-07).
    - source-assertion: `grep -c "early_delivery_date.*CURRENT_DATE\|isOverdue\|is_overdue" src/db/queries/kpis.ts` returns ≥ 1 (KPI-08 boolean).
    - source-assertion: `grep -c "formatDwell" src/db/queries/kpis.ts` returns ≥ 1 (server-side dwell formatting wired).
    - source-assertion: `grep -c "tags: \['production-orders'\]" src/db/queries/kpis.ts` returns ≥ 3 (all three queries — D-14).
    - behavior-assertion: `npm test -- --testPathPattern="db/queries/__tests__/kpis\.test"` shows ≥ 28 tests passing total.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    All three KPI query exports complete (`getKpiStrip`, `getSevenDayTrend`, `getBlockedWithDwell`); ≥ 28 unit tests green; payload types `TrendDay` and `BlockedOrderWithDwell` exported for Plan 35-05/06/07 consumption; KPI-08 `isOverdue` boolean computed server-side via Postgres; KPI-07 dwell formatted server-side via `formatDwell` from Plan 35-03 (drift guard test asserts agreement).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client cookie `tz=...` → RSC `cookies().get('tz')?.value` → `getKpiStrip(rawTz)` | Pitfall 2: operator-controlled cookie value crosses into SQL composition. Sanitized via `sanitizeIanaTimezone` (Task 1) BEFORE any `sql\`... AT TIME ZONE ${tz}\`` interpolation. |
| Drizzle `sql` template → Neon Postgres | Drizzle parameterizes safe values but `sql\`literal AT TIME ZONE ${tz}\`` substitutes `tz` directly into the SQL string. The allowlist check in `sanitizeIanaTimezone` is the ONLY barrier between cookie value and SQL string — non-bypassable validation is mandatory. |
| `order_events.note` → KPI-07 payload | Currently not surfaced in `BlockedOrderWithDwell`. If a future iteration adds the block reason to the payload, the note (operator-typed free text) must be sanitized for HTML/JSX rendering at the UI layer. Out of scope for Phase 35 but tagged for future audit. |
| KPI aggregate output → client bundle | Aggregate sums (weight, count) and percentages are non-sensitive operational metrics. No PII (no operator names, no customer financial info). Acceptable to expose to all authenticated users per D-17. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | T — Tampering | `tz` cookie injected with `'X; DROP TABLE production_orders; --'` | mitigate | `sanitizeIanaTimezone` (Task 1) validates against `Intl.supportedValuesOf('timeZone')` allowlist BEFORE any SQL composition. Test 8 in Task 1 + Test 5 in Task 2 are the security canaries. Any future change that bypasses the helper surfaces as a failing test. |
| T-35-04-02 | T — Tampering | SQL CASE expression drift from `bucketTexture` JS helper | mitigate | Test 13 in Task 2 enforces JS / SQL agreement over all known texture values plus a representative unknown value. Drift surfaces as a failing test in CI. |
| T-35-04-03 | T — Tampering | `dwellFormatted` server-string drift from `formatDwell` helper | mitigate | Test 28 in Task 3 (formatDwell agreement) directly compares server-computed dwellFormatted against the JS helper for 5 representative input values. Drift surfaces as a failing test. |
| T-35-04-04 | I — Information Disclosure | KPI payload includes aggregate weights, counts, percentages | accept | Operational metrics, non-PII. D-17: any authenticated user is authorized to view KPIs. RESEARCH.md Security Domain confirms KPI surfaces expose aggregate counts is acceptable. |
| T-35-04-05 | D — Denial of Service | Expensive KPI queries (4 sub-queries in `getKpiStrip` per request) | mitigate | `unstable_cache` wrapping (D-14) memoizes results until `revalidateTag('production-orders')` fires. Worst-case: every 30 seconds (polling tick) or on each mutating action. Cache hit rate is high during normal operator flow. |
| T-35-04-06 | E — Elevation of Privilege | KPI queries called from a non-RSC path | mitigate | `import 'server-only'` on line 1 of `kpis.ts` causes a build-time error if a client component tries to import from this file. Verified by RESEARCH.md anti-pattern + acceptance_criteria source-assertion. |
| T-35-04-07 | R — Repudiation | `unstable_cache` returns stale data after a transition action | accept | Mutating actions (Phase 33's `transitionToMixing` / `completeOrder` / `blockOrder` / `resumeFromBlocked` / `commitImportAction`) all already call `revalidateTag('production-orders')`. D-14 means KPIs invalidate alongside. No new action-side wiring required. |
| T-35-04-08 | T — Tampering | Postgres `EXTRACT(EPOCH FROM ...)` returning negative or NaN for clock skew | accept | Server clock is Neon's authoritative time (no operator influence). NOW() - MAX(changedAt) is always non-negative for past events. If clock skew between Neon servers were to produce a tiny negative interval (microseconds), `formatDwell(negative)` would return `'0m'` per the `<3600` branch — harmless. |
| T-35-04-09 | T — Tampering | KPI-08 `is_overdue` boolean computed server-side | mitigate | The boolean is computed inside the same SQL SELECT that filters rows. No operator-controllable input reaches the comparison (CURRENT_DATE is server-side). UI cannot upgrade a non-overdue row to overdue. |
| T-35-04-SC | T — Tampering (supply chain) | npm installs | n/a | Zero new package installs in this plan (D-13). The only externally-sourced packages referenced (`drizzle-orm`, `next/cache`) are already pinned in Phase 31-33. |
</threat_model>

<verification>
- `npm test -- --testPathPattern="(db/queries/__tests__/kpis|lib/__tests__/timezone)\.test"` — ≥ 40 tests passing total (12 timezone + 13 getKpiStrip + 15 getSevenDayTrend & getBlockedWithDwell).
- `npx tsc --noEmit` — TypeScript clean across `src/`.
- `grep -c "tags: \['production-orders'\]" src/db/queries/kpis.ts` returns exactly 3 (one per exported query — D-14).
- `grep -c "import 'server-only'" src/db/queries/kpis.ts` returns 1 (server-only discipline).
- `grep -c "sanitizeIanaTimezone" src/db/queries/kpis.ts` returns ≥ 2 (every tz-accepting query calls it).
- `grep -c "AT TIME ZONE" src/db/queries/kpis.ts` returns ≥ 4 (KPI-01, KPI-02, KPI-05, KPI-06).
- `grep -c "bucketTexture(" src/db/queries/kpis.ts` returns 0 (anti-pattern: JS helper not called from SQL-query code).
- All three query exports present: `grep -c "^export const get" src/db/queries/kpis.ts` returns ≥ 3.
</verification>

<success_criteria>
- KPI-01, KPI-02, KPI-04, KPI-05, KPI-06, KPI-07, KPI-08 query layer fully implemented and tested.
- D-14 satisfied: every exported query uses `tags: ['production-orders']` (existing cache invalidation contract — no new tag introduced).
- D-01 / D-02 / Pitfall 2 / Pitfall 5 satisfied: all daily aggregations use `date_trunc('day', updatedAt AT TIME ZONE $tz)` with `tz` sanitized via `Intl.supportedValuesOf` allowlist; SQL-injection probe is the security canary test.
- D-11 / D-12 satisfied: KPI-05 SQL CASE matches `bucketTexture()` for all known inputs (agreement test); NULL textures excluded from numerator AND denominator via `FILTER (WHERE bucket IS NOT NULL)` + `NULLIF(..., 0)`.
- Pitfall 1 satisfied: every `sum()` wrapped in `COALESCE(..., '0')` to prevent null propagation on empty result sets.
- D-03 satisfied: KPI-07 dwell uses `NOW() - MAX(changed_at)` joined on the most-recent Block event for orders currently in Blocked state. Resume → re-Block correctly resets the dwell because only the latest Block event drives MAX.
- KPI-08 satisfied: `isOverdue` boolean computed server-side via `early_delivery_date < CURRENT_DATE`; null dates never trigger overdue.
- TDD discipline: RED commits BEFORE GREEN commits for all three tasks; security-canary + agreement tests are FIRST RED in each task.
- Zero new dependencies (D-13 preserved).
- `import 'server-only'` on line 1 of `kpis.ts` (RESEARCH.md anti-pattern: never call from client).
</success_criteria>

<output>
After completion, create `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-04-SUMMARY.md` summarizing:
- New files: `src/db/queries/kpis.ts`, `src/db/queries/__tests__/kpis.test.ts`, `src/lib/timezone.ts`, `src/lib/__tests__/timezone.test.ts`.
- Three exported query functions with their cache keys and tags.
- Two exported helper / type declarations: `sanitizeIanaTimezone`, `DEFAULT_TIMEZONE`, `KpiStripData`, `TrendDay`, `BlockedOrderWithDwell`, `FormulaBucket` (re-export note).
- Test counts: 12 (timezone) + 13 (getKpiStrip) + 7 (getSevenDayTrend) + 8 (getBlockedWithDwell) = 40 total.
- Confirmation that no new dependencies were added (D-13) and no `bucketTexture` import in `kpis.ts` body (anti-pattern preserved).
- Any deviations from RESEARCH.md / PATTERNS.md (expected: none).
</output>
