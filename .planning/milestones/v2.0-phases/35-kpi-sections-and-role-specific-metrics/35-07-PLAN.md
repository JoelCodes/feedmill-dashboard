---
phase: 35
plan: 07
type: execute
wave: 4
depends_on: [35-04, 35-05, 35-06]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/page.test.tsx
  - src/components/ProductionDashboard.tsx
  - src/components/ProductionDashboard.test.tsx
  - src/components/MillColumn.tsx
  - src/components/MillColumn.test.tsx
autonomous: false
requirements: [KPI-01, KPI-02, KPI-03, KPI-04, KPI-05, KPI-06, KPI-07, KPI-08]
tags: [integration, rsc, layout, dashboard, mill-column, kpi-03, manual-uat]
must_haves:
  truths:
    - "D-02: `<TzBootstrap />` is mounted in `src/app/layout.tsx` inside `<ThemeProvider><NuqsAdapter>` adjacent to `{children}` — runs once on first load, writes the operator's IANA tz to a cookie"
    - "D-02: `src/app/page.tsx` reads `cookies().get('tz')?.value` and passes the sanitized IANA string into every KPI query via `sanitizeIanaTimezone`-wrapped helpers in `kpis.ts` (defense-in-depth)"
    - "D-07 zone 1: `<KpiStrip kpis={kpiStripData} />` is mounted in `ProductionDashboard.tsx` ABOVE the filter pills row, wrapped in `<Suspense fallback={<KpiStripSkeleton />}>` (D-16)"
    - "D-07 zone 2: `MillColumn` accepts a `summary: ColumnSummary` prop and renders KPI-03 inline in its header — `{N} orders — {completedLbs} / {totalLbs} lbs`"
    - "D-07 zone 3: `<KpiSection trendData={kpiTrend} exceptions={kpiBlocked} />` is mounted in `ProductionDashboard.tsx` BELOW the columns, wrapped in `<Suspense fallback={<KpiSectionSkeleton />}>` (D-16)"
    - "Pitfall 6: KPI-03 column summaries are computed from the UNFILTERED `orders` prop in `ProductionDashboard`, NOT from the `filtered` array used for column rendering — filter pills do not change the header count"
    - "RESEARCH Open Question #2 resolved: KPI-03 computed client-side from the already-fetched `orders` array (no 4th DB query); aligned with the existing `computeColumnWeights` helper"
    - "D-14 cross-action invariant preserved: no new `revalidateTag` calls anywhere in this plan — KPI freshness piggybacks on the existing `'production-orders'` tag invalidations from Phase 33/34 actions"
    - "D-15 polling preserved: KPI props flow through the RSC re-render on every 30s `router.refresh()` from `useProductionPolling` (Phase 34 hook unchanged)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "TzBootstrap mounted adjacent to {children}"
      contains: "TzBootstrap"
    - path: "src/app/page.tsx"
      provides: "Cookie read + parallel KPI query fan-out; KPI props passed to ProductionDashboard"
      contains: "cookies\\|getKpiStrip\\|getSevenDayTrend\\|getBlockedWithDwell"
    - path: "src/components/ProductionDashboard.tsx"
      provides: "KpiStrip + KpiSection slotted; columnSummaries derived from UNFILTERED orders prop; ColumnSummary type"
      exports: ["default ProductionDashboard", "type ColumnSummary"]
    - path: "src/components/MillColumn.tsx"
      provides: "summary: ColumnSummary prop + KPI-03 header strip"
      contains: "summary"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/TzBootstrap.tsx"
      via: "default-imported and rendered inside ThemeProvider"
      pattern: "<TzBootstrap"
    - from: "src/app/page.tsx"
      to: "src/db/queries/kpis.ts"
      via: "Promise.all fan-out includes getKpiStrip, getSevenDayTrend, getBlockedWithDwell"
      pattern: "getKpiStrip\\|getSevenDayTrend\\|getBlockedWithDwell"
    - from: "src/app/page.tsx"
      to: "next/headers"
      via: "cookies() to read tz value"
      pattern: "cookies\\(\\)"
    - from: "src/components/ProductionDashboard.tsx"
      to: "src/components/KpiStrip.tsx,src/components/KpiSection.tsx"
      via: "imports and renders both inside Suspense boundaries"
      pattern: "<KpiStrip|<KpiSection"
    - from: "src/components/MillColumn.tsx"
      to: "src/components/ProductionDashboard.tsx"
      via: "summary prop passed from columnSummaries derived from unfiltered orders"
      pattern: "summary={"
---

<objective>
Final integration plan: wire `<TzBootstrap />` into the root layout, extend `src/app/page.tsx` to read the `tz` cookie and fan-out the three new KPI queries (Plan 35-04) in parallel, slot `<KpiStrip />` and `<KpiSection />` into `ProductionDashboard.tsx` per the D-07 three-zone layout, and extend `MillColumn.tsx` with a `summary` prop for the KPI-03 column-header strip. This is the keystone that brings everything from Plans 35-04 (queries), 35-05 (top zone components), and 35-06 (bottom zone components) into a working dashboard.

Purpose: every requirement KPI-01 through KPI-08 lights up at the end of this plan. KPI-03 specifically lives ENTIRELY here (per RESEARCH.md Open Question #2 — computed client-side from the unfiltered `orders` prop; no separate DB query). Plan 35-04 deliberately does NOT include a `getKpiColumnSummaries` query because the data is already in memory after `getProductionOrders()`; computing `orderCount + completedLbs + totalLbs` per line is a `reduce` over an array. The work this plan does for KPI-03 is therefore mechanical (extend types + add a memoized derivation) — but the data-flow wiring (Pitfall 6: use unfiltered `orders`, not `filtered`) is the trap.

The plan ends with a `checkpoint:human-verify` Manual UAT gate. The KPI dashboard is the operator-facing surface for v2.0; pixel checks, dark-mode checks, mobile-viewport checks, and timezone-cookie-DST checks need eyes on the screen, not automated assertions. The orchestrator decided `workflow.human_verify_mode` was not flagged off in the planning context, so we use a `checkpoint:human-verify` block.

Output: 4 modified files + 3 modified test files; full Jest suite green; live dashboard at `/` renders the complete D-07 three-zone layout against the dev Neon DB; operator UAT confirms KPI-08 overdue badge visibility, KPI-06 chart bars match the 7 days of seeded data, KPI-07 exception list opens drawer on click, and the `tz` cookie writes correctly on first load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-04-PLAN.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-05-PLAN.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-06-PLAN.md
@src/app/layout.tsx
@src/app/page.tsx
@src/app/page.test.tsx
@src/components/ProductionDashboard.tsx
@src/components/MillColumn.tsx
@src/components/MillColumn.test.tsx
@src/components/TzBootstrap.tsx
@src/components/KpiStrip.tsx
@src/components/KpiSection.tsx
@src/db/queries/kpis.ts
@src/lib/timezone.ts
@src/lib/production-derivations.ts

<interfaces>
<!-- Locked types this plan consumes from prior plans. -->

From src/db/queries/kpis.ts (Plan 35-04):
- `export const getKpiStrip: (tz: string) => Promise<KpiStripData>`
- `export const getSevenDayTrend: (tz: string) => Promise<TrendDay[]>`
- `export const getBlockedWithDwell: () => Promise<BlockedOrderWithDwell[]>`
- `export type KpiStripData`, `TrendDay`, `BlockedOrderWithDwell`.

From src/lib/timezone.ts (Plan 35-04):
- `export const DEFAULT_TIMEZONE = 'America/Chicago' as const`

From src/components/KpiStrip.tsx (Plan 35-05):
- `default export KpiStrip({ kpis: KpiStripData })`
- `export { KpiStripSkeleton }`

From src/components/KpiSection.tsx (Plan 35-06):
- `default export KpiSection({ trendData: TrendDay[], exceptions: BlockedOrderWithDwell[] })`
- `export { KpiSectionSkeleton }`

From src/components/TzBootstrap.tsx (Plan 35-05):
- `default export TzBootstrap` — side-effect-only `'use client'` component returning null.

From src/lib/production-derivations.ts (Phase 34):
- `computeColumnWeights(orders: ProductionOrder[]): { completed: number; total: number }` — re-used by Pitfall 6 client-side summary derivation.

New type introduced by this plan (locked):
```typescript
// src/components/ProductionDashboard.tsx (also re-exported for MillColumn.tsx)
export interface ColumnSummary {
  orderCount: number;
  completedLbs: number;
  totalLbs: number;
}
```

MillColumn extension (locked):
```typescript
// Adds the new prop alongside existing ones:
export interface MillColumnProps {
  millLine: MillLine;
  orders: ProductionOrder[];
  onOrderClick: (orderId: string) => void;
  summary: ColumnSummary;  // NEW — KPI-03; required (NOT optional)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MillColumn with KPI-03 summary prop + header strip — RTL test the new format</name>
  <files>
    src/components/MillColumn.tsx,
    src/components/MillColumn.test.tsx
  </files>
  <read_first>
    - src/components/MillColumn.tsx (current shape — note lines 111-118 the existing header to extend; line 108 `computeColumnWeights` call to remove from header)
    - src/components/MillColumn.test.tsx (existing test patterns — extend with the summary prop)
    - src/lib/production-derivations.ts (computeColumnWeights — referenced for the new ProductionDashboard derivation in Task 2)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/components/MillColumn.tsx (EDIT — add `summary` prop)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md § "KpiCard — KPI-03 column header inline" + § "Copywriting Contract" (locked: `{N} orders — {completedLbs} / {totalLbs} lbs`)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-09
  </read_first>
  <behavior>
    - Test 1 (header strip format): `<MillColumn summary={{ orderCount: 4, completedLbs: 18400, totalLbs: 52000 }} ... />` renders the text `4 orders — 18,400 / 52,000 lbs` in the header (Intl thousands separators + em-dash separator). Use exactly `getByText` with the literal string OR an `expect(text).toMatch(/4 orders.*18,400.*52,000.*lbs/)` to allow for nested span granularity.
    - Test 2 (zero-orders edge case): with `summary={{ orderCount: 0, completedLbs: 0, totalLbs: 0 }}`, header renders `0 orders — 0 / 0 lbs` (no special empty-state copy in the header strip; the column body still shows "No orders" — unchanged behavior).
    - Test 3 (text-style classes): the order count span uses `var(--text-primary)` color class; the ratio span uses `var(--text-muted)` color class — both at `--fs-11` font-bold per UI-SPEC.
    - Test 4 (regression): existing MillColumn tests for ordering (Completed → Mixing → Blocked → Pending) + empty-state "No orders" still pass after the header change.
    - Test 5 (TypeScript regression): MillColumn callers without a `summary` prop fail at type-check. Verify indirectly via `npx tsc --noEmit` after this plan — Plan 35-04 and 35-05 do not call MillColumn so no callers need updating until Task 2.
  </behavior>
  <action>
    Step 1 (RED). Open `src/components/MillColumn.test.tsx`. Add Tests 1-3 (new) and verify existing tests still parse (Test 4 — regression). Every existing call to `<MillColumn>` in the test file MUST be updated to pass a `summary={{ orderCount: ..., completedLbs: ..., totalLbs: ... }}` prop; choose values that match what the existing test orders WOULD compute via `computeColumnWeights(orders)` so the new header text matches the prior assertions IF any (most existing tests do not assert on the header text; they assert on order grouping/ordering — those continue to pass).

    Where existing tests pass `orders` like `[makeOrder({state:'Pending', weightLbs:'500.00'}), makeOrder({state:'Completed', weightLbs:'1000.00'})]`, the expected summary would be `{ orderCount: 2, completedLbs: 1000, totalLbs: 1500 }` — pass that value in the test. Several test cases will need updating to add the prop.

    Run `npm test -- --testPathPattern="components/MillColumn\.test"` — confirm new Tests 1-3 are RED (existing header text format `formatWeight(completed) / formatWeight(total) lbs` does NOT include the `N orders — ` prefix). Commit: `test(35-07): RED — MillColumn KPI-03 summary prop + header strip format`.

    Step 2 (GREEN). Open `src/components/MillColumn.tsx`:
    - Extend the props interface:
        Add `export interface ColumnSummary { orderCount: number; completedLbs: number; totalLbs: number; }` near the top of the file (or import from ProductionDashboard — pick: define here, re-export from ProductionDashboard for ergonomic imports).
        Update `MillColumnProps` to add `summary: ColumnSummary;` (NOT optional — required prop; the type system surfaces missing callers as compile errors).
    - REMOVE the `const { completed, total } = computeColumnWeights(orders);` line (currently line 108) — this client-side weight derivation is now passed in via the `summary` prop. The `orders` array is still used below for state-grouping; do NOT remove the `groupOrdersByState(orders)` call (line 107).
    - REPLACE the header `<p>` (currently lines 115-117) with the new KPI-03 format:
        ```
        <p className="mt-1 text-[var(--fs-11)] font-bold">
          <span className="text-[var(--text-primary)]">{summary.orderCount} orders</span>
          <span className="text-[var(--text-muted)]"> — </span>
          <span className="text-[var(--text-muted)]">
            {formatWeight(summary.completedLbs)} / {formatWeight(summary.totalLbs)} lbs
          </span>
        </p>
        ```
        The `formatWeight` helper already exists in MillColumn.tsx (line 35) — re-use it; do NOT inline `.toLocaleString()` calls.
    - VERIFY `computeColumnWeights` is no longer called inside MillColumn.tsx. If the import is now unused (it was used only in the deleted line), remove the import — `npx tsc --noEmit` will not flag unused imports by default but a linter run might. Run `grep -c "computeColumnWeights" src/components/MillColumn.tsx` after — expect 0.

    Run tests — confirm 3 new tests + regression tests GREEN. Commit: `feat(35-07): GREEN — MillColumn accepts summary prop and renders KPI-03 header strip`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/MillColumn\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "summary: ColumnSummary" src/components/MillColumn.tsx` returns ≥ 1 (required prop on the interface).
    - source-assertion: `grep -c "export interface ColumnSummary" src/components/MillColumn.tsx` returns 1 (or `grep -c "import.*ColumnSummary" src/components/MillColumn.tsx` returns 1 if defined elsewhere — pick local definition for this task).
    - source-assertion: `grep -c "summary.orderCount\|summary\\.orderCount" src/components/MillColumn.tsx` returns ≥ 1.
    - source-assertion: `grep -c "summary.completedLbs\|summary\\.completedLbs" src/components/MillColumn.tsx` returns ≥ 1.
    - source-assertion: `grep -c "summary.totalLbs\|summary\\.totalLbs" src/components/MillColumn.tsx` returns ≥ 1.
    - source-assertion: `grep -c "computeColumnWeights(orders)" src/components/MillColumn.tsx` returns 0 (client-side weight derivation removed from MillColumn — moved to ProductionDashboard in Task 2).
    - source-assertion: `grep -c "orders\\b" src/components/MillColumn.tsx` is unchanged with respect to `groupOrdersByState(orders)` (still used).
    - source-assertion: `grep -c "} orders.*—" src/components/MillColumn.tsx` returns ≥ 1 (the KPI-03 format literal).
    - behavior-assertion: `npm test -- --testPathPattern="components/MillColumn\.test"` shows new Tests 1-3 passing AND prior regression tests still passing.
    - CLI output: `npx tsc --noEmit` exits 0 (existing ProductionDashboard.tsx call to <MillColumn> will fail to compile until Task 2 passes the prop — accept that ordering: Task 2 fixes it).

    Note: this task INTENTIONALLY creates a TypeScript error in `ProductionDashboard.tsx` because that file calls `<MillColumn ... />` without a `summary` prop. Task 2 fixes it. The acceptance criterion above (`tsc exits 0`) refers to MillColumn.tsx itself and its own test file — the dashboard tsc-error is expected and is the green signal that Task 2's wiring is required. If you run `npx tsc --noEmit` between Task 1 and Task 2, you will see exactly one error site in `ProductionDashboard.tsx`; that is the contract handoff.
  </acceptance_criteria>
  <done>
    MillColumn requires the `summary` prop; KPI-03 header format matches UI-SPEC copy; 3 new tests + prior tests all green; expected tsc error in ProductionDashboard.tsx is the Task-2 contract handoff signal.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire KPI props through ProductionDashboard — KpiStrip above, KpiSection below, columnSummaries from UNFILTERED orders (Pitfall 6)</name>
  <files>
    src/components/ProductionDashboard.tsx,
    src/components/ProductionDashboard.test.tsx
  </files>
  <read_first>
    - src/components/ProductionDashboard.tsx (current shape — note line 124-129 Props type, line 195-204 ordersByMill derivation from filtered, line 208 root container, line 255-293 Suspense boundaries)
    - src/components/ProductionDashboard.test.tsx (existing test harness — extend with KPI prop fixtures + columnSummaries assertion)
    - src/lib/production-derivations.ts (computeColumnWeights — re-used here, NOT in MillColumn anymore)
    - src/components/KpiStrip.tsx (Plan 35-05 — KpiStrip + KpiStripSkeleton)
    - src/components/KpiSection.tsx (Plan 35-06 — KpiSection + KpiSectionSkeleton)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/components/ProductionDashboard.tsx (EDIT — add KPI zones + TzBootstrap)" — has the exact JSX layout with Suspense
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md § "Layout — Three-Zone Dashboard" (zone order top to bottom)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pitfall 6: KPI-03 Uses Stale `orders` Prop After Filter" + § "Open Question #2" — columnSummaries derive from UNFILTERED `orders`
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-07, D-15, D-16
  </read_first>
  <behavior>
    - Test 1 (KpiStrip mounted ABOVE filter pills): renders the dashboard with KPI props; assert (via test ID or by query order in DOM) that the strip's `aria-label="KPI summary strip"` element appears in document order BEFORE the filter-pill row element.
    - Test 2 (KpiSection mounted BELOW columns): assert the trend chart heading "7-Day Volume Trend" appears AFTER the last column's content in DOM order.
    - Test 3 (Suspense fallback for top zone): the KpiStrip is wrapped in `<Suspense fallback={<KpiStripSkeleton />}>`. Hard to test without runtime suspense, BUT verify the source contains both `<Suspense` and `<KpiStripSkeleton` references near each other. Source-grep assertion (acceptance_criteria).
    - Test 4 (Suspense fallback for bottom zone): same for KpiSection / KpiSectionSkeleton — source-grep assertion.
    - Test 5 (Pitfall 6 — KPI-03 uses unfiltered orders): construct fixture with 4 Premix orders (2 Pending, 2 Completed, weights 500/500/1000/1000); set status filter to ONLY "Completed" so `filtered` has 2 rows; render. Assert the Premix MillColumn header shows `4 orders` (count from unfiltered) NOT `2 orders` (filtered count). This is the CRITICAL Pitfall 6 test.
    - Test 6 (regression): existing dashboard behaviors — column rendering, filter pills, search, drawer opening — all still work after the KPI integration.
    - Test 7 (KPI-03 weight derivation): with the same 4 Premix orders, header `completedLbs` should equal `1000 + 1000 = 2000` and `totalLbs` should equal `500 + 500 + 1000 + 1000 = 3000` (assuming `computeColumnWeights` sums Completed for `completed` and all for `total` — verify against the function's existing contract). Assert the rendered header matches `4 orders — 2,000 / 3,000 lbs`.
    - Test 8 (props pass-through): renders with `kpiStrip` payload and asserts the strip's "Completed Today" card renders with the value derived from `kpiStrip.completedTodayLbs` (Intl-formatted with " lbs" suffix per Plan 35-05).
  </behavior>
  <action>
    Step 1 (RED). Open `src/components/ProductionDashboard.test.tsx`. Add the 8 tests above. The existing test file already has `makeOrder` fixtures and a rendering harness. Construct the necessary KPI prop fixtures inline:
        ```
        const kpiStripFixture: KpiStripData = { completedTodayLbs: '18400', premixLbs: '6000', excelLbs: '8000', cgmLbs: '4400', pendingCount: 5, pendingLbs: '47200', pelletPct: 58, mashPct: 32, crumblePct: 10, uncategorizedCount: 3 };
        const trendFixture: TrendDay[] = []; // Most tests can use the empty-state chart
        const exceptionsFixture: BlockedOrderWithDwell[] = [];
        ```
    Update EVERY existing `render(<ProductionDashboard ... />)` call in the test file to pass the three new props. Run — confirm new tests 1-8 fail AND existing tests fail to compile until the props are added. Commit: `test(35-07): RED — ProductionDashboard KPI zone wiring + Pitfall 6 unfiltered column summaries`.

    Step 2 (GREEN). Open `src/components/ProductionDashboard.tsx`. Apply the changes from PATTERNS.md § "src/components/ProductionDashboard.tsx (EDIT — add KPI zones + TzBootstrap)" sequentially:

    A) Imports (top of file):
        - `import { Suspense } from 'react';` (likely already present from Phase 34 — verify)
        - `import KpiStrip, { KpiStripSkeleton } from '@/components/KpiStrip';`
        - `import KpiSection, { KpiSectionSkeleton } from '@/components/KpiSection';`
        - `import { computeColumnWeights } from '@/lib/production-derivations';` (likely already imported; if removed in Phase 34 cleanup, re-add)
        - `import type { KpiStripData, TrendDay, BlockedOrderWithDwell } from '@/db/queries/kpis';`
        - `import type { ColumnSummary } from '@/components/MillColumn';` (re-import from where Task 1 defined it).

    B) Props type (~line 124-129). Add three new fields:
        ```typescript
        type Props = {
          orders: ProductionOrder[];
          canEdit: boolean;
          drawerOrder: ProductionOrder | null;
          drawerEvents: OrderEvent[];
          kpiStrip: KpiStripData;
          kpiTrend: TrendDay[];
          kpiBlocked: BlockedOrderWithDwell[];
        };
        ```

    C) Derived data section (after existing `ordersByMill` derivation ~line 195-204). Insert the unfiltered columnSummaries derivation using `useMemo`:
        ```typescript
        const columnSummaries = useMemo<Record<MillLine, ColumnSummary>>(() => {
          const lines: MillLine[] = ['Premix', 'Excel', 'CGM'];
          return Object.fromEntries(
            lines.map((line) => {
              const lineOrders = orders.filter((o) => o.millLine === line);  // UNFILTERED — Pitfall 6
              const { completed, total } = computeColumnWeights(lineOrders);
              return [line, { orderCount: lineOrders.length, completedLbs: completed, totalLbs: total }];
            })
          ) as Record<MillLine, ColumnSummary>;
        }, [orders]);  // depends only on `orders` — NOT `filtered`
        ```
        - `useMemo` is needed because `orders` is a prop array reference that may or may not be stable across re-renders; recomputing 3 reduces per render is cheap, but useMemo with `[orders]` makes the dependency explicit and serves as documentation.
        - `orders` (the unfiltered prop array) is the input; `filtered` MUST NOT be used here. RESEARCH.md Pitfall 6.

    D) JSX layout. Inside the root `<div className="flex flex-col gap-5">` (line ~208), restructure to the D-07 three-zone order:
        i) Zone 1 (top): a `<Suspense fallback={<KpiStripSkeleton />}>` wrapping `<KpiStrip kpis={kpiStrip} />`. Insert this BEFORE the existing header strip row (filter pills + search + Import + LastUpdatedChip).
        ii) Existing header strip — unchanged from Phase 34.
        iii) Existing BlockedAlertBand — unchanged.
        iv) Existing three-column board — but each `<MillColumn>` now passes `summary={columnSummaries[line]}`. Update all 3 MillColumn render sites (one per Premix/Excel/CGM).
        v) Zone 3 (bottom): a `<Suspense fallback={<KpiSectionSkeleton />}>` wrapping `<KpiSection trendData={kpiTrend} exceptions={kpiBlocked} />`. Insert this AFTER the columns div and BEFORE the drawer.
        vi) Existing drawer — unchanged.

    E) Verify all three `<MillColumn>` sites pass the correct summary. Use:
        ```
        <MillColumn millLine="Premix" orders={ordersByMill.Premix} summary={columnSummaries.Premix} onOrderClick={...} />
        <MillColumn millLine="Excel"  orders={ordersByMill.Excel}  summary={columnSummaries.Excel}  onOrderClick={...} />
        <MillColumn millLine="CGM"    orders={ordersByMill.CGM}    summary={columnSummaries.CGM}    onOrderClick={...} />
        ```
        Note: `orders={ordersByMill.Premix}` (filtered) is correct for the column BODY — operators want to see filtered orders. But `summary={columnSummaries.Premix}` uses unfiltered data — Pitfall 6.

    Run tests — confirm all 8 new tests + prior regression tests GREEN. Commit: `feat(35-07): GREEN — slot KpiStrip + KpiSection in ProductionDashboard with unfiltered columnSummaries (Pitfall 6)`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/ProductionDashboard\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "<KpiStrip" src/components/ProductionDashboard.tsx` returns exactly 1.
    - source-assertion: `grep -c "<KpiSection" src/components/ProductionDashboard.tsx` returns exactly 1.
    - source-assertion: `grep -c "<KpiStripSkeleton" src/components/ProductionDashboard.tsx` returns exactly 1 (Suspense fallback).
    - source-assertion: `grep -c "<KpiSectionSkeleton" src/components/ProductionDashboard.tsx` returns exactly 1.
    - source-assertion: `grep -c "summary={columnSummaries" src/components/ProductionDashboard.tsx` returns exactly 3 (Premix, Excel, CGM).
    - source-assertion: `grep -c "columnSummaries" src/components/ProductionDashboard.tsx` returns ≥ 4 (definition + 3 usages).
    - source-assertion: `grep -c "useMemo" src/components/ProductionDashboard.tsx` returns ≥ 1 (memoized derivation).
    - source-assertion: `grep -B 2 "orders.filter.*millLine" src/components/ProductionDashboard.tsx | grep -c "filtered"` returns 0 — the new columnSummaries derivation does NOT reference `filtered` array.
    - source-assertion: `grep -c "kpiStrip\|kpiTrend\|kpiBlocked" src/components/ProductionDashboard.tsx` returns ≥ 3 (all three new props consumed).
    - behavior-assertion: `npm test -- --testPathPattern="components/ProductionDashboard\.test"` shows all 8 new tests passing including Test 5 (Pitfall 6 — unfiltered count) and Test 7 (weight derivation).
    - CLI output: `npx tsc --noEmit` exits 0 (the MillColumn contract handoff from Task 1 is now satisfied).
  </acceptance_criteria>
  <done>
    KpiStrip + KpiSection mounted in their correct D-07 zones inside Suspense boundaries; columnSummaries derive from UNFILTERED `orders` array (Pitfall 6); each MillColumn receives a typed summary prop; 8 new tests + regression tests all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire cookie + KPI query fan-out in src/app/page.tsx + mount TzBootstrap in layout</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/page.test.tsx
  </files>
  <read_first>
    - src/app/layout.tsx (current shape — TzBootstrap goes inside ThemeProvider adjacent to {children})
    - src/app/page.tsx (current shape — Promise.all fan-out at lines 40-44 to extend)
    - src/app/page.test.tsx (existing tests — extend with cookie-mock + KPI-query-mock assertions)
    - src/db/queries/kpis.ts (Plan 35-04 — `getKpiStrip`, `getSevenDayTrend`, `getBlockedWithDwell` exports)
    - src/lib/timezone.ts (Plan 35-04 — `DEFAULT_TIMEZONE` constant)
    - src/components/TzBootstrap.tsx (Plan 35-05 — default export)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/app/page.tsx (EDIT — add tz cookie read + KPI queries)" + § "src/components/TzBootstrap.tsx" placement note
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 5: `unstable_cache` Wrapping" + § "Pattern 9" + § "System Architecture Diagram"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-02, D-15
  </read_first>
  <behavior>
    layout.tsx:
    - Test (file-shape, no behavior test): TzBootstrap is rendered inside `<ThemeProvider>` adjacent to `{children}`. Verified by source grep (acceptance_criteria).

    page.tsx:
    - Test 1 (cookie read): mock `next/headers.cookies` to return `{ get: () => ({ value: 'America/New_York' }) }`; render the page; assert the call to `getKpiStrip` was made with `'America/New_York'`. Mock pattern: `jest.mock('@/db/queries/kpis', () => ({ getKpiStrip: jest.fn(() => Promise.resolve({...})), getSevenDayTrend: jest.fn(() => Promise.resolve([])), getBlockedWithDwell: jest.fn(() => Promise.resolve([])) }))`; then `expect(getKpiStrip).toHaveBeenCalledWith('America/New_York')`.
    - Test 2 (cookie absent → fallback): mock `cookies` to return `{ get: () => undefined }`; assert `getKpiStrip` called with `'America/Chicago'` (DEFAULT_TIMEZONE).
    - Test 3 (cookie empty string → fallback): mock `cookies` to return `{ get: () => ({ value: '' }) }`; assert called with `'America/Chicago'`.
    - Test 4 (parallel fan-out): all three KPI queries are dispatched (Promise.all includes them); existing `getProductionOrders` / `getOrderById` / `getOrderEvents` still called. Spy on each and assert call counts.
    - Test 5 (props pass-through): the rendered `<ProductionDashboard>` receives `kpiStrip`, `kpiTrend`, `kpiBlocked` props from the mocked queries. Verify by rendering with a spy on ProductionDashboard or by asserting on a DOM marker the dashboard renders given specific KPI inputs.
    - Test 6 (regression): existing tests (auth redirect, canEdit derivation, drawer hydration) still pass.
  </behavior>
  <action>
    Step 1 (RED — page.test.tsx). Open `src/app/page.test.tsx`. Add Tests 1-5 (Test 6 is regression — keep existing tests). Set up mocks for `cookies` from `next/headers` and for `@/db/queries/kpis`. Run — confirm new tests fail. Commit: `test(35-07): RED — page.tsx cookie read + KPI Promise.all fan-out + DEFAULT_TIMEZONE fallback`.

    Step 2 (GREEN — layout.tsx). Open `src/app/layout.tsx`:
    - Add import: `import TzBootstrap from '@/components/TzBootstrap';`
    - Inside `<ThemeProvider><NuqsAdapter>` adjacent to `{children}` (place BEFORE children so it mounts and sets the cookie before any RSC under children attempts to render — though TzBootstrap's `useEffect` runs after mount regardless, the source-order placement signals intent):
        Currently: `<ThemeProvider><NuqsAdapter>{children}</NuqsAdapter></ThemeProvider>`
        Becomes:   `<ThemeProvider><NuqsAdapter><TzBootstrap />{children}</NuqsAdapter></ThemeProvider>`
    - Do NOT add `<TzBootstrap />` to any other route or page — global layout placement is intentional so the cookie is set once per browser session per page mount.

    Step 3 (GREEN — page.tsx). Open `src/app/page.tsx`:
    - Add imports:
        ```typescript
        import { cookies } from 'next/headers';
        import { getKpiStrip, getSevenDayTrend, getBlockedWithDwell } from '@/db/queries/kpis';
        import { DEFAULT_TIMEZONE } from '@/lib/timezone';
        ```
    - After `const { userId } = await auth(); ... checkRole(...);` and BEFORE the existing `const { order } = await searchParamsCache.parse(searchParams);`, add the cookie read:
        ```typescript
        // D-02: read tz cookie set by <TzBootstrap />; fallback DEFAULT_TIMEZONE when absent or empty.
        // sanitizeIanaTimezone is applied inside each KPI query (defense-in-depth) — Plan 35-04 Pitfall 2.
        const cookieStore = await cookies();
        const tz = cookieStore.get('tz')?.value || DEFAULT_TIMEZONE;
        ```
    - Extend the existing `Promise.all` (currently lines 40-44) to include the three KPI queries:
        ```typescript
        const [orders, drawerOrder, drawerEvents, kpiStrip, kpiTrend, kpiBlocked] = await Promise.all([
          getProductionOrders(),
          order ? getOrderById(order) : Promise.resolve(null),
          order ? getOrderEvents(order) : Promise.resolve([]),
          getKpiStrip(tz),
          getSevenDayTrend(tz),
          getBlockedWithDwell(),  // no tz parameter — dwell is wallclock-relative per D-03
        ]);
        ```
        Note: `getBlockedWithDwell` deliberately omits the `tz` argument (Plan 35-04 Task 3 contract).
    - Pass the three new props to `<ProductionDashboard>`:
        ```typescript
        <ProductionDashboard
          orders={orders}
          canEdit={canEdit}
          drawerOrder={drawerOrder}
          drawerEvents={drawerEvents}
          kpiStrip={kpiStrip}
          kpiTrend={kpiTrend}
          kpiBlocked={kpiBlocked}
        />
        ```
    - Do NOT modify the `auth()` / `checkRole()` / `redirect()` flow. Do NOT remove `export const dynamic = 'force-dynamic';`.

    Step 4. Run `npm test -- --testPathPattern="app/page\.test"` — confirm all new tests + regression GREEN. Commit: `feat(35-07): GREEN — page.tsx cookie read + KPI fan-out + TzBootstrap mounted in layout`.

    Final verification: run `npx tsc --noEmit && npm test` — full project clean.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="app/page\.test" && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "import TzBootstrap" src/app/layout.tsx` returns exactly 1.
    - source-assertion: `grep -c "<TzBootstrap" src/app/layout.tsx` returns exactly 1.
    - source-assertion: `grep -c "cookies" src/app/page.tsx` returns ≥ 1 (cookie API imported and called).
    - source-assertion: `grep -c "getKpiStrip\|getSevenDayTrend\|getBlockedWithDwell" src/app/page.tsx` returns ≥ 3.
    - source-assertion: `grep -c "DEFAULT_TIMEZONE\|'America/Chicago'" src/app/page.tsx` returns ≥ 1 (cookie-absent fallback path).
    - source-assertion: `grep -c "kpiStrip={kpiStrip}\|kpiTrend={kpiTrend}\|kpiBlocked={kpiBlocked}" src/app/page.tsx` returns 3.
    - source-assertion: `grep -c "export const dynamic = 'force-dynamic'" src/app/page.tsx` returns 1 (unchanged from Phase 34).
    - source-assertion: `grep -c "getBlockedWithDwell(tz\|getBlockedWithDwell(rawTz" src/app/page.tsx` returns 0 (no tz argument — Plan 35-04 contract).
    - source-assertion: `grep -c "revalidateTag" src/app/page.tsx` is unchanged from pre-edit (do NOT add any revalidateTag calls).
    - behavior-assertion: `npm test -- --testPathPattern="app/page\.test"` shows all new tests passing including cookie absent/empty fallbacks.
    - CLI output: `npx tsc --noEmit` exits 0 (full project clean).
    - CLI output: `npm test` exits 0 (full Jest suite green — every plan in Phase 35 has landed correctly).
  </acceptance_criteria>
  <done>
    layout.tsx mounts TzBootstrap; page.tsx reads tz cookie + fans out 6 queries in parallel + passes KPI props to ProductionDashboard; cookie-absent/empty paths fall back to DEFAULT_TIMEZONE; full Jest + tsc clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — operator verifies the live KPI dashboard end-to-end</name>
  <files>
    (none — manual verification of the running dev server; no source files modified)
  </files>
  <what-built>
    The complete Phase 35 dashboard at `/`:
    - Top zone: KpiStrip with 6 cards (Completed Today / Premix / Excel / CGM / Pending Backlog / Formula Mix), wrapped in Suspense.
    - Filter pills + search + Import button + LastUpdatedChip — unchanged from Phase 34.
    - BlockedAlertBand — unchanged from Phase 34.
    - Three columns (Premix / Excel / CGM) with KPI-03 header strips showing `{N} orders — {completed} / {total} lbs`.
    - Bottom zone: KpiSection containing SevenDayTrendChart (KPI-06) and BlockedExceptionList (KPI-07 + KPI-08), wrapped in Suspense.
    - TzBootstrap writes the operator's IANA timezone to a `tz` cookie on first page load.
    - All KPI queries use the cookie's tz value (sanitized server-side) for daily window calculations.
    - 30-second polling (Phase 34 useProductionPolling) re-runs every query and refreshes every zone.

    The integration is code-complete with the full Jest suite green and tsc --noEmit clean. The Manual UAT verifies behaviors that automated tests cannot economically cover: visual layout, dark-mode rendering, mobile viewport stacking, DST boundary handling, actual cookie write/read, and the operator's perception of the empty states.
  </what-built>
  <how-to-verify>
    1. Pre-flight: confirm dev DB is seeded with Phase 35 data. Run `npm run db:seed` if not already done (Plan 35-01 Task 3); confirm via `psql "$DATABASE_URL_UNPOOLED" -c "SELECT COUNT(*) FROM production_orders WHERE early_delivery_date IS NOT NULL"` returns 33.

    2. Start the dev server: `npm run dev`. Open http://localhost:3000/ in Chrome.

    3. Authenticate as the `mill_operator` demo user (Phase 31 setup); if the manual Clerk Dashboard cutover (STATE.md "Open Blockers") is not complete, this gate is unreachable — surface that to the operator and pause.

    4. Verify zone 1 (KpiStrip):
       - 6 cards render in a horizontal flex row, left to right: Completed Today | Premix Today | Excel Today | CGM Today | Pending Backlog | Formula Mix.
       - Cards 1, 5, 6 each show a teal-colored icon container on the right (Wheat / ClipboardList / Activity).
       - Values are non-zero (the dev DB has seed data — every line should have at least some Completed orders given how transitions were exercised in Phase 33-34 testing). If "Completed Today" reads "0 lbs", that is correct iff no orders have moved to Completed today — confirm with the operator.
       - Formula Mix card shows the dominant bucket (e.g., "58% Pellet") + subValue with the other two buckets + a footnote "Excludes N uncategorized orders" if any seeded orders have NULL texture_type.

    5. Verify zone 2 (per-column KPI-03):
       - Each of Premix / Excel / CGM columns has a header showing `{N} orders — {completedLbs} / {totalLbs} lbs` exactly per UI-SPEC copywriting contract.
       - Toggle a status filter pill (e.g., select only "Pending"). The COLUMN content changes (only Pending cards render), but the HEADER STRIP counts MUST NOT change — they reflect the unfiltered totals. This is the Pitfall 6 manual verification.
       - Reset filters.

    6. Verify zone 3 (KpiSection):
       - SevenDayTrendChart renders 7 SVG bars (since dev DB has multiple days of seeded orders from Phase 33-34 testing). If fewer than 7 days of data exist, the empty state "Not enough data yet" + "Check back after 7 days of production" displays — confirm this is correct given the actual data state.
       - BlockedExceptionList renders one row per currently-Blocked order with the dwell-time column populated (e.g., "2h 14m") and an "Overdue" badge on rows whose `early_delivery_date` is past AND state is not Completed.
       - If no blocked orders exist, the empty state "No blocked orders" displays.

    7. Verify KPI-08 overdue badge:
       - With the seeded data spread `today ±5 days` (Plan 35-01 Task 3), some Blocked orders (if any exist in the seed) should have past `early_delivery_date` values triggering the overdue badge.
       - To force-test: in psql, `UPDATE production_orders SET state='Blocked', early_delivery_date=CURRENT_DATE - 3 WHERE order_number='<some_order>';` then reload — the order should appear in BlockedExceptionList with the Overdue badge. Revert after test.

    8. Verify KPI-07 row click → drawer:
       - Click any row in BlockedExceptionList.
       - The drawer opens with the clicked order's details.
       - The URL gains `?order=<id>`.
       - Closing the drawer removes the query param.

    9. Verify `tz` cookie:
       - Open DevTools → Application → Cookies → http://localhost:3000.
       - Confirm a cookie named `tz` exists with a value matching `Intl.DateTimeFormat().resolvedOptions().timeZone` in the browser console.
       - Open a private/incognito window without the cookie. Reload `/`. KPI values for the first paint may use `America/Chicago` (default), then re-render with the operator's actual tz on the next polling tick (≤ 30s). Acceptable behavior per CONTEXT.md "build-time risks" section.

    10. Verify Suspense skeletons:
       - Throttle Slow 3G in DevTools Network panel.
       - Reload `/`. The KpiStripSkeleton and KpiSectionSkeleton placeholders should be visible briefly before the live data swaps in.

    11. Verify dark mode:
       - Toggle dark mode in the UI (or via `prefers-color-scheme` in DevTools).
       - All KPI components render with the dark palette (verify card backgrounds, text colors, warning badge contrast).

    12. Verify mobile viewport:
       - Switch DevTools to a mobile-sized viewport (e.g., iPhone 12).
       - KpiStrip uses horizontal overflow-x-auto scrolling (cards don't wrap).
       - KpiSection stacks vertically (SevenDayTrendChart above BlockedExceptionList).

    13. Verify 30s polling refresh:
       - Wait 30 seconds while watching the LastUpdatedChip and the KPI values.
       - The chip's timestamp updates; if any mutation happened (e.g., a transition action) in another tab, KPI values should re-aggregate.

    14. Verify no console errors:
       - Confirm browser console has no errors or React hydration warnings (especially for the hand-rolled SVG — RESEARCH.md "build-time risks" flags hydration as a concern; if you see "Hydration failed" related to the SVG, escalate — likely a date-formatting non-determinism between server and client render).

    If ANY of steps 4-14 fail or look wrong (visual misalignment, wrong copy, missing badge, etc.), describe the failure precisely (which step, what was observed, screenshot if helpful) and pause before approving.
  </how-to-verify>
  <resume-signal>
    Type "kpi dashboard approved" when steps 4-14 all pass. If any step needs follow-up (e.g., visual polish), describe the gap and either (a) defer to a v2.1 follow-up (preserving the Phase 35 ship) or (b) request a targeted fix plan.
  </resume-signal>
  <action>
    Manual UAT performed by the operator on the running dev server (`npm run dev`). The executor's responsibility is to confirm the dev server boots cleanly (`npm run build` + `npm run dev` succeed), then pause for the operator to follow the steps in <how-to-verify>. Once the operator types the resume-signal "kpi dashboard approved" (or describes any blocking issues), the checkpoint resolves. No automated execution path replaces the operator's eyes for visual layout, dark mode, mobile viewport, and DST/timezone cookie validation.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
    <human-check>Operator follows steps 4-14 in <how-to-verify> and signs off via the resume-signal.</human-check>
  </verify>
  <acceptance_criteria>
    - Manual: operator confirms zones 1-3 render per UI-SPEC.
    - Manual: filter-pill toggle does NOT change column header KPI-03 counts (Pitfall 6 verification).
    - Manual: at least one Overdue badge is visible (forced via psql update if natural seed data doesn't produce one).
    - Manual: BlockedExceptionList row click opens drawer; URL updates with `?order=<id>`.
    - Manual: `tz` cookie present in DevTools with operator's IANA timezone value.
    - Manual: Suspense skeletons visible on slow network throttle.
    - Manual: dark mode renders correctly.
    - Manual: mobile viewport stacks KpiSection vertically; KpiStrip scrolls horizontally.
    - Manual: 30s polling refresh ticks without breaking KPI values.
    - Manual: zero console errors / hydration warnings.
  </acceptance_criteria>
  <done>
    Operator approves the live dashboard end-to-end; Phase 35 is ship-ready. Any deferred polish items are captured as v2.1 todos, NOT as Phase 35 gap-closure plans (the requirements bar — KPI-01..KPI-08 lit — is met).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `<TzBootstrap />` writes `tz` cookie → `src/app/page.tsx` reads via `cookies()` | Operator-controlled cookie. Sanitized at the SQL boundary by Plan 35-04's `sanitizeIanaTimezone`. Defense-in-depth: page.tsx falls back to `DEFAULT_TIMEZONE` on missing cookie. |
| `src/app/page.tsx` → Plan 35-04's KPI queries | Server-only flow via `import 'server-only'`. No client can bypass the auth gate at line 30 (`if (!userId) redirect('/sign-in')`). |
| `<ProductionDashboard>` (client) renders KPI payloads | Aggregate metrics, non-PII. `canEdit` gate still controls transition affordances inside the drawer (unchanged from Phase 34). |
| `<MillColumn>` summary prop (KPI-03) | Computed client-side from the unfiltered orders array. No DB access from the component itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 | T — Tampering | `tz` cookie injected by browser extension | mitigate | Sanitized at every consumer: page.tsx falls back to DEFAULT_TIMEZONE on empty/null, kpis.ts (Plan 35-04 Task 1) validates against `Intl.supportedValuesOf` allowlist. Two-tier defense. |
| T-35-07-02 | T — Tampering | Pitfall 6 — KPI-03 header count manipulated by filter pills | mitigate | Test 5 in Task 2 explicitly tests this scenario (4 unfiltered orders, status filter set to Completed only, header still reads "4 orders"). The Pitfall is structural — `columnSummaries` derivation uses `orders` not `filtered`; future regression surfaces as a failing test. |
| T-35-07-03 | I — Information Disclosure | KPI payloads visible to non-mill_operator authenticated users | accept | D-17: all authenticated users see KPIs (no role gate on display). `canEdit` still gates transition buttons in the drawer for users without `mill_operator`. |
| T-35-07-04 | R — Repudiation | KPI queries return stale data after a mutation | mitigate | D-14 invariant: Phase 33's mutating actions already call `revalidateTag('production-orders')`; KPIs are wrapped with the same tag in Plan 35-04 → invalidate on every mutation. Plan 35-07 does NOT add any new `revalidateTag` call. |
| T-35-07-05 | D — Denial of Service | Promise.all fan-out of 6 queries per request | mitigate | All 6 queries are `unstable_cache`-wrapped; cache hit rate is high during normal operator flow. Worst case: every 30 seconds (polling tick) OR on each mutating action. Neon dev DB capacity is far above 6 cached queries per 30s. |
| T-35-07-06 | E — Elevation of Privilege | Manual UAT step 7 (force-flag an order overdue via psql) | accept | psql access requires `DATABASE_URL_UNPOOLED` — operator-only credential. The UPDATE is a UAT test fixture, not a runtime path. Document revert step in the UAT instructions. |
| T-35-07-07 | T — Tampering | Hydration mismatch in SevenDayTrendChart (weekday label) | mitigate | The chart's `weekdayShort()` helper anchors the date to noon UTC and formats with `timeZone: 'UTC'` — deterministic across server and client. Manual UAT step 14 watches for hydration warnings as a canary. |
| T-35-07-08 | I — Information Disclosure | `tz` cookie name predictable; readable by any subdomain | accept | The cookie value is non-sensitive (just an IANA timezone string). Path scoped to `/`; same-site Lax. No PII risk. |
| T-35-07-SC | T — Tampering (supply chain) | npm installs | n/a | Zero new package installs in this plan (D-13). The only imports introduced (`next/headers.cookies`, project-local types) are already pinned. |
</threat_model>

<verification>
- `npm test` — full Jest suite green across all 7 plans of Phase 35.
- `npx tsc --noEmit` — TypeScript clean.
- `grep -c '<TzBootstrap' src/app/layout.tsx` returns 1.
- `grep -c '<KpiStrip\|<KpiSection' src/components/ProductionDashboard.tsx` returns 2.
- `grep -c 'getKpiStrip\|getSevenDayTrend\|getBlockedWithDwell' src/app/page.tsx` returns 3.
- `grep -c 'summary={columnSummaries' src/components/ProductionDashboard.tsx` returns 3.
- `grep -c 'revalidateTag' src/app/page.tsx` is unchanged from pre-edit count (Phase 35 introduces no new tag invalidations — D-14).
- `cat package.json | grep -E '"(recharts|visx|@nivo|chart\.js|d3|moment|date-fns-tz)"' returns 0 lines (no new dependencies in any Phase 35 plan — D-13 holistically verified).
- Manual UAT (Task 4 checkpoint) approved by operator.
</verification>

<success_criteria>
- KPI-01 through KPI-08 all lit and visible on the live dashboard at `/`.
- D-02 implemented: TzBootstrap mounted in layout; page.tsx reads `tz` cookie via `cookies()`; fallback to `DEFAULT_TIMEZONE`.
- D-07 three-zone layout satisfied: KpiStrip top, MillColumn KPI-03 headers, KpiSection bottom — each in its prescribed position.
- D-15 polling preserved: KPI props flow through the existing 30s `router.refresh()` cycle from Phase 34's `useProductionPolling` hook.
- D-16 per-zone Suspense boundaries: KpiStripSkeleton + KpiSectionSkeleton render during streaming.
- Pitfall 6 closed: KPI-03 column summaries computed from UNFILTERED `orders` array (Test 5 in Task 2 is the canary).
- D-14 preserved: zero new `revalidateTag` calls in Phase 35; KPI invalidation piggybacks on existing `'production-orders'` tag.
- D-13 holistically preserved: `package.json` shows zero new dependencies across all 7 plans.
- Manual UAT approved: visual, dark-mode, mobile, DST, cookie, and polling behaviors confirmed by operator.
- v2.0 milestone unblocked: all 38 v2.0 requirements satisfied (AUTH-01..AUTH-04, DATA-01..DATA-08, PROD-01..PROD-11, TRANS-01..TRANS-07, IMPORT-01..IMPORT-07, KPI-01..KPI-08).
</success_criteria>

<output>
After completion, create `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-07-SUMMARY.md` summarizing:
- 4 files modified (layout.tsx, page.tsx, ProductionDashboard.tsx, MillColumn.tsx) + 3 test files extended.
- New type introduced: `ColumnSummary { orderCount, completedLbs, totalLbs }`.
- The 6-query parallel Promise.all in page.tsx (3 existing from Phase 34 + 3 new from Plan 35-04).
- Cookie read fallback chain: `cookies().get('tz')?.value || DEFAULT_TIMEZONE` (page.tsx) → `sanitizeIanaTimezone(rawTz)` (kpis.ts) — defense-in-depth documented.
- Pitfall 6 mitigation: `columnSummaries` derived from unfiltered `orders` (Test 5 in Task 2).
- Test counts: 3 MillColumn + 8 ProductionDashboard + 5 page.tsx = 16 new tests; plus regression-protected prior tests.
- Manual UAT outcome (steps 4-14 with the operator's specific notes / sign-off).
- v2.0 milestone completion summary: all 38 requirements satisfied; phase ready for `/gsd:verify-work` and milestone close.
- Any deviations from PATTERNS.md / RESEARCH.md (expected: none; if the Manual UAT surfaced cosmetic gaps, capture them as v2.1 follow-ups, NOT as Phase 35 gap-closure plans).
</output>
