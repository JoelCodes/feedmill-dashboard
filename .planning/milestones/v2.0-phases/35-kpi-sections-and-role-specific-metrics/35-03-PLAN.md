---
phase: 35
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/formula-mix.ts
  - src/lib/__tests__/formula-mix.test.ts
  - src/lib/format-dwell.ts
  - src/lib/__tests__/format-dwell.test.ts
autonomous: true
requirements: [KPI-05, KPI-07]
tags: [tdd, pure-helper, formula-mix, dwell, lib]
must_haves:
  truths:
    - "D-11: `bucketTexture(raw)` maps 'PELLET' | 'SH PELLET' → 'Pellet', 'MASH' → 'Mash', 'FINE CR' | 'C. CRUMBLE' → 'Crumble', NULL/unrecognized → null"
    - "D-11: comparisons are case-sensitive on the canonical uppercase DB form; lowercase 'pellet' returns null"
    - "D-12: `bucketTexture` returns null for both NULL and unrecognized inputs — KPI-05 SQL will use this for both numerator AND denominator exclusion"
    - "UI-SPEC: `formatDwell(epochSeconds)` returns '{N}m' for < 3600, '{N}h {M}m' for < 86400, '{N}d {M}h' for ≥ 86400"
    - "Both helpers are pure (no DB, no browser APIs, no React) — safely importable from RSC + client + Node test runner"
  artifacts:
    - path: "src/lib/formula-mix.ts"
      provides: "Pure helper bucketTexture(raw: string | null): 'Pellet' | 'Mash' | 'Crumble' | null"
      exports: ["bucketTexture"]
    - path: "src/lib/__tests__/formula-mix.test.ts"
      provides: "Exhaustive tests over all 5 known textures + NULL + 4 unrecognized strings"
    - path: "src/lib/format-dwell.ts"
      provides: "Pure helper formatDwell(epochSeconds: number): string"
      exports: ["formatDwell"]
    - path: "src/lib/__tests__/format-dwell.test.ts"
      provides: "Tests over <1h, 1-24h, 24h+ boundaries and edge cases (0s, exact thresholds)"
  key_links:
    - from: "src/lib/formula-mix.ts"
      to: "(none — pure)"
      via: "no imports"
      pattern: "^export function bucketTexture"
    - from: "src/lib/format-dwell.ts"
      to: "(none — pure)"
      via: "no imports"
      pattern: "^export function formatDwell"
---

<objective>
TDD-build two pure server-importable helpers consumed by the KPI query layer in Plan 35-04 and the exception list in Plan 35-06:
1. `bucketTexture(raw: string | null): 'Pellet' | 'Mash' | 'Crumble' | null` — implements D-11's locked texture-bucketing rule. Used as the JS-side parallel of the SQL CASE expression in KPI-05; their agreement is enforced by a unit test (CONTEXT.md "build-time risks" section + RESEARCH.md anti-pattern "Never call `bucketTexture()` from the SQL query").
2. `formatDwell(epochSeconds: number): string` — formats Postgres `EXTRACT(EPOCH FROM (NOW() - MAX(changed_at)))` output for the BlockedExceptionList table cells (UI-SPEC Dwell time format).

Purpose: both helpers are independent of Plan 35-01's schema migration AND Plan 35-04's query layer — they can be built in parallel with Wave 1. Plan 35-04 imports `bucketTexture` to drive the agreement test (which proves the SQL CASE in `kpis.ts` matches this helper for every known input). Plan 35-06's `BlockedExceptionList` uses `formatDwell` to render dwell strings server-side or client-side (CONTEXT.md leaves the formatting tier open; D-03 says "Display formatting is a UI concern, locked via UI-SPEC" — choose server-side formatting and pass the formatted string in the KPI payload to keep the client component dumb).

Output: two pure files + two test files; RED → GREEN → REFACTOR commit sequence; no new dependencies; no React, no DB, no `'use server'` / `'use client'` directives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md
@src/lib/production-derivations.ts
@src/lib/__tests__/production-derivations.test.ts

<interfaces>
<!-- Pure-lib file shape (from `src/lib/production-derivations.ts` — the canonical analog). -->

Both files MUST:
- Have NO `'use server'` and NO `'use client'` directives.
- Have NO imports (these are dependency-free).
- Export named functions only (no default export — matches `production-derivations.ts` convention).
- Live alongside `production-derivations.ts` in `src/lib/`.
- Be importable from BOTH RSC and client contexts without modification.

Test files:
- Located at `src/lib/__tests__/{name}.test.ts` (matches existing `production-derivations.test.ts` location).
- Use Jest 30 + plain assertions (no React Testing Library needed — pure functions).
- No mocks; the helpers have no dependencies to mock.

Type contracts (locked):
```
// src/lib/formula-mix.ts
export type FormulaBucket = 'Pellet' | 'Mash' | 'Crumble';
export function bucketTexture(raw: string | null): FormulaBucket | null;

// src/lib/format-dwell.ts
export function formatDwell(epochSeconds: number): string;
```

`FormulaBucket` is exported for downstream type-safety in `kpis.ts` and `BlockedExceptionList.tsx`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD bucketTexture (RED → GREEN → REFACTOR)</name>
  <files>
    src/lib/formula-mix.ts,
    src/lib/__tests__/formula-mix.test.ts
  </files>
  <read_first>
    - src/lib/production-derivations.ts (the canonical pure-lib shape — no imports, named exports, JSDoc on each function)
    - src/lib/__tests__/production-derivations.test.ts (test file structure to mirror — plain Jest, no mocks)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/lib/formula-mix.ts (utility, transform)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 2: Formula Mix Bucketing — SQL CASE Expression"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-11, D-12
  </read_first>
  <behavior>
    Test suite for `bucketTexture(raw)`:
    - Test 1: `bucketTexture('PELLET') === 'Pellet'`
    - Test 2: `bucketTexture('SH PELLET') === 'Pellet'`
    - Test 3: `bucketTexture('MASH') === 'Mash'`
    - Test 4: `bucketTexture('FINE CR') === 'Crumble'`
    - Test 5: `bucketTexture('C. CRUMBLE') === 'Crumble'`
    - Test 6: `bucketTexture(null) === null`
    - Test 7 (case sensitivity, D-11 explicit): `bucketTexture('pellet') === null` — lowercase input is NOT recognized.
    - Test 8 (case sensitivity): `bucketTexture('Pellet') === null` — title-case input is NOT recognized; only the canonical UPPERCASE DB form is mapped.
    - Test 9 (whitespace): `bucketTexture('PELLET ') === null` — trailing space is NOT trimmed; DB values are stored as-is.
    - Test 10 (unrecognized): `bucketTexture('CHUNKY') === null` — unknown texture returns null (D-11 "any future raw value not in this list is treated as null until the mapping is updated explicitly").
    - Test 11 (empty string): `bucketTexture('') === null` — empty string is not recognized.

    All 11 tests RED before implementation; all 11 GREEN after.
  </behavior>
  <action>
    Step 1 (RED). Create `src/lib/__tests__/formula-mix.test.ts` with the 11 tests above. Use the test file structure from `production-derivations.test.ts`: one top-level `describe('bucketTexture', () => { ... })` block with `it('...', () => expect(...).toBe(...))` cases. No imports beyond `import { bucketTexture } from '@/lib/formula-mix';` (the helper does not yet exist — TypeScript will surface a `Cannot find module` error; that IS the RED state).

    Run `npm test -- --testPathPattern="lib/__tests__/formula-mix\.test"` — confirm all 11 tests fail (module-not-found OR all assertions fail once a stub exists). Commit: `test(35-03): RED — assert bucketTexture maps 5 textures + NULL + unrecognized correctly`.

    Step 2 (GREEN). Create `src/lib/formula-mix.ts` per the PATTERNS.md template:
    - JSDoc block at file top explaining D-11 rules + case-sensitivity contract + that this is the JS-side parallel of the SQL CASE expression in `kpis.ts`.
    - Export `type FormulaBucket = 'Pellet' | 'Mash' | 'Crumble';` (used by downstream code; locked union).
    - Export `function bucketTexture(raw: string | null): FormulaBucket | null` implemented as a `switch (raw)` over the 5 known cases, default returns null. Use the switch syntax from PATTERNS.md verbatim (fall-through cases for PELLET/SH PELLET and FINE CR/C. CRUMBLE).
    - Do NOT use `.toUpperCase()` / `.trim()` / `.toLowerCase()` — D-11 explicitly locks case-sensitive comparison on the canonical DB form. Pre-processing would silently absorb data-quality issues that should surface as `null` and be reported via the KPI-05 "N uncategorized" footnote.

    Run tests — confirm all 11 GREEN. Commit: `feat(35-03): GREEN — implement bucketTexture with D-11 case-sensitive bucketing rules`.

    Step 3 (REFACTOR). Read the implementation. If a cleaner shape emerges (e.g., a frozen Record literal for lookups), apply it ONLY IF all 11 tests stay green AND TypeScript still strictly narrows the return type to `FormulaBucket | null` (no widening to `string | null`). Otherwise skip REFACTOR — the switch is already idiomatic and matches the canonical pattern. Commit only if a change was made: `refactor(35-03): <description>`.

    Do NOT add a CASE-expression-agreement test here. That test belongs in Plan 35-04 (`kpis.test.ts`), which imports `bucketTexture` to drive the SQL parallel.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="lib/__tests__/formula-mix\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "^export function bucketTexture" src/lib/formula-mix.ts` returns exactly 1.
    - source-assertion: `grep -c "^export type FormulaBucket" src/lib/formula-mix.ts` returns exactly 1.
    - source-assertion: `grep -c "'PELLET'" src/lib/formula-mix.ts` returns exactly 1, similarly for `'SH PELLET'`, `'MASH'`, `'FINE CR'`, `'C. CRUMBLE'`.
    - source-assertion: `grep -c "toUpperCase\|toLowerCase\|\\.trim" src/lib/formula-mix.ts` returns 0 (no normalization — D-11 case-sensitive).
    - source-assertion: `grep -c "^import" src/lib/formula-mix.ts` returns 0 (zero imports — pure helper).
    - source-assertion: `grep -c "'use server'\|'use client'" src/lib/formula-mix.ts` returns 0 (no directives — importable from any context).
    - behavior-assertion: `npm test -- --testPathPattern="lib/__tests__/formula-mix\.test"` shows 11 tests passing, 0 failing.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `bucketTexture` is pure, dependency-free, exhaustively tested over D-11's locked bucket rules; downstream Plan 35-04 can import it for the SQL CASE agreement test.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD formatDwell (RED → GREEN → REFACTOR)</name>
  <files>
    src/lib/format-dwell.ts,
    src/lib/__tests__/format-dwell.test.ts
  </files>
  <read_first>
    - src/lib/production-derivations.ts (the pure-lib shape — match its style)
    - src/lib/__tests__/production-derivations.test.ts (test file structure)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/lib/format-dwell.ts (utility, transform)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Dwell-time Duration Formatting (pure TS helper)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md (Dwell time format rows + Copywriting Contract — locks the exact string output)
  </read_first>
  <behavior>
    Test suite for `formatDwell(epochSeconds)`:

    Sub-hour bucket (< 3600 seconds → `{N}m`):
    - Test 1: `formatDwell(0) === '0m'`
    - Test 2: `formatDwell(59) === '0m'` — under one minute floors to 0m (intentional; UI-SPEC does not show seconds).
    - Test 3: `formatDwell(60) === '1m'`
    - Test 4: `formatDwell(2520) === '42m'` (UI-SPEC sample value).
    - Test 5: `formatDwell(3599) === '59m'` — last second in the < 3600 bucket.

    Hour bucket (3600 ≤ s < 86400 → `{N}h {M}m`):
    - Test 6: `formatDwell(3600) === '1h 0m'` — first second crossing 1h boundary.
    - Test 7: `formatDwell(8040) === '2h 14m'` (UI-SPEC sample: 2*3600 + 14*60 = 8040).
    - Test 8: `formatDwell(86399) === '23h 59m'` — last second in the < 86400 bucket.

    Day bucket (≥ 86400 → `{N}d {M}h`):
    - Test 9: `formatDwell(86400) === '1d 0h'` — first second crossing 1d boundary.
    - Test 10: `formatDwell(97200) === '1d 3h'` (UI-SPEC sample: 86400 + 3*3600 = 97200).
    - Test 11: `formatDwell(7*86400 + 5*3600) === '7d 5h'` — week-long block, still uses days+hours (no week unit per UI-SPEC).

    All 11 tests RED before implementation; all 11 GREEN after.
  </behavior>
  <action>
    Step 1 (RED). Create `src/lib/__tests__/format-dwell.test.ts` with the 11 tests above. Same shape as `production-derivations.test.ts` — one `describe('formatDwell', ...)` with 11 `it(...)` cases. Import: `import { formatDwell } from '@/lib/format-dwell';`. Run the suite — confirm all 11 fail. Commit: `test(35-03): RED — assert formatDwell formats <1h, 1-24h, 24h+ correctly`.

    Step 2 (GREEN). Create `src/lib/format-dwell.ts` with the exact body from RESEARCH.md / PATTERNS.md:
    - JSDoc explaining the three buckets + that input is epoch SECONDS (not milliseconds — comes from `EXTRACT(EPOCH FROM interval)` in SQL).
    - `export function formatDwell(epochSeconds: number): string` with three branches:
      `if (epochSeconds < 3600) return ${Math.floor(epochSeconds / 60)}m;`
      `if (epochSeconds < 86400) { const h = ...; const m = ...; return ${h}h ${m}m; }`
      `const d = ...; const h = ...; return ${d}d ${h}h;`
    - Use `Math.floor` (integer division). Do NOT use `Math.round` — operator copy expects truncation per UI-SPEC ("2h 14m" not "2h 15m" for 2h 14m 30s).
    - Do NOT accept null/undefined inputs. Caller passes `number` (Pattern 3 in RESEARCH.md uses `sql<number>` for the EXTRACT EPOCH output). If a future caller might pass null, that's a TypeScript error site; resolve at the call site, not by polluting this pure helper with a null guard.

    Run tests — confirm all 11 GREEN. Commit: `feat(35-03): GREEN — implement formatDwell with UI-SPEC three-bucket format`.

    Step 3 (REFACTOR). The branch structure is already simple; skip REFACTOR unless a clear improvement emerges that keeps all 11 tests green and the public signature unchanged.

    No agreement-with-SQL test here (formatDwell is purely UI-side; SQL only produces the epochSeconds number, formatting is JS-side per D-03 + UI-SPEC).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="lib/__tests__/format-dwell\.test"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "^export function formatDwell" src/lib/format-dwell.ts` returns exactly 1.
    - source-assertion: `grep -c "epochSeconds: number" src/lib/format-dwell.ts` returns ≥ 1 (function signature).
    - source-assertion: `grep -c "3600\|86400" src/lib/format-dwell.ts` returns ≥ 2 (both bucket thresholds present).
    - source-assertion: `grep -c "Math.floor" src/lib/format-dwell.ts` returns ≥ 2 (integer truncation, NOT rounding).
    - source-assertion: `grep -c "Math.round" src/lib/format-dwell.ts` returns 0.
    - source-assertion: `grep -c "^import" src/lib/format-dwell.ts` returns 0 (pure helper, no imports).
    - source-assertion: `grep -c "'use server'\|'use client'" src/lib/format-dwell.ts` returns 0.
    - behavior-assertion: `npm test -- --testPathPattern="lib/__tests__/format-dwell\.test"` shows 11 tests passing.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `formatDwell` is pure, dependency-free, exhaustively tested at all three UI-SPEC bucket boundaries; downstream Plan 35-04 will call it server-side to pre-format the BlockedExceptionList dwell strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure helpers (no I/O) | These functions take primitive inputs and return primitive outputs. There is no external boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | T — Tampering | DB row with unknown texture_type triggering null exclusion | accept | D-11 explicitly locks "unknown returns null; UI surfaces an N uncategorized footnote." This is by design, not a vulnerability. |
| T-35-03-02 | I — Information Disclosure | bucketTexture / formatDwell exposed to client bundle | accept | Both helpers are pure value mapping. No secrets, no PII, no DB credentials. Safe to ship to the browser. |
| T-35-03-03 | D — Denial of Service | formatDwell with extreme negative or NaN input | accept | Input contract is `number` (epoch seconds from SQL EXTRACT). Caller-side guarantees non-negative finite values. Defensive null/NaN handling would mask upstream bugs — the type system is the contract. |
| T-35-03-04 | E — Elevation of Privilege | n/a | accept | No auth surface; pure value transforms. |
| T-35-03-SC | T — Tampering (supply chain) | npm installs | n/a | Zero new package installs (D-13). |
</threat_model>

<verification>
- `npm test -- --testPathPattern="lib/__tests__/(formula-mix|format-dwell)\.test"` — 22 tests green (11 + 11).
- `npx tsc --noEmit` — clean across `src/`.
- `grep -c '^import' src/lib/formula-mix.ts src/lib/format-dwell.ts` — both return 0 (pure helpers).
- Plan 35-04 will consume `bucketTexture` for the SQL CASE agreement test AND `formatDwell` for server-side pre-formatting of the BlockedExceptionList payload.
</verification>

<success_criteria>
- D-11 implemented: `bucketTexture` exhaustively covers the 5 known canonical textures + NULL + unrecognized; case-sensitive on uppercase DB form.
- D-12 prep: `bucketTexture` returns null for both NULL and unrecognized; downstream KPI-05 SQL uses this same logic in its CASE expression to exclude these from numerator AND denominator.
- UI-SPEC Dwell time format implemented: three buckets (`{N}m`, `{N}h {M}m`, `{N}d {H}h`) with `Math.floor` truncation.
- Both helpers are pure and dependency-free; importable from RSC, client, and Node test contexts without modification.
- TDD discipline: RED commit BEFORE GREEN commit for both helpers; REFACTOR commits only if a real improvement emerged.
- Zero new dependencies (D-13 preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-03-SUMMARY.md` summarizing:
- The two new files (`src/lib/formula-mix.ts`, `src/lib/format-dwell.ts`) with their export shapes.
- Total test counts: 11 formula-mix + 11 format-dwell = 22 unit tests green.
- The RED → GREEN → REFACTOR commit chain (3-6 commits total depending on whether REFACTOR yielded a change).
- Note that the SQL-CASE-agreement test for `bucketTexture` will be added in Plan 35-04, not here (downstream dependency).
- Any deviations from RESEARCH.md / PATTERNS.md (expected: none).
</output>
