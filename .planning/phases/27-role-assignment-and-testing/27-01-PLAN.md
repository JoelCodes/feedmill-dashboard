---
phase: 27-role-assignment-and-testing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/auth.test.ts
autonomous: true
requirements: [ACCESS-02]
requirements_addressed: [ACCESS-02]
tags: [clerk, rbac, auth, server-utilities, tdd]
must_haves:
  truths:
    - "D-01: Role utilities live in src/lib/auth.ts (auth-focused module)"
    - "D-02: checkRole(role: Role): Promise<boolean> reads auth().sessionClaims?.metadata?.role with no network call; returns false on missing session/claim"
    - "D-03: requireRole(role: Role): Promise<void> uses next/navigation redirect — redirect('/') on wrong role, redirect('/sign-in') on missing userId"
    - "D-08: Unit tests cover matching role, mismatched role, missing sessionClaims, missing userId, and redirect targets for requireRole (8 cases via jest.mock('@clerk/nextjs/server'))"
    - "checkRole('demo') returns true when sessionClaims.metadata.role === 'demo'"
    - "checkRole(role) returns false on any missing-data path (no sessionClaims, no metadata.role, no userId, mismatched role)"
    - "requireRole(role) redirects to /sign-in when userId is missing"
    - "requireRole(role) redirects to / when the role does not match"
    - "requireRole(role) resolves without throwing when the role matches"
  artifacts:
    - path: "src/lib/auth.test.ts"
      provides: "Unit tests for checkRole + requireRole (8 cases)"
      contains: "describe('checkRole'"
    - path: "src/lib/auth.test.ts"
      provides: "requireRole describe block"
      contains: "describe('requireRole'"
    - path: "src/lib/auth.ts"
      provides: "checkRole + requireRole exports"
      exports: ["checkRole", "requireRole"]
  key_links:
    - from: "src/lib/auth.ts"
      to: "@clerk/nextjs/server"
      via: "import { auth }"
      pattern: "import \\{ auth \\} from ['\"]@clerk/nextjs/server['\"]"
    - from: "src/lib/auth.ts"
      to: "next/navigation"
      via: "import { redirect }"
      pattern: "import \\{ redirect \\} from ['\"]next/navigation['\"]"
    - from: "src/lib/auth.ts"
      to: "@/types/clerk"
      via: "import type { Role }"
      pattern: "import type \\{ Role \\} from ['\"]@/types/clerk['\"]"
---

<objective>
Deliver server-only role utility functions `checkRole(role)` and `requireRole(role)` in `src/lib/auth.ts`, fully TDD-driven against `src/lib/auth.test.ts`. These utilities are the ACCESS-02 deliverable — any server component or route handler needing role-aware logic uses them.

Purpose: ACCESS-02 requires role utility functions available for server components. The utility reads `auth().sessionClaims?.metadata?.role` (no network call) so it inherits the same role-source as the migrated middleware (D-02, D-03, D-04).

Output:
- `src/lib/auth.test.ts` — Jest behavior suite, 8 test cases, sentinel-throw mock for `redirect`.
- `src/lib/auth.ts` — two exported async functions with JSDoc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md
@.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md
@.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md
@src/types/clerk.d.ts
@src/middleware.ts
@src/middleware.test.ts
@src/lib/utils.ts
@src/lib/utils.test.ts

<interfaces>
<!-- Types and contracts the executor uses. Extracted verbatim from codebase. -->

From `src/types/clerk.d.ts` (already declared — no changes needed):
- `export type Role = 'demo' | 'admin' | 'user'`
- Global `CustomJwtSessionClaims { metadata: { role?: Role } }` — this means `(await auth()).sessionClaims?.metadata?.role` is automatically typed as `Role | undefined`.

From `@clerk/nextjs/server`:
- `auth(): Promise<{ userId: string | null; sessionClaims: CustomJwtSessionClaims | null; ... }>` — v7+ async API, already used in `src/middleware.ts:30`.

From `next/navigation`:
- `redirect(url: string): never` — server-only primitive that throws `NEXT_REDIRECT` sentinel internally. Callers do NOT need to `return` after it. Not currently used anywhere server-side in this codebase; introduced cleanly here.

Target signatures (per D-02, D-03):
- `export async function checkRole(role: Role): Promise<boolean>`
- `export async function requireRole(role: Role): Promise<void>`
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Author src/lib/auth.test.ts with 8 failing test cases</name>
  <files>src/lib/auth.test.ts</files>
  <read_first>
    - `src/middleware.test.ts` (lines 1-12) — project-canonical jest.mock placement pattern; mock declared BEFORE import (Pattern C from PATTERNS.md)
    - `src/lib/utils.test.ts` — describe-per-export convention in src/lib/ (Pattern D from PATTERNS.md)
    - `src/app/demo/customers/page.test.tsx` (lines 6-11) — existing project precedent for mocking `next/navigation` module surface
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pattern 3 — sentinel-throw redirect mock pattern (the canonical reference for the mock shape)
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`src/lib/auth.test.ts` — adapted mock shape for `auth()` + sentinel-throw `redirect()`
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decision D-08 — enumerated test cases the suite must cover
  </read_first>
  <behavior>
    The suite MUST contain exactly these eight `it(...)` cases (matching D-08 enumeration + RESEARCH §Phase Requirements → Test Map):

    describe('checkRole'):
      - Test 1: "returns true when claim matches" — auth returns `{ userId: 'u1', sessionClaims: { metadata: { role: 'demo' } } }`, `checkRole('demo')` resolves to `true`.
      - Test 2: "returns false when claim does not match" — sessionClaims.metadata.role is `'admin'`, `checkRole('demo')` resolves to `false`.
      - Test 3: "returns false when sessionClaims is undefined" — auth returns `{ userId: 'u1', sessionClaims: undefined }`, `checkRole('demo')` resolves to `false`.
      - Test 4: "returns false when metadata.role is missing" — auth returns `{ userId: 'u1', sessionClaims: { metadata: {} } }`, `checkRole('demo')` resolves to `false`.
      - Test 5: "returns false when userId is null (unauthenticated)" — auth returns `{ userId: null, sessionClaims: null }`, `checkRole('demo')` resolves to `false`.

    describe('requireRole'):
      - Test 6: "redirects to /sign-in when userId is missing" — auth returns `{ userId: null, sessionClaims: null }`, awaiting `requireRole('demo')` rejects with the sentinel error carrying url `/sign-in`.
      - Test 7: "redirects to / when role does not match" — auth returns `{ userId: 'u1', sessionClaims: { metadata: { role: 'user' } } }`, awaiting `requireRole('demo')` rejects with the sentinel error carrying url `/`.
      - Test 8: "resolves without throwing when role matches" — auth returns `{ userId: 'u1', sessionClaims: { metadata: { role: 'demo' } } }`, `await requireRole('demo')` resolves with `undefined` and does NOT throw.
  </behavior>
  <action>
    Create `src/lib/auth.test.ts`. At the top of the file (BEFORE any `import` of the module under test), declare these mocks:

    1. `const mockAuth = jest.fn();` then `jest.mock('@clerk/nextjs/server', () => ({ auth: () => mockAuth() }))`. Reset `mockAuth` in a `beforeEach`.
    2. `jest.mock('next/navigation', () => ({ redirect: (url: string) => { throw Object.assign(new Error('NEXT_REDIRECT'), { url }); } }))`. The sentinel-throw shape is critical — a plain `jest.fn()` would let the function under test continue executing past the redirect call and mask bugs (RESEARCH Pitfall 5).

    AFTER the mocks, `import { checkRole, requireRole } from './auth';`.

    Use the describe-per-export convention from `src/lib/utils.test.ts`: one `describe('checkRole', ...)` block and one `describe('requireRole', ...)` block, each containing the cases listed in `<behavior>`. Test names MUST match the strings in `<behavior>` verbatim — the Per-Task Verification Map in `27-VALIDATION.md` calls them out by `-t "<exact name>"`.

    For each `checkRole` test, set up `mockAuth.mockResolvedValue({...})` with the case-specific shape, then `expect(await checkRole('demo')).toBe(true|false)`.

    For `requireRole` redirect cases (Tests 6, 7), use `await expect(requireRole('demo')).rejects.toMatchObject({ url: '/sign-in' })` (or `'/'`). For Test 8, use `await expect(requireRole('demo')).resolves.toBeUndefined()`.

    DO NOT create `src/lib/auth.ts` yet. This is the RED step — the test file imports from a non-existent module, so the suite MUST fail with a module-resolution error or unresolved-export error. Verify the failure mode before moving on.
  </action>
  <verify>
    <automated>npm test -- src/lib/auth.test.ts 2>&1 | grep -E "Cannot find module|FAIL src/lib/auth.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/auth.test.ts` exists.
    - `grep -c "^  it(" src/lib/auth.test.ts` returns at least `8`.
    - `grep -F "describe('checkRole'" src/lib/auth.test.ts` matches at least one line.
    - `grep -F "describe('requireRole'" src/lib/auth.test.ts` matches at least one line.
    - `grep -F "jest.mock('@clerk/nextjs/server'" src/lib/auth.test.ts` matches.
    - `grep -F "jest.mock('next/navigation'" src/lib/auth.test.ts` matches.
    - `grep -F "NEXT_REDIRECT" src/lib/auth.test.ts` matches (sentinel-throw error name).
    - `npm test -- src/lib/auth.test.ts` exits non-zero (RED state — `./auth` module does not exist yet).
    - The 8 exact test-name strings from `<behavior>` are present in the file (verified via `grep -F "returns true when claim matches"` etc. — each name must match).
  </acceptance_criteria>
  <done>
    RED step complete: 8 test cases are written, the suite fails because `src/lib/auth.ts` does not exist, mock shapes are sentinel-throw for `redirect` and standard `jest.fn()` for `auth`. Commit message: `test(27-01): add failing tests for checkRole and requireRole (RED)`.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement src/lib/auth.ts so all 8 tests pass</name>
  <files>src/lib/auth.ts</files>
  <read_first>
    - `src/lib/auth.test.ts` (just created in Task 1) — the contract being satisfied
    - `src/middleware.ts` (lines 1-3, 29-43) — project-canonical `await auth()` destructure shape (Pattern A from PATTERNS.md); imports come from `@clerk/nextjs/server` exactly like middleware
    - `src/lib/utils.ts` — JSDoc-per-export convention for `src/lib/*` modules (Pattern D from PATTERNS.md)
    - `src/types/clerk.d.ts` — confirms `Role` type and `CustomJwtSessionClaims.metadata.role` declaration (no type changes needed; auto-narrows at the call site)
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pattern 1 — target shape for both functions
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decisions D-01, D-02, D-03 — locked behavior contract
  </read_first>
  <action>
    Create `src/lib/auth.ts` with exactly two exported async functions matching the D-02 and D-03 contracts. Imports (in this order):
    1. `import { auth } from '@clerk/nextjs/server'` — same package the middleware uses.
    2. `import { redirect } from 'next/navigation'` — the server-side redirect primitive that throws `NEXT_REDIRECT` internally.
    3. `import type { Role } from '@/types/clerk'` — verbatim path alias from `src/middleware.ts:3`.

    `checkRole(role: Role): Promise<boolean>` — async, destructure `sessionClaims` from `await auth()`, return `sessionClaims?.metadata?.role === role`. Per D-02: no network call, no throws, returns `false` on every missing-data path. Add a JSDoc paragraph that documents the contract (mirror the `src/lib/utils.ts` JSDoc style).

    `requireRole(role: Role): Promise<void>` — async, destructure `{ userId, sessionClaims }` from `await auth()`. Per D-03 and RESEARCH Pattern 1, branch in this exact order:
    1. If `!userId`, call `redirect('/sign-in')` and let it throw the sentinel.
    2. Otherwise if `sessionClaims?.metadata?.role !== role`, call `redirect('/')`.
    3. Otherwise return (implicit `undefined`).

    Do NOT wrap `redirect()` in try/catch — the sentinel-throw is the intended control flow. Do NOT add a fallback to `clerkClient.users.getUser()` (D-06: clean migration). Do NOT add admin-bypass or role-hierarchy logic (REQUIREMENTS.md "Out of Scope: Nested role hierarchies").

    Add a top-of-file JSDoc paragraph documenting that this module is server-only (server components and route handlers only — never imported into client components).
  </action>
  <verify>
    <automated>npm test -- src/lib/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/auth.ts` exists.
    - `grep -F "import { auth } from '@clerk/nextjs/server'" src/lib/auth.ts` matches OR equivalent double-quoted import matches.
    - `grep -F "import { redirect } from 'next/navigation'" src/lib/auth.ts` matches OR equivalent double-quoted import matches.
    - `grep -F "import type { Role } from '@/types/clerk'" src/lib/auth.ts` matches OR equivalent.
    - `grep -E "export async function checkRole\(role: Role\): Promise<boolean>" src/lib/auth.ts` matches (exact signature per D-02).
    - `grep -E "export async function requireRole\(role: Role\): Promise<void>" src/lib/auth.ts` matches (exact signature per D-03).
    - `grep -F "sessionClaims?.metadata?.role" src/lib/auth.ts` matches in at least two places (checkRole + requireRole both read the claim).
    - `grep -F "redirect('/sign-in')" src/lib/auth.ts` matches (D-03: unauthenticated path).
    - `grep -F "redirect('/')" src/lib/auth.ts` matches (D-03: wrong-role path; mirrors D-01 middleware behavior).
    - `grep -F "clerkClient" src/lib/auth.ts` returns no matches (D-04, D-06: no fallback to network call).
    - `npm test -- src/lib/auth.test.ts` exits zero with all 8 cases reported as passing.
  </acceptance_criteria>
  <done>
    GREEN step complete: All 8 tests pass against the new implementation. Commit message: `feat(27-01): implement checkRole and requireRole utilities (GREEN)`.
  </done>
</task>

<task type="auto">
  <name>Task 3 (REFACTOR + suite check): JSDoc polish and full-suite regression</name>
  <files>src/lib/auth.ts</files>
  <read_first>
    - `src/lib/auth.ts` (just created in Task 2)
    - `src/lib/utils.ts` — JSDoc shape and `@example` block convention
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decisions D-02, D-03 — exact behavioral contract to mirror in JSDoc
  </read_first>
  <action>
    Polish `src/lib/auth.ts` JSDoc. Each function gets:
    - A one-line summary
    - A paragraph explaining the server-only nature and the no-network-call guarantee (D-02 for checkRole) / the never-returns-when-redirecting note (D-03 for requireRole — call out that callers don't need `return` after `requireRole`)
    - An `@param role` line typed as the project's `Role` union
    - An `@returns` line
    - An `@example` block (mirror the shape in `src/lib/utils.ts`); for `checkRole`, the example shows a server-component conditional render; for `requireRole`, the example shows a guarded server-component file

    Do NOT change function signatures, control flow, or imports — this step is JSDoc-only. Re-run the auth.test.ts suite to confirm the refactor did not regress behavior, then run the full Jest suite to catch any unintended cross-file impact (no cross-file impact is expected because nothing imports from `src/lib/auth.ts` yet — Phase 28 / future phases will be the first consumers).
  </action>
  <verify>
    <automated>npm test -- src/lib/auth.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^ \* @example" src/lib/auth.ts` returns at least `2` (one example per function).
    - `grep -c "^ \* @param" src/lib/auth.ts` returns at least `2`.
    - `grep -c "^ \* @returns" src/lib/auth.ts` returns at least `2`.
    - `npm test -- src/lib/auth.test.ts` exits zero with all 8 cases passing.
    - `npm test` exits zero — the full Jest suite (304 baseline tests per STATE.md) plus the new 8 tests all pass. No prior tests regress.
    - `npx tsc --noEmit` exits zero — TypeScript narrowing via `CustomJwtSessionClaims` works correctly without manual casts.
  </acceptance_criteria>
  <done>
    REFACTOR step complete: JSDoc improved, behavior preserved, full Jest suite green, TypeScript clean. Commit message: `refactor(27-01): polish JSDoc for checkRole and requireRole (REFACTOR)`.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- src/lib/auth.test.ts` — 8 cases pass.
2. `npm test` — full suite green (no regressions; baseline 304 + 8 new = at least 312 passing).
3. `npx tsc --noEmit` — zero TypeScript errors.
4. `grep -F "export async function checkRole" src/lib/auth.ts` — signature present.
5. `grep -F "export async function requireRole" src/lib/auth.ts` — signature present.
6. `grep -F "clerkClient" src/lib/auth.ts` — no matches (clean session-claim migration, per D-04 / D-06).
</verification>

<success_criteria>
- `src/lib/auth.ts` exports `checkRole(role: Role): Promise<boolean>` and `requireRole(role: Role): Promise<void>` per D-02 and D-03.
- `src/lib/auth.test.ts` contains 8 named test cases (5 for checkRole, 3 for requireRole) per D-08, all passing.
- Full Jest suite remains green.
- Module is purely session-claim-based: no `clerkClient` import or `getUser()` call (D-04, D-06).
- Three atomic commits land (RED → GREEN → REFACTOR).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Component | Untrusted user identity becomes trusted only after `auth()` resolves with a verified session JWT. `src/lib/auth.ts` runs on the trusted side. |
| Session JWT → Application | Clerk signs the JWT; `auth()` validates the signature. The `metadata.role` claim is trustworthy iff the JWT signature is valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Spoofing | `checkRole` reads `sessionClaims.metadata.role` | mitigate | `auth()` from `@clerk/nextjs/server` verifies the session JWT signature before returning claims. We do not decode the JWT manually (RESEARCH §Don't Hand-Roll: "Reading role from JWT"). |
| T-27-02 | Elevation of Privilege | A client component imports `checkRole` and trusts the boolean | mitigate | `src/lib/auth.ts` top-of-file JSDoc explicitly documents server-only usage. `auth()` from `@clerk/nextjs/server` throws when invoked on the client — runtime enforcement reinforces the documentation. Phase 28 will audit imports of this module. |
| T-27-03 | Tampering | Missing `metadata.role` defaults to access denial | mitigate | `checkRole` returns `false` for every missing-data path (Tests 3, 4, 5); `requireRole` redirects for every non-match path. Default-deny posture — no fallback to "assume role" anywhere. |
| T-27-04 | Information Disclosure | Role check failure leaks role info via redirect target | accept | Per D-03, wrong-role redirects to `/` (a public Coming Soon page) — same target as middleware redirect (Phase 25 D-01). No role information is leaked through the redirect URL or any error message. |
</threat_model>

<output>
After completion, create `.planning/phases/27-role-assignment-and-testing/27-01-SUMMARY.md`
</output>
