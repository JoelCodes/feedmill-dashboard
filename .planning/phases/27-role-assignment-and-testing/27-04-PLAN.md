---
phase: 27-role-assignment-and-testing
plan: 04
type: execute
wave: 2
depends_on: [27-03]
# files_modified is intentionally empty: this plan modifies external state (Clerk
# Dashboard) and `.env.local` (operator-managed and gitignored). `.env.local` is
# referenced under must_haves.artifacts as expected operator state — it is NOT a
# tracked repo file, and including it in `files_modified` would create a
# misleading "modified file" signal in commit/CI tooling that scans this field.
# Per checker warning #7: this comment documents the intentional omission.
files_modified: []
notes:
  files_modified_rationale: ".env.local is operator-managed and gitignored. It appears under must_haves.artifacts as expected operator state but is intentionally absent from files_modified because no repo-tracked file is changed by this plan."
autonomous: false
requirements: [ACCESS-02]
requirements_addressed: [ACCESS-02]
tags: [clerk-dashboard, manual, jwt-template, test-users, role-assignment, checkpoint]
must_haves:
  truths:
    - "D-13: Joel creates the three test users manually in the Clerk Dashboard (no backend-API seeding script in this phase)"
    - "D-05: Clerk Dashboard JWT template is activated with {\"metadata\": {\"role\": \"{{user.public_metadata.role}}\"}} per docs/clerk-setup.md"
    - "D-12: Three real Clerk users exist with the documented email patterns (e2e-demo→demo, e2e-norole→none, e2e-admin→admin)"
    - "Clerk Dashboard JWT template is active with {\"metadata\": {\"role\": \"{{user.public_metadata.role}}\"}}"
    - "publicMetadata.role is set to 'demo' on e2e-demo, 'admin' on e2e-admin, and is NOT SET on e2e-norole"
    - ".env.local contains the six E2E_* keys with real passwords for the three test users"
    - "Verification cookie decode confirms metadata.role claim is present in the JWT after sign-in"
  artifacts:
    # Note: .env.local is gitignored and operator-managed. It is referenced here
    # as the EXPECTED OPERATOR STATE this plan produces, NOT as a repo artifact.
    # frontmatter.files_modified is intentionally empty (see top-of-file comment).
    - path: ".env.local"
      provides: "Real E2E credentials for Playwright global setup (operator-managed; gitignored)"
      contains: "E2E_DEMO_USER_PASSWORD="
  key_links:
    - from: "Clerk Dashboard JWT template"
      to: "Session token issued at sign-in"
      via: "Sessions → Customize session token configuration"
      pattern: "metadata.role claim populated in JWT payload"
    - from: "publicMetadata.role on each user"
      to: "JWT metadata.role at next sign-in"
      via: "{{user.public_metadata.role}} substitution"
      pattern: "role value matches Dashboard publicMetadata"
---

<objective>
Perform the manual Clerk Dashboard configuration required for Phase 27 E2E tests: activate the JWT template, create three test users, assign their publicMetadata.role values, and populate `.env.local` with their credentials. Verify each user's session token contains the expected role claim before unblocking Plan 05.

Purpose: Plan 05 (Playwright E2E fixtures) cannot run green without these three preconditions. `@clerk/testing` signs the test users in via Clerk's testing-token endpoint, but the user records must already exist in the Clerk dev instance and their JWT template must be active. The Dashboard is the only place these values are configured.

Output:
- Clerk Dashboard JWT template configured (no repo artifact — Dashboard-side state).
- Three Clerk users created in the dev instance with correct publicMetadata.role.
- `.env.local` updated with `E2E_*_PASSWORD` values for the three users (gitignored — not committed; intentionally absent from frontmatter `files_modified` per the top-of-file comment).
- Operator confirmation that the demo-user JWT decodes with the expected metadata.role claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/clerk-setup.md
@.env.example
@.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md
@.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md
@src/types/clerk.d.ts

<interfaces>
<!-- This plan's "interface" is the Clerk Dashboard UI + the .env.local key shape. -->

From `docs/clerk-setup.md` (created by Plan 03):
- The single source of truth for the manual steps. Plan 04 is executed against this runbook.
- JWT template JSON (Step 1): `{"metadata": {"role": "{{user.public_metadata.role}}"}}`
- Three users (Step 2): emails `e2e-demo+clerk_test@example.com`, `e2e-norole+clerk_test@example.com`, `e2e-admin+clerk_test@example.com`
- Role assignment (Step 3): publicMetadata `{"role": "demo"}` for e2e-demo, `{"role": "admin"}` for e2e-admin; e2e-norole is left without publicMetadata.role set.

From `.env.example` (modified by Plan 03):
- Six key names already documented: `E2E_DEMO_USER_EMAIL`, `E2E_DEMO_USER_PASSWORD`, `E2E_NOROLE_USER_EMAIL`, `E2E_NOROLE_USER_PASSWORD`, `E2E_ADMIN_USER_EMAIL`, `E2E_ADMIN_USER_PASSWORD`.
- This task populates the three `_PASSWORD` values in `.env.local` (the operator chooses the passwords during Dashboard user creation).

From `src/types/clerk.d.ts`:
- The claim shape in the JWT must match `CustomJwtSessionClaims.metadata.role: Role | undefined` where `Role = 'demo' | 'admin' | 'user'`.
- Decoded JWT payload after sign-in MUST contain a top-level `metadata` object with a `role` string (or absence-of-role for e2e-norole).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Clerk Dashboard JWT template and create three test users</name>
  <files>(no repo files; Dashboard-side state + .env.local which is gitignored and operator-managed)</files>
  <read_first>
    - `docs/clerk-setup.md` — the runbook to follow step-by-step
    - `.env.example` — confirms the key names that need values in `.env.local`
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decisions D-05, D-07, D-12, D-13, D-14 — locked decisions for this manual step
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Common Pitfalls (Pitfalls 1, 2, 4) — order-of-operations and dev-instance caveats
  </read_first>
  <what-built>
    Plan 03 has already shipped `docs/clerk-setup.md` (the runbook), `.env.example` (the documented key names), and the `playwright/.clerk/` line in `.gitignore`. This task does NOT produce any committed repo artifact — it produces Clerk-Dashboard-side state and a populated `.env.local`. Nothing in this task can be automated from inside the repo: the Clerk Dashboard is a web UI managed by Clerk, not a CLI surface available to this codebase, and D-13 explicitly keeps user creation manual (no `@clerk/backend` script in this phase).
  </what-built>
  <action>
    This is a checkpoint:human-action task — the operator (Joel) executes the manual steps below in the Clerk Dashboard and in `.env.local`. The agent's role is to wait for the `verified` resume-signal before unblocking downstream plans. See `<how-to-verify>` for the six-step procedure; the procedure is the action.

    Summary of the operator's action sequence (full detail in `<how-to-verify>` and `docs/clerk-setup.md`):
    1. Save the JWT template `{"metadata": {"role": "{{user.public_metadata.role}}"}}` in Clerk Dashboard → Sessions → Customize session token.
    2. Create three users (`e2e-demo`, `e2e-norole`, `e2e-admin`) in Clerk Dashboard → Users → Add user using the `+clerk_test@example.com` mailbox suffix.
    3. Edit each user's publicMetadata to set `role` (demo, admin; leave e2e-norole unset).
    4. Populate `.env.local` with the three passwords against the six `E2E_*` keys.
    5. Sign in as `e2e-demo` and decode the JWT cookie at jwt.io to confirm `metadata.role: "demo"` is present.
    6. Confirm `.env.local` uses `pk_test_*` (dev-instance) keys, not `pk_live_*`.
  </action>
  <how-to-verify>
    Follow `docs/clerk-setup.md` in order. The runbook is the authoritative procedure; the steps below are a checklist that this task is complete.

    1. **JWT template active** (Step 1 in runbook):
       - Visit https://dashboard.clerk.com → select the dev instance (the one whose `pk_test_*` / `sk_test_*` keys are in `.env.local`).
       - Navigate to Sessions → Customize session token.
       - Paste exactly `{"metadata": {"role": "{{user.public_metadata.role}}"}}` into the template body.
       - Click Save. Verify the saved template body matches verbatim (no smart quotes, no trailing whitespace).

    2. **Three users created** (Step 2 in runbook):
       - Visit Users → Add user (top right).
       - For each row in the runbook's user table:
         - Set the email to the documented value (e.g., `e2e-demo+clerk_test@example.com`).
         - Set a password — choose any strong password; this is the value you will paste into `.env.local`.
         - Disable email verification if the dev instance has it enabled (the `+clerk_test` mailbox suffix skips actual delivery, but the dev instance may still require verification — use the Dashboard's "Mark as verified" option if needed).
       - Confirm all three users now appear in the Users list.

    3. **Roles assigned via publicMetadata** (Step 3 in runbook):
       - Click `e2e-demo+clerk_test@example.com` → Metadata → Edit publicMetadata → paste `{"role": "demo"}` → Save.
       - Click `e2e-admin+clerk_test@example.com` → Metadata → Edit publicMetadata → paste `{"role": "admin"}` → Save.
       - Do NOT touch `e2e-norole+clerk_test@example.com`'s publicMetadata — it stays unset. This user is the negative control for D-11 scenario #2.

    4. **`.env.local` populated**:
       - Copy each line from `.env.example` that begins with `E2E_` into `.env.local` (if not already present).
       - For each `E2E_*_EMAIL` line, the value from `.env.example` (the `e2e-{role}+clerk_test@example.com` form) is already correct — keep as-is.
       - For each `E2E_*_PASSWORD` line, paste the password you set during Dashboard user creation in Step 2.
       - Save the file. Confirm `.env.local` is gitignored (`grep -F '.env.local' .gitignore` should match — verify this BEFORE saving any passwords).

    5. **Smoke verification — JWT decode** (per RESEARCH Pitfall 1 fail-fast guidance):
       - Open the app in an incognito browser tab.
       - Sign in as `e2e-demo+clerk_test@example.com` using the password you just set.
       - Open browser devtools → Application → Cookies → find the `__session` cookie value.
       - Paste the cookie value into https://jwt.io and decode the payload.
       - Confirm the payload contains: `"metadata": {"role": "demo"}`. If this field is missing, the JWT template did not save correctly or the user did not have publicMetadata.role set before this sign-in — go back to Step 1 or Step 3 as appropriate.
       - Sign out and repeat for `e2e-norole`: decoded JWT payload's `metadata` should either be absent or have no `role` field.

    6. **Pre-flight env-shape check** (per RESEARCH Pitfall 4 / Assumption A1):
       - Run from the repo root: `grep -E '^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_' .env.local` — MUST match. If your `.env.local` has `pk_live_…` keys, `@clerk/testing` will reject testing tokens and Plan 05 will fail. Switch to a dev instance before proceeding.

    All six conditions above must hold before resuming. If any check fails, fix the corresponding Dashboard or `.env.local` state and re-verify.
  </how-to-verify>
  <verify>
    Manual verification only — see `<how-to-verify>` for the six-step operator checklist. There is no automated assertion possible from within the repo (the Clerk Dashboard state is not introspectable, `.env.local` is gitignored). The downstream automated proof comes from Plan 05's `e2e/global.setup.ts` run.
  </verify>
  <acceptance_criteria>
    - Operator has replied `verified` and confirmed all six checks in `<how-to-verify>` passed.
    - JWT template body in Clerk Dashboard matches `{"metadata": {"role": "{{user.public_metadata.role}}"}}` exactly (operator-confirmed visually in Dashboard).
    - Three users exist in the Clerk dev instance with the documented emails.
    - `e2e-demo` user's publicMetadata is `{"role": "demo"}` (operator-confirmed in Dashboard).
    - `e2e-admin` user's publicMetadata is `{"role": "admin"}` (operator-confirmed in Dashboard).
    - `e2e-norole` user's publicMetadata does not contain a `role` field (operator-confirmed in Dashboard).
    - `.env.local` (gitignored) contains all six `E2E_*` keys with non-empty `_PASSWORD` values (operator-confirmed locally).
    - JWT decode of the demo user's session cookie at jwt.io shows `metadata.role: "demo"` (operator-confirmed).
    - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` in `.env.local` begins with `pk_test_` (operator-confirmed via grep).
  </acceptance_criteria>
  <resume-signal>
    Reply `verified` if and only if all six checks above pass.

    If any check fails or you hit a blocker, describe the failure mode (e.g., "JWT decoded payload has metadata: {} but no role field" or "Dashboard rejects the JSON template with error: ...") so the agent can advise on remediation before retrying.
  </resume-signal>
  <done>
    Operator has replied `verified` and all acceptance criteria are satisfied. The Clerk dev instance is in the correct state for Plan 05's Playwright global setup to authenticate each role and persist storage state.
  </done>
</task>

</tasks>

<verification>
This plan has no automated verification — the work happens in Clerk's Dashboard and in `.env.local` (gitignored, operator-managed; intentionally absent from frontmatter `files_modified` per the top-of-file comment). The verification is operator confirmation via the checkpoint resume-signal.

Plan 05's Wave 3 `global.setup.ts` is the first automated proof that this plan's manual state is correct: if the JWT template is missing, every user's sign-in produces a session without `metadata.role`, and the demo-user's access to `/demo/orders` fails (it redirects to `/`). If users do not exist, `clerk.signIn` fails with an unauthenticated error. Both failure modes are caught by Plan 05's first test run, providing a downstream check on this plan's correctness.
</verification>

<success_criteria>
- Operator has signed off via the `verified` resume-signal.
- Clerk Dashboard JWT template body matches `{"metadata": {"role": "{{user.public_metadata.role}}"}}` exactly.
- Three users exist in the Clerk dev instance: `e2e-demo+clerk_test@example.com` (role: demo), `e2e-norole+clerk_test@example.com` (no role), `e2e-admin+clerk_test@example.com` (role: admin).
- `.env.local` contains all six `E2E_*` keys with non-empty values for the three `_PASSWORD` keys.
- A sign-in as `e2e-demo` produces a session cookie whose decoded JWT contains `metadata.role: "demo"`.
- Repo `pk_test_*` / `sk_test_*` keys confirmed as dev-instance (no `pk_live_*`).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Clerk Dashboard | The operator is trusted to follow `docs/clerk-setup.md` accurately. Clerk Dashboard requires the operator's Dashboard admin credentials. |
| Repo → Clerk Backend | No repo-side network connection to Clerk Backend is made in this plan; all changes flow through the operator's browser session to the Clerk Dashboard UI. |
| `.env.local` → Test runner | The Playwright global setup (Plan 05) reads passwords from `.env.local` via dotenv. `.env.local` is gitignored; passwords never enter the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Information Disclosure | Test user passwords leaked via accidental commit | mitigate | `.env.local` is gitignored (verified before saving). Plan 04 explicitly instructs the operator to confirm gitignore status before writing passwords. Pre-commit hook (if any) will refuse to add `.env.local`. |
| T-27-13 | Spoofing | An external attacker discovers test user passwords and signs in | accept | Test users are dev-instance only — they have no access to production data. The `+clerk_test` mailbox suffix prevents any password-reset email from reaching a real inbox. Risk is bounded to the dev environment. |
| T-27-14 | Tampering | Dashboard configuration drifts (e.g., JWT template gets edited by another developer) | accept | No automated drift detection in v1.5. If Plan 05's E2E fails with a "demo user redirected to /" symptom, RESEARCH Pitfall 1 names this exact failure mode — operator re-checks the Dashboard JWT template. Documented in runbook Step 1. |
| T-27-15 | Repudiation | Operator forgets which password they set for which user | mitigate | `.env.local` is the authoritative password store — the operator pastes the same password they typed into the Dashboard into `.env.local` immediately after creation. Any future automation reads from `.env.local`, eliminating Dashboard-vs-env drift. |
</threat_model>

<output>
After completion, create `.planning/phases/27-role-assignment-and-testing/27-04-SUMMARY.md`. The summary captures: which user IDs Joel created (just the email patterns is fine — no passwords in the summary), confirmation of JWT template activation, JWT-decode verification result for the demo user, and any deviations from `docs/clerk-setup.md` that should be updated in the runbook.
</output>
