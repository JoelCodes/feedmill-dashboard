---
phase: 27-role-assignment-and-testing
plan: 05
type: execute
wave: 3
depends_on: [27-01, 27-02, 27-04]
files_modified:
  - e2e/global.setup.ts
  - playwright.config.ts
  - e2e/demo-route-protection.spec.ts
autonomous: false
requirements: [ACCESS-02]
requirements_addressed: [ACCESS-02]
tags: [playwright, e2e, clerk-testing, fixtures, storage-state, uat]
must_haves:
  truths:
    - "e2e/global.setup.ts signs in all three test users via clerk.signIn and saves their session storage state"
    - "playwright.config.ts has a 'global setup' project plus 'demo-user' and 'norole-user' projects, each with appropriate storageState"
    - "e2e/demo-route-protection.spec.ts has the .skip() removed from the non-demo redirect test"
    - "e2e/demo-route-protection.spec.ts covers all four D-11 scenarios"
    - "Running npm run test:e2e -- e2e/demo-route-protection.spec.ts exits zero with all four scenarios green"
    - "Manual UAT checklist signed off per D-15"
  artifacts:
    - path: "e2e/global.setup.ts"
      provides: "Playwright global setup that authenticates each role and saves storage state"
      contains: "clerkSetup"
    - path: "playwright/.clerk/demo.json"
      provides: "Persisted demo-user session storage state (gitignored, generated at runtime)"
      exists_after_run: true
    - path: "playwright/.clerk/norole.json"
      provides: "Persisted norole-user session storage state (gitignored, generated at runtime)"
      exists_after_run: true
    - path: "playwright.config.ts"
      provides: "Extended projects array with global setup + per-role projects"
      contains: "global setup"
    - path: "e2e/demo-route-protection.spec.ts"
      provides: "All four D-11 E2E scenarios"
      contains: "ACCESS-02"
  key_links:
    - from: "e2e/global.setup.ts"
      to: "@clerk/testing/playwright"
      via: "clerk.signIn + clerkSetup"
      pattern: "import \\{ clerk, clerkSetup \\} from ['\"]@clerk/testing/playwright['\"]"
    - from: "playwright.config.ts demo-user project"
      to: "playwright/.clerk/demo.json"
      via: "storageState property"
      pattern: "storageState: ['\"]playwright/\\.clerk/demo\\.json['\"]"
    - from: "e2e/demo-route-protection.spec.ts"
      to: "src/middleware.ts (sessionClaims-based enforcement)"
      via: "Real HTTP request to /demo/* with authenticated cookie"
      pattern: "page\\.goto\\(['\"]\\/demo\\/"
---

<objective>
Wire `@clerk/testing` into Playwright via a `global.setup.ts` file that signs in all three test users (demo, norole, admin) and persists their session storage state. Extend `playwright.config.ts` with per-role projects. Unskip the existing non-demo-redirect test and add the three new ACCESS-02 scenarios required by D-11. Verify all four scenarios pass and capture the D-15 manual UAT sign-off.

Purpose: D-10 and D-11 require four E2E scenarios proving end-to-end RBAC behavior. The session-claim-based middleware (Plan 02) and the role utilities (Plan 01) are unit-tested already; this plan is the integration proof — a real browser, a real cookie, real Clerk middleware in front of the dev server.

Output:
- `e2e/global.setup.ts` — sets up Clerk testing tokens and authenticates each role into persisted storage state.
- `playwright.config.ts` — adds three new projects (`global setup`, `demo-user`, `norole-user`).
- `e2e/demo-route-protection.spec.ts` — `.skip()` removed; three new describe blocks added.
- Manual UAT checklist (D-15) executed and signed off in the summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md
@.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md
@.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md
@docs/clerk-setup.md
@.env.example
@playwright.config.ts
@e2e/demo-route-protection.spec.ts
@e2e/production-smoke.spec.ts
@src/middleware.ts

<interfaces>
<!-- The contracts that Plan 05 produces and depends on. -->

From `@clerk/testing/playwright` (installed: 2.0.27 per RESEARCH §Standard Stack):
- `clerkSetup(): Promise<void>` — obtains a per-suite testing token. Call once in the global setup.
- `clerk.signIn({ page, signInParams: { strategy: 'password', identifier, password } }): Promise<void>` — signs the page's browser context in as the identified user. Internally calls `setupClerkTestingToken` (do NOT call separately per RESEARCH §Anti-Patterns).
- Constraint: `page.goto('/sign-in')` (or another route loading Clerk) MUST precede `clerk.signIn` because `window.Clerk` must be hydrated (RESEARCH §Pitfall 3).
- Constraint: Only works against Clerk **development** instances (`pk_test_*` / `sk_test_*`) per RESEARCH §Pitfall 4 — Plan 04 verified this.

From `@playwright/test`:
- `test as setup` — alias for declaring setup steps.
- `setup.describe.configure({ mode: 'serial' })` — forces setup steps to run in order so the `clerkSetup` token is obtained before any sign-in attempt.
- `page.context().storageState({ path })` — persists cookies and storage to a JSON file.
- Project `dependencies: ['global setup']` — declarative ordering so test projects await setup completion.
- Project `storageState: 'playwright/.clerk/<role>.json'` — preloads cookies before each test in the project.

From `playwright.config.ts` (existing, lines 19-34):
- Existing `chromium` project (catch-all for unauthenticated specs).
- Existing `production-smoke` project (separate testMatch). Leave both untouched.
- The `chromium` project currently has no `testMatch`/`testIgnore`, so it would attempt to run `global.setup.ts` and `demo-route-protection.spec.ts` without auth — this MUST be fixed by adding a `testIgnore` for those two files OR by narrowing the `chromium` project's `testMatch` to specs that work without auth (e.g., `e2e/route-protection.spec.ts`).
- Existing dotenv load (lines 2-6) already exposes the `E2E_*` env vars to the global setup; no extra config needed.

From `e2e/demo-route-protection.spec.ts` (current state — lines 13, 29-52):
- `const demoRoutes = ['/demo/orders', '/demo/customers'] as const` — extend to include `'/demo/mill-production'` per D-11 scenario #1.
- `test.skip('authenticated user without demo role is redirected to root', ...)` (line 32) — `.skip` must be removed and the test must rely on the `norole-user` project's storageState to provide the unauthenticated-with-no-role context.

E2E scenarios to deliver (per D-11):
- **Scenario #1** (ACCESS-02 #1): Under the `demo-user` project — demo user navigates to each of `/demo/orders`, `/demo/customers`, `/demo/mill-production` and the URL does NOT redirect (page renders).
- **Scenario #2** (ACCESS-02 #2): Under the `norole-user` project — norole user navigates to each demo route and the URL becomes `/` (redirect to root, matching middleware D-01).
- **Scenario #3** (ACCESS-02 #3): Under BOTH `demo-user` and `norole-user` projects — both users navigate to `/settings` and the URL stays at `/settings` (no redirect).
- **Scenario #4** (existing PROT-03 regression): Under the `chromium` (unauthenticated) project — unauthenticated user accesses each demo route and is redirected to `/sign-in`. This already passes; the only requirement is to not break it.

Persisted storage-state files live at `playwright/.clerk/<role>.json` — these are generated at test time and must be gitignored. The `playwright/` directory likely does not exist yet — `global.setup.ts` should ensure the path exists (`fs.mkdir(..., { recursive: true })`) before calling `storageState`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gitignore playwright/.clerk storage-state directory and add E2E credential env-shape check</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (full file — confirm current ignored paths)
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §Pattern F — confirms dotenv is loaded at the config level so storage state files have access to env-derived state
  </read_first>
  <action>
    Append a single section to `.gitignore` that ignores the runtime-generated Playwright Clerk storage state files. Add (with a leading comment header):

    ```
    # Playwright Clerk session storage state (generated at test time per Phase 27)
    playwright/.clerk/
    ```

    Do NOT modify any existing gitignore entry. If `playwright/.clerk/` is already covered by an existing pattern (e.g., a broad `playwright/` ignore), the explicit entry is still desirable for clarity — but in that case, change nothing else.

    Verify by attempting `git check-ignore playwright/.clerk/demo.json` from the repo root — should report the path is ignored, even though the file does not exist yet.
  </action>
  <verify>
    <automated>grep -F "playwright/.clerk/" .gitignore && git check-ignore -q playwright/.clerk/demo.json && echo "ignored"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "playwright/.clerk/" .gitignore` matches at least one line.
    - `git check-ignore -q playwright/.clerk/demo.json; echo $?` exits with status 0 (path is ignored).
    - No existing gitignore entries were removed: `git diff .gitignore` shows only additions.
  </acceptance_criteria>
  <done>
    `.gitignore` excludes the storage state directory so runtime artifacts cannot be accidentally committed. Commit message: `chore(27-05): gitignore Playwright Clerk storage state directory`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create e2e/global.setup.ts for clerkSetup + per-role authentication</name>
  <files>e2e/global.setup.ts</files>
  <read_first>
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pattern 4 — verbatim canonical shape from github.com/clerk/clerk-playwright-nextjs
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`e2e/global.setup.ts (CREATE — no analog, greenfield)` — adapted shape with env-var lookup and storage-state save
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pitfall 3 — must `page.goto` before `clerk.signIn`
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pitfall 6 — known concurrency limitation; storage-state pattern avoids it because setup runs serial
    - `e2e/production-smoke.spec.ts` (lines 17-43) — existing env-var lookup precedent (DO NOT copy the form-fill pattern; we use `clerk.signIn` instead)
    - `playwright.config.ts` (lines 2-6) — dotenv load at config level means `process.env.E2E_*` is already populated when global.setup.ts runs
  </read_first>
  <action>
    Create `e2e/global.setup.ts`. Imports:
    - `import { clerk, clerkSetup } from '@clerk/testing/playwright'`
    - `import { test as setup } from '@playwright/test'`
    - `import path from 'path'`
    - `import { promises as fs } from 'fs'`

    Configure serial mode at the top: `setup.describe.configure({ mode: 'serial' });` — this ensures the `clerkSetup` step completes before any of the per-role sign-in steps (avoids the concurrency landmine in RESEARCH §Pitfall 6).

    Declare the first setup step: `setup('global setup', async () => { await clerkSetup(); });`. This obtains the testing token used by subsequent `clerk.signIn` calls.

    Define a `roles` record mapping role name → `{ envEmail, envPassword, file }` for the three users:
    - `demo`: envEmail `'E2E_DEMO_USER_EMAIL'`, envPassword `'E2E_DEMO_USER_PASSWORD'`, file `'playwright/.clerk/demo.json'`
    - `norole`: envEmail `'E2E_NOROLE_USER_EMAIL'`, envPassword `'E2E_NOROLE_USER_PASSWORD'`, file `'playwright/.clerk/norole.json'`
    - `admin`: envEmail `'E2E_ADMIN_USER_EMAIL'`, envPassword `'E2E_ADMIN_USER_PASSWORD'`, file `'playwright/.clerk/admin.json'`

    For each role, declare a setup step named `authenticate <role>` (e.g., `setup('authenticate demo', async ({ page }) => { ... })`). The step body:
    1. Read `email = process.env[cfg.envEmail]` and `password = process.env[cfg.envPassword]`.
    2. If either is missing or empty, `throw new Error('Missing env: <name>; populate .env.local per docs/clerk-setup.md')` — fail loud and early with the runbook reference.
    3. Compute the absolute output file path: `const outPath = path.resolve(__dirname, '..', cfg.file)` (resolves to `<repo>/playwright/.clerk/<role>.json`).
    4. Ensure the parent directory exists: `await fs.mkdir(path.dirname(outPath), { recursive: true })`.
    5. `await page.goto('/sign-in')` — load Clerk (per RESEARCH §Pitfall 3, `clerk.signIn` requires `window.Clerk` hydrated).
    6. `await clerk.signIn({ page, signInParams: { strategy: 'password', identifier: email, password } })`.
    7. `await page.goto('/')` — drive a post-sign-in navigation so the session cookie is committed to the context.
    8. `await page.context().storageState({ path: outPath })` — persist the authenticated state.

    Include the admin role even though no D-11 scenario uses it — it makes the setup symmetric and lets future phases (e.g., Phase 28 audit, or future admin features) reuse the same `playwright/.clerk/admin.json` without changes.

    Do NOT call `setupClerkTestingToken()` separately anywhere — RESEARCH explicitly warns this is wrong because `clerk.signIn` already does it internally.

    Do NOT add fixtures, do NOT add a parallel-worker workaround. The serial `describe.configure` plus the `dependencies: ['global setup']` projects array in Task 3 is the correct concurrency model per RESEARCH §Pitfall 6.
  </action>
  <verify>
    <automated>test -f e2e/global.setup.ts && grep -F "clerkSetup" e2e/global.setup.ts && grep -F "clerk.signIn" e2e/global.setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `e2e/global.setup.ts` exists.
    - `grep -F "import { clerk, clerkSetup } from '@clerk/testing/playwright'" e2e/global.setup.ts` matches (or the double-quoted equivalent).
    - `grep -F "setup.describe.configure({ mode: 'serial' })" e2e/global.setup.ts` matches (or double-quoted equivalent).
    - `grep -F "await clerkSetup()" e2e/global.setup.ts` matches.
    - `grep -cF "setup('authenticate" e2e/global.setup.ts` returns at least `3` (one per role).
    - `grep -F "E2E_DEMO_USER_EMAIL" e2e/global.setup.ts` matches.
    - `grep -F "E2E_NOROLE_USER_EMAIL" e2e/global.setup.ts` matches.
    - `grep -F "E2E_ADMIN_USER_EMAIL" e2e/global.setup.ts` matches.
    - `grep -F "playwright/.clerk/demo.json" e2e/global.setup.ts` matches.
    - `grep -F "playwright/.clerk/norole.json" e2e/global.setup.ts` matches.
    - `grep -F "playwright/.clerk/admin.json" e2e/global.setup.ts` matches.
    - `grep -F "await page.goto('/sign-in')" e2e/global.setup.ts` matches (Pitfall 3 compliance).
    - `grep -F "storageState({ path:" e2e/global.setup.ts` matches.
    - `grep -F "setupClerkTestingToken" e2e/global.setup.ts` returns zero matches (RESEARCH anti-pattern).
    - `grep -F "fs.mkdir" e2e/global.setup.ts` matches (output directory creation).
    - `npx tsc --noEmit` exits zero — type-checks against `@playwright/test` and `@clerk/testing/playwright` types.
  </acceptance_criteria>
  <done>
    Global setup authored. Commit message: `test(27-05): add Playwright global setup for Clerk per-role authentication`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend playwright.config.ts with global-setup and per-role projects</name>
  <files>playwright.config.ts</files>
  <read_first>
    - `playwright.config.ts` (full current file)
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`playwright.config.ts (MODIFY)` — exact append-block and the constraint on the existing `chromium` project
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pattern 4 — canonical projects-array shape including `dependencies: ['global setup']`
  </read_first>
  <action>
    Edit `playwright.config.ts`. Two changes are required:

    1. **Narrow the existing `chromium` project** (lines 19-23): the catch-all `chromium` project currently has no `testMatch` or `testIgnore`, which means it would attempt to run both `e2e/global.setup.ts` and `e2e/demo-route-protection.spec.ts` without auth — `global.setup.ts` would run twice (once as part of the standalone `chromium` project and once as the dedicated setup project), and `demo-route-protection.spec.ts` would run unauthenticated and fail Scenario #1.

       Add a `testIgnore` array to the `chromium` project: `testIgnore: [/global\.setup\.ts/, /demo-route-protection\.spec\.ts/]`. This keeps `chromium` covering only the existing unauthenticated regression test (`e2e/route-protection.spec.ts`) — exactly the regression scope it had pre-Phase-27.

    2. **Append three new projects** to the `projects` array (AFTER the existing `chromium` and `production-smoke` projects — do NOT remove or modify either). Use this exact shape:

       - **Project A: `'global setup'`**
         - `name: 'global setup'`
         - `testMatch: /global\.setup\.ts/`
         - No `use` block, no `dependencies` — this is the bootstrap project.
       - **Project B: `'demo-user'`**
         - `name: 'demo-user'`
         - `testMatch: /demo-route-protection\.spec\.ts/`
         - `use: { ...devices['Desktop Chrome'], storageState: 'playwright/.clerk/demo.json' }`
         - `dependencies: ['global setup']`
       - **Project C: `'norole-user'`**
         - `name: 'norole-user'`
         - `testMatch: /demo-route-protection\.spec\.ts/`
         - `use: { ...devices['Desktop Chrome'], storageState: 'playwright/.clerk/norole.json' }`
         - `dependencies: ['global setup']`

    Leave `production-smoke` untouched (it has its own testMatch and is unrelated to Phase 27). Leave the top-level `webServer`, `use`, `reporter`, etc. unchanged. Leave the dotenv load at lines 2-6 unchanged.

    `devices['Desktop Chrome']` is already imported at line 1 — reuse it; do not re-import.

    Note: no `admin-user` project is added. The admin storage state is generated by `global.setup.ts` for symmetry, but there are no admin scenarios in D-11. Future phases can add a project that uses `playwright/.clerk/admin.json` if needed.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "name: 'global setup'" playwright.config.ts` matches (or double-quoted equivalent).
    - `grep -F "name: 'demo-user'" playwright.config.ts` matches.
    - `grep -F "name: 'norole-user'" playwright.config.ts` matches.
    - `grep -F "storageState: 'playwright/.clerk/demo.json'" playwright.config.ts` matches.
    - `grep -F "storageState: 'playwright/.clerk/norole.json'" playwright.config.ts` matches.
    - `grep -F "dependencies: ['global setup']" playwright.config.ts` matches at least twice (demo-user + norole-user both depend on global setup).
    - In the existing `chromium` project block, `grep -F "testIgnore" playwright.config.ts` matches and the ignore list mentions `global.setup.ts` and `demo-route-protection.spec.ts`.
    - The existing `production-smoke` project is intact: `grep -F "name: 'production-smoke'" playwright.config.ts` still matches; its block is unchanged.
    - `npx playwright test --list` exits zero and lists tests from all five projects (`chromium`, `production-smoke`, `global setup`, `demo-user`, `norole-user`) — no syntax or config errors.
    - `npx tsc --noEmit` exits zero.
  </acceptance_criteria>
  <done>
    Playwright config extended with global-setup project, per-role projects, and `chromium` project narrowed. Commit message: `test(27-05): add per-role Playwright projects with storage state`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Unskip and extend e2e/demo-route-protection.spec.ts with all four D-11 scenarios</name>
  <files>e2e/demo-route-protection.spec.ts</files>
  <read_first>
    - `e2e/demo-route-protection.spec.ts` (full file — current state)
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`e2e/demo-route-protection.spec.ts (MODIFY)` — describe-block shape, scenario list, and the per-project storage-state pattern (no `clerk.signIn` inside the test body)
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decision D-11 — the locked four-scenario list
    - `playwright.config.ts` (post-Task-3) — confirms the project names this spec runs under: `demo-user`, `norole-user`, and `chromium`
  </read_first>
  <action>
    Edit `e2e/demo-route-protection.spec.ts`. Apply these changes in order:

    1. **Extend the demoRoutes constant** (line 13): change from `['/demo/orders', '/demo/customers']` to `['/demo/orders', '/demo/customers', '/demo/mill-production']`. D-11 scenario #1 explicitly names mill-production.

    2. **Remove the .skip()** from the existing test at line 32: change `test.skip('authenticated user without demo role is redirected to root', ...)` to `test('authenticated user without demo role is redirected to root', ...)`. Replace the contents of the function body with a loop that asserts the redirect for each route in `demoRoutes` — every demo route should redirect a norole user to `/`. The describe block name stays as `'ACCESS-01: Authenticated user without demo role redirected to root'`. Delete the long block of "// To implement this test:" comments inside the function body — they were placeholder guidance; the test is now real.

    3. **Add a new describe block** named `'ACCESS-02 #1: Demo user accesses /demo/* routes successfully'`. Inside, for each `route` in the extended `demoRoutes`, add a test named `` `demo user accesses ${route} without redirect` `` that:
       - `await page.goto(route)`
       - `await expect(page).toHaveURL(route)` — strict equality, no redirect happened
       Do NOT call `clerk.signIn` inside the spec — the `demo-user` project's `storageState` loads the demo cookie automatically. Add a short comment at the top of the block: `// Runs under the demo-user Playwright project (storageState = playwright/.clerk/demo.json)`.

    4. **Add a new describe block** named `'ACCESS-02 #3: Both demo and norole users can access /settings'`. Inside, add a single test named `'authenticated user can access /settings regardless of role'` that:
       - `await page.goto('/settings')`
       - `await expect(page).toHaveURL('/settings')` — no redirect from /settings; this assertion will fire under both the `demo-user` and `norole-user` projects (Playwright runs the spec once per project per `testMatch`).
       Add a top-of-block comment: `// Runs under both demo-user and norole-user projects; settings is accessible to all authenticated users (Phase 26 D-07).`

    5. **Update describe-block names** to match the requirement IDs (Pattern E from PATTERNS.md): the existing block at line 16 `'PROT-03: Unauthenticated user accessing /demo/* redirects to sign-in'` stays as-is (it covers D-11 scenario #4). The block at line 29 `'ACCESS-01: Authenticated user without demo role redirected to root'` covers D-11 scenario #2 — leave the prefix `ACCESS-01` but add the scenario reference: rename to `'ACCESS-01 / ACCESS-02 #2: Authenticated user without demo role redirected to root'`. The two new blocks added above carry `ACCESS-02 #1` and `ACCESS-02 #3` prefixes.

    Do NOT add explicit `test.use({ storageState: ... })` inside the spec — Playwright projects supply storage state at the project level, which is the canonical pattern per RESEARCH §Pattern 4 and PATTERNS.md §Open Question O-1 recommendation.

    Do NOT touch the existing PROT-03 (unauthenticated) describe block — it covers scenario #4 and runs under the `chromium` project (which now has a `testIgnore` excluding this spec from Task 3's edit). The PROT-03 tests should still run because the `chromium` project's `testIgnore` excludes only `global.setup.ts` and `demo-route-protection.spec.ts` — WAIT: this is a contradiction. Re-check.

    **Conflict resolution:** The `chromium` project's `testIgnore` (Task 3) excludes `demo-route-protection.spec.ts`, but the PROT-03 unauthenticated regression test inside this file needs the `chromium` (no-storage-state) project to run it. Resolution: add a fourth Playwright project named `'demo-route-unauth'` in Task 3 OR (preferred, simpler) move PROT-03 to a separate spec file. The simplest path is in this Task 4: move the PROT-03 describe block into a new file `e2e/demo-route-protection-unauth.spec.ts` so the original spec only contains authenticated scenarios that run under `demo-user` / `norole-user`. Edit `playwright.config.ts` (revisit) so `chromium` runs `e2e/demo-route-protection-unauth.spec.ts`.

    **Implement this conflict resolution as follows in THIS task:**
    - Create `e2e/demo-route-protection-unauth.spec.ts` containing the PROT-03 describe block (verbatim copy of current lines 13-27, with the demoRoutes constant updated to include `/demo/mill-production`).
    - Remove the PROT-03 describe block from `e2e/demo-route-protection.spec.ts` so the original file contains ONLY the three authenticated describe blocks (ACCESS-01 / ACCESS-02 #1 / ACCESS-02 #3).
    - Update `playwright.config.ts` `chromium` project's `testIgnore` to exclude only `global.setup.ts` and `demo-route-protection.spec.ts` (NOT the new `-unauth` file) — the new unauthenticated spec file should run under `chromium`.

    Files modified by this task therefore include the new `e2e/demo-route-protection-unauth.spec.ts` and a re-edit of `playwright.config.ts`. Update the frontmatter `files_modified` mentally — no need to rewrite the YAML; reviewers know these files are in scope from the conflict-resolution note above.

    Add a top-of-file JSDoc comment block at the top of `e2e/demo-route-protection.spec.ts` summarizing: "Authenticated demo-route protection scenarios per D-11 (#1, #2, #3). Runs under `demo-user` and `norole-user` Playwright projects. Unauthenticated regression (D-11 #4) lives in demo-route-protection-unauth.spec.ts."
  </action>
  <verify>
    <automated>grep -cF "test('" e2e/demo-route-protection.spec.ts && grep -F "/demo/mill-production" e2e/demo-route-protection.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "test.skip(" e2e/demo-route-protection.spec.ts` returns zero matches (skipped test removed).
    - `grep -F "/demo/mill-production" e2e/demo-route-protection.spec.ts` matches (D-11 scenario #1 explicitly names it).
    - `grep -F "ACCESS-02 #1" e2e/demo-route-protection.spec.ts` matches (new describe block for demo-user access).
    - `grep -F "ACCESS-02 #3" e2e/demo-route-protection.spec.ts` matches (new describe block for /settings access).
    - `grep -F "/settings" e2e/demo-route-protection.spec.ts` matches.
    - `grep -F "PROT-03" e2e/demo-route-protection.spec.ts` returns zero matches (moved to `-unauth` file).
    - File `e2e/demo-route-protection-unauth.spec.ts` exists.
    - `grep -F "PROT-03" e2e/demo-route-protection-unauth.spec.ts` matches.
    - `grep -F "/demo/mill-production" e2e/demo-route-protection-unauth.spec.ts` matches (new route covered in unauth regression too).
    - `playwright.config.ts` `chromium` project `testIgnore` does NOT mention `demo-route-protection-unauth.spec.ts` (so it runs there): `grep -F "demo-route-protection-unauth.spec.ts" playwright.config.ts` returns zero matches when grepping for testIgnore context.
    - `npx playwright test --list` lists the new scenarios under their expected projects (demo-user runs demo + settings tests; norole-user runs norole-redirect + settings tests; chromium runs the unauth regression).
    - `npx tsc --noEmit` exits zero.
  </acceptance_criteria>
  <done>
    Spec restructured: authenticated scenarios in `demo-route-protection.spec.ts` run under `demo-user`/`norole-user` projects; unauthenticated regression in `demo-route-protection-unauth.spec.ts` runs under `chromium`. Commit message: `test(27-05): add D-11 authenticated scenarios and split unauth regression`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run full Playwright suite and confirm all four D-11 scenarios pass</name>
  <files></files>
  <read_first>
    - `e2e/global.setup.ts` (Task 2 output)
    - `playwright.config.ts` (Task 3 + Task 4 output)
    - `e2e/demo-route-protection.spec.ts` and `e2e/demo-route-protection-unauth.spec.ts` (Task 4 output)
    - `.env.local` — confirm the six `E2E_*` keys have non-empty values (Plan 04 prerequisite)
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pitfall 1 — fail-fast signal: if all three roles get redirected to `/`, the JWT template is missing in Clerk Dashboard
  </read_first>
  <action>
    Run the E2E suite for the demo-route-protection scenarios. Use the existing `npm run test:e2e` script if it points at the right config; otherwise invoke Playwright directly.

    First run a project-list sanity check: `npx playwright test --list` — confirm tests are enumerated under `demo-user`, `norole-user`, and `chromium` projects (and `global setup` shows the three authenticate-* setup steps).

    Then run the full Playwright suite: `npm run test:e2e` (or `npx playwright test` if the script doesn't exist or is misconfigured). Expected outcome: all four D-11 scenarios pass green:
    - Demo user → `/demo/orders`, `/demo/customers`, `/demo/mill-production` → all render (three tests).
    - Norole user → `/demo/orders`, `/demo/customers`, `/demo/mill-production` → all redirect to `/` (three tests).
    - Demo user + Norole user → `/settings` → both stay at `/settings` (two tests — one per project).
    - Unauthenticated → `/demo/orders`, `/demo/customers`, `/demo/mill-production` → all redirect to `/sign-in` (three tests under `chromium`).

    Total: 11 new or modified test assertions, all expected to be green.

    Failure-mode triage (per RESEARCH §Pitfalls):
    - If the global-setup step fails with "Missing env: E2E_DEMO_USER_EMAIL", Plan 04 did not populate `.env.local`. Stop and ask the operator to complete Plan 04.
    - If global-setup signs in but the demo-user spec fails with "expected /demo/orders but got /", the JWT template is not active in the Clerk Dashboard. Re-run Plan 04 Step 1.
    - If `clerk.signIn` throws "Cannot read properties of undefined (reading 'signIn')", the `page.goto('/sign-in')` call is missing or executed too late in `global.setup.ts`. Re-check Task 2.
    - If tests fail intermittently with sign-in timeouts under high parallelism, the storage-state pattern should already prevent this; verify the `dependencies: ['global setup']` declaration is present in Task 3's projects.

    Once the full E2E suite is green, run the unit suite again to confirm no cross-impact: `npm test`.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` exits zero.
    - The reporter output lists at least 11 passing tests in the `demo-user`, `norole-user`, and `chromium` projects (plus the `global setup` project's setup steps).
    - The `production-smoke` project is unchanged in status (it runs against a separate baseURL and is unrelated to Phase 27).
    - `npm test` (unit suite) still exits zero — no cross-impact from the new E2E config.
    - `npx tsc --noEmit` exits zero.
    - Artifact check: `test -f playwright/.clerk/demo.json && test -f playwright/.clerk/norole.json && test -f playwright/.clerk/admin.json` — all three storage state files were generated by the global setup.
    - `git status` confirms `playwright/.clerk/*.json` are NOT in the staged or untracked list (gitignored per Task 1).
  </acceptance_criteria>
  <done>
    All four D-11 scenarios green; unit suite still green; storage-state files exist on disk but are gitignored. No new commit needed (this task is a verification, not a code change).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual UAT — sign in as each role and verify access behavior (D-15)</name>
  <files>(no repo files; live application UAT against the running dev server)</files>
  <read_first>
    - `docs/clerk-setup.md` — the runbook (especially the Verification section)
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decision D-15 — manual UAT requirement
  </read_first>
  <what-built>
    Plan 05 has wired the Playwright E2E suite to prove all four D-11 scenarios automatically. D-15 additionally requires a manual UAT — Joel signs in as each of the three users via the actual browser and confirms the felt behavior matches expectations. This is the human-in-the-loop verification that the E2E test framework is not lying (e.g., that the dev-server middleware is the same one a real user would hit, that the redirect happens promptly without flash-of-content, that /settings really is reachable).
  </what-built>
  <action>
    This is a checkpoint:human-verify task — the operator (Joel) executes the 11-row UAT checklist in a real browser against `npm run dev`. The agent's role is to wait for the `verified` resume-signal before declaring the phase complete. See `<how-to-verify>` for the full checklist; the checklist is the action.

    Pre-action requirement: `npm run dev` must be running locally so the operator can navigate the app at `http://localhost:3000`. The same `.env.local` populated in Plan 04 supplies the Clerk dev-instance keys for the dev server.
  </action>
  <how-to-verify>
    With the dev server running (`npm run dev`), perform each row in this checklist. Each row should match the documented expectation exactly. If any row fails, stop and triage before proceeding.

    | # | Sign in as | Navigate to | Expected URL after settle | Expected page content |
    |---|------------|-------------|---------------------------|----------------------|
    | 1 | `e2e-demo+clerk_test@example.com` | `/demo/orders` | `/demo/orders` | Orders demo page renders, no redirect flash |
    | 2 | `e2e-demo+clerk_test@example.com` | `/demo/customers` | `/demo/customers` | Customers demo page renders |
    | 3 | `e2e-demo+clerk_test@example.com` | `/demo/mill-production` | `/demo/mill-production` | Mill production demo page renders |
    | 4 | `e2e-demo+clerk_test@example.com` | `/settings` | `/settings` | Settings page renders |
    | 5 | `e2e-norole+clerk_test@example.com` | `/demo/orders` | `/` | Coming Soon homepage renders |
    | 6 | `e2e-norole+clerk_test@example.com` | `/demo/customers` | `/` | Coming Soon homepage renders |
    | 7 | `e2e-norole+clerk_test@example.com` | `/demo/mill-production` | `/` | Coming Soon homepage renders |
    | 8 | `e2e-norole+clerk_test@example.com` | `/settings` | `/settings` | Settings page renders (D-07 from Phase 26: settings always accessible) |
    | 9 | `e2e-admin+clerk_test@example.com` | `/demo/orders` | `/` | Coming Soon homepage renders (admin does NOT have demo role; same redirect as norole) |
    | 10 | `e2e-admin+clerk_test@example.com` | `/settings` | `/settings` | Settings page renders |
    | 11 | (signed out, incognito) | `/demo/orders` | `/sign-in...` (with query param `?redirect_url=...` from Clerk) | Clerk sign-in page renders |

    Between users, sign out from the Clerk UserButton in the Header. Use the same browser session for the demo/norole/admin runs — Clerk should issue a fresh JWT on each sign-in that includes the `metadata.role` claim per Plan 04's JWT template.

    Note that rows 1-3 also verify that the demo page navigation works post-Phase-26 (orders/customers/mill-production were moved to `/demo/*`). If a row's "page content" assertion fails (e.g., a /demo/orders sign-in lands on a blank or 404 page), the issue may be Phase 26 leftover, not Phase 27 — escalate accordingly.
  </how-to-verify>
  <verify>
    Manual UAT — see `<how-to-verify>` for the 11-row checklist. No automated assertion supplants the felt-behavior check D-15 requires. The downstream automated proof (Task 5's `npm run test:e2e`) covers scenarios 1-9 and 11; this UAT confirms the same outcomes hold when a human drives the browser.
  </verify>
  <acceptance_criteria>
    - Operator has replied `verified` and confirmed all 11 rows in the UAT checklist pass.
    - Rows 1-3 (demo user) all confirmed: URL stays at requested /demo/* path, demo page renders with expected content.
    - Row 4 confirmed: demo user reaches /settings without redirect.
    - Rows 5-7 (norole user) all confirmed: URL becomes / after demo-route navigation, Coming Soon homepage renders.
    - Row 8 confirmed: norole user reaches /settings without redirect (Phase 26 D-07 holds).
    - Row 9 confirmed: admin user (without demo role) is redirected to / on /demo/* navigation.
    - Row 10 confirmed: admin user reaches /settings.
    - Row 11 confirmed: unauthenticated user redirects to /sign-in with redirect_url query param.
    - Any deviations are recorded in 27-05-SUMMARY.md with row number, observed outcome, and remediation taken.
  </acceptance_criteria>
  <resume-signal>
    Reply `verified` if all 11 rows in the UAT checklist pass.

    If any row fails, describe the failure mode (which row, observed vs expected URL, observed page content) so the agent can advise on triage. Common triage paths:
    - Row 1-3 fails (demo user redirected): JWT template not active OR user's session was issued before role was assigned. Sign out, sign back in.
    - Row 4/8/10 fails (settings redirects): Phase 26 D-07 regression — settings must remain visible regardless of role.
    - Row 5-9 succeeds but redirects to a different URL than `/`: Phase 25 D-01 may have been edited. Re-check `src/middleware.ts`.
    - Row 11 fails (unauthenticated does not redirect): Phase 25 `auth.protect()` is misconfigured.
  </resume-signal>
  <done>
    Operator has replied `verified` and all 11 acceptance rows are confirmed. The phase has delivered ACCESS-02 end-to-end: utility functions (Plan 01) + middleware (Plan 02) + Dashboard config (Plans 03/04) + automated E2E proof (Plan 05 Task 5) + manual UAT (this task). The phase is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:e2e` exits zero with all four D-11 scenarios green.
2. `npm test` exits zero (unit suite — no regressions).
3. `npx tsc --noEmit` exits zero.
4. `git check-ignore -q playwright/.clerk/demo.json` exits zero (storage state gitignored).
5. UAT checklist (D-15) signed off via Task 6 resume-signal.
6. `e2e/demo-route-protection.spec.ts` contains describe blocks for ACCESS-02 #1, ACCESS-02 #3, and ACCESS-01 / ACCESS-02 #2; PROT-03 lives in the separate `-unauth` spec file.
</verification>

<success_criteria>
- `e2e/global.setup.ts` authenticates all three roles and persists storage state.
- `playwright.config.ts` declares `global setup`, `demo-user`, `norole-user` projects; the existing `chromium` project's `testIgnore` correctly excludes the setup + authenticated spec files.
- All four D-11 E2E scenarios pass green automatically.
- D-15 manual UAT signed off across 11 checklist rows.
- ACCESS-02 requirement satisfied end-to-end: utility functions (Plan 01) + middleware (Plan 02) + dashboard config (Plans 03/04) + E2E proof (Plan 05).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → Clerk dev instance | `@clerk/testing` testing token bypasses bot detection on dev instances only; production rejects these tokens. |
| `.env.local` → Playwright process | Passwords flow from `.env.local` (gitignored) to the test runner via dotenv. Never logged, never persisted to repo. |
| Storage state files → Playwright project | `playwright/.clerk/*.json` files contain authenticated cookies. Gitignored. Regenerated on every test run by global setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-16 | Information Disclosure | Storage-state JSON files contain session cookies, committed accidentally | mitigate | Task 1 adds `playwright/.clerk/` to `.gitignore` BEFORE the global setup ever runs. `git check-ignore` verification in Task 1 confirms the path is ignored. |
| T-27-17 | Spoofing | A developer copies the gitignored storage state to a public location | accept | Same risk profile as any test credential; bounded to the dev instance. The `+clerk_test` mailbox suffix and the dev-instance constraint mean these tokens cannot reach production. |
| T-27-18 | Denial of Service | Parallel Playwright workers hit Clerk sign-in concurrency limits | mitigate | RESEARCH §Pitfall 6 (clerk/javascript#7891). `setup.describe.configure({ mode: 'serial' })` in Task 2 ensures the three sign-ins happen sequentially. Test projects use `dependencies: ['global setup']` so they only run AFTER setup completes — no concurrent sign-in attempts. |
| T-27-19 | Tampering | A test bypasses storage state by calling `clerk.signIn` inside the spec, drifting from the project's role assumption | mitigate | Spec convention (PATTERNS.md Pattern 4 / Open Question O-1): no `clerk.signIn` inside `e2e/demo-route-protection.spec.ts`. Acceptance criteria checks that the file does not contain `clerk.signIn` (only `page.goto` + URL assertions). |
| T-27-20 | Repudiation | UAT failures (D-15) are observed but not logged | mitigate | The Task 6 resume-signal explicitly asks the operator to record the failing row and the observed vs expected outcome. The 27-05-SUMMARY.md captures the sign-off result. |
</threat_model>

<output>
After completion, create `.planning/phases/27-role-assignment-and-testing/27-05-SUMMARY.md`. The summary records: the E2E test counts and projects, confirmation that the UAT checklist passed (or a description of any failures and remediation), and a note that the phase is ready for `/gsd-verify-work`.
</output>
