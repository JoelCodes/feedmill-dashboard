---
phase: 27-role-assignment-and-testing
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/middleware.test.ts
autonomous: true
requirements: [ACCESS-02]
requirements_addressed: [ACCESS-02]
tags: [clerk, middleware, rbac, session-claims, tdd, migration]
must_haves:
  truths:
    - "D-04: Middleware migrates to auth().sessionClaims?.metadata?.role; drops per-request clerkClient.users.getUser() call and clerkClient import"
    - "D-06: No fallback to clerkClient.getUser() if the claim is missing — clean migration; existing sessions get the new claim on next sign-in"
    - "D-09: src/middleware.test.ts source-string assertions updated to reflect the session-claims source (file-wide grep gates enforce no stale clerkClient/publicMetadata positive assertions)"
    - "src/middleware.ts reads role from sessionClaims.metadata.role (no clerkClient network call)"
    - "src/middleware.ts no longer imports clerkClient or the Role type from @/types/clerk"
    - "src/middleware.ts retains the literal route matcher '/demo(.*)' and the isDemoRoute matcher does NOT include '/settings' (Success Criterion #4 protection: settings stays accessible to all authenticated users)"
    - "src/middleware.test.ts string-search assertions match the new source: sessionClaims/metadata, not clerkClient/publicMetadata"
    - "src/middleware.test.ts source-string suite verifies clerkClient and publicMetadata are absent across the whole file (D-09 narrowing fix: assertions are not scoped to a single it() block)"
    - "Existing structural tests for /demo(.*) matcher, NextResponse.redirect, and redirect target / continue to pass unchanged"
    - "All other middleware tests (public route, auth.protect, matcher config) continue to pass"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Session-claim-based role enforcement in middleware"
      contains: "sessionClaims?.metadata?.role"
    - path: "src/middleware.test.ts"
      provides: "Updated string-search assertions for the migrated middleware"
      contains: "sessionClaims"
  key_links:
    - from: "src/middleware.ts"
      to: "@clerk/nextjs/server"
      via: "auth() destructure of { userId, sessionClaims }"
      pattern: "const \\{ userId, sessionClaims \\} = await auth\\(\\)"
    - from: "src/middleware.test.ts"
      to: "src/middleware.ts"
      via: "fs.readFile string assertions"
      pattern: "middlewareContent.*toContain.*sessionClaims"
---

<objective>
Migrate `src/middleware.ts` from a per-request `clerkClient.users.getUser()` network call to a session-claim read, and update the `src/middleware.test.ts` source-string assertion suite to match. After this plan, both the middleware and the new `src/lib/auth.ts` utilities read role from the same source: `auth().sessionClaims?.metadata?.role`.

Purpose: D-04 locks the migration. The current middleware adds ~80–200ms Edge latency per request via a Backend API call. Reading the claim is local. Phase 25 success criterion #1 ("role data available in session token without additional network requests") only becomes TRUE after this plan ships.

Output:
- `src/middleware.test.ts` — updated string assertions targeting the new source shape (RED step first per D-09).
- `src/middleware.ts` — refactored to destructure `sessionClaims` and drop the `clerkClient` / `Role` imports (GREEN step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md
@.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md
@.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md
@src/middleware.ts
@src/middleware.test.ts
@src/types/clerk.d.ts

<interfaces>
<!-- The migration target shape — extracted verbatim from PATTERNS.md §src/middleware.ts and RESEARCH.md §Pattern 2. -->

Current source state (PRE-migration, `src/middleware.ts:1, 3, 36-38`):
- `import { clerkClient, clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server"` (line 1 — `clerkClient` to drop)
- `import type { Role } from '@/types/clerk'` (line 3 — unused after migration, to drop)
- `const client = await clerkClient(); const user = await client.users.getUser(userId); const role = user.publicMetadata?.role as Role | undefined;` (lines 36-38 — to remove)

Target source state (POST-migration):
- `import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server"` (only two named imports)
- `import { NextResponse } from 'next/server'` (unchanged)
- No `Role` import (the comparison is against the string literal `'demo'`; `CustomJwtSessionClaims` auto-narrows `sessionClaims?.metadata?.role` to `Role | undefined`)
- Inside `if (isDemoRoute(request))`: `const { userId, sessionClaims } = await auth();` followed by the userId guard and the `sessionClaims?.metadata?.role !== 'demo'` guard (RESEARCH §Pattern 2 exact shape)
- The literal route matcher `'/demo(.*)'` MUST remain in `createRouteMatcher(['/demo(.*)'])` for `isDemoRoute` (Success Criterion #4 protection: settings is NOT in the demo matcher, so all authenticated users — regardless of role — can access `/settings`).

Test source state changes (per D-09, `src/middleware.test.ts:140-151`):
- The single `it("checks role for demo routes via publicMetadata", ...)` test (lines 140-151) replaces its `toContain("clerkClient")` and `toContain("publicMetadata")` assertions with `toContain("sessionClaims")` and `toContain("metadata")`, and adds negative `.not.toContain("clerkClient")` / `.not.toContain("publicMetadata")` checks. The test name updates from "via publicMetadata" → "via sessionClaims".
- The D-09 broadened scope (revision): the negative `clerkClient`/`publicMetadata` assertions are also expressed at the whole-file level (acceptance criteria below), so stale positive references elsewhere in the test file are caught by acceptance gates even when not embedded in the rewritten `it()` block.
- All other tests in `src/middleware.test.ts` reference strings that remain in the file post-migration (e.g., `"/demo(.*)"`, `"isDemoRoute"`, `"NextResponse.redirect"`, `"new URL('/', request.url)"`); leave them untouched.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Rewrite the publicMetadata source-string test to target the new sessionClaims source</name>
  <files>src/middleware.test.ts</files>
  <read_first>
    - `src/middleware.test.ts` (full file, especially lines 124-176 — the entire "demo route protection" describe block)
    - `src/middleware.ts` (lines 1-3, 29-43) — current source the existing tests assert against (RED expectations land BEFORE we change this file)
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`src/middleware.test.ts (MODIFY)` — exact before/after assertion shapes
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pitfall 7 — explanation that this file is a source-string suite, not a behavior suite
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decisions D-04, D-09 — locked decision that test assertions must update to the new source
  </read_first>
  <action>
    Edit `src/middleware.test.ts` lines 140-151 (the `it("checks role for demo routes via publicMetadata", ...)` block). Rewrite:

    - Test name (line 140): change from `"checks role for demo routes via publicMetadata"` to `"checks role for demo routes via sessionClaims"`. Update the comment on line 147 from `// Per ACCESS-01: Check user.publicMetadata.role via clerkClient` to `// Per D-04: Read role from sessionClaims.metadata.role (no network call)`.
    - Replace positive assertions on lines 148-150:
      - `expect(middlewareContent).toContain("clerkClient")` → `expect(middlewareContent).toContain("sessionClaims")`
      - `expect(middlewareContent).toContain("publicMetadata")` → `expect(middlewareContent).toContain("metadata")`
      - Keep `expect(middlewareContent).toContain("role")` (still present in the new source)
    - Append two negative assertions immediately after the three positives:
      - `expect(middlewareContent).not.toContain("clerkClient")`
      - `expect(middlewareContent).not.toContain("publicMetadata")`
    - Append two Success Criterion #4 protection assertions (revision per checker warning #8):
      - `expect(middlewareContent).toContain("'/demo(.*)'")` — the literal demo-route matcher string MUST remain in the source so middleware continues to enforce role checks on `/demo/*` only.
      - `expect(middlewareContent).not.toMatch(/isDemoRoute[\s\S]*settings/i)` — `/settings` MUST NOT be added to the `isDemoRoute` matcher. This guards against an accidental scope-broadening that would break Phase 26 D-07 (settings always accessible to authenticated users regardless of role).

    Additionally, scan the rest of `src/middleware.test.ts` (outside the `it("checks role for demo routes via sessionClaims", ...)` block) for any stale `toContain("clerkClient")` or `toContain("publicMetadata")` positive assertions. Per D-09 broadened-scope (checker warning #6): if any such stale positive assertions exist in OTHER tests in this file, they MUST also be updated to reference the new shape (`"sessionClaims"` / `"metadata"`) or removed if they belong to a test that no longer makes sense. The acceptance criteria below enforce this via whole-file grep gates — do not let stale references survive.

    Do NOT change the surrounding describe block name (`"demo route protection"`), do NOT touch lines 1-12 (the jest.mock declarations — they remain unexercised by this source-string suite). All other source-string assertions reference strings (`"isDemoRoute"`, `"/demo(.*)"`, `"new URL('/', request.url)"`, `"NextResponse.redirect"`, `"NextResponse"`, `"from 'next/server'"`) that survive the middleware migration unchanged.

    This is the RED step. After this edit, `src/middleware.ts` still contains `"clerkClient"` and `"publicMetadata"` (we haven't migrated yet), so the new positive assertions for `"sessionClaims"`/`"metadata"` fail, and the new negative assertions for `"clerkClient"`/`"publicMetadata"` also fail. The Success Criterion #4 protection assertions (`'/demo(.*)'` positive and the settings-negative regex) should pass already against the un-migrated middleware because Phase 25 already wrote the correct matcher — they exist to lock that invariant forward. Verify both expected-failure modes before moving on.
  </action>
  <verify>
    <automated>npm test -- src/middleware.test.ts 2>&1 | grep -E "checks role for demo routes via sessionClaims|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'checks role for demo routes via sessionClaims' src/middleware.test.ts` matches exactly one line.
    - `grep -F 'checks role for demo routes via publicMetadata' src/middleware.test.ts` returns zero matches (old name removed).
    - In the demo-route-protection describe block, `grep -cE 'toContain\("sessionClaims"\)' src/middleware.test.ts` returns at least `1`.
    - `grep -cE 'toContain\("metadata"\)' src/middleware.test.ts` returns at least `1`.
    - `grep -cE '\.not\.toContain\("clerkClient"\)' src/middleware.test.ts` returns at least `1`.
    - `grep -cE '\.not\.toContain\("publicMetadata"\)' src/middleware.test.ts` returns at least `1`.
    - **D-09 narrowing fix (per checker warning #6):** No stale positive `toContain("clerkClient")` or `toContain("publicMetadata")` references remain anywhere in `src/middleware.test.ts`. Specifically:
      - `grep -F 'toContain("clerkClient")' src/middleware.test.ts` returns zero matches (whole-file scope).
      - `grep -F 'toContain("publicMetadata")' src/middleware.test.ts` returns zero matches (whole-file scope).
    - **Success Criterion #4 protection (per checker warning #8):** The middleware-content assertion includes both a positive `'/demo(.*)'` matcher check and a negative settings-in-demo-matcher check:
      - `grep -F "toContain(\"'/demo(.*)'\")" src/middleware.test.ts` matches (literal `'/demo(.*)'` is asserted as present in the middleware source).
      - `grep -F 'not.toMatch' src/middleware.test.ts` matches at least once AND the regex literal `/isDemoRoute[\s\S]*settings/i` appears alongside it. Acceptance check: `grep -F 'isDemoRoute' src/middleware.test.ts | grep -F 'settings'` returns at least one line in the rewritten test.
    - `npm test -- src/middleware.test.ts` exits non-zero — the rewritten test fails because `src/middleware.ts` still contains the old `clerkClient`/`publicMetadata` shape (RED state).
    - All OTHER tests in `src/middleware.test.ts` (config matcher, public route, auth.protect, redirect target, isDemoRoute) still pass — failure is isolated to the single rewritten test.
  </acceptance_criteria>
  <done>
    RED step complete: the renamed/rewritten "checks role for demo routes via sessionClaims" test fails against the un-migrated middleware, AND the whole-file grep gates confirm no stale `clerkClient`/`publicMetadata` positive assertions remain anywhere in the file (D-09 narrowing fix), AND the Success Criterion #4 protection (literal `'/demo(.*)'` + negative settings regex) is encoded. Commit message: `test(27-02): rewrite demo-route assertion for sessionClaims source (RED)`.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Migrate src/middleware.ts to read sessionClaims and drop clerkClient</name>
  <files>src/middleware.ts</files>
  <read_first>
    - `src/middleware.ts` (full file — the pre-migration source)
    - `src/middleware.test.ts` (updated in Task 1) — the contract this migration must satisfy
    - `src/types/clerk.d.ts` — confirms `CustomJwtSessionClaims.metadata.role: Role | undefined` is globally declared, so no `Role` import is needed in middleware after migration
    - `.planning/phases/27-role-assignment-and-testing/27-RESEARCH.md` §Pattern 2 — target shape verbatim
    - `.planning/phases/27-role-assignment-and-testing/27-PATTERNS.md` §`src/middleware.ts (MODIFY)` — exact diff regions to change and to leave alone
    - `.planning/phases/27-role-assignment-and-testing/27-CONTEXT.md` decisions D-04 (migrate to sessionClaims), D-06 (no fallback to getUser)
  </read_first>
  <action>
    Edit `src/middleware.ts`. Make exactly these changes:

    1. Line 1 — drop `clerkClient` from the named imports: change `import { clerkClient, clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";` to `import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";`.
    2. Line 3 — delete the entire `import type { Role } from '@/types/clerk';` line. The type narrowing now comes from the global `CustomJwtSessionClaims` augmentation; an explicit `Role` import would be unused (RESEARCH §Anti-Patterns to Avoid: "Don't import `Role` into `src/middleware.ts` after migration").
    3. Lines 29-43 — refactor the `if (isDemoRoute(request))` block to:
       - Destructure both `userId` and `sessionClaims` from `await auth()`: `const { userId, sessionClaims } = await auth();`
       - Keep the unauthenticated guard: `if (!userId) { const url = new URL('/', request.url); return NextResponse.redirect(url); }` (unchanged behavior — middleware still redirects to `/` for unauthenticated demo-route attempts).
       - Replace the entire three-line `clerkClient` block (current lines 36-38: `const client = ...; const user = ...; const role = ...;`) with a single guard: `if (sessionClaims?.metadata?.role !== 'demo') { const url = new URL('/', request.url); return NextResponse.redirect(url); }`. Note the string literal `'demo'` is used directly — no `Role` import needed; TypeScript narrows via `CustomJwtSessionClaims`.

    Do NOT touch:
    - Lines 5-18 (route matchers — `isPublicRoute`, `isDemoRoute`). In particular, the existing `createRouteMatcher(['/demo(.*)'])` MUST remain unchanged — `isDemoRoute` covers only `/demo/*`, NOT `/settings`. This is the Success Criterion #4 invariant the Task 1 test gate protects.
    - Lines 20-28 (the `clerkMiddleware` wrapper and `auth.protect()` call)
    - Lines 47-59 (the `config.matcher` export)

    Do NOT add any fallback path that invokes `clerkClient.users.getUser()` on missing claims (D-06: clean migration). If `sessionClaims?.metadata?.role` is undefined for any reason (user signed in before JWT template activated), the user gets the same redirect to `/` as any other non-demo user — `docs/clerk-setup.md` (Plan 03) documents the sign-out/sign-in remediation per D-07.

    Verify after the edit that `src/middleware.ts` no longer contains the literal strings `clerkClient` or `publicMetadata` anywhere — these are exactly the strings the Task 1 negative assertions check.
  </action>
  <verify>
    <automated>npm test -- src/middleware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "clerkClient" src/middleware.ts` returns zero matches (import dropped, no usage).
    - `grep -F "publicMetadata" src/middleware.ts` returns zero matches.
    - `grep -F "import type { Role }" src/middleware.ts` returns zero matches (unused after migration).
    - `grep -F "const { userId, sessionClaims } = await auth()" src/middleware.ts` matches exactly one line.
    - `grep -F "sessionClaims?.metadata?.role !== 'demo'" src/middleware.ts` matches exactly one line.
    - `grep -F "new URL('/', request.url)" src/middleware.ts` matches at least two lines (unauthenticated redirect + wrong-role redirect, both still target `/` per D-01 from Phase 25).
    - `grep -F "NextResponse.redirect" src/middleware.ts` matches at least two lines.
    - `grep -F "auth.protect()" src/middleware.ts` still matches (Phase 25 protection of non-public routes is preserved).
    - **Success Criterion #4 protection (per checker warning #8):** `grep -F "'/demo(.*)'" src/middleware.ts` matches (the literal demo-route matcher remains). `grep -E "createRouteMatcher\(\[[^]]*settings" src/middleware.ts` returns zero matches (settings is NOT in any createRouteMatcher call that defines demo-route enforcement).
    - `npm test -- src/middleware.test.ts` exits zero — the previously-failing "checks role for demo routes via sessionClaims" test now passes, AND every other test in the file still passes (config matcher, sign-in route, auth.protect, isDemoRoute, redirect target).
    - `npx tsc --noEmit` exits zero — no unused-import errors from the dropped `clerkClient`/`Role` imports.
  </acceptance_criteria>
  <done>
    GREEN step complete: middleware reads from sessionClaims, source-string suite green, TypeScript clean. Commit message: `refactor(27-02): migrate middleware to sessionClaims-based role read (GREEN)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full Jest regression check</name>
  <files></files>
  <read_first>
    - `src/middleware.ts` (post-migration)
    - `src/middleware.test.ts` (post-update)
  </read_first>
  <action>
    Run the full Jest suite to confirm the migration did not regress any other unit test in the repo. Specifically, the test files that touch Clerk-adjacent surfaces — `src/middleware.test.ts`, `src/components/Sidebar.test.tsx` (if it exists), and any page tests under `src/app/demo/**/*.test.tsx` — must all remain green. The middleware refactor is internally-scoped (no consumers import from `src/middleware.ts` directly), so cross-file impact is expected to be zero, but the regression run is the safety net.

    If any test fails, do NOT proceed to commit. Read the failure, determine if it is (a) a missed string-search assertion in `src/middleware.test.ts` itself, or (b) an unrelated breakage that must be fixed. Do NOT introduce ad-hoc fixes — the only changes permitted in this task are reverting the migration or fixing forgotten string assertions.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits zero. Output reports the baseline 304 tests (per STATE.md) all passing, with no regressions.
    - `npx tsc --noEmit` exits zero. No unused-import warnings or type errors.
    - `npm run lint 2>&1 | grep -iE "(error|warning)"` reports no new lint issues introduced by the middleware edit (if `lint` script does not exist, skip this assertion — it is informational).
  </acceptance_criteria>
  <done>
    Migration regression-clear. No additional commit needed (this task is a check, not a code change).
  </done>
</task>

</tasks>

<verification>
1. `npm test -- src/middleware.test.ts` — all middleware tests pass, including the new "via sessionClaims" assertion.
2. `npm test` — full suite (304 baseline) green.
3. `grep -F "clerkClient" src/middleware.ts` — no matches.
4. `grep -F "sessionClaims?.metadata?.role !== 'demo'" src/middleware.ts` — exactly one match.
5. `grep -F "'/demo(.*)'" src/middleware.ts` — matches (demo-route matcher preserved; Success Criterion #4 invariant).
6. `grep -F 'toContain("clerkClient")' src/middleware.test.ts` — zero matches (D-09 narrowing fix; no stale positive assertions anywhere in the file).
7. `grep -F 'toContain("publicMetadata")' src/middleware.test.ts` — zero matches.
8. `npx tsc --noEmit` — clean.
</verification>

<success_criteria>
- `src/middleware.ts` reads role from `sessionClaims.metadata.role` only; no `clerkClient` or `Role` import remains.
- `src/middleware.ts` retains the literal `'/demo(.*)'` route matcher; `isDemoRoute` does NOT include `/settings` (Success Criterion #4 invariant — settings remains accessible to all authenticated users per Phase 26 D-07).
- `src/middleware.test.ts` source-string assertions match the new shape (positive `sessionClaims`/`metadata`, negative `clerkClient`/`publicMetadata`) AND no stale positive references to `clerkClient`/`publicMetadata` survive anywhere in the file (D-09 broadened scope per checker warning #6).
- `src/middleware.test.ts` includes a unit-level assertion that the demo-route matcher contains the literal `'/demo(.*)'` and does NOT extend to settings — closes Success Criterion #4 protection at the unit layer, not only at E2E (per checker warning #8).
- All existing middleware tests for matcher config, public routes, auth.protect, and redirect target continue to pass unchanged.
- Full Jest suite green (no regressions).
- Two atomic commits land (RED → GREEN).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge middleware | Untrusted cookie crosses boundary; Clerk's `auth()` validates the session JWT signature before middleware reads the role claim. |
| Edge middleware → Route handler | Trusted; middleware has already enforced both auth.protect() and role check before the handler runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Spoofing | Forged role claim in cookie | mitigate | `auth()` validates the session JWT signature using Clerk's published JWKS; an attacker cannot inject a `metadata.role: 'demo'` claim into a cookie without forging Clerk's signature. |
| T-27-06 | Tampering | JWT template not active in Dashboard → claim is `undefined` for all users | mitigate | The default-deny posture (`!== 'demo'` redirects to `/`) means a missing claim fails closed. Plan 03 documents the activation in `docs/clerk-setup.md`. RESEARCH §Pitfall 1 names this failure mode. |
| T-27-07 | Denial of Service | Per-request Backend API call latency (pre-migration) | mitigate (delivered by this plan) | The migration eliminates the `clerkClient.users.getUser()` Edge-runtime network call; role check is now constant-time local read. Closes Phase 25 success criterion #1. |
| T-27-08 | Elevation of Privilege | Stale session token after role assignment | accept | Per D-07, propagation requires sign-out/sign-in. Programmatic session revocation is out of scope for v1.5; documented operational procedure. |
| T-27-21 | Elevation of Privilege | Accidental inclusion of `/settings` in `isDemoRoute` matcher | mitigate | Task 1 RED-step assertion `expect(middlewareContent).not.toMatch(/isDemoRoute[\s\S]*settings/i)` locks the invariant at the unit layer; CI fails immediately if the matcher is widened. Preserves Phase 26 D-07 (settings always accessible). |
</threat_model>

<output>
After completion, create `.planning/phases/27-role-assignment-and-testing/27-02-SUMMARY.md`
</output>
