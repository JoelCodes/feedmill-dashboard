---
phase: 10-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - designs/customers.pen
  - designs/customer-detail.pen
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03

must_haves:
  truths:
    - "customers.pen exists with customer list view design"
    - "customer-detail.pen exists with detail page layout"
    - "Bin gauges show vertical tank visualization with color thresholds"
    - "Timeline extends existing TimelineItem pattern"
    - "All colors reference design tokens from globals.css"
  artifacts:
    - path: "designs/customers.pen"
      provides: "Customer list view design"
      contains: "CustomerTable"
    - path: "designs/customer-detail.pen"
      provides: "Customer detail page design"
      contains: "BinGaugeRow"
  key_links:
    - from: "designs/customers.pen"
      to: "designs/order-dashboard.pen"
      via: "table row and search box patterns"
      pattern: "Table Header|Search"
    - from: "designs/customer-detail.pen"
      to: "designs/order-dashboard.pen"
      via: "timeline item pattern"
      pattern: "step1|leftCol|rightCol"
---

<objective>
Create Pencil.dev design files for the customers page: customer list view (customers.pen) and customer detail page (customer-detail.pen) with bin visualization and activity timeline.

Purpose: Establish visual contract before implementation in Phases 12-15. Designs will be referenced by frontend developers to build components matching exact specifications.

Output: Two .pen design files following established project patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-design/10-CONTEXT.md
@.planning/phases/10-design/10-UI-SPEC.md
@.planning/phases/10-design/10-PATTERNS.md

<interfaces>
<!-- Design tokens from globals.css to reference in .pen files -->
Color tokens (hex with alpha):
- background: #f8f9faff
- card/white: #ffffffff
- primary/accent: #4fd1c5ff
- text-primary: #2d3748ff
- text-secondary: #a0aec0ff
- border: #e2e8f0ff
- success (bin normal): #48bb78ff
- warning (bin low): #975a16ff
- error (bin critical): #e53e3eff

Typography:
- Small/Label: fontSize 10, fontWeight 700, lineHeight 1.2
- Body: fontSize 12, fontWeight normal, lineHeight 1.5
- Heading: fontSize 16, fontWeight 700, lineHeight 1.2
- Display: fontSize 20, fontWeight 700, lineHeight 1.2

Spacing (4px-based):
- xs: 4, sm: 8, md: 16, lg: 24, xl: 32

Icons (lucide iconFontFamily):
- search, package, alert-triangle, clock, map-pin, phone, mail, chevron-down
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create customers.pen (Customer List View)</name>
  <files>designs/customers.pen</files>
  <read_first>
    - designs/order-dashboard.pen (table and search box patterns)
    - designs/page-layout.pen (sidebar + main content structure)
    - .planning/phases/10-design/10-PATTERNS.md (extracted patterns with line numbers)
    - .planning/phases/10-design/10-UI-SPEC.md (component inventory and states)
    - .planning/phases/10-design/10-CONTEXT.md (decisions D-01 through D-04, D-13, D-14, D-16)
  </read_first>
  <action>
Create `designs/customers.pen` as a JSON file with version "2.8" following the exact structure from order-dashboard.pen.

**Page Structure (per D-01, D-04, D-16):**
- Root frame: width 1920, height 1200, fill #f8f9faff, name "Customer List View"
- Left: Sidebar frame (280px width, reuse exact structure from page-layout.pen including logo, nav items)
- Right: Main Content frame (fill_container width, padding [24, 32])

**Main Content Children:**

1. **Header Row** (reuse from order-dashboard.pen lines 471-616):
   - Frame with justifyContent "space_between"
   - Left: Text "Customers" fontSize 20, fontWeight 700, fill #2d3748ff
   - Right: Search box (copy exact structure from order-dashboard.pen lines 549-592)
     - Frame width 200, height 40, cornerRadius 15, fill #ffffffff
     - Stroke: align "inside", thickness 0.5, fill #e2e8f0ff
     - Children: icon_font (search, lucide, 14x14, #a0aec0ff) + text placeholder "Search customers by name..." fontSize 12, #a0aec0ff

2. **CustomerTable** (per D-01, D-02, D-03):
   - Frame name "CustomerTable", width fill_container, layout vertical

   **Table Header** (per UI-SPEC and PATTERNS.md lines 88-154):
   - Frame padding [10, 0], layout horizontal
   - Column 1: "CUSTOMER NAME" - text fontSize 10, fontWeight 700, fill #a0aec0ff, width fill_container
   - Column 2: "STATUS" - text fontSize 10, fontWeight 700, fill #a0aec0ff, width 120
   - Followed by divider rectangle height 1, fill #e2e8f0ff

   **Table Rows** (create 6 sample rows per PATTERNS.md lines 156-221):
   Each row is a frame with padding [12, 0], layout horizontal, children:

   Row 1 - "Riverside Poultry Farm":
   - Column 1: Text fontSize 12, fontWeight 700, fill #2d3748ff
   - Column 2: StatusIndicators frame (gap 8, alignItems center):
     - icon_font: package (lucide), 14x14, #4fd1c5ff (has 3 orders)
     - ellipse: 8x8, #e53e3eff (has changes)
     - icon_font: alert-triangle (lucide), 14x14, #975a16ff (low bin alert)

   Row 2 - "Valley View Farms":
   - Status: package icon only (2 orders, no changes, no bin alerts)

   Row 3 - "Green Acres Ranch":
   - Status: package icon + alert-triangle #e53e3eff (critical bin alert)

   Row 4 - "Sunrise Feedlot":
   - Status: package icon only (1 order)

   Row 5 - "Mountain Peak Dairy":
   - Status: package icon + changes dot + alert-triangle #975a16ff

   Row 6 - "Cedar Creek Farm":
   - Status: no icons (no active orders, bins OK)

   Add hover state frame variant named "Row Hover" with fill #f7fafcff (gray-50 equivalent)

**Empty State** (per UI-SPEC copywriting contract):
- Create additional frame "Empty State" positioned below table
- Center-aligned text: "No customers found" fontSize 16, fontWeight 700, fill #2d3748ff
- Subtext: "Try adjusting your search or check back later for customer data." fontSize 12, fill #a0aec0ff
  </action>
  <verify>
    <automated>grep -c '"version": "2.8"' designs/customers.pen | grep -q "^1$" && grep -c '"name": "CustomerTable"' designs/customers.pen | grep -q "^1$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - designs/customers.pen exists and is valid JSON
    - File contains `"version": "2.8"` on line 2
    - File contains frame named "Customer List View" as root
    - File contains frame named "CustomerTable"
    - File contains icon_font with iconFontName "search"
    - File contains icon_font with iconFontName "package"
    - File contains icon_font with iconFontName "alert-triangle"
    - File contains text "CUSTOMER NAME" for table header
    - File contains text "STATUS" for table header
    - File contains 6 customer names: Riverside Poultry Farm, Valley View Farms, Green Acres Ranch, Sunrise Feedlot, Mountain Peak Dairy, Cedar Creek Farm
    - File contains text "No customers found" for empty state
    - All fill colors use tokens from globals.css (no arbitrary hex values)
  </acceptance_criteria>
  <done>DSGN-01 satisfied: Customer list view designed in customers.pen with CustomerTable (6 rows, search box, status indicators, empty state) matching order-dashboard.pen patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create customer-detail.pen (Customer Detail Page)</name>
  <files>designs/customer-detail.pen</files>
  <read_first>
    - designs/order-dashboard.pen (timeline pattern lines 3367-3453)
    - designs/page-layout.pen (header structure)
    - .planning/phases/10-design/10-PATTERNS.md (vertical gauge pattern lines 408-475, timeline pattern lines 316-406)
    - .planning/phases/10-design/10-UI-SPEC.md (BinGaugeRow, ActivityTimeline, CustomerDetailHeader specs)
    - .planning/phases/10-design/10-CONTEXT.md (decisions D-05 through D-12, D-13, D-15)
  </read_first>
  <action>
Create `designs/customer-detail.pen` as a JSON file with version "2.8" following established patterns.

**Page Structure (per D-05, D-08):**
- Root frame: width 1920, height 1200, fill #f8f9faff, name "Customer Detail Page"
- Left: Sidebar frame (280px, same as customers.pen)
- Right: Main Content frame (fill_container, layout vertical, gap 24, padding [24, 32])

**Main Content Children:**

1. **CustomerDetailHeader** (per D-06, UI-SPEC lines 147-149):
   - Frame name "CustomerDetailHeader", width fill_container, layout horizontal, justifyContent space_between, alignItems flex_start
   - Left side (contact card frame, layout vertical, gap 4):
     - Name: Text "Riverside Poultry Farm" fontSize 20, fontWeight 700, fill #2d3748ff
     - Location row (gap 8, alignItems center):
       - icon_font: map-pin (lucide), 14x14, #a0aec0ff
       - Text "123 Farm Road, Springfield, IL 62701" fontSize 12, fill #a0aec0ff
     - Phone row (gap 8, alignItems center):
       - icon_font: phone (lucide), 14x14, #a0aec0ff
       - Text "(555) 123-4567" fontSize 12, fill #a0aec0ff
     - Email row (gap 8, alignItems center):
       - icon_font: mail (lucide), 14x14, #a0aec0ff
       - Text "orders@riversidepoultry.com" fontSize 12, fill #a0aec0ff
     - Delivery preferences: Text "Delivery: Mon/Wed/Fri, 6-8 AM" fontSize 10, fontWeight 700, fill #4fd1c5ff
   - Right side (summary stats, layout horizontal, gap 16):
     - Stat card 1: "12" (fontSize 20, bold) + "Total Orders" (fontSize 10, secondary)
     - Stat card 2: "4" (fontSize 20, bold) + "Active Bins" (fontSize 10, secondary)
     - Stat card 3: "8" (fontSize 20, bold) + "Recent Activity" (fontSize 10, secondary)

2. **Section Divider**: Rectangle height 1, fill #e2e8f0ff, width fill_container

3. **BinGaugeRow Section** (per D-07 - bins first, D-09, D-10, D-11, D-12):
   - Section header: Text "Feed Bins" fontSize 16, fontWeight 700, fill #2d3748ff
   - Frame name "BinGaugeRow", width fill_container, gap 24, layout horizontal, alignItems flex_end

   **Create 4 bin gauges using PATTERNS.md lines 408-475 structure:**

   Bin 1 - Normal (75%):
   - Gauge Container: frame 40x100, cornerRadius 8, stroke #e2e8f0ff thickness 2
   - Fill Bar: rectangle x=2, y=25, width 36, height 73 (75% of 100-4 for stroke), fill #48bb78ff, cornerRadius [0,0,6,6]
   - Percentage: text "75%" centered in gauge, fontSize 12, fontWeight 700, fill #ffffffff
   - Labels below: "Bin A" fontSize 10, bold, #2d3748ff + "Starter" fontSize 10, #a0aec0ff

   Bin 2 - Normal (62%):
   - Fill height 60 (62%), fill #48bb78ff
   - Labels: "Bin B" + "Grower"

   Bin 3 - Low (22%):
   - Fill height 21 (22%), fill #975a16ff (warning)
   - Labels: "Bin C" + "Finisher"

   Bin 4 - Critical (8%):
   - Fill height 8 (8%), fill #e53e3eff (error)
   - Labels: "Bin D" + "Starter"

4. **Section Divider**: Rectangle height 1, fill #e2e8f0ff

5. **ActivityTimeline Section** (per D-07, D-15, UI-SPEC lines 166-170):
   - Section header: Text "Activity Timeline" fontSize 16, fontWeight 700, fill #2d3748ff
   - Frame name "ActivityTimeline", width fill_container, layout vertical

   **Create timeline items using PATTERNS.md lines 316-406 structure:**
   Each item: frame layout horizontal, gap 14, children = leftCol (36px, icon circle + connector) + rightCol (content)

   Item 1 - Order Event (expanded):
   - Icon: file-text in 28x28 circle #4fd1c5ff, white icon
   - Connector: 2px wide rectangle fill #4fd1c5ff, height fill_container
   - Title: "Order #ORD-2026-0847 Placed" fontSize 13, bold, #2d3748ff
   - Description: "20 tons Starter feed ordered for delivery" fontSize 11, #a0aec0ff
   - Date: "Apr 28, 2026 · 2:15 PM" fontSize 10, bold, #4fd1c5ff
   - Expanded detail frame (padding 12, fill #f8f9faff, cornerRadius 8):
     - "Quantity: 20 tons" + "Product: Starter" + "Status: In Production"
     - Link text: "View Order Details" fontSize 10, fill #4fd1c5ff, underline

   Item 2 - Delivery Event (collapsed):
   - Icon: truck in 28x28 circle #48bb78ff
   - Connector: #48bb78ff
   - Title: "Delivery Completed - Bin A"
   - Description: "15 tons Grower feed delivered"
   - Date: "Apr 25, 2026 · 7:30 AM"

   Item 3 - Bin Alert Event (collapsed):
   - Icon: alert-triangle in 28x28 circle #975a16ff
   - Connector: #975a16ff
   - Title: "Low Feed Alert - Bin C"
   - Description: "Finisher feed level dropped below 30%"
   - Date: "Apr 24, 2026 · 11:00 AM"

   Item 4 - Order Event (collapsed):
   - Same pattern as Item 1 but collapsed (no expanded detail frame)

   Create 6+ more timeline items to demonstrate scrolling behavior.

   Last item has no connector (showConnector: false)

**Interaction State Variants** (per UI-SPEC lines 178-185):
- Create frame "Timeline Item Hover" showing hover state
- Create frame "Timeline Item Expanded" showing full expanded state
- Create frame "BinGauge Hover" showing tooltip with full metadata
  </action>
  <verify>
    <automated>grep -c '"version": "2.8"' designs/customer-detail.pen | grep -q "^1$" && grep -c '"name": "BinGaugeRow"' designs/customer-detail.pen | grep -q "^1$" && grep -c '"name": "ActivityTimeline"' designs/customer-detail.pen | grep -q "^1$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - designs/customer-detail.pen exists and is valid JSON
    - File contains `"version": "2.8"` on line 2
    - File contains frame named "Customer Detail Page" as root
    - File contains frame named "CustomerDetailHeader"
    - File contains frame named "BinGaugeRow"
    - File contains frame named "ActivityTimeline"
    - File contains 4 bin gauges with different fill levels
    - Bin gauge fills use colors: #48bb78ff (normal), #975a16ff (low), #e53e3eff (critical)
    - File contains icon_font with iconFontName "map-pin"
    - File contains icon_font with iconFontName "phone"
    - File contains icon_font with iconFontName "mail"
    - File contains icon_font with iconFontName "alert-triangle"
    - File contains text "Feed Bins" for section header
    - File contains text "Activity Timeline" for section header
    - File contains at least 6 timeline items
    - At least one timeline item shows expanded state with detail frame
    - All fill colors use tokens from globals.css
  </acceptance_criteria>
  <done>DSGN-02 satisfied: Customer detail page layout designed in customer-detail.pen with CustomerDetailHeader (contact card, summary stats). DSGN-03 satisfied: Bin visualization with fill bars and alert states designed in BinGaugeRow (4 vertical tank gauges showing normal/low/critical thresholds) and ActivityTimeline (10+ events with expand/collapse)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Design phase - no runtime code, data processing, or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | .pen files in git | accept | Design files are non-sensitive visual specs; no secrets or PII |

**Note:** Phase 10 creates static design artifacts (.pen JSON files) with no code execution. Security requirements apply to implementation phases (12-15) where designs are converted to React components handling user input.
</threat_model>

<verification>
## Phase Verification

After both tasks complete:

1. **File existence:**
   ```bash
   ls -la designs/customers.pen designs/customer-detail.pen
   ```

2. **JSON validity:**
   ```bash
   node -e "JSON.parse(require('fs').readFileSync('designs/customers.pen'))"
   node -e "JSON.parse(require('fs').readFileSync('designs/customer-detail.pen'))"
   ```

3. **Pattern compliance:**
   ```bash
   # Verify version
   grep '"version": "2.8"' designs/customers.pen designs/customer-detail.pen

   # Verify required components exist
   grep -l "CustomerTable" designs/customers.pen
   grep -l "BinGaugeRow" designs/customer-detail.pen
   grep -l "ActivityTimeline" designs/customer-detail.pen

   # Verify color tokens used (no arbitrary colors)
   grep -E "#[0-9a-f]{6}ff" designs/customers.pen | grep -v -E "(f8f9fa|ffffff|4fd1c5|2d3748|a0aec0|e2e8f0|48bb78|975a16|e53e3e|f7fafc)"
   # Should return nothing (all colors from token set)
   ```

4. **Icon library compliance:**
   ```bash
   grep "iconFontFamily" designs/customers.pen designs/customer-detail.pen | grep -v lucide
   # Should return nothing (all icons from lucide)
   ```
</verification>

<success_criteria>
- [ ] designs/customers.pen exists with valid JSON structure
- [ ] designs/customer-detail.pen exists with valid JSON structure
- [ ] Both files use version "2.8" format
- [ ] customers.pen contains CustomerTable with 6 sample rows
- [ ] customers.pen contains search box matching Header pattern
- [ ] customers.pen contains StatusIndicators with stacked icons (per D-03)
- [ ] customer-detail.pen contains CustomerDetailHeader with full contact card (per D-06)
- [ ] customer-detail.pen contains BinGaugeRow with 4 vertical tank gauges (per D-09)
- [ ] customer-detail.pen shows bin color thresholds: green/yellow/red (per D-10)
- [ ] customer-detail.pen contains ActivityTimeline with expand/collapse states (per D-15)
- [ ] All designs use colors from globals.css token set (per D-13)
- [ ] All icons use lucide iconFontFamily (per D-14)
- [ ] DSGN-01 satisfied: Customer list view designed in customers.pen
- [ ] DSGN-02 satisfied: Customer detail page layout designed in customer-detail.pen
- [ ] DSGN-03 satisfied: Bin visualization with fill bars and alert states designed in customer-detail.pen
</success_criteria>

<output>
After completion, create `.planning/phases/10-design/10-01-SUMMARY.md`
</output>
