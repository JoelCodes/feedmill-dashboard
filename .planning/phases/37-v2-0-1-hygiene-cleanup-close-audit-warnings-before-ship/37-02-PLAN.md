---
phase: 37
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
tags:
  - docs-hygiene
  - traceability-table
  - milestone-audit-closure
must_haves:
  decisions_covered:
    - "scope-lock: flip 45 checkboxes (lines 14-73) AND 45 traceability-table cells (lines 127-171). Use `[x]` + `Complete` (matches v1.5-REQUIREMENTS.md verbatim per RESEARCH §Warning 2 §Pattern 2)."
    - "atomic commit: both edit zones land in ONE docs commit (Pitfall 6 — avoid checkbox/cell desync). Subject: `docs(37-02): mark 45 v2.0 requirements Complete in REQUIREMENTS.md`."
    - "edit-tool guidance per RESEARCH §Don't Hand-Roll: do NOT use `sed -i` for either zone (over-match risk on the 90 lines). Use the Edit tool with exact-string match per row, or multi-line Edit blocks if executor first reads the file in full."
    - "immutable audit records (D-13 per v1.5 30-02-PLAN.md:27): do NOT edit `.planning/v2.0-MILESTONE-AUDIT.md` or `.planning/integration-check-v2.0-postphase36.md`."
  truths:
    - "Every `- [ ] **{REQ-ID}**:` in lines 14-73 becomes `- [x] **{REQ-ID}**:` (DATA-01..08, AUTH-01..04, PROD-01..11, TRANS-01..07, IMPORT-01..07, KPI-01..08 = 45 IDs)"
    - "Every `| Pending |` cell in lines 127-171 becomes `| Complete |` (45 cells)"
    - "Markdown body outside the two edit zones is byte-for-byte preserved"
    - "Audit re-run (Plan 37-05) confirms `Traceability [x]` count flips from 0/45 to 45/45"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "45 checked requirements + 45 traceability cells marked Complete"
      contains: "- [x] **DATA-01**:"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table (lines 127-171)"
      to: "VERIFICATION.md SATISFIED status per REQ-ID"
      via: "audit `Traceability [x]` count"
      pattern: "| {REQ-ID} | Phase {N} | Complete |"
---

<objective>
Close v2.0 milestone-audit cross-phase tech-debt item 2: REQUIREMENTS.md traceability table — all 45 status cells still read `Pending` despite VERIFICATION.md tables confirming SATISFIED. Mirrors v1.5 INT-07 traceability format (`.planning/milestones/v1.5-REQUIREMENTS.md:21-37` + `:73-81`).

Two edit zones inside a single file (`.planning/REQUIREMENTS.md`):
1. **Zone 1 — Requirement definition list (lines 14-73, 45 items):** Every `- [ ] **{REQ-ID}**:` becomes `- [x] **{REQ-ID}**:`. The remainder of each line (the description text) is preserved byte-for-byte.
2. **Zone 2 — Traceability table (lines 127-171, 45 rows):** Every `| Pending |` cell becomes `| Complete |`. Use `Complete` (NOT `Done`) — matches v1.5-REQUIREMENTS.md:73-81 verbatim per RESEARCH §"Warning 2 §Pattern 2".

The two zones MUST land in ONE atomic docs commit (Pitfall 6 in RESEARCH — checkbox/cell desync risk).

Purpose: After this plan, `/gsd:audit-milestone v2.0` (Plan 37-05) re-parses REQUIREMENTS.md traceability and reports `Traceability [x] 45/45` instead of `0/45`. Closes audit `tech_debt.cross-phase` item 2.

Output: `.planning/REQUIREMENTS.md` updated; 45 checkboxes flipped to `[x]`; 45 table cells flipped to `Complete`. Single atomic docs commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-RESEARCH.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md
@.planning/milestones/v1.5-REQUIREMENTS.md
@.planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md

<interfaces>
<!-- Verbatim format from the v1.5 precedent. Executor uses as-is — no codebase exploration needed. -->

Zone 1 target shape (from .planning/milestones/v1.5-REQUIREMENTS.md:21-37 — verbatim except for REQ-ID literals):
  - [x] **DATA-01**: Neon Postgres project provisioned; pooled (`-pooler.neon.tech`) and direct connection URLs stored in Vercel env as `DATABASE_URL` and `DATABASE_URL_UNPOOLED`.
  - [x] **DATA-02**: Drizzle schema for `production_orders` table...
  ...

Zone 2 target shape (from .planning/milestones/v1.5-REQUIREMENTS.md:73-81 — verbatim column reads "Complete"):
  | REQ-ID | Phase | Status |
  |--------|-------|--------|
  | AUTH-01 | Phase 31 | Complete |
  | AUTH-02 | Phase 31 | Complete |
  ...

The 45 REQ-IDs (verified this session via grep of .planning/REQUIREMENTS.md):
- DATA-01..DATA-08 (8 IDs, Zone 1 lines 14-21, Zone 2 in `| DATA-NN | Phase 31|32 | Pending |` rows)
- AUTH-01..AUTH-04 (4 IDs, Zone 1 lines 25-28, Zone 2 in `| AUTH-NN | Phase 31 | Pending |`)
- PROD-01..PROD-11 (11 IDs, Zone 1 lines 32-42, Zone 2 in `| PROD-NN | Phase 34 | Pending |`)
- TRANS-01..TRANS-07 (7 IDs, Zone 1 lines 46-52, Zone 2 in `| TRANS-NN | Phase 33 | Pending |`)
- IMPORT-01..IMPORT-07 (7 IDs, Zone 1 lines 56-62, Zone 2 in `| IMPORT-NN | Phase 33 | Pending |`)
- KPI-01..KPI-08 (8 IDs, Zone 1 lines 66-73, Zone 2 in `| KPI-NN | Phase 35 | Pending |`)

Total: 8 + 4 + 11 + 7 + 7 + 8 = 45 (matches `requirements_in_scope: 45/45` in v2.0-MILESTONE-AUDIT.md).

DO NOT TOUCH outside the two zones:
- Lines 1-13 (preamble / `## v2.0 Requirements` heading)
- Lines 22-24, 29-31, 43-45, 53-55, 63-65 (section sub-headings between requirement groups: `### AUTH`, `### PROD`, etc.)
- Lines 75-122 (Future Requirements + Out of Scope sections — these have INTENTIONAL `- [ ]` style for deferred items, do NOT flip)
- Lines 123-126 (`---` separator + `## Traceability` heading + table header rows)
- Lines 172-end (Decision Log + footer)

Pitfall callouts (from RESEARCH §"Pitfall 1-5"):
- Pitfall 1 ("Pending" appears in non-status contexts): DATA-02 description (line 15) contains "Pending/Mixing/Completed/Blocked"; PROD-03 description (line 34) contains "Pending, Mixing, Completed, Blocked"; PROD-07 (line 38), TRANS-01 (line 46), TRANS-04 (line 49), KPI-04 (line 69) also contain the word "Pending" as part of state machine description. These MUST NOT be edited — only the TABLE CELL `| Pending |` form is flipped. The acceptance grep specifically uses `'| Pending |'` (pipe-space-Pending-space-pipe) to avoid false-positive matches.
- Pitfall 2 (`Pending` cell count is 45, NOT total occurrences of "Pending"): use exact-cell grep, not free-text grep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zone 1 — flip 45 checkboxes in requirement definition list (lines 14-73)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — verify exact current shape of lines 14-73; confirm 45 `- [ ] **REQ-ID**:` lines; observe that lines 22-24, 29-31, 43-45, 53-55, 63-65 are section sub-headings (NOT checkboxes); confirm lines 75+ are out-of-scope Future Requirements / Out of Scope sections that MUST NOT be flipped)
    - .planning/milestones/v1.5-REQUIREMENTS.md (lines 21-37 — verbatim target shape: `- [x] **REQ-ID**:` format)
    - .planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md (Task 1 pattern — single-file, exact-string-match edits with grep-based acceptance criteria)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md (W2 section)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Edit 45 lines, each in the range 14-73 (verified per RESEARCH §"Warning 2 §Edit 1"). For EACH of the 45 lines matching `- [ ] **{REQ-ID}**: ...`, change the leading `[ ]` to `[x]`, preserving the rest of the line byte-for-byte (including the bold REQ-ID, the colon, and the description text).

    REQ-IDs to flip (exhaustive list — copy verbatim into Edit-tool exact-string-match operations):
    - DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08 (8 lines, range 14-21)
    - AUTH-01, AUTH-02, AUTH-03, AUTH-04 (4 lines, range 25-28)
    - PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07, PROD-08, PROD-09, PROD-10, PROD-11 (11 lines, range 32-42)
    - TRANS-01, TRANS-02, TRANS-03, TRANS-04, TRANS-05, TRANS-06, TRANS-07 (7 lines, range 46-52)
    - IMPORT-01, IMPORT-02, IMPORT-03, IMPORT-04, IMPORT-05, IMPORT-06, IMPORT-07 (7 lines, range 56-62)
    - KPI-01, KPI-02, KPI-03, KPI-04, KPI-05, KPI-06, KPI-07, KPI-08 (8 lines, range 66-73)

    Total: 45 flips.

    Recommended approach (per RESEARCH §"Don't Hand-Roll"): For each REQ-ID, use the Edit tool with `old_string` set to `- [ ] **{REQ-ID}**:` and `new_string` set to `- [x] **{REQ-ID}**:`. This exact-string match avoids over-matching the word "Pending" or other text on the same line. Do NOT use `sed -i`.

    Do NOT touch the "Future Requirements" section (lines 75+) or "Out of Scope" section (lines 100+) — those `- [ ]` style markers belong to DEFERRED items and MUST stay unchecked.

    Do NOT commit yet — Task 3 handles the atomic commit.
  </action>
  <verify>
    <automated>grep -cE '^- \[x\] \*\*(DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9]\*\*:' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 45 checked v2.0 requirements: `grep -cE '^- \[x\] \*\*(DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9]\*\*:' .planning/REQUIREMENTS.md` returns `45`.
    - Zero unchecked v2.0 requirements remain: `grep -cE '^- \[ \] \*\*(DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9]\*\*:' .planning/REQUIREMENTS.md` returns `0`.
    - Future / Out-of-Scope unchecked items preserved: `grep -cE '^- \[ \] \*\*(TRANS-FUT|PROD-FUT|IMPORT-FUT|KPI-FUT|REALTIME-FUT)' .planning/REQUIREMENTS.md` returns a count consistent with the file's current FUT-marker count (executor should record the pre-edit count first, then verify post-edit count is unchanged — RESEARCH lists at least 15 deferred items in §Future Requirements).
    - Diff is purely 45 character flips in Zone 1: `git diff .planning/REQUIREMENTS.md | grep -E '^[-+]- \[' | wc -l | tr -d ' '` returns `90` (45 `-` removals + 45 `+` additions from the checkbox-only changes).
  </acceptance_criteria>
  <done>Zone 1 complete: 45 v2.0 requirement checkboxes flipped from `[ ]` to `[x]`. Deferred and Out-of-Scope items preserved. Not yet committed — Task 3 commits.</done>
</task>

<task type="auto">
  <name>Task 2: Zone 2 — flip 45 traceability-table status cells (lines 127-171)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — verify current shape of lines 127-171; confirm 45 rows of form `| {REQ-ID} | Phase {N} | Pending |`)
    - .planning/milestones/v1.5-REQUIREMENTS.md (lines 73-81 — verbatim target shape: column reads `Complete`)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md (W2 section — explicit "use `Complete` not `Done`" guidance)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-RESEARCH.md (§"Warning 2 §Pitfall 1" — the description text of several REQs contains the WORD "Pending" as part of state machine prose; the cell flip MUST only touch `| Pending |` form)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Edit 45 lines in the traceability table (range 127-171). For each row matching `| {REQ-ID} | Phase {N} | Pending |`, change `| Pending |` to `| Complete |`. Other cells (REQ-ID, Phase) preserved byte-for-byte.

    Use `Complete` (capital C, no trailing punctuation) — matches v1.5-REQUIREMENTS.md:73-81 verbatim per RESEARCH §"Warning 2 §Pattern 2". Do NOT use `Done` or any other variant — uniformity is required for the audit's `Traceability [x]` count gate.

    Recommended approach (per RESEARCH §"Don't Hand-Roll" + PATTERNS §"Edit-tool guidance"):
    - Option A (per-row): 45 Edit invocations, each replacing one exact-match row (preserves any incidental whitespace differences between rows). For REQ-IDs that appear in MULTIPLE places in the file (e.g., DATA-02 appears as `**DATA-02**` in Zone 1 and `| DATA-02 |` in Zone 2), the row form `| DATA-02 | Phase 32 | Pending |` is unique to Zone 2.
    - Option B (multi-line block): Read the full file first, then perform a single multi-line Edit replacing the entire table block (lines 127-171) with the post-edit form. Faster but requires careful preservation of phase-number assignments per row.

    Do NOT use `sed -i` — RESEARCH §"Don't Hand-Roll" warns the 45 cells × 2 zones risks over-matching. The Edit tool's exact-string match is safer.

    Do NOT touch the table header (lines 125-126: `| REQ-ID | Phase | Status |` + `|--------|-------|--------|`).
    Do NOT touch the Decision Log (lines 172+ if present).

    Do NOT commit yet — Task 3 handles the atomic commit.
  </action>
  <verify>
    <automated>grep -c '| Complete |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 45 `| Complete |` cells: `grep -c '| Complete |' .planning/REQUIREMENTS.md` returns `45`.
    - Zero `| Pending |` cells remain: `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns `0`.
    - Free-text "Pending" still appears in state-machine descriptions (Pitfall 1 sanity check): `grep -c 'Pending' .planning/REQUIREMENTS.md` returns a number >= 6 (DATA-02 + PROD-03 + PROD-07 + TRANS-01 + TRANS-04 + KPI-04 descriptions each contain the word, per RESEARCH Pitfall 1). These MUST be preserved.
    - All 45 REQ-IDs still present in Zone 2 table rows: `grep -cE '^\| (DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9] \| Phase 3[1-5] \| Complete \|$' .planning/REQUIREMENTS.md` returns `45`.
    - Diff in this zone is purely 45 cell flips: `git diff .planning/REQUIREMENTS.md | grep -cE '^[-+]\| (DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9] '` returns `90` (45 `-` + 45 `+`).
  </acceptance_criteria>
  <done>Zone 2 complete: 45 traceability-table cells flipped from `Pending` to `Complete`. State-machine description prose preserved. Not yet committed — Task 3 commits.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-cut verification + atomic docs commit (both zones)</name>
  <files>(none — verification + commit only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (re-read full file post-edit; spot-check Zone 1 + Zone 2 are both updated; spot-check Future/Out-of-Scope sections untouched)
    - .planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md (Task 4 pattern — atomic commit shape at lines 288-316; explicit `git add` paths; heredoc commit message)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md (Shared Pattern 2 — explicit `git add`; Shared Pattern 3 — heredoc commit; Shared Pattern 6 — D-13 immutability)
    - .planning/v2.0-MILESTONE-AUDIT.md (read-only — verify NOT in git diff)
    - .planning/integration-check-v2.0-postphase36.md (read-only — verify NOT in git diff)
  </read_first>
  <action>
    Cross-cut verification + single atomic commit per D-12 convention. Both Zone 1 + Zone 2 edits land in ONE commit (Pitfall 6 — desync risk).

    Step 1 (Zone 1 + Zone 2 sanity grep): Re-run both verify commands from Tasks 1 + 2. Confirm 45 `[x]` checkboxes AND 45 `| Complete |` cells AND zero `[ ]` v2.0 requirements AND zero `| Pending |` cells.

    Step 2 (preservation grep — Pitfall 1): Confirm `grep -c 'Pending' .planning/REQUIREMENTS.md` returns >= 6 (state-machine description prose still references the word `Pending` in DATA-02, PROD-03, PROD-07, TRANS-01, TRANS-04, KPI-04 description lines). If this returns 0, the executor over-matched — halt and restore from git.

    Step 3 (Future section preservation grep): Confirm Future + Out-of-Scope sections still have their `- [ ]` markers (these are intentionally deferred): `grep -cE '^- \[ \] \*\*(TRANS-FUT|PROD-FUT|IMPORT-FUT|KPI-FUT|REALTIME-FUT)' .planning/REQUIREMENTS.md` should return the pre-edit count (executor records pre-edit count as a sanity baseline).

    Step 4 (immutable audit records, D-13): `git diff --name-only | grep -E '(v2.0-MILESTONE-AUDIT|integration-check-v2\.0-postphase36)' | wc -l | tr -d ' '` returns `0`.

    Step 5 (stage + commit, NEVER `git add .`):

      git add .planning/REQUIREMENTS.md
      git commit -m "$(cat <<'EOF'
      docs(37-02): mark 45 v2.0 requirements Complete in REQUIREMENTS.md

      Closes v2.0 milestone-audit cross-phase tech-debt item 2: REQUIREMENTS.md
      traceability table all 45 cells were 'Pending' despite VERIFICATION.md
      tables confirming SATISFIED. Mirrors v1.5 INT-07 traceability format
      (Complete cell value; [x] checkbox marker).

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      EOF
      )"

    Step 6 (post-commit verification): `git log -1 --pretty=format:'%s'` returns `docs(37-02): mark 45 v2.0 requirements Complete in REQUIREMENTS.md`. `git log -1 --name-only --pretty=format:'' | grep -v '^$'` returns exactly `.planning/REQUIREMENTS.md`.

    Do NOT edit `.planning/v2.0-MILESTONE-AUDIT.md` or `.planning/integration-check-v2.0-postphase36.md` (D-13).
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format:'' | grep -v '^$'</automated>
  </verify>
  <acceptance_criteria>
    - Both zones flipped: `grep -cE '^- \[x\] \*\*(DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9]\*\*:' .planning/REQUIREMENTS.md` returns `45`; `grep -c '| Complete |' .planning/REQUIREMENTS.md` returns `45`.
    - Zero remnants: `grep -cE '^- \[ \] \*\*(DATA|AUTH|PROD|TRANS|IMPORT|KPI)-[0-9][0-9]\*\*:' .planning/REQUIREMENTS.md` returns `0`; `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns `0`.
    - State-machine description prose intact (Pitfall 1): `grep -c 'Pending' .planning/REQUIREMENTS.md` returns >= 6.
    - Future + Out-of-Scope deferred markers preserved: pre-edit count of `'^- \[ \] \*\*.*FUT'` matches post-edit count.
    - Audit records untouched (D-13): `git diff HEAD~1 HEAD -- .planning/v2.0-MILESTONE-AUDIT.md .planning/integration-check-v2.0-postphase36.md | wc -l | tr -d ' '` returns `0`.
    - Commit subject exact: `git log -1 --pretty=format:'%s'` returns `docs(37-02): mark 45 v2.0 requirements Complete in REQUIREMENTS.md`.
    - Commit touches exactly 1 file: `git log -1 --name-only --pretty=format:'' | grep -v '^$' | wc -l | tr -d ' '` returns `1`; the file is `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>REQUIREMENTS.md has 45 checked v2.0 requirements and 45 Complete traceability cells. All deferred / state-machine prose preserved. Atomic docs commit landed. v2.0 milestone-audit cross-phase tech-debt item 2 is closed pending Plan 37-05 audit re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan edits a single planning-meta documentation file. No runtime, build, test, auth/role, or user-input surface is touched. The edits are pure markdown body changes consumed by `/gsd:audit-milestone` to count `Traceability [x]` rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02 | Tampering | REQUIREMENTS.md markdown table parser (audit tooling) | accept | Risk is over-matching the word "Pending" outside the table cells (Pitfall 1 — state-machine prose contains the word). Mitigation: Tasks 1+2 use exact-string-match Edit operations (NOT `sed`); Task 3 has explicit preservation grep (`grep -c 'Pending'` returns >= 6 confirms prose intact). The `| Pending |` (pipe-space-Pending-space-pipe) acceptance grep further narrows the success criterion to the table cells only. |

**applicable_threats: [T-37-02]**

**Rationale for minimal threat surface:** Per security_enforcement gate guidance in `<planning_context>`, this is a documentation-only phase. No ASVS L1 category applies. The single retained threat covers the only realistic failure mode (over-match on the word "Pending") and is mitigated by exact-string-match edits + preservation grep in Task 3.
</threat_model>

<verification>
- 45 v2.0 requirement checkboxes flipped to `[x]`; 45 traceability cells flipped to `Complete`.
- Free-text "Pending" still present in state-machine description prose (>= 6 occurrences across DATA-02, PROD-03, PROD-07, TRANS-01, TRANS-04, KPI-04).
- Future Requirements + Out-of-Scope `- [ ] **{ID-FUT}**:` markers preserved.
- Single atomic docs commit `docs(37-02): mark 45 v2.0 requirements Complete in REQUIREMENTS.md` touches exactly `.planning/REQUIREMENTS.md`.
- `.planning/v2.0-MILESTONE-AUDIT.md` and `.planning/integration-check-v2.0-postphase36.md` untouched (D-13).
</verification>

<success_criteria>
- v2.0 milestone-audit cross-phase tech-debt item 2 is mechanically closed. When Plan 37-05 runs `/gsd:audit-milestone v2.0`, `Traceability [x]` count flips from 0/45 to 45/45.
- REQUIREMENTS.md uses v1.5 INT-07 verbatim format: `[x]` checkbox + `Complete` cell.
- Zone 1 + Zone 2 land in ONE commit (Pitfall 6 — desync risk).
- State-machine prose ("Pending/Mixing/Completed/Blocked" descriptions) preserved byte-for-byte.
- Deferred items in Future Requirements + Out-of-Scope sections preserved with their `- [ ]` markers.
</success_criteria>

<output>
After completion, create `.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-02-SUMMARY.md`.
</output>
