---
phase: 37
plan: "05"
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
  - 37-03
  - 37-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
tags:
  - docs-hygiene
  - audit-rerun
  - ship-indicator
must_haves:
  decisions_covered:
    - "scope-lock: edit ROADMAP.md (line 11 milestone-list entry + line ~65 `<details open>` summary block), STATE.md (progress block: completed_phases, last_shipped). Do NOT manually edit `.planning/v2.0-MILESTONE-AUDIT.md` (skill writes it) or `.planning/integration-check-v2.0-postphase36.md` (immutable per D-13)."
    - "audit-gate (sequential, Wave-2 invariant): ROADMAP/STATE flips happen AFTER `/gsd:audit-milestone v2.0` returns `status: passed`. If audit returns `passed_with_warnings` or `gaps_found`, HALT and escalate per RESEARCH Open Question 2 + PATTERNS Anti-Pattern Catalog (no silent remediation)."
    - "checkpoint: Task 1 is `checkpoint:human-action` (operator runs the skill — skill orchestration is interactive). Task 2 is `auto` (executor edits ROADMAP/STATE post-audit-pass)."
    - "ship date: planner does NOT hardcode the date. Executor reads it from the new audit doc's `audited:` timestamp (date portion) or operator supplies a ship date verbally; format `(shipped YYYY-MM-DD)`."
    - "depends_on Wave-1 atomic commits: 37-01..04 MUST be on main first; the audit re-run consumes those edits as input. The orchestrator's wave executor enforces this; executor can sanity-check via `git log --oneline -10 | head -5 | grep -cE 'docs\\(37-0[1-4]\\)'` returns `4`."
    - "D-13 immutability check (revised post-plan-checker review): instead of a brittle `37-01..HEAD` ref-range (no such ref exists), the acceptance criterion asserts that the MOST-RECENT commit touching each audit file has subject prefix `audit(` — the exclusive signature of the `/gsd:audit-milestone` skill. Determinism: `git log -1 --pretty=format:'%s' -- <file>` requires no ref/SHA and runs in a non-interactive shell. Fewest moving parts vs. SHA-capture-checkpoint or stash-diff alternatives. A manual D-13 violation from Phase 37 would surface as `docs(37-...)` instead of `audit(...)`."
  truths:
    - "`.planning/v2.0-MILESTONE-AUDIT.md` post-Task-1 contains `status: passed` (no `_with_warnings`)"
    - "Cross-phase tech-debt items 1-3 (SUMMARY-FM backfill, REQUIREMENTS table, INT-02) absent from new audit"
    - "Phase 35 `missing_artifacts` tech-debt item absent from new audit"
    - "ROADMAP.md milestone-list line 11 shows `✅ **v2.0 Mill Production MVP**` with ship date"
    - "ROADMAP.md `<details>` summary block shows `✅` + ship date and is collapsed (matches v1.5 convention)"
    - "STATE.md progress block reflects `completed_phases: 7` (was 6) and a `last_shipped:` entry for v2.0"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "v2.0 ship indicator flipped to ✅ with ship date"
      contains: "✅ **v2.0 Mill Production MVP**"
    - path: .planning/STATE.md
      provides: "milestone status: completed; completed_phases: 7"
      contains: "completed_phases: 7"
    - path: .planning/v2.0-MILESTONE-AUDIT.md
      provides: "(skill-authored) new audit with status: passed; tech-debt items 1-4 absent"
      contains: "status: passed"
  key_links:
    - from: ".planning/v2.0-MILESTONE-AUDIT.md (skill output post Plans 37-01..04)"
      to: ".planning/ROADMAP.md milestone-list ship indicator"
      via: "operator-gated audit-pass -> flip indicator"
      pattern: "^✅ \\*\\*v2.0 Mill Production MVP\\*\\*"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md (progress + last_shipped)"
      via: "shared milestone-completion update"
      pattern: "completed_phases: 7"
---

<objective>
Close v2.0 milestone-audit cross-phase tech-debt item 5 (the final audit re-run) and flip the v2.0 ship indicator. This is the **Wave 2** plan — sequential, depends on Plans 37-01..04 atomic commits landing on main first.

Two-step structure (per RESEARCH §"Suggested Plan Decomposition — Plan 37-05"):

1. **Task 1 (`checkpoint:human-action`):** Operator runs `/gsd:audit-milestone v2.0`. The skill orchestrates `gsd-integration-checker`, re-parses Phases 31-37 VERIFICATION.md / SUMMARY.md / REQUIREMENTS.md, and writes:
   - `.planning/v2.0-MILESTONE-AUDIT.md` (overwritten — previous content moves to git history)
   - `.planning/integration-check-v2.0-postphase37.md` (created — matches `-postphase36` convention)

   Verify the new audit returns `status: passed` AND cross-phase tech-debt items 1-3 + Phase 35 missing_artifacts are absent. If audit returns `passed_with_warnings` or `gaps_found`, HALT and escalate to operator with the specific failed assertions (NO silent remediation — RESEARCH Open Question 2 + PATTERNS Anti-Pattern Catalog).

2. **Task 2 (`auto`):** After Task 1 confirms `status: passed`, edit:
   - `.planning/ROADMAP.md` line 11 (milestone-list entry)
   - `.planning/ROADMAP.md` line ~65 (`<details open>` summary block)
   - `.planning/STATE.md` (progress block: `completed_phases:`, `last_shipped:` addition for v2.0)
   Then commit with `docs(37-05): flip v2.0 ship indicator to ✅ (shipped YYYY-MM-DD)`.

   Ship date: derived from the new audit doc's `audited:` timestamp (date portion only) OR supplied verbally by operator. Format: `(shipped 2026-MM-DD)`. Executor does NOT hardcode a date — read from audit metadata.

Purpose: After this plan, v2.0 milestone status is shippable + shipped; ROADMAP reflects final state; STATE.md tracks completion.

Output: Skill-authored new audit + 2 executor-edited markdown files + 1 atomic docs commit. v2.0 milestone CLOSED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-RESEARCH.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md
@.planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md

<interfaces>
<!-- ROADMAP.md current state (line numbers verified this session). -->

ROADMAP.md line 11 (current):
  - 🔄 **v2.0 Mill Production MVP** — Phases 31-37 (audit re-run passed_with_warnings 2026-05-16; ship gated on Phase 37 hygiene cleanup)

ROADMAP.md line 11 (target):
  - ✅ **v2.0 Mill Production MVP** — Phases 31-37 (shipped YYYY-MM-DD)

ROADMAP.md line ~65 (current — note line drift possible if Phase 37 entry has been refined by mvp-phase orchestrator since planning):
  <details open>
  <summary>🔄 v2.0 Mill Production MVP (Phases 31-37) — AUDIT RE-RUN PASSED_WITH_WARNINGS; SHIP GATED ON PHASE 37</summary>

ROADMAP.md line ~65 (target):
  <details>
  <summary>✅ v2.0 Mill Production MVP (Phases 31-37) — SHIPPED YYYY-MM-DD</summary>

(Note: change `<details open>` to `<details>` to collapse, matching v1.5 convention at ROADMAP.md line 51.)

STATE.md current progress block (verified this session — frontmatter, lines 1-14):
  ---
  gsd_state_version: 1.0
  milestone: v2.0
  milestone_name: milestone
  status: completed
  last_updated: "..."
  last_activity: ...
  progress:
    total_phases: 7
    completed_phases: 6
    total_plans: 47
    completed_plans: 47
    percent: 86
  ---

STATE.md progress block (target — minimum edits):
  - Update `completed_phases: 6` -> `completed_phases: 7` (Phase 37 now complete)
  - Update `percent: 86` -> `percent: 100` (recompute: completed/total * 100)
  - Update `total_plans: 47` -> `total_plans: 52` (47 + 5 Phase-37 plans)
  - Update `completed_plans: 47` -> `completed_plans: 52`
  - Update `last_updated:` and `last_activity:` to today's date
  - The body of STATE.md has `**Last shipped:** v1.5 Production Transition (2026-05-12)` near line 19 — update to `v2.0 Mill Production MVP (YYYY-MM-DD)`

Skill invocation (Task 1):
  /gsd:audit-milestone v2.0

Skill output gate (Task 1 verification):
  grep "^status: passed$" .planning/v2.0-MILESTONE-AUDIT.md
  # Expected: 1 match (no `_with_warnings`)

Audit doc inspection (Task 1 — confirm no tech-debt items 1-3 + Phase 35 missing_artifacts):
  Per RESEARCH §"Warning 5 — Verification command", run these greps against the NEW audit:
    grep -E "SUMMARY frontmatter requirements-completed" .planning/v2.0-MILESTONE-AUDIT.md  # Expected: 0 (or historical-only)
    grep -E "REQUIREMENTS.md traceability" .planning/v2.0-MILESTONE-AUDIT.md                 # Expected: 0 (or historical-only)
    grep -E "INT-02|33-HUMAN-UAT amendment" .planning/v2.0-MILESTONE-AUDIT.md                # Expected: 0
    grep -E "35-LEARNINGS.*missing_artifacts" .planning/v2.0-MILESTONE-AUDIT.md              # Expected: 0
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs `/gsd:audit-milestone v2.0`; verify status: passed + warnings absent</name>
  <what-built>
    Wave 1 (Plans 37-01..04) closed 4 of the 5 audit warnings. This task re-runs the milestone audit skill so it (1) regenerates `.planning/v2.0-MILESTONE-AUDIT.md` based on the new repo state and (2) writes `.planning/integration-check-v2.0-postphase37.md`. The skill is interactive and requires operator orchestration.
  </what-built>
  <how-to-verify>
    1. Operator runs the skill in a clean session (recommended: `/clear` first):
         /gsd:audit-milestone v2.0
    2. Wait for the skill to complete. It writes:
       - `.planning/v2.0-MILESTONE-AUDIT.md` (NEW content, overwritten)
       - `.planning/integration-check-v2.0-postphase37.md` (NEW file)
    3. Verify the new audit reports `status: passed` (no `_with_warnings`):
         grep '^status: passed$' .planning/v2.0-MILESTONE-AUDIT.md
       Expected: 1 match. If 0 matches or `status: passed_with_warnings` / `status: gaps_found` appears, HALT — Wave 1 missed something. Capture the audit's `tech_debt:` block and escalate (RESEARCH Open Question 2: NO silent remediation).
    4. Verify the 4 tech-debt items closed by Wave 1 are absent from the new audit (per Interfaces grep contracts):
         grep -E "SUMMARY frontmatter requirements-completed" .planning/v2.0-MILESTONE-AUDIT.md            # Expected: 0
         grep -E "REQUIREMENTS.md traceability" .planning/v2.0-MILESTONE-AUDIT.md                          # Expected: 0
         grep -E "INT-02|33-HUMAN-UAT amendment" .planning/v2.0-MILESTONE-AUDIT.md                         # Expected: 0
         grep -E "35-LEARNINGS.*missing_artifacts" .planning/v2.0-MILESTONE-AUDIT.md                       # Expected: 0
    5. Note the new audit's `audited:` timestamp — Task 2 reads the date portion as the ship date.
    6. The skill may itself produce a git commit (it does in v1.5 precedent). That commit is the skill's own — Phase 37 does NOT manually edit `.planning/v2.0-MILESTONE-AUDIT.md` (D-13).

    DO NOT proceed to Task 2 if any of the above verifications fail. Type `approved` only when status: passed AND all 4 grep contracts return `0`.
  </how-to-verify>
  <resume-signal>Type `approved` with the ship date (e.g., `approved 2026-05-16`) OR describe any failures from the new audit</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP + STATE ship indicators; atomic docs commit</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (re-read full file — verify lines 11 + ~65 still match the Interfaces "current" shape; line drift possible if other phases edited the file post-planning)
    - .planning/STATE.md (re-read full frontmatter — verify `completed_phases: 6` is current; body near line 19 still says `**Last shipped:** v1.5 Production Transition`)
    - .planning/v2.0-MILESTONE-AUDIT.md (NEW skill output from Task 1 — read `audited:` timestamp for ship date; confirm `status: passed`)
    - .planning/integration-check-v2.0-postphase37.md (NEW skill output — sanity check exists; do NOT edit)
    - .planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md (universal exemplar — Task 4 commit pattern at lines 288-316; explicit `git add` paths; heredoc commit message)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md (W5 section + Shared Pattern 2 + 3)
  </read_first>
  <action>
    Extract ship date from the new audit doc (Task 1 output):
      DATE=$(grep '^audited:' .planning/v2.0-MILESTONE-AUDIT.md | head -1 | sed -E 's/^audited: *([0-9]{4}-[0-9]{2}-[0-9]{2}).*/\1/')
    Or if operator supplied a date verbally in the resume-signal, use that. Format MUST be `YYYY-MM-DD`.

    Edit 1 — `.planning/ROADMAP.md` line 11 (milestone-list):
      Use Edit tool with exact-string match:
        old_string: `- 🔄 **v2.0 Mill Production MVP** — Phases 31-37 (audit re-run passed_with_warnings 2026-05-16; ship gated on Phase 37 hygiene cleanup)`
        new_string: `- ✅ **v2.0 Mill Production MVP** — Phases 31-37 (shipped <DATE>)`
      Substitute `<DATE>` with the extracted ship date.

    Edit 2 — `.planning/ROADMAP.md` `<details>` summary block (around line 65; may have drifted):
      Use Edit tool with exact-string match on the `<summary>` line:
        old_string: `<summary>🔄 v2.0 Mill Production MVP (Phases 31-37) — AUDIT RE-RUN PASSED_WITH_WARNINGS; SHIP GATED ON PHASE 37</summary>`
        new_string: `<summary>✅ v2.0 Mill Production MVP (Phases 31-37) — SHIPPED <DATE></summary>`
      Then collapse the disclosure by changing `<details open>` -> `<details>` on the line immediately above the `<summary>`. (RESEARCH §"Warning 5": v1.5 convention is collapsed.)

    Edit 3 — `.planning/STATE.md` frontmatter progress block:
      Use Edit tool to make these exact updates:
        `completed_phases: 6` -> `completed_phases: 7`
        `total_plans: 47` -> `total_plans: 52`
        `completed_plans: 47` -> `completed_plans: 52`
        `percent: 86` -> `percent: 100`
        `last_updated: "<old-timestamp>"` -> `last_updated: "<DATE>T<HH:MM:SS>Z"` (use today's UTC time)
        `last_activity: <old-date>` -> `last_activity: <DATE>`

    Edit 4 — `.planning/STATE.md` body near line 19:
      Use Edit tool with exact-string match:
        old_string: `**Last shipped:** v1.5 Production Transition (2026-05-12)`
        new_string: `**Last shipped:** v2.0 Mill Production MVP (<DATE>)`

    Then commit using GSD heredoc convention with explicit `git add` paths:

      git add .planning/ROADMAP.md .planning/STATE.md
      git commit -m "$(cat <<'EOF'
      docs(37-05): flip v2.0 ship indicator to ✅ (shipped <DATE>)

      v2.0 milestone audit re-ran clean (status: passed). All 5 audit warnings
      closed by Phase 37 Plans 01-04 + this plan. Ship indicator flipped on
      ROADMAP.md (line 11 + details summary) and STATE.md (progress +
      last_shipped). The skill-authored v2.0-MILESTONE-AUDIT.md and
      integration-check-v2.0-postphase37.md are NOT touched by this commit
      (D-13 immutability + skill-only writes).

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      EOF
      )"

    Substitute `<DATE>` literally in the commit subject AND body. Use the ship date format `YYYY-MM-DD` (no quotes).

    Do NOT add `.planning/v2.0-MILESTONE-AUDIT.md` or `.planning/integration-check-v2.0-postphase37.md` to this commit — the skill manages those files exclusively (D-13).
  </action>
  <verify>
    <automated>echo "roadmap_shipped=$(grep -c '^- ✅ \*\*v2.0 Mill Production MVP\*\*' .planning/ROADMAP.md)"; echo "roadmap_legacy=$(grep -c '^- 🔄 \*\*v2.0 Mill Production MVP\*\*' .planning/ROADMAP.md)"; echo "state_phases=$(grep -c '^  completed_phases: 7$' .planning/STATE.md)"; echo "state_last_shipped_v2=$(grep -c '\*\*Last shipped:\*\* v2.0' .planning/STATE.md)"</automated>
  </verify>
  <acceptance_criteria>
    - Audit returned passed (Task 1 precondition): `grep -c '^status: passed$' .planning/v2.0-MILESTONE-AUDIT.md` returns `1`.
    - ROADMAP indicator flipped: `grep -c '^- ✅ \*\*v2.0 Mill Production MVP\*\*' .planning/ROADMAP.md` returns `1`.
    - No legacy 🔄 marker for v2.0: `grep -c '^- 🔄 \*\*v2.0 Mill Production MVP\*\*' .planning/ROADMAP.md` returns `0`.
    - `<details>` summary updated + collapsed: `grep -c '<summary>✅ v2.0 Mill Production MVP' .planning/ROADMAP.md` returns `1`; `grep -B 1 '<summary>✅ v2.0' .planning/ROADMAP.md | grep -c '^<details>$'` returns `1` (NOT `<details open>`).
    - STATE.md progress updated: `grep -c '^  completed_phases: 7$' .planning/STATE.md` returns `1`; `grep -c '^  total_plans: 52$' .planning/STATE.md` returns `1`; `grep -c '^  percent: 100$' .planning/STATE.md` returns `1`.
    - STATE.md last-shipped line updated: `grep -c '\*\*Last shipped:\*\* v2.0 Mill Production MVP' .planning/STATE.md` returns `1`.
    - Phase 37 cross-phase tech-debt items 1-3 GONE from new audit: `grep -E '(SUMMARY frontmatter requirements-completed|REQUIREMENTS.md traceability|INT-02|33-HUMAN-UAT amendment)' .planning/v2.0-MILESTONE-AUDIT.md | wc -l | tr -d ' '` returns `0`.
    - Phase 35 `missing_artifacts` item GONE: `grep -E '35-LEARNINGS.*missing_artifacts' .planning/v2.0-MILESTONE-AUDIT.md | wc -l | tr -d ' '` returns `0`.
    - Audit records were NOT manually edited by Phase 37 commits (D-13): the most-recent commit touching each audit file MUST come from the `/gsd:audit-milestone` skill (subject prefix `audit(`), never from a manual `docs(37-...)` commit. Two assertions: `git log -1 --pretty=format:'%s' -- .planning/v2.0-MILESTONE-AUDIT.md | grep -cE '^audit\('` returns `1`; `git log -1 --pretty=format:'%s' -- .planning/integration-check-v2.0-postphase37.md | grep -cE '^audit\('` returns `1`. (Rationale: deterministic, no ref/SHA needed; the skill is the only legitimate writer per D-13, and its commits are recognizable in `git log -1 --pretty=format:'%s' -- <file>`.)
    - Commit touches exactly 2 files: `git log -1 --name-only --pretty=format:'' | grep -v '^$' | sort -u | wc -l | tr -d ' '` returns `2`; the files are `.planning/ROADMAP.md` and `.planning/STATE.md`.
    - Commit subject begins with `docs(37-05): flip v2.0 ship indicator to ✅`: `git log -1 --pretty=format:'%s' | grep -c '^docs(37-05): flip v2.0 ship indicator to ✅'` returns `1`.
  </acceptance_criteria>
  <done>v2.0 ship indicator flipped on ROADMAP + STATE. New audit returned `status: passed`. All 5 audit warnings closed (Plans 37-01..05). Audit records (skill-written) untouched by Phase 37 commits. v2.0 milestone CLOSED — shipped on the ship date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-skill <-> repo | The `/gsd:audit-milestone v2.0` skill is the ONLY legitimate writer to `.planning/v2.0-MILESTONE-AUDIT.md` and `.planning/integration-check-v2.0-postphase37.md` (D-13). Phase 37 executor MUST NOT manually edit these files. |
| operator <-> ship-indicator-flip | The ship-indicator flip is gated on the audit's `status: passed` output. Operator can override (resume-signal: `approved <DATE>`) only after sanity-checking the new audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | v2.0 ship-indicator flip without legitimate audit gate | mitigate | Task 1 is `checkpoint:human-action` gated on `status: passed`. Task 2 cannot run autonomously without operator confirmation in resume-signal. Acceptance criteria explicitly grep on `^status: passed$` AND on 4 tech-debt absence assertions before flipping ROADMAP/STATE. Silent remediation forbidden (RESEARCH Open Question 2). |
| T-37-SC | Tampering | audit-record immutability (D-13) | mitigate | Acceptance asserts the most-recent commit touching each audit file has subject prefix `audit(` (the `/gsd:audit-milestone` skill's exclusive signature). Two checks: `git log -1 --pretty=format:'%s' -- .planning/v2.0-MILESTONE-AUDIT.md \| grep -cE '^audit\('` returns `1`; same against `.planning/integration-check-v2.0-postphase37.md`. A manual D-13 violation would surface as `docs(37-...)`. Deterministic: no ref/SHA required. |

**applicable_threats: [T-37-05, T-37-SC]**

**Rationale:** This is the only Phase 37 plan that interacts with the audit skill + flips a ship indicator. Both threats are mechanical/protocol-level (not user-input-driven). Mitigations are gates in the plan itself (checkpoint:human-action + acceptance grep contracts). No ASVS L1 category applies; this is documentation/skill orchestration.
</threat_model>

<verification>
- New `.planning/v2.0-MILESTONE-AUDIT.md` (skill-written) contains `status: passed`.
- 4 Phase-37-closed tech-debt items absent from the new audit (grep returns 0 for each).
- ROADMAP.md milestone-list line shows `✅ **v2.0 Mill Production MVP** ... (shipped YYYY-MM-DD)`.
- ROADMAP.md `<details>` summary shows `✅` + ship date and the disclosure is collapsed.
- STATE.md progress block: `completed_phases: 7`, `total_plans: 52`, `completed_plans: 52`, `percent: 100`.
- STATE.md body line: `**Last shipped:** v2.0 Mill Production MVP (YYYY-MM-DD)`.
- Single atomic docs commit `docs(37-05): flip v2.0 ship indicator to ✅ (shipped YYYY-MM-DD)` touches exactly ROADMAP.md + STATE.md.
- D-13 immutability preserved: no Phase-37 commit (outside the skill's own write) edits `v2.0-MILESTONE-AUDIT.md` or `integration-check-v2.0-postphase37.md`.
</verification>

<success_criteria>
- v2.0 milestone audit re-run returns `status: passed` (final SC of Phase 37).
- All 5 audit warnings closed: items 1-3 (cross-phase) by Plans 37-01..03, item 4 (Phase 35 missing_artifacts) by Plan 37-04, item 5 (audit re-run + ship indicator) by this plan.
- ROADMAP + STATE reflect v2.0 SHIPPED status with date.
- Skill orchestration respects D-13: executor never manually edits audit records.
- HALT-on-failure invariant: if Task 1 audit returns `passed_with_warnings` or `gaps_found`, Task 2 does NOT execute and operator escalates with the failed-assertion list.
</success_criteria>

<output>
After completion, create `.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-05-SUMMARY.md`.
</output>
