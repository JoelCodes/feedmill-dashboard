---
phase: 37
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
autonomous: true
requirements: []
tags:
  - docs-hygiene
  - tech-debt-closure
  - frontmatter-edit
must_haves:
  decisions_covered:
    - "scope-lock: 3 lines deleted, 1 line added — `missing_artifacts: []` (empty list) replaces the 2-item list (`- '35-VERIFICATION.md'`, `- '35-UAT.md'`). Net diff: -3 +1."
    - "rationale-preservation per RESEARCH §Claude's Discretion #2: use `[]` (empty list) instead of key-deletion to preserve evidence the gap was actively closed (not silently dropped)."
    - "YAML validity: js-yaml load hook MUST pass post-edit."
    - "additive-only outside this list: do NOT touch other frontmatter keys (`phase:`, `phase_name:`, `project:`, `generated:`, `counts:`). Do NOT touch markdown body."
    - "immutable audit records (D-13): do NOT edit `.planning/v2.0-MILESTONE-AUDIT.md` or `.planning/integration-check-v2.0-postphase36.md`."
    - "proof of artifact existence (W4 audit closure precondition): both `35-VERIFICATION.md` and `35-UAT.md` exist and are status:verified / status:closed respectively per `36-02-SUMMARY.md` and `36-03-SUMMARY.md` (verified during Phase 36 closure)."
  truths:
    - "35-LEARNINGS.md frontmatter declares `missing_artifacts: []`"
    - "35-LEARNINGS.md frontmatter no longer mentions `35-VERIFICATION.md` or `35-UAT.md`"
    - "35-LEARNINGS.md YAML frontmatter parses cleanly"
    - "Audit re-run (Plan 37-05) confirms no tech-debt item references `35-LEARNINGS.md missing_artifacts`"
  artifacts:
    - path: .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
      provides: "missing_artifacts: [] (cleared; Phase 35 verification artifacts now exist per Phase 36)"
      contains: "missing_artifacts: []"
  key_links:
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md (frontmatter `missing_artifacts:`)"
      to: "Phase 36 outputs (`35-VERIFICATION.md` status=verified, `35-UAT.md` status=closed)"
      via: "tech-debt closure (artifacts now exist)"
      pattern: "^missing_artifacts: \\[\\]$"
---

<objective>
Close v2.0 milestone-audit Phase 35 tech-debt item 1: `35-LEARNINGS.md` frontmatter `missing_artifacts:` still lists `35-VERIFICATION.md` + `35-UAT.md` (stale — both authored by Phase 36 Plans 02-03 and now exist).

Single edit inside a single file (`.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md`):
- Lines 11-13 (3 lines: `missing_artifacts:` + 2 list items) -> 1 line: `missing_artifacts: []`

Net diff: -3 lines, +1 line.

Per RESEARCH §"Claude's Discretion #2", `[]` (empty list) is preferred over key deletion — it preserves evidence that the gap was actively investigated and closed, not silently dropped.

Proof of artifact existence (audit closure precondition):
- `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` exists with `status: verified` (authored by Phase 36 Plan 02 per `36-02-SUMMARY.md`).
- `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` exists with `status: closed` (authored by Phase 36 Plan 03 per `36-03-SUMMARY.md`).

Both can be confirmed via `ls` before editing.

Purpose: After this plan, `/gsd:audit-milestone v2.0` (Plan 37-05) re-parses `35-LEARNINGS.md` and removes the Phase 35 `missing_artifacts` entry from its tech-debt list. Closes audit `tech_debt.35-kpi-sections-and-role-specific-metrics` item 1.

Output: `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md` updated; `missing_artifacts:` block replaced with empty-list form. Single atomic docs commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-02-SUMMARY.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-03-SUMMARY.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-RESEARCH.md
@.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md
@.planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md

<interfaces>
<!-- Verbatim diff from RESEARCH §"Warning 4". Executor uses as-is. -->

Current state (read this session — `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md` lines 1-14):
  ---
  phase: 35
  phase_name: "kpi-sections-and-role-specific-metrics"
  project: "CGM Dashboard"
  generated: "2026-05-15"
  counts:
    decisions: 13
    lessons: 9
    patterns: 11
    surprises: 9
  missing_artifacts:
    - "35-VERIFICATION.md"
    - "35-UAT.md"
  ---

Target state (post-edit):
  ---
  phase: 35
  phase_name: "kpi-sections-and-role-specific-metrics"
  project: "CGM Dashboard"
  generated: "2026-05-15"
  counts:
    decisions: 13
    lessons: 9
    patterns: 11
    surprises: 9
  missing_artifacts: []
  ---

Verbatim diff:
  --- a/.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
  +++ b/.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
  @@ -8,9 +8,7 @@ counts:
     lessons: 9
     patterns: 11
     surprises: 9
  -missing_artifacts:
  -  - "35-VERIFICATION.md"
  -  - "35-UAT.md"
  +missing_artifacts: []
   ---

YAML validity hook (verbatim from 30-02-PLAN.md):
  node -e "const fs=require('fs'); const yaml=require('js-yaml'); const m=fs.readFileSync('FILE','utf8').match(/^---\n([\s\S]+?)\n---/); yaml.load(m[1]); console.log('OK');"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 3-line `missing_artifacts:` block with `missing_artifacts: []`</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md</files>
  <read_first>
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md (full file — confirm lines 11-13 contain `missing_artifacts:` + `  - "35-VERIFICATION.md"` + `  - "35-UAT.md"`; confirm no other `missing_artifacts:` mention elsewhere in frontmatter)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md (confirm file exists — proof that the gap is genuinely closed)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md (confirm file exists — same)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-02-SUMMARY.md (cites authoring of 35-VERIFICATION.md by Phase 36 Plan 02)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-03-SUMMARY.md (cites authoring of 35-UAT.md by Phase 36 Plan 03)
    - .planning/milestones/v1.5-phases/30-close-gap-int-07-customerorderstab-href-summary-frontmatter-/30-02-PLAN.md (universal exemplar — Task 1 pattern at lines 155-193 for single-file YAML frontmatter edits with js-yaml validation)
    - .planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-PATTERNS.md (W4 section)
  </read_first>
  <action>
    Pre-flight (proof of artifact existence; this is the AUDIT-CLOSURE PRECONDITION per W4):
      ls .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md
      ls .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md
    Both MUST exist (exit 0). If either is missing, HALT — Phase 36 closure invariant is broken; escalate to operator.

    Open `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md`. Use the Edit tool with a multi-line exact-string match:

      old_string:
        missing_artifacts:
          - "35-VERIFICATION.md"
          - "35-UAT.md"
      new_string:
        missing_artifacts: []

    (The old_string is exactly 3 lines — the key + 2 list items with their 2-space indent. The new_string is exactly 1 line — the key with the inline empty-list literal.)

    Do NOT touch other frontmatter keys: `phase:`, `phase_name:`, `project:`, `generated:`, `counts:` (and its nested keys `decisions:`, `lessons:`, `patterns:`, `surprises:`) all remain unchanged. Do NOT touch markdown body below the closing `---`. Do NOT re-date `generated:` (which is `"2026-05-15"`).

    Run the YAML validity hook from Interfaces:
      `node -e "const fs=require('fs'); const yaml=require('js-yaml'); const m=fs.readFileSync('.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md','utf8').match(/^---\n([\s\S]+?)\n---/); yaml.load(m[1]); console.log('OK');"`
    Exit 0 + `OK` stdout = frontmatter still parses.

    Then commit using GSD heredoc convention with explicit `git add` path:

      git add .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
      git commit -m "$(cat <<'EOF'
      docs(37-04): clear missing_artifacts in 35-LEARNINGS.md (artifacts now exist)

      Closes v2.0 milestone-audit Phase 35 tech-debt item 1: 35-LEARNINGS.md
      frontmatter missing_artifacts still listed 35-VERIFICATION.md +
      35-UAT.md (stale — both authored by Phase 36 Plans 02-03 per
      36-02-SUMMARY.md + 36-03-SUMMARY.md and now exist as status:verified
      + status:closed). Replaced with empty list per RESEARCH Discretion #2
      (preserves evidence the gap was actively closed).

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      EOF
      )"

    Do NOT edit `.planning/v2.0-MILESTONE-AUDIT.md` or `.planning/integration-check-v2.0-postphase36.md` (D-13).
  </action>
  <verify>
    <automated>F=.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md; echo "empty_list=$(grep -c '^missing_artifacts: \[\]$' $F)"; echo "stale_VERIFICATION_refs=$(grep -c '35-VERIFICATION.md' $F)"; echo "stale_UAT_refs=$(grep -c '35-UAT.md' $F)"</automated>
  </verify>
  <acceptance_criteria>
    - Empty-list form present exactly once: `grep -c '^missing_artifacts: \[\]$' .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md` returns `1`.
    - No frontmatter references to stale artifact names remain: `grep -c '35-VERIFICATION.md' .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md` returns `0`; `grep -c '35-UAT.md' .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md` returns `0`. (NOTE: if the markdown body references these filenames in any prose, those references are preserved — but the frontmatter list is the only authoritative source for the audit gate, and there are no body references per RESEARCH inventory.)
    - YAML frontmatter parses: `node -e "const fs=require('fs'); const yaml=require('js-yaml'); const m=fs.readFileSync('.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md','utf8').match(/^---\n([\s\S]+?)\n---/); yaml.load(m[1]); console.log('OK');"` prints `OK` and exits 0.
    - Diff is -3 +1 lines: `git diff HEAD~1 HEAD .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md | grep -E '^-' | grep -vE '^---' | wc -l | tr -d ' '` returns `3`; `git diff HEAD~1 HEAD .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md | grep -E '^\+' | grep -vE '^\+\+\+' | wc -l | tr -d ' '` returns `1`.
    - Artifacts genuinely exist: `ls .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` exits 0 (both files present).
    - Audit records untouched (D-13): `git diff HEAD~1 HEAD -- .planning/v2.0-MILESTONE-AUDIT.md .planning/integration-check-v2.0-postphase36.md | wc -l | tr -d ' '` returns `0`.
    - Commit subject exact: `git log -1 --pretty=format:'%s'` returns `docs(37-04): clear missing_artifacts in 35-LEARNINGS.md (artifacts now exist)`.
    - Commit touches exactly 1 file: `git log -1 --name-only --pretty=format:'' | grep -v '^$' | wc -l | tr -d ' '` returns `1`; the file is `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md`.
  </acceptance_criteria>
  <done>35-LEARNINGS.md frontmatter declares `missing_artifacts: []`. Stale references to 35-VERIFICATION.md / 35-UAT.md gone from frontmatter. YAML parses cleanly. Atomic docs commit landed. Phase 35 tech-debt item 1 closed pending Plan 37-05 audit re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan edits 4 lines (3 deleted, 1 added) in a single planning-meta documentation file. No runtime, build, test, auth/role, or user-input surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | YAML frontmatter parser (downstream audit tooling) | accept | Risk is malformed YAML if the multi-line Edit fails to match exactly. Mitigation: Task 1 uses exact-string-match multi-line Edit (NOT `sed`); js-yaml hook validates post-edit. Pre-flight `ls` confirms artifact existence so the closure is genuine, not aspirational. |

**applicable_threats: [T-37-04]**

**Rationale for minimal threat surface:** Per security_enforcement gate guidance in `<planning_context>`, this is a documentation-only phase with a single 3->1 line edit. No ASVS L1 category applies.
</threat_model>

<verification>
- `35-LEARNINGS.md` frontmatter declares `missing_artifacts: []`.
- Stale references (`35-VERIFICATION.md`, `35-UAT.md`) removed from frontmatter.
- YAML frontmatter parses cleanly.
- `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` and `35-UAT.md` both exist (pre-flight `ls` exits 0).
- Single atomic docs commit `docs(37-04): clear missing_artifacts in 35-LEARNINGS.md (artifacts now exist)` touches exactly the one file.
- `.planning/v2.0-MILESTONE-AUDIT.md` and `.planning/integration-check-v2.0-postphase36.md` untouched (D-13).
</verification>

<success_criteria>
- v2.0 milestone-audit Phase 35 tech-debt item 1 mechanically closed. When Plan 37-05 runs `/gsd:audit-milestone v2.0`, no tech-debt entry references `35-LEARNINGS.md missing_artifacts`.
- Edit is a 3->1 line frontmatter mutation with no other content touched.
- `[]` (empty list) used per RESEARCH Discretion #2 — preserves evidence of active closure.
- Pre-flight artifact existence check passes (35-VERIFICATION.md + 35-UAT.md both present).
</success_criteria>

<output>
After completion, create `.planning/phases/37-v2-0-1-hygiene-cleanup-close-audit-warnings-before-ship/37-04-SUMMARY.md`.
</output>
