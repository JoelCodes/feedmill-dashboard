---
phase: 13-customer-detail-infrastructure
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/CustomerDetailHeader.tsx
  - src/components/CustomerDetailHeader.test.tsx
autonomous: true
requirements:
  - CDET-01
  - CDET-02
must_haves:
  truths:
    - "CustomerDetailHeader renders customer name in 20px bold"
    - "CustomerDetailHeader renders location with MapPin icon"
    - "CustomerDetailHeader renders phone with Phone icon when present"
    - "CustomerDetailHeader renders email with Mail icon when present"
    - "CustomerDetailHeader renders delivery preferences in accent color"
    - "CustomerDetailHeader renders Total Orders stat"
    - "CustomerDetailHeader renders Active Bins stat"
    - "CustomerDetailHeader renders Recent Activity stat with placeholder"
  artifacts:
    - path: "src/components/CustomerDetailHeader.tsx"
      provides: "Customer detail header component"
      exports: ["default"]
      min_lines: 50
    - path: "src/components/CustomerDetailHeader.test.tsx"
      provides: "Component tests with 8+ test cases"
      contains: "describe"
  key_links:
    - from: "src/components/CustomerDetailHeader.tsx"
      to: "src/types/customer.ts"
      via: "Customer and CustomerStats imports"
      pattern: "import.*Customer.*from"
---

<objective>
Create CustomerDetailHeader component using TDD for customer detail page header display.

Purpose: CDET-01 requires header with customer info (name, location, contact, delivery prefs). CDET-02 requires summary stats (Total Orders, Active Bins, Recent Activity).
Output: Tested CustomerDetailHeader component matching 13-UI-SPEC.md design contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-customer-detail-infrastructure/13-CONTEXT.md
@.planning/phases/13-customer-detail-infrastructure/13-RESEARCH.md
@.planning/phases/13-customer-detail-infrastructure/13-UI-SPEC.md

<interfaces>
<!-- Types for component props -->

From src/types/customer.ts (after Plan 01):
```typescript
export interface Customer {
  id: string;
  name: string;
  location: string;
  contactName?: string;
  contactPhone?: string;
  contactEmail?: string;
  deliveryPreferences?: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface CustomerStats {
  totalOrders: number;
  activeOrders: number;
  completedOrders: number;
  hasChanges: boolean;
  binAlertLevel: BinAlertLevel;
  activeBins: number;
}
```

<!-- Design specification from UI-SPEC.md -->
Component styling:
- White card: bg-white, rounded-[15px], shadow-[0_3.5px_5px_rgba(0,0,0,0.02)], p-5
- Customer name: text-xl font-bold text-[#2d3748]
- Contact rows: flex items-center gap-2, icon 14x14 #a0aec0, text 12px #a0aec0
- Delivery prefs: text-[10px] font-bold text-primary (#4fd1c5)
- Stats: 20px bold #2d3748 value, 10px #a0aec0 label
</interfaces>
</context>

<feature>
  <name>CustomerDetailHeader</name>
  <files>src/components/CustomerDetailHeader.tsx, src/components/CustomerDetailHeader.test.tsx</files>
  <behavior>
    Test cases (RED phase):
    1. Renders customer name in text-xl font-bold
    2. Renders location with MapPin icon
    3. Renders phone with Phone icon when contactPhone present
    4. Does NOT render phone row when contactPhone is undefined
    5. Renders email with Mail icon when contactEmail present
    6. Does NOT render email row when contactEmail is undefined
    7. Renders delivery preferences in accent color when present
    8. Does NOT render delivery row when deliveryPreferences is undefined
    9. Renders "Total Orders" stat with stats.totalOrders value
    10. Renders "Active Bins" stat with stats.activeBins value
    11. Renders "Recent Activity" stat with placeholder dash (Phase 14 will implement)

    Props interface:
    ```typescript
    interface CustomerDetailHeaderProps {
      customer: Customer;
      stats: CustomerStats;
    }
    ```

    Expected output format:
    - White rounded card container
    - Left side: customer name, location row, phone row (conditional), email row (conditional), delivery prefs (conditional)
    - Right side: three stat columns (Total Orders, Active Bins, Recent Activity)
  </behavior>
  <implementation>
    After tests pass (GREEN phase):
    1. Create component file with Props interface
    2. Import Customer, CustomerStats from @/types/customer
    3. Import MapPin, Phone, Mail from lucide-react
    4. Implement layout per UI-SPEC.md:
       - Outer div: rounded-[15px] bg-white p-5 shadow-[0_3.5px_5px_rgba(0,0,0,0.02)]
       - Inner div: flex items-start justify-between
       - Left section: flex flex-col gap-1
       - Right section: flex gap-4
    5. Use conditional rendering for optional fields (contactPhone, contactEmail, deliveryPreferences)
    6. Apply exact colors from UI-SPEC: #2d3748 for text-primary, #a0aec0 for text-secondary, #4fd1c5 for accent
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for CustomerDetailHeader</name>
  <files>src/components/CustomerDetailHeader.test.tsx</files>
  <read_first>src/app/customers/page.test.tsx</read_first>
  <behavior>
    - Test 1: renders customer name
    - Test 2: renders location with MapPin icon
    - Test 3: renders phone with Phone icon when present
    - Test 4: does not render phone when undefined
    - Test 5: renders email with Mail icon when present
    - Test 6: does not render email when undefined
    - Test 7: renders delivery preferences when present
    - Test 8: does not render delivery when undefined
    - Test 9: renders Total Orders stat
    - Test 10: renders Active Bins stat
    - Test 11: renders Recent Activity with dash placeholder
  </behavior>
  <action>
Create test file with 11 test cases covering all rendering scenarios.

Mock data for tests:
```typescript
const mockCustomer: Customer = {
  id: 'CUST-001',
  name: 'Greenfield Farms',
  location: 'Springfield, IL',
  contactName: 'John Green',
  contactPhone: '(217) 555-0101',
  contactEmail: 'jgreen@greenfieldfarms.com',
  deliveryPreferences: 'Mon/Wed/Fri, 6-8 AM',
  createdAt: new Date('2024-01-15'),
  updatedAt: new Date('2026-03-11'),
};

const mockStats: CustomerStats = {
  totalOrders: 5,
  activeOrders: 2,
  completedOrders: 3,
  hasChanges: false,
  binAlertLevel: 'low',
  activeBins: 2,
};
```

Test structure:
- Use @testing-library/react render and screen
- Query by text content for customer name, location, stats
- Query by testid for icons (data-testid="icon-map-pin", etc.)
- Use queryByText for negative assertions (should NOT be in document)

Tests MUST fail initially (component doesn't exist yet).
  </action>
  <verify>
    <automated>npm test -- src/components/CustomerDetailHeader.test.tsx --bail 2>&1 | grep -E "(FAIL|Cannot find module)"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at src/components/CustomerDetailHeader.test.tsx
    - Running tests fails with "Cannot find module" or test failures (RED state)
    - Test file has 11 test cases
  </acceptance_criteria>
  <done>Test file created with 11 test cases, all failing (RED phase complete)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement CustomerDetailHeader component</name>
  <files>src/components/CustomerDetailHeader.tsx</files>
  <read_first>src/components/CustomerDetailHeader.test.tsx, src/app/customers/page.tsx</read_first>
  <behavior>
    - Component renders all required elements
    - All 11 tests pass
  </behavior>
  <action>
Create CustomerDetailHeader component implementing all tested behaviors.

Component structure:
```typescript
import { Customer, CustomerStats } from '@/types/customer';
import { MapPin, Phone, Mail } from 'lucide-react';

interface CustomerDetailHeaderProps {
  customer: Customer;
  stats: CustomerStats;
}

export default function CustomerDetailHeader({
  customer,
  stats,
}: CustomerDetailHeaderProps) {
  return (
    <div className="rounded-[15px] bg-white p-5 shadow-[0_3.5px_5px_rgba(0,0,0,0.02)]">
      <div className="flex items-start justify-between">
        {/* Left - Contact Card */}
        <div className="flex flex-col gap-1">
          <h1 className="text-xl font-bold" style={{ color: '#2d3748' }}>
            {customer.name}
          </h1>

          <div className="flex items-center gap-2">
            <MapPin data-testid="icon-map-pin" className="h-3.5 w-3.5" style={{ color: '#a0aec0' }} />
            <span className="text-xs" style={{ color: '#a0aec0' }}>{customer.location}</span>
          </div>

          {customer.contactPhone && (
            <div className="flex items-center gap-2">
              <Phone data-testid="icon-phone" className="h-3.5 w-3.5" style={{ color: '#a0aec0' }} />
              <span className="text-xs" style={{ color: '#a0aec0' }}>{customer.contactPhone}</span>
            </div>
          )}

          {customer.contactEmail && (
            <div className="flex items-center gap-2">
              <Mail data-testid="icon-mail" className="h-3.5 w-3.5" style={{ color: '#a0aec0' }} />
              <span className="text-xs" style={{ color: '#a0aec0' }}>{customer.contactEmail}</span>
            </div>
          )}

          {customer.deliveryPreferences && (
            <div className="mt-1">
              <span className="text-[10px] font-bold" style={{ color: '#4fd1c5' }}>
                Delivery: {customer.deliveryPreferences}
              </span>
            </div>
          )}
        </div>

        {/* Right - Summary Stats */}
        <div className="flex gap-4">
          <div className="flex flex-col items-center gap-0.5">
            <span className="text-xl font-bold" style={{ color: '#2d3748' }}>
              {stats.totalOrders}
            </span>
            <span className="text-[10px]" style={{ color: '#a0aec0' }}>
              Total Orders
            </span>
          </div>

          <div className="flex flex-col items-center gap-0.5">
            <span className="text-xl font-bold" style={{ color: '#2d3748' }}>
              {stats.activeBins}
            </span>
            <span className="text-[10px]" style={{ color: '#a0aec0' }}>
              Active Bins
            </span>
          </div>

          <div className="flex flex-col items-center gap-0.5">
            <span className="text-xl font-bold" style={{ color: '#2d3748' }}>
              —
            </span>
            <span className="text-[10px]" style={{ color: '#a0aec0' }}>
              Recent Activity
            </span>
          </div>
        </div>
      </div>
    </div>
  );
}
```

All tests must pass after implementation.
  </action>
  <verify>
    <automated>npm test -- src/components/CustomerDetailHeader.test.tsx --bail</automated>
  </verify>
  <acceptance_criteria>
    - Component file exists at src/components/CustomerDetailHeader.tsx
    - All 11 tests pass (GREEN state)
    - npm run build passes (no TypeScript errors)
  </acceptance_criteria>
  <done>CustomerDetailHeader component implemented, all tests passing (GREEN phase complete)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Presentational component only, receives data via props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Spoofing | CustomerDetailHeader | accept | Component is display-only, no user input. React auto-escapes all text content. |
</threat_model>

<verification>
```bash
# Run component tests
npm test -- src/components/CustomerDetailHeader.test.tsx --bail

# Verify test count
grep -c "it(" src/components/CustomerDetailHeader.test.tsx  # Should be >= 11

# Verify component exports
grep "export default function CustomerDetailHeader" src/components/CustomerDetailHeader.tsx

# Build verification
npm run build
```
</verification>

<success_criteria>
- CustomerDetailHeader.test.tsx has 11+ test cases
- All tests pass (npm test -- CustomerDetailHeader)
- Component renders customer name, location, conditional contact info
- Component renders summary stats: Total Orders, Active Bins, Recent Activity
- Styling matches UI-SPEC.md (white card, correct typography, colors)
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-customer-detail-infrastructure/13-02-SUMMARY.md`
</output>
