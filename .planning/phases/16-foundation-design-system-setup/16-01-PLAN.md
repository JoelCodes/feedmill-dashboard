---
phase: 16-foundation-design-system-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "All color tokens have interactive states (hover, active, disabled)"
    - "Spacing scale --space-1 through --space-12 is available for use"
    - "Dark mode primitives override light mode values"
    - "Semantic aliases in @theme inline reference primitives correctly"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Expanded token system with interactive states and spacing"
      contains: "--primary-hover"
    - path: "src/app/globals.css"
      provides: "Dark mode overrides"
      contains: ".dark {"
    - path: "src/app/globals.css"
      provides: "Spacing scale"
      contains: "--space-12"
  key_links:
    - from: "@theme inline"
      to: ":root primitives"
      via: "var() references"
      pattern: "var\\(--primary-hover\\)"
---

<objective>
Expand the existing token system with interactive states and spacing scale.

Purpose: Provide the foundational design tokens that all Phase 17 components will use for hover/focus/active/disabled states and consistent spacing.

Output: Extended globals.css with ~40 new tokens (interactive states for 5 color families + spacing scale + dark mode overrides).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-foundation-design-system-setup/16-CONTEXT.md
@.planning/phases/16-foundation-design-system-setup/16-RESEARCH.md

<interfaces>
<!-- Current globals.css structure (to extend, not replace) -->

From src/app/globals.css:
```css
:root {
  /* Primary Colors */
  --primary: #4fd1c5;
  --primary-dark: #38b2ac;

  /* Status Colors */
  --success: #48bb78;
  --success-dark: #2f855a;
  --warning: #f59e0b;
  --error: #e53e3e;
  --error-dark: #c53030;

  /* Backgrounds */
  --bg-page: #f8f9fa;
  --bg-card: #ffffff;
  --text-primary: #2d3748;
  --text-secondary: #a0aec0;
}

@theme inline {
  --color-primary: var(--primary);
  --color-success: var(--success);
  /* ... semantic aliases */
}
```

<!-- Pattern: Two-tier naming (primitives in :root, semantics in @theme inline) per D-01 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive state tokens and spacing scale</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current token definitions to extend)
    - .planning/phases/16-foundation-design-system-setup/16-RESEARCH.md (token patterns)
  </read_first>
  <action>
Extend :root with interactive state tokens for each color family following the two-tier pattern (per D-01):

**Primary interactive states (after existing --primary-dark):**
```css
--primary-hover: #45b8ad;
--primary-active: #3a9d94;
--primary-disabled: #a8d5d0;
```

**Success interactive states (after existing --success-light):**
```css
--success-hover: #38a169;
--success-active: #2f855a;
--success-disabled: #9ae6b4;
```

**Warning interactive states (after existing --warning-light):**
```css
--warning-hover: #d97706;
--warning-active: #b45309;
--warning-disabled: #fcd34d;
```

**Error interactive states (after existing --error-light):**
```css
--error-hover: #d53f3f;
--error-active: #c53030;
--error-disabled: #fc8181;
```

**Info interactive states (after existing --info-light):**
```css
--info-hover: #2563eb;
--info-active: #1d4ed8;
--info-disabled: #93c5fd;
```

**Spacing scale (new section after Border Radius):**
```css
/* Spacing Scale */
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.5rem;    /* 24px */
--space-6: 2rem;      /* 32px */
--space-8: 3rem;      /* 48px */
--space-10: 4rem;     /* 64px */
--space-12: 6rem;     /* 96px */
```

Add semantic aliases in @theme inline (after existing color utilities):
```css
/* Interactive state aliases */
--color-primary-hover: var(--primary-hover);
--color-primary-active: var(--primary-active);
--color-primary-disabled: var(--primary-disabled);

--color-success-hover: var(--success-hover);
--color-success-active: var(--success-active);
--color-success-disabled: var(--success-disabled);

--color-warning-hover: var(--warning-hover);
--color-warning-active: var(--warning-active);
--color-warning-disabled: var(--warning-disabled);

--color-error-hover: var(--error-hover);
--color-error-active: var(--error-active);
--color-error-disabled: var(--error-disabled);

--color-info-hover: var(--info-hover);
--color-info-active: var(--info-active);
--color-info-disabled: var(--info-disabled);

/* Spacing aliases */
--spacing-xs: var(--space-1);
--spacing-sm: var(--space-2);
--spacing-md: var(--space-4);
--spacing-lg: var(--space-6);
--spacing-xl: var(--space-8);
```

Do NOT modify any existing tokens. Only ADD new ones.
  </action>
  <verify>
    <automated>grep -c "primary-hover" src/app/globals.css && grep -c "space-12" src/app/globals.css && grep -c "color-primary-hover" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "primary-hover" returns "--primary-hover: #45b8ad;" in :root
    - grep "space-12" returns "--space-12: 6rem;" in :root
    - grep "color-primary-hover" returns "--color-primary-hover: var(--primary-hover);" in @theme inline
    - All 15 interactive state tokens present (3 per color family x 5 families)
    - All 9 spacing tokens present (--space-1 through --space-12)
    - All 5 spacing aliases present (--spacing-xs through --spacing-xl)
  </acceptance_criteria>
  <done>Interactive state tokens and spacing scale added to globals.css following two-tier naming pattern</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode primitive overrides</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (from Task 1 with new tokens)
    - .planning/phases/16-foundation-design-system-setup/16-RESEARCH.md (dark mode patterns)
  </read_first>
  <action>
Add .dark class selector with primitive overrides (per D-03). Place AFTER :root block, BEFORE @theme inline block.

```css
/* Dark Mode Overrides */
.dark {
  /* Primary colors - shift to cooler blue for dark mode */
  --primary: #63b3ed;
  --primary-dark: #4299e1;
  --primary-hover: #7ab8ef;
  --primary-active: #4299e1;
  --primary-disabled: #4a5568;

  /* Backgrounds - dark surfaces */
  --bg-page: #1a202c;
  --bg-card: #2d3748;
  --bg-sidebar: #2d3748;

  /* Text - light on dark */
  --text-primary: #e2e8f0;
  --text-secondary: #a0aec0;
  --text-muted: #a0aec0;
  --text-medium: #cbd5e0;

  /* Status colors - slightly desaturated for dark mode */
  --success: #68d391;
  --success-dark: #48bb78;
  --success-light: #276749;
  --success-hover: #48bb78;
  --success-active: #38a169;
  --success-disabled: #2f855a;

  --warning: #fbd38d;
  --warning-light: #744210;
  --warning-hover: #f6ad55;
  --warning-active: #ed8936;
  --warning-disabled: #975a16;

  --error: #fc8181;
  --error-dark: #f56565;
  --error-light: #742a2a;
  --error-hover: #f56565;
  --error-active: #e53e3e;
  --error-disabled: #9b2c2c;

  --info: #90cdf4;
  --info-light: #2a4365;
  --info-hover: #63b3ed;
  --info-active: #4299e1;
  --info-disabled: #2c5282;

  /* Borders & Dividers */
  --divider: #4a5568;

  /* Shadows - more subtle in dark mode */
  --shadow-sm: 0 3.5px 5px rgba(0, 0, 0, 0.3);
  --shadow-card: 0 3.5px 5px rgba(0, 0, 0, 0.4);

  /* Pending colors */
  --pending: #718096;
  --pending-light: #4a5568;

  /* Status-specific colors */
  --status-completed-border: #48bb78;
  --status-completed-header: #68d391;
  --status-completed-bg-22: #27674938;

  --status-mixing-border: #f6ad55;
  --status-mixing-header: #fbd38d;
  --status-mixing-bg-22: #97521638;

  --status-blocked-border: #fc8181;
  --status-blocked-header: #feb2b2;
  --status-blocked-bg-22: #9b2c2c38;

  --status-pending-border: #a0aec0;
  --status-pending-header: #cbd5e0;
  --status-pending-bg-22: #4a556838;
}
```

IMPORTANT: Override primitives only. Semantic tokens in @theme inline automatically use the swapped primitives via var() references (per D-01 two-tier pattern).
  </action>
  <verify>
    <automated>grep -c "^\.dark {" src/app/globals.css && grep "\.dark" src/app/globals.css | grep -c "primary:"</automated>
  </verify>
  <acceptance_criteria>
    - grep "^\.dark {" returns exactly 1 (dark mode block exists)
    - .dark block contains "--primary: #63b3ed;"
    - .dark block contains "--bg-page: #1a202c;"
    - .dark block contains "--text-primary: #e2e8f0;"
    - .dark block appears AFTER :root block in file
    - .dark block appears BEFORE @theme inline block in file
    - All primitive tokens that have light mode values also have dark mode overrides
  </acceptance_criteria>
  <done>Dark mode primitive overrides added for all color tokens, backgrounds, and shadows</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | CSS-only changes with no user input or data processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | N/A | globals.css | accept | Pure CSS variable definitions; no executable code or user input |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "primary-hover" src/app/globals.css` returns >= 2 (light + dark)
2. `grep -c "space-12" src/app/globals.css` returns 1
3. `grep -c "^\.dark {" src/app/globals.css` returns 1
4. `npm run build` succeeds (CSS parses correctly)
</verification>

<success_criteria>
- All 15 interactive state tokens defined in :root (3 states x 5 color families)
- All 9 spacing tokens defined in :root (--space-1 through --space-12)
- All 15 interactive state semantic aliases in @theme inline
- All 5 spacing semantic aliases in @theme inline
- Dark mode block overrides all color primitives
- Two-tier naming pattern preserved (primitives -> semantics)
- Existing tokens unchanged, only additions made
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-design-system-setup/16-01-SUMMARY.md`
</output>
