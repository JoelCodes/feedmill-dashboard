---
phase: 14
slug: activity-timeline
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-05
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for Activity Timeline component. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom CSS variables) |
| Preset | not applicable |
| Component library | none (custom components) |
| Icon library | lucide-react |
| Font | Helvetica, Arial, sans-serif |

**Source:** Existing globals.css design tokens, CONTEXT.md D-06

---

## Component Structure

### ActivityTimeline (container)

Location: `src/components/ActivityTimeline.tsx`

Props:
```typescript
interface ActivityTimelineProps {
  events: ActivityEvent[];
}
```

Renders: White card with rounded corners containing scrollable list of timeline items.

### TimelineItem (child)

Location: Internal to ActivityTimeline or extracted to `src/components/TimelineItem.tsx`

Props:
```typescript
interface TimelineItemProps {
  event: ActivityEvent;
  isExpanded: boolean;
  onToggle: () => void;
  showConnector: boolean;
}
```

### ActivityEvent Type

Location: `src/types/activity.ts`

```typescript
type ActivityEventType =
  | 'order_placed'
  | 'production_started'
  | 'ready'
  | 'out_for_delivery'
  | 'delivered'
  | 'delivery_completed'
  | 'bin_alert_low'
  | 'bin_alert_critical';

interface ActivityEvent {
  id: string;
  customerId: string;
  type: ActivityEventType;
  timestamp: Date;
  title: string;
  description: string;
  // Order-specific fields (optional)
  orderId?: string;
  orderQuantity?: number;
  orderProduct?: string;
  orderStatus?: string;
  // Bin-specific fields (optional)
  binId?: string;
  binLocationCode?: string;
  binFillPercentage?: number;
}
```

**Source:** CONTEXT.md D-01, D-02, D-03

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline text gaps, icon internal padding |
| sm | 8px | Gap between title/description/date in content column |
| md | 14px | Gap between left column (icon) and right column (content) |
| lg | 20px | Card vertical padding |
| xl | 24px | Card horizontal padding |
| 2xl | 36px | Left column width (icon + connector) |

Exceptions: none

**Source:** Design file customer-detail.pen timeline-section padding [20, 24], gap 14

---

## Typography

| Role | Size | Weight | Line Height | Color |
|------|------|--------|-------------|-------|
| Title | 13px | bold (700) | 1.5 | #2d3748 (--text-primary) |
| Description | 11px | normal (400) | 1.5 | #a0aec0 (--text-secondary) |
| Date | 10px | bold (700) | 1.2 | Event color (see Color section) |
| Detail text | 11px | normal (400) | 1.5 | #2d3748 (--text-primary) |
| Link text | 10px | normal (400) | 1.5 | #4fd1c5 (--primary), underline |

**Source:** Design file customer-detail.pen typography values from timeline items

---

## Color

### Timeline Card

| Role | Value | Usage |
|------|-------|-------|
| Card background | #ffffff | Timeline section container |
| Card corner radius | 15px (--radius-xl) | Consistent with other cards |
| Card shadow | 0 3.5px 5px rgba(0,0,0,0.05) | Consistent with header card |

### Event Type Colors

| Event Type | Dot Fill | Connector | Date Text |
|------------|----------|-----------|-----------|
| Order events (order_placed, production_started, ready, out_for_delivery) | #4fd1c5 (--primary) | #4fd1c5 | #4fd1c5 |
| Delivery completed | #48bb78 (--success) | #48bb78 | #48bb78 |
| Bin alert low | #f59e0b (--warning) | #f59e0b | #f59e0b |
| Bin alert critical | #e53e3e (--error) | #e53e3e | #e53e3e |
| Delivered (order complete) | #48bb78 (--success) | #48bb78 | #48bb78 |

### Icon Colors

All icons: #ffffff (white) on colored dot background

### Expanded Detail Box

| Property | Value |
|----------|-------|
| Background | #f8f9fa (--bg-page) |
| Corner radius | 8px (--radius-md) |
| Padding | 12px |
| Gap | 4px |

**Source:** Design file customer-detail.pen t1-expanded frame

---

## Visual Structure

### Timeline Item Layout

```
+--[36px]--+--[fill]--------------------+
|   [dot]  | Title (13px bold)          |
|    28px  | Description (11px normal)  |
|   [icon] | Date (10px bold, colored)  |
|   [line] | [Expanded detail box]      | <- only when expanded
|    2px   |                            |
+----------+----------------------------+
```

### Measurements from Design

| Element | Value |
|---------|-------|
| Left column width | 36px |
| Dot diameter | 28px |
| Icon size | 14px x 14px |
| Connector line width | 2px |
| Row gap (between items) | 0 (connector fills gap) |
| Content gap (title/desc/date) | 4px |
| Icon-content gap | 14px |

**Source:** Design file customer-detail.pen leftcol-1 width 36, dot-1 28x28, icon-1 14x14, stepline-1 width 2

---

## Interaction Patterns

### Expand/Collapse Behavior

| Interaction | Behavior |
|-------------|----------|
| Click target | Entire timeline row (D-04) |
| Multiple expanded | Yes, simultaneous expansion allowed (D-05) |
| Default state | All items collapsed |
| Animation | None required (simple display toggle) |

### State Management

```typescript
// Managed in ActivityTimeline component
const [expandedIds, setExpandedIds] = useState<Set<string>>(new Set());

const toggleExpand = (eventId: string) => {
  setExpandedIds(prev => {
    const next = new Set(prev);
    if (next.has(eventId)) {
      next.delete(eventId);
    } else {
      next.add(eventId);
    }
    return next;
  });
};
```

**Source:** CONTEXT.md D-04, D-05

### Expanded Detail Content (Order Events Only)

Display exactly:
1. "Quantity: {X} tons"
2. "Product: {textureType}"
3. "Status: {status}"
4. "View Order Details" link (underlined, primary color)

Link behavior: Navigate to `/orders?selected={orderId}`

**Source:** CONTEXT.md D-06, design file t1-expanded children

---

## Icon Mapping

| Event Type | Icon | Library |
|------------|------|---------|
| order_placed | file-text | lucide-react (FileText) |
| production_started | factory | lucide-react (Factory) |
| ready | check-circle | lucide-react (CheckCircle) |
| out_for_delivery | truck | lucide-react (Truck) |
| delivered | check-circle | lucide-react (CheckCircle) |
| delivery_completed | truck | lucide-react (Truck) |
| bin_alert_low | alert-triangle | lucide-react (AlertTriangle) |
| bin_alert_critical | alert-triangle | lucide-react (AlertTriangle) |

**Source:** Design file iconFontName values, existing OrderDetails.tsx imports

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section heading | "Activity Timeline" (if needed) or no heading (timeline is self-evident in card) |
| Empty state heading | "No recent activity" |
| Empty state body | "Activity will appear here as orders, deliveries, and bin alerts occur." |
| Order event title pattern | "Order #{documentNumber} {stage}" |
| Delivery event title pattern | "Delivery Completed - {binLocationCode}" |
| Bin alert title pattern | "{Low\|Critical} Feed Alert - {binLocationCode}" |
| View order link | "View Order Details" |

### Title Templates by Event Type

| Event Type | Title Template |
|------------|----------------|
| order_placed | "Order #{documentNumber} Placed" |
| production_started | "Production Started - Order #{documentNumber}" |
| ready | "Order #{documentNumber} Ready for Pickup" |
| out_for_delivery | "Order #{documentNumber} Out for Delivery" |
| delivered | "Order #{documentNumber} Delivered" |
| delivery_completed | "Delivery Completed - Bin {locationCode}" |
| bin_alert_low | "Low Feed Alert - Bin {locationCode}" |
| bin_alert_critical | "Critical Feed Alert - Bin {locationCode}" |

### Description Templates

| Event Type | Description Template |
|------------|---------------------|
| order_placed | "{quantity} tons {feedType} ordered for delivery" |
| production_started | "Producing {quantity} tons {feedType}" |
| ready | "Order ready for shipment" |
| out_for_delivery | "Shipment departed for {location}" |
| delivered | "Delivered to {location}" |
| delivery_completed | "{quantity} tons {feedType} delivered" |
| bin_alert_low | "{feedType} level dropped below 30%" |
| bin_alert_critical | "{feedType} level dropped below 15%" |

**Source:** Design file t1-title, t2-title, t3-title content examples

---

## Performance Contract

| Concern | Strategy |
|---------|----------|
| Event count | Support up to 100 events without degradation (D-07) |
| Rendering | Simple list with CSS overflow scroll, no virtualization |
| Memory | No memory leaks from expand/collapse state |
| Scroll container | Parent card is scrollable, timeline grows naturally |

**Source:** CONTEXT.md D-07, ROADMAP.md success criteria #5

---

## Date Formatting

Use existing `formatTimelineDate` pattern from OrderDetails.tsx:

```typescript
function formatTimelineDate(date: Date): string {
  return new Intl.DateTimeFormat('en-US', {
    month: 'short',
    day: 'numeric',
    year: 'numeric',
    hour: 'numeric',
    minute: '2-digit',
    hour12: true
  }).format(date).replace(',', ' ·');
}
// Output: "Apr 28, 2026 · 2:15 PM"
```

**Source:** Existing OrderDetails.tsx formatTimelineDate function, design file date examples

---

## Accessibility

| Concern | Implementation |
|---------|----------------|
| Click target | Entire row clickable (44px+ height from content) |
| Keyboard | Row focusable, Enter/Space toggles expand |
| Screen reader | `role="button"` on row, `aria-expanded={isExpanded}` |
| Focus indicator | Use default browser focus ring or primary color outline |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

Project does not use shadcn. All components are custom with Tailwind CSS.

---

## Integration Points

### Customer Detail Page

Add ActivityTimeline below CustomerDetailHeader in `/customers/[id]/page.tsx`:

```tsx
// src/app/customers/[id]/page.tsx
import { ActivityTimeline } from '@/components/ActivityTimeline';
import { getActivityEvents } from '@/services/activity';

// In component:
const events = await getActivityEvents(customerId);

return (
  <>
    <CustomerDetailHeader customer={customer} stats={stats} />
    <ActivityTimeline events={events} />
    {/* Bins section will be added in Phase 15 */}
  </>
);
```

### Activity Service

New service: `src/services/activity.ts`

```typescript
export async function getActivityEvents(customerId: string): Promise<ActivityEvent[]> {
  // Returns events sorted by timestamp descending (newest first)
}
```

### Mock Data

Add ActivityEvent records to `src/data/mockData.ts` for testing.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
