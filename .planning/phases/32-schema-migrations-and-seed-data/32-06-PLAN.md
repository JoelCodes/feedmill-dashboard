---
phase: 32-schema-migrations-and-seed-data
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - package.json
  - src/db/seed.ts
autonomous: false
requirements:
  - DATA-07
tags: [seed, runtime, tsx, truncate, insert]

must_haves:
  truths:
    - "`tsx` is installed as a devDependency at version `^4.21.0` (per RESEARCH.md §4)"
    - "`package.json` scripts include `db:seed: tsx src/db/seed.ts`"
    - "`src/db/seed.ts` exists, loads `.env.local` via dotenv (mirroring drizzle.config.ts pattern), guards on missing `DATABASE_URL_UNPOOLED`, connects via the Neon HTTP driver"
    - "Seed runtime issues `TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE` — and EXPLICITLY does NOT include `users` in the TRUNCATE list (D-17)"
    - "After `npm run db:seed` runs successfully, `SELECT count(*) FROM production_orders` returns 33; `SELECT count(*) FROM users` returns 0 (unchanged); `SELECT DISTINCT created_by FROM production_orders` returns `system-seed`"
    - "Seed runtime uses parameterized Drizzle inserts (`db.insert(productionOrders).values(seedData)`) — no SQL string concatenation"
    - "`src/db/seed.ts` does NOT have `import 'server-only'` (CLI script, not a Next.js module)"
    - "D-15: seed source is a static JSON snapshot at `src/db/seed-data.json` (decoupled from `src/services/millProduction.ts` runtime)"
    - "D-16: `npm run db:seed` → `tsx src/db/seed.ts`; seed reads `DATABASE_URL_UNPOOLED`; `.env.local` loaded via dotenv mirroring drizzle.config.ts"
    - "D-18: seed scope is 33 production_orders rows only; order_events, import_batches, users remain EMPTY after seed (clean day-zero baseline)"
  artifacts:
    - path: "src/db/seed.ts"
      provides: "Seed runtime — TRUNCATE + bulk INSERT against the Neon dev DB"
      contains: "TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE"
    - path: "package.json"
      provides: "db:seed script + tsx devDependency"
      contains: "\"db:seed\": \"tsx src/db/seed.ts\""
  key_links:
    - from: "src/db/seed.ts"
      to: "src/db/seed-data.json"
      via: "import seedData from './seed-data.json'"
      pattern: "import seedData from"
    - from: "src/db/seed.ts"
      to: "src/db/schema/index.ts"
      via: "import { productionOrders } from './schema/index'"
      pattern: "from './schema"
    - from: "src/db/seed.ts"
      to: "Neon dev DB"
      via: "neon(process.env.DATABASE_URL_UNPOOLED)"
      pattern: "DATABASE_URL_UNPOOLED"
---

<objective>
Build the seed runtime: install `tsx` as a devDep, add the `db:seed` npm script, and write `src/db/seed.ts` to TRUNCATE the three transactional tables (production_orders, order_events, import_batches — NEVER users per D-17), then bulk-insert the 33 rows from `src/db/seed-data.json` (built in Plan 05). The operator runs `npm run db:seed` against the Phase 31 Neon dev database; after run, the dev DB mirrors the `/demo/mill-production` baseline.

Purpose: Satisfies the RUNTIME half of DATA-07. The seed makes dev sessions immediately useful (33 realistic orders visible in the Phase 34 dashboard without manual data entry) and is the canonical test-state-reset mechanism for Phase 33+ executor runs.
Output: One devDep install, one new package.json script, one new TS file. The seed run itself is operator-gated (`autonomous: false`) because it issues destructive TRUNCATE statements against the dev database — operator must confirm the DB target is correct before execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-schema-migrations-and-seed-data/32-CONTEXT.md
@.planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md
@.planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md
@.planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md
@drizzle.config.ts
@src/db/index.ts
@src/db/schema/index.ts
@src/db/seed-data.json
@package.json

<interfaces>
<!-- Full skeleton in 32-RESEARCH.md §4 "src/db/seed.ts" (lines 436-484) and 32-PATTERNS.md §"src/db/seed.ts" (lines 246-303). -->

src/db/seed.ts structure (top → bottom):
  1. dotenv config: `import { config } from 'dotenv'; import path from 'path'; config({ path: path.resolve(__dirname, '../../.env.local') });` — path depth differs from drizzle.config.ts because seed.ts lives at src/db/ (two dirs deep).
  2. Env guard: `if (!process.env.DATABASE_URL_UNPOOLED) throw new Error(...)` — same error message style as drizzle.config.ts line 10-14.
  3. Imports (AFTER dotenv config to avoid module-load ordering issues): `neon` from '@neondatabase/serverless', `drizzle` from 'drizzle-orm/neon-http', `sql` from 'drizzle-orm', `productionOrders` from './schema/index', `seedData` from './seed-data.json'.
  4. Async `seed()` function: create db client via `neon(process.env.DATABASE_URL_UNPOOLED!)` + `drizzle({ client })`, log "Truncating...", `await db.execute(sql\`TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE\`)`, log row count, `await db.insert(productionOrders).values(seedData as any)`, log "Seed complete", `process.exit(0)`.
  5. Top-level invocation: `seed().catch((err) => { console.error('Seed failed:', err); process.exit(1); });`

Key invariants:
  - `users` is NEVER in the TRUNCATE list (D-17 — would orphan real Clerk-mapped rows once Phase 33 lazy-sync ships)
  - No `import 'server-only'` — CLI script, not a Next.js module (32-PATTERNS.md §"Shared Patterns / import 'server-only' scope")
  - Uses UNPOOLED (direct) connection, not DATABASE_URL (pooled) — same routing as drizzle.config.ts
  - `__dirname` is available because package.json has no `"type": "module"` (CJS default, verified)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install tsx + add db:seed script to package.json</name>
  <files>package.json</files>
  <read_first>
    .planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md,
    package.json
  </read_first>
  <action>
    (1) Install tsx as a devDependency: `npm install -D tsx@4.21.0` (the version pinned in 32-RESEARCH.md §4 "Install command" line 508 and "Environment Availability" line 859). This writes `"tsx": "4.21.0"` (or `^4.21.0`) into `package.json` devDependencies and updates `package-lock.json`. (2) Edit `package.json` scripts to add `"db:seed": "tsx src/db/seed.ts"`. Place it after `db:migrate` (added in Plan 04). Do NOT touch existing scripts (test, build, lint, e2e, etc.). After this task, `package.json` scripts contains exactly three new entries (db:generate, db:migrate from Plan 04; db:seed from this plan) and ZERO `db:push` script (verified by grep).
  </action>
  <verify>
    <automated>grep -q '"tsx"' package.json &amp;&amp; grep -q '"db:seed": "tsx src/db/seed.ts"' package.json &amp;&amp; ! grep -q '"db:push"' package.json &amp;&amp; npx tsx --version</automated>
  </verify>
  <acceptance_criteria>
    - Source: package.json devDependencies contains `"tsx"` (value matches 4.21.0 or ^4.21.0)
    - Source: package.json scripts contains `"db:seed": "tsx src/db/seed.ts"`
    - Source: package.json does NOT contain `"db:push"` (D-23 ban preserved)
    - Source: package-lock.json updated (tsx + transitive deps recorded)
    - Command: `npx tsx --version` exits 0 and prints a version starting with `4.`
  </acceptance_criteria>
  <done>
    tsx installed; db:seed script wired; D-23 ban still enforced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author src/db/seed.ts (TRUNCATE + bulk insert runtime)</name>
  <files>src/db/seed.ts</files>
  <read_first>
    .planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-PATTERNS.md,
    drizzle.config.ts,
    src/db/index.ts,
    src/db/schema/index.ts,
    src/db/seed-data.json
  </read_first>
  <action>
    Create `src/db/seed.ts` per the exact skeleton in 32-RESEARCH.md §4 "src/db/seed.ts" (lines 438-484) and the per-section pattern in 32-PATTERNS.md §"src/db/seed.ts" (lines 246-303). Critical requirements (each is independently testable via the acceptance criteria below):
    (1) Line-1 import of dotenv FIRST, before any DB import (module-load ordering).
    (2) `path.resolve(__dirname, '../../.env.local')` — two-level path depth from src/db/ to repo root (NOT one level — that would target src/.env.local which does not exist).
    (3) Env-var guard with the same throw message style as drizzle.config.ts line 10-14 — mention DATABASE_URL_UNPOOLED, PgBouncer transaction-mode incompatibility, and the `.env.example` reference.
    (4) Use `neon(process.env.DATABASE_URL_UNPOOLED!)` + `drizzle({ client })` — same singleton shape as `src/db/index.ts` line 26-27 except for the env-var name.
    (5) The TRUNCATE statement is exactly `TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE` — the `users` table is intentionally and explicitly NOT included (D-17 / threat T-32-05).
    (6) `await db.insert(productionOrders).values(seedData as any)` — single batch insert for 33 rows. The `as any` cast is acceptable here because `seed-data.json`'s shape was validated by the Plan 05 test; type-safe alternative would require importing the inferred `NewProductionOrder` and casting per-row, which is verbose for no gain.
    (7) Top-level `.catch()` handler exits with code 1 on failure.
    (8) NO `import 'server-only'` directive (CLI script — verified by acceptance grep).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE" src/db/seed.ts &amp;&amp; ! grep -E "TRUNCATE[^\"]*users|users[^\"]*TRUNCATE" src/db/seed.ts &amp;&amp; ! grep -E "^\s*import 'server-only'" src/db/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/db/seed.ts contains `import { config } from 'dotenv'`
    - Source: src/db/seed.ts contains `path.resolve(__dirname, '../../.env.local')` (two-level depth)
    - Source: src/db/seed.ts contains `if (!process.env.DATABASE_URL_UNPOOLED)` (env guard)
    - Source: src/db/seed.ts contains `neon(process.env.DATABASE_URL_UNPOOLED!)` (UNPOOLED routing — NOT DATABASE_URL pooled)
    - Source: src/db/seed.ts contains the EXACT string `TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE`
    - Source: src/db/seed.ts does NOT contain `users` ANYWHERE in the TRUNCATE-context source code (D-17 invariant — `users` MUST NOT appear in the TRUNCATE list). Verified by: `grep -c 'TRUNCATE.*users' src/db/seed.ts` returns 0
    - Source: src/db/seed.ts contains `db.insert(productionOrders).values(`
    - Source: src/db/seed.ts contains `import seedData from './seed-data.json'`
    - Source: src/db/seed.ts contains `process.exit(1)` (error path)
    - Source: src/db/seed.ts does NOT contain `import 'server-only'` (CLI script — verified by grep)
    - Build: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    seed.ts compiles clean; TRUNCATE list excludes users (D-17 enforced); UNPOOLED routing matches Phase 31 discipline.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Run npm run db:seed against the Neon dev DB + verify row count</name>
  <files></files>
  <read_first>
    .planning/phases/32-schema-migrations-and-seed-data/32-RESEARCH.md,
    .planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md,
    src/db/seed.ts
  </read_first>
  <what-built>
    Claude will run `npm run db:seed` from the repo root. The script: (1) loads `.env.local`, (2) connects to Neon via `DATABASE_URL_UNPOOLED`, (3) executes `TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE` (note: users is NOT in this list per D-17), (4) inserts the 33 rows from `src/db/seed-data.json`, (5) logs `Truncating...`, `Inserting 33 seed rows...`, `Seed complete.` and exits 0. Expected runtime: <5 seconds for 33 rows over the Neon HTTP driver. This is the canonical SC#4 verification from 32-RESEARCH.md §6 "SC#4 — Seed row count" (lines 640-657).
  </what-built>
  <how-to-verify>
    1. After `npm run db:seed` completes (exit 0), verify the row count: `psql "$DATABASE_URL_UNPOOLED" -c "SELECT count(*) FROM production_orders;"` → expect `33`.
    2. Verify the D-19 sentinel: `psql "$DATABASE_URL_UNPOOLED" -c "SELECT DISTINCT created_by FROM production_orders;"` → expect a single value `system-seed`.
    3. Verify D-18 (other tables empty): `psql "$DATABASE_URL_UNPOOLED" -c "SELECT count(*) FROM order_events;"` → expect `0`. Same for `import_batches`.
    4. Verify the D-17 invariant (users NOT truncated): `psql "$DATABASE_URL_UNPOOLED" -c "SELECT count(*) FROM users;"` → expect `0` (users was empty before — no rows orphaned, no rows wiped). Note: this is the FIRST seed run; users would have been empty regardless. The real D-17 test fires once Phase 33 lazy-sync ships and users contains rows; THIS run sets the baseline that the table is reachable + present, not that the protection works in anger.
    5. Verify mill-line distribution: `psql "$DATABASE_URL_UNPOOLED" -c "SELECT mill_line, count(*) FROM production_orders GROUP BY mill_line;"` → expect three rows: Premix 11, Excel 11, CGM 11 (per 32-PATTERNS.md §"All 33 orders confirmed" line 242).
    6. Re-runnable check: run `npm run db:seed` a second time. Expect: TRUNCATE + INSERT again, exit 0, row count still 33. This proves D-17 idempotency.
    7. Type "seeded" if all checks pass, or describe the failure for Claude to diagnose.
  </how-to-verify>
  <resume-signal>Type "seeded" after psql verifies the 33-row count + sentinel + distribution, or describe error to trigger diagnosis.</resume-signal>
  <acceptance_criteria>
    - Command output: `npm run db:seed` exits 0 with `Seed complete.` in stdout
    - DB state: `SELECT count(*) FROM production_orders` returns 33
    - DB state: `SELECT DISTINCT created_by FROM production_orders` returns the single value `system-seed`
    - DB state: `SELECT count(*) FROM order_events` returns 0 (D-18)
    - DB state: `SELECT count(*) FROM import_batches` returns 0 (D-18)
    - DB state: `SELECT count(*) FROM users` returns 0 (D-17 — table not touched)
    - DB state: GROUP BY mill_line returns Premix=11, Excel=11, CGM=11
    - Idempotency: second `npm run db:seed` run also exits 0, row count still 33
    - Operator has typed "seeded"
  </acceptance_criteria>
  <done>
    Seed applied successfully; dev DB has 33 baseline rows; D-17 (users untouched) verified; D-18 (only orders seeded) verified; D-19 (system-seed sentinel) verified; idempotency verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` (gitignored) → seed.ts process | Operator-controlled secret file; Phase 31 verified gitignore at `.env*` (line 40 of `.gitignore`). |
| seed.ts → Neon dev DB (over HTTPS) | DATABASE_URL_UNPOOLED is the dev URL only; production credentials are not yet provisioned (Vercel env-var work deferred to Phase 34 per Phase 31 D-04). |
| src/db/seed-data.json (committed, reviewed) → db.insert via Drizzle parameterized inserts | Drizzle parameterizes all INSERT values; no SQL string interpolation; even tampered JSON cannot inject SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Seed data injection via SQL string concat | mitigate | Drizzle `db.insert().values()` always parameterizes; no `db.execute(sql\`INSERT ...\`)` with string interpolation. Acceptance criterion (Task 2) requires `db.insert(productionOrders).values(` literal — no raw SQL insert path. |
| T-32-03 | Denial of Service / Destructive Operation | `npm run db:seed` accidentally run against PRODUCTION DB (truncates real data) | mitigate | Four layers: (1) `DATABASE_URL_UNPOOLED` lives only in `.env.local` (gitignored), pointing at the Neon dev branch. (2) Task 3 is `checkpoint:human-action gate="blocking"` — operator must verify and type "seeded". (3) No CI workflow runs `db:seed` in Phase 32. (4) The seed prints the TRUNCATE statement before executing — operator sees what is about to happen. Vercel env-var work explicitly deferred (Phase 31 D-04), so DATABASE_URL_UNPOOLED literally cannot be the production URL on a dev machine without operator action. |
| T-32-05 | Tampering | TRUNCATE accidentally includes `users` table (would orphan Clerk-mapped rows once Phase 33 lazy-sync ships) | mitigate | D-17 + Task 2 acceptance criterion: `grep -c 'TRUNCATE.*users' src/db/seed.ts` returns 0. The TRUNCATE statement is an exact string match in the acceptance — any modification including `users` in the list fails the gate. Task 3 step 4 verifies `SELECT count(*) FROM users = 0` as a baseline (the real fire test arrives in Phase 33). |
| T-32-10 | Information Disclosure | Connection-string leak in error logs / stack traces | accept | The env guard's error message does not echo the URL value (just the env var name). Stack traces from `neon()` or `drizzle()` failures may include partial URL components, but DATABASE_URL_UNPOOLED is a dev-only credential per Phase 31 D-04. Production credentials are not yet provisioned. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep -c 'TRUNCATE.*users' src/db/seed.ts` returns 0 (D-17 invariant)
- `grep -c 'TRUNCATE production_orders, order_events, import_batches RESTART IDENTITY CASCADE' src/db/seed.ts` returns 1
- `grep -c 'DATABASE_URL_UNPOOLED' src/db/seed.ts` returns at least 2 (env-guard + connection)
- `grep -c "import 'server-only'" src/db/seed.ts` returns 0
- After Task 3 "seeded" signal: dev DB has 33 production_orders rows, 0 events, 0 imports, 0 users; all created_by = 'system-seed'; mill_line distribution 11/11/11
</verification>

<success_criteria>
- RUNTIME half of DATA-07 satisfied: `npm run db:seed` populates the dev DB with 33 baseline orders
- D-17 enforced: `users` table is NOT truncated by the seed script (verified both by source-grep and by live-DB check)
- D-18 enforced: other transactional tables (order_events, import_batches) remain empty post-seed
- D-19 enforced: every seeded row has `created_by = 'system-seed'`
- Idempotency: re-running `npm run db:seed` produces the same final DB state (33 rows; no duplicates)
</success_criteria>

<output>
After completion, create `.planning/phases/32-schema-migrations-and-seed-data/32-06-SUMMARY.md` per the standard template. Include the live row-count verification output as evidence of DATA-07 satisfaction.
</output>
