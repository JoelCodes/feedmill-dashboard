---
phase: 26-route-restructuring-and-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/Sidebar.test.tsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Sidebar shows Orders, Customers, Mill Production links when user is on /demo/* route"
    - "Sidebar shows Coming Soon placeholder link when user is on non-demo route"
    - "Settings link visible in both demo and production contexts"
    - "Section label displays DEMO when on demo routes, PRODUCTION otherwise"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Context-aware navigation rendering"
      exports: ["default (Sidebar)"]
      contains: "isDemoContext"
    - path: "src/components/Sidebar.test.tsx"
      provides: "Unit tests for context-aware navigation"
      contains: "demoNavItems"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "next/navigation"
      via: "usePathname()"
      pattern: "pathname\\.startsWith\\('/demo'\\)"
---

<objective>
Implement context-aware sidebar navigation using TDD

Purpose: Sidebar must display different navigation items based on route context — demo routes show Orders/Customers/Mill Production links pointing to /demo/* paths, while production routes show a single "Coming Soon" placeholder. This enables users to navigate correctly within their authorized context.

Output: Updated Sidebar component with conditional navItems arrays and comprehensive tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/tdd.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-route-restructuring-and-migration/26-CONTEXT.md
@.planning/phases/26-route-restructuring-and-migration/26-RESEARCH.md
@.planning/phases/26-route-restructuring-and-migration/26-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/Sidebar.tsx (current implementation):
```typescript
// Navigation item structure (lines 15-26)
const navItems = [
  { icon: LayoutDashboard, label: "Dashboard", id: "dashboard", href: "/" },
  { icon: Factory, label: "Production", id: "production", href: "/mill-production" },
  { icon: ClipboardList, label: "Orders", id: "orders", href: "/orders" },
  { icon: Users, label: "Customers", id: "customers", href: "/customers" },
  { icon: Package, label: "Inventory", id: "inventory", href: "/inventory" },
  { icon: Truck, label: "Shipments", id: "shipments", href: "/shipments" },
];

const settingsItems = [
  { icon: FlaskConical, label: "Formulas", id: "formulas", href: "#" },
];

// Active detection pattern (lines 28-33)
function isActive(href: string, pathname: string): boolean {
  if (href === "/") {
    return pathname === "/";
  }
  return pathname.startsWith(href);
}

// Component structure (lines 35-84)
export default function Sidebar() {
  const pathname = usePathname();
  // ... renders navItems and settingsItems
}
```

From next/navigation:
```typescript
import { usePathname } from 'next/navigation';
// Returns current pathname as string, e.g., "/demo/orders" or "/"
```
</interfaces>
</context>

<feature>
  <name>Context-Aware Sidebar Navigation</name>
  <files>src/components/Sidebar.tsx, src/components/Sidebar.test.tsx</files>
  <behavior>
    Input: usePathname() returns current route
    Output: Conditional rendering of navigation items

    Test Cases:
    - pathname="/demo/orders" -> renders Orders, Customers, Mill Production links with /demo/* hrefs
    - pathname="/demo/customers" -> same demo navigation rendered
    - pathname="/" -> renders single "Coming Soon" link with href="/"
    - pathname="/settings" -> renders "Coming Soon" link (production context)
    - Demo context: section label displays "DEMO"
    - Production context: section label displays "PRODUCTION"
    - Both contexts: Settings link visible with href="/settings"
  </behavior>
  <implementation>
    1. Replace single navItems array with demoNavItems and productionNavItems
    2. Add isDemoContext = pathname.startsWith('/demo') after usePathname() call
    3. Select mainNavItems = isDemoContext ? demoNavItems : productionNavItems
    4. Update section label to show "DEMO" or "PRODUCTION" based on isDemoContext
    5. Update settingsItems to use href="/settings" and label="Settings"
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for context-aware navigation</name>
  <files>src/components/Sidebar.test.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (current implementation to understand structure)
    - src/components/DashboardLayout.test.tsx (for mock patterns)
  </read_first>
  <behavior>
    - Test: "renders demo navigation when pathname starts with /demo"
      - Mock usePathname to return "/demo/orders"
      - Expect "Orders" link with href="/demo/orders"
      - Expect "Customers" link with href="/demo/customers"
      - Expect "Mill Production" link with href="/demo/mill-production"
    - Test: "renders production navigation when pathname is root"
      - Mock usePathname to return "/"
      - Expect "Coming Soon" link with href="/"
      - Expect NOT to find "Orders" link
    - Test: "renders DEMO section label on demo routes"
      - Mock usePathname to return "/demo/orders"
      - Expect text "DEMO" in component
    - Test: "renders PRODUCTION section label on non-demo routes"
      - Mock usePathname to return "/"
      - Expect text "PRODUCTION" in component
    - Test: "renders Settings link in both contexts"
      - Mock usePathname to return "/demo/orders"
      - Expect "Settings" link with href="/settings"
      - Remock to return "/"
      - Expect "Settings" link with href="/settings"
  </behavior>
  <action>
    Create src/components/Sidebar.test.tsx with Jest + RTL tests. Use jest.mock("next/navigation") to mock usePathname. Structure tests with describe blocks for "demo context" and "production context". Each test should render Sidebar and query for expected links by role="link" and name attribute. Tests will FAIL initially because current Sidebar has static navItems.
  </action>
  <verify>
    <automated>npm test -- src/components/Sidebar.test.tsx 2>&1 | grep -E "(FAIL|PASS|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Sidebar.test.tsx exists with 5+ test cases
    - Tests import Sidebar and mock usePathname from next/navigation
    - Running tests shows FAIL status (RED phase)
    - Test file contains describe("demo context") and describe("production context") blocks
  </acceptance_criteria>
  <done>Test file created, tests run and FAIL as expected (RED phase complete)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement context-aware navigation in Sidebar</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (current implementation)
    - src/components/Sidebar.test.tsx (tests to satisfy)
    - .planning/phases/26-route-restructuring-and-migration/26-PATTERNS.md (target pattern)
  </read_first>
  <action>
    Modify src/components/Sidebar.tsx to implement context-aware navigation per D-02, D-03, D-04, D-07:

    1. Replace single navItems array (lines 15-22) with two arrays:
       - demoNavItems: Orders (/demo/orders), Customers (/demo/customers), Mill Production (/demo/mill-production)
       - productionNavItems: Coming Soon (/)

    2. Update settingsItems (lines 24-26):
       - Change label from "Formulas" to "Settings"
       - Change href from "#" to "/settings"
       - Change id from "formulas" to "settings"

    3. Add context detection after usePathname() call (line 36):
       - const isDemoContext = pathname.startsWith('/demo')
       - const mainNavItems = isDemoContext ? demoNavItems : productionNavItems

    4. Update section label (line 51):
       - Change static "PRODUCTION" to: {isDemoContext ? "DEMO" : "PRODUCTION"}

    5. Update navItems.map (line 55) to use mainNavItems instead of navItems

    Keep: isActive function, NavItem component, all styling unchanged.
  </action>
  <verify>
    <automated>npm test -- src/components/Sidebar.test.tsx 2>&1 | grep -E "(FAIL|PASS|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Sidebar.tsx contains "isDemoContext"
    - src/components/Sidebar.tsx contains "demoNavItems" array with /demo/* hrefs
    - src/components/Sidebar.tsx contains "productionNavItems" array with Coming Soon item
    - src/components/Sidebar.tsx contains settingsItems with href="/settings"
    - All Sidebar tests PASS (GREEN phase)
    - npm run build succeeds with no type errors
  </acceptance_criteria>
  <done>All tests pass, Sidebar renders correct navigation based on route context</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - Clean up and verify no regressions</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (implementation from Task 2)
  </read_first>
  <action>
    Review implementation for cleanup opportunities:
    1. Ensure demoNavItems and productionNavItems are defined as const arrays outside component (no re-creation on render)
    2. Verify icon imports are minimal (remove unused icons: LayoutDashboard, Package, Truck if no longer referenced)
    3. Add JSDoc comment explaining context-aware behavior
    4. Ensure consistent naming: mainNavItems for the selected array

    Run full test suite to verify no regressions in other components using Sidebar.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No unused icon imports in Sidebar.tsx (Lucide icons: only ClipboardList, Factory, Users, FlaskConical, LayoutDashboard needed)
    - demoNavItems and productionNavItems defined outside Sidebar function (not recreated on render)
    - Full test suite passes (300+ tests)
    - npm run build succeeds
  </acceptance_criteria>
  <done>Code is clean, all tests pass, no regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Sidebar is a UI component with no data fetching or authentication logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | Sidebar navigation links | accept | Navigation links are public route paths; actual access control enforced by middleware (Phase 25) |
| T-26-02 | Tampering | isDemoContext detection | accept | Client-side detection is for UX only; server-side middleware enforces actual access control |

Note: This is a UI-only change. Security enforcement remains in middleware (ACCESS-01 from Phase 25).
</threat_model>

<verification>
- All 5+ Sidebar tests pass
- Full test suite passes (npm test)
- Build succeeds (npm run build)
- Manual: Navigate to / and verify "Coming Soon" + "Settings" in sidebar
- Manual: Navigate to /demo/orders and verify "Orders", "Customers", "Mill Production", "Settings" in sidebar
</verification>

<success_criteria>
- NAV-01 satisfied: Sidebar displays context-appropriate navigation
- Demo routes (/demo/*) show demo navigation with correct /demo/* hrefs
- Non-demo routes show production placeholder navigation
- Settings accessible from both contexts
- Section labels correctly display "DEMO" or "PRODUCTION"
</success_criteria>

<output>
After completion, create `.planning/phases/26-route-restructuring-and-migration/26-01-SUMMARY.md`
</output>
