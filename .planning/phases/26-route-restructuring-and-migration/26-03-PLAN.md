---
phase: 26-route-restructuring-and-migration
plan: 03
type: execute
wave: 2
depends_on: [26-01, 26-02]
files_modified:
  - src/app/demo/orders/page.tsx
  - src/app/demo/orders/__tests__/page.test.tsx
  - src/app/demo/customers/page.tsx
  - src/app/demo/customers/__tests__/page.test.tsx
  - src/app/demo/customers/[id]/page.tsx
  - src/app/demo/customers/[id]/page.test.tsx
  - src/app/demo/mill-production/page.tsx
  - src/app/demo/mill-production/__tests__/page.test.tsx
autonomous: true
requirements: [ROUTE-01]

must_haves:
  truths:
    - "Orders page accessible at /demo/orders"
    - "Customers page accessible at /demo/customers"
    - "Customer detail page accessible at /demo/customers/[id]"
    - "Mill Production page accessible at /demo/mill-production"
    - "All migrated pages use DashboardLayout wrapper"
    - "All existing tests pass after migration"
  artifacts:
    - path: "src/app/demo/orders/page.tsx"
      provides: "Orders page at /demo/orders"
      contains: "DashboardLayout"
    - path: "src/app/demo/customers/page.tsx"
      provides: "Customers page at /demo/customers"
      contains: "DashboardLayout"
    - path: "src/app/demo/customers/[id]/page.tsx"
      provides: "Customer detail page"
      contains: "DashboardLayout"
    - path: "src/app/demo/mill-production/page.tsx"
      provides: "Mill Production page at /demo/mill-production"
      contains: "DashboardLayout"
  key_links:
    - from: "src/app/demo/*/page.tsx"
      to: "src/components/DashboardLayout.tsx"
      via: "import and wrapper"
      pattern: "<DashboardLayout>"
---

<objective>
Migrate existing pages to /demo/* namespace with DashboardLayout refactor

Purpose: Move orders, customers, and mill-production pages from root-level routes to /demo/* subdirectory. Each page is refactored to use the shared DashboardLayout component instead of duplicating Sidebar/Header/layout structure. Tests are migrated alongside pages with updated pathname mocks.

Output: All demo pages accessible at /demo/* paths, using shared DashboardLayout, with passing tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-route-restructuring-and-migration/26-CONTEXT.md
@.planning/phases/26-route-restructuring-and-migration/26-PATTERNS.md
@.planning/phases/25-foundation-and-middleware-configuration/25-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/DashboardLayout.tsx:
```typescript
interface DashboardLayoutProps {
  children: React.ReactNode;
}

export default function DashboardLayout({ children }: DashboardLayoutProps) {
  return (
    <div className="bg-bg-page flex h-screen">
      <Sidebar />
      <main className="flex flex-1 flex-col gap-6 overflow-auto p-6 pr-8">
        <Header />
        {children}
      </main>
    </div>
  );
}
```

From src/app/orders/page.tsx (current - to be migrated):
```typescript
// Lines 31-43 - layout structure to REMOVE
export default function OrdersPage() {
  return (
    <div className="bg-bg-page flex h-screen">
      <Sidebar />
      <main className="flex flex-1 flex-col gap-6 overflow-auto p-6 pr-8">
        <Header />
        <Suspense fallback={...}>
          <OrdersContent />
        </Suspense>
      </main>
    </div>
  );
}

// Target structure after migration:
export default function OrdersPage() {
  return (
    <DashboardLayout>
      <Suspense fallback={...}>
        <OrdersContent />
      </Suspense>
    </DashboardLayout>
  );
}
```

From 26-PATTERNS.md test mock pattern:
```typescript
jest.mock("next/navigation", () => ({
  useSearchParams: jest.fn(() => ({
    get: jest.fn(() => null),
  })),
  useRouter: jest.fn(() => ({
    push: jest.fn(),
  })),
  usePathname: jest.fn(() => "/demo/orders"), // Update from "/orders"
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Orders page to /demo/orders</name>
  <files>src/app/demo/orders/page.tsx, src/app/demo/orders/__tests__/page.test.tsx</files>
  <read_first>
    - src/app/orders/page.tsx (source file to migrate)
    - src/app/orders/__tests__/page.test.tsx (test file to migrate)
    - src/components/DashboardLayout.tsx (target layout wrapper)
  </read_first>
  <action>
    1. Create directory: src/app/demo/orders/__tests__/

    2. Create src/app/demo/orders/page.tsx by copying src/app/orders/page.tsx with modifications:
       - Keep "use client" directive
       - Keep useState, useEffect, Suspense imports from react
       - Keep useSearchParams import from next/navigation
       - REMOVE Sidebar import (line 5)
       - REMOVE Header import (line 6)
       - ADD import DashboardLayout from "@/components/DashboardLayout"
       - Keep OrdersTable import
       - Keep OrdersContent function unchanged (lines 9-29)
       - Modify OrdersPage function (lines 31-43):
         - REMOVE outer div with bg-bg-page
         - REMOVE Sidebar component
         - REMOVE main element
         - REMOVE Header component
         - WRAP Suspense with DashboardLayout

    3. Create src/app/demo/orders/__tests__/page.test.tsx by copying src/app/orders/__tests__/page.test.tsx with modifications:
       - Update import path: from "../page" (unchanged - relative path still works)
       - Update usePathname mock return value: "/demo/orders" (was "/orders")

    4. Delete old files:
       - rm src/app/orders/page.tsx
       - rm -r src/app/orders/__tests__/
       - rmdir src/app/orders/ (if empty)
  </action>
  <verify>
    <automated>npm test -- src/app/demo/orders 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/demo/orders/page.tsx exists and contains DashboardLayout import
    - src/app/demo/orders/page.tsx does NOT contain Sidebar or Header imports
    - src/app/demo/orders/__tests__/page.test.tsx exists with usePathname mock returning "/demo/orders"
    - src/app/orders/ directory no longer exists
    - Tests pass for demo/orders
  </acceptance_criteria>
  <done>Orders page migrated to /demo/orders with DashboardLayout, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Customers pages to /demo/customers</name>
  <files>src/app/demo/customers/page.tsx, src/app/demo/customers/__tests__/page.test.tsx, src/app/demo/customers/[id]/page.tsx, src/app/demo/customers/[id]/page.test.tsx</files>
  <read_first>
    - src/app/customers/page.tsx (main customers page)
    - src/app/customers/__tests__/page.test.tsx (main page test)
    - src/app/customers/[id]/page.tsx (detail page)
    - src/app/customers/[id]/page.test.tsx (detail page test)
    - src/components/DashboardLayout.tsx (target layout wrapper)
  </read_first>
  <action>
    1. Create directories:
       - src/app/demo/customers/__tests__/
       - src/app/demo/customers/[id]/

    2. Migrate src/app/customers/page.tsx to src/app/demo/customers/page.tsx:
       - Keep "use client" directive
       - Keep all useState, useEffect, useMemo imports
       - Keep useRouter import from next/navigation
       - REMOVE Sidebar import
       - REMOVE Header import
       - ADD import DashboardLayout from "@/components/DashboardLayout"
       - Keep all other imports (Card, services, utils, types)
       - Keep CustomerTableSkeleton and EmptyState helper functions
       - Modify CustomersPage return: wrap Card content in DashboardLayout instead of div>Sidebar>main>Header

    3. Migrate src/app/customers/__tests__/page.test.tsx to src/app/demo/customers/__tests__/page.test.tsx:
       - Update usePathname mock: "/demo/customers"

    4. Migrate src/app/customers/[id]/page.tsx to src/app/demo/customers/[id]/page.tsx:
       - Same DashboardLayout refactor pattern
       - REMOVE Sidebar and Header imports
       - ADD DashboardLayout import
       - Wrap content in DashboardLayout

    5. Migrate src/app/customers/[id]/page.test.tsx to src/app/demo/customers/[id]/page.test.tsx:
       - Update usePathname mock: "/demo/customers/123" or similar

    6. Also check for src/app/customers/page.test.tsx (non-__tests__ location) and migrate if exists

    7. Delete old files:
       - rm -r src/app/customers/
  </action>
  <verify>
    <automated>npm test -- src/app/demo/customers 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/demo/customers/page.tsx exists with DashboardLayout import
    - src/app/demo/customers/[id]/page.tsx exists with DashboardLayout import
    - Neither file contains Sidebar or Header imports
    - Test files exist in new locations with updated pathname mocks
    - src/app/customers/ directory no longer exists
    - All customers tests pass
  </acceptance_criteria>
  <done>Customers pages migrated to /demo/customers with DashboardLayout, tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Mill Production page to /demo/mill-production</name>
  <files>src/app/demo/mill-production/page.tsx, src/app/demo/mill-production/__tests__/page.test.tsx</files>
  <read_first>
    - src/app/mill-production/page.tsx (source file to migrate)
    - src/app/mill-production/__tests__/page.test.tsx (test file to migrate)
    - src/components/DashboardLayout.tsx (target layout wrapper)
  </read_first>
  <action>
    1. Create directory: src/app/demo/mill-production/__tests__/

    2. Migrate src/app/mill-production/page.tsx to src/app/demo/mill-production/page.tsx:
       - Keep "use client" directive
       - Keep all react imports (useEffect, useState, useMemo)
       - Keep type imports from @/types/millProduction
       - Keep service import from @/services/millProduction
       - REMOVE Sidebar import (line 10)
       - REMOVE Header import (line 11)
       - ADD import DashboardLayout from "@/components/DashboardLayout"
       - Keep FilterPill import
       - Keep all constants (STATE_ORDER, STATE_COLORS, PRODUCTION_STATE_PILL_CONFIG)
       - Keep all helper components (MillColumn, ProductionOrderCard, LoadingSkeleton)
       - Modify MillProductionPage return: wrap filter div and loading/content in DashboardLayout

    3. Migrate test file:
       - src/app/mill-production/__tests__/page.test.tsx to src/app/demo/mill-production/__tests__/page.test.tsx
       - Update usePathname mock: "/demo/mill-production"

    4. Delete old files:
       - rm -r src/app/mill-production/
  </action>
  <verify>
    <automated>npm test -- src/app/demo/mill-production 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/demo/mill-production/page.tsx exists with DashboardLayout import
    - src/app/demo/mill-production/page.tsx does NOT contain Sidebar or Header imports
    - src/app/demo/mill-production/__tests__/page.test.tsx exists with usePathname mock returning "/demo/mill-production"
    - src/app/mill-production/ directory no longer exists
    - Mill production tests pass
  </acceptance_criteria>
  <done>Mill Production page migrated to /demo/mill-production with DashboardLayout, tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Verify full test suite and build</name>
  <files>None (verification only)</files>
  <read_first>
    - src/app/demo/ (verify new structure)
  </read_first>
  <action>
    Run comprehensive verification:

    1. Verify directory structure:
       - ls -la src/app/demo/ should show orders/, customers/, mill-production/
       - ls -la src/app/ should NOT show orders/, customers/, mill-production/ at root

    2. Run full test suite:
       - npm test

    3. Run build to verify no import errors:
       - npm run build

    4. Verify no broken imports by checking TypeScript:
       - npm run build should complete without errors

    Expected: 300+ tests passing, build succeeds, no type errors
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/app/orders/ does not exist
    - src/app/customers/ does not exist
    - src/app/mill-production/ does not exist
    - src/app/demo/orders/page.tsx exists
    - src/app/demo/customers/page.tsx exists
    - src/app/demo/mill-production/page.tsx exists
    - npm test shows all tests passing (expect 300+)
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <done>All pages migrated, full test suite passes, build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Middleware | Users accessing /demo/* routes are validated by middleware (Phase 25) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation of Privilege | Route migration | mitigate | Middleware pattern /demo(.*) already matches new paths - verified in Phase 25-02. No middleware changes needed. |
| T-26-05 | Denial of Service | Old route 404s | accept | Per D-01, old routes will 404. This is intentional (clean break). Users should update bookmarks. |

Note: Security enforced by existing middleware. Route migration does not change access control model.
</threat_model>

<verification>
- npm test passes (300+ tests)
- npm run build succeeds
- Old route directories deleted (src/app/orders, src/app/customers, src/app/mill-production)
- New route directories exist (src/app/demo/orders, src/app/demo/customers, src/app/demo/mill-production)
- All page files use DashboardLayout wrapper
- No Sidebar/Header imports in migrated page files
</verification>

<success_criteria>
- ROUTE-01 satisfied: Existing pages moved to /demo/* subdirectory
- Orders accessible at /demo/orders
- Customers accessible at /demo/customers
- Customer detail accessible at /demo/customers/[id]
- Mill Production accessible at /demo/mill-production
- All pages use shared DashboardLayout (NAV-02 from Phase 25)
- All tests pass after migration
- Build succeeds with no import errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-route-restructuring-and-migration/26-03-SUMMARY.md`
</output>
