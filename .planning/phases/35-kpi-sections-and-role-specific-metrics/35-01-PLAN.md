---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/orders.ts
  - src/db/schema/__tests__/orders.test.ts
  - drizzle/0001_*.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0001_snapshot.json
  - src/db/seed.ts
  - src/components/MillColumn.test.tsx
  - src/components/BlockedAlertBand.test.tsx
  - src/components/ProductionDashboard.test.tsx
  - src/components/ProductionDrawer.test.tsx
  - src/components/TransitionButtons.test.tsx
  - src/lib/__tests__/production-derivations.test.ts
autonomous: false
requirements: [KPI-08]
tags: [schema, migration, drizzle, seed, fixtures, early_delivery_date]
must_haves:
  truths:
    - "D-04: `production_orders.early_delivery_date` exists as a nullable `date` column in the live dev Neon database (verified via direct SQL or drizzle-kit introspect)"
    - "D-04: `ProductionOrder = typeof productionOrders.$inferSelect` gains `earlyDeliveryDate: string | null` — `npx tsc --noEmit` is green project-wide"
    - "D-06: After running `npm run db:seed`, all 33 seeded rows have a non-null `early_delivery_date`, deterministically spread across `today ±5 days` via `(i % 11) - 5` offset"
    - "Every `makeOrder` test factory in the codebase includes `earlyDeliveryDate: null` in its default spread — `npm test` is green project-wide"
    - "Phase 32 D-07 TRUNCATE-protection rule is preserved: `seed.ts` does NOT truncate the `users` table; backfill only adds `earlyDeliveryDate` to `production_orders` insert payload"
    - "Drizzle migration journal contains `0001_*` as the second entry; `0001_*.sql` is committed and re-runnable from scratch"
  artifacts:
    - path: "src/db/schema/orders.ts"
      provides: "earlyDeliveryDate: date('early_delivery_date') nullable column"
      contains: "earlyDeliveryDate"
    - path: "drizzle/0001_*.sql"
      provides: "ALTER TABLE production_orders ADD COLUMN early_delivery_date date"
      contains: "early_delivery_date"
    - path: "drizzle/meta/_journal.json"
      provides: "Second migration entry in tag-ordered journal"
    - path: "src/db/seed.ts"
      provides: "Deterministic earlyDeliveryDateFor(i) helper + injection into the rows.map() transform"
      contains: "earlyDeliveryDate"
    - path: "src/db/schema/__tests__/orders.test.ts"
      provides: "Schema assertion test for earlyDeliveryDate nullable date column"
  key_links:
    - from: "src/db/schema/orders.ts"
      to: "drizzle-orm/pg-core"
      via: "date() column constructor import"
      pattern: "import.*date.*drizzle-orm/pg-core"
    - from: "src/db/seed.ts"
      to: "src/db/schema/orders.ts"
      via: "productionOrders insert with earlyDeliveryDate"
      pattern: "earlyDeliveryDate"
    - from: "drizzle/meta/_journal.json"
      to: "drizzle/0001_*.sql"
      via: "tag entry pointing at the new migration"
      pattern: "0001_"
---

<objective>
Phase 35 foundation: add the nullable `early_delivery_date` column to `production_orders` (D-04), generate AND apply the Drizzle migration against the dev Neon DB, backfill synthetic dates for the 33 seeded rows using the locked `(i % 11) - 5` formula (D-06), and propagate the `ProductionOrder` type shape change to every `makeOrder` factory in the test suite (Pitfall 3 from RESEARCH.md).

Purpose: every downstream KPI plan in Phase 35 depends on this column existing both in TypeScript (the `$inferSelect` type that propagates to fixtures, queries, and UI) AND in the live database (KPI-08's `WHERE early_delivery_date < CURRENT_DATE AND state != 'Completed'` predicate cannot run without the column). The schema-push + fixture-propagation work is the only irreversible foundation step; once it lands, all subsequent plans are additive.

Output: schema column committed, Drizzle migration generated and applied to the dev Neon DB (BLOCKING migrate gate verifies live-DB state — TypeScript-only checks would falsely pass), seed runtime populates `earlyDeliveryDate` for all 33 rows, six fixture files updated, full Jest suite + `tsc --noEmit` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md
@.planning/phases/32-schema-migrations-and-seed-data/32-04-PLAN.md
@.planning/phases/32-schema-migrations-and-seed-data/32-06-PLAN.md
@src/db/schema/orders.ts
@src/db/schema/__tests__/orders.test.ts
@src/db/seed.ts
@src/db/seed-data.json
@src/components/MillColumn.test.tsx
@drizzle.config.ts

<interfaces>
<!-- Type definitions executors will rely on (extracted from existing files). -->

From src/db/schema/orders.ts (canonical, lines 25-86):
- `ProductionState = (typeof productionStateEnum.enumValues)[number]` — 'Pending' | 'Mixing' | 'Completed' | 'Blocked'
- `MillLine = (typeof millLineEnum.enumValues)[number]` — 'Premix' | 'Excel' | 'CGM'
- `ProductionOrder = typeof productionOrders.$inferSelect` — currently shaped:
    `{ id, orderNumber, customer, product, weightLbs (string), deliveryTime, state, millLine, textureType (string|null), lineCode (string|null), version, createdBy, createdAt, updatedAt }`
- Phase 35 adds: `earlyDeliveryDate: string | null` (PgDateString — Drizzle's default `date()` column mode infers as string)

From src/db/seed.ts (current shape, lines 93-140):
- `type SnakeRow = { order_number, customer, product, weight_lbs, delivery_time, state, mill_line, texture_type, line_code, created_by, version }`
- `seed-data.json` is a 33-row array of SnakeRow objects (NO `early_delivery_date` key — Option B from RESEARCH.md: computed at runtime, not stored)
- The `.map((r, i) => ({...}))` transform converts snake_case JSON to camelCase Drizzle insert objects.
- TRUNCATE discipline (Phase 32 D-07): the script truncates `production_orders`, `order_events`, `import_batches` only — NEVER `users`.

From src/db/schema/__tests__/orders.test.ts (current shape):
- Uses Drizzle's `getTableConfig` to assert column existence, types, nullability — extend the same pattern for the new column.
- Tests are pure assertions on the schema object, no DB connection needed.

Drizzle column-type rules (from RESEARCH.md Pattern 6 + Pitfall 3, VERIFIED):
- `date('early_delivery_date')` with NO `{ mode: 'date' }` option → `PgDateString` column → TS infers `string | null`
- ADD COLUMN NULL on existing populated table → non-destructive; existing rows get NULL
- Drizzle journal increments tag from `0000_aromatic_stone_men` to `0001_*` (snake_case slug auto-generated)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema column + schema test (TDD red/green) + propagate ProductionOrder type to all makeOrder fixtures</name>
  <files>
    src/db/schema/orders.ts,
    src/db/schema/__tests__/orders.test.ts,
    src/components/MillColumn.test.tsx,
    src/components/BlockedAlertBand.test.tsx,
    src/components/ProductionDashboard.test.tsx,
    src/components/ProductionDrawer.test.tsx,
    src/components/TransitionButtons.test.tsx,
    src/lib/__tests__/production-derivations.test.ts
  </files>
  <read_first>
    - src/db/schema/orders.ts (current column map lines 29-55 + ProductionOrder type at line 85 — the source of truth)
    - src/db/schema/__tests__/orders.test.ts (existing schema-assertion test pattern — extend with one new test case)
    - src/components/MillColumn.test.tsx lines 8-26 (canonical `makeOrder` shape — used as the model for the 5 other fixtures)
    - src/components/BlockedAlertBand.test.tsx lines 27-45 (second makeOrder factory)
    - src/components/ProductionDashboard.test.tsx line 74 (third makeOrder factory — note ID parameter shape)
    - src/components/ProductionDrawer.test.tsx line 56 (fourth makeOrder factory)
    - src/components/TransitionButtons.test.tsx (fifth makeOrder factory — verify location via grep)
    - src/lib/__tests__/production-derivations.test.ts lines 10-28 (sixth makeOrder factory)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/db/schema/orders.ts (EDIT — add `earlyDeliveryDate` column)" AND § "All `makeOrder` test fixtures (EDIT — add `earlyDeliveryDate: null`)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 6: `early_delivery_date` Drizzle Column + Migration" AND § "Pitfall 3: `ProductionOrder` Type Shape Changes Break Test Fixtures"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-04
  </read_first>
  <behavior>
    Schema test (extends `src/db/schema/__tests__/orders.test.ts`):
    - Test 1 (RED): `getTableConfig(productionOrders).columns.find(c => c.name === 'early_delivery_date')` returns a column object (currently undefined → assertion fails).
    - Test 2 (RED): the resolved column has `dataType === 'string'` (PgDateString default) AND `notNull === false` (nullable). D-04 explicitly locks nullable.
    - Test 3 (RED): the TypeScript type assertion `const _: ProductionOrder = { ..., earlyDeliveryDate: null }` compiles (proves the field is present and accepts null). Express as a `it.todo`-free runtime assertion: build a partial object via spread and assign — TypeScript surfaces a compile error if the field is missing.
    - After implementation (GREEN): all three tests pass.

    Fixture propagation tests (regression-only, no new assertions):
    - Every `makeOrder` factory across the 6 fixture files MUST include `earlyDeliveryDate: null` in its default spread (between `lineCode` and `version` per PATTERNS.md ordering). Existing tests continue to pass after the addition because the new field defaults to `null` and no existing test asserts on its presence.
  </behavior>
  <action>
    Step 1 (RED — schema test FIRST). Open `src/db/schema/__tests__/orders.test.ts` and add the three new tests described in <behavior>. Run `npm test -- --testPathPattern=schema/__tests__/orders.test` and CONFIRM the new tests are red (current schema has no `early_delivery_date` column). Commit message: `test(35-01): RED — assert earlyDeliveryDate nullable date column on productionOrders`.

    Step 2 (GREEN — schema column). Open `src/db/schema/orders.ts`:
    - Line 3-13 imports: add `date,` to the `drizzle-orm/pg-core` import list (alphabetic order is not strictly enforced in the existing import; place after `integer` per PATTERNS.md).
    - Inside the `pgTable` column map, immediately after `lineCode: text('line_code'),` (currently line 41), add:
        `earlyDeliveryDate: date('early_delivery_date'),`
      with an inline comment `// D-04: nullable date; PgDateString → TS: string | null`.
    - Do NOT add `{ mode: 'date' }` — Drizzle's default `PgDateString` mode is the locked choice (RESEARCH.md Pattern 6, VERIFIED via node inspection).
    - Do NOT add `.default(null)` — Postgres `date` columns nullable-by-default; adding `.default(null)` would emit an explicit DEFAULT clause that drizzle-kit may flag as a redundant change. The existing nullable `textureType: text('texture_type')` line is the analog — match its pattern exactly.
    - Do NOT add any new index on this column for Phase 35; KPI-08 query volume is small and `state` index suffices.
    Run the schema test again — CONFIRM all three tests are GREEN. Commit: `feat(35-01): GREEN — add earlyDeliveryDate column to productionOrders schema`.

    Step 3 (REFACTOR — fixture propagation). The schema change causes `tsc --noEmit` to fail in every `makeOrder` factory because `ProductionOrder = $inferSelect` now requires `earlyDeliveryDate: string | null`. Run `grep -rln 'makeOrder' src/` to confirm the 6 files listed in <files>. For EACH file:
    - Locate the `makeOrder` function body (search for `function makeOrder` or `const makeOrder`).
    - Add the line `earlyDeliveryDate: null,` between `lineCode: null,` and `version: 1,` (or wherever the field set ends — match the existing field order ProductionOrder's $inferSelect produces). Use exactly `earlyDeliveryDate: null,` — `undefined` is NOT acceptable because Drizzle's $inferSelect produces `string | null` not `string | null | undefined`.
    - Do NOT modify any other code in the fixture. Do NOT change assertion expectations. Do NOT add new test cases here.

    Run `npx tsc --noEmit` — confirm zero errors. Run `npm test` — confirm full suite green. Commit: `refactor(35-01): propagate earlyDeliveryDate: null to all makeOrder fixtures (Pitfall 3)`.

    Do NOT run `drizzle-kit generate` yet — that is Task 2's job. Do NOT update `seed.ts` yet — that is Task 3's job.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="schema/__tests__/orders\.test" && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -n "earlyDeliveryDate: date" src/db/schema/orders.ts` returns exactly 1 match on a line inside the `pgTable` column map.
    - source-assertion: `grep -c "^import.*date" src/db/schema/orders.ts` confirms `date` is imported from `drizzle-orm/pg-core` (combined with the existing import block — verify via `grep "from 'drizzle-orm/pg-core'" src/db/schema/orders.ts` shows `date` in the import list).
    - source-assertion: `grep -rn "earlyDeliveryDate: null" src/components/MillColumn.test.tsx src/components/BlockedAlertBand.test.tsx src/components/ProductionDashboard.test.tsx src/components/ProductionDrawer.test.tsx src/components/TransitionButtons.test.tsx src/lib/__tests__/production-derivations.test.ts | wc -l | tr -d ' '` returns ≥ 6 (one per fixture file; some files have multiple `makeOrder` declarations that share defaults — count may be higher).
    - source-assertion: `grep -L "earlyDeliveryDate" src/components/MillColumn.test.tsx src/components/BlockedAlertBand.test.tsx src/components/ProductionDashboard.test.tsx src/components/ProductionDrawer.test.tsx src/components/TransitionButtons.test.tsx src/lib/__tests__/production-derivations.test.ts` returns NO output (every fixture file now contains the field).
    - source-assertion: `grep -n "early_delivery_date" src/db/schema/__tests__/orders.test.ts` returns at least 1 match (the new schema test).
    - behavior-assertion: `npm test -- --testPathPattern="schema/__tests__/orders\.test"` shows the three new tests passing.
    - CLI output: `npx tsc --noEmit` exits 0 (no errors anywhere in `src/`).
    - CLI output: `npm test` exits 0 (full Jest suite green).
  </acceptance_criteria>
  <done>
    Schema column added with three TDD assertions green; ProductionOrder type now includes `earlyDeliveryDate: string | null`; all six `makeOrder` fixtures updated to include `earlyDeliveryDate: null`; full Jest suite and tsc --noEmit both green. Migration generation and live-DB apply deferred to Task 2.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 [BLOCKING]: Generate migration via drizzle-kit generate AND apply to dev Neon DB via drizzle-kit migrate</name>
  <files>
    drizzle/0001_*.sql,
    drizzle/meta/_journal.json,
    drizzle/meta/0001_snapshot.json
  </files>
  <read_first>
    - drizzle.config.ts (confirm `out: './drizzle'` and `schema` pointer; `DATABASE_URL_UNPOOLED` is the connection used for migrations)
    - drizzle/meta/_journal.json (confirm current journal has exactly one entry `0000_*` — the new migration will be appended as `0001_*`)
    - drizzle/0000_aromatic_stone_men.sql (analog migration shape — the new file will contain a single ALTER TABLE statement)
    - .planning/phases/32-schema-migrations-and-seed-data/32-04-PLAN.md (Phase 32's analogous generate+migrate task — same discipline applies here)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 6: `early_delivery_date` Drizzle Column + Migration" — expected SQL output documented
    - .planning/STATE.md § "Migration discipline" — `drizzle-kit push` is BANNED; only `drizzle-kit generate` + `drizzle-kit migrate` per Phase 32 D-06
  </read_first>
  <what-built>
    The schema-side column (Task 1) exists in TypeScript but the live dev Neon DB has not yet been altered. This checkpoint task runs `drizzle-kit generate` to produce the SQL migration file AND `drizzle-kit migrate` to apply it. Both commands MUST succeed for the phase to pass verification. TypeScript build will pass without this step (types come from the schema file, not the live DB) — that is a false-positive verification state. KPI-08's `WHERE early_delivery_date < CURRENT_DATE` predicate will fail at runtime if the column is absent from the live DB.

    This task is `checkpoint:human-action` because `drizzle-kit migrate` requires `DATABASE_URL_UNPOOLED` to point at the operator's Neon dev database. If the executor's environment has this variable, the executor may run the commands directly; if not, the operator runs them manually. Either path produces the same artifacts.
  </what-built>
  <how-to-verify>
    Sequence:
    1. Run `npx drizzle-kit generate` from the repo root. Expected output: a new file `drizzle/0001_<snake_case_slug>.sql` is created containing exactly one statement:
       `ALTER TABLE "production_orders" ADD COLUMN "early_delivery_date" date;`
       AND `drizzle/meta/_journal.json` is updated with a second entry (tag matching the new filename), AND a new `drizzle/meta/0001_snapshot.json` is written.
    2. Inspect the generated SQL file. If drizzle-kit produced ANY statement other than the single ADD COLUMN above (e.g., it tries to drop indices, recreate enums, change column types), STOP — Task 1 has a schema drift relative to the live DB. Report the diff to the operator before applying.
    3. Run `npx drizzle-kit migrate` to apply against the dev Neon DB. Expected: command exits 0 with a log line like `[✓] Migrations applied successfully!`. The drizzle_migrations table in the dev DB now records the `0001_*` entry.
    4. Verify the column exists in the live DB. Either:
       - `npx drizzle-kit introspect` — confirm `early_delivery_date` appears in the introspected `production_orders` table, OR
       - Connect via `psql "$DATABASE_URL_UNPOOLED"` and run `\d production_orders` — confirm `early_delivery_date | date` row present.
    5. Run `npm run db:seed` is NOT required at this stage — Task 3 handles seed data.

    If the executor environment lacks `DATABASE_URL_UNPOOLED`: pause and surface the env-var requirement to the operator. The operator runs the two commands locally and signals completion.

    If `drizzle-kit migrate` fails because the dev DB is in an unexpected state (e.g., a previous failed migration left it dirty), the operator inspects via `psql` and either rolls back manually or notifies — DO NOT use `drizzle-kit push` to force the schema in. Push is banned (STATE.md / Phase 32 D-06).
  </how-to-verify>
  <resume-signal>
    Type "migration applied" once `npx drizzle-kit migrate` has succeeded AND the dev Neon DB introspect confirms `early_delivery_date date` is present on `production_orders`. If migration generation produced unexpected SQL, describe the discrepancy and pause before applying.
  </resume-signal>
  <action>
    Sequence the executor (or operator) follows:
    1. Run `npx drizzle-kit generate` from the repo root. Expected output: a new file `drizzle/0001_<snake_case_slug>.sql` containing exactly the statement `ALTER TABLE "production_orders" ADD COLUMN "early_delivery_date" date;`. Also confirm `drizzle/meta/_journal.json` is updated with a second tag entry and a new `drizzle/meta/0001_snapshot.json` is written.
    2. Inspect the generated SQL. If drizzle-kit produced ANY statement beyond the single ADD COLUMN above (e.g., it tries to drop indices, recreate enums, change column types), STOP — Task 1 has a schema drift relative to the live DB. Report the diff to the operator before applying.
    3. Run `npx drizzle-kit migrate` to apply against the dev Neon DB. Expected: command exits 0 with a log line like `[✓] Migrations applied successfully!`. The `drizzle_migrations` table in the dev DB now records the `0001_*` entry.
    4. Verify the column exists in the live DB via either `npx drizzle-kit introspect` (the `production_orders` table introspect output includes `early_delivery_date`) OR `psql "$DATABASE_URL_UNPOOLED" -c "\d production_orders"` showing the `early_delivery_date | date` row.
    5. Do NOT run `npm run db:seed` here — Task 3 handles seed data.
    6. If `DATABASE_URL_UNPOOLED` is not set in the executor environment, pause and surface the env-var requirement to the operator; the operator runs steps 1 + 3 locally and signals completion via the resume-signal.
    7. If `drizzle-kit migrate` fails because the dev DB is in an unexpected state (e.g., a prior failed migration left it dirty), the operator inspects via psql and either rolls back manually or notifies. Do NOT use `drizzle-kit push` to force the schema — push is banned (STATE.md / Phase 32 D-06).
  </action>
  <verify>
    <automated>ls drizzle/0001_*.sql 2>/dev/null | head -1 && grep -c '"tag"' drizzle/meta/_journal.json</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `ls drizzle/0001_*.sql` returns exactly one new file.
    - source-assertion: `grep -c "ALTER TABLE.*production_orders.*ADD COLUMN.*early_delivery_date" drizzle/0001_*.sql` returns ≥ 1 (the expected ALTER statement).
    - source-assertion: `grep -c '"tag"' drizzle/meta/_journal.json` returns exactly 2 (two entries: 0000 and 0001).
    - source-assertion: `ls drizzle/meta/0001_snapshot.json` returns the file (snapshot persisted).
    - CLI output: the executor (or operator) reports `[✓] Migrations applied successfully!` from `npx drizzle-kit migrate`.
    - CLI output (post-apply): `npx drizzle-kit introspect` shows `early_delivery_date date` on `production_orders` — OR the operator confirms via `\d production_orders` in psql.
    - Negative: `grep -c "DROP" drizzle/0001_*.sql` returns 0 (migration is purely additive; no destructive operations on existing data).
  </acceptance_criteria>
  <done>
    `drizzle/0001_*.sql`, `drizzle/meta/_journal.json`, and `drizzle/meta/0001_snapshot.json` committed to git. Live dev Neon DB has the new column verified by introspection. The phase can now safely run KPI-08 queries against the live DB.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill synthetic earlyDeliveryDate for 33 seeded rows via seed.ts runtime computation (Option B — D-06)</name>
  <files>
    src/db/seed.ts
  </files>
  <read_first>
    - src/db/seed.ts (current shape lines 93-140 — SnakeRow type + .map() transform that runs at seed time)
    - src/db/seed-data.json (33-row source-of-truth; remains UNCHANGED — Option B is runtime computation, NOT static JSON edit)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-PATTERNS.md § "src/db/seed.ts + src/db/seed-data.json (EDIT — backfill `earlyDeliveryDate`)"
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-RESEARCH.md § "Pattern 8: Seed Backfill for `early_delivery_date`" — Option B rationale + exact formula
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md D-06 (deterministic spread today ±5 days via i % 11 - 5)
    - .planning/phases/32-schema-migrations-and-seed-data/32-06-PLAN.md (Phase 32's seed script discipline — TRUNCATE protection for `users` must be preserved)
  </read_first>
  <action>
    Modify `src/db/seed.ts` to inject a computed `earlyDeliveryDate` value into each of the 33 rows during the `.map()` transform (lines 128-140 region).

    Step 1: Add a top-level helper (defined OUTSIDE the seed function, near the SnakeRow type) per RESEARCH.md Pattern 8:
    - Compute `const today = new Date();` once at module load (or once inside the seed function before the map — either is fine, but capture once so all 33 rows reference the same baseline date and the spread is internally consistent).
    - Define `function earlyDeliveryDateFor(i: number): string` that returns a `YYYY-MM-DD` string computed as `today + ((i % 11) - 5) days`. Use `setUTCDate` to avoid local-tz off-by-one drift; format via `getUTCFullYear()` + zero-padded `getUTCMonth() + 1` + zero-padded `getUTCDate()`.

    Step 2: Inside the existing `.map((r, i) => ({ ... }))` transform, add `earlyDeliveryDate: earlyDeliveryDateFor(i),` as the last line of the returned object literal (before the closing brace). The position relative to other fields does not matter to Drizzle, but place it after `version: r.version,` for readability matching the schema order.

    Step 3: Do NOT modify the SnakeRow type. The earlyDeliveryDate is injected at runtime, not read from JSON — adding it to SnakeRow would require editing `seed-data.json` (which Option B explicitly avoids). Phase 35 RESEARCH.md Pattern 8 calls this out: "`seed-data.json` may not need to change (Option B)".

    Step 4: Do NOT touch the TRUNCATE block. The script currently truncates `production_orders`, `order_events`, `import_batches` (in dependency-safe order) — Phase 32 D-07 explicitly forbids truncating `users`. Verify via `grep -n "truncate" src/db/seed.ts` that `users` is NOT in the TRUNCATE list.

    Step 5: Manually verify the date spread is correct. With 33 rows and `i % 11`, the modulo cycles `0..10` three times. Subtracting 5 yields offsets `-5..+5` deterministically. Expected: rows with `i ∈ {0..4, 11..15, 22..26}` are overdue (`< today`), rows with `i ∈ {5, 16, 27}` are exactly today, rows with `i ∈ {6..10, 17..21, 28..32}` are upcoming. This gives KPI-08 visible overdue data on first seed.

    Step 6: Run `npm run db:seed` to apply the backfill against the dev Neon DB (after Task 2's migration has been applied — the column must exist or the insert will fail with a Postgres "column does not exist" error). Verify via `psql "$DATABASE_URL_UNPOOLED"`:
      `SELECT order_number, early_delivery_date FROM production_orders ORDER BY order_number LIMIT 5;`
      All 5 rows show a non-null `early_delivery_date` value. Some are in the past, some today, some in the future.

    Step 7: Do NOT add a Jest test for the date formula here — `seed.ts` is a runtime script, not an exported helper. A future refactor that extracts `earlyDeliveryDateFor` to `src/lib/seed-helpers.ts` could add a unit test, but that is out of scope for Phase 35.
  </action>
  <verify>
    <automated>grep -c "earlyDeliveryDate" src/db/seed.ts | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -n "earlyDeliveryDateFor" src/db/seed.ts` returns ≥ 2 matches (helper definition + call site).
    - source-assertion: `grep -n "i % 11" src/db/seed.ts` returns exactly 1 match (the deterministic spread formula from D-06).
    - source-assertion: `grep -n "setUTCDate" src/db/seed.ts` returns exactly 1 match (avoids local-tz off-by-one).
    - source-assertion: `grep -c "TRUNCATE.*users" src/db/seed.ts` returns 0 (Phase 32 D-07 preserved).
    - source-assertion: `grep -i "truncate" src/db/seed.ts` shows `production_orders`, `order_events`, `import_batches` but NOT `users` — read each truncate line and confirm.
    - source-assertion: `git diff src/db/seed-data.json` shows NO changes (Option B: JSON is untouched).
    - CLI output (manual, post-apply): `psql "$DATABASE_URL_UNPOOLED" -c "SELECT COUNT(*) FROM production_orders WHERE early_delivery_date IS NULL"` returns `0` after re-seed (all 33 rows have a value).
    - CLI output (manual, post-apply): `psql "$DATABASE_URL_UNPOOLED" -c "SELECT COUNT(*) FROM production_orders WHERE early_delivery_date < CURRENT_DATE"` returns ≥ 5 (at least the first batch of overdue rows is visible).
  </acceptance_criteria>
  <done>
    `seed.ts` injects `earlyDeliveryDate` into all 33 rows using the locked `(i % 11) - 5` formula; `seed-data.json` is unchanged; dev Neon DB has 33 rows with non-null `early_delivery_date` spanning today ±5 days; Phase 32 TRUNCATE protection for `users` is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle TS schema → live Neon DB | `drizzle-kit migrate` is the canonical bridge. Push is banned (Phase 32 D-06). |
| `seed.ts` runtime → production_orders table | TRUNCATE + INSERT path; protected from `users` table by explicit allow-list (Phase 32 D-07). |
| Test fixtures → ProductionOrder type | `$inferSelect` is the type contract; missing fields surface as `tsc --noEmit` errors (Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | T — Tampering | `drizzle-kit generate` output (the new 0001_*.sql migration file) | mitigate | Task 2 `how-to-verify` step 2 inspects the generated SQL before applying; any drift beyond the expected `ADD COLUMN` is surfaced as a stop condition. Migration file is committed to git so future runs are deterministic. |
| T-35-01-02 | T — Tampering | seed.ts injecting `earlyDeliveryDate` | accept | Dates are computed from `new Date()` and a deterministic modulo formula. No operator-controlled input flows into the computed value. The seed script requires `DATABASE_URL_UNPOOLED` env var, which is operator-owned. |
| T-35-01-03 | D — Denial of Service | ALTER TABLE on live DB during migration | accept | ADD COLUMN NULL on a 33-row dev DB completes in milliseconds. No production DB exists yet (Phase 31 footnote: Vercel env-var provisioning deferred). No DOS surface. |
| T-35-01-04 | I — Information Disclosure | earlyDeliveryDate visible in /demo or /production order views | accept | The date is operator-internal scheduling metadata, not PII. Phase 35 surfaces it only on the dashboard (auth-only per Phase 31 D-02). |
| T-35-01-05 | E — Elevation of Privilege | Migration apply via drizzle-kit migrate | accept | drizzle-kit operates with the DATABASE_URL_UNPOOLED credential set by the operator. No new privilege paths introduced. The credential boundary is unchanged from Phase 32. |
| T-35-01-06 | R — Repudiation | drizzle journal mutation | mitigate | `drizzle/meta/_journal.json` is committed to git after Task 2. Any future migration that mutates the journal without a corresponding `0002_*.sql` file is detectable in PR diff review. |
| T-35-01-SC | T — Tampering (supply chain) | npm/pip/cargo installs | n/a | No new package installs in this plan. Phase 35 has zero new dependencies per CONTEXT.md D-13. RESEARCH.md Package Legitimacy Audit explicitly marks this section "Not applicable." |
</threat_model>

<verification>
- `npm test` — full Jest suite green (schema test + all 5 fixture-impacted test files).
- `npx tsc --noEmit` — TypeScript clean across `src/` (no `Property 'earlyDeliveryDate' is missing` errors).
- `ls drizzle/0001_*.sql` returns one file (Task 2 artifact committed).
- `grep -c '"tag"' drizzle/meta/_journal.json` returns exactly 2 (one for 0000, one for 0001).
- `grep -c "earlyDeliveryDate" src/db/seed.ts` returns ≥ 2 (helper + map injection).
- Manual / operator: `psql "$DATABASE_URL_UNPOOLED" -c "\d production_orders"` shows the `early_delivery_date | date` row.
- Manual / operator: after `npm run db:seed`, `SELECT COUNT(*) FROM production_orders WHERE early_delivery_date IS NULL` returns `0`.
</verification>

<success_criteria>
- D-04 implemented: `production_orders.early_delivery_date` is a nullable `date` column in both the Drizzle schema AND the live dev Neon DB.
- D-06 implemented: 33 seed rows have deterministically computed `earlyDeliveryDate` values spanning today ±5 days, surfacing visible overdue data for KPI-08 in dev/demo environments.
- Pitfall 3 (RESEARCH.md) mitigated: all 6 `makeOrder` fixture factories propagate `earlyDeliveryDate: null` — full Jest suite + `tsc --noEmit` are both green project-wide.
- BLOCKING migration gate satisfied: `drizzle-kit generate` produced the expected ALTER TABLE migration AND `drizzle-kit migrate` applied it to the dev Neon DB. TypeScript-only verification (which would falsely pass) is supplemented by live-DB introspection.
- Phase 32 D-07 preserved: `seed.ts` does not truncate `users`; only `production_orders`, `order_events`, `import_batches`.
- Migration discipline preserved: `drizzle-kit push` was NOT used at any point.
</success_criteria>

<output>
After completion, create `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-01-SUMMARY.md` summarizing:
- Schema change diff (exact lines added in `src/db/schema/orders.ts`).
- Generated migration filename (`0001_<slug>.sql`) and its single ALTER statement.
- Confirmation that `drizzle-kit migrate` succeeded against the dev Neon DB (with operator's verification message captured).
- The deterministic `earlyDeliveryDateFor` formula committed in `seed.ts`, plus an example of expected output for a representative i value.
- The 6 fixture files updated (one line each: `earlyDeliveryDate: null,`).
- Verification: `SELECT COUNT(*) FROM production_orders WHERE early_delivery_date IS NULL` returns 0 after re-seed.
- Any deviations from PATTERNS.md or RESEARCH.md (expected: none).
</output>
