---
phase: 31-role-expansion-and-db-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/global.setup.ts
  - e2e/mill-operator-smoke.spec.ts
  - docs/clerk-setup.md
autonomous: true
requirements: [AUTH-04]
tags: [playwright, e2e, clerk, runbook, docs]

must_haves:
  truths:
    - "Playwright has an `auth-mill-operator` project paralleling `demo-user` / `norole-user` (D-14)"
    - "e2e/global.setup.ts roles record contains a 'mill-operator' entry that signs in via E2E_MILL_OPERATOR_USER_{EMAIL,PASSWORD} and writes playwright/.clerk/mill-operator.json"
    - "e2e/mill-operator-smoke.spec.ts asserts data-mode='edit' on the <MillReadOnlyStub> indicator when signed in as the mill-operator user (per RESEARCH Open Question 2 recommendation)"
    - "docs/clerk-setup.md Step 2 table has a new row for e2e-mill-operator+clerk_test@example.com with roles ['mill_operator'] (D-12)"
    - "docs/clerk-setup.md Step 3 documents the dual-role JSON for the existing e2e-demo user with roles ['demo','mill_operator'] (D-13)"
    - "docs/clerk-setup.md Step 4/Verification reiterates Pitfall 6 sign-out/sign-in propagation for the demo user metadata change"
  artifacts:
    - path: "playwright.config.ts"
      provides: "auth-mill-operator Playwright project entry"
      contains: "auth-mill-operator"
    - path: "e2e/global.setup.ts"
      provides: "mill-operator role added to roles record; storage state generated"
      contains: "'mill-operator'"
    - path: "e2e/mill-operator-smoke.spec.ts"
      provides: "E2E smoke asserting data-mode='edit' for mill_operator on /"
      contains: "data-mode"
    - path: "docs/clerk-setup.md"
      provides: "Updated runbook with mill_operator user row + dual-role demo example + propagation reminder"
      contains: "e2e-mill-operator+clerk_test"
  key_links:
    - from: "playwright.config.ts (auth-mill-operator project)"
      to: "playwright/.clerk/mill-operator.json"
      via: "storageState"
      pattern: "storageState: 'playwright/.clerk/mill-operator.json'"
    - from: "e2e/global.setup.ts (mill-operator entry)"
      to: "E2E_MILL_OPERATOR_USER_EMAIL / E2E_MILL_OPERATOR_USER_PASSWORD env vars"
      via: "process.env lookup + clerk.signIn"
      pattern: "E2E_MILL_OPERATOR_USER_EMAIL"
    - from: "e2e/mill-operator-smoke.spec.ts"
      to: "[data-testid='mill-mode'][data-mode='edit'] in <MillReadOnlyStub>"
      via: "expect(locator).toHaveAttribute"
      pattern: "data-mode.*edit"
---

<objective>
Stand up the test-infrastructure and operator-runbook scaffolding for the new `mill_operator` role: add the `auth-mill-operator` Playwright project (D-14), extend `e2e/global.setup.ts` to authenticate the new user fixture, write a minimal E2E smoke spec (per RESEARCH Open Question 2's recommendation — keeps Phase 31's E2E proof point tight and cheap), and update `docs/clerk-setup.md` with the new test user row (D-12), the dual-role demo example (D-13), and the sign-out/sign-in propagation reminder (RESEARCH Pitfall 6).

Purpose: Operationalize D-12 / D-13 / D-14 / AUTH-04. The Clerk Dashboard cutover itself is operator-only (Plan 31-05), but everything in this plan is fully autonomous and lands the durable code + docs artifacts. The E2E spec is initially un-runnable end-to-end until Plan 31-05 (which creates the Clerk users) and Plan 31-04 (which lands the `<MillReadOnlyStub>` component the spec targets) ship — that's fine. The spec compiles, lints, and lives in the right project; the dynamic execution happens at the Plan 31-05 verification gate.

Output: Three config/test files modified plus one new spec, one runbook updated. No package additions, no source-code changes. Independent of Plan 31-01 (no shared files) and Plan 31-02 (no shared files — note that .env.example keys for E2E_MILL_OPERATOR_* are owned by Plan 31-02, this plan only consumes them in global.setup.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md
@playwright.config.ts
@e2e/global.setup.ts
@e2e/demo-route-protection.spec.ts
@docs/clerk-setup.md

<interfaces>
<!-- Key contracts and shapes the executor needs. -->

Existing playwright.config.ts project pattern (lines 36-54 — demo-user and norole-user) shows: each project has name, testMatch regex, use block with devices spread + storageState path + baseURL, and a `dependencies: ['global setup']` array. Trailing comma after closing brace.

Existing e2e/global.setup.ts roles record (lines 36-52) is `Record<'demo' | 'norole' | 'admin', RoleFixture>` with three entries (demo, norole, admin), each providing envEmail / envPassword / file paths. The for-loop at line 54 (`for (const [role, cfg] of Object.entries(roles))`) auto-picks-up new entries; no loop changes needed.

Existing demo-route-protection.spec.ts test shape (lines 30-54) uses role-asymmetric `test.skip(testInfo.project.name !== 'norole-user', ...)` guards. Our new spec does NOT need this skip pattern because the new Playwright project's testMatch regex scopes the file to one project only.

Target <MillReadOnlyStub> markers (from Plan 31-04 — RESEARCH §"Code Examples" lines 380-385):
- `data-testid="mill-stub"` on the outer wrapper div
- `data-testid="mill-mode"` on the mode-indicator <p>
- `data-mode="edit"` when canEdit=true; `data-mode="read-only"` when canEdit=false

docs/clerk-setup.md Step 2 table header + row format (lines 33-37) is a 3-column markdown table: Email | Password | publicMetadata.roles. Existing demo row reads `["demo"]`.

docs/clerk-setup.md Step 3 metadata JSON example format (lines 49-57) uses fenced ```json code blocks containing the literal `{"roles": [...]}` shape.

D-12 user details: email `e2e-mill-operator+clerk_test@example.com`, password env var `E2E_MILL_OPERATOR_USER_PASSWORD`, publicMetadata.roles `["mill_operator"]`.

D-13 update: existing `e2e-demo+clerk_test@example.com` user's publicMetadata.roles changes from `["demo"]` to `["demo", "mill_operator"]`.

D-14 Playwright project: `auth-mill-operator` (note: project-name uses kebab-case `auth-mill-operator`; role string uses snake_case `mill_operator`; storage file uses kebab-case `mill-operator.json`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add auth-mill-operator Playwright project + extend global.setup.ts roles record</name>
  <files>playwright.config.ts, e2e/global.setup.ts</files>
  <read_first>
    - playwright.config.ts (existing project entries lines 36-54 — copy the demo-user shape exactly)
    - e2e/global.setup.ts (existing roles record lines 36-52 and the iteration loop at line 54)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`playwright.config.ts`" and §"`e2e/global.setup.ts`" — exact additions)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 642-665)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (D-14)
  </read_first>
  <action>
    Step 1: Edit playwright.config.ts. Append a new project entry to the `projects: [...]` array (after the existing `norole-user` entry at line 54). The new entry's name is `auth-mill-operator` per D-14. Fields: name = `'auth-mill-operator'`; testMatch = `/mill-operator-smoke\.spec\.ts$/` (points at the new spec created in Task 2 — do NOT reuse the demo-route-protection spec because its role-asymmetric assertions are not meaningful for a mill_operator-only user); use = `{ ...devices['Desktop Chrome'], storageState: 'playwright/.clerk/mill-operator.json', baseURL: 'http://localhost:3000' }`; dependencies = `['global setup']`. Match the indentation and trailing-comma style of the existing entries.

    Step 2: Edit e2e/global.setup.ts. Widen the roles record TypeScript type from `Record<'demo' | 'norole' | 'admin', RoleFixture>` to `Record<'demo' | 'norole' | 'admin' | 'mill-operator', RoleFixture>` at line 36. Note: the record key uses kebab-case `'mill-operator'` (hyphenated) to match the file naming convention (`playwright/.clerk/mill-operator.json`), distinct from the `mill_operator` (snake_case) role string used in Clerk publicMetadata. This is intentional — the kebab-case is a file-system identifier; the snake_case is a role-string identifier.

    Step 3: Add a new entry to the roles record (after the existing admin entry at line 51): key `'mill-operator'`, value `{ envEmail: 'E2E_MILL_OPERATOR_USER_EMAIL', envPassword: 'E2E_MILL_OPERATOR_USER_PASSWORD', file: 'playwright/.clerk/mill-operator.json' }`. The `Object.entries(roles)` for-loop at line 54 auto-picks-up the new entry — do NOT modify the loop body.

    Step 4: Run `npx tsc --noEmit` to confirm no type errors. The test will not run end-to-end until Plan 31-05 creates the Clerk user and populates .env.local; for now, the goal is compile-clean config.

    Commit: `feat(31-03): add auth-mill-operator Playwright project + global setup entry`.
  </action>
  <verify>
    <automated>grep -c "auth-mill-operator" playwright.config.ts && grep -c "'mill-operator':" e2e/global.setup.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "name: 'auth-mill-operator'" playwright.config.ts` returns exactly 1 line
    - `grep -E "storageState: 'playwright/\.clerk/mill-operator\.json'" playwright.config.ts` returns exactly 1 line
    - `grep -E "testMatch: /mill-operator-smoke\\\\.spec\\\\.ts\\$/" playwright.config.ts` returns exactly 1 line (new spec wired in)
    - The pre-existing project names (`chromium`, `global setup`, `demo-user`, `norole-user`) all still appear (`grep -c "name: 'demo-user'" playwright.config.ts` returns 1; same for the other three)
    - `grep -E "'mill-operator': \{" e2e/global.setup.ts` returns exactly 1 line
    - `grep -E "envEmail: 'E2E_MILL_OPERATOR_USER_EMAIL'" e2e/global.setup.ts` returns exactly 1 line
    - `grep -E "envPassword: 'E2E_MILL_OPERATOR_USER_PASSWORD'" e2e/global.setup.ts` returns exactly 1 line
    - `grep -E "file: 'playwright/\.clerk/mill-operator\.json'" e2e/global.setup.ts` returns exactly 1 line
    - The Record type widens: `grep -E "Record<'demo' \| 'norole' \| 'admin' \| 'mill-operator', RoleFixture>" e2e/global.setup.ts` returns exactly 1 line
    - The pre-existing demo, norole, admin record entries are unchanged (`grep -c "envEmail: 'E2E_DEMO_USER_EMAIL'" e2e/global.setup.ts` returns 1; same for NOROLE / ADMIN)
    - The for-loop at line ~54 is unchanged (`grep -E "for \(const \[role, cfg\] of Object.entries\(roles\)\)" e2e/global.setup.ts` still returns 1)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Playwright + global setup pick up the new role; tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write e2e/mill-operator-smoke.spec.ts asserting data-mode='edit' on /</name>
  <files>e2e/mill-operator-smoke.spec.ts</files>
  <read_first>
    - e2e/demo-route-protection.spec.ts (lines 1-64 — canonical Playwright spec shape: `test.describe`, role-asymmetric `test.skip` guards, `expect(page).toHaveURL`)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Open Questions" #2 — recommendation to add this spec; §"Code Examples" lines 369-393 — `<MillReadOnlyStub>` markers; §"Validation Architecture" Wave 0 Gaps lines 803-805 — this spec is listed as recommended Wave 0)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (D-12 — mill_operator-only user; the spec asserts edit mode)
  </read_first>
  <action>
    Step 1: Create e2e/mill-operator-smoke.spec.ts. Use the demo-route-protection.spec.ts file structure as the analog: one `test.describe('Mill operator role — edit mode smoke', ...)` block. Inside it, ONE test named "mill-operator user sees edit mode on /". The test body: (1) `await page.goto('/')`; (2) `await expect(page).toHaveURL('/')` — asserts URL stays at `/` because `/` is open to any authenticated user per D-01 / D-05; (3) locate the mode indicator via `page.getByTestId('mill-mode')`; (4) assert `data-mode="edit"` via `await expect(mode).toHaveAttribute('data-mode', 'edit')`; (5) assert visible text contains "Edit mode" via `await expect(mode).toContainText('Edit mode')`.

    Step 2: This spec is intentionally minimal — its job is to prove (a) the mill-operator storageState authenticates the user without redirect, (b) the page-level `await checkRole('mill_operator')` returns true for this user, (c) the `<MillReadOnlyStub>` renders `data-mode="edit"` as designed. The single-test scope keeps the smoke fast (< 5s once Clerk is ready).

    Step 3: Do NOT add tests for read-only mode in this spec — those branches are covered by Plan 31-04's Jest unit tests on `src/app/page.test.tsx`. The E2E layer's job is the integration proof (Clerk JWT → checkRole → rendered data-mode); detailed branch coverage at the unit layer is cheaper and faster.

    Step 4: Add a top-of-file comment block explaining: (a) this spec runs ONLY under the `auth-mill-operator` project (no `test.skip(testInfo.project.name !== ...)` needed since the project's `testMatch` already scopes the file); (b) the spec is a Phase 31 smoke proof that D-12 (new user) + D-14 (Playwright project) wired correctly; (c) Phase 32+ will broaden coverage as transitions land.

    Step 5: Run `npx tsc --noEmit` to confirm type-cleanliness. Do NOT run the spec end-to-end — it depends on the new Clerk user (Plan 31-05) and the new `<MillReadOnlyStub>` (Plan 31-04). Compile-clean is enough for Wave 1.

    Commit: `test(31-03): add mill-operator E2E smoke spec`.
  </action>
  <verify>
    <automated>test -f e2e/mill-operator-smoke.spec.ts && grep -c "data-mode.*edit" e2e/mill-operator-smoke.spec.ts && grep -c "page.goto" e2e/mill-operator-smoke.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls e2e/mill-operator-smoke.spec.ts` succeeds (file exists)
    - `grep -c "@playwright/test" e2e/mill-operator-smoke.spec.ts` returns at least 1 (Playwright imports present)
    - `grep -c "test.describe" e2e/mill-operator-smoke.spec.ts` returns at least 1
    - `grep -E "page\\.goto\\('/'\\)" e2e/mill-operator-smoke.spec.ts` returns at least 1 line (navigates to root)
    - `grep -E "expect\\(page\\)\\.toHaveURL\\('/'\\)" e2e/mill-operator-smoke.spec.ts` returns at least 1 line (no-redirect assertion)
    - `grep -E "getByTestId\\('mill-mode'\\)" e2e/mill-operator-smoke.spec.ts` returns at least 1 line
    - `grep -E "toHaveAttribute\\('data-mode', 'edit'\\)" e2e/mill-operator-smoke.spec.ts` returns at least 1 line
    - `grep -E "toContainText\\('Edit mode'\\)" e2e/mill-operator-smoke.spec.ts` returns at least 1 line
    - `npx tsc --noEmit` exits 0
    - The spec file is discoverable: `npx playwright test --list 2>&1` includes `mill-operator-smoke.spec.ts` in its output (only fails if Clerk env vars are required at list-time, in which case treat as "verified in Plan 31-05")
  </acceptance_criteria>
  <done>
    Smoke spec compiles, lives under the correct project, asserts the integration proof point. E2E execution deferred to Plan 31-05.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update docs/clerk-setup.md with mill_operator user + dual-role demo + propagation reminder</name>
  <files>docs/clerk-setup.md</files>
  <read_first>
    - docs/clerk-setup.md (entire file — note Step 2 table at lines 33-37, Step 3 metadata blocks at lines 49-57, Step 4 propagation note at lines 61-65, Step 5 .env.local block at lines 67-78, Verification at lines 82-90)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`docs/clerk-setup.md`" — exact target diffs)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Pitfall 5" — `+clerk_test` suffix rationale; §"Pitfall 6" — sign-out/sign-in propagation; §"Code Examples" — runbook updates context)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Step 1: Update Step 2 table (currently lines 33-37). UPDATE the existing demo row: change the publicMetadata.roles cell from `["demo"]` to `["demo", "mill_operator"]` (per D-13). APPEND a new row immediately after the admin row for the new mill_operator user (per D-12): email column = `e2e-mill-operator+clerk_test@example.com`, password column = "(set during creation, store in `.env.local` as `E2E_MILL_OPERATOR_USER_PASSWORD`)", roles column = `["mill_operator"]`. Match the existing pipe-delimited markdown table formatting.

    Step 2: Update Step 3 (currently lines 41-59). The existing Step 3 shows two single-role JSON examples: `{"roles": ["demo"]}` and `{"roles": ["admin"]}`. REPLACE the demo JSON block with `{"roles": ["demo", "mill_operator"]}` (per D-13). ADD a third labelled JSON block for the new mill-operator user showing `{"roles": ["mill_operator"]}`. Preserve the existing admin JSON block. Update the surrounding prose to explicitly mention "the demo user gets BOTH `demo` AND `mill_operator` roles per Phase 31 D-13" and add a sentence introducing the mill-operator-only user.

    Step 3: Update Step 4 (currently lines 61-65). Add an explicit reminder line tied to D-13: "Because Step 3 updates the EXISTING `e2e-demo` user's publicMetadata, any active dev session for that user must sign out and sign back in to refresh the JWT — otherwise `checkRole('mill_operator')` returns false against a stale token and the user sees `Read-only mode` on `/` instead of `Edit mode`." Cross-reference RESEARCH Pitfall 6.

    Step 4: Update Step 5 (currently lines 67-78). Extend the `.env.local` example bash block to include the two new mill-operator keys: `E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator+clerk_test@example.com` and `E2E_MILL_OPERATOR_USER_PASSWORD=<the password you set for the mill_operator user>`.

    Step 5: Update Verification section (currently lines 82-90). Add a new step #4: "Sign in as `e2e-mill-operator+clerk_test@example.com`, navigate to `/`. Expect the page to render with the 'Edit mode (mill_operator)' indicator. Decode the `__session` cookie at jwt.io and confirm `metadata.roles: ['mill_operator']` is present." Add step #5: "Sign in as the updated `e2e-demo+clerk_test@example.com` (after dual-role assignment per Step 3), navigate to `/`. Expect the 'Edit mode (mill_operator)' indicator (because the demo user now has BOTH `demo` and `mill_operator`). Decode the JWT and confirm `metadata.roles: ['demo', 'mill_operator']` is present — this is the canonical multi-role coverage path per D-13."

    Step 6: Update the file's opening title/heading. The current title is "Clerk Dashboard Setup — Phase 27". Add a note paragraph just below the title that Phase 31 extends this runbook with the new mill_operator user. Do NOT rename the file. Do NOT remove any pre-existing content. The Phase 27 norole user, JWT template, etc. remain canonical.

    Commit: `docs(31-03): update clerk-setup runbook with mill_operator user + dual-role demo`.
  </action>
  <verify>
    <automated>grep -c "e2e-mill-operator+clerk_test@example.com" docs/clerk-setup.md && grep -cE 'roles.*mill_operator' docs/clerk-setup.md && grep -c "E2E_MILL_OPERATOR_USER_EMAIL" docs/clerk-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e2e-mill-operator+clerk_test@example.com" docs/clerk-setup.md` returns at least 2 (Step 2 table row + Step 5 .env.local block + Verification step references)
    - `grep -E '\["mill_operator"\]' docs/clerk-setup.md` returns at least 2 (Step 2 table cell + Step 3 JSON block)
    - `grep -E '\["demo", "mill_operator"\]' docs/clerk-setup.md` returns at least 2 (Step 2 updated demo row + Step 3 dual-role JSON block + Verification step #5 reference)
    - `grep -c "^\| .e2e-demo+clerk_test@example.com." docs/clerk-setup.md` returns 1 (existing row preserved — line still exists, just modified)
    - `grep -c '\["demo"\]' docs/clerk-setup.md` returns 0 (old single-role demo JSON `["demo"]` has been REPLACED with dual-role form)
    - `grep -c "E2E_MILL_OPERATOR_USER_EMAIL" docs/clerk-setup.md` returns at least 2 (Step 2 password column reference + Step 5 .env.local block)
    - `grep -c "E2E_MILL_OPERATOR_USER_PASSWORD" docs/clerk-setup.md` returns at least 2
    - `grep -iE "sign out|sign-out" docs/clerk-setup.md` returns multiple lines (the propagation reminder is added)
    - `grep -c "Phase 31" docs/clerk-setup.md` returns at least 1 (note added under title or in updated sections)
    - The existing JWT template JSON `{"metadata": {"roles": "{{user.public_metadata.roles}}"}}` is unchanged (`grep -c 'user.public_metadata.roles' docs/clerk-setup.md` returns 1)
    - The existing admin user row and admin JSON block are preserved (`grep -c '\["admin"\]' docs/clerk-setup.md` returns at least 2)
    - The existing norole user row is preserved (`grep -c "e2e-norole+clerk_test" docs/clerk-setup.md` returns at least 1)
  </acceptance_criteria>
  <done>
    Runbook updated; the operator has explicit, step-by-step guidance for D-12 + D-13 dashboard work; propagation reminder is durable in committed text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk Dashboard (external) ↔ Repo runbook | The runbook documents the operator's manual cutover steps; deviation produces stale JWTs or missing claims |
| Test user identities ↔ Production users | Test users carry the `+clerk_test` suffix which Clerk treats as a safe mailbox; they are isolated from production user space |
| Playwright storage state ↔ committed repo | `playwright/.clerk/*.json` files are gitignored (existing); only the test config + spec files are committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Spoofing | Test user identity collisions if `+clerk_test` suffix is dropped | mitigate | The runbook update explicitly preserves the `+clerk_test` suffix on the new user (D-12 email is verbatim). `grep -c "e2e-mill-operator+clerk_test@example.com"` >= 2 in Task 3 acceptance criteria locks this. Severity: low (dev-instance only). Mitigation reference: docs/clerk-setup.md updated Step 2 row. |
| T-31-03-02 | Information Disclosure | Real Clerk credentials accidentally committed to playwright.config.ts or e2e/mill-operator-smoke.spec.ts | mitigate | All credentials flow via `process.env.E2E_MILL_OPERATOR_USER_*` — never literals. `grep -c "E2E_MILL_OPERATOR" playwright.config.ts` is 0 (correct — env-var lookup happens in global.setup.ts only); spec file uses storageState (no credentials in spec). Severity: high (if exploited). Mitigation reference: existing `playwright/.clerk/` .gitignore + env-var indirection. |
| T-31-03-03 | Repudiation | Operator forgets to sign out/sign back in after updating demo user's metadata; stale JWT presents as a regression | mitigate | The propagation reminder is added in Task 3 Step 3 with explicit symptom description ("user sees `Read-only mode` instead of `Edit mode`"). RESEARCH Pitfall 6 documented; Verification step #5 in updated Step 5 confirms the JWT carries the new claim. Severity: medium (testing-flow risk). Mitigation reference: docs/clerk-setup.md updated Step 4 + Verification step #5. |
| T-31-03-04 | Denial of Service | E2E spec fails to execute because the spec depends on Plan 31-04's <MillReadOnlyStub> and Plan 31-05's Clerk user | accept | Acknowledged in the plan objective. The spec is compile-clean in Wave 1; end-to-end execution is the verification gate in Plan 31-05. No mitigation needed beyond clear sequencing. Severity: low (process risk, not security risk). |
</threat_model>

<verification>
After all three tasks complete:
- `npx tsc --noEmit` exits 0
- `grep -c "auth-mill-operator" playwright.config.ts` >= 1
- `grep -c "'mill-operator':" e2e/global.setup.ts` >= 1
- `ls e2e/mill-operator-smoke.spec.ts` succeeds
- `grep -c "e2e-mill-operator+clerk_test@example.com" docs/clerk-setup.md` >= 2
- `grep -c "Phase 31" docs/clerk-setup.md` >= 1
- All pre-existing Phase 27 content in docs/clerk-setup.md is preserved (admin row, norole row, JWT template block)

(Note: `npx playwright test --project=auth-mill-operator` end-to-end execution is verified in Plan 31-05 after the Clerk Dashboard cutover.)
</verification>

<success_criteria>
- Playwright `auth-mill-operator` project exists and points at the new smoke spec
- `e2e/global.setup.ts` roles record contains `'mill-operator'` entry; for-loop iterates it
- `e2e/mill-operator-smoke.spec.ts` exists, type-checks, asserts `data-mode="edit"` after navigation to `/`
- `docs/clerk-setup.md` Step 2 table contains the new mill_operator row; Step 3 contains both the dual-role demo JSON and the mill-operator-only JSON
- The sign-out/sign-in propagation reminder is durable in docs/clerk-setup.md
- All Phase 27 content (JWT template, admin/norole users, existing verification steps) is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/31-role-expansion-and-db-infrastructure/31-03-SUMMARY.md`.
</output>
