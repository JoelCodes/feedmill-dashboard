---
phase: 31-role-expansion-and-db-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - drizzle.config.ts
  - src/db/index.ts
  - src/db/schema.ts
  - src/db/__tests__/index.test.ts
  - .env.example
autonomous: true
requirements: [DATA-01, DATA-08]
tags: [drizzle, neon, db, server-only, infrastructure]

must_haves:
  truths:
    - "drizzle-orm, @neondatabase/serverless, drizzle-kit are installed at the versions pinned by RESEARCH.md"
    - "src/db/index.ts exists with `import 'server-only';` as line 1 (D-10) and exports `db`"
    - "src/db/schema.ts exists as a valid TS module (placeholder per D-09)"
    - "drizzle.config.ts exists at repo root, reads DATABASE_URL_UNPOOLED (D-08), and points at ./src/db/schema.ts (D-09)"
    - ".env.example contains placeholder entries for DATABASE_URL, DATABASE_URL_UNPOOLED, E2E_MILL_OPERATOR_USER_EMAIL, E2E_MILL_OPERATOR_USER_PASSWORD"
    - "A source-assertion test verifies line 1 of src/db/index.ts is exactly `import 'server-only';`"
  artifacts:
    - path: "src/db/index.ts"
      provides: "Drizzle/Neon HTTP client singleton (server-only)"
      contains: "import 'server-only';"
    - path: "src/db/schema.ts"
      provides: "Phase 31 placeholder schema module"
      contains: "export {}"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit CLI configuration (repo root)"
      contains: "DATABASE_URL_UNPOOLED"
    - path: "src/db/__tests__/index.test.ts"
      provides: "Source-string assertion that import 'server-only' is line 1"
      contains: "lines[0]"
    - path: ".env.example"
      provides: "Placeholder env keys for DB + mill_operator E2E user"
      contains: "DATABASE_URL_UNPOOLED="
    - path: "package.json"
      provides: "drizzle-orm + @neondatabase/serverless deps; drizzle-kit devDep"
      contains: "drizzle-orm"
  key_links:
    - from: "drizzle.config.ts"
      to: "process.env.DATABASE_URL_UNPOOLED"
      via: "dotenv-loaded .env.local + dbCredentials.url"
      pattern: "process\\.env\\.DATABASE_URL_UNPOOLED"
    - from: "src/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "neon() HTTP driver consuming pooled URL"
      pattern: "neon\\(process\\.env\\.DATABASE_URL\\)"
    - from: "src/db/__tests__/index.test.ts"
      to: "src/db/index.ts"
      via: "fs.readFile + line[0] source assertion"
      pattern: "import 'server-only';"
---

<objective>
Stand up the Drizzle/Neon HTTP infrastructure: install dependencies, create `src/db/index.ts` (server-only singleton with `import 'server-only';` as line 1 per D-10), create a placeholder `src/db/schema.ts` per D-09, create `drizzle.config.ts` at repo root pointing at the UNPOOLED Neon URL per D-08 + D-11, and extend `.env.example` with the four new keys. Add a source-assertion test that enforces line-1 placement of the server-only directive (D-10's load-bearing readability discipline).

Purpose: This plan establishes the persistence layer's compile-time scaffolding without writing any tables or queries. The verification end-state is a clean `next build` (executed in Plan 31-05) where `src/db/index.ts` compiles into the server bundle only and never leaks into the Edge or client bundle. Independent of Plan 31-01 (no shared files); independent of Plan 31-03 (no shared files). Wave 1.

Output: Three new files under `src/db/`, one new repo-root config file, two new packages installed, one new devDep, two new entries in `.env.example` (DB pair) plus two more for the E2E mill-operator user. Plan 31-03 owns the Playwright/global-setup additions; this plan owns only the DB-side env keys for `.env.example`. We add the mill-operator E2E keys here because they sit in the same `.env.example` file and combining them avoids a Wave 1 file conflict — see file_ownership note below.

NOTE on file ownership: This plan exclusively owns `.env.example`. Plan 31-03 (Playwright runbook) consumes the E2E_MILL_OPERATOR_* keys but does NOT modify `.env.example` itself — the keys already exist after this plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md
@package.json
@.env.example
@playwright.config.ts
@src/lib/auth.ts
@src/middleware.test.ts

<interfaces>
<!-- Key contracts and shapes the executor needs. -->

From package.json (current — relevant deps):
```json
"dependencies": {
  "@clerk/nextjs": "^7.3.3",
  "next": "16.1.6",
  "react": "19.2.3",
  ...
},
"devDependencies": {
  "dotenv": "^17.4.2",
  ...
}
```

Versions to add (pinned exact, no `^` per RESEARCH §"Standard Stack" lines 110-114):
- dependencies: `drizzle-orm@0.45.2`, `@neondatabase/serverless@1.1.0`
- devDependencies: `drizzle-kit@0.31.10`

Existing dotenv-at-repo-root pattern from playwright.config.ts lines 1-6:
```typescript
import { defineConfig, devices } from '@playwright/test';
import dotenv from 'dotenv';
import path from 'path';
dotenv.config({ path: path.resolve(__dirname, '.env.local') });
```

Target src/db/index.ts shape (from RESEARCH §"Code Examples" lines 593-607):
```typescript
import 'server-only';                              // LINE 1 — D-10
import { drizzle } from 'drizzle-orm/neon-http';
import { neon } from '@neondatabase/serverless';

if (!process.env.DATABASE_URL) {
  throw new Error(/* helpful message */);
}

const sql = neon(process.env.DATABASE_URL);
export const db = drizzle({ client: sql });
```

Target drizzle.config.ts shape (from RESEARCH §"Code Examples" lines 564-587):
```typescript
import { config } from 'dotenv';
import path from 'path';
import { defineConfig } from 'drizzle-kit';

config({ path: path.resolve(__dirname, '.env.local') });

if (!process.env.DATABASE_URL_UNPOOLED) {
  throw new Error(/* helpful message */);
}

export default defineConfig({
  schema: './src/db/schema.ts',     // D-09
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL_UNPOOLED,  // D-08
  },
});
```

Source-assertion test pattern (from existing src/middleware.test.ts lines 156-174):
```typescript
import { promises as fs } from 'fs';
import path from 'path';

it('line 1 is exactly import server-only', async () => {
  const content = await fs.readFile(path.resolve(__dirname, '../index.ts'), 'utf-8');
  const lines = content.split('\n');
  expect(lines[0]).toBe("import 'server-only';");
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Drizzle + Neon packages at pinned versions</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies and devDependencies)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Standard Stack" lines 105-144 — exact versions; §"Installation" lines 131-135 — install commands)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`package.json`" — only these 3 packages; no nuqs/zod/read-excel-file)
  </read_first>
  <action>
    Step 1: Run `npm view drizzle-orm version` and `npm view @neondatabase/serverless version` and `npm view drizzle-kit version` to confirm the pinned versions (drizzle-orm@0.45.2, @neondatabase/serverless@1.1.0, drizzle-kit@0.31.10) are still current. If newer patch versions exist (e.g., drizzle-orm@0.45.3), still install the RESEARCH-pinned exact version — do not float to latest within this plan.

    Step 2: Install runtime dependencies (exact versions, no `^` per RESEARCH line 114): `npm install drizzle-orm@0.45.2 @neondatabase/serverless@1.1.0 --save-exact`.

    Step 3: Install drizzle-kit devDep (exact version): `npm install -D drizzle-kit@0.31.10 --save-exact`.

    Step 4: Verify three new entries appear in package.json — `drizzle-orm` and `@neondatabase/serverless` in `dependencies`, `drizzle-kit` in `devDependencies`. Confirm package-lock.json is updated. Confirm `dotenv` (^17.4.2) is unchanged (existing devDep).

    Step 5: Do NOT install nuqs, zod, or read-excel-file (RESEARCH §"Anti-Patterns" line 422 — these belong to later phases). Do NOT add the `^` semver prefix to any of the three new entries (per RESEARCH line 114 — pin exact for Phase 31 baseline).

    Commit: `feat(31-02): install drizzle-orm + @neondatabase/serverless + drizzle-kit`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies['drizzle-orm']!=='0.45.2'||p.dependencies['@neondatabase/serverless']!=='1.1.0'||p.devDependencies['drizzle-kit']!=='0.31.10'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').dependencies['drizzle-orm'])"` prints exactly `0.45.2` (no `^` prefix)
    - `node -e "console.log(require('./package.json').dependencies['@neondatabase/serverless'])"` prints exactly `1.1.0`
    - `node -e "console.log(require('./package.json').devDependencies['drizzle-kit'])"` prints exactly `0.31.10`
    - `node -e "console.log(require('./package.json').dependencies['nuqs']||'absent')"` prints `absent` (not installed in Phase 31)
    - `node -e "console.log(require('./package.json').dependencies['zod']||'absent')"` prints `absent`
    - `node -e "console.log(require('./package.json').dependencies['read-excel-file']||'absent')"` prints `absent`
    - `ls node_modules/drizzle-orm/package.json node_modules/@neondatabase/serverless/package.json node_modules/drizzle-kit/package.json` shows all three exist
    - package-lock.json contains entries for all three new packages (verified via grep for the package names)
  </acceptance_criteria>
  <done>
    Three packages installed at exact pinned versions; lockfile updated; no unrelated package additions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/db/{index,schema}.ts with line-1 server-only enforcement (source-assertion TDD)</name>
  <files>src/db/index.ts, src/db/schema.ts, src/db/__tests__/index.test.ts</files>
  <read_first>
    - src/lib/auth.ts (existing server-only JSDoc form for tone/style — lines 1-12)
    - src/middleware.test.ts (existing source-string assertion pattern — read fs + assert content)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`src/db/index.ts`" — full target shape; §"`src/db/schema.ts`" — placeholder shape; §"Shared Patterns / Module-level test pattern for new tests under `src/db/`")
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 593-621; §"Pattern 1" lines 254-277; §"Pitfall 7" — verifies that `checkRole` exists in Plan 31-01 before this plan starts; this plan does NOT depend on Plan 31-01 since no file overlap, but the source-assertion approach is independent of role logic)
    - src/types/clerk.d.ts (existing `export {}` empty-module pattern at line 8 — copy this shape for src/db/schema.ts)
  </read_first>
  <behavior>
    RED: Write src/db/__tests__/index.test.ts FIRST. The test reads src/db/index.ts via `fs.readFile` and asserts (a) `lines[0] === "import 'server-only';"` exactly (no leading whitespace, no trailing semicolon variant), (b) the file contains `export const db`, (c) the file contains `from 'drizzle-orm/neon-http'`, (d) the file contains `from '@neondatabase/serverless'`, (e) negative test — the file does NOT contain any `'use client'` directive or any import from `next/navigation`/`react`. This test will FAIL with `ENOENT` because src/db/index.ts does not yet exist.
    GREEN: Create src/db/index.ts using the exact shape from RESEARCH §"Code Examples" lines 593-607. `import 'server-only';` is line 1. Create src/db/schema.ts containing only `export {};` and a comment block explaining the Phase 31 placeholder per D-09. Re-run the test; expect 5 assertions pass.
    REFACTOR: None — both files are minimal by design.
  </behavior>
  <action>
    Step 1 (RED — write test before source):
    Create directory `src/db/__tests__/` (use `mkdir -p` or rely on the editor creating it). Create `src/db/__tests__/index.test.ts` with a Jest test suite `describe('src/db/index.ts source-string contract (D-10 line-1 enforcement)', ...)`. Use `fs.promises.readFile` + `path.resolve(__dirname, '..', 'index.ts')` to read the file content. Split by `\n`. Add five `it(...)` assertions: (a) `expect(lines[0]).toBe("import 'server-only';")`; (b) `expect(content).toContain('export const db')`; (c) `expect(content).toContain("from 'drizzle-orm/neon-http'")`; (d) `expect(content).toContain("from '@neondatabase/serverless'")`; (e) `expect(content).not.toContain("'use client'")` AND `expect(content).not.toContain("from 'next/navigation'")` AND `expect(content).not.toContain("from 'react'")`. Run `npm test -- --testPathPattern='db/__tests__/index' --watchAll=false` and expect failure: `ENOENT: no such file or directory, open '.../src/db/index.ts'`. Commit: `test(31-02): add failing source-string contract for src/db/index.ts`.

    Step 2 (GREEN — create source files):
    Create `src/db/index.ts` with content per RESEARCH §"Code Examples" lines 593-607:
      - Line 1: `import 'server-only';` (literal — no extra whitespace, no trailing comment on the same line)
      - Followed by `import { drizzle } from 'drizzle-orm/neon-http';`
      - Followed by `import { neon } from '@neondatabase/serverless';`
      - Then an `if (!process.env.DATABASE_URL) { throw new Error(...) }` guard with the helpful message from RESEARCH lines 598-602 (mentions pooled endpoint and that drizzle.config.ts uses the UNPOOLED variant)
      - Then `const sql = neon(process.env.DATABASE_URL);`
      - Then `export const db = drizzle({ client: sql });`
    Create `src/db/schema.ts` with only: (a) a 2-3 line comment header noting Phase 31 placeholder per D-09 and that real tables (production_orders, order_events, import_batches, users) land in Phase 32 per DATA-02..05; (b) the single line `export {};`. No other content.
    Re-run `npm test -- --testPathPattern='db/__tests__/index' --watchAll=false`; expect all 5 assertions pass. Run `npx tsc --noEmit` to confirm no type errors. Commit: `feat(31-02): create src/db/index.ts server-only singleton + schema placeholder`.

    Step 3 (verify drizzle-kit accepts the placeholder): Run `npx drizzle-kit generate` once. Expected behavior per RESEARCH §"Pitfall 8" + Assumption A1: command completes without error and produces zero migration files in `./drizzle/`. If it errors with "No tables found in schema" or similar, apply the fallback from RESEARCH lines 501-509: edit `src/db/schema.ts` to replace `export {};` with `import { pgTable, serial } from 'drizzle-orm/pg-core'; export const _phase31_placeholder = pgTable('_phase31_placeholder', { id: serial('id').primaryKey() });` and re-run drizzle-kit generate. Document the chosen path in the commit message. Note: this step requires DATABASE_URL_UNPOOLED to be set — for executor convenience set it to a syntactically-valid placeholder URL like `DATABASE_URL_UNPOOLED=postgresql://placeholder@localhost/postgres` in the current shell ONLY (do NOT write to .env.local). The drizzle-kit `generate` command only parses the schema; it does not connect for generate-with-no-tables. If drizzle-kit nonetheless attempts a connection, treat this verification as deferred to Plan 31-05 after the operator populates `.env.local` (mark this acceptance criterion as "verified in 31-05" in the task done note).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='db/__tests__/index' --watchAll=false && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/db/index.ts` outputs exactly `import 'server-only';` (with no leading whitespace and exactly one trailing semicolon)
    - `grep -c "from 'drizzle-orm/neon-http'" src/db/index.ts` returns exactly 1
    - `grep -c "from '@neondatabase/serverless'" src/db/index.ts` returns exactly 1
    - `grep -c "export const db" src/db/index.ts` returns exactly 1
    - `grep -E "if \(!process.env.DATABASE_URL\)" src/db/index.ts` returns at least 1 line (env guard present)
    - `grep -c "'use client'" src/db/index.ts` returns 0 (negative — must not appear)
    - `grep -c "from 'next/navigation'" src/db/index.ts` returns 0 (negative)
    - `cat src/db/schema.ts | grep -c "export {}"` returns at least 1 (placeholder export present)
    - `ls -la src/db/schema/` fails (directory does NOT exist — D-09 single-file requirement)
    - `npm test -- --testPathPattern='db/__tests__/index' --watchAll=false` exits 0; all 5 source-string assertions pass
    - `npx tsc --noEmit` exits 0
    - `npx drizzle-kit generate` either (a) completes without error producing zero migration files in ./drizzle/ confirming Assumption A1, or (b) errors and the schema.ts fallback (no-op pgTable) is applied — either path is acceptable; document in commit message which path was taken
  </acceptance_criteria>
  <done>
    src/db/index.ts and src/db/schema.ts compile clean; line-1 server-only assertion test passes; schema placeholder accepted by drizzle-kit (or fallback applied).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create drizzle.config.ts at repo root reading DATABASE_URL_UNPOOLED</name>
  <files>drizzle.config.ts</files>
  <read_first>
    - playwright.config.ts (lines 1-7 — canonical dotenv-at-repo-root pattern)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`drizzle.config.ts`" — full target shape with all required keys)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 564-587 — canonical drizzle.config.ts; §"Pattern 2" — env-loading rationale; §"Pitfall 4" — dotenv must be loaded explicitly because drizzle-kit is a separate Node process)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (D-08, D-09, D-11)
  </read_first>
  <action>
    Step 1: Create `/Users/joel/Desktop/Projects/cgm-dashboard/drizzle.config.ts` (at repo root per D-11) with the exact shape from RESEARCH §"Code Examples" lines 564-587:
    - Imports: `import { config } from 'dotenv';`, `import path from 'path';`, `import { defineConfig } from 'drizzle-kit';`
    - Then call `config({ path: path.resolve(__dirname, '.env.local') });` — mirror playwright.config.ts lines 5-6 exactly so the pattern is consistent across repo-root configs
    - Then a runtime guard: `if (!process.env.DATABASE_URL_UNPOOLED) throw new Error('DATABASE_URL_UNPOOLED is not set. Use the Neon DIRECT (non-pooler) URL — PgBouncer transaction mode is incompatible with migration SET commands. See docs/clerk-setup.md or .env.example for the expected shape.');`
    - Then default export: `defineConfig({ schema: './src/db/schema.ts', out: './drizzle', dialect: 'postgresql', dbCredentials: { url: process.env.DATABASE_URL_UNPOOLED } })`

    Step 2: Do NOT use `process.env.DATABASE_URL` anywhere in this file (D-08 — pooled URL would break migrations under PgBouncer transaction mode). Do NOT place the file under `src/` — D-11 mandates repo root next to next.config.ts and eslint.config.mjs.

    Step 3: Run `npx tsc --noEmit` to confirm the file type-checks. The file should compile without error even if `.env.local` is not yet populated (drizzle-kit only reads it at CLI invocation time).

    Commit: `feat(31-02): add drizzle.config.ts at repo root reading DATABASE_URL_UNPOOLED`.
  </action>
  <verify>
    <automated>test -f drizzle.config.ts && grep -c "process.env.DATABASE_URL_UNPOOLED" drizzle.config.ts && grep -c "schema: './src/db/schema.ts'" drizzle.config.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls drizzle.config.ts` succeeds (file exists at repo root per D-11)
    - `ls src/drizzle.config.ts` fails (file NOT under src/ — negative assertion)
    - `grep -c "process.env.DATABASE_URL_UNPOOLED" drizzle.config.ts` returns at least 2 (one in the guard, one in dbCredentials.url)
    - `grep -c "process.env.DATABASE_URL\b" drizzle.config.ts` returns 0 (pooled URL is NEVER referenced — D-08)
    - `grep -E "schema: '\./src/db/schema\.ts'" drizzle.config.ts` returns 1 line (D-09 single-file path)
    - `grep -E "dialect: 'postgresql'" drizzle.config.ts` returns 1 line
    - `grep -E "out: '\./drizzle'" drizzle.config.ts` returns 1 line
    - `grep -c "config({ path: path.resolve(__dirname, '.env.local') })" drizzle.config.ts` returns 1 (dotenv-at-repo-root pattern matches playwright.config.ts)
    - `grep -c "from 'drizzle-kit'" drizzle.config.ts` returns 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    drizzle.config.ts exists at repo root, locks the UNPOOLED URL at config-write time, points at the single-file schema per D-09, and type-checks cleanly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Extend .env.example with DB and mill-operator E2E keys</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current file — lines 11-18 show the established `E2E_*_USER_EMAIL` / `_PASSWORD` pattern)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`.env.example`" — exact target shape)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 623-637)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (D-07 — DATABASE_URL = pooled, DATABASE_URL_UNPOOLED = direct; D-12 + D-14 — new E2E user keys)
  </read_first>
  <action>
    Step 1: Append to `.env.example` (after the existing `E2E_ADMIN_USER_PASSWORD=` line at line 18) the new block. Match the existing pattern exactly: comment header followed by `KEY=value` (with empty value for password fields and the literal email for the email fields). The block to append (4 new lines for the E2E user pair + 6 new lines including comments for the DB pair):

    ```
    # E2E mill-operator test user (Phase 31, D-12 + D-14)
    E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator+clerk_test@example.com
    E2E_MILL_OPERATOR_USER_PASSWORD=

    # Neon Postgres (Phase 31, D-07)
    # DATABASE_URL: pooled endpoint (-pooler.neon.tech) for application queries.
    # DATABASE_URL_UNPOOLED: direct endpoint for drizzle-kit migrations only.
    # PgBouncer transaction mode breaks migration SET commands — never swap these.
    DATABASE_URL=
    DATABASE_URL_UNPOOLED=
    ```

    The email value `e2e-mill-operator+clerk_test@example.com` is verbatim from D-12 — the `+clerk_test` suffix is Clerk's documented safe-mailbox marker (RESEARCH §"Pitfall 5"). Do not change the casing.

    Step 2: Do NOT remove or reorder any existing entry. Do NOT add real secrets (passwords / connection strings) — placeholder pattern only. The actual values are operator-populated in `.env.local` (gitignored) per Plan 31-05.

    Commit: `chore(31-02): add DB + mill-operator E2E placeholders to .env.example`.
  </action>
  <verify>
    <automated>grep -c "^DATABASE_URL=$" .env.example && grep -c "^DATABASE_URL_UNPOOLED=$" .env.example && grep -c "^E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator\+clerk_test@example\.com$" .env.example && grep -c "^E2E_MILL_OPERATOR_USER_PASSWORD=$" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^DATABASE_URL=$" .env.example` returns exactly 1 (placeholder with empty value, no quotes)
    - `grep -c "^DATABASE_URL_UNPOOLED=$" .env.example` returns exactly 1
    - `grep -c "^E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator\+clerk_test@example.com$" .env.example` returns exactly 1 (literal email with +clerk_test suffix preserved)
    - `grep -c "^E2E_MILL_OPERATOR_USER_PASSWORD=$" .env.example` returns exactly 1 (empty placeholder)
    - All four existing keys (`E2E_DEMO_USER_EMAIL`, `E2E_DEMO_USER_PASSWORD`, `E2E_NOROLE_USER_EMAIL`, `E2E_NOROLE_USER_PASSWORD`, `E2E_ADMIN_USER_EMAIL`, `E2E_ADMIN_USER_PASSWORD`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_SIGN_IN_URL`, `NEXT_PUBLIC_CLERK_SIGN_UP_URL`) still appear in .env.example (no regression — `grep -c "E2E_DEMO_USER_EMAIL" .env.example` >= 1)
    - `grep -c "DATABASE_URL=postgres" .env.example` returns 0 (no real connection string committed)
    - `grep -c "DATABASE_URL=https://" .env.example` returns 0 (no URL committed)
    - The comment line `# PgBouncer transaction mode breaks migration SET commands` is present
  </acceptance_criteria>
  <done>
    .env.example has 4 new placeholder keys; existing entries unchanged; no secrets committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge runtime ↔ Node.js runtime | `src/db/index.ts` MUST execute in Node.js only; leaking into the Edge bundle exposes `neon()`/`drizzle` to a runtime that cannot resolve `node:fs`/`node:net` |
| Build process ↔ committed env | `.env.example` is committed as a placeholder; real values live only in `.env.local` (gitignored) and never reach the repo |
| drizzle-kit CLI (Node process) ↔ Next.js runtime | drizzle.config.ts is loaded ONLY by drizzle-kit, not by Next.js; the two processes have independent env-loading semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Information Disclosure | DB credentials leak via Edge bundle if src/db/index.ts is transitively imported by a client component or middleware | mitigate | `import 'server-only';` as line 1 of src/db/index.ts (D-10) triggers a build-time error in any wrong-bundle inclusion. Verified by: (a) the source-string assertion test in Task 2; (b) clean `next build` smoke in Plan 31-05. Severity: high (if exploited). Mitigation reference: src/db/__tests__/index.test.ts source assertion + Plan 31-05 build smoke. |
| T-31-02-02 | Tampering | drizzle.config.ts misconfigured to read DATABASE_URL (pooled) instead of DATABASE_URL_UNPOOLED, silently breaking Phase 32 migrations | mitigate | drizzle.config.ts source-asserts `DATABASE_URL_UNPOOLED` (Task 3); negative grep asserts `DATABASE_URL\b` is NEVER referenced; runtime guard throws a helpful error if the unpooled var is missing. Severity: medium. Mitigation reference: drizzle.config.ts source assertions in Task 3 acceptance criteria. |
| T-31-02-03 | Information Disclosure | Real Postgres credentials accidentally committed to .env.example | mitigate | .env.example placeholders only; grep asserts no `postgres://` or `https://` strings present (Task 4). `.env.local` is gitignored (confirmed by existing Phase 27 work in STATE.md). Severity: high (if committed). Mitigation reference: Task 4 acceptance grep + .gitignore. |
| T-31-02-04 | Denial of Service | drizzle-kit generate fails on `export {}` empty schema, blocking Phase 32 | accept | RESEARCH §"Pitfall 8" + Assumption A1: drizzle-kit is documented to accept empty schemas; if not, the fallback (no-op `pgTable('_phase31_placeholder', ...)`) is documented in RESEARCH lines 501-509 and applied during Task 2 verification. Severity: low (workaround exists, both paths land Phase 32). Mitigation reference: Task 2 Step 3 fallback. |
| T-31-02-05 | Information Disclosure | Future static-analysis tooling fails to detect a server-only file because `import 'server-only'` is not at line 1 | mitigate | D-10 mandates line-1 placement; Task 2's source-string assertion test enforces this at every commit. Severity: low (process risk). Mitigation reference: src/db/__tests__/index.test.ts `expect(lines[0]).toBe("import 'server-only';")`. |
</threat_model>

<verification>
After all four tasks complete:
- `npx tsc --noEmit` exits 0
- `npm test -- --testPathPattern='db/__tests__/index' --watchAll=false` exits 0; all 5 source-string assertions pass
- `head -1 src/db/index.ts` outputs exactly `import 'server-only';`
- `ls drizzle.config.ts src/db/index.ts src/db/schema.ts src/db/__tests__/index.test.ts .env.example` shows all 5 files exist
- `node -e "const p=require('./package.json'); console.log(p.dependencies['drizzle-orm'], p.dependencies['@neondatabase/serverless'], p.devDependencies['drizzle-kit'])"` prints `0.45.2 1.1.0 0.31.10`
- `npx drizzle-kit generate` either succeeds with zero migrations OR fallback schema was applied (either acceptable)

(Note: `npm run build` Edge-bundle smoke is verified in Plan 31-05 after all Wave 1 plans complete — that test is the canonical end-of-phase proof.)
</verification>

<success_criteria>
- Three packages installed at exact pinned versions (drizzle-orm@0.45.2, @neondatabase/serverless@1.1.0, drizzle-kit@0.31.10)
- src/db/index.ts exists with `import 'server-only';` as line 1, exports `db`, uses neon-http driver, throws helpful error if DATABASE_URL missing
- src/db/schema.ts exists as a valid TS module (single file per D-09)
- drizzle.config.ts exists at repo root, reads DATABASE_URL_UNPOOLED per D-08, references './src/db/schema.ts' per D-09
- .env.example has 4 new placeholder keys (DATABASE_URL, DATABASE_URL_UNPOOLED, E2E_MILL_OPERATOR_USER_EMAIL, E2E_MILL_OPERATOR_USER_PASSWORD); no real secrets committed
- src/db/__tests__/index.test.ts enforces line-1 placement via fs.readFile source assertion; test passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-role-expansion-and-db-infrastructure/31-02-SUMMARY.md`.
</output>