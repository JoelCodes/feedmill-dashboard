---
phase: 31-role-expansion-and-db-infrastructure
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [AUTH-04, DATA-01]
tags: [runbook, operator, neon, clerk, verification-gate, smoke]

must_haves:
  truths:
    - "Neon project 'cgm-dashboard' provisioned at neon.tech; pooled + unpooled URLs are present in .env.local (D-06, D-07)"
    - "Clerk Dashboard contains e2e-mill-operator+clerk_test@example.com with publicMetadata.roles ['mill_operator'] (D-12)"
    - "Clerk Dashboard's e2e-demo+clerk_test@example.com publicMetadata.roles updated to ['demo','mill_operator'] (D-13)"
    - "JWT decoded at jwt.io for both users confirms the expected metadata.roles array — proves Pitfall 6 propagation succeeded"
    - "npm run build (Edge-runtime contamination smoke) exits 0 with no 'Module not found: Can't resolve fs' errors — proves DATA-08 line-1 server-only enforcement works"
    - "npm test exits 0 (full Jest suite); npx tsc --noEmit exits 0; npx playwright test --project=auth-mill-operator exits 0 (smoke spec proves data-mode='edit' end-to-end)"
  artifacts: []
  key_links:
    - from: "Operator runbook execution"
      to: ".env.local DATABASE_URL + DATABASE_URL_UNPOOLED + E2E_MILL_OPERATOR_USER_PASSWORD"
      via: "Manual values copied from Neon and Clerk dashboards"
      pattern: "(manual — verified by Verification step below)"
    - from: "Phase 31 verification gate"
      to: "Phase 32 readiness"
      via: "Full smoke (tsc + jest + build + playwright) green"
      pattern: "all four commands exit 0"
---

<objective>
Execute the operator-only runbook steps (Neon project provisioning + Clerk Dashboard test-user setup) and run the canonical end-of-phase verification gate (`npx tsc --noEmit && npm test && npm run build && npx playwright test --project=auth-mill-operator`). This plan is the only non-autonomous plan in Phase 31; it pauses for user interaction at three points (Neon provisioning, Clerk Dashboard cutover, Vercel CI secrets — Vercel secrets are optional and can be deferred per Open Q4).

Purpose: This plan turns the Wave 1 + Wave 2 code + config into a working end-to-end system. The operator-supplied secrets in `.env.local` and the Clerk Dashboard cutover are prerequisites for the Playwright smoke spec (Plan 31-03 Task 2) to actually run. The `npm run build` Edge-runtime contamination smoke is the canonical proof point for DATA-08 — without this step the line-1 server-only assertion is only a static test, not a runtime build verification.

Output: No file changes. Three checkpoint pauses, followed by a fully-automated verification gate that runs four commands. End state: Phase 31 is verifiably complete and Phase 32 is unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md
@docs/clerk-setup.md
@.env.example
@.planning/phases/31-role-expansion-and-db-infrastructure/31-01-PLAN.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-02-PLAN.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-03-PLAN.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-04-PLAN.md

<interfaces>
<!-- Operator-supplied state contracts -->

After this plan completes, .env.local MUST contain (real values, not placeholders):
- `DATABASE_URL=postgresql://...@<branch>-pooler.<region>.neon.tech/<db>?sslmode=require` (pooled — hostname contains `-pooler`)
- `DATABASE_URL_UNPOOLED=postgresql://...@<branch>.<region>.neon.tech/<db>?sslmode=require` (direct — hostname does NOT contain `-pooler`)
- `E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator+clerk_test@example.com` (from Plan 31-02 — already in .env.example)
- `E2E_MILL_OPERATOR_USER_PASSWORD=<password set during Clerk user creation>` (operator-supplied)

After this plan completes, the Clerk Dashboard MUST have:
- User `e2e-mill-operator+clerk_test@example.com` with publicMetadata.roles `["mill_operator"]` (D-12)
- User `e2e-demo+clerk_test@example.com` with publicMetadata.roles `["demo", "mill_operator"]` (D-13 — updated from `["demo"]`)
- JWT template unchanged from Phase 27 (`{"metadata": {"roles": "{{user.public_metadata.roles}}"}}`)

Verification gate commands (all four must exit 0):
- `npx tsc --noEmit` — TypeScript compilation
- `npm test -- --watchAll=false` — full Jest suite
- `npm run build` — Next.js build (canonical Edge-runtime contamination smoke per DATA-08 and CONTEXT.md §specifics)
- `npx playwright test --project=auth-mill-operator` — E2E smoke (mill-operator-smoke.spec.ts asserts data-mode='edit')
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator provisions Neon project + populates .env.local DB URLs</name>
  <files>(manual checkpoint — no autonomous file modifications)</files>
  <action>
    WHAT THIS CHECKPOINT GATES:
    Plan 31-02 created `.env.example` with empty placeholders for `DATABASE_URL` (pooled) and `DATABASE_URL_UNPOOLED` (direct). Plan 31-02 also created `src/db/index.ts` (reads pooled URL at module-evaluation time) and `drizzle.config.ts` (reads unpooled URL at CLI invocation time). Neither file is imported at runtime in Phase 31, so the build can succeed without the URLs being set — BUT the Playwright smoke spec in Task 6 below requires the dev server to start cleanly, and Phase 32 cannot ship without these values. So the operator must do this now.

    OPERATOR RUNBOOK:
    Operator runbook (this is the human-only portion — Claude cannot automate Neon Dashboard interactions):

    1. Open https://console.neon.tech in a browser, sign in.
    2. Create a new project: name = `cgm-dashboard`; region = closest to user (e.g., `aws-us-east-1`); Postgres version = latest stable.
    3. In the project dashboard, copy the connection strings:
       - "Pooled connection" (hostname will contain `-pooler.<region>.neon.tech`) → this is the value for `DATABASE_URL`.
       - "Direct connection" (no `-pooler` in hostname) → this is the value for `DATABASE_URL_UNPOOLED`.
    4. Open `.env.local` at the repo root (create it if it doesn't exist — must NOT be committed). Add (or update) two lines:
       ```
       DATABASE_URL=postgresql://...@...-pooler.<region>.neon.tech/...?sslmode=require
       DATABASE_URL_UNPOOLED=postgresql://...@...<region>.neon.tech/...?sslmode=require
       ```
    5. Save .env.local. Confirm `.env.local` is gitignored (it is — Phase 27 work).
    6. Sanity-check by running `node -e "require('dotenv').config({ path: '.env.local' }); console.log('pooled:', !!process.env.DATABASE_URL, 'unpooled:', !!process.env.DATABASE_URL_UNPOOLED)"` — expect output `pooled: true unpooled: true`.
    7. Optional: run `npx drizzle-kit generate` once — expect it to complete without error (zero migration files produced because schema.ts is empty per D-09). If this errors with a connection failure, the unpooled URL is correct but the project may not be fully provisioned yet — wait 30 seconds and retry.

    Severity if skipped: Phase 32 is blocked. Phase 31 verification gate (Task 6 below) will fail on `npm run build` if the dev server cannot start.
  </action>
  <verify>
    <automated>echo "manual checkpoint — operator confirms via resume signal (no automated check)"</automated>
  </verify>
  <done>Operator types the resume signal after completing the runbook and confirming the acceptance criteria below.</done>
  <acceptance_criteria>
    - Operator confirms .env.local contains both `DATABASE_URL` and `DATABASE_URL_UNPOOLED` lines with non-empty values
    - Both values start with `postgresql://`
    - The DATABASE_URL hostname contains `-pooler` (pooled endpoint per D-07)
    - The DATABASE_URL_UNPOOLED hostname does NOT contain `-pooler` (direct endpoint per D-07)
    - Optional: `npx drizzle-kit generate` runs without error and produces zero migration files in ./drizzle/
  </acceptance_criteria>
  <resume-signal>Type "neon provisioned" (or describe issues encountered).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes Clerk Dashboard cutover per docs/clerk-setup.md</name>
  <files>(manual checkpoint — no autonomous file modifications)</files>
  <action>
    WHAT THIS CHECKPOINT GATES:
    Plan 31-03 updated `docs/clerk-setup.md` with: Step 2 table row for the new mill_operator user (D-12); Step 3 dual-role JSON example for the existing demo user (D-13); Step 4 propagation reminder; Step 5 .env.local block; Verification steps #4 and #5 for the new users. Plan 31-02 added `E2E_MILL_OPERATOR_USER_EMAIL` and `E2E_MILL_OPERATOR_USER_PASSWORD` to .env.example.

    OPERATOR RUNBOOK:
    Operator runbook — follow docs/clerk-setup.md (updated by Plan 31-03) exactly. Summary:

    1. Open the Clerk Dashboard for the dev instance (the same instance whose keys are in .env.local: `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_*`).
    2. JWT template (Step 1 of docs/clerk-setup.md): NO CHANGE required — the existing `{"metadata": {"roles": "{{user.public_metadata.roles}}"}}` template forwards the roles array verbatim.
    3. Create new user (Step 2):
       - Email: `e2e-mill-operator+clerk_test@example.com` (verbatim — preserves Clerk safe-mailbox suffix per RESEARCH Pitfall 5)
       - Password: choose a strong password. RECORD IT for step 6 below.
       - Save the user.
    4. Set publicMetadata for the new user (Step 3):
       - Navigate to the new user → Metadata → Edit publicMetadata.
       - Set: `{"roles": ["mill_operator"]}`.
       - Save.
    5. Update existing demo user (D-13):
       - Navigate to `e2e-demo+clerk_test@example.com` → Metadata → Edit publicMetadata.
       - Change `{"roles": ["demo"]}` to `{"roles": ["demo", "mill_operator"]}`.
       - Save.
    6. Sign out / sign in propagation (Step 4 / Pitfall 6):
       - If any browser is signed in as the demo user, sign out and sign back in to refresh the cookie. The JWT does NOT pick up publicMetadata changes until sign-in.
    7. Populate .env.local (Step 5):
       - Open .env.local at repo root.
       - Add (or confirm existing): `E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator+clerk_test@example.com`
       - Add: `E2E_MILL_OPERATOR_USER_PASSWORD=<the password set in step 3 above>`
       - Save.
    8. Verification (Step 7 in docs/clerk-setup.md):
       - Sign in as `e2e-mill-operator+clerk_test@example.com`. Navigate to `/`. Expect "Edit mode (mill_operator)" indicator. Open browser devtools, copy the `__session` cookie value, paste into jwt.io. Confirm `metadata.roles: ['mill_operator']` is present in the decoded payload.
       - Sign in as the updated `e2e-demo+clerk_test@example.com`. Navigate to `/`. Expect "Edit mode (mill_operator)" indicator (because the demo user now has BOTH roles). Decode JWT; confirm `metadata.roles: ['demo', 'mill_operator']`.
       - Sign in as `e2e-norole+clerk_test@example.com` (existing Phase 27 user). Navigate to `/`. Expect "Read-only mode" indicator (no `mill_operator` role; D-01 grants read-only access to all authenticated users).

    Severity if skipped: Playwright smoke (Task 6 below) will fail with "User not found" or auth-redirect; the demo user E2E coverage will not validate D-13.
  </action>
  <verify>
    <automated>echo "manual checkpoint — operator confirms via resume signal (no automated check)"</automated>
  </verify>
  <done>Operator types the resume signal after completing the runbook and confirming the acceptance criteria below.</done>
  <acceptance_criteria>
    - Clerk Dashboard shows user `e2e-mill-operator+clerk_test@example.com` with publicMetadata `{"roles": ["mill_operator"]}` (operator visually confirms)
    - Clerk Dashboard shows user `e2e-demo+clerk_test@example.com` with publicMetadata `{"roles": ["demo", "mill_operator"]}` (operator visually confirms)
    - `.env.local` contains `E2E_MILL_OPERATOR_USER_EMAIL=e2e-mill-operator+clerk_test@example.com` and `E2E_MILL_OPERATOR_USER_PASSWORD=<non-empty>` (operator confirms)
    - Decoded JWT for the new mill_operator user (via jwt.io) shows `metadata.roles: ['mill_operator']`
    - Decoded JWT for the updated demo user shows `metadata.roles: ['demo', 'mill_operator']` (after sign-out + sign-in)
    - Manual browser smoke: signing in as mill_operator user and navigating to `/` shows "Edit mode" indicator
    - Manual browser smoke: signing in as updated demo user and navigating to `/` shows "Edit mode" indicator (dual-role coverage proof)
    - Manual browser smoke: signing in as norole user and navigating to `/` shows "Read-only mode" indicator (D-01 read-only access proof)
  </acceptance_criteria>
  <resume-signal>Type "clerk users created" (or describe issues encountered).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide on CI/GitHub Actions secret addition (optional — per RESEARCH Open Q4)</name>
  <files>(manual checkpoint — no autonomous file modifications unless option-add is selected)</files>
  <action>
    DECISION GATE:
    Whether to add `E2E_MILL_OPERATOR_USER_EMAIL` and `E2E_MILL_OPERATOR_USER_PASSWORD` as GitHub Actions secrets now (if a CI workflow exists), or defer until Phase 32+ ships and the E2E test matrix is broadened.

    CONTEXT:
    RESEARCH §"Open Questions" #4 notes: "No `.github/workflows/` directory was inspected; STATE.md does not mention CI/CD configuration." If no CI workflow currently exists, this task has nothing to do — defer. If a CI workflow exists, the new env vars must be added as secrets for the auth-mill-operator project to run under CI.

    Discover the current CI state first: `ls .github/workflows/ 2>/dev/null`. If the directory does not exist or is empty, choose option-defer. If it exists with test-running workflows, choose option-add.

    OPTIONS (operator selects one via resume signal):
    <option id="option-add">
      <name>Add GitHub Actions secrets now</name>
      <pros>CI runs the auth-mill-operator project end-to-end immediately. No future blocker.</pros>
      <cons>Requires gh CLI authenticated against the repo. If CI doesn't yet run E2E tests, this is wasted setup.</cons>
      <command>gh secret set E2E_MILL_OPERATOR_USER_EMAIL --body 'e2e-mill-operator+clerk_test@example.com'; gh secret set E2E_MILL_OPERATOR_USER_PASSWORD --body '&lt;password from Task 2&gt;'</command>
    </option>
    <option id="option-defer">
      <name>Defer to when CI gains E2E coverage</name>
      <pros>Zero work now. The new env vars are documented in .env.example and docs/clerk-setup.md so a future contributor can wire CI.</pros>
      <cons>If CI workflow exists and runs E2E now, the auth-mill-operator project will fail until secrets are added.</cons>
      <command>(no action — note in PHASE_SUMMARY that GitHub secrets are deferred)</command>
    </option>
  </action>
  <verify>
    <automated>echo "manual checkpoint — operator selects an option via resume signal"</automated>
  </verify>
  <done>Operator types the resume signal indicating which option was selected (and executes option-add commands if chosen).</done>
  <acceptance_criteria>
    - If option-add: `gh secret list 2>&1 | grep -c E2E_MILL_OPERATOR` returns at least 2 (both secret names present)
    - If option-defer: `ls .github/workflows/ 2>/dev/null` either fails (no directory) OR shows the operator has documented the deferral in their resume signal
    - Either path: the choice is recorded in the resume signal
  </acceptance_criteria>
  <resume-signal>Select: `option-add` (with gh secrets added) OR `option-defer` (with the rationale, e.g., "no .github/workflows/ directory exists yet").</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Pre-gate environment sanity check</name>
  <files></files>
  <read_first>
    - .env.example (verify the four Phase-31 keys are committed as empty placeholders)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md (§"Manual-Only Verifications" — operator action checklist)
  </read_first>
  <action>
    Step 1: Confirm the operator-supplied .env.local state by running a one-shot Node check (does NOT print the secret values — only their presence):
    `node -e "require('dotenv').config({ path: '.env.local' }); ['DATABASE_URL','DATABASE_URL_UNPOOLED','E2E_MILL_OPERATOR_USER_EMAIL','E2E_MILL_OPERATOR_USER_PASSWORD'].forEach(k => console.log(k + ':', process.env[k] ? 'set' : 'MISSING'))"`. Expect all 4 keys print `set`.

    Step 2: Verify .env.local is NOT committed: `git status .env.local 2>&1 | grep -E "(untracked|ignored)" || echo "WARN: .env.local may be tracked"`. Expect the gitignore to be active.

    Step 3: Confirm Clerk publishable + secret keys are still set (preserved from Phase 27): `node -e "require('dotenv').config({ path: '.env.local' }); ['NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY','CLERK_SECRET_KEY'].forEach(k => console.log(k + ':', process.env[k] ? 'set' : 'MISSING'))"`. Expect both `set`.

    If any check fails, abort the verification gate and return to Task 1 or Task 2 as appropriate. Do NOT proceed to Task 5 with missing env state.
  </action>
  <verify>
    <automated>node -e "require('dotenv').config({ path: '.env.local' }); const keys=['DATABASE_URL','DATABASE_URL_UNPOOLED','E2E_MILL_OPERATOR_USER_EMAIL','E2E_MILL_OPERATOR_USER_PASSWORD','NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY','CLERK_SECRET_KEY']; const missing=keys.filter(k=>!process.env[k]); if(missing.length){console.error('MISSING:',missing); process.exit(1)} else {console.log('ok')}"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 Phase-31 .env.local keys are present and non-empty
    - All 2 pre-existing Clerk keys are still present (no regression)
    - .env.local is gitignored (not tracked in git status)
    - Phase 27 E2E user keys (`E2E_DEMO_USER_*`, `E2E_NOROLE_USER_*`, `E2E_ADMIN_USER_*`) are still present in .env.local (operator should not have wiped them)
  </acceptance_criteria>
  <done>
    .env.local is fully populated; safe to proceed to the verification gate.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run the canonical verification gate (tsc + jest + build + playwright)</name>
  <files></files>
  <read_first>
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md (§"Test Infrastructure" — sampling rate; §"Phase gate")
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Validation Architecture" §"Phase Requirements → Test Map" — DATA-08 "next build succeeds with no Edge-bundle errors"; §"Specifics" — `npm run build` IS the canonical end-of-phase smoke check)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (§"Specifics" — "Phase 31's 'next build passes' success criterion is the proof point")
  </read_first>
  <action>
    Run all four commands in sequence; each must exit 0 before proceeding to the next.

    Step 1 — TypeScript compile: `npx tsc --noEmit`. Expect exit 0 with no error output. If errors: Phase 31 has a regression — abort and triage.

    Step 2 — Jest full suite: `npm test -- --watchAll=false`. Expect exit 0. Acceptable: the pre-existing 14 ClerkProvider failures in `src/app/settings/__tests__/page.test.tsx` (per STATE.md "Carried Deferred Items") may still fail — these are documented and not a Phase 31 regression. Compare against Plan 30 baseline if available. Critical Phase-31 tests that MUST pass: `src/lib/auth.test.ts` (5 requireRole + 5 checkRole cases), `src/test/fixtures/clerkAuth.test.ts` (10 cases), `src/app/page.test.tsx` (4 cases), `src/db/__tests__/index.test.ts` (5 source-string assertions).

    Step 3 — Next.js build (DATA-08 canonical smoke): `npm run build`. Expect exit 0. Critical: scan the build output for any of these strings — if found, the build "succeeded" but DATA-08 is violated and the phase fails:
    - `Module not found: Can't resolve 'fs'`
    - `Module not found: Can't resolve 'net'`
    - `Module not found: Can't resolve 'stream'`
    - `This module cannot be imported from a Client Component module`
    - `Edge runtime does not support Node.js`
    Use: `npm run build 2>&1 | tee /tmp/phase31-build.log; grep -E "Module not found|Edge runtime|cannot be imported" /tmp/phase31-build.log && echo "PHASE_FAIL_DATA08" || echo "phase31_build_clean"`. If `PHASE_FAIL_DATA08` is printed, src/db/index.ts is being transitively imported somewhere it shouldn't — abort and triage.

    Step 4 — Playwright auth-mill-operator E2E smoke: `npx playwright test --project=auth-mill-operator`. Expect exit 0; one test should run (mill-operator-smoke.spec.ts) and pass. This proves: (a) the auth-mill-operator storage state was generated by global.setup.ts (operator-supplied password is correct); (b) the mill_operator JWT has the role claim; (c) page.tsx's checkRole returns true; (d) MillReadOnlyStub renders data-mode='edit'. If this fails with "Missing env" or "User not found" errors, return to Task 1/2.

    Step 5 — If all four green, the phase verification gate has passed. Print a summary line: `echo "PHASE_31_VERIFICATION_PASSED: $(date)"`. Do NOT commit any tracked files in this task — no source changes occur. The verification log can be retained as a phase artifact at .planning/phases/31-role-expansion-and-db-infrastructure/verification.log if desired (gitignored or committed at developer discretion).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --watchAll=false && (npm run build 2>&1 | tee /tmp/phase31-build.log; ! grep -qE "Module not found: Can't resolve 'fs'|Module not found: Can't resolve 'net'|Edge runtime does not support|cannot be imported from a Client Component" /tmp/phase31-build.log) && npx playwright test --project=auth-mill-operator</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; no TS error lines in stderr
    - `npm test -- --watchAll=false` exits 0 (modulo the pre-existing 14 ClerkProvider failures in settings/__tests__/page.test.tsx documented in STATE.md "Carried Deferred Items" — if those 14 fail, that is acceptable; any OTHER failure is a regression)
    - All 4 Phase-31 critical test suites pass: src/lib/auth.test.ts (10 cases — 5 requireRole + 5 checkRole), src/test/fixtures/clerkAuth.test.ts (10 cases), src/app/page.test.tsx (4 behavior branches), src/db/__tests__/index.test.ts (5 source-string assertions)
    - `npm run build` exits 0
    - Build output does NOT contain `Module not found: Can't resolve 'fs'` (negative — DATA-08 enforcement check)
    - Build output does NOT contain `Module not found: Can't resolve 'net'` (negative)
    - Build output does NOT contain `This module cannot be imported from a Client Component module` (negative)
    - Build output does NOT contain `Edge runtime does not support` (negative)
    - `npx playwright test --project=auth-mill-operator` exits 0; the mill-operator-smoke spec passes — asserts URL stays at `/`, mill-mode element has `data-mode="edit"` attribute, contains text "Edit mode"
    - All 4 commands run in the order specified; if any fails the gate is INCOMPLETE and the phase is not yet shippable
  </acceptance_criteria>
  <done>
    Phase 31 verification gate is GREEN. The role expansion is enforced, the Drizzle infrastructure is provisioned and compile-clean, the test fixtures cover the new role, the docs are updated, and the smoke proves edit-mode end-to-end. Phase 32 (schema + migrations) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied secrets ↔ committed repo | Real DB connection strings and Clerk passwords live in .env.local (gitignored); operator must NOT paste them into terminal output, commit messages, or PR descriptions |
| Clerk Dashboard ↔ runtime auth | The JWT template + publicMetadata become the runtime source of truth for `metadata.roles` claims; misconfiguration here cascades into runtime auth failures |
| Build process ↔ Edge runtime | `npm run build` is the canonical end-of-phase proof that DATA-08 line-1 server-only enforcement prevents Edge contamination |
| Local dev ↔ Vercel production | Phase 31 explicitly defers Vercel env-var provisioning to Phase 34 per D-06 / D-16 — operator should NOT add these to Vercel env yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Information Disclosure | Operator accidentally pastes a real DATABASE_URL into a commit / PR / terminal log | mitigate | .env.local is gitignored (Phase 27 work; verified in Task 4); the runbook in Task 1 instructs the operator to never display the values. Severity: high (if leaked, Neon DB credentials are exposed). Mitigation reference: .gitignore enforcement + Task 4 status check + operator awareness from docs/clerk-setup.md prior conventions. |
| T-31-05-02 | Information Disclosure | Operator pastes a Clerk password into a commit / log | mitigate | Same protection — .env.local gitignored; Task 4 status check. Severity: medium (dev-instance password; impact limited to test users). Mitigation reference: same as T-31-05-01. |
| T-31-05-03 | Elevation of Privilege | Clerk JWT template misconfigured (e.g., swapped to the whole-object `{"metadata": "{{user.public_metadata}}"}`) inflates the token beyond Clerk's 1.2 KB cap | mitigate | docs/clerk-setup.md Step 1 explicitly preserves the existing template; Plan 31-03 Task 3 acceptance criterion: `grep -c 'user.public_metadata.roles' docs/clerk-setup.md` returns 1 (unchanged template). The runbook reiterates "JWT template: NO CHANGE required." Severity: low (operational risk; would manifest as auth failures, not silent escalation). Mitigation reference: Plan 31-03 Task 3 + Task 2 step 2 above. |
| T-31-05-04 | Information Disclosure | DB driver leaks into Edge bundle, exposing Neon credentials at runtime | mitigate | Task 5 Step 3 (`npm run build` smoke) checks for "Module not found: Can't resolve 'fs'" and "This module cannot be imported from a Client Component module" patterns. Plan 31-02's line-1 `import 'server-only'` enforcement (via source-string assertion test + build smoke) is the canonical mitigation. Severity: high (if exploited). Mitigation reference: Task 5 Step 3 negative grep on build output. |
| T-31-05-05 | Spoofing | E2E test user credentials are used for production auth attacks | accept | All Phase 31 test users carry the `+clerk_test` mailbox suffix (Clerk safe-mailbox marker) and exist only in the dev Clerk instance (`pk_test_*` keys). Production E2E remains blocked per v1.4 carried deferred item ("Production E2E automation requires custom domain to disable Clerk 2FA"). Severity: low (production not in scope for Phase 31 test users). Mitigation reference: STATE.md "Carried Deferred Items" + docs/clerk-setup.md dev-instance disclaimer. |
| T-31-05-06 | Denial of Service | Playwright smoke fails because the dev server cannot start (missing env, port conflict, Neon downtime) | mitigate | Task 4 pre-gate sanity check catches missing env; the playwright.config.ts webServer block (lines 56-63) brings up `next dev` only when not running against PLAYWRIGHT_BASE_URL. Neon downtime is rare and not Phase-31 specific. Severity: medium (would manifest as a test failure, blocking phase completion). Mitigation reference: Task 4 sanity check + Task 5 explicit error message handling. |
</threat_model>

<verification>
After all tasks complete:
- `node -e "require('dotenv').config({ path: '.env.local' }); console.log(!!process.env.DATABASE_URL && !!process.env.DATABASE_URL_UNPOOLED && !!process.env.E2E_MILL_OPERATOR_USER_EMAIL && !!process.env.E2E_MILL_OPERATOR_USER_PASSWORD)"` prints `true`
- `npx tsc --noEmit && npm test -- --watchAll=false && npm run build && npx playwright test --project=auth-mill-operator` exits 0 (all four green)
- Build log contains no Edge-runtime contamination markers
- Manual browser verification per Task 2 Step 8 (mill_operator user, demo dual-role user, norole user) all match the expected mode indicators
</verification>

<success_criteria>
- Neon project `cgm-dashboard` is provisioned; `.env.local` has both pooled and unpooled connection URLs
- Clerk Dashboard has the new `e2e-mill-operator+clerk_test@example.com` user with `["mill_operator"]` metadata (D-12)
- Clerk Dashboard's `e2e-demo+clerk_test@example.com` user has updated metadata `["demo", "mill_operator"]` (D-13)
- `.env.local` has `E2E_MILL_OPERATOR_USER_PASSWORD` populated
- CI secrets decision recorded (added now via gh CLI, or deferred with rationale)
- Verification gate: `tsc --noEmit` + `npm test` + `npm run build` + `npx playwright test --project=auth-mill-operator` all exit 0
- Build output is free of Edge-runtime contamination markers (DATA-08 proven)
- Playwright smoke confirms data-mode="edit" for the mill_operator user (full integration loop closed)
</success_criteria>

<output>
After completion, create `.planning/phases/31-role-expansion-and-db-infrastructure/31-05-SUMMARY.md`. The summary should record:
- Neon project name + region (no credentials)
- Clerk users created/updated (emails only — no passwords)
- CI secrets decision outcome (added / deferred)
- Verification gate command outputs (exit codes — no env values)
- Any Pitfall 6 propagation issues encountered + resolution
- Phase 32 readiness confirmation
</output>
