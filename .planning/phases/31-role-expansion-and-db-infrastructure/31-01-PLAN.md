---
phase: 31-role-expansion-and-db-infrastructure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/clerk.d.ts
  - src/lib/auth.ts
  - src/lib/auth.test.ts
  - src/test/fixtures/clerkAuth.ts
  - src/test/fixtures/clerkAuth.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
tags: [auth, role, clerk, types, test-fixtures]

must_haves:
  truths:
    - "'mill_operator' is a member of the Role union and tsc --noEmit passes"
    - "checkRole(role: Role) is exported from src/lib/auth.ts and returns boolean from sessionClaims.metadata.roles.includes(role)"
    - "Test fixtures expose mockMillOperatorSession() and mockDualRoleSession() factories"
    - "REQUIREMENTS.md AUTH-02 and AUTH-03 rewritten per D-04 and D-05 (no page-level role gate; no mill_operator middleware coarse-gate)"
    - "ROADMAP.md Phase 31 SC#2 and SC#4 rewritten per D-15 and D-16 (read-only-mode access; .env.local scope)"
  artifacts:
    - path: "src/types/clerk.d.ts"
      provides: "Role union including 'mill_operator'"
      contains: "'mill_operator'"
    - path: "src/lib/auth.ts"
      provides: "checkRole boolean predicate (server-only)"
      contains: "export async function checkRole"
    - path: "src/lib/auth.test.ts"
      provides: "checkRole TDD test cases + requireRole('mill_operator') coverage"
      contains: "checkRole"
    - path: "src/test/fixtures/clerkAuth.ts"
      provides: "mockMillOperatorSession + mockDualRoleSession factories"
      contains: "mockMillOperatorSession"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTH-02 + AUTH-03 rewritten text per D-17"
      contains: "Mutating server actions"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 31 SC#2 + SC#4 amended text per D-15 + D-16"
      contains: "read-only mode"
  key_links:
    - from: "src/lib/auth.ts (checkRole)"
      to: "@clerk/nextjs/server auth()"
      via: "sessionClaims.metadata.roles.includes(role)"
      pattern: "sessionClaims\\?\\.metadata\\?\\.roles\\?\\.includes"
    - from: "src/test/fixtures/clerkAuth.ts (mockDualRoleSession)"
      to: "mockAuth jest.fn"
      via: "mockResolvedValue with roles: ['demo','mill_operator']"
      pattern: "roles: \\['demo', 'mill_operator'\\]"
---

<objective>
Establish the type, server-side helper, and test-fixture foundation for the `mill_operator` role. Extend the `Role` union, add the `checkRole` helper (load-bearing for D-03 — `src/app/page.tsx` cannot compile without it), and extend the Clerk auth test fixtures so downstream Wave 2 plans can TDD against multi-role and mill-operator-only sessions. Also rewrite the AUTH-02, AUTH-03 requirement bullets and the ROADMAP success criteria amendments locked in D-15/D-16/D-17.

Purpose: This is the unblocking plan. `src/app/page.tsx` (Plan 31-04) requires `checkRole` to be exported. The Drizzle infrastructure (Plan 31-02) is independent. The Playwright project (Plan 31-03) is independent. Therefore this plan ships first in Wave 1 alongside 31-02 and 31-03.

Output: Type union widened; `checkRole` exported and tested; fixture factories ready for consumption by `src/app/page.test.tsx`; planning docs aligned with locked decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md
@.planning/phases/31-role-expansion-and-db-infrastructure/31-VALIDATION.md
@src/types/clerk.d.ts
@src/lib/auth.ts
@src/lib/auth.test.ts
@src/test/fixtures/clerkAuth.ts
@src/test/fixtures/clerkAuth.test.ts
@docs/security-patterns.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types/clerk.d.ts (current state — line 16):
```typescript
export type Role = 'demo' | 'admin' | 'user';

declare global {
  interface CustomJwtSessionClaims {
    metadata: {
      roles?: Role[];
    };
  }
}
```

From src/lib/auth.ts (current exports — only requireRole; checkRole NOT yet present):
```typescript
export async function requireRole(role: Role): Promise<void>;
```

Existing requireRole behavior:
- No userId → redirect('/sign-in')
- !sessionClaims.metadata.roles.includes(role) → redirect('/')
- Otherwise resolves with undefined

Target checkRole signature (NEW — to add per RESEARCH §"Pattern 4"):
```typescript
export async function checkRole(role: Role): Promise<boolean>;
```

checkRole semantics:
- Returns true iff sessionClaims.metadata.roles.includes(role)
- Returns false when: no userId, no sessionClaims, no metadata, no roles array, or role absent
- NO redirect side-effects; pure boolean predicate

From src/test/fixtures/clerkAuth.ts (existing factories — lines 167-186):
```typescript
export function mockDemoSession(): void;         // roles: ['demo']
export function mockNonDemoSession(role: Exclude<Role, 'demo'> = 'user'): void;  // roles: [role]
export function mockUnauthenticatedSession(): void;  // userId: null, sessionClaims: null
```

New fixture factories to add (per RESEARCH §"Code Examples" lines 670-693):
```typescript
export function mockMillOperatorSession(): void;  // roles: ['mill_operator']
export function mockDualRoleSession(): void;      // roles: ['demo','mill_operator']
```

Test mock pattern (canonical — from src/lib/auth.test.ts lines 1-22):
```typescript
const mockAuth = jest.fn();
jest.mock('@clerk/nextjs/server', () => ({ auth: () => mockAuth() }));
jest.mock('next/navigation', () => ({
  redirect: (url: string) => { throw Object.assign(new Error('NEXT_REDIRECT'), { url }); },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Role union to include 'mill_operator' (RED→GREEN)</name>
  <files>src/types/clerk.d.ts, src/lib/auth.test.ts</files>
  <read_first>
    - src/types/clerk.d.ts (current `Role` union and CustomJwtSessionClaims shape)
    - src/lib/auth.test.ts (existing requireRole test patterns — copy the mock setup at lines 1-22)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`src/types/clerk.d.ts`" excerpt)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 516-528 — target union shape)
  </read_first>
  <behavior>
    RED: Add a new test case to src/lib/auth.test.ts inside the existing `describe('requireRole', ...)` block:
      - "requireRole('mill_operator') resolves when session has roles: ['mill_operator']"
      - "requireRole('mill_operator') redirects to / when role missing"
      - "requireRole('mill_operator') resolves when session has roles: ['demo','mill_operator'] (multi-role)"
    These tests will FAIL at compile-time (or runtime) because 'mill_operator' is not yet in the Role union; TypeScript will error on the string literal type.
    GREEN: Add 'mill_operator' to the Role union in src/types/clerk.d.ts. Update the JSDoc comment in the same block to document the new variant per RESEARCH §"Code Examples" lines 520-526.
    REFACTOR: None expected — single-token additive change.
  </behavior>
  <action>
    Step 1 (RED): In src/lib/auth.test.ts, append 3 new `it(...)` cases inside the existing `describe('requireRole', ...)` block that exercise `requireRole('mill_operator')` for: (a) session with `roles: ['mill_operator']` → resolves undefined; (b) session with `roles: ['user']` → redirects to '/'; (c) session with `roles: ['demo', 'mill_operator']` → resolves undefined. Reuse the `mockAuth.mockResolvedValue({...})` pattern from the existing tests at lines 26-30. Run `npm test -- --testPathPattern='auth.test' --watchAll=false` and confirm TypeScript errors on the `'mill_operator'` string literal (failure mode: `Argument of type '"mill_operator"' is not assignable to parameter of type 'Role'`). Commit: `test(31-01): add failing requireRole('mill_operator') test cases`.

    Step 2 (GREEN): Edit src/types/clerk.d.ts line 16. Change the union from `'demo' | 'admin' | 'user'` to `'demo' | 'admin' | 'user' | 'mill_operator'`. Update the JSDoc block (lines 10-15) to add a fourth bullet documenting `'mill_operator'` as the edit role for the mill production dashboard (v2.0) that gates mutating server actions and edit affordances but does NOT gate page access — per CONTEXT.md D-01. Do not modify the `declare global` block. Run `npx tsc --noEmit` (expect exit 0) and `npm test -- --testPathPattern='auth.test' --watchAll=false` (expect 3 new cases pass plus all 5 existing cases pass). Commit: `feat(31-01): add 'mill_operator' to Role union`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --testPathPattern='auth\.test' --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "'mill_operator'" src/types/clerk.d.ts` returns at least 1 line
    - `grep -E "Role = 'demo' \| 'admin' \| 'user' \| 'mill_operator'" src/types/clerk.d.ts` returns exactly 1 line
    - `npx tsc --noEmit` exits 0
    - `npm test -- --testPathPattern='auth\.test' --watchAll=false` exits 0; new test names "requireRole('mill_operator') resolves when session has roles: ['mill_operator']", "requireRole('mill_operator') redirects to / when role missing", "requireRole('mill_operator') resolves when session has roles: ['demo','mill_operator']" all PASS
    - Existing 5 requireRole test cases continue to pass (no regression)
    - CustomJwtSessionClaims interface in declare global block is UNCHANGED (the metadata.roles?: Role[] line still references the now-widened Role)
  </acceptance_criteria>
  <done>
    `'mill_operator'` is a Role union member, three new requireRole TDD tests pass, and tsc --noEmit is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add checkRole helper to src/lib/auth.ts (RED→GREEN→REFACTOR)</name>
  <files>src/lib/auth.ts, src/lib/auth.test.ts</files>
  <read_first>
    - src/lib/auth.ts (existing requireRole shape at lines 41-49 — checkRole mirrors this minus the redirect side-effect)
    - src/lib/auth.test.ts (existing requireRole test mocks at lines 1-22)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`src/lib/auth.ts`" — JSDoc shape and signature constraint)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 539-559; §"Pitfall 7" line 489)
    - docs/security-patterns.md (§3 — checkRole as "the third member of the family")
  </read_first>
  <behavior>
    RED: Write a new `describe('checkRole', ...)` block in src/lib/auth.test.ts with 4 test cases BEFORE adding the export:
      - Test 1: "returns true when session has the role" — mockAuth resolves with `roles: ['mill_operator']`; `await checkRole('mill_operator')` returns `true`
      - Test 2: "returns false when session does not have the role" — mockAuth resolves with `roles: ['user']`; `await checkRole('mill_operator')` returns `false`
      - Test 3: "returns false when metadata is missing" — mockAuth resolves with `sessionClaims: {}`; `await checkRole('mill_operator')` returns `false`
      - Test 4: "returns true for any of multiple roles in the session" — mockAuth resolves with `roles: ['demo','mill_operator']`; `await checkRole('demo')` and `await checkRole('mill_operator')` both return `true` in sequence (re-mock between calls)
      - Test 5: "returns false when userId is null (unauthenticated)" — mockAuth resolves with `{ userId: null, sessionClaims: null }`; `await checkRole('mill_operator')` returns `false`
    These tests will fail with "Module '@/lib/auth' has no exported member 'checkRole'" or similar.
    GREEN: Append the `checkRole` function to src/lib/auth.ts after `requireRole`. Function signature: `async function checkRole(role: Role): Promise<boolean>`. Body reads `sessionClaims.metadata.roles?.includes(role) ?? false` via `await auth()`. Export it.
    REFACTOR: Add JSDoc block above checkRole documenting server-only nature, intended use (boolean prop for client components), and `@example` per RESEARCH lines 545-554.
  </behavior>
  <action>
    Step 1 (RED): In src/lib/auth.test.ts, append a new `describe('checkRole', ...)` block AFTER the existing `describe('requireRole', ...)` block. Add the 5 test cases listed in `<behavior>`. Import checkRole at the top: change the existing `import { requireRole } from './auth';` line to `import { requireRole, checkRole } from './auth';`. Run `npm test -- --testPathPattern='auth\.test' --watchAll=false`; expect TypeScript/module-resolution failure: `Module '"./auth"' has no exported member 'checkRole'`. Commit: `test(31-01): add failing checkRole test cases`.

    Step 2 (GREEN): Edit src/lib/auth.ts. After the existing `requireRole` export at lines 41-49, append a new exported `async function checkRole(role: Role): Promise<boolean>`. Body: `const { sessionClaims } = await auth();` then `return sessionClaims?.metadata?.roles?.includes(role) ?? false;`. Do NOT call `redirect`. Do NOT import `redirect` for this function (it is already imported at top of file for requireRole; leave the import as-is). Run `npm test -- --testPathPattern='auth\.test' --watchAll=false`; expect all 10 tests pass (5 requireRole + 5 checkRole). Commit: `feat(31-01): add checkRole boolean predicate to auth.ts`.

    Step 3 (REFACTOR): Add a JSDoc block above the `checkRole` declaration with: brief description ("Returns whether the current session has `role`. Server-only."), usage note ("Use when a page needs to branch on role membership without a redirect — e.g., to compute a `canEdit` prop for a client component."), and an `@example` showing the canonical Phase 31 usage `const canEdit = await checkRole('mill_operator');`. Cross-reference D-03. Re-run tests to confirm no regression. Commit: `refactor(31-01): add JSDoc to checkRole`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='auth\.test' --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export async function checkRole\(role: Role\): Promise<boolean>" src/lib/auth.ts` returns exactly 1 line
    - `grep -E "sessionClaims\\?\\.metadata\\?\\.roles\\?\\.includes\(role\)" src/lib/auth.ts` returns exactly 1 line (inside checkRole)
    - `grep -c "^import { redirect } from 'next/navigation'" src/lib/auth.ts` returns 1 (still imported for requireRole, not duplicated)
    - `npm test -- --testPathPattern='auth\.test' --watchAll=false` exits 0; all 5 new checkRole test cases plus 5 existing requireRole cases pass
    - `await checkRole('mill_operator')` with `roles: ['mill_operator']` returns `true` (behavior assertion via Test 1)
    - `await checkRole('mill_operator')` with `roles: ['user']` returns `false` (behavior assertion via Test 2)
    - `await checkRole('mill_operator')` with `sessionClaims: {}` returns `false` (behavior assertion via Test 3)
    - JSDoc block above checkRole contains `@example` and the string "server-only"
    - checkRole does NOT call `redirect` (negative source assertion: no `redirect(` inside the checkRole function body)
  </acceptance_criteria>
  <done>
    `checkRole` is exported from src/lib/auth.ts, 5 TDD test cases pass, function returns boolean without redirect side-effects. Plan 31-04's page rewrite can now import this symbol.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Clerk auth test fixtures for mill_operator and dual-role sessions</name>
  <files>src/test/fixtures/clerkAuth.ts, src/test/fixtures/clerkAuth.test.ts</files>
  <read_first>
    - src/test/fixtures/clerkAuth.ts (existing factories at lines 167-197 — copy the mockDemoSession / mockNonDemoSession shape)
    - src/test/fixtures/clerkAuth.test.ts (existing fixture contract tests — extend, do not replace)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`src/test/fixtures/clerkAuth.ts`" — factory additions block)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-RESEARCH.md (§"Code Examples" lines 670-693)
  </read_first>
  <behavior>
    RED: In src/test/fixtures/clerkAuth.test.ts, append 2 new `it(...)` cases inside the existing `describe('clerkAuth fixture', ...)` block:
      - "mockMillOperatorSession resolves mockAuth with roles containing mill_operator" — mirrors the mockDemoSession test pattern
      - "mockDualRoleSession resolves mockAuth with roles containing both demo and mill_operator" — verifies multi-role array contains BOTH strings via .toContain assertions
    Tests will fail with `mockMillOperatorSession is not a function` (or import error) — the factories don't exist yet.
    GREEN: Append two new exported factory functions to src/test/fixtures/clerkAuth.ts: `mockMillOperatorSession()` (roles: ['mill_operator']) and `mockDualRoleSession()` (roles: ['demo','mill_operator']). Both follow the exact shape of the existing `mockDemoSession` factory at lines 167-172. Update the import statement in clerkAuth.test.ts to pull the new factories.
    REFACTOR: Add JSDoc above each new factory: mockMillOperatorSession's doc mirrors mockDemoSession; mockDualRoleSession's doc references D-13 as the multi-role coverage path.
  </behavior>
  <action>
    Step 1 (RED): In src/test/fixtures/clerkAuth.test.ts, extend the existing import block (lines 27-32) to add `mockMillOperatorSession` and `mockDualRoleSession`. Append two new `it(...)` cases to the existing `describe('clerkAuth fixture', ...)` block: (a) calls mockMillOperatorSession() then awaits mockAuth() and asserts `result.sessionClaims.metadata.roles` contains 'mill_operator'; (b) calls mockDualRoleSession() then awaits mockAuth() and asserts the roles array contains BOTH 'demo' AND 'mill_operator' (use `.toContain('demo')` AND `.toContain('mill_operator')`). Run `npm test -- --testPathPattern='clerkAuth\.test' --watchAll=false`; expect import-resolution failure. Commit: `test(31-01): add failing mockMillOperatorSession + mockDualRoleSession fixture tests`.

    Step 2 (GREEN): Edit src/test/fixtures/clerkAuth.ts. After the existing `mockUnauthenticatedSession` at line 192-197, append two new exports: `mockMillOperatorSession(): void` setting `mockAuth.mockResolvedValue({ userId: 'u1', sessionClaims: { metadata: { roles: ['mill_operator'] } } })`; and `mockDualRoleSession(): void` setting `mockAuth.mockResolvedValue({ userId: 'u1', sessionClaims: { metadata: { roles: ['demo', 'mill_operator'] } } })`. Run `npm test -- --testPathPattern='clerkAuth\.test' --watchAll=false`; expect all 8+2=10 fixture tests pass. Commit: `feat(31-01): add mill_operator + dual-role session fixtures`.

    Step 3 (REFACTOR): Add JSDoc above each new factory. mockMillOperatorSession: "Seed `mockAuth` to resolve with a session whose roles array contains 'mill_operator'." (one-liner mirroring mockDemoSession). mockDualRoleSession: longer JSDoc citing D-13 as the canonical multi-role coverage path that verifies Array.prototype.includes membership semantics for users with more than one role. Do NOT modify the existing `mockNonDemoSession(role: Exclude<Role, 'demo'> = 'user')` signature — it widens automatically per RESEARCH §"Compile-time check note" (line 695). Re-run tests to confirm no regression. Commit: `refactor(31-01): document new session fixtures`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='clerkAuth\.test' --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function mockMillOperatorSession\(\): void" src/test/fixtures/clerkAuth.ts` returns exactly 1 line
    - `grep -E "^export function mockDualRoleSession\(\): void" src/test/fixtures/clerkAuth.ts` returns exactly 1 line
    - `grep -E "roles: \['mill_operator'\]" src/test/fixtures/clerkAuth.ts` returns at least 1 line (inside mockMillOperatorSession)
    - `grep -E "roles: \['demo', 'mill_operator'\]" src/test/fixtures/clerkAuth.ts` returns at least 1 line (inside mockDualRoleSession)
    - `grep -E "mockNonDemoSession\(role: Exclude<Role, 'demo'> = 'user'\)" src/test/fixtures/clerkAuth.ts` still returns 1 line (signature unchanged — auto-widens per A2)
    - `npm test -- --testPathPattern='clerkAuth\.test' --watchAll=false` exits 0; existing 8 fixture tests still pass AND 2 new tests pass
    - The 2 new test names appear in jest output: "mockMillOperatorSession resolves mockAuth with roles containing mill_operator", "mockDualRoleSession resolves mockAuth with roles containing both demo and mill_operator"
  </acceptance_criteria>
  <done>
    Fixture factories ready for Plan 31-04 consumption; multi-role coverage path verified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Rewrite REQUIREMENTS.md AUTH-02/AUTH-03 and ROADMAP.md Phase 31 SC#2/SC#4 per D-15/D-16/D-17</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 25-28 — current AUTH-01..04 bullets to be edited)
    - .planning/ROADMAP.md (lines 82-86 — current Phase 31 Success Criteria bullets to be edited)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-CONTEXT.md (§"Implementation Decisions" D-04, D-05, D-15, D-16, D-17 — exact replacement text)
    - .planning/phases/31-role-expansion-and-db-infrastructure/31-PATTERNS.md (§"`.planning/REQUIREMENTS.md`" and §"`.planning/ROADMAP.md`" — diff target shapes)
  </read_first>
  <action>
    Step 1: In .planning/REQUIREMENTS.md, replace the AUTH-02 bullet (line ~26) with the new text from D-04: "Mutating server actions (Phase 33: transitions, bulk import) enforce `await requireRole('mill_operator')` as the canonical server-side guard for v2.0 write operations. `/` page-level enforcement is NOT used — any authenticated user may view `/` in read-only mode." Replace the AUTH-03 bullet (line ~27) with the new text from D-05: "Middleware adds `/` to the `auth.protect()` flow only (already covered by the existing `!isPublicRoute(request)` branch). NO `mill_operator` coarse-gate matcher mirroring `/demo/*`." Leave AUTH-01 (line 25) and AUTH-04 (line 28) UNCHANGED. Leave DATA-01 (line 14) and DATA-08 (line 21) UNCHANGED in text — D-16 amends only the ROADMAP success criterion, not the requirement text.

    Step 2: In .planning/ROADMAP.md, edit the Phase 31 Success Criteria block (lines 82-86). Replace SC#2 with the D-15 text: "An authenticated user without `mill_operator` sees `/` in read-only mode (edit affordances hidden); mutating server actions (Phase 33) reject without `mill_operator`." Replace SC#4 with the D-16 text: "`DATABASE_URL` (pooled) and `DATABASE_URL_UNPOOLED` (direct) set in `.env.local`; `drizzle.config.ts` references `DATABASE_URL_UNPOOLED` for migrations. Vercel env-var provisioning deferred to Phase 34 first deploy." Leave SC#1, SC#3, SC#5 UNCHANGED. Do not modify the Phase 31 Goal line, Depends on line, Requirements line, or any other phase's success criteria.

    Step 3: Verify edits with the grep assertions in `<acceptance_criteria>`. Commit (single commit covering both files): `docs(31-01): amend AUTH-02/AUTH-03 and Phase 31 SC#2/SC#4 per D-15/D-16/D-17`.
  </action>
  <verify>
    <automated>grep -E "Mutating server actions \(Phase 33" .planning/REQUIREMENTS.md && grep -E "NO .mill_operator. coarse-gate matcher" .planning/REQUIREMENTS.md && grep -E "read-only mode \(edit affordances hidden\)" .planning/ROADMAP.md && grep -E "Vercel env-var provisioning deferred to Phase 34" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AUTH-01.*mill_operator role string added to the .Role. union" .planning/REQUIREMENTS.md` returns 1 (AUTH-01 unchanged)
    - `grep -c "AUTH-04.*docs/clerk-setup.md.*runbook updated" .planning/REQUIREMENTS.md` returns 1 (AUTH-04 unchanged)
    - `grep -c "AUTH-02.*Mutating server actions" .planning/REQUIREMENTS.md` returns exactly 1 (AUTH-02 rewritten per D-04)
    - `grep -c "AUTH-03.*auth\\.protect\\(\\).*flow only" .planning/REQUIREMENTS.md` returns exactly 1 (AUTH-03 rewritten per D-05)
    - `grep -c "requireRole..mill_operator..*enforced at the page level" .planning/REQUIREMENTS.md` returns 0 (old AUTH-02 text fully removed)
    - `grep -c "Middleware coarse-gate added for ./.* as defense-in-depth" .planning/REQUIREMENTS.md` returns 0 (old AUTH-03 text fully removed)
    - `grep -E "read-only mode \\(edit affordances hidden\\); mutating server actions \\(Phase 33\\) reject" .planning/ROADMAP.md` returns at least 1 line (SC#2 rewritten per D-15)
    - `grep -E "DATABASE_URL_UNPOOLED. \\(direct\\) set in \\\\\\`.env.local\\\\\\`" .planning/ROADMAP.md` returns at least 1 line OR `grep "set in .env.local" .planning/ROADMAP.md` succeeds (SC#4 rewritten per D-16)
    - `grep -c "redirected away from ./. at both the middleware and page level" .planning/ROADMAP.md` returns 0 (old SC#2 text fully removed)
    - `grep -c "set in Vercel env" .planning/ROADMAP.md` returns 0 (old SC#4 "set in Vercel env" claim removed)
    - Other phases' success criteria (Phase 32-35) are unchanged: `grep -c "Phase 32:" .planning/ROADMAP.md` still returns at least 1; line counts before/after the Phase 31 block are stable
  </acceptance_criteria>
  <done>
    Planning docs aligned with locked decisions; D-15, D-16, D-17 are durable in committed text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (RSC) | Client UI consumes `canEdit: boolean` prop computed server-side; never recomputed in the browser |
| Clerk session JWT → server | `sessionClaims.metadata.roles` is read inside `checkRole` via server-only `auth()` from `@clerk/nextjs/server`; JWT integrity is enforced by Clerk's session-token verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | `Role` union — forged role string in `publicMetadata.roles` | accept | Type union is compile-time only; runtime security depends on Clerk JWT integrity (signed by Clerk; dashboard-only writes). No new attack surface introduced. Severity: low. Mitigation reference: existing Clerk JWT verification + JSDoc on `src/lib/auth.ts` documenting server-only nature. |
| T-31-01-02 | Information Disclosure | `checkRole` accidentally imported into a client component | mitigate | `checkRole` lives in `src/lib/auth.ts` which already has the SERVER-ONLY JSDoc disclaimer (lines 7-12). `auth()` from `@clerk/nextjs/server` is server-only and will throw if invoked from the client. Severity: low. Mitigation reference: existing JSDoc + Clerk runtime check; no new code needed. |
| T-31-01-03 | Elevation of Privilege | A bug in `checkRole` returns `true` for a user lacking the role | mitigate | 5 TDD test cases cover all branches (role present, role absent, no metadata, multi-role, unauthenticated). The function is 2 lines; the test surface fully covers its decision logic. Severity: medium. Mitigation reference: src/lib/auth.test.ts new `describe('checkRole', ...)` block (Task 2). |
| T-31-01-04 | Repudiation | REQUIREMENTS.md / ROADMAP.md edits made without traceable rationale | mitigate | Each edit is traceable to a specific decision ID (D-04, D-05, D-15, D-16, D-17) via the CONTEXT.md decision log. Commit message includes the decision IDs. Severity: low (process risk, not security risk). |
</threat_model>

<verification>
After all four tasks complete:
- `npx tsc --noEmit` exits 0 (no type errors anywhere)
- `npm test -- --testPathPattern='auth|clerkAuth' --watchAll=false` exits 0 (all auth + fixture tests pass)
- `grep -l 'mill_operator' src/types/clerk.d.ts src/lib/auth.ts src/lib/auth.test.ts src/test/fixtures/clerkAuth.ts src/test/fixtures/clerkAuth.test.ts` returns all 5 files
- `grep -c 'mill_operator' .planning/REQUIREMENTS.md` >= 4 (mentions in AUTH-01, rewritten AUTH-02, AUTH-04, plus header)
- `grep -c 'read-only mode' .planning/ROADMAP.md` >= 1 (SC#2 amendment)
</verification>

<success_criteria>
- `'mill_operator'` is a member of the `Role` union; `tsc --noEmit` is clean
- `checkRole(role: Role): Promise<boolean>` is exported from `src/lib/auth.ts` and tested with 5 cases including multi-role and unauthenticated branches
- `mockMillOperatorSession()` and `mockDualRoleSession()` factories exist in `src/test/fixtures/clerkAuth.ts` and their contract tests pass
- REQUIREMENTS.md AUTH-02 and AUTH-03 rewritten per D-04 + D-05
- ROADMAP.md Phase 31 SC#2 and SC#4 rewritten per D-15 + D-16
- All existing tests in `src/lib/auth.test.ts` and `src/test/fixtures/clerkAuth.test.ts` continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/31-role-expansion-and-db-infrastructure/31-01-SUMMARY.md`.
</output>