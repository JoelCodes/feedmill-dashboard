---
phase: 20-clerk-foundation-setup
plan: 03
type: execute
wave: 1
depends_on: ["20-01", "20-02"]
files_modified: [".env.local", ".env.example"]
autonomous: true
gap_closure: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "Unauthenticated users are redirected to /sign-in (local page), not Clerk hosted page"
    - "Theme toggle on sign-in page switches Clerk component colors"
  artifacts:
    - path: ".env.local"
      provides: "Clerk sign-in/sign-up URL configuration"
      contains: "NEXT_PUBLIC_CLERK_SIGN_IN_URL"
    - path: ".env.example"
      provides: "Documented template for Clerk URL configuration"
      contains: "NEXT_PUBLIC_CLERK_SIGN_IN_URL"
  key_links:
    - from: "clerkMiddleware auth.protect()"
      to: "/sign-in page"
      via: "NEXT_PUBLIC_CLERK_SIGN_IN_URL env var"
      pattern: "NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in"
---

<objective>
Close UAT gap: Add missing Clerk URL environment variables so auth.protect() redirects to custom sign-in page instead of Clerk hosted pages.

Purpose: Without NEXT_PUBLIC_CLERK_SIGN_IN_URL and NEXT_PUBLIC_CLERK_SIGN_UP_URL, Clerk defaults to its hosted authentication pages at accounts.clerk.com. This prevents users from seeing the themed sign-in page with CGM Dashboard branding.

Output: Updated .env.local and .env.example with Clerk URL configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-clerk-foundation-setup/20-01-SUMMARY.md
@.planning/phases/20-clerk-foundation-setup/20-02-SUMMARY.md
@.planning/phases/20-clerk-foundation-setup/20-UAT.md
@.planning/debug/clerk-redirect-hosted-signin.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Clerk URL environment variables</name>
  <files>.env.local, .env.example</files>
  <action>
    Add the missing Clerk URL environment variables to configure custom sign-in/sign-up page routing.

    For .env.local, append after existing CLERK_SECRET_KEY line:
    ```
    NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
    NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
    ```

    For .env.example, append after existing CLERK_SECRET_KEY line with documentation:
    ```
    # Clerk custom page URLs (required for custom sign-in/sign-up pages)
    NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
    NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
    ```

    Gap reason: Per Clerk documentation, these variables tell Clerk where the SignIn and SignUp components are hosted. Without them, auth.protect() in middleware redirects to Clerk's hosted pages instead of the custom /sign-in route.
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_CLERK_SIGN_IN_URL" .env.local .env.example | grep -v ":0"</automated>
  </verify>
  <done>Both .env.local and .env.example contain NEXT_PUBLIC_CLERK_SIGN_IN_URL and NEXT_PUBLIC_CLERK_SIGN_UP_URL variables</done>
</task>

<task type="auto">
  <name>Task 2: Verify redirect behavior</name>
  <files></files>
  <action>
    Build the application and verify the configuration is valid:
    1. Run npm run build to ensure no environment variable errors
    2. Verify the sign-in page route still compiles correctly

    The actual redirect behavior will be verified during UAT re-test, but we confirm here that:
    - Environment variables are syntactically correct
    - No build errors from the new configuration
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build succeeds with new environment variables configured</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Configuration-only change, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Information Disclosure | .env.local | mitigate | .env.local already gitignored (verified in 20-01); URL paths are non-sensitive |
</threat_model>

<verification>
1. grep confirms NEXT_PUBLIC_CLERK_SIGN_IN_URL in both .env.local and .env.example
2. grep confirms NEXT_PUBLIC_CLERK_SIGN_UP_URL in both .env.local and .env.example
3. npm run build succeeds without errors
</verification>

<success_criteria>
- .env.local contains NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
- .env.local contains NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
- .env.example documents both variables with comments
- Build completes successfully
- UAT Test 4 (Theme Toggle on Sign-in Page) can be re-verified
</success_criteria>

<output>
After completion, create `.planning/phases/20-clerk-foundation-setup/20-03-SUMMARY.md`
</output>
