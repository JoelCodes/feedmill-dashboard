---
phase: 36-close-gap-build-01-void-cast-phase-35-verification
plan: 04
type: execute
wave: 3
depends_on:
  - 36-01
  - 36-02
  - 36-03
files_modified:
  - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md
autonomous: true
requirements:
  - KPI-01
  - KPI-02
  - KPI-03
  - KPI-04
  - KPI-05
  - KPI-06
  - KPI-07
  - KPI-08
tags:
  - validation
  - nyquist
  - documentation
  - phase-35
  - re-classification
user_setup: []

must_haves:
  truths:
    - "Phase 35 Wave-0 tests are green at re-classification time (`npm test -- --testPathPattern='kpis|formula-mix|format-dwell'` exits 0)"
    - "`npm run build` exits 0 at re-classification time (BUILD-01 closed by Plan 01)"
    - "`35-UAT.md` exists with frontmatter `status: closed` (all 10 UATs passed in Plan 03)"
    - "`35-VERIFICATION.md` exists with frontmatter `status: verified` (Plan 02 + Plan 03 sync complete)"
    - "`35-VALIDATION.md` frontmatter is flipped from `draft/false/false` to `complete/true/true` plus added `updated: 2026-05-15`"
    - "`35-VALIDATION.md` body Approval line is flipped from `pending` to `approved 2026-05-15`"
  artifacts:
    - path: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md"
      provides: "Re-classified Nyquist validation contract for Phase 35"
      contains: "status: complete"
  key_links:
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md frontmatter"
      to: "Three preconditions: KPI test suite green + npm run build green + 35-UAT.md status:closed + 35-VERIFICATION.md status:verified"
      via: "automated grep gates run by this plan's Task 1 BEFORE the frontmatter edit"
      pattern: "status: complete|nyquist_compliant: true|wave_0_complete: true"
---

<objective>
Re-classify `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` from the current draft state (`status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`, `Approval: pending`) to the target completed state (`status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `updated: 2026-05-15`, `Approval: approved 2026-05-15`).

The re-classification is a 3-line frontmatter mutation plus a one-line Approval flip. It is gated by FOUR preconditions verified by automated checks at the start of Task 1:

1. `npm test -- --testPathPattern='kpis|formula-mix|format-dwell'` exits 0 (Phase 35 Wave-0 test infrastructure still green)
2. `npm run build` exits 0 (BUILD-01 closed by Plan 01)
3. `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` frontmatter contains `status: closed` (Plan 03's UAT execution passed)
4. `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` frontmatter contains `status: verified` and `gaps: []` (Plans 02 and 03 finished cleanly)

If ANY precondition fails, Task 1 aborts with a clear error message and the chain halts. This prevents a false-positive re-classification on a red branch (Risk R3 from 36-RESEARCH.md).

Purpose: Close ROADMAP Phase 36 SC#5 (`35-VALIDATION.md` frontmatter is updated to `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`). Provides the final piece of evidence that Phase 35 is Nyquist-compliant and fully validated.

Output:
- Edited `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` frontmatter (3 status flips + 1 added `updated` field)
- Edited body line `**Approval:** pending` → `**Approval:** approved 2026-05-15 (post Phase 36 verification + UAT; see 35-VERIFICATION.md status: verified + 35-UAT.md status: closed)`
- One atomic commit `docs(36-04): re-classify 35-VALIDATION.md to complete (Nyquist gates green)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md
@.planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md

<interfaces>
Current state of 35-VALIDATION.md frontmatter (lines 1-8, read verbatim):

  ---
  phase: 35
  slug: kpi-sections-and-role-specific-metrics
  status: draft
  nyquist_compliant: false
  wave_0_complete: false
  created: 2026-05-14
  ---

Target state (per 36-RESEARCH.md §Investigation 4 §Target state + 36-PATTERNS.md section 5):

  ---
  phase: 35
  slug: kpi-sections-and-role-specific-metrics
  status: complete
  nyquist_compliant: true
  wave_0_complete: true
  created: 2026-05-14
  updated: 2026-05-15
  ---

Reference clean-complete frontmatter (from .planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md:1-9):

  ---
  phase: 32
  slug: schema-migrations-and-seed-data
  status: complete
  nyquist_compliant: true
  wave_0_complete: true
  created: 2026-05-13
  audited: 2026-05-13
  ---

Body change: line 99 of 35-VALIDATION.md currently reads:

  **Approval:** pending

Target form (matches the 34-VALIDATION.md verbose variant per 36-PATTERNS.md section 5):

  **Approval:** approved 2026-05-15 (post Phase 36 verification + UAT; see 35-VERIFICATION.md status: verified + 35-UAT.md status: closed)

Precondition grep patterns (for Task 1 gate enforcement):

  # P1: KPI test suite green
  npm test -- --testPathPattern='kpis|formula-mix|format-dwell' (exit 0 required)

  # P2: Build green
  npm run build (exit 0 required)

  # P3: UAT closed
  grep -E "^status: closed" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md (1 match required)

  # P4: VERIFICATION verified, no gaps
  grep -E "^status: verified" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md (1 match required)
  grep -E "^gaps: \[\]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md (1 match required)

Optionally update body Wave 0 checklist (lines 51-58 of 35-VALIDATION.md) — flip the `[ ]` boxes to `[x]` for the test files that actually shipped. Prefer minimal-edit policy per 36-RESEARCH.md §Investigation 4 §Body-section updates; only flip the boxes that map to files that exist in src/db/queries/__tests__/, src/lib/__tests__/, and src/components/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Nyquist preconditions then flip 35-VALIDATION.md frontmatter + Approval line</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md</files>
  <read_first>
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md (full file — confirms the current draft frontmatter at lines 1-8, the Wave 0 checklist at lines 51-58, and the Approval pending line at line 99)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md (frontmatter — confirms status: closed; if status is gaps_flagged, this task MUST abort)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md (frontmatter — confirms status: verified and gaps: []; if status != verified or gaps contains entries, this task MUST abort)
    - .planning/phases/32-schema-migrations-and-seed-data/32-VALIDATION.md (lines 1-9 — the canonical clean `status: complete` frontmatter shape)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md (Investigation 4 — preconditions table and target state)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md (section 5 — adapted diff)
  </read_first>
  <behavior>
    Phase 1 — Precondition gate (Task aborts if any check fails):

      1. Run `npm test -- --testPathPattern='kpis|formula-mix|format-dwell'`. Exit code MUST be 0.
      2. Run `npm run build`. Exit code MUST be 0.
      3. Grep `35-UAT.md` for `^status: closed`. MUST return exactly 1 match.
      4. Grep `35-VERIFICATION.md` for `^status: verified`. MUST return exactly 1 match.
      5. Grep `35-VERIFICATION.md` for `^gaps: \[\]`. MUST return exactly 1 match.

    If ANY of the 5 checks fails, print a clear diagnostic ("Precondition N failed: <reason>") and exit non-zero. Do NOT proceed to Phase 2. Do NOT modify any file.

    Phase 2 — Frontmatter + Approval edit (only if all preconditions pass):

      6. Apply the frontmatter diff to `35-VALIDATION.md`:
         - line 4: `status: draft` → `status: complete`
         - line 5: `nyquist_compliant: false` → `nyquist_compliant: true`
         - line 6: `wave_0_complete: false` → `wave_0_complete: true`
         - line 8 (just before the closing `---`): add new line `updated: 2026-05-15`
      7. Apply the body Approval edit:
         - line 99 (or current location of `**Approval:** pending`): replace with `**Approval:** approved 2026-05-15 (post Phase 36 verification + UAT; see 35-VERIFICATION.md status: verified + 35-UAT.md status: closed)`
      8. (Optional, minimal-edit) Flip the Wave 0 checklist boxes at lines 51-58 from `[ ]` to `[x]` for the test files that exist on disk. Confirm existence by `test -f` before flipping each box.

    Phase 3 — Verification:

      9. Re-read `35-VALIDATION.md` frontmatter and confirm the three target values are present.
      10. Re-read the Approval line and confirm the new form is present.
      11. Confirm the body is otherwise unchanged (no accidental edits to the §Sampling Rate / §Per-Task Verification Map / §Wave 0 Requirements section content beyond the optional checkbox flips).
  </behavior>
  <action>
    Execute the precondition gate, then the edit, then verification. The end-to-end flow:

    1. Run the 5 precondition checks in order. If any fails, abort with `echo "Plan 36-04 ABORT: precondition <N> failed: <reason>" >&2 && exit 1` and do NOT touch `35-VALIDATION.md`. The orchestrator will surface this and the chain halts.

    2. If all 5 preconditions pass, apply the three frontmatter status flips and the `updated:` insertion. The diff is the four-line change documented in 36-PATTERNS.md section 5.

    3. Replace the body `**Approval:** pending` line with the new approved form (verbose variant matching the 34-VALIDATION.md pattern, but cite Phase 36 instead of phase-internal review).

    4. Confirm the post-edit state via three grep checks:
       - `grep "^status: complete" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md`
       - `grep "^nyquist_compliant: true" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md`
       - `grep "^wave_0_complete: true" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md`
       - `grep "^\\*\\*Approval:\\*\\* approved 2026-05-15" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md`

       Each must return exactly 1 match.

    5. Commit as `docs(36-04): re-classify 35-VALIDATION.md to complete (Nyquist gates green)`.
  </action>
  <verify>
    <automated>grep -E "^(status: complete|nyquist_compliant: true|wave_0_complete: true|updated: 2026-05-15)$" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md | wc -l &amp;&amp; grep "^\*\*Approval:\*\* approved 2026-05-15" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md | wc -l &amp;&amp; grep -cE "^(status: draft|nyquist_compliant: false|wave_0_complete: false)$" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md</automated>
    Expected: first command returns 4 (all four target frontmatter lines present); second command returns 1 (the new Approval line); third command returns 0 (none of the old draft tokens remain).
  </verify>
  <acceptance_criteria>
    - All 5 preconditions ran and returned green; OR the task aborted cleanly with a precondition failure diagnostic and the file is untouched.
    - On success: `grep "^status: complete" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` returns 1 match.
    - On success: `grep "^nyquist_compliant: true" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` returns 1 match.
    - On success: `grep "^wave_0_complete: true" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` returns 1 match.
    - On success: `grep "^updated: 2026-05-15" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` returns 1 match.
    - On success: the old draft tokens (`status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`) no longer appear. Run `grep -cE "^(status: draft|nyquist_compliant: false|wave_0_complete: false)$" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md` returns 0.
    - On success: the Approval line is `**Approval:** approved 2026-05-15 (post Phase 36 verification + UAT; see 35-VERIFICATION.md status: verified + 35-UAT.md status: closed)`. The `pending` token no longer appears on the Approval line.
    - On success: a commit lands with the message `docs(36-04): re-classify 35-VALIDATION.md to complete (Nyquist gates green)`.
    - On failure (any precondition red): no file is modified, no commit lands, and the chain halts with a diagnostic.
  </acceptance_criteria>
  <done>Either: (success) `35-VALIDATION.md` is re-classified to complete and a commit is recorded; OR (abort) the chain halts cleanly with a clear precondition-failure diagnostic and `35-VALIDATION.md` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase task modifies a Markdown documentation artifact (frontmatter + one body line). No code, no data, no external interfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VALIDATION.md | mitigate | The re-classification is a status mutation that downstream tools (gsd-sdk, audit) treat as a trust signal. The 5-precondition gate (KPI tests green, build green, UAT closed, VERIFICATION verified + no gaps) ensures the mutation only happens when the underlying evidence is in place. This is the documented mitigation for Risk R3 in 36-RESEARCH.md. No CWE applies — this is documentation hygiene, not a runtime threat. |

No high-severity threats introduced. `security_enforcement: true` satisfied via this minimal threat model per Phase 36 §security_threat_model note.
</threat_model>

<verification>
- All 5 preconditions verified before any file edit.
- Post-edit grep gates confirm the target frontmatter values are present and the old draft tokens are absent.
- Post-edit grep gate confirms the Approval line is the new approved form.
- Git log shows one new docs commit.
</verification>

<success_criteria>
- ROADMAP Phase 36 SC#5 satisfied: `35-VALIDATION.md` frontmatter is updated to `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true` — re-classified after Wave-0 test confirmation (`npm test -- kpis`) and human UAT.
- The re-classification is gated by 5 explicit preconditions; no false-positive flip is possible on a red branch (Risk R3 mitigated).
- The body Approval line is updated with a cross-reference to Plan 36-02 (35-VERIFICATION.md) and Plan 36-03 (35-UAT.md) for traceability.
- Minimal-edit policy honored — only the frontmatter, the Approval line, and (optionally) the Wave 0 checkboxes are touched. The §Sampling Rate, §Per-Task Verification Map (now drift-tolerant), §Wave 0 Requirements, and §Validation Sign-Off body sections remain as authored.
</success_criteria>

<output>
Create `.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-04-SUMMARY.md` summarizing:
- The precondition gate result (all 5 green) — include the exact commands and exit codes
- The frontmatter diff applied (3 status flips + `updated` addition)
- The Approval line edit
- Confirmation that `35-VALIDATION.md` now reflects the verified state of Phase 35 and the v2.0 milestone gate is one step closer to passing
</output>
