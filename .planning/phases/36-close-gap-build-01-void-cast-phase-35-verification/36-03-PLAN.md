---
phase: 36-close-gap-build-01-void-cast-phase-35-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
files_modified:
  - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md
  - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md
autonomous: false
requirements:
  - KPI-01
  - KPI-02
  - KPI-03
  - KPI-04
  - KPI-05
  - KPI-06
  - KPI-07
  - KPI-08
  - PROD-06
tags:
  - uat
  - human-verification
  - kpi
  - phase-35
  - tz-cookie
  - sql-retest
user_setup: []

must_haves:
  truths:
    - "`.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` exists with frontmatter and 10 UAT scenarios (UAT-1..UAT-10)"
    - "After operator UAT execution, every UAT scenario has a recorded `Observed result: pass` (or `fail` with severity)"
    - "UAT-3 (7-day trend chart post-SQL-fix retest) confirms getSevenDayTrend runs cleanly under multiple browser timezones with NO 42803 GROUP BY error in server logs"
    - "35-UAT.md frontmatter `status: closed` after all 10 UATs pass; `35-VERIFICATION.md retest_outcome.results` bullets populated with one summary line per UAT"
  artifacts:
    - path: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md"
      provides: "Human UAT execution record for Phase 35 (preconditions + 10 scenarios + observed results)"
      contains: "## Preconditions"
  key_links:
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md UAT-3"
      to: "src/db/queries/kpis.ts:313 sql.raw() tz inlining + commits ba54b4a..24b34bf"
      via: "operator confirms chart renders + server logs have no 42803 error under at least 2 IANA timezones"
      pattern: "UAT-3|42803|sql\\.raw"
    - from: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md status: closed"
      to: ".planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md retest_outcome.results"
      via: "Task 3 of this plan updates the 35-VERIFICATION.md frontmatter bullets after Task 2 records all UAT outcomes"
      pattern: "retest_outcome|results:"
---

<objective>
Author `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` — the human UAT contract for Phase 35 — then pause for the operator to execute the 10 UAT scenarios against `npm run dev`. After the operator records each scenario's observed result, update both files: (a) set `status: closed` in `35-UAT.md` frontmatter, (b) backfill the `retest_outcome.results` bullet list in `35-VERIFICATION.md` with one summary line per UAT scenario.

The doc structure mirrors the analog at `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md`. The 10 UAT scenarios are derived from `36-RESEARCH.md §Investigation 3` and are mapped to the 8 KPI-* requirements + the carried-forward decisions D-10, D-12, D-14, D-17.

This plan contains the **only `checkpoint:human-verify` in Phase 36** — Task 2 pauses the auto-chain for operator UAT execution. UAT-3 (7-day trend chart post-SQL-fix retest) is the **mandatory-pass gate**: if it fails, the chain halts and the failure surfaces as a gap in `35-VERIFICATION.md §Gaps Summary` for orchestrator decision before Plan 04 runs.

Purpose: Close ROADMAP Phase 36 SC#4 (`35-UAT.md` exists with a written human-UAT pass record covering KPI strip visual rendering, tz cookie flow, 7-day trend chart post-SQL-fix retest, overdue badge rendering, formula-mix breakdown). Retroactively close the Nyquist sampling gap from the 5 post-phase SQL fix commits that never ran through Phase 35's closing UAT cycle.

Output:
- New file `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` with the 10-scenario UAT contract
- After UAT execution: updated frontmatter `status: closed` + populated `35-VERIFICATION.md retest_outcome.results` bullets
- Three atomic commits: (1) author UAT skeleton, (2) record operator UAT results, (3) sync VERIFICATION.md retest_outcome
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md
@.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md
@.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md

<interfaces>
UAT scenario specifications (from 36-RESEARCH.md §Investigation 3 §UAT scenarios, table at lines 290-301):

  UAT-1 — KPI strip visual rendering (Completed Today mill-wide + per-line, Pending Backlog, Formula Mix) — KPI-01, 02, 04, 05
  UAT-2 — Tz cookie flow + America/Chicago fallback — KPI-01, KPI-06
  UAT-3 — 7-day trend chart post-SQL-fix retest (MANDATORY PASS; covers ba54b4a..4d61194; capture server log + chart screenshot) — KPI-06
  UAT-4 — Empty state for 7-day chart (< 7 days of data → "Not enough data yet") — KPI-06
  UAT-5 — Overdue badge renders when earlyDeliveryDate < today AND state != 'Completed' — KPI-08
  UAT-6 — Formula mix sums to 100%; em-dash null-state if all uncategorized — KPI-05
  UAT-7 — KPI-03 per-column header strip values match cards below — KPI-03
  UAT-8 — Polling preserves KPI freshness (30s tick re-renders + skeleton flash) — KPI-01..08
  UAT-9 — BlockedExceptionList dwell-time sort + row-click drawer open — KPI-07
  UAT-10 — Coexistence: BlockedAlertBand (sticky top) AND BlockedExceptionList (bottom) both render per D-10 — KPI-07, PROD-06

Preconditions (from 36-RESEARCH.md §Investigation 3 §Preconditions):
1. `npm run build` exits 0 (BUILD-01 fixed by Plan 01) — gates the whole UAT pass
2. `npm run dev` running at http://localhost:3000
3. Seed DB has 33 orders with earlyDeliveryDate populated across today ±5 days
4. Signed in as any authenticated user (KPIs are read-only per D-17)
5. At least one Blocked order with a recent to_state='Blocked' event
6. At least one Completed order today in each of the 3 lines (Premix, Excel, CGM)

Per-test format (from 34-HUMAN-UAT.md §T1..T12 template):
- ### UAT-N. <Scenario name>
- **Steps:** numbered list
- **Pass criteria:** bulleted
- **Fail criteria:** bulleted
- **Observed result:** `pass` / `fail` (filled in by operator)
- **Severity:** only if fail
- **Reported:** only if fail or amended

Frontmatter shape (from 34-HUMAN-UAT.md:1-9, adapted per 36-PATTERNS.md section 4):
  ---
  phase: 35-kpi-sections-and-role-specific-metrics
  type: human-uat
  created: 2026-05-15
  status: gaps_flagged   ← initial; flips to `closed` after Task 2 if all pass
  updated: 2026-05-15
  ---

UAT-3 execution discipline (per 36-RESEARCH.md §Investigation 3 §UAT execution discipline + §Investigation 5):
- Switch browser tz to at least 2 distinct IANA values (e.g., America/Chicago → America/Los_Angeles) using DevTools "Sensors" panel
- Confirm SevenDayTrendChart re-renders day boundaries per the new tz
- Inspect server logs for the string `42803` (Postgres GROUP BY error code). MUST be absent.
- Capture: server log excerpt (no 42803) + rendered chart screenshot (saved to /tmp or attached inline by reference)

Failure handling: If any UAT fails, do NOT auto-advance to Plan 04. The failing scenario must:
1. Be marked `Observed result: fail` with a `Severity:` field
2. Be added as a gap entry in `35-VERIFICATION.md §Gaps Summary` (revert that section from "No remaining gaps." to a gap listing)
3. Update `35-VERIFICATION.md` frontmatter `gaps: []` to populate the failing scenario name
4. Update `35-UAT.md` frontmatter to `status: gaps_flagged`
5. Halt the chain; surface to orchestrator. Plan 04 cannot run until the gap is closed (likely via a new sub-plan or a Phase 35 reopen).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 35-UAT.md skeleton with preconditions and 10 UAT scenario sections (no observed results yet)</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md</files>
  <read_first>
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md (full file — the per-test template structure, especially T1/T2 as the canonical layout and T3/T9/T10 as examples of failed-then-resolved tests with Reported/Retest blocks)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-RESEARCH.md (Investigation 3 — preconditions, the 10 UAT scenarios with mapped requirements, UAT-3 execution discipline; Investigation 5 — the post-phase SQL fix details and what UAT-3 must verify)
    - .planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-PATTERNS.md (section 4 — adapted frontmatter and per-test format)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UI-SPEC.md (full file — visual contract for UAT-1, UAT-2, UAT-5, UAT-7, UAT-9 visual claims)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-CONTEXT.md (D-10, D-12, D-14, D-17 — informs scenario rationales for UAT-7, UAT-6, UAT-10)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-LEARNINGS.md (em-dash null-state decision for UAT-6; sql.raw tz inlining surprise for UAT-3)
  </read_first>
  <action>
    Create the file `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` with the following structure:

    1. **YAML frontmatter** with `phase: 35-kpi-sections-and-role-specific-metrics`, `type: human-uat`, `created: 2026-05-15`, `status: gaps_flagged` (initial — Task 2 flips to `closed`), `updated: 2026-05-15`. Do NOT include `inherited_from` (Phase 35 has no inherited UAT).

    2. **H1 title** "Phase 35 Human UAT Contract" and a short intro paragraph stating the doc captures the manual UAT for Phase 35 (KPI Sections and Role-Specific Metrics) and that all tests must be executed against `npm run dev` before the phase verification can flip from gaps_flagged to closed.

    3. **`## Preconditions`** with the 6 preconditions enumerated in the <interfaces> block above. Number each.

    4. **`## Tests`** section header, then 10 `### UAT-N. <Scenario name>` subsections in the order UAT-1..UAT-10. For EACH scenario:
       - **Steps:** 3-5 numbered steps describing what the operator does
       - **Pass criteria:** bulleted; reference the UI-SPEC visual contract or behavioral expectation
       - **Fail criteria:** bulleted; what would mean the scenario fails
       - **Observed result:** `<pending operator UAT execution>` (placeholder for Task 2 to overwrite)
       - For UAT-3 specifically, also include a `**UAT-3 execution discipline:**` block (between Pass criteria and Fail criteria) detailing the multi-tz switch, the 42803 log absence check, and the screenshot + log excerpt capture requirement. Cite commits ba54b4a..4d61194 and the load-bearing `sql.raw()` tz inlining at src/db/queries/kpis.ts:313.

       Specific scenario content (use the 36-RESEARCH.md §Investigation 3 §UAT scenarios table for the canonical "Maps to" + "Why human" details):

       - **UAT-1. KPI strip visual rendering (Completed Today mill-wide + per-line, Pending Backlog, Formula Mix)** — Maps to KPI-01, KPI-02, KPI-04, KPI-05. Pass: 6 cards render in spec order per 35-UI-SPEC.md.
       - **UAT-2. Tz cookie flow + America/Chicago fallback** — Maps to KPI-01, KPI-06. Pass: DevTools shows `tz` cookie set to operator's IANA tz; on first render before TzBootstrap fires, fallback `America/Chicago` is used; on second render cookie is set; "today" KPI uses local midnight.
       - **UAT-3. 7-day trend chart post-SQL-fix retest (MANDATORY PASS — covers ba54b4a..4d61194)** — Maps to KPI-06. Pass: SevenDayTrendChart renders 0..7 bars; day boundaries follow operator tz; NO `42803` GROUP BY error in server logs; Network tab shows successful RSC payload. **UAT-3 execution discipline:** switch browser tz via DevTools Sensors (e.g., America/Chicago → America/Los_Angeles); confirm chart re-renders; tail server logs and grep for `42803` (MUST be absent); capture chart screenshot + log excerpt. Cite final fix at src/db/queries/kpis.ts:313 `sql.raw()` tz inlining (commits ba54b4a..24b34bf).
       - **UAT-4. Empty state for 7-day chart** — Maps to KPI-06. Pass: query a tz/date range where fewer than 7 days have completed orders; "Not enough data yet" copy appears (not a broken chart).
       - **UAT-5. Overdue badge renders when earlyDeliveryDate < today AND state != 'Completed'** — Maps to KPI-08. Pass: orders with past earlyDeliveryDate show the badge; orders with future earlyDeliveryDate do NOT show the badge.
       - **UAT-6. Formula mix sums to 100%; em-dash null-state if all uncategorized** — Maps to KPI-05. Pass: percentages sum to 100% over categorized orders; if uncategorized orders exist, the "N uncategorized" footnote appears OR the em-dash "—" null-state if all are uncategorized (D-12 NULLIF + em-dash null-state per 35-LEARNINGS.md).
       - **UAT-7. KPI-03 per-column header strip values match cards below** — Maps to KPI-03. Pass: each MillColumn header shows `{N} orders — {completed} / {total} lbs` and the values match the cards in the column. Note client-side derivation per D-14/OQ-2.
       - **UAT-8. Polling preserves KPI freshness (30s tick re-renders + skeleton flash)** — Maps to KPI-01..08. Pass: wait 30s on the dashboard, observe KPI cards/chart re-render after polling tick; KPI Suspense skeleton flashes briefly.
       - **UAT-9. BlockedExceptionList dwell-time sort + row-click drawer open** — Maps to KPI-07. Pass: rows sorted oldest-block-first (longest dwell at top); click a row → drawer opens via `?order=`.
       - **UAT-10. Coexistence: BlockedAlertBand (sticky top) AND BlockedExceptionList (bottom) both render per D-10** — Maps to KPI-07, PROD-06. Pass: both surfaces render when blocked orders exist; band is sticky-top terse chips; list is the bottom sortable table; neither replaces the other.

    5. **`## Summary`** section with a placeholder table:
       ```
       total: 10
       passed: <pending>
       issues: <pending>
       pending: 10
       skipped: 0
       blocked: 0
       deferred: 0
       ```
       and a `### Pass / Issue Breakdown` table with 10 rows (UAT-1..UAT-10) and a column "Result (post-UAT)" with `<pending>` placeholders. Task 2 will overwrite these.

    6. **`## Gaps`** section (empty placeholder — populated by Task 2 only if any UAT fails).

    7. **`## Deferred Items`** section listing the KPI SQL integration smoke test as a v2.1 candidate (per 36-RESEARCH.md §Investigation 5 §Retest method and the 4d61194 commit message).

    Commit as `docs(36-03): author 35-UAT.md skeleton (10 scenarios, UAT-3 mandatory-pass gate)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md &amp;&amp; grep -c '^### UAT-[0-9]' .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md &amp;&amp; grep -E "MANDATORY|42803|ba54b4a|sql\.raw" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md | head -5 &amp;&amp; grep -c "Observed result" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md</automated>
    Expected: file exists; exactly 10 UAT subsection headers; UAT-3 discipline block contains at least one of MANDATORY, 42803, ba54b4a, sql.raw; "Observed result" appears 10 times (one per scenario).
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` exists.
    - Frontmatter has `phase: 35-kpi-sections-and-role-specific-metrics`, `type: human-uat`, `status: gaps_flagged`. Run `grep -E "^(phase|type|status):" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` and verify all three tokens are present.
    - All 10 UAT scenario headers exist. Run `grep -c "^### UAT-[0-9]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 10.
    - UAT-3 contains the mandatory-pass discipline block. Run `grep -A20 "^### UAT-3" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md | grep -E "MANDATORY|42803|sql\.raw|ba54b4a"` returns at least one match.
    - Each UAT scenario has an `**Observed result:**` line with a `<pending operator UAT execution>` placeholder. Run `grep -c "<pending operator UAT execution>" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 10.
    - A commit lands with message `docs(36-03): author 35-UAT.md skeleton (10 scenarios, UAT-3 mandatory-pass gate)`.
  </acceptance_criteria>
  <done>The 35-UAT.md skeleton exists with 10 scenarios, preconditions, UAT-3 mandatory-pass discipline, and the placeholder Observed result lines awaiting operator entry. The commit is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator executes the 10 UAT scenarios and records observed results in 35-UAT.md</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md</files>
  <what-built>
    A 10-scenario human UAT contract is in place at `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md`. The doc lists 6 preconditions, 10 numbered UAT scenarios (UAT-1..UAT-10) with steps + pass/fail criteria, and `Observed result: <pending operator UAT execution>` placeholders.

    Also in place from Plan 01: BUILD-01 closed (`npm run build` exits 0). Plan 02 emitted `35-VERIFICATION.md` with placeholder `retest_outcome.results` bullets awaiting these UAT outcomes.
  </what-built>
  <how-to-verify>
    **Preconditions checklist (must all be TRUE before starting UAT):**

    1. Run `npm run build` and confirm exit 0 (BUILD-01 fix from Plan 01 is live).
    2. Run `npm run dev` and confirm the dev server is listening at http://localhost:3000.
    3. Confirm seed DB has 33 orders with `earlyDeliveryDate` populated across today ±5 days. If not, run `npm run db:seed` to refresh.
    4. Confirm at least one Blocked order with a recent `to_state='Blocked'` event exists (use the Block flow on any Pending/Mixing order if not).
    5. Confirm at least one Completed order today in each of the 3 lines (Premix, Excel, CGM).
    6. Sign in as any authenticated user (KPIs are read-only per D-17).

    **Execute UAT-1 through UAT-10 in order.** For each scenario:

    1. Open `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md`. Locate the scenario.
    2. Follow the **Steps** section.
    3. Compare observed behavior against the **Pass criteria** and **Fail criteria**.
    4. Edit the scenario's `**Observed result:** <pending operator UAT execution>` line to either:
       - `**Observed result:** pass` — if pass criteria met
       - `**Observed result:** fail` plus a new `**Severity:**` line (`blocker` / `major` / `minor` / `cosmetic`) plus a `**Reported:** <one-paragraph diagnosis>` plus the URLs/screenshots/log excerpts you captured

    **UAT-3 is the mandatory-pass gate.** When you reach UAT-3:
    - Switch browser tz to a non-default IANA value (e.g., open DevTools → "More tools" → "Sensors" → Location → pick a different region OR set `Intl.DateTimeFormat().resolvedOptions().timeZone` override).
    - Reload the dashboard.
    - Confirm SevenDayTrendChart re-renders with day boundaries shifted to the new tz.
    - In a separate terminal, tail the server log: `npm run dev 2>&1 | grep -i "42803\|GROUP BY"`. The grep should return NO matches during normal navigation.
    - Capture: chart screenshot (save to `/tmp/uat-3-chart-<tz>.png`) and server log excerpt (save to `/tmp/uat-3-log.txt`).
    - Switch tz again (e.g., back to America/Chicago) and confirm chart re-renders again with the original boundaries.
    - Record `**Observed result:** pass` only if BOTH tz renders correctly AND no 42803 appears in logs.

    **After all 10 UATs are recorded:**

    1. Update `35-UAT.md` frontmatter: change `status: gaps_flagged` → `status: closed` IF AND ONLY IF all 10 UATs show `**Observed result:** pass`. If any FAIL, leave `status: gaps_flagged`.
    2. Update the `## Summary` block: set `passed: <count>`, `issues: <count>`, `pending: 0`.
    3. Update the `### Pass / Issue Breakdown` table: fill the "Result (post-UAT)" column with one-line summaries per scenario.
    4. If any UAT failed: populate the `## Gaps` section with a gap entry (truth / status / reason / severity / test / artifacts) modeled on `34-HUMAN-UAT.md §Gaps` and update `35-VERIFICATION.md` frontmatter `gaps:` from `[]` to a list naming the failing UATs + revert `35-VERIFICATION.md §Gaps Summary` body section from "No remaining gaps." to a gap listing.
    5. Commit as `docs(36-03): record 35-UAT.md operator results (<N> pass / <M> fail)`.

    **HALT THE CHAIN if UAT-3 fails or if any other UAT fails.** Surface the gap to the orchestrator. Plans 04 and 05 cannot run until the gap is closed.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `uat passed` — all 10 UATs recorded as `pass`; `35-UAT.md status: closed`; ready for Task 3 to sync `35-VERIFICATION.md`
    - `uat partial: <list-of-failing-uats>` — at least one UAT failed; chain HALTS for orchestrator decision (e.g., `uat partial: UAT-3, UAT-6`)
    - `uat blocked: <reason>` — UAT cannot proceed (e.g., dev server won't start, seed data missing); chain HALTS
  </resume-signal>
  <acceptance_criteria>
    - Operator has personally executed every UAT-N scenario against the live dashboard at http://localhost:3000.
    - Every `**Observed result:**` line in `35-UAT.md` has been updated from `<pending operator UAT execution>` to either `pass` or `fail` (with severity + report on fail). Run `grep -c "<pending operator UAT execution>" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 0.
    - UAT-3 result is `pass` (mandatory-pass gate); if `fail`, the chain HALTS and Plan 04 does not run.
    - The `## Summary` block has `passed`, `issues`, `pending: 0` populated.
    - The `### Pass / Issue Breakdown` table has the "Result (post-UAT)" column populated for all 10 rows.
    - If all 10 pass: `35-UAT.md` frontmatter `status: closed`. If any fail: `status: gaps_flagged` and a gap entry is added to `## Gaps` + a corresponding gap appears in `35-VERIFICATION.md`.
    - A commit lands recording the operator results.
  </acceptance_criteria>
  <action>Operator-driven manual UAT: pause here for the operator to execute the 6 preconditions and 10 UAT scenarios per the how-to-verify block. The operator edits `35-UAT.md` in place to record `**Observed result:** pass` (or `fail` with severity + report) for each scenario, then updates the §Summary block, the §Pass / Issue Breakdown table, and the frontmatter `status:` field (to `closed` if all pass, `gaps_flagged` if any fail). On any fail, the operator also populates `35-UAT.md §Gaps` and updates `35-VERIFICATION.md §Gaps Summary` + frontmatter `gaps:` list. Operator commits the result with the message `docs(36-03): record 35-UAT.md operator results (<N> pass / <M> fail)`. The operator then types one of the three resume signals listed above to either advance the chain (uat passed) or halt it (uat partial / uat blocked).</action>
  <verify>
    <human-check>Operator confirms 35-UAT.md has been edited: zero `<pending operator UAT execution>` placeholders remain, frontmatter status is `closed` (or `gaps_flagged` on any fail), and a commit recording the results is in git log. Run `grep -c "<pending operator UAT execution>" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 0.</human-check>
  </verify>
  <done>Operator UAT execution complete. Either all 10 UATs passed and 35-UAT.md status is `closed` (chain proceeds to Task 3), or at least one UAT failed and the chain halts at the resume-signal pause for orchestrator decision.</done>
</task>

<task type="auto">
  <name>Task 3: Sync 35-VERIFICATION.md retest_outcome.results bullets from 35-UAT.md observed results (gated on Task 2 pass)</name>
  <files>.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md (full file — read all 10 scenarios' Observed result lines for the summary backfill)
    - .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md (Plan 02's output — locate the frontmatter `retest_outcome.results:` placeholder bullets and the body §Human Verification section)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-VERIFICATION.md (lines 17-27 — `retest_outcome.results` reference shape with "T3: pass — ..." style bullets)
  </read_first>
  <behavior>
    - Each UAT scenario in 35-UAT.md contributes one bullet to 35-VERIFICATION.md frontmatter `retest_outcome.results`.
    - The bullet format is `- UAT-N: pass — <one-line summary>` for passing scenarios, or `- UAT-N: fail (severity: <s>) — <one-line summary>` for failing ones.
    - If all 10 UATs passed: `35-VERIFICATION.md §Gaps Summary` remains "No remaining gaps." (no change).
    - If any UAT failed: the gap is already populated by Task 2; this task simply confirms the synchronization.
    - `35-VERIFICATION.md §Human Verification` body section's reference to `35-UAT.md` is updated from "pending Plan 03 UAT execution" to "complete — see 35-UAT.md status: closed" with the date.
  </behavior>
  <action>
    Open `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md`. Locate the frontmatter `retest_outcome` block emitted by Plan 02 with placeholder bullets.

    Replace each placeholder bullet (currently "UAT-1..UAT-10 — pending Plan 03 execution") with one bullet per UAT scenario, drawing the result + summary from `35-UAT.md`:

      results:
        - UAT-1: pass — KPI strip 6 cards visible in spec order (Completed Today mill-wide + per-line, Pending Backlog, Formula Mix); KPI-01, KPI-02, KPI-04, KPI-05 visually verified
        - UAT-2: pass — tz cookie flow + America/Chicago fallback confirmed; cookie writes on second render
        - UAT-3: pass — 7-day trend chart re-renders across America/Chicago and America/Los_Angeles; NO 42803 GROUP BY error in server logs; commits ba54b4a..24b34bf retest-validated
        - UAT-4: pass — empty state "Not enough data yet" renders when fewer than 7 days of data exist
        - UAT-5: pass — overdue badge renders for orders with earlyDeliveryDate < today AND state != 'Completed'; not rendered otherwise
        - UAT-6: pass — formula mix sums to 100%; em-dash null-state per D-12 confirmed on all-uncategorized fixture
        - UAT-7: pass — per-column header strip values match cards below; client-side derivation per D-14/OQ-2 confirmed
        - UAT-8: pass — KPI cards/chart re-render after 30s polling tick; Suspense skeleton flashes briefly
        - UAT-9: pass — BlockedExceptionList rows sorted oldest-block-first; row click opens drawer via ?order=
        - UAT-10: pass — BlockedAlertBand (sticky top) and BlockedExceptionList (bottom) both render per D-10

    (Adjust each bullet to the actual observed result from 35-UAT.md. If a UAT failed, use `fail (severity: <s>)` instead of `pass` and reference the gap entry.)

    Then update the body `### Human Verification — pending Plan 03 UAT execution` section header and paragraph:
    - New header: `### Human Verification — COMPLETE (2026-05-15)`
    - New paragraph: "All 10 UAT scenarios from `35-UAT.md` were executed against `npm run dev` on 2026-05-15 (status: closed). UAT-3 mandatory-pass gate confirmed `getSevenDayTrend` runs cleanly under at least two distinct browser timezones with no `42803` GROUP BY errors in server logs — retest-validating commits ba54b4a..4d61194 and the load-bearing `sql.raw()` tz inlining at `src/db/queries/kpis.ts:313`. See `35-UAT.md` for per-scenario steps, observed results, and operator notes."

    Trailing line: update `_Verified: <UTC timestamp>_` to the actual completion time.

    Commit as `docs(36-03): sync 35-VERIFICATION.md retest_outcome from 35-UAT.md (all 10 UAT scenarios)`.

    **If any UAT failed in Task 2**, this task instead writes the failure summary into `retest_outcome.results` and leaves the body §Gaps Summary populated with the gap entry. Do NOT mark §Human Verification as COMPLETE; instead, label it "PARTIAL — see §Gaps Summary".
  </action>
  <verify>
    <automated>UAT_BULLETS=$(awk '/^retest_outcome:/{f=1;next} f &amp;&amp; /^[a-zA-Z]/{f=0} f &amp;&amp; /^[[:space:]]+- UAT-[0-9]+:/' .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | wc -l | tr -d ' ') &amp;&amp; [ "$UAT_BULLETS" = "10" ] &amp;&amp; grep -c "pending Plan 03 execution" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | grep -qx "0" &amp;&amp; grep -E "ba54b4a|sql\.raw|kpis\.ts:313" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md | head -3</automated>
    Expected: the `retest_outcome.results` block (scoped via awk to the lines between `retest_outcome:` and the next top-level frontmatter key) contains EXACTLY 10 `- UAT-N:` bullets — neither more nor fewer; the original `pending Plan 03 execution` placeholder has zero matches anywhere in the file; KPI-06 / UAT-3 SQL-fix retest reference remains in place. The exact-count gate (`= 10`) catches both over-merges (e.g., 12 bullets from accidental duplication) and under-syncs (e.g., 7 bullets with 3 placeholders left behind).
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` frontmatter `retest_outcome.results:` block contains 10 bullets, one per UAT scenario, each with the form `- UAT-N: <pass|fail> — <summary>`.
    - The string "pending Plan 03 execution" no longer appears in the file. Run `grep -c "pending Plan 03 execution" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns 0.
    - The `### Human Verification` heading has been updated to either `COMPLETE (2026-05-15)` (if all pass) or `PARTIAL — see §Gaps Summary` (if any fail).
    - If all UATs passed: `35-UAT.md` frontmatter status is `closed` and `35-VERIFICATION.md` frontmatter `gaps:` is still `[]`.
    - If any UAT failed: `35-UAT.md` status is `gaps_flagged` and `35-VERIFICATION.md` `gaps:` is populated. **Plan 04 cannot run in this branch.**
    - A commit lands with message `docs(36-03): sync 35-VERIFICATION.md retest_outcome from 35-UAT.md (all 10 UAT scenarios)`.
  </acceptance_criteria>
  <done>The `35-VERIFICATION.md retest_outcome` block reflects the actual UAT execution outcome from `35-UAT.md`. The two artifacts are in sync. Plan 04 may proceed if all 10 UATs passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live dev server | The UAT execution requires the operator to navigate the live Next.js dev server. No new untrusted-input surface; the surface exercised already has Phase 34 + Phase 35 mitigations in place (parseAsString, IANA tz allowlist, server-only DB driver). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | (no new threat) | Documentation + manual UAT execution | accept | This plan authors a Markdown artifact and pauses for operator dashboard verification. No new code paths or input surfaces. The UAT exercises existing Phase 35 surfaces under operator credentials. CWE catalogue does not apply. |

No high-severity threats introduced. `security_enforcement: true` satisfied via this minimal threat model per Phase 36 §security_threat_model note.
</threat_model>

<verification>
- `test -f .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` exits 0.
- `grep -c "^### UAT-[0-9]" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 10.
- `grep -c "<pending operator UAT execution>" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns 0 (every placeholder has been overwritten).
- `grep -c "UAT-[0-9]\+:" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns >= 10 (retest_outcome.results populated).
- `grep -c "pending Plan 03 execution" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-VERIFICATION.md` returns 0.
- If all UATs passed: `grep -E "^status: closed" .planning/phases/35-kpi-sections-and-role-specific-metrics/35-UAT.md` returns one match.
- Three commits land: skeleton author, operator results, VERIFICATION sync.
</verification>

<success_criteria>
- ROADMAP Phase 36 SC#4 satisfied: `35-UAT.md` exists with a written human-UAT pass record covering KPI strip visual rendering (UAT-1), tz cookie flow incl. fallback (UAT-2), 7-day trend chart post-SQL-fix retest (UAT-3 — mandatory pass), overdue badge rendering (UAT-5), formula-mix breakdown (UAT-6).
- The Nyquist sampling gap from the 5 post-phase SQL fix commits is retroactively closed by UAT-3's operator-validated multi-tz retest.
- `35-VERIFICATION.md` retest_outcome reflects the actual UAT execution (no orphan placeholders).
- Chain halts gracefully if UAT-3 or any other UAT fails — Plan 04 (the irreversible VALIDATION re-classification) cannot run on a failing UAT.
- The UAT scenarios are mapped to the 8 KPI-* requirements such that operator verification visually + behaviorally confirms each one is shipped and functional.
</success_criteria>

<output>
Create `.planning/phases/36-close-gap-build-01-void-cast-phase-35-verification/36-03-SUMMARY.md` summarizing:
- The `35-UAT.md` artifact authored (10 scenarios, preconditions, UAT-3 mandatory-pass discipline)
- The operator's UAT execution outcome (pass count / fail count, severity distribution)
- The `35-VERIFICATION.md retest_outcome` sync result
- The chain disposition (proceed to Plan 04 if all pass; halt for orchestrator decision if any fail)
- Any deferred items captured during UAT (e.g., KPI SQL integration smoke test backlog)
</output>
