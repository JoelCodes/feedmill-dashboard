---
phase: 19-documentation-accessibility
plan: 10
type: execute
wave: 2
depends_on: [19-05, 19-06, 19-07, 19-08, 19-09]
files_modified: []
autonomous: true
gap_closure: true
requirements: [DOC-03]

must_haves:
  truths:
    - "npm run lint passes with zero errors"
    - "npm test passes with all tests green"
    - "UAT Test 2 can be re-verified as passing"
  artifacts: []
  key_links: []
---

<objective>
Final verification that all gap closure fixes work together: lint passes with zero errors and all tests pass.

Purpose: Confirm UAT Test 2 (ESLint Accessibility Rules Active) can now pass.
Output: Clean lint output and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-documentation-accessibility/19-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full lint and verify zero errors</name>
  <files></files>
  <action>
Run the full lint check and verify:
1. No "Cannot resolve default tailwindcss config path" warnings
2. No jsx-a11y errors
3. No custom/no-hardcoded-values errors
4. No @typescript-eslint errors
5. No react/display-name errors

Commands:
```bash
npm run lint 2>&1 | tee lint-output.txt

# Check for errors specifically
grep -c "error" lint-output.txt
# Should be 0 or only count header text

# Count remaining warnings (classnames-order is acceptable)
grep -c "warning" lint-output.txt
```

If any errors remain, document them for follow-up fix.
  </action>
  <verify>npm run lint exits with code 0 (no errors)</verify>
  <done>ESLint passes with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all pass</name>
  <files></files>
  <action>
Run the full test suite:
```bash
npm test
```

Verify:
- All test suites pass
- No test failures
- Test count is stable (133+ tests)

If any tests fail due to the accessibility fixes, investigate and fix.
  </action>
  <verify>npm test exits with code 0 (all tests pass)</verify>
  <done>All tests pass after gap closure fixes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Gap closure fixes for all lint errors identified in UAT</what-built>
  <how-to-verify>
1. Run `npm run lint` - should complete without errors (warnings for classnames-order are OK)
2. Run `npm test` - all tests should pass
3. Optional: Run the app with `npm run dev` and verify:
   - Customers page rows are clickable with keyboard (Tab to row, Enter to navigate)
   - Orders table rows work with keyboard navigation
   - Settings theme section displays correctly
  </how-to-verify>
  <resume-signal>Type "approved" if UAT Test 2 now passes, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
npm run lint 2>&1 | grep -v "warning" | grep -c "error"
# Should return 0

npm test 2>&1 | tail -5
# Should show all tests passing
</verification>

<success_criteria>
- npm run lint exits with code 0 (zero errors, warnings OK)
- npm test exits with code 0 (all tests pass)
- UAT Test 2 can be marked as passed
</success_criteria>

<output>
After completion, create `.planning/phases/19-documentation-accessibility/19-10-SUMMARY.md`

Update `.planning/phases/19-documentation-accessibility/19-UAT.md`:
- Change Test 2 result from "issue" to "pass"
- Update summary counts
- Clear the Gaps section
</output>
