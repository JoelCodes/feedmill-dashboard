---
phase: 19-documentation-accessibility
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/ui/Button.test.tsx
  - src/components/ui/Card.test.tsx
  - src/components/ui/Input.test.tsx
  - src/components/ui/Select.test.tsx
  - src/components/ui/Textarea.test.tsx
  - src/components/ui/StatusBadge.test.tsx
  - src/components/ui/FilterPill.test.tsx
  - src/components/ui/Gauge.test.tsx
  - src/components/ui/Timeline.test.tsx
  - src/components/ui/ThemeToggle.test.tsx
autonomous: true
requirements:
  - DOC-03
user_setup: []

must_haves:
  truths:
    - "All 10 UI components pass automated accessibility testing"
    - "Each component test file includes axe-core validation"
    - "Accessibility tests cover default and edge states"
  artifacts:
    - path: "src/components/ui/Button.test.tsx"
      provides: "Accessibility tests for Button component"
      contains: "axe"
    - path: "src/components/ui/Card.test.tsx"
      provides: "Accessibility tests for Card component"
      contains: "axe"
    - path: "src/components/ui/Input.test.tsx"
      provides: "Accessibility tests for Input component"
      contains: "axe"
    - path: "src/components/ui/Select.test.tsx"
      provides: "Accessibility tests for Select component"
      contains: "axe"
    - path: "src/components/ui/Textarea.test.tsx"
      provides: "Accessibility tests for Textarea component"
      contains: "axe"
    - path: "src/components/ui/StatusBadge.test.tsx"
      provides: "Accessibility tests for StatusBadge component"
      contains: "axe"
    - path: "src/components/ui/FilterPill.test.tsx"
      provides: "Accessibility tests for FilterPill component"
      contains: "axe"
    - path: "src/components/ui/Gauge.test.tsx"
      provides: "Accessibility tests for Gauge component"
      contains: "axe"
    - path: "src/components/ui/Timeline.test.tsx"
      provides: "Accessibility tests for Timeline component"
      contains: "axe"
    - path: "src/components/ui/ThemeToggle.test.tsx"
      provides: "Accessibility tests for ThemeToggle component"
      contains: "axe"
  key_links:
    - from: "*.test.tsx"
      to: "jest-axe"
      via: "import { axe }"
      pattern: "import.*axe.*from.*jest-axe"
---

<objective>
Add accessibility tests to all 10 UI component test files using jest-axe.

Purpose: Per D-06, automated testing with axe-core is required. This plan extends each existing test file with accessibility validation, testing default states and significant edge cases (loading, error, disabled, clickable).

Output: All 10 component test files updated with accessibility tests that pass axe-core validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-documentation-accessibility/19-RESEARCH.md
@.planning/phases/19-documentation-accessibility/19-PATTERNS.md

<interfaces>
<!-- jest-axe pattern (from 19-PATTERNS.md) -->
```typescript
import { axe } from "jest-axe";

describe("[ComponentName] - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(<Component>Content</Component>);
    const results = await axe(container, {
      rules: {
        region: { enabled: false },  // Disable page-level rules
      },
    });
    expect(results).toHaveNoViolations();
  });
});
```

<!-- Existing test file pattern (from Button.test.tsx) -->
```typescript
import { render, screen } from "@testing-library/react";
import Button from "./Button";

describe("Button", () => {
  it("renders with primary variant classes by default", () => {
    render(<Button>Click me</Button>);
    // existing tests...
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessibility tests to Button, Card, Input test files</name>
  <files>
    src/components/ui/Button.test.tsx
    src/components/ui/Card.test.tsx
    src/components/ui/Input.test.tsx
  </files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Button.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Card.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Input.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Button.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Card.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Input.tsx
  </read_first>
  <action>
For each test file, add `import { axe } from "jest-axe";` at the top (after existing imports) and append a new describe block at the end of the file with accessibility tests.

**Button.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Button - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(<Button>Click me</Button>);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations in loading state", async () => {
    const { container } = render(<Button loading>Loading</Button>);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when disabled", async () => {
    const { container } = render(<Button disabled>Disabled</Button>);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**Card.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Card - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(<Card>Test content</Card>);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when clickable", async () => {
    const handleClick = jest.fn();
    const { container } = render(
      <Card onClick={handleClick}>Clickable card</Card>
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations with compound pattern", async () => {
    const { container } = render(
      <Card>
        <Card.Header>Header</Card.Header>
        <Card.Content>Content</Card.Content>
        <Card.Footer>Footer</Card.Footer>
      </Card>
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**Input.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Input - Accessibility", () => {
  it("has no accessibility violations with label", async () => {
    const { container } = render(
      <Input label="Email Address" placeholder="Enter email" />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations in error state", async () => {
    const { container } = render(
      <Input
        label="Email Address"
        placeholder="Enter email"
        error="Invalid email format"
      />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when disabled", async () => {
    const { container } = render(
      <Input label="Email Address" placeholder="Enter email" disabled />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(Button|Card|Input)\\.test\\.tsx" 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - Button.test.tsx contains `import { axe } from "jest-axe"`
    - Card.test.tsx contains `import { axe } from "jest-axe"`
    - Input.test.tsx contains `import { axe } from "jest-axe"`
    - Each file has a `describe("[Name] - Accessibility"` block
    - `npm test -- --testPathPattern="(Button|Card|Input)\\.test\\.tsx"` shows all tests passing
  </acceptance_criteria>
  <done>Button, Card, Input test files have accessibility tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Add accessibility tests to Select, Textarea, StatusBadge test files</name>
  <files>
    src/components/ui/Select.test.tsx
    src/components/ui/Textarea.test.tsx
    src/components/ui/StatusBadge.test.tsx
  </files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Select.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Textarea.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/StatusBadge.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Select.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Textarea.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/StatusBadge.tsx
  </read_first>
  <action>
For each test file, add `import { axe } from "jest-axe";` and append accessibility tests.

**Select.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Select - Accessibility", () => {
  it("has no accessibility violations with label", async () => {
    const { container } = render(
      <Select label="Country">
        <option value="us">United States</option>
        <option value="ca">Canada</option>
      </Select>
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations in error state", async () => {
    const { container } = render(
      <Select label="Country" error="Please select a country">
        <option value="">Select...</option>
        <option value="us">United States</option>
      </Select>
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when disabled", async () => {
    const { container } = render(
      <Select label="Country" disabled>
        <option value="us">United States</option>
      </Select>
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**Textarea.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Textarea - Accessibility", () => {
  it("has no accessibility violations with label", async () => {
    const { container } = render(
      <Textarea label="Description" placeholder="Enter description" />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations in error state", async () => {
    const { container } = render(
      <Textarea
        label="Description"
        placeholder="Enter description"
        error="Description is required"
      />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when disabled", async () => {
    const { container } = render(
      <Textarea label="Description" placeholder="Enter description" disabled />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**StatusBadge.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("StatusBadge - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(<StatusBadge status="pending" />);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations for each status variant", async () => {
    const statuses = ["pending", "mixing", "completed", "blocked"] as const;
    for (const status of statuses) {
      const { container } = render(<StatusBadge status={status} />);
      const results = await axe(container, {
        rules: { region: { enabled: false } },
      });
      expect(results).toHaveNoViolations();
    }
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(Select|Textarea|StatusBadge)\\.test\\.tsx" 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - Select.test.tsx contains `import { axe } from "jest-axe"`
    - Textarea.test.tsx contains `import { axe } from "jest-axe"`
    - StatusBadge.test.tsx contains `import { axe } from "jest-axe"`
    - Each file has a `describe("[Name] - Accessibility"` block
    - `npm test -- --testPathPattern="(Select|Textarea|StatusBadge)\\.test\\.tsx"` shows all tests passing
  </acceptance_criteria>
  <done>Select, Textarea, StatusBadge test files have accessibility tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Add accessibility tests to FilterPill, Gauge, Timeline, ThemeToggle test files</name>
  <files>
    src/components/ui/FilterPill.test.tsx
    src/components/ui/Gauge.test.tsx
    src/components/ui/Timeline.test.tsx
    src/components/ui/ThemeToggle.test.tsx
  </files>
  <read_first>
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/FilterPill.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Gauge.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Timeline.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/ThemeToggle.test.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/FilterPill.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Gauge.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/Timeline.tsx
    - /Users/joel/Desktop/Projects/cgm-dashboard/src/components/ui/ThemeToggle.tsx
  </read_first>
  <action>
For each test file, add `import { axe } from "jest-axe";` and append accessibility tests.

**FilterPill.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("FilterPill - Accessibility", () => {
  it("has no accessibility violations when inactive", async () => {
    const { container } = render(
      <FilterPill label="Pending" count={5} active={false} onClick={jest.fn()} />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations when active", async () => {
    const { container } = render(
      <FilterPill label="Pending" count={5} active={true} onClick={jest.fn()} />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**Gauge.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Gauge - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(
      <Gauge value={75} label="Bin A" status="normal" />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations for warning status", async () => {
    const { container } = render(
      <Gauge value={25} label="Bin B" status="warning" />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations for critical status", async () => {
    const { container } = render(
      <Gauge value={10} label="Bin C" status="critical" />
    );
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**Timeline.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("Timeline - Accessibility", () => {
  const mockEvents = [
    {
      id: "1",
      type: "order" as const,
      title: "Order #123",
      description: "Order placed",
      timestamp: new Date("2024-01-15"),
    },
    {
      id: "2",
      type: "delivery" as const,
      title: "Delivery scheduled",
      description: "Scheduled for tomorrow",
      timestamp: new Date("2024-01-16"),
    },
  ];

  it("has no accessibility violations", async () => {
    const { container } = render(<Timeline events={mockEvents} />);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });

  it("has no violations with empty events", async () => {
    const { container } = render(<Timeline events={[]} />);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

**ThemeToggle.test.tsx** — Add after existing tests:
```typescript
import { axe } from "jest-axe";

// ... existing imports and tests ...

describe("ThemeToggle - Accessibility", () => {
  it("has no accessibility violations", async () => {
    const { container } = render(<ThemeToggle />);
    const results = await axe(container, {
      rules: { region: { enabled: false } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

Note: ThemeToggle uses next-themes which may require ThemeProvider wrapper. Check the existing tests for how ThemeToggle is rendered and follow the same pattern.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(FilterPill|Gauge|Timeline|ThemeToggle)\\.test\\.tsx" 2>&1 | grep -E "(PASS|FAIL|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - FilterPill.test.tsx contains `import { axe } from "jest-axe"`
    - Gauge.test.tsx contains `import { axe } from "jest-axe"`
    - Timeline.test.tsx contains `import { axe } from "jest-axe"`
    - ThemeToggle.test.tsx contains `import { axe } from "jest-axe"`
    - Each file has a `describe("[Name] - Accessibility"` block
    - `npm test -- --testPathPattern="(FilterPill|Gauge|Timeline|ThemeToggle)\\.test\\.tsx"` shows all tests passing
  </acceptance_criteria>
  <done>FilterPill, Gauge, Timeline, ThemeToggle test files have accessibility tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test files do not handle untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | — | — | — | Test code only, no security implications |
</threat_model>

<verification>
After all tasks complete:
1. `grep -l "jest-axe" src/components/ui/*.test.tsx | wc -l` returns 10
2. `npm test -- --testPathPattern="ui/.*\\.test\\.tsx"` passes all tests
3. Each test file has "Accessibility" describe block
</verification>

<success_criteria>
- All 10 component test files import jest-axe
- All 10 component test files have accessibility describe block
- All accessibility tests pass (no axe violations)
- Total test count increased by 20+ tests
</success_criteria>

<output>
After completion, create `.planning/phases/19-documentation-accessibility/19-02-SUMMARY.md`
</output>
