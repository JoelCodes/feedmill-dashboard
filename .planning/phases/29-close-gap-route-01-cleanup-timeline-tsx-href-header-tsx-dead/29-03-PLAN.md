---
phase: 29
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/settings/page.tsx
autonomous: true
requirements:
  - NAV-02
tags:
  - cleanup
  - layout-swap
must_haves:
  truths:
    - "src/app/settings/page.tsx wraps its body in <DashboardLayout> instead of inlining <Sidebar/> + <main><Header/>...</main>"
    - "The page no longer imports Sidebar or Header directly"
    - "Visual output of /settings is unchanged (DashboardLayout renders the same Sidebar + main + Header shell)"
    - "/settings page tests at src/app/settings/__tests__/page.test.tsx are NOT modified (deferred per D-08)"
  artifacts:
    - path: src/app/settings/page.tsx
      provides: "Settings page using DashboardLayout wrapper"
      contains: "<DashboardLayout>"
  key_links:
    - from: src/app/settings/page.tsx
      to: src/components/DashboardLayout.tsx
      via: default import + JSX wrapper
      pattern: "import DashboardLayout from .@/components/DashboardLayout."
---

<objective>
Close INT-02 (the last NAV-02 gap): replace the inline `<div className="bg-bg-page flex h-screen"><Sidebar /><main …><Header />…</main></div>` shell in `src/app/settings/page.tsx` with a single `<DashboardLayout>…</DashboardLayout>` wrapper. Drop the now-unused direct `Sidebar` and `Header` imports.

Purpose: NAV-02 requires that DashboardLayout wraps "all pages, eliminating layout duplication." `/settings` is the last page that pre-dates the layout component and still duplicates its structure inline. After this swap, every dashboard page (homepage + 3 demo pages + settings) consumes DashboardLayout.
Output: One file modified, three imports adjusted (2 dropped, 1 added), one outer JSX shell replaced. Visual output is identical (DashboardLayout renders the same Sidebar + main + Header structure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md

<interfaces>
<!-- DashboardLayout contract + current settings JSX shell. -->

From src/components/DashboardLayout.tsx (lines 16–30):
- interface DashboardLayoutProps { children: React.ReactNode; }
- export default function DashboardLayout({ children }: DashboardLayoutProps) — children-only, no other props.
- The component renders: `<div className="bg-bg-page flex h-screen"><Sidebar /><main className="flex flex-1 flex-col gap-6 overflow-auto p-6 pr-8"><Header />{children}</main></div>`. Visual output of the post-swap `/settings` page is IDENTICAL to the current inline structure.

From src/app/settings/page.tsx (current state):
- Imports include (lines 3–6): `import Sidebar from "@/components/Sidebar";` and `import Header from "@/components/Header";` — both must be dropped.
- The component's return statement starts (~ line 54) with `<div className="bg-bg-page flex h-screen">` and ends (~ line 140) with the closing `</div>`. Inside is `<Sidebar />` followed by `<main className="flex flex-1 flex-col gap-6 overflow-auto p-6 pr-8">` containing `<Header />` then the actual page content (a `<div className="mx-auto w-full max-w-2xl">…</div>` block).
- The "actual page content" — the `<div className="mx-auto w-full max-w-2xl">` block and everything inside it — is unchanged. It becomes the children of `<DashboardLayout>`.

From .planning/phases/29-.../29-CONTEXT.md D-08:
- DO NOT touch `src/app/settings/__tests__/page.test.tsx`. The 14 pre-existing failing tests there are deferred to a follow-up phase (ClerkProvider wrapper rework). This plan is layout-swap only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap settings/page.tsx outer JSX to DashboardLayout; drop Sidebar/Header imports</name>
  <files>src/app/settings/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (entire file — both the imports near lines 3–6 and the return-statement JSX from line ~54 to line ~140)
    - src/components/DashboardLayout.tsx (lines 1–30; confirms children-only contract — read-only reference)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/app/settings/page.tsx — D-07: DashboardLayout swap"
  </read_first>
  <action>
    Make exactly these edits to `src/app/settings/page.tsx`:
    1. Delete the import line `import Sidebar from "@/components/Sidebar";` (currently at line ~3).
    2. Delete the import line `import Header from "@/components/Header";` (currently at line ~4 or wherever it sits adjacent to the Sidebar import).
    3. Add the import line `import DashboardLayout from "@/components/DashboardLayout";` in the same import block, alphabetized or grouped with the existing `@/components/*` imports per the file's existing convention.
    4. In the JSX returned from the page component, replace the outer wrapper. Specifically: remove the opening `<div className="bg-bg-page flex h-screen">` element, the `<Sidebar />` self-closing element directly inside it, the opening `<main className="flex flex-1 flex-col gap-6 overflow-auto p-6 pr-8">` element, and the `<Header />` self-closing element directly inside that `<main>`. Replace those four lines with a single opening `<DashboardLayout>` element. At the bottom of the return statement, replace the closing `</main>` and `</div>` (the matching closing tags of the outer wrapper) with a single closing `</DashboardLayout>`.
    5. Leave everything between (the `<div className="mx-auto w-full max-w-2xl">…</div>` block and all of its contents — every form field, every section, every conditional) BYTE-IDENTICAL.
    Do not touch `src/app/settings/__tests__/page.test.tsx` (D-08 — those tests have a pre-existing ClerkProvider-wrapper failure mode and are deferred to a follow-up phase). Do not change any other file. Per D-07, this is a layout swap only — no behavior changes, no styling tweaks, no prop additions (DashboardLayout has only `children`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/app/settings/page\\.tsx" | grep -v "^$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/settings/page.tsx` imports DashboardLayout: `grep -c "import DashboardLayout from .@/components/DashboardLayout." src/app/settings/page.tsx` returns `1`.
    - File no longer imports Sidebar: `grep -c "import Sidebar from .@/components/Sidebar." src/app/settings/page.tsx` returns `0`.
    - File no longer imports Header: `grep -c "import Header from .@/components/Header." src/app/settings/page.tsx` returns `0`.
    - File contains the DashboardLayout JSX wrapper: `grep -c "<DashboardLayout>" src/app/settings/page.tsx` returns `1` AND `grep -c "</DashboardLayout>" src/app/settings/page.tsx` returns `1`.
    - File no longer contains the bare inline shell: `grep -c "bg-bg-page flex h-screen" src/app/settings/page.tsx` returns `0` (this className lived only on the now-replaced outer div).
    - Negative-existence: `grep -c "<Sidebar />" src/app/settings/page.tsx` returns `0` AND `grep -c "<Header />" src/app/settings/page.tsx` returns `0`.
    - Type-check passes for this file: `npx tsc --noEmit 2>&1 | grep "src/app/settings/page.tsx"` returns no matches (zero TS errors introduced).
    - Settings test file UNCHANGED: `git diff --name-only HEAD src/app/settings/__tests__/page.test.tsx` returns empty (no changes recorded for that path).
  </acceptance_criteria>
  <done>Settings page wraps its body in DashboardLayout; Sidebar and Header are not imported directly; settings tests untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (no new type errors introduced by this plan — pre-existing fixture errors are addressed by 29-06, not this plan).
- Grep gates above all pass.
- Visual smoke check (Manual / dev-server, per 29-VALIDATION.md): start `npm run dev`, visit `/settings`, confirm the Sidebar appears on the left and the Header (with breadcrumb showing "Settings") appears on top, with the form content centered as before.
</verification>

<success_criteria>
- INT-02 closed: `/settings` consumes DashboardLayout; NAV-02 is fully satisfied (all dashboard pages now wrapped).
- Zero behavior changes (the rendered DOM tree under `<main>` is identical because DashboardLayout's JSX shell is the same shape).
- Pre-existing failing settings tests remain pre-existing (not made worse, not fixed — per D-08 deferred).
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-03-SUMMARY.md`.
</output>
