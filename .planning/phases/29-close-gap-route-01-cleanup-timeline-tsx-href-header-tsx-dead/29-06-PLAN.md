---
phase: 29
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.ts
  - src/__tests__/design-system/tokens.test.ts
  - src/__tests__/design-system/theme.test.tsx
  - src/components/__tests__/OrderDetails.test.tsx
  - src/utils/customerSort.test.ts
  - src/app/globals.css
autonomous: true
requirements: []
tags:
  - tech-debt
  - test-pipeline
  - config
must_haves:
  truths:
    - "jest.config.ts ignores the e2e/ directory (Playwright specs no longer scanned by Jest)"
    - "`npx tsc --noEmit` exits 0 — all 12 pre-existing fixture errors are resolved without touching tsconfig.json"
    - "demo-user / norole-user Playwright projects are unaffected by .env.local PLAYWRIGHT_BASE_URL (handled by 29-04; this plan handles only the orthogonal pipeline items)"
    - "Tailwind v4 @source not directive recursively excludes .planning/**/*.md (verified post-edit by dev-server startup)"
  artifacts:
    - path: jest.config.ts
      provides: "Jest config with testPathIgnorePatterns for /node_modules/ and <rootDir>/e2e/"
      contains: "testPathIgnorePatterns"
    - path: src/__tests__/design-system/tokens.test.ts
      provides: "Token regex tests using [\\s\\S] instead of /s flag"
      contains: "[\\s\\S]"
    - path: src/__tests__/design-system/theme.test.tsx
      provides: "Theme tests with capturedProps non-null assertions"
      contains: "capturedProps!."
    - path: src/components/__tests__/OrderDetails.test.tsx
      provides: "OrderDetails mock with customerId field"
      contains: "customerId:"
    - path: src/utils/customerSort.test.ts
      provides: "Customer fixture with activeBins field"
      contains: "activeBins:"
  key_links:
    - from: npm test
      to: src/ jest scope only
      via: testPathIgnorePatterns
      pattern: "testPathIgnorePatterns:.*e2e"
    - from: src/app/globals.css
      to: tailwind source detection
      via: "@source not directive"
      pattern: "@source not .*\\.planning"
---

<objective>
Close the four test-pipeline tech-debt items aggregated from Phases 27 and 28 audit findings: (1) Jest currently scans `e2e/` and the Playwright specs error inside Jest; (2) `npx tsc --noEmit` reports 12 errors across 4 test-fixture files (drifted mock fields + ES2017-incompatible regex `/s` flag); (3) Tailwind v4 `@source not` directive in `globals.css` needs verification that `.planning/**/*.md` is recursively excluded.

The PLAYWRIGHT_BASE_URL leak (Phase 27 tech-debt item #5) is handled by plan 29-04 because it touches `playwright.config.ts` which is owned by that plan; this plan owns only the orthogonal Jest/tsc/Tailwind items.

Purpose: Make the local test pipeline run clean. Before this plan, `npm test` produces 14 failures from Playwright specs Jest shouldn't even see; `npx tsc --noEmit` reports 12 errors that drown out real type drift; the Tailwind dev-server scans `.planning/**/*.md` on every change. After this plan, all three are clean. None of these changes affect production code — fixture files only, config files only.
Output: 6 files modified — 1 config (`jest.config.ts`), 1 CSS (`globals.css`), 4 test fixtures. Touch fixture files only per D-15 (do NOT touch `tsconfig.json`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md

<interfaces>
<!-- Current shape of each affected file. Verified per 29-RESEARCH.md §Key Technical Facts and 29-PATTERNS.md. -->

From jest.config.ts (current config object):
- The exported config has keys `coverageProvider`, `testEnvironment`, `setupFilesAfterEnv`, `moduleNameMapper`. There is NO `testPathIgnorePatterns` key currently — Jest's default still scans `e2e/`. Verified by `npm test -- --listTests` showing `e2e/production-smoke.spec.ts`, `e2e/route-protection.spec.ts`, `e2e/demo-route-protection.spec.ts` in the test list.

From src/__tests__/design-system/tokens.test.ts (lines 93, 99, 105 — verified):
- Three instances of `const darkBlock = globalsCss.match(/\\.dark\\s*\\{[^}]+\\}/s);` (identical regex pattern each time). The `/s` (dotAll) flag requires the ES2018 target; the project's tsconfig targets ES2017, so tsc emits `TS1501`.
- Replacement: rewrite each regex to `/\\.dark\\s*\\{[\\s\\S]+?\\}/` — `[\\s\\S]` matches any character including newlines, no `/s` flag needed, lazy `+?` prevents over-matching.

From src/__tests__/design-system/theme.test.tsx (~lines 38, 48, 58, 68, 78, 88, 109 — verified 7 instances):
- The pattern is `expect(capturedProps.<attribute>).toBe(...)` where `capturedProps` is typed as possibly `null`. Tsc emits `TS18047: 'capturedProps' is possibly 'null'`.
- Minimal fix: change each `capturedProps.<attribute>` to `capturedProps!.<attribute>` (non-null assertion). 7 instances. Runtime behavior is unchanged.

From src/components/__tests__/OrderDetails.test.tsx (~line 13 — verified):
- `const mockOrder: Order = { id: 'ORD-001', documentNumber: '12345', customer: 'Test Farm', textureType: 'Coarse', formulaType: 'Grower', quantity: 10, location: 'Springfield, IL', deliveryDate: new Date('2026-05-15'), status: 'Producing', hasChanges: false, createdAt: new Date('2026-05-01'), updatedAt: new Date('2026-05-01'), };`
- Missing field on the `Order` type: `customerId` (string). Add `customerId: 'CUST-001'` as a new property in the object literal.

From src/utils/customerSort.test.ts (~lines 12–18 — verified):
- The `stats` object inside the `createCustomer` helper has `{ totalOrders: 0, activeOrders: 0, completedOrders: 0, hasChanges: false, binAlertLevel: 'none', }` — missing `activeBins: number`.
- Fix: add `activeBins: 0` to the object literal.

From src/app/globals.css (line 4 — verified):
- Currently `@source not "../../.planning";`. Path resolves to project-root `.planning/` directory.
- Per Tailwind v4 docs (29-RESEARCH.md citation), bare directory paths recursively exclude all contents — this is syntactically correct. The audit's "doesn't recursively exclude" note is most plausibly a stale dev-server cache. The action here is VERIFY post-edit (run `npm run dev` and confirm no warnings about scanning `.planning/**/*.md`); if recursive exclusion still doesn't fire, switch to the explicit glob form `@source not "../../.planning/**";`.

From 29-CONTEXT.md D-15 (constraint):
- "Touch fixture files only; do not migrate type definitions." → tsconfig.json must NOT be edited. Even though bumping the target to ES2018 would be the cleanest fix for the `/s` flag issue, the user-locked decision is to keep tsconfig untouched and rewrite the regexes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testPathIgnorePatterns to jest.config.ts (D-14)</name>
  <files>jest.config.ts</files>
  <read_first>
    - jest.config.ts (entire file — the exported `config: Config` object)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"jest.config.ts — D-14"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"Pitfall 6: Dropping Default /node_modules/ from testPathIgnorePatterns"
  </read_first>
  <action>
    In `jest.config.ts`, add a new key `testPathIgnorePatterns: ['/node_modules/', '<rootDir>/e2e/']` to the `config: Config` object. Place it after `moduleNameMapper` (or anywhere in the object — order doesn't matter for Jest). Per RESEARCH.md Pitfall 6: Jest's default `testPathIgnorePatterns` includes `/node_modules/`, but setting `testPathIgnorePatterns` explicitly REPLACES the default — the explicit array MUST include `/node_modules/` to preserve the standard behavior. Do not edit the other config keys, the `createJestConfig` wrapper, or the imports.
  </action>
  <verify>
    <automated>npm test -- --listTests 2>&1 | grep -c "e2e/"</automated>
  </verify>
  <acceptance_criteria>
    - File contains the new key: `grep -c "testPathIgnorePatterns" jest.config.ts` returns `1`.
    - Both required patterns present: `grep -c "'/node_modules/'" jest.config.ts` returns at least `1` AND `grep -c "'<rootDir>/e2e/'" jest.config.ts` returns `1`.
    - Jest no longer scans e2e: `npm test -- --listTests 2>&1 | grep -c "e2e/"` returns `0`.
    - Jest still finds src tests: `npm test -- --listTests 2>&1 | grep -c "src/"` returns at least `5` (sanity check that we didn't accidentally ignore everything).
  </acceptance_criteria>
  <done>`npm test` no longer picks up Playwright specs; node_modules also excluded; src tests still discovered.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tokens.test.ts regexes to use [\s\S] instead of /s flag (D-15, part 1 — tokens)</name>
  <files>src/__tests__/design-system/tokens.test.ts</files>
  <read_first>
    - src/__tests__/design-system/tokens.test.ts (the 3 regex instances; per 29-RESEARCH.md the lines are 93, 99, 105 — re-grep for `globalsCss.match` to locate them precisely)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/__tests__/design-system/tokens.test.ts — D-15: regex fix"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"Pitfall 4: Touching tsconfig.json for D-15"
  </read_first>
  <action>
    In `src/__tests__/design-system/tokens.test.ts`, locate every occurrence of the regex `/\\.dark\\s*\\{[^}]+\\}/s` (or any other regex using the `/s` dotAll flag — re-grep to be sure). Replace each one with `/\\.dark\\s*\\{[\\s\\S]+?\\}/` (no `/s` flag; uses the `[\\s\\S]` character class to match any character including newlines; lazy `+?` prevents over-matching). There are 3 such regex occurrences per RESEARCH.md, all on the `globalsCss.match(...)` lines. Per CONTEXT.md D-15 + RESEARCH.md Pitfall 4: do NOT change `tsconfig.json` target from ES2017 to ES2018. The constraint is "touch fixture files only" — the cleanup is achieved by rewriting the regexes locally.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "tokens\\.test\\.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Negative-existence of dotAll flag: `grep -cE "\\.dark.*\\}/s" src/__tests__/design-system/tokens.test.ts` returns `0` (the `/s` flag suffix is gone from any `.dark` regex).
    - New form present: `grep -c "\\[\\\\s\\\\S\\]" src/__tests__/design-system/tokens.test.ts` returns at least `3` (or use a simpler form: `grep -c "[\\\\\\\\s\\\\\\\\S]" src/__tests__/design-system/tokens.test.ts` returns at least 3 — the executor should use whichever shell-escape form the grep tool requires).
    - tsc passes for this file: `npx tsc --noEmit 2>&1 | grep -E "tokens\\.test\\.ts" | wc -l` returns `0`.
    - Test still passes: `npm test -- --testPathPattern=tokens` exits 0.
    - tsconfig.json untouched: `git diff --name-only HEAD tsconfig.json` returns empty.
  </acceptance_criteria>
  <done>tokens.test.ts compiles under the ES2017 target; tests still pass; tsconfig unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add non-null assertions to capturedProps accesses in theme.test.tsx (D-15, part 2 — theme)</name>
  <files>src/__tests__/design-system/theme.test.tsx</files>
  <read_first>
    - src/__tests__/design-system/theme.test.tsx (the 7 `capturedProps.<attribute>` accesses; per 29-RESEARCH.md they live around lines 38, 48, 58, 68, 78, 88, 109 — re-grep for `capturedProps\\.` to locate them all precisely)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/__tests__/design-system/theme.test.tsx — D-15: non-null assertion fix"
  </read_first>
  <action>
    In `src/__tests__/design-system/theme.test.tsx`, find every property access of the form `capturedProps.<identifier>` (where `<identifier>` is any property name) and replace it with `capturedProps!.<identifier>` (insert a `!` non-null assertion between the variable and the dot). Per 29-RESEARCH.md, there are 7 such accesses. Re-grep to locate them — do not rely solely on line numbers (the count is verified). Do NOT replace any occurrence of just `capturedProps` (the bare identifier, e.g., in `if (!capturedProps)` or `expect(capturedProps).not.toBeNull()` guards) — only the property accesses. Do NOT change the variable's declaration, the `let capturedProps: ... | null = null;` line, or any other code in the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "theme\\.test\\.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Non-null assertion applied: `grep -c "capturedProps!\\." src/__tests__/design-system/theme.test.tsx` returns at least `7`.
    - No remaining unguarded property accesses: `grep -E "capturedProps\\.[a-zA-Z]" src/__tests__/design-system/theme.test.tsx | grep -v "capturedProps!\\.\\|capturedProps\\?\\\\." | wc -l` returns `0` (no bare `capturedProps.foo` patterns left — only `!.` or `?.` permitted).
    - tsc passes for this file: `npx tsc --noEmit 2>&1 | grep -E "theme\\.test\\.tsx" | wc -l` returns `0`.
    - Tests still pass: `npm test -- --testPathPattern=theme` exits 0.
  </acceptance_criteria>
  <done>theme.test.tsx compiles cleanly; tests still pass; non-null assertion applied to all 7 capturedProps accesses.</done>
</task>

<task type="auto">
  <name>Task 4: Add missing customerId field to OrderDetails mock + activeBins field to customerSort fixture (D-15, parts 3+4)</name>
  <files>src/components/__tests__/OrderDetails.test.tsx, src/utils/customerSort.test.ts</files>
  <read_first>
    - src/components/__tests__/OrderDetails.test.tsx (the `mockOrder` object literal around line 13)
    - src/utils/customerSort.test.ts (the `stats` object literal inside the `createCustomer` helper around lines 12–18)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/components/__tests__/OrderDetails.test.tsx — D-15" and §"src/utils/customerSort.test.ts — D-15"
  </read_first>
  <action>
    Two coordinated mock-data field additions (single commit):
    1. In `src/components/__tests__/OrderDetails.test.tsx`, locate the `const mockOrder: Order = { ... };` literal (around line 13). Add a new line `customerId: 'CUST-001',` inside the object literal. Place it adjacent to the existing `customer: 'Test Farm',` field (alphabetical or grouped — match the surrounding style). The `Order` type requires this field; adding the missing key resolves the `TS2741: 'customerId' missing in Order mock` error. Do not change any other field; do not change the import or the surrounding test code.
    2. In `src/utils/customerSort.test.ts`, locate the `stats: { ... }` object literal inside the `createCustomer` helper (around lines 12–18). Add a new line `activeBins: 0,` inside that `stats` literal. Place it after the existing `binAlertLevel: 'none',` line (matching the surrounding style). The `CustomerStats` type requires this field; adding it resolves the `TS2741: 'activeBins' missing in CustomerStats mock` error. Do not change any other field or any other test in the file.
    Per CONTEXT.md D-15: touch fixture files only — these are exactly that. Do not touch the `Order` or `CustomerStats` type definitions.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(OrderDetails\\.test\\.tsx|customerSort\\.test\\.ts)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - OrderDetails mock has customerId: `grep -c "customerId:" src/components/__tests__/OrderDetails.test.tsx` returns at least `1`.
    - customerSort fixture has activeBins: `grep -c "activeBins:" src/utils/customerSort.test.ts` returns at least `1`.
    - tsc passes for both files: `npx tsc --noEmit 2>&1 | grep -E "(OrderDetails\\.test\\.tsx|customerSort\\.test\\.ts)" | wc -l` returns `0`.
    - Tests still pass: `npm test -- --testPathPattern="(OrderDetails|customerSort)"` exits 0.
    - Type definitions untouched: `git diff --name-only HEAD src/types/ 2>/dev/null | wc -l` returns `0` AND the modules defining `Order` / `CustomerStats` show no diff.
  </acceptance_criteria>
  <done>Both fixture types satisfied; tsc happy for these files; tests still pass; type definitions unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Verify (and conditionally tighten) the Tailwind @source not directive in globals.css (D-17)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (line 4 currently reads `@source not "../../.planning";`)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/app/globals.css — D-17"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"D-17: Tailwind v4 @source not fix"
  </read_first>
  <action>
    Two-step verify-then-conditionally-edit:
    1. First, read `src/app/globals.css` line 4 and confirm the directive value. Per Tailwind v4 docs (29-RESEARCH.md citation), the bare directory path `"../../.planning"` is the canonical recursive-exclude syntax. If that is the current value, no edit is needed in this step — proceed to the verification below.
    2. Run `npm run dev` for ~5 seconds (or `npm run build`, whichever is faster locally) and observe Tailwind output for any "scanning .planning/**/*.md" warnings or visible perf-hit symptoms. If Tailwind still scans `.planning/**/*.md`, switch the directive to the explicit glob form: change `@source not "../../.planning";` to `@source not "../../.planning/**";`. If no scanning is observed, leave the directive unchanged (the audit's note was likely a stale dev-server cache resolved by this point).
    Per CONTEXT.md D-17: this is a cosmetic dev-server perf fix; the worst case (no edit needed) is fine, the next case (one-line glob bump) is the only fallback edit allowed.
  </action>
  <verify>
    <automated>grep -E "@source not" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - The directive exists in some form: `grep -cE "@source not.*\\.planning" src/app/globals.css` returns at least `1`.
    - Exactly one of two valid post-states:
      EITHER `grep -c '@source not "../../.planning";' src/app/globals.css` returns `1` (unchanged — bare form retained because dev-server already excludes recursively)
      OR `grep -c '@source not "../../.planning/\\*\\*";' src/app/globals.css` returns `1` (glob form — applied because bare form did not recursively exclude).
    - Manual verification recorded: SUMMARY.md notes whether the directive was changed or left as-is, plus the observed dev-server behavior (no Tailwind warnings about scanning `.planning/**/*.md`).
  </acceptance_criteria>
  <done>The directive either stays at its current (already-correct-per-docs) value or is upgraded to the explicit glob form; dev-server confirms no `.planning/` scanning. Decision and rationale recorded in 29-06-SUMMARY.md.</done>
</task>

</tasks>

<verification>
- `npm test -- --listTests` produces zero lines containing `e2e/`.
- `npx tsc --noEmit` exits 0 (no remaining errors in any of the 4 fixture files).
- `npm test` runs the full Jest suite without scanning e2e/ and with the fixture type errors resolved (target: green or fewer-failing than baseline; the only allowed pre-existing failures are the 14 `/settings` ClerkProvider-wrapper tests deferred by D-08, which are out of scope).
- `src/app/globals.css` has a `@source not` directive targeting `.planning` (either bare or glob form per Task 5).
- `tsconfig.json` is unchanged (`git diff --name-only HEAD tsconfig.json` returns empty).
</verification>

<success_criteria>
- D-14: Jest no longer scans `e2e/` — `npm test` runs only src tests.
- D-15: `npx tsc --noEmit` exits 0 (all 12 fixture errors fixed without touching type defs or tsconfig).
- D-17: Tailwind directive verified or tightened; no `.planning/**/*.md` scanning observed in dev-server.
- The 14 deferred `/settings` tests remain pre-existing failures (D-08 — they are NOT addressed by this plan; that's Phase 30).
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-06-SUMMARY.md`. The SUMMARY must record Task 5's verify-vs-edit decision and whether the Tailwind directive was changed.
</output>
