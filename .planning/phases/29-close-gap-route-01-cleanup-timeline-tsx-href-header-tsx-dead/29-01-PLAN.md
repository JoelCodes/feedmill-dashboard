---
phase: 29
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Timeline.tsx
  - src/components/ui/Timeline.test.tsx
autonomous: true
requirements:
  - ROUTE-01
tags:
  - cleanup
  - href-migration
  - tdd
must_haves:
  decisions_covered:
    - "D-01: scope — INT-01 is in scope (ROUTE-01 audit item)"
    - "D-05: change Timeline.tsx:123 href from /orders to /demo/orders"
    - "D-06: add Jest component test asserting demo-prefixed href shape"
  truths:
    - "Timeline 'View Order Details' Link href navigates to /demo/orders?selected=<orderId> (the live route after Phase 26) [D-05]"
    - "An automated component test asserts the demo-prefixed href shape, preventing this exact regression class going forward [D-06]"
  artifacts:
    - path: src/components/ui/Timeline.tsx
      provides: "Order-event Link that points to the live /demo/orders route"
      contains: "/demo/orders?selected="
    - path: src/components/ui/Timeline.test.tsx
      provides: "Component test asserting the demo-prefixed href"
      contains: "/demo/orders?selected=order-1"
  key_links:
    - from: src/components/ui/Timeline.tsx
      to: /demo/orders route
      via: next/link href template literal at line 123
      pattern: "href=\\{`/demo/orders\\?selected="
    - from: src/components/ui/Timeline.test.tsx
      to: Timeline.tsx href shape
      via: toHaveAttribute assertion at line 82
      pattern: "toHaveAttribute\\('href', '/demo/orders\\?selected=order-1'\\)"
---

<objective>
Close INT-01 / FLOW-01 (the milestone-audit blocker): the Timeline UI primitive still links to the deleted `/orders` path, breaking the demo customer-detail → order-detail flow. Apply the one-character-group fix to the href and update the existing component test to lock the corrected href shape.

Purpose: Restore the broken demo navigation flow and add a structural mechanism (a Jest component test) that prevents the same class of regression in any future route migration.
Output: Updated Timeline.tsx href + updated Timeline.test.tsx assertion. Jest test for Timeline goes RED then GREEN as the TDD cycle completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md

<interfaces>
<!-- Existing test mock + Link target. Executor uses these as-is — no codebase exploration needed. -->

From src/components/ui/Timeline.test.tsx (existing — DO NOT recreate; lines 7–14):
The file already mocks next/link as a plain anchor:
- jest.mock('next/link', () => { const MockLink = ({ children, href }: { children: React.ReactNode; href: string }) => (<a href={href}>{children}</a>); MockLink.displayName = 'MockLink'; return MockLink; });
This is why `toHaveAttribute('href', ...)` works directly on the rendered DOM.

From src/components/ui/Timeline.test.tsx (existing test to UPDATE; lines 76–83):
The test named "shows View Order Details link when expanded" already exists. The last line is the STALE assertion to update:
- expect(link).toHaveAttribute('href', '/orders?selected=order-1');  // line 82

From src/components/ui/Timeline.tsx (line 123, current value):
- href={`/orders?selected=${event.orderId}`}  // STALE — this is the broken link
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — update existing Timeline href assertion to /demo/orders</name>
  <files>src/components/ui/Timeline.test.tsx</files>
  <read_first>
    - src/components/ui/Timeline.test.tsx (entire file; the relevant test is "shows View Order Details link when expanded" at lines 76–83)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/components/ui/Timeline.test.tsx — D-06: stale assertion update"
  </read_first>
  <behavior>
    - Existing test "shows View Order Details link when expanded" must assert that, given a mockEvent with orderId: 'order-1', the rendered Link has the attribute href='/demo/orders?selected=order-1' (the demo-prefixed path).
    - Running `npm test -- --testPathPattern=Timeline` MUST show this single test FAIL with a diff between expected '/demo/orders?selected=order-1' and received '/orders?selected=order-1'. RED state proves the test is exercising the right code path.
  </behavior>
  <action>
    Update the one assertion at line 82 of `src/components/ui/Timeline.test.tsx`. Change the second argument of `toHaveAttribute` from the string literal `'/orders?selected=order-1'` to `'/demo/orders?selected=order-1'`. Do not touch the jest.mock('next/link', ...) block, the `mockEvents` fixture, or any other test in the file. Do not add new tests. Do not delete any imports. The file's `mockEvents[0].orderId` is already `'order-1'` (per 29-PATTERNS.md) — no fixture changes needed. This task implements D-06 per user decision and is the RED step of the TDD pair with Task 2 (D-05).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Timeline 2>&1 | grep -E "(FAIL|/demo/orders|/orders\\?selected=order-1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/Timeline.test.tsx` contains exactly one match for `/demo/orders?selected=order-1`: `grep -c "'/demo/orders?selected=order-1'" src/components/ui/Timeline.test.tsx` returns `1`.
    - File `src/components/ui/Timeline.test.tsx` contains zero matches for the stale path: `grep -c "'/orders?selected=order-1'" src/components/ui/Timeline.test.tsx` returns `0` (note: counts only the exact stale literal).
    - Running `npm test -- --testPathPattern=Timeline` exits non-zero AND the output contains the substring `FAIL` AND mentions `/demo/orders?selected=order-1` (the new expected value) — proving the assertion fires against the current stale href.
  </acceptance_criteria>
  <done>The Timeline component test exists in updated form and FAILS against the current stale `Timeline.tsx` href. Commit before proceeding to Task 2.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — change Timeline.tsx href to /demo/orders</name>
  <files>src/components/ui/Timeline.tsx</files>
  <read_first>
    - src/components/ui/Timeline.tsx (the `Link` at lines 122–127; specifically the `href` template literal on line 123)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/components/ui/Timeline.tsx — D-05: href fix"
  </read_first>
  <behavior>
    - Running `npm test -- --testPathPattern=Timeline` MUST exit zero and the previously-RED test "shows View Order Details link when expanded" MUST pass.
    - No other Timeline test changes status (the file's other tests already pass on baseline).
  </behavior>
  <action>
    In `src/components/ui/Timeline.tsx`, on line 123, change the href template literal from `` `/orders?selected=${event.orderId}` `` to `` `/demo/orders?selected=${event.orderId}` ``. The fix is the addition of `/demo` before `/orders` inside the backtick template literal. Do not touch the surrounding `<Link>` JSX, the `className` prop, the import statements, or any other code in the file. Do not refactor `event.orderId` or the surrounding map. This task implements D-05 per user decision and is the GREEN step of the TDD pair with Task 1 (D-06).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=Timeline</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/Timeline.tsx` contains exactly one match for the demo-prefixed path inside a template literal: `grep -c "/demo/orders?selected=" src/components/ui/Timeline.tsx` returns `1`.
    - File `src/components/ui/Timeline.tsx` contains zero matches for the bare-prefixed path: `grep -vc "demo/orders" src/components/ui/Timeline.tsx | grep -c "/orders?selected=" src/components/ui/Timeline.tsx` (use the equivalent: `grep -E "(^|[^/])/orders\\?selected=" src/components/ui/Timeline.tsx` returns zero matches).
    - Running `npm test -- --testPathPattern=Timeline` exits 0; output contains `PASS` and no `FAIL` for the Timeline suite.
  </acceptance_criteria>
  <done>The Timeline component test passes against the corrected `Timeline.tsx` href. Both files committed (atomically, or as the RED/GREEN pair if the executor prefers two commits).</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=Timeline` exits 0.
- `grep -c "/demo/orders?selected=" src/components/ui/Timeline.tsx` returns `1`.
- `grep -c "'/demo/orders?selected=order-1'" src/components/ui/Timeline.test.tsx` returns `1`.
- Neither file contains a bare `/orders?selected=` reference (verify by reading the rg/grep output, or `grep -nE "(^|[^/])/orders\\?selected=" src/components/ui/Timeline.tsx src/components/ui/Timeline.test.tsx` returns no matches).
</verification>

<success_criteria>
- INT-01 (blocker) and FLOW-01 are closed: the Timeline order-event link points to `/demo/orders`.
- A component test now locks the href shape and would have caught this exact regression had it existed during Phase 26.
- No other Timeline tests regressed.
- No changes to `Timeline.tsx` apart from the single href string; no changes to `Timeline.test.tsx` apart from the single assertion string.
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-01-SUMMARY.md`.
</output>
