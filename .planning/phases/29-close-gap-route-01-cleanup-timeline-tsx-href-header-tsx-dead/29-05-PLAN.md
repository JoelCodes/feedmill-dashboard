---
phase: 29
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/auth.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ACCESS-02
tags:
  - cleanup
  - dead-code
  - docs
must_haves:
  decisions_covered:
    - "D-02: scope — INT-03 (checkRole orphan) is in scope, reversing audit's intentional-API-surface judgment"
    - "D-12: delete checkRole export from src/lib/auth.ts and remove its describe block tests"
    - "D-13: update REQUIREMENTS.md ACCESS-02 to reference requireRole only (drop checkRole)"
  truths:
    - "src/lib/auth.ts no longer exports a checkRole function [D-12]"
    - "src/lib/auth.test.ts no longer contains the describe('checkRole') block; the requireRole tests remain [D-12]"
    - "REQUIREMENTS.md ACCESS-02 references only requireRole() (drops the checkRole() mention), keeps [x] Complete status [D-13]"
    - "No production source file imports or references checkRole"
  artifacts:
    - path: src/lib/auth.ts
      provides: "Auth utilities — requireRole only"
      contains: "export async function requireRole"
    - path: src/lib/auth.test.ts
      provides: "Unit tests for requireRole only"
      contains: "describe('requireRole'"
    - path: .planning/REQUIREMENTS.md
      provides: "Updated ACCESS-02 row matching code reality"
      contains: "Role utility functions (`requireRole()`) available for server components"
  key_links:
    - from: src/lib/auth.ts
      to: requireRole sole-export pattern
      via: removal of checkRole export
      pattern: "export async function requireRole"
---

<objective>
Close INT-03 (the only ACCESS-02 audit gap) by deleting the unused `checkRole` export and its 5 unit tests, then update the REQUIREMENTS.md ACCESS-02 description so the audit trail matches the post-deletion code surface. `requireRole` becomes the sole role-guard export.

Purpose: Per the milestone audit, `checkRole` was exported, tested, and never imported by any production source — pure surface-area dead code. CONTEXT.md D-02 reversed the audit's "intentional API surface" judgment because there is no near-term production consumer; rather than carry an unused API, delete it. Re-add later when a real feature scopes a non-redirecting role check (recorded as a deferred idea in CONTEXT.md). The REQUIREMENTS.md text edit (D-13) keeps the requirement's `[x]` Complete status — `requireRole` alone satisfies ACCESS-02 as worded for the current guarding use case.
Output: Three files modified — `auth.ts` loses ~25 lines (one JSDoc + one function), `auth.test.ts` loses ~41 lines (1 import name + the 5-test describe block), `REQUIREMENTS.md` loses the substring `` `checkRole()`,  `` from one line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md

<interfaces>
<!-- Current auth.ts + auth.test.ts shape. Verified per 29-PATTERNS.md and 29-RESEARCH.md. -->

From src/lib/auth.ts (VERIFIED):
- Lines 1–16: module-level JSDoc + imports (`auth` from `@clerk/nextjs/server`, `redirect` from `next/navigation`, `Role` from `@/types/auth`) — ALL KEPT.
- Lines 17–41: `checkRole` JSDoc + `export async function checkRole(role: Role): Promise<boolean>` body — TARGET FOR DELETION.
- Lines 43–end: `requireRole` JSDoc + export — KEPT.

From src/lib/auth.test.ts (VERIFIED — file lives at src/lib/auth.test.ts, NOT under __tests__/):
- Lines 1–17: jest.mock setup for `@clerk/nextjs/server` and `next/navigation`, plus the `mockAuth` and `mockRedirect` helpers — ALL KEPT.
- Line 18: `import { checkRole, requireRole } from './auth';` — UPDATE to `import { requireRole } from './auth';`.
- Lines 24–64: `describe('checkRole', () => { ...5 tests... })` block — TARGET FOR DELETION. The 5 tests are: returns true when claim matches; returns false when claim does not match; returns false when sessionClaims is undefined; returns false when metadata.role is missing; returns false when userId is null (unauthenticated).
- Lines 66–90: `describe('requireRole', () => { ...3 tests... })` — KEPT.

Note (29-RESEARCH.md): CONTEXT.md says "8 checkRole unit tests" but the verified count in the `describe('checkRole')` block is 5 (the "8" total counts the entire file: 5 checkRole + 3 requireRole). Only the 5 checkRole tests are deleted; the 3 requireRole tests STAY.

From .planning/REQUIREMENTS.md (VERIFIED — line 23):
- Current: `` - [x] **ACCESS-02**: Role utility functions (`checkRole()`, `requireRole()`) available for server components ``
- Target: `` - [x] **ACCESS-02**: Role utility functions (`requireRole()`) available for server components ``
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete checkRole export and its 5 unit tests (D-12)</name>
  <files>src/lib/auth.ts, src/lib/auth.test.ts</files>
  <read_first>
    - src/lib/auth.ts (entire file — locate the `checkRole` JSDoc + function body around lines 17–41)
    - src/lib/auth.test.ts (entire file — locate the `describe('checkRole', ...)` block and the import line at line 18)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"src/lib/auth.ts — D-12" and §"src/lib/auth.test.ts — D-12"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"D-12: checkRole deletion" and §"Pitfall 3: Counting checkRole Tests as 8 Instead of 5"
  </read_first>
  <action>
    Two coordinated edits (single commit):
    1. In `src/lib/auth.ts`: delete the entire `checkRole` block — the JSDoc starting around line 17 plus the function `export async function checkRole(role: Role): Promise<boolean> { ... }` ending around line 41. Keep the module-level JSDoc and imports (lines 1–16) intact. Keep the `requireRole` JSDoc and function (lines 43–end) intact. Do NOT add a re-export shim or any backward-compatibility wrapper — clean removal per D-12.
    2. In `src/lib/auth.test.ts`: change the import on line 18 from `import { checkRole, requireRole } from './auth';` to `import { requireRole } from './auth';`. Delete the entire `describe('checkRole', () => { ... });` block (lines 24–64, containing the 5 tests). Keep all jest.mock setup at the top (lines 1–17) intact. Keep the `describe('requireRole', () => { ... });` block (lines 66–90, 3 tests) intact.
    Per CONTEXT.md D-12: this is a pure code removal — no behavior change to the kept `requireRole` function. Per RESEARCH.md Pitfall 3: the verified count is 5 tests in the `describe('checkRole')` block (not 8 as stated in CONTEXT.md); only those 5 are deleted; the 3 `requireRole` tests STAY. The file location for the tests is `src/lib/auth.test.ts` (NOT `src/lib/__tests__/auth.test.ts`).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=auth 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Negative-existence in auth.ts: `grep -cE "export.*checkRole|function checkRole" src/lib/auth.ts` returns `0`.
    - requireRole still exported in auth.ts: `grep -c "export async function requireRole" src/lib/auth.ts` returns `1`.
    - Negative-existence in auth.test.ts: `grep -c "describe('checkRole'" src/lib/auth.test.ts` returns `0` AND `grep -c "describe(\"checkRole\"" src/lib/auth.test.ts` returns `0` (covers both quote styles).
    - Import updated in auth.test.ts: `grep -c "import { requireRole } from './auth';" src/lib/auth.test.ts` returns `1` AND `grep -c "import { checkRole" src/lib/auth.test.ts` returns `0`.
    - requireRole describe block still present: `grep -c "describe('requireRole'" src/lib/auth.test.ts` (or equivalent quote variant) returns `1`.
    - Test suite passes: `npm test -- --testPathPattern=auth` exits 0; output contains `PASS` for the auth.test.ts suite; the 3 requireRole tests pass; no `FAIL` lines in the auth scope.
    - Type-check passes (no broken imports of the now-deleted symbol anywhere): `npx tsc --noEmit 2>&1 | grep -E "checkRole" | wc -l` returns `0`.
    - No production source imports the deleted symbol: `grep -rE "from ['\"]@/lib/auth['\"].*checkRole|checkRole.*from ['\"]@/lib/auth['\"]" src/ --include='*.ts' --include='*.tsx' | grep -v 'auth.test.ts' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>checkRole is removed from production and test code; the auth Jest suite stays green; no callers anywhere import the deleted symbol.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md ACCESS-02 description to reference requireRole only (D-13)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 23, the ACCESS-02 row in the Access Control section)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §".planning/REQUIREMENTS.md — D-13"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md §"Specific Ideas" (REQUIREMENTS.md edit keeps [x] Complete status)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, locate the ACCESS-02 row (around line 23 in the `### Access Control` section). Change the line from `` - [x] **ACCESS-02**: Role utility functions (`checkRole()`, `requireRole()`) available for server components `` to `` - [x] **ACCESS-02**: Role utility functions (`requireRole()`) available for server components ``. The edit drops the substring `` `checkRole()`,  `` (the backticked function name, comma, and trailing space). Keep the `- [x]` Complete status (per CONTEXT.md "Specific Ideas" — `requireRole` alone satisfies the requirement as worded for current guarding use cases). Do not edit any other line in this file. Do not touch the Traceability table at the bottom (it references ACCESS-02 → Phase 27 → Complete and remains accurate).
  </action>
  <verify>
    <automated>grep -c "checkRole" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Negative-existence: `grep -c "checkRole" .planning/REQUIREMENTS.md` returns `0`.
    - Updated text present: `grep -c "Role utility functions (\\\`requireRole()\\\`) available for server components" .planning/REQUIREMENTS.md` returns `1`.
    - Complete status preserved: `grep -c "\\[x\\] \\*\\*ACCESS-02\\*\\*" .planning/REQUIREMENTS.md` returns `1`.
    - Only one line changed (ACCESS-02 row): `git diff --numstat .planning/REQUIREMENTS.md | awk '{print $1+$2}'` returns `2` (1 line added, 1 line removed — the single replaced line).
  </acceptance_criteria>
  <done>REQUIREMENTS.md ACCESS-02 row matches the post-D-12 code reality; status stays Complete.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=auth` exits 0.
- `grep -c "checkRole" src/lib/auth.ts` returns 0; same for `src/lib/auth.test.ts` and `.planning/REQUIREMENTS.md`.
- `npx tsc --noEmit` does not surface any new errors caused by removed symbols (existing fixture errors are addressed by plan 29-06, not this plan).
- `grep -c "export async function requireRole" src/lib/auth.ts` returns 1.
</verification>

<success_criteria>
- INT-03 closed: checkRole is gone from production code, tests, and docs.
- ACCESS-02 audit trail matches reality: only requireRole is documented as a utility.
- The 3 requireRole tests still pass (they exercise the sole remaining role-guard utility).
- No backward-compat shim left behind.
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-05-SUMMARY.md`.
</output>
