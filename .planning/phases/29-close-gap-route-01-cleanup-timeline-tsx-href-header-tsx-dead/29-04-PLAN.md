---
phase: 29
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/production-smoke.spec.ts
  - e2e/route-protection.spec.ts
  - playwright.config.ts
autonomous: true
requirements:
  - ROUTE-01
tags:
  - cleanup
  - e2e
  - playwright-config
must_haves:
  truths:
    - "e2e/production-smoke.spec.ts no longer exists in the repo"
    - "playwright.config.ts has no 'production-smoke' project entry (atomic with file deletion)"
    - "e2e/route-protection.spec.ts targets the /demo/* paths (orders, customers, mill-production) plus /settings — no references to the deleted bare paths"
    - "demo-user and norole-user Playwright projects do NOT inherit .env.local's PLAYWRIGHT_BASE_URL — they explicitly use http://localhost:3000"
  artifacts:
    - path: e2e/route-protection.spec.ts
      provides: "Updated protected-routes constant + PROT-02 body targeting /demo/orders"
      contains: "/demo/orders"
    - path: playwright.config.ts
      provides: "Project config without production-smoke entry; demo-user/norole-user with explicit baseURL override"
      contains: "baseURL: 'http://localhost:3000'"
  key_links:
    - from: e2e/route-protection.spec.ts
      to: middleware redirect behavior
      via: protectedRoutes constant + page.goto/returnBackUrl assertions
      pattern: "/demo/orders"
    - from: playwright.config.ts demo-user/norole-user projects
      to: localhost dev server
      via: explicit baseURL key in project use: block
      pattern: "baseURL: 'http://localhost:3000'"
---

<objective>
Close INT-04, INT-05, and the PLAYWRIGHT_BASE_URL leak in one atomic plan — they all touch `e2e/` and `playwright.config.ts` and must not interleave with parallel work on those files. Delete `e2e/production-smoke.spec.ts` (its tests depend on a real Clerk sign-in workaround deferred from v1.4; Phase 27 added richer replacement coverage). In the same atomic edit, remove the orphaned `production-smoke` project block from `playwright.config.ts`. Repoint `e2e/route-protection.spec.ts` to the live `/demo/*` paths. Add explicit `baseURL: 'http://localhost:3000'` to the `demo-user` and `norole-user` project blocks so `.env.local`'s `PLAYWRIGHT_BASE_URL` cannot leak into the authenticated E2E projects.

Purpose: After Phase 26's clean break (no 308 redirects per Phase 26 D-01), the bare `/orders`/`/customers`/`/mill-production` paths return 404 — so E2E tests asserting redirects from those paths are passing for the wrong reason, and the production-smoke spec navigates to a dead route. Cleaning these up makes the E2E suite an accurate description of production behavior. The baseURL fix prevents demo-user/norole-user runs from hitting a production deployment when the local `.env.local` happens to have `PLAYWRIGHT_BASE_URL` set.
Output: 1 file deleted, 2 files modified. Playwright config has 1 project block removed (production-smoke), 2 project blocks gain a `baseURL` key (demo-user, norole-user). Route-protection spec has 5 path-string updates (3 in the constant + 2 in PROT-02 body).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-CONTEXT.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md
@.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md
@.planning/phases/26-route-restructuring-and-migration/26-CONTEXT.md

<interfaces>
<!-- Current Playwright project structure + current route-protection spec shape. -->

From playwright.config.ts (current — VERIFIED in 29-PATTERNS.md):
- Lines ~32–40: `production-smoke` project block — references `testMatch: '**/production-smoke.spec.ts'`. Must be removed atomically with the spec deletion (otherwise Playwright errors with "no matching test files").
- Lines ~46–52: `demo-user` project block — `use:` block has `storageState: 'playwright/.clerk/demo.json'` but no `baseURL` override → inherits global `use.baseURL` (which reads `process.env.PLAYWRIGHT_BASE_URL`).
- Lines ~53–60: `norole-user` project block — same shape as demo-user with `storageState: 'playwright/.clerk/norole.json'`.
- Global `use.baseURL` at line ~16 reads `process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000'`. The `.env.local` sets `PLAYWRIGHT_BASE_URL` (verified per 29-RESEARCH.md).

From e2e/route-protection.spec.ts (current — VERIFIED in 29-PATTERNS.md):
- Lines 13–18: `const protectedRoutes = ['/orders', '/customers', '/mill-production', '/settings'] as const;`
- Lines 34–48: PROT-02 test body hardcodes `await page.goto('/orders')` (line ~35) and `expect(returnBackUrl).toContain('/orders')` (line ~47). Both must be updated to `/demo/orders` — only updating the constant (Pitfall 5) would leave stale references in the PROT-02 body.

From e2e/production-smoke.spec.ts (current):
- File navigates to `/orders` and asserts URL state. Phase 27's `demo-route-protection.spec.ts` (via Clerk session fixtures) replaced this coverage with a more robust setup. The CONTEXT.md D-09 mandates outright deletion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete production-smoke spec AND its Playwright project block (atomic pair, D-09 + D-09b)</name>
  <files>e2e/production-smoke.spec.ts, playwright.config.ts</files>
  <read_first>
    - e2e/production-smoke.spec.ts (entire file — confirm it's the file slated for deletion)
    - playwright.config.ts (the `production-smoke` project block — lines ~32–40)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"e2e/production-smoke.spec.ts — D-09: delete file"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"Pitfall 2: Leaving Orphaned production-smoke Playwright Project"
  </read_first>
  <action>
    Two coordinated edits, executed together (single commit):
    1. Delete the file `e2e/production-smoke.spec.ts` (use `rm` or the appropriate file-deletion mechanism). The file is the entire deletion — no partial edits.
    2. In `playwright.config.ts`, locate the `production-smoke` project block (it begins with `{ name: 'production-smoke', testMatch: '**/production-smoke.spec.ts', ...` and ends with the closing `},`). Delete the entire block, including the trailing comma after the closing brace, so the remaining `projects: [ ... ]` array stays syntactically valid. Do not touch the other project blocks (`chromium`, `demo-user`, `norole-user`, etc.), the global `use:` block, or any other top-level config keys. Per CONTEXT.md D-09 + RESEARCH.md Pitfall 2, leaving the orphaned project entry would cause Playwright to error on `npx playwright test` with "no matching test files" — these two edits MUST land together.
  </action>
  <verify>
    <automated>[ ! -f e2e/production-smoke.spec.ts ] && grep -c "production-smoke" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - File does not exist: `[ ! -f e2e/production-smoke.spec.ts ]` exits 0 (and the equivalent `ls e2e/production-smoke.spec.ts` exits 2 "No such file or directory").
    - Negative-existence in playwright.config.ts: `grep -c "production-smoke" playwright.config.ts` returns `0`.
    - The Playwright config file still parses: `npx playwright test --list 2>&1 | grep -E "(error|Error)" | wc -l` returns `0` (no parse error).
    - Other project blocks survive: `grep -c "name: 'demo-user'" playwright.config.ts` returns `1` AND `grep -c "name: 'norole-user'" playwright.config.ts` returns `1` AND `grep -c "name: 'chromium'" playwright.config.ts` returns at least `1`.
  </acceptance_criteria>
  <done>The production-smoke spec is gone and its config block is gone. `npx playwright test --list` runs without parse errors. Single commit.</done>
</task>

<task type="auto">
  <name>Task 2: Update route-protection.spec.ts paths (constant + PROT-02 body, D-10)</name>
  <files>e2e/route-protection.spec.ts</files>
  <read_first>
    - e2e/route-protection.spec.ts (entire file — both the `protectedRoutes` constant at lines 13–18 AND the PROT-02 test body at lines 34–48)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"e2e/route-protection.spec.ts — D-10: path updates"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"Pitfall 5: Missing PROT-02 Path Update in D-10"
  </read_first>
  <action>
    In `e2e/route-protection.spec.ts`, make exactly these 5 string substitutions:
    1. In the `protectedRoutes` constant (around lines 13–18): replace the string literal `'/orders'` with `'/demo/orders'`.
    2. Same constant: replace `'/customers'` with `'/demo/customers'`.
    3. Same constant: replace `'/mill-production'` with `'/demo/mill-production'`.
    4. In the PROT-02 test body (around line 35): replace `await page.goto('/orders')` with `await page.goto('/demo/orders')`.
    5. In the PROT-02 test body (around line 47): replace `expect(returnBackUrl).toContain('/orders')` with `expect(returnBackUrl).toContain('/demo/orders')`.
    Keep `'/settings'` in the `protectedRoutes` array unchanged — settings is still at the bare path (it's protected by Clerk `auth.protect()` rather than the demo middleware, but the test's redirect-to-sign-in assertion is identical). Per CONTEXT.md D-10 + RESEARCH.md Pitfall 5 + Open Question 2, both the constant AND the PROT-02 hardcoded body references must be updated together; leaving the PROT-02 body stale produces a false-passing test against deleted routes.
  </action>
  <verify>
    <automated>grep -cE "'/orders'|'/customers'|'/mill-production'" e2e/route-protection.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Bare-path references gone: `grep -cE "'/orders'|'/customers'|'/mill-production'" e2e/route-protection.spec.ts` returns `0` (the bare strings in single-quote form must not appear).
    - Demo paths present in the constant: `grep -c "'/demo/orders'" e2e/route-protection.spec.ts` returns at least `2` (once in the constant, once in PROT-02 goto), `grep -c "'/demo/customers'" e2e/route-protection.spec.ts` returns at least `1`, `grep -c "'/demo/mill-production'" e2e/route-protection.spec.ts` returns at least `1`.
    - PROT-02 returnBackUrl assertion updated: `grep -c "toContain('/demo/orders')" e2e/route-protection.spec.ts` returns `1`.
    - `/settings` reference still present: `grep -c "'/settings'" e2e/route-protection.spec.ts` returns at least `1`.
    - File still parses for Playwright: `npx playwright test --list e2e/route-protection.spec.ts 2>&1 | grep -E "(error|Error)" | wc -l` returns `0`.
  </acceptance_criteria>
  <done>route-protection.spec.ts targets the live /demo/* paths exclusively; all 5 substitutions applied; file parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit baseURL override to demo-user and norole-user Playwright projects (D-16)</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (the `demo-user` block at lines ~46–52 and the `norole-user` block at lines ~53–60, post-Task-1 edits)
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-PATTERNS.md §"playwright.config.ts — D-16"
    - .planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-RESEARCH.md §"D-16: PLAYWRIGHT_BASE_URL leak fix"
  </read_first>
  <action>
    In `playwright.config.ts`, add `baseURL: 'http://localhost:3000',` to the `use:` block of both the `demo-user` and `norole-user` project entries. Specifically:
    1. In the `demo-user` project's `use:` block (currently has `...devices['Desktop Chrome']` and `storageState: 'playwright/.clerk/demo.json'`), add a new line `baseURL: 'http://localhost:3000',` after the `storageState` line.
    2. In the `norole-user` project's `use:` block (currently has `...devices['Desktop Chrome']` and `storageState: 'playwright/.clerk/norole.json'`), add a new line `baseURL: 'http://localhost:3000',` after the `storageState` line.
    Do not touch the global `use:` block (line ~16) — it still reads `process.env.PLAYWRIGHT_BASE_URL` so the `chromium` (unauthenticated) project can target a custom URL via the env var. The project-level `use.baseURL` overrides the global one for these two authenticated projects only, isolating them from `.env.local` env leak per CONTEXT.md D-16 and the Playwright project-config precedence (29-RESEARCH.md Assumption A2).
  </action>
  <verify>
    <automated>grep -c "baseURL: 'http://localhost:3000'" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both project blocks gained the baseURL: `grep -c "baseURL: 'http://localhost:3000'" playwright.config.ts` returns at least `2`.
    - The demo-user block contains both storageState and baseURL: `awk '/name: '\''demo-user'\''/,/^  \\}/' playwright.config.ts | grep -c "baseURL: 'http://localhost:3000'"` returns `1` AND the same awk extraction contains `playwright/.clerk/demo.json`.
    - The norole-user block contains both storageState and baseURL: `awk '/name: '\''norole-user'\''/,/^  \\}/' playwright.config.ts | grep -c "baseURL: 'http://localhost:3000'"` returns `1` AND the same awk extraction contains `playwright/.clerk/norole.json`.
    - Global use.baseURL is unchanged (still reads env var): `grep -c "process.env.PLAYWRIGHT_BASE_URL" playwright.config.ts` returns at least `1`.
    - File parses cleanly: `npx playwright test --list 2>&1 | grep -E "(error|Error)" | wc -l` returns `0`.
  </acceptance_criteria>
  <done>Both authenticated projects pin to localhost:3000 regardless of .env.local; global env-var behavior preserved for the unauthenticated chromium project.</done>
</task>

</tasks>

<verification>
- `[ ! -f e2e/production-smoke.spec.ts ]` exits 0.
- `grep -c "production-smoke" playwright.config.ts` returns 0.
- `grep -cE "'/orders'|'/customers'|'/mill-production'" e2e/route-protection.spec.ts` returns 0.
- `grep -c "baseURL: 'http://localhost:3000'" playwright.config.ts` returns at least 2.
- `npx playwright test --list` exits 0 (config parses; all remaining projects have matching test files).
- Manual / dev-server check (per 29-VALIDATION.md): with `npm run dev` running, `npm run test:e2e -- --project=demo-user` connects to `http://localhost:3000` (visible in Playwright trace/headers), not to whatever URL `PLAYWRIGHT_BASE_URL` resolves to in `.env.local`.
</verification>

<success_criteria>
- INT-04 closed: production-smoke spec gone, no orphaned Playwright project entry.
- INT-05 closed: route-protection spec targets live demo paths; PROT-02 body matches the constant.
- PLAYWRIGHT_BASE_URL leak (Phase 27 tech-debt item) closed: demo-user / norole-user pinned to localhost.
- Playwright config still parses; no false-passing tests against deleted routes.
</success_criteria>

<output>
After completion, create `.planning/phases/29-close-gap-route-01-cleanup-timeline-tsx-href-header-tsx-dead/29-04-SUMMARY.md`.
</output>
