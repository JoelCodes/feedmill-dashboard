---
phase: 28
plan: 06
type: execute
wave: 3
depends_on: [28-02, 28-03, 28-04, 28-05]
files_modified:
  - docs/security-patterns.md
autonomous: true
requirements: []
tags:
  - security
  - documentation
  - rsc
  - clerk
must_haves:
  truths:
    - "`docs/security-patterns.md` exists at the repository root under `docs/`."
    - "The doc has six top-level (H2) sections in the exact order specified by CONTEXT.md D-09:"
    - "  Section 1 H2 — `## 1. Audit Findings` (or `## 1. Audit findings`)"
    - "  Section 2 H2 — `## 2. The Server-Fetch Pattern` (or equivalent server-fetch heading)"
    - "  Section 3 H2 — `## 3. requireRole vs checkRole vs Middleware` (or equivalent decision-table heading)"
    - "  Section 4 H2 — `## 4. <Protect> Is UX, Not Security` (or equivalent <Protect> heading)"
    - "  Section 5 H2 — `## 5. localStorage / Browser-State Exception` (or equivalent localStorage heading)"
    - "  Section 6 H2 — `## 6. Onboarding Checklist for New Role-Gated Pages` (or equivalent onboarding heading)"
    - "Section 1 contains the per-component audit findings table with rows for every `/demo/*` page + `/settings` + the Header notifications fetch (documented as out-of-scope). Per D-03, audit findings live in this single doc — no separate 28-AUDIT.md artifact."
    - "Section 2 includes a code excerpt of the canonical server-fetch pattern mirroring `src/app/demo/customers/[id]/page.tsx`."
    - "Section 3 includes the decision table covering middleware vs `requireRole` vs `checkRole` vs `<Protect>`."
    - "Section 4 includes the verbatim Clerk caveat about `<Show />` / `<Protect>` only visually hiding children + the link to `.planning/research/PITFALLS.md` §Pitfall 6."
    - "Section 5 uses `src/app/settings/page.tsx` as the worked example for the localStorage exception."
    - "Section 6 is an onboarding checklist for adding a new role-gated page."
    - "`<Protect>` does NOT appear in any production code file under `src/` (D-10) — only in doc snippets."
  artifacts:
    - path: "docs/security-patterns.md"
      provides: "Canonical client-vs-server security reference for the codebase; audit findings + forward-looking guidelines"
      min_lines: 150
      contains: "Audit findings"
  key_links:
    - from: "docs/security-patterns.md"
      to: ".planning/research/PITFALLS.md"
      via: "§4 link to Pitfall 6 anchor"
      pattern: "PITFALLS.md.*Pitfall 6"
    - from: "docs/security-patterns.md"
      to: "src/app/demo/customers/[id]/page.tsx"
      via: "§2 code excerpt citing the canonical RSC shape"
      pattern: "demo/customers"
    - from: "docs/security-patterns.md"
      to: "src/app/settings/page.tsx"
      via: "§5 worked example for browser-state exception"
      pattern: "settings/page.tsx"
---

<objective>
Create the single forward-looking documentation deliverable for Phase 28: `docs/security-patterns.md`. The doc combines an audit-findings table (point-in-time inventory of every page's before/after status from the Wave 2 refactors) with the rules of the road for future security-gated work. This is the codified knowledge that prevents the next contributor from reaching for `<Protect>` as a security boundary or fetching sensitive data in a client component.

Purpose: Satisfy Success Criterion 2 (`<Protect>` usage documented with clear guidelines on client vs server checks) and codify the patterns established in 28-02 through 28-05 so future phases inherit them without renegotiation. Per CONTEXT.md **D-03**, audit findings live inline at the top of this same doc — no separate `28-AUDIT.md` artifact, so future readers see the point-in-time inventory and the forward-looking guidance together.

Output: `docs/security-patterns.md` — single new file, six sections per CONTEXT.md D-09, following the structural style of `docs/clerk-setup.md` (existing sister document).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-client-component-security-audit/28-CONTEXT.md
@.planning/phases/28-client-component-security-audit/28-RESEARCH.md
@.planning/phases/28-client-component-security-audit/28-PATTERNS.md
@.planning/phases/28-client-component-security-audit/28-02-SUMMARY.md
@.planning/phases/28-client-component-security-audit/28-03-SUMMARY.md
@.planning/phases/28-client-component-security-audit/28-04-SUMMARY.md
@.planning/phases/28-client-component-security-audit/28-05-SUMMARY.md
@.planning/research/PITFALLS.md
@docs/clerk-setup.md
@src/lib/auth.ts
@src/app/demo/customers/[id]/page.tsx
@src/app/settings/page.tsx
@src/middleware.ts

<style_reference>
**Match the structural conventions of `docs/clerk-setup.md`:**
- H1 title at top + one-paragraph intro.
- Numbered H2 sections in declared order.
- Tables for matrix/decision content.
- Fenced code blocks for examples.
- `**Note:** ...` inline rather than callout blocks.
- Sentence-case headings (`## The server-fetch pattern` — not Title Case).
- Inline backticks for file paths and identifiers.
- Verbatim quotes for citations (single block-quote line; cite source at end).
</style_reference>

<source_anchor_table>
**Embedded references the executor will need (already gathered):**

| Need | Source | Note |
|------|--------|------|
| Verbatim Clerk caveat for Show/Protect | `.planning/phases/28-client-component-security-audit/28-RESEARCH.md` Code Examples Example C (also Anti-Patterns) | Quote text: "The Show component only visually hides its children; the contents remain accessible via the browser's source code even if the user fails authentication or authorization checks." Cite: `clerk-docs/reference/components/control/show.mdx` |
| Pitfall 6 anchor | `.planning/research/PITFALLS.md` line 179, heading "Pitfall 6: Protect Component Exposes Data in Browser Source" | Link via relative path |
| Canonical RSC code example for section 2 | `src/app/demo/customers/[id]/page.tsx` (full file, post-28-02 — now includes `await requireRole('demo')`) | Use the literal post-refactor source |
| Decision table data for section 3 | `28-RESEARCH.md` Code Examples Example D (4 rows: middleware, requireRole, checkRole, Protect / Show) | Drop verbatim |
| Audit findings table format for section 1 | `28-RESEARCH.md` Code Examples Example E (7 rows including Header notifications footnote) | Update each row's "Component type after" / "Data fetch site after" / "Role guard" cells with the ACTUAL state from the post-Wave-2 codebase (read the SUMMARY files first to confirm what shipped vs what changed during execution) |
| Settings page localStorage call site for section 5 | `src/app/settings/page.tsx` line 18-21 (`useLocalStorage("user-preferences", defaultPreferences)`) | Use the literal call site as the worked example |
| Onboarding checklist items for section 6 | `28-RESEARCH.md` Don't Hand-Roll + Common Pitfalls 1-6 | Distill into a 6-8 item checklist |
</source_anchor_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile audit findings table against actual post-Wave-2 state</name>
  <files>(read-only verification step — produces no file artifact directly; outputs reconciliation notes consumed by Task 2)</files>
  <read_first>
    - .planning/phases/28-client-component-security-audit/28-02-SUMMARY.md
    - .planning/phases/28-client-component-security-audit/28-03-SUMMARY.md
    - .planning/phases/28-client-component-security-audit/28-04-SUMMARY.md
    - .planning/phases/28-client-component-security-audit/28-05-SUMMARY.md
    - src/app/demo/orders/page.tsx (post-28-03 actual state)
    - src/app/demo/customers/page.tsx (post-28-04 actual state)
    - src/app/demo/customers/[id]/page.tsx (post-28-02 actual state)
    - src/app/demo/mill-production/page.tsx (post-28-05 actual state)
    - src/app/settings/page.tsx (unchanged baseline)
    - src/components/Header.tsx (confirm notifications fetch is still client-side)
    - .planning/phases/28-client-component-security-audit/28-RESEARCH.md section "Code Examples" Example E (the planned-state table)
  </read_first>
  <action>
For each of the 5 in-scope routes (`/demo/orders`, `/demo/customers`, `/demo/customers/[id]`, `/demo/mill-production`, `/settings`), open the actual page file and read the post-refactor state: presence/absence of `'use client'` directive, the data-fetch site (`await getX()` in the page vs not present), and the role guard line (`await requireRole(...)` line or absence). Cross-reference each row of the planned audit table from RESEARCH.md Example E against the actual code. Produce a 5-7 bullet reconciliation note covering each route's final state and any deviations (e.g., if an executor extracted to a different filename than planned, or if a refactor introduced a Suspense wrapper the table didn't anticipate). Verify the Header notifications fetch is still client-side (run `grep -rn "getNotifications" src/components/Header.tsx` — expect at least one match). This reconciliation feeds Task 2's table cells; record it inline as a comment block at the top of `docs/security-patterns.md` is NOT acceptable — the reconciliation lives in the SUMMARY.md only. The doc file itself stays clean.
  </action>
  <verify>
    <automated>echo "=== orders ==="; head -5 src/app/demo/orders/page.tsx; echo "=== customers ==="; head -5 src/app/demo/customers/page.tsx; echo "=== customers/[id] ==="; head -5 src/app/demo/customers/\[id\]/page.tsx; echo "=== mill-production ==="; head -5 src/app/demo/mill-production/page.tsx; echo "=== settings ==="; head -5 src/app/settings/page.tsx; echo "=== Header notifications ==="; grep -c "getNotifications" src/components/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: Executor has read the four SUMMARY files and confirmed actual code state of all five page files.
    - CLI: `grep -l "await requireRole" src/app/demo/*/page.tsx src/app/demo/*/*/page.tsx 2>/dev/null | wc -l` returns `4` (orders, customers, customers/[id], mill-production — settings excluded by design).
    - CLI: `grep -c "use client" src/app/settings/page.tsx` returns at least 1 (settings remains client; D-06 / D-08 unchanged).
    - CLI: `grep -c "getNotifications" src/components/Header.tsx` returns at least 1 (Header notifications fetch is still client-side; the audit-table footnote remains accurate).
    - Behavior: Executor has a written 5-7 bullet reconciliation note ready for Task 2 (delivered inline in the eventual SUMMARY.md, not a separate file).
  </acceptance_criteria>
  <done>Reconciliation between planned audit table and actual codebase complete. Any deviations between planned and actual state are explicitly listed and ready to flow into Task 2's table cells.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/security-patterns.md with all six required sections</name>
  <files>docs/security-patterns.md</files>
  <read_first>
    - docs/clerk-setup.md (structural conventions; tone, heading style, table format, code-block style)
    - .planning/phases/28-client-component-security-audit/28-RESEARCH.md (Examples C, D, E provide the verbatim table contents + the Clerk caveat quote)
    - .planning/research/PITFALLS.md lines 179-200 area (Pitfall 6 content for cross-reference linkage)
    - src/lib/auth.ts (for the JSDoc-cited function signatures in section 3 decision table)
    - src/app/demo/customers/[id]/page.tsx (post-28-02; the literal source to embed in section 2)
    - src/app/settings/page.tsx lines 1-25 (the localStorage call site for section 5)
    - src/middleware.ts (the canonical edge-gate reference for section 3 decision table)
    - Task 1 reconciliation notes
  </read_first>
  <action>
Create `docs/security-patterns.md`. Begin with an H1 + one-paragraph intro stating the doc's purpose (audit findings + forward-looking rules; pairs with `docs/clerk-setup.md`). Then write the six numbered H2 sections in this exact order:

**Section 1 — Audit findings.** Lift the table from `28-RESEARCH.md` Example E. Columns: `Path | Component type before | Component type after | Data fetch site before | Data fetch site after | Role guard | Notes`. Seven rows: `/demo/orders`, `/demo/customers`, `/demo/customers/[id]`, `/demo/mill-production`, `/settings`, `/sign-in/*`, Header notifications (the last as a footnote row marked "out of scope, non-`/demo/*` UI metadata"). Update each row's "after" cells against the Task 1 reconciliation — e.g., the Notes column for `/demo/orders` should reference `OrdersTableContent.tsx` if 28-03 introduced that filename. Add a one-sentence date stamp under the table heading: "Findings as of phase 28 completion."

**Section 2 — The server-fetch pattern.** One-paragraph framing ("Every role-gated page follows this shape: guard, then server-side fetch, then render client child with data as a prop."). Embed the post-refactor `src/app/demo/customers/[id]/page.tsx` as a fenced TypeScript code block (literal source). Add a `**Note:**` paragraph after the block: when the page needs multiple data sources, use `Promise.all` for parallel fetch (cite `[id]/page.tsx`'s pattern as the canonical example); for a single data source, a plain `await getX()` is sufficient (cite `/demo/orders`).

**Section 3 — When to use middleware, `requireRole`, `checkRole`, or `<Protect>`.** Lift Example D's 4-row decision table verbatim: columns `Helper | Tier | Behavior on failure | When to use`. After the table, a one-paragraph guideline summarizing: "Middleware is the outer gate; `requireRole` is the inner gate at the page entry; `checkRole` is for in-page boolean branches; `<Protect>` is for UX cues, not security." Cite `src/lib/auth.ts`'s JSDoc for the `requireRole` / `checkRole` signatures.

**Section 4 — `<Protect>` is UX, not security.** Embed the do-vs-don't snippet pair from `28-RESEARCH.md` Example C (the literal three code blocks: DON'T wrap data; DO gate the fetch; DO use Protect for presentational role cues like badges). Then a paragraph including the verbatim Clerk caveat as a Markdown block-quote, with citation at the end:
> The Show component only visually hides its children; the contents remain accessible via the browser's source code even if the user fails authentication or authorization checks. For truly sensitive data that should be completely inaccessible to unauthorized users, perform authorization checks on the server before sending data to the client.

Citation line (italicized below the quote): *Source: `clerk-docs/reference/components/control/show.mdx` — the same caveat applies to `<Protect>`.*

End the section with: "See also `.planning/research/PITFALLS.md` Pitfall 6 for the project-internal canonical statement." (Use this exact relative path; it is the actual file location.)

**Section 5 — `localStorage` is not data fetching.** One-paragraph framing of the exception. Cite the worked example from `src/app/settings/page.tsx` (lines 18-21): the `useLocalStorage("user-preferences", defaultPreferences)` call. Embed those 3-4 lines as a fenced TypeScript block. State the rule explicitly: "Browser-local state (theme, density, UI preferences, locally-cached read-state) is NOT data fetching. Such state may live in a client component. The `/demo/*` rule above applies to remote data acquisition only." Add a `**Note:**` clarifying that as soon as the same data needs to be authoritative on the server (e.g., saving preferences to a backend), the read flips to server-fetched RSC and follows section 2.

**Section 6 — Onboarding checklist: adding a role-gated page.** A numbered list (6-8 items), each item is one obligation. The list:
1. Place the page at `src/app/demo/<feature>/page.tsx` (or another role-gated namespace).
2. Make the default export an `async function` — do NOT add `'use client'`.
3. As the FIRST statement in the function body, write `await requireRole('demo')` (or the appropriate role). Per Pitfall 4 in `28-RESEARCH.md`, the `await` is mandatory; missing it lets the function continue past the redirect.
4. Fetch all sensitive data after the guard, using `await` or `Promise.all`. Never `import` a service module from a `'use client'` file (Pitfall 1).
5. Render the data by passing it as a prop to a client child component. The client child handles interactivity (filter, search, URL params, keyboard nav). The client child must NOT import the service module.
6. Add a page test under `__tests__/page.test.tsx` using the `src/test/fixtures/clerkAuth.ts` fixture. Cover three branches: unauthenticated → `/sign-in`, wrong role → `/`, demo session → renders content.
7. Update `docs/security-patterns.md` section 1 (audit findings table) with a new row for the page.

End the doc with: "---" + a date footer "*Created: 2026-05-11 (phase 28)*" matching the `docs/clerk-setup.md` style (no italic date footer there; mirror whatever the existing file does — read it and copy the format exactly).

Final check before commit: run `grep -c "^## " docs/security-patterns.md` — must return exactly 6.

Commit as `docs(28-06): add security-patterns reference with audit findings + guidelines`.
  </action>
  <verify>
    <automated>test -f docs/security-patterns.md && echo "FILE EXISTS" && wc -l docs/security-patterns.md && echo "--- H2 count ---" && grep -c "^## " docs/security-patterns.md && echo "--- key markers ---" && grep -c "Audit findings\|server-fetch pattern\|requireRole\|checkRole\|Protect\|localStorage\|onboarding" docs/security-patterns.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/security-patterns.md` exists.
    - Source: `wc -l docs/security-patterns.md` returns at least 150 (the doc is substantial, not a stub).
    - Source: `grep -c "^## " docs/security-patterns.md` returns exactly 6 (six H2 sections).
    - Source: `grep -c "Audit findings\|audit findings" docs/security-patterns.md` returns at least 1 (section 1 heading or table caption present).
    - Source: `grep -c "requireRole\|checkRole" docs/security-patterns.md` returns at least 3 (decision table + intro paragraph + onboarding checklist all reference them).
    - Source: `grep -c "useLocalStorage\|localStorage" docs/security-patterns.md` returns at least 2 (section 5 references the hook + the general rule).
    - Source: `grep -c "PITFALLS.md" docs/security-patterns.md` returns at least 1 (the section 4 cross-reference).
    - Source: `grep -c "demo/customers/\[id\]/page.tsx\|customers/\[id\]" docs/security-patterns.md` returns at least 1 (the canonical RSC example is cited).
    - Source: `grep -c "visually hides" docs/security-patterns.md` returns at least 1 (the Clerk caveat verbatim quote).
    - Source: `grep -c "settings/page.tsx" docs/security-patterns.md` returns at least 1 (section 5 worked example).
    - CLI: `grep -rn "<Protect" src/ 2>/dev/null | wc -l` returns `0` (D-10: no live Protect usage in production source).
    - CLI: `grep -c "out of scope\|out-of-scope" docs/security-patterns.md` returns at least 1 (Header notifications footnote).
  </acceptance_criteria>
  <done>docs/security-patterns.md exists with all six sections; audit findings table reflects post-Wave-2 actual state; no Protect usage was introduced into source code. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → developer mental model | Misleading documentation could cause a future contributor to misplace a security check. The doc must be unambiguous about which tier owns what. |
| Doc snippets → production code | D-10: snippets in the doc must NEVER motivate a developer to add `<Protect>` to production code as a security boundary. The Clerk caveat quote + Pitfall 6 cross-reference are the mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Information disclosure | Future contributor reads the doc, misses the Protect caveat, ships a Protect-wrapped data view that leaks data | mitigate | Section 4 includes the verbatim Clerk caveat as a block-quote with explicit citation; section 6 (onboarding checklist) item 4 reinforces that client children must not import service modules. Source-level guard: Task 2 acceptance criterion `grep "<Protect" src/` returns zero — no live example in code to imitate. |
| T-28-06-02 | Repudiation | Audit findings table goes stale; reader assumes the codebase still matches the table even after a future refactor | accept | Date stamp in section 1 makes the time-bounded nature explicit ("Findings as of phase 28 completion"). Lint-rule enforcement is out of scope per CONTEXT.md "Deferred Ideas." |
| T-28-06-03 | Tampering | Doc inadvertently advises a pattern that contradicts middleware (D-04) or settings exception (D-06/D-08) | mitigate | Decision table in section 3 explicitly names middleware as outer layer (D-04); section 5 names settings as the explicit exception (D-06/D-08). Cross-referenced to CONTEXT.md decision IDs implicitly via consistency. |
| T-28-06-04 | Spoofing | Reader confuses `<Show>` and `<Protect>` (both Clerk control components; the caveat technically reads "Show" in the quote) | mitigate | Section 4 explicitly states "the same caveat applies to `<Protect>`" immediately after the verbatim quote so the reader does not need to deduce the equivalence. |
</threat_model>

<verification>
- `test -f docs/security-patterns.md` exits zero.
- `wc -l docs/security-patterns.md` returns at least 150.
- `grep -c "^## " docs/security-patterns.md` returns exactly 6.
- `grep -rn "<Protect" src/ 2>/dev/null` returns no matches (D-10 holds — no Protect usage was introduced anywhere in source).
- `grep -rn "import.*requireRole.*'@/lib/auth'" src/components/ src/hooks/ 2>/dev/null` returns NO matches (Pitfall 1 — `requireRole` is never imported into a client component). This is a regression-style check on the whole codebase; it must remain zero by the end of Phase 28.
- All four `/demo/*` page files contain `await requireRole('demo')`: `grep -l "await requireRole" src/app/demo/orders/page.tsx src/app/demo/customers/page.tsx src/app/demo/customers/[id]/page.tsx src/app/demo/mill-production/page.tsx | wc -l` returns `4`.
- `npm test -- --runInBand` (full suite) exits zero. The doc-only change in this plan should not break any tests.
</verification>

<success_criteria>
- Success Criterion 2 satisfied: `<Protect>` usage documented in a forward-looking reference.
- Audit findings table reflects the actual post-Wave-2 codebase (not the planning-time aspiration).
- All six required sections present in the documented order.
- The doc is consumable by a new contributor without requiring them to read the planning artifacts — it is self-contained guidance.
- No production code regression: no new Protect usage; no client component importing `requireRole`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-component-security-audit/28-06-SUMMARY.md` covering: the reconciliation between planned audit table (RESEARCH.md Example E) and actual post-refactor state (any cells that changed), confirmation of all six sections present, the result of the cross-codebase guard checks (`grep "<Protect" src/`, `requireRole` in client components), and a one-paragraph closer suitable for the phase's overall close-out: what shipped, what remains deferred (live Protect usage, lint rules), and the green-light test for whether the next phase inherits the boundary correctly.
</output>
