---
phase: 28
plan: 05
type: execute
wave: 2
depends_on: [28-02]
files_modified:
  - src/app/demo/mill-production/page.tsx
  - src/app/demo/mill-production/__tests__/page.test.tsx
  - src/components/MillProductionUI.tsx
  - src/components/__tests__/MillProductionUI.test.tsx
autonomous: true
requirements: []
tags:
  - security
  - rsc
  - refactor
  - mill-production
must_haves:
  truths:
    - "`src/app/demo/mill-production/page.tsx` is an async Server Component (no `'use client'` directive)."
    - "`MillProductionPage()` calls `await requireRole('demo')` BEFORE `await getProductionOrders()`."
    - "A new client component `src/components/MillProductionUI.tsx` owns the filter strip (`activeStates: Set<ProductionState>`), filter logic, and column rendering."
    - "`MillProductionUI` receives `orders: ProductionOrder[]` as a prop; no service import."
    - "The helper components (`ProductionCard`, `StateSection`, `MillColumn`, constants) live inside `MillProductionUI.tsx` (lifted verbatim from the old page)."
    - "Non-demo session → page rejects with `{ url: '/' }`; missing userId → rejects with `{ url: '/sign-in' }`."
    - "D-01: `/demo/mill-production` is now an async Server Component that calls `await requireRole('demo')`, `await`s the mock-service fetch (`getProductionOrders`), and renders a client child with data passed as props — the canonical server-fetch pattern."
    - "D-02: The filter-strip interactivity (filter pill toggling, state counts) is preserved in a new `MillProductionUI` client component that receives `orders` as a prop. A thin wrapper was justified per D-02 because the original page tightly coupled `Set<ProductionState>` initialization to its own data fetch."
    - "D-05: `requireRole('demo')` is added to `/demo/mill-production/page.tsx` as part of the D-05 enumeration of all `/demo/*` entry points."
    - "D-07: Even though `getProductionOrders()` returns mock data today, it is treated as canonical 'sensitive' — server-fetched after the role check with no exit ramp for client-side fetching. Real data lands into the same shape unchanged."
  artifacts:
    - path: "src/app/demo/mill-production/page.tsx"
      provides: "Async RSC mill-production page with role guard and server-side fetch"
      contains: "await requireRole('demo')"
    - path: "src/components/MillProductionUI.tsx"
      provides: "Client component owning filter state and column layout"
      contains: "interface MillProductionUIProps"
    - path: "src/components/__tests__/MillProductionUI.test.tsx"
      provides: "Component tests for filter toggle, state counts, column distribution"
      contains: "orders={mockOrders}"
    - path: "src/app/demo/mill-production/__tests__/page.test.tsx"
      provides: "Migrated to async RSC test harness with redirect branches"
      contains: "redirects to /"
  key_links:
    - from: "src/app/demo/mill-production/page.tsx"
      to: "src/components/MillProductionUI.tsx"
      via: "orders prop pass-through"
      pattern: "<MillProductionUI orders={"
    - from: "src/app/demo/mill-production/page.tsx"
      to: "@/services/millProduction"
      via: "await getProductionOrders() at RSC level"
      pattern: "await getProductionOrders()"
    - from: "src/app/demo/mill-production/page.tsx"
      to: "@/lib/auth"
      via: "import { requireRole }"
      pattern: "requireRole.*@/lib/auth"
---

<objective>
Refactor `/demo/mill-production` from a client component with internal fetch + filter state to an async Server Component that calls `await requireRole('demo')`, fetches production orders on the server, and passes them as a prop to a new `MillProductionUI` client component owning the filter strip, state counts, and column rendering.

Purpose: Satisfy Success Criteria 1 and 3 for `/demo/mill-production` — production data fetched server-side after the role guard; client component holds only interactivity. Per CONTEXT.md **D-07**, mock data is treated as canonical "sensitive" — the pattern carries forward unchanged when real data lands.

Output:
- `src/app/demo/mill-production/page.tsx`: async RSC with guard + `getProductionOrders` + render `<MillProductionUI orders={orders} />`.
- `src/components/MillProductionUI.tsx` (NEW): `'use client'` component containing the lifted helper functions and the filter-strip / column UI body; receives `orders` prop.
- `src/components/__tests__/MillProductionUI.test.tsx` (NEW): component tests for filter pill toggle, state count math, column distribution.
- `src/app/demo/mill-production/__tests__/page.test.tsx`: migrated to test the RSC (async invocation, redirect branches, fetch assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-client-component-security-audit/28-CONTEXT.md
@.planning/phases/28-client-component-security-audit/28-RESEARCH.md
@.planning/phases/28-client-component-security-audit/28-PATTERNS.md
@.planning/phases/28-client-component-security-audit/28-01-SUMMARY.md
@.planning/phases/28-client-component-security-audit/28-02-SUMMARY.md
@src/lib/auth.ts
@src/app/demo/customers/[id]/page.tsx
@src/app/demo/mill-production/page.tsx
@src/app/demo/mill-production/__tests__/page.test.tsx

<interfaces>
<!-- From src/services/millProduction.ts -->
```typescript
export function getProductionOrders(): Promise<ProductionOrder[]>;
```

<!-- From src/types/millProduction.ts -->
```typescript
export interface ProductionOrder {
  id: string;
  orderNumber: string;
  customer: string;
  product: string;
  weightLbs: number;
  deliveryTime: string;
  state: ProductionState;  // "Completed" | "Mixing" | "Blocked" | "Pending"
  millLine: MillLine;      // "Premix" | "Excel" | "CGM"
}
export type ProductionState = "Completed" | "Mixing" | "Blocked" | "Pending";
export type MillLine = "Premix" | "Excel" | "CGM";
```

<!-- Target MillProductionUI signature -->
```typescript
'use client';
interface MillProductionUIProps {
  orders: ProductionOrder[];
}
export default function MillProductionUI({ orders }: MillProductionUIProps): JSX.Element;
```

<!-- Current mill-production/page.tsx (BEFORE):
     'use client' at line 1
     Lines 13-67 declare constants: STATE_ORDER, STATE_COLORS, PRODUCTION_STATE_PILL_CONFIG — KEEP, move into MillProductionUI.tsx
     Lines 69-74 formatWeight helper — KEEP, move
     Lines 76-101 ProductionCard subcomponent — KEEP, move
     Lines 103-132 StateSection subcomponent — KEEP, move
     Lines 134-173 MillColumn subcomponent — KEEP, move
     Lines 175-196 LoadingSkeleton — DROP (no loading state needed)
     Lines 198-273 MillProductionPage component body (useState<orders>, useState<loading>, useEffect fetch, useMemo state counts, filter logic, render) — split: the useState<orders>/useState<loading>/useEffect block DELETED; the rest moves into MillProductionUI.tsx
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MillProductionUI client component + component tests (RED → GREEN)</name>
  <files>src/components/MillProductionUI.tsx, src/components/__tests__/MillProductionUI.test.tsx</files>
  <read_first>
    - src/app/demo/mill-production/page.tsx (full 273 lines — lift constants, helper components, and the body of MillProductionPage minus the fetch effect; LoadingSkeleton is dropped)
    - src/components/OrdersTable.tsx (post-28-03 — the shape of "client component receiving data as prop")
    - src/types/millProduction.ts (verify the ProductionOrder shape)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (`MillProductionUI.tsx` NEW client-data-component spec)
  </read_first>
  <behavior>
    - Test 1: `MillProductionUI` renders the three mill column headings ("Premix", "Excel", "CGM") when given any non-empty orders fixture.
    - Test 2: State counts on filter pills match the fixture (e.g., 2 orders with state "Mixing" → "Mixing" pill shows count "2"). Use `screen.getByText` or by adjacent count element. (Note: existing test asserts via inline check via `waitFor` — convert to synchronous.)
    - Test 3: Clicking a filter pill toggles its active state and filters the rendered orders. Assert the filtered order count appears (e.g., click "Mixing" → only Mixing-state ProductionCards render).
    - Test 4: With `orders={[]}`, the component renders the filter strip but no ProductionCards. (Important: do NOT render LoadingSkeleton — it was dropped.)
    - Test 5: Filter-pill clicks are cumulative (clicking "Mixing" then "Completed" shows both). Toggle semantics: clicking the same pill twice clears it.
    - Test 6 (design-token regression — carries from existing mill-production page test): no hardcoded `bg-gray-200` class appears in the rendered output; the token-based `bg-[var(--divider)]` appears at least once if any skeleton/divider markup remains (since LoadingSkeleton is dropped, this assertion likely returns zero for both — that's OK; the test must be re-scoped to assert design-token classes on the ProductionCard border, e.g., `style={{ backgroundColor: 'var(--status-completed-border)' }}` per existing STATE_COLORS map).
    - Source assertion: `grep -c "from '@/services/millProduction'" src/components/MillProductionUI.tsx` returns `0`.
  </behavior>
  <action>
**Create `src/components/MillProductionUI.tsx`:** Top: `'use client';`. Imports: `useState, useMemo` from `react`; `ProductionOrder, ProductionState, MillLine` from `@/types/millProduction`; `FilterPill, FilterPillColorConfig` from `@/components/ui/FilterPill`. Lift verbatim from the old page.tsx (lines 13-173): `STATE_ORDER`, `STATE_COLORS`, `PRODUCTION_STATE_PILL_CONFIG`, `formatWeight`, `ProductionCard`, `StateSection`, `MillColumn`. Do NOT lift `LoadingSkeleton` (lines 175-196 — drop). Define `interface MillProductionUIProps { orders: ProductionOrder[]; }`. Export default function `MillProductionUI({ orders }: MillProductionUIProps)`. Body lifts from old page.tsx lines 199-272 with these DELETIONS: remove `const [orders, setOrders] = useState<ProductionOrder[]>([])` (line 199), remove `const [loading, setLoading] = useState(true)` (line 200), remove the entire `useEffect(() => { getProductionOrders().then(...).catch(...) }, []);` block (lines 227-239), remove the `loading ? <LoadingSkeleton /> : (...)` conditional — render the column flex directly. KEEP `const [activeStates, setActiveStates] = useState<Set<ProductionState>>(new Set())`, `toggleState`, `stateCounts` useMemo, `filteredOrders` useMemo, and the `ordersByMill` reduction. The return is `<><div className="flex gap-2.5">[filter pills]</div><div className="flex gap-6"><MillColumn millLine="Premix" .../><MillColumn millLine="Excel" .../><MillColumn millLine="CGM" .../></div></>`. IMPORTANT: drop the `<DashboardLayout>` wrapper from the old page body — the layout wraps the RSC page now, not this client component.

**Create `src/components/__tests__/MillProductionUI.test.tsx`:** Component test with no Clerk fixture needed. Build `mockOrders: ProductionOrder[]` with at least one order per state (Mixing, Completed, Blocked, Pending) so state-count assertions are meaningful; distribute across all three mill lines. `userEvent` for filter pill clicks. Implement the 6 behavior tests. All renders are synchronous (`render(<MillProductionUI orders={mockOrders} />)`). Test 4 uses `render(<MillProductionUI orders={[]} />)`. Run: `npm test -- --runInBand src/components/__tests__/MillProductionUI.test.tsx` — MUST GO GREEN. Commit as `feat(28-05): extract MillProductionUI client component`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/components/__tests__/MillProductionUI.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/components/__tests__/MillProductionUI.test.tsx` exits zero with at least 6 tests passing.
    - Source: `head -1 src/components/MillProductionUI.tsx` returns `'use client';`.
    - Source: `grep -c "from '@/services/millProduction'" src/components/MillProductionUI.tsx` returns `0`.
    - Source: `grep -c "useEffect" src/components/MillProductionUI.tsx` returns `0`.
    - Source: `grep -c "LoadingSkeleton" src/components/MillProductionUI.tsx` returns `0` (loading skeleton dropped).
    - Source: `grep -c "useState<ProductionOrder\[\]>" src/components/MillProductionUI.tsx` returns `0` (orders comes via prop).
    - Source: `grep "interface MillProductionUIProps" src/components/MillProductionUI.tsx` returns at least one line.
    - Source: `grep -c "STATE_ORDER\|ProductionCard\|StateSection\|MillColumn" src/components/MillProductionUI.tsx` returns at least 4 (constants + helper components lifted).
    - CLI: `npx tsc --noEmit 2>&1 | grep "src/components/MillProductionUI" | grep -c "error TS"` returns `0`.
  </acceptance_criteria>
  <done>MillProductionUI exists, accepts orders prop, has 6 passing tests covering filter toggle, state counts, column distribution, empty state, and design tokens. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert mill-production page.tsx to async RSC + migrate its page test</name>
  <files>src/app/demo/mill-production/page.tsx, src/app/demo/mill-production/__tests__/page.test.tsx</files>
  <read_first>
    - src/app/demo/mill-production/page.tsx (full 273 lines — being mostly replaced; the imports/helpers/constants moved to MillProductionUI.tsx in Task 1)
    - src/app/demo/mill-production/__tests__/page.test.tsx (full 157 lines — existing mocks to keep, design-token tests to adapt or move)
    - src/components/MillProductionUI.tsx (the just-created consumer)
    - src/app/demo/customers/[id]/page.tsx + src/app/demo/orders/page.tsx (post-28-03 — parallel patterns)
    - src/test/fixtures/clerkAuth.ts (fixture API)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (Pattern Assignments §`/demo/mill-production/page.tsx`)
  </read_first>
  <behavior>
    - `MillProductionPage()` is async, no args, returns JSX.
    - First statement: `await requireRole('demo')`. Second: `const orders = await getProductionOrders()`. Return: `<DashboardLayout><MillProductionUI orders={orders} /></DashboardLayout>`.
    - No `'use client'` directive. No `useState`/`useEffect`/`useMemo` imports.
    - Three new redirect-branch tests cover sign-in / non-demo / admin.
    - Existing tests are migrated to async invocation. The "LoadingSkeleton uses design tokens (no bg-gray-200)" test from the old file (currently lines 80-97 of the existing page test) is RELOCATED to the new `MillProductionUI.test.tsx` (Task 1, Test 6) OR adapted in place if the executor decides the page test should retain a coarse design-token regression. The "FilterPill renders state filters" test (lines 64-77 of existing) likewise belongs on the component now.
  </behavior>
  <action>
**Step A — Overwrite `src/app/demo/mill-production/page.tsx`:** Replace the entire file. Imports: `import { requireRole } from '@/lib/auth';`, `import { getProductionOrders } from '@/services/millProduction';`, `import DashboardLayout from '@/components/DashboardLayout';`, `import MillProductionUI from '@/components/MillProductionUI';`. Export: `export default async function MillProductionPage()` with body — `await requireRole('demo'); const orders = await getProductionOrders(); return ( <DashboardLayout><MillProductionUI orders={orders} /></DashboardLayout> );`. Do NOT add `'use client'`. Do NOT lift helpers back into this file. Do NOT wrap in Suspense — there is no `useSearchParams` boundary here.

**Step B — Migrate `src/app/demo/mill-production/__tests__/page.test.tsx`:** Replace `jest.mock('next/navigation', ...)` with `jest.mock('next/navigation', nextNavigationMockFactory)`. Add `jest.mock('@clerk/nextjs/server', clerkAuthMockFactory)`. Import the fixture symbols (`mockAuth`, `mockDemoSession`, `mockNonDemoSession`, `mockUnauthenticatedSession`, both factories). Keep the existing `jest.mock('@clerk/nextjs', ...)` block, `jest.mock('@/services/millProduction', ...)` block, and `jest.mock('@/services/notifications', ...)` block. In `beforeEach`: ADD `mockAuth.mockReset(); mockDemoSession();` first; keep `(getProductionOrders as jest.Mock).mockResolvedValue(mockOrders)`.

Convert every `render(<MillProductionPage />)` to `const element = await MillProductionPage(); render(element);`. Remove `await waitFor(...)` wrappers around assertions of statically-rendered content (data is now synchronously available before render).

**Adapt or relocate design-token tests:** The "LoadingSkeleton uses design tokens" test (current lines 80-97) cannot run as-is because the LoadingSkeleton was dropped in Task 1 (no longer renders). DELETE that test from the page test file (the design-token assertion is preserved by Task 1's Test 6 on the COMPONENT — ProductionCard's border uses `STATE_COLORS[order.state].border` which is a `var(--status-*)` token). The "FilterPill renders state filters" test (lines 64-77) can either stay (it asserts state-name text appears in the page's rendered output; that still works post-refactor because MillProductionUI renders the pills) or be migrated to MillProductionUI.test.tsx (Task 1 covers it via Test 5). Pick the simpler path: keep these tests in the page test file but use the new render pattern.

**Add 3 NEW redirect-branch tests** at the start of the main describe: `redirects to /sign-in when unauthenticated`, `redirects to / when role is not demo`, `redirects to / when role is admin`. Each calls the appropriate helper, then `await expect(MillProductionPage()).rejects.toMatchObject({ url: '/' or '/sign-in' })`.

Run: `npm test -- --runInBand src/app/demo/mill-production` — MUST GO GREEN. Commit as `refactor(28-05): mill-production page becomes async RSC with requireRole guard`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/app/demo/mill-production 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/app/demo/mill-production` exits zero.
    - Source: `grep -c "^'use client'" src/app/demo/mill-production/page.tsx` returns `0`.
    - Source: `grep "export default async function MillProductionPage" src/app/demo/mill-production/page.tsx` returns at least one line.
    - Source: `grep "await requireRole('demo')" src/app/demo/mill-production/page.tsx` returns at least one line.
    - Source: `awk '/await requireRole/{r=NR} /await getProductionOrders/{g=NR} END{exit (r<g ? 0 : 1)}' src/app/demo/mill-production/page.tsx` exits `0`.
    - Source: `wc -l src/app/demo/mill-production/page.tsx` returns ≤ 25 lines.
    - Source: `grep -c "useState\|useEffect\|useMemo" src/app/demo/mill-production/page.tsx` returns `0`.
    - Source: `grep -c "redirects to /\|redirects to /sign-in" src/app/demo/mill-production/__tests__/page.test.tsx` returns at least 3.
    - Source: `grep -c "clerkAuthMockFactory" src/app/demo/mill-production/__tests__/page.test.tsx` returns at least 1.
    - Source: `grep -c "render(<MillProductionPage" src/app/demo/mill-production/__tests__/page.test.tsx` returns `0`.
    - Source: `grep -c "await MillProductionPage()" src/app/demo/mill-production/__tests__/page.test.tsx` returns at least 1.
    - CLI: `npx tsc --noEmit 2>&1 | grep -E "src/app/demo/mill-production|src/components/MillProductionUI" | grep -c "error TS"` returns `0`.
  </acceptance_criteria>
  <done>page.tsx is a thin async RSC; page test green with redirect coverage; LoadingSkeleton-related test relocated or removed appropriately. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware (edge) | ACCESS-01 demo gate at route level — unchanged. |
| Middleware → RSC | NEW page-level `requireRole('demo')` guard before `getProductionOrders`. |
| RSC → mock service | `getProductionOrders()` executes server-side ONLY. ProductionOrder data crosses as a prop. |
| RSC → MillProductionUI client | Data passes via serialized prop edge. Client never imports `@/services/millProduction`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Information disclosure | Production-order data shipped in client bundle via MillProductionUI importing `@/services/millProduction` | mitigate | Task 1 acceptance criterion enforces zero `from '@/services/millProduction'` imports in MillProductionUI.tsx. |
| T-28-05-02 | Elevation of privilege | Non-demo user reaching `/demo/mill-production` due to middleware drift | mitigate | Page-level `await requireRole('demo')` (D-05). Three new redirect-branch tests in the page test file. |
| T-28-05-03 | Tampering | Developer re-introduces `useEffect(() => getProductionOrders().then(setOrders))` in MillProductionUI | accept | Phase-scoped point-in-time guard (Task 1 source assertion). Future lint-rule phase out of scope. |
| T-28-05-04 | Repudiation | No logging | accept | Per Phase 25 D-02. |
</threat_model>

<verification>
- `npm test -- src/app/demo/mill-production --runInBand` exits zero.
- `npm test -- src/components/__tests__/MillProductionUI.test.tsx --runInBand` exits zero.
- `npm test -- --runInBand` (full suite) exits zero.
- `npx tsc --noEmit` is clean.
- `grep -rn "from '@/services/millProduction'" src/components/ src/hooks/` returns NO matches.
</verification>

<success_criteria>
- `/demo/mill-production` follows the canonical RSC + client-wrapper pattern.
- Filter strip, state counts, and column rendering live in MillProductionUI (with its own test file).
- Page test migrated to async RSC harness with 3 redirect-branch tests added.
- LoadingSkeleton dropped; design-token regression preserved at the component level via the existing `STATE_COLORS` `var(--status-*-border)` map (still in source, still asserted in component tests).
- No new `'use client'` files import service modules.
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-component-security-audit/28-05-SUMMARY.md` covering: the MillProductionUI extraction scope (lifted constants/helpers/state), the LoadingSkeleton deletion rationale, the new before/after row for the 28-06 audit findings table, and any cross-file fallout discovered (e.g., if another file imported `LoadingSkeleton` from mill-production/page.tsx — verify with `grep -rn "LoadingSkeleton" src/`).
</output>
