---
phase: 28
plan: 04
type: execute
wave: 2
depends_on: [28-02]
files_modified:
  - src/app/demo/customers/page.tsx
  - src/app/demo/customers/page.test.tsx
  - src/app/demo/customers/__tests__/page.test.tsx
  - src/components/CustomersList.tsx
  - src/components/__tests__/CustomersList.test.tsx
autonomous: true
requirements: []
tags:
  - security
  - rsc
  - refactor
  - customers
must_haves:
  truths:
    - "`src/app/demo/customers/page.tsx` is an async Server Component (no `'use client'` directive)."
    - "`CustomersPage()` calls `await requireRole('demo')` BEFORE `await getCustomers()`."
    - "Sort (`sortCustomersByRecentActivity`) runs on the server before data crosses the prop boundary."
    - "A new client component `src/components/CustomersList.tsx` owns search + row-click navigation; receives `customers: CustomerWithStats[]` as a prop."
    - "Non-demo session → page rejects with `{ url: '/' }`; missing userId → rejects with `{ url: '/sign-in' }`."
    - "Both existing test files (`customers/page.test.tsx` and `customers/__tests__/page.test.tsx`) are migrated to either test the RSC via async invocation or test the new CustomersList component directly."
  artifacts:
    - path: "src/app/demo/customers/page.tsx"
      provides: "Async RSC customer list page with role guard and server-side fetch + sort"
      contains: "await requireRole('demo')"
    - path: "src/components/CustomersList.tsx"
      provides: "Client component for customer list interactivity (search + row click)"
      contains: "interface CustomersListProps"
    - path: "src/components/__tests__/CustomersList.test.tsx"
      provides: "Component tests for CustomersList rendering, search behavior, row-click navigation"
      contains: "customers={mockCustomers}"
    - path: "src/app/demo/customers/page.test.tsx"
      provides: "Migrated to test the async RSC: render via await invocation; covers redirect branches"
      contains: "redirects to /"
    - path: "src/app/demo/customers/__tests__/page.test.tsx"
      provides: "Migrated similarly OR consolidated (see Task 3)"
      contains: "CustomersPage"
  key_links:
    - from: "src/app/demo/customers/page.tsx"
      to: "src/components/CustomersList.tsx"
      via: "customers prop pass-through"
      pattern: "<CustomersList customers={"
    - from: "src/app/demo/customers/page.tsx"
      to: "@/services/customers"
      via: "await getCustomers() at the RSC level"
      pattern: "await getCustomers()"
    - from: "src/app/demo/customers/page.tsx"
      to: "@/utils/customerSort"
      via: "sortCustomersByRecentActivity called server-side"
      pattern: "sortCustomersByRecentActivity"
    - from: "src/app/demo/customers/page.tsx"
      to: "@/lib/auth"
      via: "import { requireRole }"
      pattern: "requireRole.*@/lib/auth"
---

<objective>
Refactor `/demo/customers` (the list page) from a client component with internal fetch + sort + search to an async Server Component that calls `await requireRole('demo')`, fetches and sorts on the server, then passes the sorted customers as a prop to a new `CustomersList` client component owning search + row-click navigation. Migrate both existing test files for this page (they predate Phase 28 and were authored against the client shape).

Purpose: Satisfy Success Criteria 1 and 3 for `/demo/customers` — sensitive customer data fetched server-side after the role guard; client component never touches the service module.

Output:
- `src/app/demo/customers/page.tsx`: async RSC; guard + server fetch + server sort + render `<CustomersList customers={sorted} />`.
- `src/components/CustomersList.tsx` (NEW): `'use client'` component owning search state, `useDebounce`, `useRouter` for row-click navigation; receives `customers: CustomerWithStats[]` prop; renders the existing JSX from the old page body.
- `src/components/__tests__/CustomersList.test.tsx` (NEW): component-level tests for search, empty state, status indicators, row-click navigation — synchronous renders from prop, no service mock.
- `src/app/demo/customers/page.test.tsx`: migrated to test the RSC (async invocation, redirect branches, fetch+sort assertions).
- `src/app/demo/customers/__tests__/page.test.tsx`: see Task 3 — either migrated or consolidated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-client-component-security-audit/28-CONTEXT.md
@.planning/phases/28-client-component-security-audit/28-RESEARCH.md
@.planning/phases/28-client-component-security-audit/28-PATTERNS.md
@.planning/phases/28-client-component-security-audit/28-01-SUMMARY.md
@.planning/phases/28-client-component-security-audit/28-02-SUMMARY.md
@src/lib/auth.ts
@src/app/demo/customers/[id]/page.tsx
@src/app/demo/customers/page.tsx
@src/app/demo/customers/page.test.tsx
@src/app/demo/customers/__tests__/page.test.tsx

<interfaces>
<!-- From src/services/customers.ts (consumed by the RSC) -->
```typescript
export function getCustomers(): Promise<CustomerWithStats[]>;
```

<!-- From src/utils/customerSort.ts -->
```typescript
export function sortCustomersByRecentActivity(customers: CustomerWithStats[]): CustomerWithStats[];
```

<!-- From src/types/customer.ts (consumed) -->
```typescript
export interface CustomerWithStats {
  id: string;
  name: string;
  location?: string;
  /* ... per fixture in existing tests; full shape varies by test */
  createdAt: Date;
  updatedAt: Date;
  stats: {
    totalOrders: number;
    activeOrders: number;
    completedOrders: number;
    hasChanges: boolean;
    binAlertLevel: 'none' | 'low' | 'critical';
    activeBins?: number;
  };
}
```

<!-- Target signature for CustomersList -->
```typescript
'use client';
interface CustomersListProps {
  customers: CustomerWithStats[];  // pre-sorted by the RSC page
}
export default function CustomersList({ customers }: CustomersListProps): JSX.Element;
```

<!-- Current customers/page.tsx (BEFORE):
     'use client' at line 1
     Lines 1-12 imports include useState/useEffect/useMemo/useRouter/useDebounce/getCustomers/sortCustomersByRecentActivity
     Lines 13-30 define CustomerTableSkeleton (loading state — DROP per RESEARCH §Pitfall 5)
     Lines 32-42 define EmptyState (KEEP — moves to CustomersList.tsx)
     Lines 44-188 are the CustomersPage component body (search state, useEffect fetch, useMemo filter, JSX) — moves to CustomersList.tsx, minus the useState<customers>/useState<loading>/useState<error>/useEffect block
-->
</interfaces>

<two_test_files_note>
**There are TWO existing test files for the customers list page:**
- `src/app/demo/customers/page.test.tsx` (242 lines, imports `from './page'`)
- `src/app/demo/customers/__tests__/page.test.tsx` (276 lines, imports `from '../page'`)

Both target the same `CustomersPage` default export. This duplication predates Phase 28 (likely a Phase 13 migration artifact). Task 3 below addresses the strategy: migrate both in place to test the new async RSC shape (each gains its `clerkAuthMockFactory` block and async render invocation), since both files contain non-overlapping test cases — consolidating them is OUT OF SCOPE for this security audit (would expand the diff without security benefit).

Verify both files import paths during execution: the executor must NOT delete either file unless it manually re-verifies that all unique test cases survive.
</two_test_files_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CustomersList client component + component tests (RED → GREEN)</name>
  <files>src/components/CustomersList.tsx, src/components/__tests__/CustomersList.test.tsx</files>
  <read_first>
    - src/app/demo/customers/page.tsx (full 188 lines — every piece of behavior that moves into CustomersList: imports lines 1-12 except service+sort, EmptyState lines 32-42, search/useMemo/handleRowClick lines 49-74, JSX lines 76-187)
    - src/components/OrdersTable.tsx (post-28-03 — the shape of "client component receiving data as prop")
    - src/types/customer.ts (the `CustomerWithStats` interface)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (`CustomersList.tsx` NEW client-data-component spec)
  </read_first>
  <behavior>
    - Test 1: `CustomersList` renders the first customer's name when given `customers={mockCustomers}`.
    - Test 2: Empty state ("No customers found") renders when `customers={[]}`.
    - Test 3: Typing in the search input filters by name (case-insensitive substring match). Use `userEvent.type` and a small debounce wait (`act` + `jest.advanceTimersByTime(300)` if fake timers, or `waitFor` for debounce flush). Search debounce is 300ms.
    - Test 4: Clicking a customer row calls `router.push('/demo/customers/CUST-001')` (assert via the mocked `useRouter().push`).
    - Test 5: Status indicator (`status-orders`, `status-bin-low`, `status-changes`) data-testids render correctly for customers with the relevant stats.
    - All assertions are SYNCHRONOUS (no `await waitFor` wrapping initial render — no fetch happens).
    - Source assertion: `grep -c "from '@/services/customers'" src/components/CustomersList.tsx` returns `0`.
  </behavior>
  <action>
**Create `src/components/CustomersList.tsx`:** Top: `'use client';`. Imports: `useState, useMemo` from `react`; `useRouter` from `next/navigation`; `Package, AlertTriangle, Search, Users` from `lucide-react`; `Card` from `@/components/ui/Card`; `useDebounce` from `@/hooks/useDebounce`; `CustomerWithStats` from `@/types/customer`. Define `EmptyState` helper (lift from `customers/page.tsx` lines 32-42 verbatim). Do NOT define `CustomerTableSkeleton` — drop entirely (no loading state needed; data arrives via prop). Define `interface CustomersListProps { customers: CustomerWithStats[]; }`. Export default function `CustomersList({ customers }: CustomersListProps)`. Inside: `const router = useRouter()`, `const [searchTerm, setSearchTerm] = useState('')`, `const debouncedSearch = useDebounce(searchTerm, 300)`. `filteredCustomers` useMemo (lift from lines 66-70: filter by debouncedSearch lowercased substring of customer.name). `handleRowClick` callback (lift from lines 72-74: `router.push(\`/demo/customers/${customerId}\`)`). Render JSX: lift the entire `<Card>...` block from page.tsx lines 79-186 — BUT remove the `loading ? <CustomerTableSkeleton />` branch and the `error ?` branch (lines 109-118 and the loading branch at 109-110); the conditional becomes `{filteredCustomers.length === 0 ? <EmptyState /> : filteredCustomers.map(...)}`. Update the `aria-live` polite block (lines 101-105) to drop the `loading ? 'Loading customers...' :` branch — just the empty / count states remain. Wrap entire return in `<Card className="flex flex-1 flex-col">` per existing shape.

**Create `src/components/__tests__/CustomersList.test.tsx`:** Standard component test (no Clerk fixture needed — this is a pure client component, not a page). `jest.mock('next/navigation', () => ({ useRouter: jest.fn(() => mockRouter), usePathname: jest.fn(() => '/demo/customers') }))`. Define `mockRouter = { push: jest.fn() }` and reset in beforeEach. Build `mockCustomers: CustomerWithStats[]` fixture (3 entries — name "Greenfield Farms", "Valley Ranch", "Highland Feed" — mirror the existing fixture in customers/page.test.tsx lines 43-86). Implement the 5 behavior tests listed. Tests rendering only call `render(<CustomersList customers={mockCustomers} />)` synchronously; no `await waitFor` needed at render time (debounce wait only when testing search). Run: `npm test -- --runInBand src/components/__tests__/CustomersList.test.tsx` — MUST GO GREEN. Commit as `feat(28-04): extract CustomersList client component`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/components/__tests__/CustomersList.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/components/__tests__/CustomersList.test.tsx` exits zero with all 5 tests passing.
    - Source: `head -1 src/components/CustomersList.tsx` returns `'use client';` (with surrounding quotes).
    - Source: `grep -c "from '@/services/customers'" src/components/CustomersList.tsx` returns `0`.
    - Source: `grep -c "useEffect" src/components/CustomersList.tsx` returns `0` (no fetch effect; no loading-sync effect).
    - Source: `grep -c "CustomerTableSkeleton" src/components/CustomersList.tsx` returns `0` (the skeleton was dropped).
    - Source: `grep "interface CustomersListProps" src/components/CustomersList.tsx` returns at least one line.
    - Source: `grep "customers: CustomerWithStats\[\]" src/components/CustomersList.tsx` returns at least one line.
    - CLI: `npx tsc --noEmit 2>&1 | grep "src/components/CustomersList" | grep -c "error TS"` returns `0`.
  </acceptance_criteria>
  <done>CustomersList component exists, accepts customers prop, has 5 passing tests covering render/empty/search/row-click/status indicators. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert customers/page.tsx to async RSC</name>
  <files>src/app/demo/customers/page.tsx</files>
  <read_first>
    - src/app/demo/customers/page.tsx (full file — being rewritten)
    - src/components/CustomersList.tsx (the just-created consumer)
    - src/app/demo/customers/[id]/page.tsx (canonical async RSC pattern, including guard placement)
    - src/app/demo/orders/page.tsx (post-28-03 — the parallel pattern)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (Pattern Assignments §`/demo/customers/page.tsx`)
  </read_first>
  <behavior>
    - `CustomersPage()` is async, takes no arguments, returns JSX.
    - First statement is `await requireRole('demo')`.
    - Second statement is `const customers = sortCustomersByRecentActivity(await getCustomers())` — sort runs server-side.
    - Returns `<DashboardLayout><CustomersList customers={customers} /></DashboardLayout>`.
    - No `'use client'` directive.
    - No `useState`, `useEffect`, `useMemo`, `useRouter` imports.
  </behavior>
  <action>
Overwrite `src/app/demo/customers/page.tsx`. Replace the entire file with the new async RSC shape. Imports: `import { requireRole } from '@/lib/auth';`, `import { getCustomers } from '@/services/customers';`, `import { sortCustomersByRecentActivity } from '@/utils/customerSort';`, `import DashboardLayout from '@/components/DashboardLayout';`, `import CustomersList from '@/components/CustomersList';`. Export: `export default async function CustomersPage()` with body — `await requireRole('demo'); const customers = sortCustomersByRecentActivity(await getCustomers()); return ( <DashboardLayout><CustomersList customers={customers} /></DashboardLayout> );`. Do NOT add a `'use client'` directive. Do NOT add a Suspense boundary (this page has no `useSearchParams` boundary need — search debounce is internal to CustomersList and doesn't require Suspense). Commit as `refactor(28-04): customers page becomes async RSC with requireRole guard`.
  </action>
  <verify>
    <automated>grep -c "^'use client'\|export default async function CustomersPage\|await requireRole('demo')\|await getCustomers\|sortCustomersByRecentActivity" src/app/demo/customers/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^'use client'" src/app/demo/customers/page.tsx` returns `0`.
    - Source: `grep "export default async function CustomersPage" src/app/demo/customers/page.tsx` returns at least one line.
    - Source: `grep "await requireRole('demo')" src/app/demo/customers/page.tsx` returns at least one line.
    - Source: `awk '/await requireRole/{r=NR} /await getCustomers/{g=NR} END{exit (r<g ? 0 : 1)}' src/app/demo/customers/page.tsx` exits `0`.
    - Source: `grep -c "sortCustomersByRecentActivity" src/app/demo/customers/page.tsx` returns at least 1 (called from the RSC).
    - Source: `grep -c "useState\|useEffect\|useMemo\|useRouter" src/app/demo/customers/page.tsx` returns `0` (no client hooks remain).
    - Source: `wc -l src/app/demo/customers/page.tsx` returns a small number (≤ 25 lines — this is a thin RSC).
    - CLI: `npx tsc --noEmit 2>&1 | grep "src/app/demo/customers/page.tsx" | grep -c "error TS"` returns `0`.
  </acceptance_criteria>
  <done>page.tsx is a thin async RSC. Commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate both customer page test files to RSC test harness</name>
  <files>src/app/demo/customers/page.test.tsx, src/app/demo/customers/__tests__/page.test.tsx</files>
  <read_first>
    - src/app/demo/customers/page.test.tsx (full 242 lines — every test case to preserve)
    - src/app/demo/customers/__tests__/page.test.tsx (full 276 lines — every test case to preserve)
    - src/app/demo/customers/[id]/page.test.tsx (post-28-02 — the RSC test pattern)
    - src/components/__tests__/CustomersList.test.tsx (the new component tests — interactive search/click behaviors that USED to live in the page test now live there)
    - src/test/fixtures/clerkAuth.ts (fixture API)
    - .planning/phases/28-client-component-security-audit/28-RESEARCH.md (test harness pattern)
  </read_first>
  <behavior>
    - Both page test files invoke the page as an async function (`const element = await CustomersPage(); render(element)`), NOT as JSX (`render(<CustomersPage />)`).
    - Both files include 3 new redirect-branch tests (sign-in / non-demo / admin) at the top of their main describe.
    - Both files use the 28-01 fixture (`clerkAuthMockFactory`, `nextNavigationMockFactory`, helpers).
    - Existing tests that asserted "search filters customers" or "click navigates to detail page" now appear in CustomersList.test.tsx (Task 1) — those exact assertions are DELETED from the page tests because the page no longer owns that behavior. The page tests retain assertions about whether the page renders the customer name (proves data was fetched + passed via prop).
    - Tests that previously asserted on `getCustomers` being called via `useEffect` are rewritten to assert `getCustomers` was called once during the RSC execution.
    - Both files pass full test suite.
  </behavior>
  <action>
**For BOTH files (`src/app/demo/customers/page.test.tsx` AND `src/app/demo/customers/__tests__/page.test.tsx`):**

Replace the `jest.mock('next/navigation', ...)` block at the top with `jest.mock('next/navigation', nextNavigationMockFactory)`. Add `jest.mock('@clerk/nextjs/server', clerkAuthMockFactory)` near the top. Import `mockAuth`, `mockDemoSession`, `mockNonDemoSession`, `mockUnauthenticatedSession`, `clerkAuthMockFactory`, `nextNavigationMockFactory` from `'@/test/fixtures/clerkAuth'`. Keep the existing `jest.mock('@clerk/nextjs', ...)` block (Header's ClerkLoaded/UserButton). Keep `jest.mock('@/services/customers', ...)` and `jest.mock('@/services/notifications', ...)`. The `jest.mock('@/utils/customerSort', ...)` block (present in customers/page.test.tsx lines 32-34) should be KEPT or REPLACED with a no-op pass-through; since sort now runs server-side in the RSC, this mock determines whether the test asserts sorted-or-original order.

In `beforeEach`: ADD `mockAuth.mockReset(); mockDemoSession();` as first statements. Keep existing `(getCustomers as jest.Mock).mockResolvedValue(mockCustomers)`.

Convert every `render(<CustomersPage />)` to `const element = await CustomersPage(); render(element);`. Wrap render in `await` since CustomersPage is now async. Replace every `await waitFor(() => expect(screen.getByText('Greenfield Farms')).toBeInTheDocument())` — the page is now async-resolved before render, so the assertion can be synchronous: `expect(screen.getByText('Greenfield Farms')).toBeInTheDocument();`.

**Delete tests that no longer belong on the page** (they belong on CustomersList.test.tsx now): search-filter behavior (assertions on `setSearchTerm` debounced filter), row-click navigation assertions, EmptyState rendering via empty fixture. These behaviors are component-level concerns post-refactor. Replace with simpler assertions: "page renders the customer list" (assert at least one customer name appears), "page calls getCustomers exactly once", "page calls sortCustomersByRecentActivity exactly once" (if sort is mocked) OR "page renders customers in sorted order" (if sort runs real). Choose based on which the existing test file mocked.

**Add 3 NEW redirect-branch tests** at the start of the main describe block in EACH file: `redirects to /sign-in when unauthenticated`, `redirects to / when role is not demo`, `redirects to / when role is admin` — each calls the fixture helper then `await expect(CustomersPage()).rejects.toMatchObject({ url: '/' or '/sign-in' })`.

If a test asserts hardcoded design-token classes (`rounded-[15px]`, `bg-gray-100`) on the page rendering — those are now under CustomersList, so either move that assertion to CustomersList.test.tsx (preferred if not already there) or update the page test to assert via the rendered element (`container.querySelector` on the RSC result).

Run: `npm test -- --runInBand src/app/demo/customers` — MUST GO GREEN across BOTH files. Commit as `test(28-04): migrate customer page tests to async RSC harness`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/app/demo/customers 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/app/demo/customers` exits zero. Note: scope includes `[id]/page.test.tsx` (28-02), the list page tests modified here, AND `__tests__/page.test.tsx`.
    - Source: `grep -c "clerkAuthMockFactory" src/app/demo/customers/page.test.tsx` returns at least 1.
    - Source: `grep -c "clerkAuthMockFactory" src/app/demo/customers/__tests__/page.test.tsx` returns at least 1.
    - Source: `grep -c "redirects to /\|redirects to /sign-in" src/app/demo/customers/page.test.tsx` returns at least 3.
    - Source: `grep -c "redirects to /\|redirects to /sign-in" src/app/demo/customers/__tests__/page.test.tsx` returns at least 3.
    - Source: `grep -c "render(<CustomersPage" src/app/demo/customers/page.test.tsx src/app/demo/customers/__tests__/page.test.tsx` returns `0` (no synchronous JSX render of an async RSC).
    - Source: `grep -c "await CustomersPage()" src/app/demo/customers/page.test.tsx src/app/demo/customers/__tests__/page.test.tsx` returns at least 1 in each file (async invocation pattern).
    - CLI: No test takes more than 5 seconds (`--testTimeout 5000` in CI would pass).
  </acceptance_criteria>
  <done>Both customer page test files green. The 3 redirect branches covered in each. Search/row-click behavior tests now live in CustomersList.test.tsx. Commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware (edge) | ACCESS-01 demo gate at route level — unchanged. |
| Middleware → RSC | NEW page-level `requireRole('demo')` guard before `getCustomers`. |
| RSC → mock service | `getCustomers()` + `sortCustomersByRecentActivity()` execute server-side ONLY. Customer data crosses the boundary as a pre-sorted prop. |
| RSC → CustomersList client | Data passes via serialized prop edge. Client component never imports `@/services/customers`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information disclosure | Customer data shipped in client bundle via CustomersList importing `@/services/customers` | mitigate | Task 1 acceptance criterion enforces zero `from '@/services/customers'` imports in CustomersList.tsx. |
| T-28-04-02 | Elevation of privilege | Non-demo user reaching `/demo/customers` due to middleware drift | mitigate | Page-level `await requireRole('demo')` (D-05) runs first. Three new redirect-branch tests in EACH page test file (6 new tests total in this plan) prove the three branches. |
| T-28-04-03 | Information disclosure | Sort algorithm leaks information about activity ordering before role check | mitigate | Sort runs AFTER the role check (line-order assertion in Task 2's acceptance criteria). |
| T-28-04-04 | Tampering | Developer accidentally re-introduces `useEffect(() => getCustomers().then(setCustomers))` in CustomersList | accept | Phase-scoped point-in-time guard (Task 1 source assertion). A lint rule belongs to a future tooling phase per CONTEXT.md "Deferred Ideas." |
| T-28-04-05 | Repudiation | No logging | accept | Per Phase 25 D-02 — intentional. |
</threat_model>

<verification>
- `npm test -- src/app/demo/customers --runInBand` exits zero. This covers `[id]/page.test.tsx` (28-02), `page.test.tsx` (this plan), and `__tests__/page.test.tsx` (this plan).
- `npm test -- src/components/__tests__/CustomersList.test.tsx --runInBand` exits zero.
- `npm test -- --runInBand` (full suite) exits zero — no other test should reference the old `CustomersPage` JSX-render pattern.
- `npx tsc --noEmit` is clean.
- `grep -rn "from '@/services/customers'" src/components/ src/hooks/` returns NO matches (no client component imports the service).
</verification>

<success_criteria>
- `/demo/customers` follows the canonical RSC + client-wrapper pattern.
- Search + row-click behavior live in CustomersList (with their own test file).
- Both legacy page test files migrated to the RSC harness (preserving non-overlapping coverage).
- 3 redirect-branch tests in each page test file (6 net new redirect tests).
- No new `'use client'` files import service modules.
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-component-security-audit/28-04-SUMMARY.md` covering: the CustomersList extraction (what JSX moved, what stayed); the test-file consolidation decision (kept both files, migrated each in place — or, if the executor chose otherwise, why); the new before/after row for the 28-06 audit findings table; any tricky bits with the `customerSort` mock (sort runs server-side now; the test mock might need adjusting depending on whether tests assert order).
</output>
