---
phase: 28
plan: 02
type: tdd
wave: 1
depends_on: [28-01]
files_modified:
  - src/app/demo/customers/[id]/page.tsx
  - src/app/demo/customers/[id]/page.test.tsx
autonomous: true
requirements: []
tags:
  - security
  - rsc
  - requireRole
  - clerk
must_haves:
  truths:
    - "Calling `CustomerDetailPage({ params: Promise.resolve({ id: '...' }) })` with a non-demo session rejects with NEXT_REDIRECT and `url: '/'`."
    - "Calling `CustomerDetailPage(...)` with a missing userId rejects with NEXT_REDIRECT and `url: '/sign-in'`."
    - "Calling `CustomerDetailPage(...)` with a demo session continues into the existing Promise.all data fetch and renders the customer name (existing pass-through behavior preserved)."
    - "`src/app/demo/customers/[id]/page.tsx` contains `await requireRole('demo')` as the FIRST statement in the async function body, BEFORE `const { id } = await params`."
    - "The file imports `requireRole` from `@/lib/auth`."
    - "No other line of the existing file is touched (Promise.all shape, notFound branch, DashboardLayout render — unchanged)."
  artifacts:
    - path: "src/app/demo/customers/[id]/page.tsx"
      provides: "Async RSC customer detail page with page-level demo-role guard (defense-in-depth)"
      contains: "await requireRole('demo')"
    - path: "src/app/demo/customers/[id]/page.test.tsx"
      provides: "Test coverage for the new redirect branches plus regression coverage for existing render cases"
      contains: "redirects to /sign-in"
  key_links:
    - from: "src/app/demo/customers/[id]/page.tsx"
      to: "@/lib/auth"
      via: "import { requireRole } from '@/lib/auth'"
      pattern: "import.*requireRole.*@/lib/auth"
    - from: "src/app/demo/customers/[id]/page.test.tsx"
      to: "src/test/fixtures/clerkAuth"
      via: "jest.mock factories + mockAuth helpers"
      pattern: "clerkAuthMockFactory|mockNonDemoSession|mockUnauthenticatedSession"
---

<objective>
Add `await requireRole('demo')` to the existing async Server Component at `src/app/demo/customers/[id]/page.tsx` and prove its three branches (sign-in redirect, demo-mismatch redirect, demo pass-through) with three new tests. This is the MINIMAL-DELTA validation of the canonical pattern — only one production file is touched, and only one line is added. If this slice does not green, plans 28-03/04/05 must not start.

Purpose: Validate the canonical guard placement and the consumer pattern for the 28-01 fixture before the three parallel refactors begin. Touch as little as possible: this page is ALREADY an async RSC, so the only change is the guard line.

Output:
- `src/app/demo/customers/[id]/page.tsx` with one new import + one new line at the top of the function body.
- `src/app/demo/customers/[id]/page.test.tsx` migrated to use the 28-01 fixture, with three new branch-coverage tests and all existing tests still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-client-component-security-audit/28-CONTEXT.md
@.planning/phases/28-client-component-security-audit/28-RESEARCH.md
@.planning/phases/28-client-component-security-audit/28-PATTERNS.md
@.planning/phases/28-client-component-security-audit/28-01-SUMMARY.md
@src/lib/auth.ts
@src/lib/auth.test.ts
@src/app/demo/customers/[id]/page.tsx
@src/app/demo/customers/[id]/page.test.tsx

<interfaces>
<!-- From src/lib/auth.ts (production contract) -->
```typescript
export function requireRole(role: Role): Promise<void>;
// - Redirects to '/sign-in' if userId is missing
// - Redirects to '/' if sessionClaims.metadata.role !== role
// - Resolves with undefined on success
```

<!-- From src/test/fixtures/clerkAuth.ts (28-01 output) -->
```typescript
export const mockAuth: jest.Mock;
export function clerkAuthMockFactory(): { auth: () => unknown };
export function nextNavigationMockFactory(): {
  redirect: (url: string) => never;
  notFound: () => never;
  usePathname: jest.Mock;
  useRouter: jest.Mock;
  useSearchParams: jest.Mock;
};
export function mockDemoSession(): void;
export function mockNonDemoSession(role?: Role): void;
export function mockUnauthenticatedSession(): void;
```

<!-- From src/app/demo/customers/[id]/page.tsx (existing function signature) -->
```typescript
export default async function CustomerDetailPage({
  params,
}: { params: Promise<{ id: string }> }): Promise<JSX.Element>;
```

<!-- The existing page.test.tsx (276 lines, in __tests__/ — see note below) ALREADY mocks
     'next/navigation' with `notFound: jest.fn()` only; this plan replaces that block with
     the 28-01 nextNavigationMockFactory which provides BOTH `notFound` and `redirect`. -->
</interfaces>

<file_location_note>
**Test file location:** The canonical existing test is at `src/app/demo/customers/[id]/page.test.tsx` (254 lines, sibling to page.tsx — NOT in a `__tests__/` directory). This plan modifies that file in place.

Do NOT confuse with `src/app/demo/customers/__tests__/page.test.tsx` or `src/app/demo/customers/page.test.tsx` (both belong to the customers LIST page and are covered by plan 28-04).
</file_location_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing redirect-branch tests for CustomerDetailPage (RED)</name>
  <files>src/app/demo/customers/[id]/page.test.tsx</files>
  <read_first>
    - src/app/demo/customers/[id]/page.test.tsx (full 254 lines — existing jest.mock placement, existing 5 tests, the `beforeEach` shape; THIS FILE IS BEING MODIFIED IN PLACE)
    - src/test/fixtures/clerkAuth.ts (the fixture API from 28-01; how to wire it as the jest.mock factory)
    - src/test/fixtures/clerkAuth.test.ts (the consumer pattern proven in 28-01)
    - src/lib/auth.test.ts (canonical redirect-sentinel-throw assertion shape, lines 67-89)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (the in-place addition spec for this file)
  </read_first>
  <behavior>
    - New Test A: `CustomerDetailPage({ params: Promise.resolve({ id: 'CUST-001' }) })` rejects with `{ url: '/sign-in' }` when `mockUnauthenticatedSession()` was called in beforeEach override.
    - New Test B: `CustomerDetailPage(...)` rejects with `{ url: '/' }` when `mockNonDemoSession('user')` was set.
    - New Test C: `CustomerDetailPage(...)` rejects with `{ url: '/' }` when `mockNonDemoSession('admin')` was set (proves any non-demo role redirects, not just 'user').
    - All existing 5 tests in the file continue to pass (regression guard) once the file is wired to set `mockDemoSession()` in beforeEach.
  </behavior>
  <action>
Modify `src/app/demo/customers/[id]/page.test.tsx` in place. At the top, ADD `jest.mock('@clerk/nextjs/server', clerkAuthMockFactory)` and replace the existing `jest.mock('next/navigation', ...)` block (lines 5-11) with `jest.mock('next/navigation', nextNavigationMockFactory)` — import both factories from `@/test/fixtures/clerkAuth`. Import `mockDemoSession`, `mockNonDemoSession`, `mockUnauthenticatedSession`, `mockAuth` from the same module. In the existing `beforeEach` block (currently `jest.clearAllMocks(); (getBinsByCustomerId as jest.Mock)...`), ADD `mockAuth.mockReset(); mockDemoSession();` as the first two statements so all existing tests default to a demo session pass-through. Add three new `it(...)` cases inside the existing `describe('CustomerDetailPage', ...)` block (insert AFTER the "renders CustomerDetailHeader" test, BEFORE the nested `describe('partial failure handling', ...)`): the three cases listed in `<behavior>`. Each new case calls the appropriate helper (`mockUnauthenticatedSession()`, `mockNonDemoSession('user')`, `mockNonDemoSession('admin')`) and asserts `await expect(CustomerDetailPage({ params: Promise.resolve({ id: 'CUST-001' }) })).rejects.toMatchObject({ url: <expected> })`. Run the test — Tests A/B/C MUST FAIL because the production page does not yet call `requireRole`. Commit as `test(28-02): add redirect-branch tests for CustomerDetailPage`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/app/demo/customers/\[id\]/page.test.tsx 2>&1 | grep -E "(FAIL|redirects to /sign-in|redirects to / when role)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/app/demo/customers/\[id\]/page.test.tsx` exits non-zero (RED) because tests A/B/C fail (no redirect thrown — `CustomerDetailPage` continues into Promise.all instead).
    - Source: `grep -c "redirects to /sign-in\|redirects to /" src/app/demo/customers/\[id\]/page.test.tsx` returns at least 3 (one for /sign-in, two for /).
    - Source: `grep -c "clerkAuthMockFactory\|nextNavigationMockFactory" src/app/demo/customers/\[id\]/page.test.tsx` returns at least 2.
    - Source: The file still contains the original `getBinsByCustomerId` / `getOrdersByCustomerId` / `notFound` setup in `beforeEach` (existing tests preserved).
    - Source: `grep -c "mockDemoSession()" src/app/demo/customers/\[id\]/page.test.tsx` returns at least 1 (set in beforeEach).
  </acceptance_criteria>
  <done>Three new tests committed; suite is red on those three tests; existing 5 tests still green (regression check: count passing tests in jest output).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add requireRole guard to CustomerDetailPage (GREEN)</name>
  <files>src/app/demo/customers/[id]/page.tsx</files>
  <read_first>
    - src/app/demo/customers/[id]/page.tsx (full 37 lines — the EXACT existing shape; only one line is being inserted)
    - src/lib/auth.ts (the JSDoc usage example, lines 58-65)
    - .planning/phases/28-client-component-security-audit/28-PATTERNS.md (Pattern Assignments: `[id]/page.tsx` minimal-delta)
    - src/app/demo/customers/[id]/page.test.tsx (the now-failing test file from Task 1)
  </read_first>
  <behavior>
    - All 3 new redirect-branch tests from Task 1 pass.
    - All 5 existing tests in `page.test.tsx` continue to pass.
    - The file diff is exactly two added lines: one `import { requireRole } from '@/lib/auth';` and one `await requireRole('demo');` inside the function body.
  </behavior>
  <action>
Modify `src/app/demo/customers/[id]/page.tsx` in place. Add the import line `import { requireRole } from '@/lib/auth';` after the existing `import { getOrdersByCustomerId }` line (line 8) — placing it at the bottom of the import group keeps the diff minimal. Inside the `CustomerDetailPage` async function body, insert `await requireRole('demo');` as the VERY FIRST statement — BEFORE the existing `const { id } = await params;` line (currently line 16). Do NOT modify any other line. Do NOT touch the Promise.all block, the notFound branch, or the JSX return. Run the test — all 8 cases (3 new + 5 existing) MUST PASS. Commit as `feat(28-02): add page-level demo-role guard to customer detail page`.
  </action>
  <verify>
    <automated>npm test -- --runInBand src/app/demo/customers/\[id\]/page.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm test -- src/app/demo/customers/\[id\]/page.test.tsx` exits zero with 8/8 passing (5 existing + 3 new).
    - Source: `grep -c "await requireRole" src/app/demo/customers/\[id\]/page.tsx` returns exactly `1`.
    - Source: `grep -c "from '@/lib/auth'" src/app/demo/customers/\[id\]/page.tsx` returns exactly `1`.
    - Source: The `await requireRole('demo');` line precedes the `await params` line — assert via `awk '/await requireRole/{r=NR} /await params/{p=NR} END{exit (r<p ? 0 : 1)}' src/app/demo/customers/\[id\]/page.tsx; echo $?` returns `0`.
    - Source: `git diff src/app/demo/customers/\[id\]/page.tsx | grep -c "^+"` is at most 4 (one import line + one guard line + git diff `+++` header lines, allow some slack for blank lines).
    - CLI: `npx tsc --noEmit 2>&1 | grep "src/app/demo/customers/\[id\]/page.tsx" | grep -c "error TS"` returns `0`.
  </acceptance_criteria>
  <done>8/8 tests pass. Commit `feat(28-02): add page-level demo-role guard to customer detail page` lands. The canonical pattern is now proven on a real page and ready for parallel application in 28-03/04/05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware (edge) | ACCESS-01 demo-role gate runs first; rejects non-demo at the edge with redirect to `/`. |
| Middleware → page RSC | NEW inner guard added in this plan: `await requireRole('demo')` rejects again at the page entry if any middleware drift exposes the route. |
| RSC → mock service modules | `getCustomerById`, `getActivityEvents`, `getBinsByCustomerId`, `getOrdersByCustomerId` are server-side only after this change. The data NEVER touches the client until passed as props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Elevation of privilege | `/demo/customers/[id]` accessible without demo role due to middleware drift (e.g., matcher regression) | mitigate | Inner page-level `await requireRole('demo')` (D-05) — even if `src/middleware.ts` ever stops matching `/demo/[id]`, the page-level guard still redirects to `/`. Verified by Task 1's tests B and C. |
| T-28-02-02 | Information disclosure | Sensitive customer data fetched before the guard runs | mitigate | Per D-01 / RESEARCH §Pitfall 4, `requireRole` is the FIRST statement before any data fetch. Task 2's source assertion enforces the order via the `awk` line-number check. |
| T-28-02-03 | Tampering | Developer accidentally drops the `await` keyword (Pitfall 4 in RESEARCH) | mitigate | Task 2's behavior-test set covers Tests A/B/C which all `await expect(...).rejects` — a missing `await` would let the function proceed into Promise.all and not throw NEXT_REDIRECT, failing the test. |
| T-28-02-04 | Repudiation | No logging of failed role checks | accept | Per Phase 25 D-02 (carried forward) — intentional non-logging this milestone. Phase 28 inherits. |
</threat_model>

<verification>
- `npm test -- src/app/demo/customers/\[id\]/page.test.tsx` exits zero with 8 passing tests (5 existing + 3 new branch tests).
- `npm test -- --runInBand` (full suite) exits zero (regression: no other test breaks because this is an in-place addition with backward-compatible session default).
- Source guarantee: `await requireRole('demo')` appears before `await params` in the page file (Task 2 acceptance criterion).
- `npx tsc --noEmit` is clean across the modified files.
</verification>

<success_criteria>
- The minimal-delta refactor pattern is proven on a real, already-RSC page: one import + one guard line + three new tests.
- The 28-01 fixture is exercised end-to-end by a real consumer for the first time — any fixture API drift surfaces here before plans 28-03/04/05 begin.
- Defense-in-depth verified: tests B and C prove the page rejects non-demo even though middleware also rejects (test runs the page function directly, bypassing middleware — this is the inner-layer proof).
- Commit history: one RED commit (failing branch tests), one GREEN commit (guard added + all tests pass).
</success_criteria>

<output>
After completion, create `.planning/phases/28-client-component-security-audit/28-02-SUMMARY.md` covering: the exact diff applied to page.tsx, the test count before/after (5 → 8), any fixture-import quirks encountered (e.g., if Jest hoisting required adjusting the factory shape from 28-01), and explicit confirmation that all 8 tests pass.
</output>
