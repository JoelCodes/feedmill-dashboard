---
phase: 06-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - designs/mill-production.pen
autonomous: false
requirements:
  - DESGN-01
  - DESGN-02

must_haves:
  truths:
    - "User can see status filter pills design in mill-production.pen"
    - "Filter pills show 4 statuses: Completed, Mixing, Blocked, Pending"
    - "Each pill has count badge showing total orders per status"
    - "Design shows hover, active, and filtered states"
    - "User can approve design before implementation begins"
  artifacts:
    - path: "designs/mill-production.pen"
      provides: "Updated mill production design with filter pills"
      contains: "Status Filters"
  key_links:
    - from: "designs/mill-production.pen"
      to: "src/app/globals.css"
      via: "Color values matching design tokens"
      pattern: "#48bb78.*#e53e3e.*#975a16.*#cbd5e0"
---

<objective>
Design status filter pills for mill production view in mill-production.pen file.

Purpose: Establish visual design for filter pills before Phase 8 implementation. User approval ensures design matches expectations before any code is written.

Output: Updated mill-production.pen with 4 frames showing filter pill states (default, hover, active, filtered result).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design/06-CONTEXT.md

# Design files
@designs/mill-production.pen
@designs/order-dashboard.pen (reference for filter pill patterns)

# Design tokens
@src/app/globals.css
</context>

<interfaces>
<!-- Filter pill pattern from order-dashboard.pen -->
Filter pill structure:
- Frame with cornerRadius: 12, padding: [6, 10], gap: 6
- Ellipse (8x8) for status dot
- Text: fontSize 11, fontWeight 700
- Count badge frame: cornerRadius 8, padding: [2, 7]
- Count text: fontSize 10

Status color mapping (from globals.css):
- Completed: --success (#48bb78), --success-dark (#2f855a), --success-light (#c6f6d5)
- Mixing: --warning (#975a16), --warning-light (#fefcbf)
- Blocked: --error (#e53e3e), --error-dark (#c53030), --error-light (#fed7d7)
- Pending: --pending (#cbd5e0), --pending-light (#edf2f7)

Mill production view structure (from mill-production.pen):
- Main Content frame contains Header and Production Content
- Filter strip goes between Header (id: ksWVD) and Production Content (id: prodContent)
- Current Production Content starts at y: 80 (will shift down to accommodate filter strip)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Status Filters frame to Mill Production View</name>
  <files>designs/mill-production.pen</files>
  <read_first>
    - designs/mill-production.pen (current structure to modify)
    - designs/order-dashboard.pen (filter pill pattern reference, lines 1162-1480)
    - src/app/globals.css (color tokens)
  </read_first>
  <action>
Update mill-production.pen to add a Status Filters frame between Header and Production Content in the "Mill Production View" frame (id: wwb9c).

1. In the "Main Content" frame (id: 7nKKB), add a new "Status Filters" frame after the Header with:
   - name: "Status Filters"
   - width: "fill_container"
   - gap: 10
   - alignItems: "center"
   - justifyContent: "flex_start" (per D-02: left-aligned compact)

2. Create 4 filter pills inside Status Filters, in order (per specifics): Completed, Mixing, Blocked, Pending

3. Each pill follows order-dashboard.pen pattern:
   - Frame with cornerRadius: 12, padding: [6, 10], gap: 6, alignItems: "center"
   - Ellipse (8x8) for status dot with status color
   - Text: fontFamily "Helvetica", fontSize 11, fontWeight "700"
   - Count badge frame: cornerRadius 8, padding: [2, 7], fill with 22 opacity
   - Count text: fontSize 10

4. Use these EXACT colors from globals.css:
   - Completed: dot=#48bb78, text=#2f855a, bg=#c6f6d5ff, count-bg=#2f855a22
   - Mixing: dot=#975a16, text=#975a16, bg=#fefcbfff, count-bg=#975a1622
   - Blocked: dot=#e53e3e, text=#c53030, bg=#fed7d7ff, count-bg=#c5303022
   - Pending: dot=#cbd5e0, text=#718096, bg=#edf2f7ff, count-bg=#71809622

5. Use placeholder counts: Completed (6), Mixing (4), Blocked (2), Pending (3)

6. Shift Production Content down by updating the "Column Headers" group y position from 0 to 48 (to accommodate filter strip height ~40px + spacing)
  </action>
  <verify>
    <automated>grep -c '"name": "Status Filters"' designs/mill-production.pen | grep -q '^1$' && echo "PASS: Status Filters frame exists"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"name": "Status Filters"' returns exactly 1 match
    - grep '"name": "Filter Completed"' returns exactly 1 match
    - grep '"name": "Filter Mixing"' returns exactly 1 match
    - grep '"name": "Filter Blocked"' returns exactly 1 match
    - grep '"name": "Filter Pending"' returns exactly 1 match
    - grep '"content": "Completed"' in Status Filters section returns match
    - grep '"fill": "#c6f6d5ff"' exists (Completed pill background)
    - grep '"fill": "#fefcbfff"' exists (Mixing pill background)
    - grep '"fill": "#fed7d7ff"' exists (Blocked pill background)
    - grep '"fill": "#edf2f7ff"' exists (Pending pill background)
  </acceptance_criteria>
  <done>Status filter pills appear in mill-production.pen with correct structure, colors, and count badges</done>
</task>

<task type="auto">
  <name>Task 2: Create interaction state frames (hover, active, filtered)</name>
  <files>designs/mill-production.pen</files>
  <read_first>
    - designs/mill-production.pen (after Task 1 modifications)
  </read_first>
  <action>
Duplicate the "Mill Production View" frame 3 times to show interaction states per D-07.

1. Duplicate frame "Mill Production View" (id: wwb9c) 3 times, positioning each 2000px to the right of the previous:
   - Frame 1 (original): name "Mill Production View - Default" at x: 13495
   - Frame 2: name "Mill Production View - Hover" at x: 15495
   - Frame 3: name "Mill Production View - Active" at x: 17495
   - Frame 4: name "Mill Production View - Filtered" at x: 19495

2. In "Mill Production View - Hover" frame:
   - Add stroke to "Filter Completed" pill: {"align": "inside", "thickness": 1.5, "fill": "#2f855aff"}
   - This shows hover state on first pill

3. In "Mill Production View - Active" frame:
   - Make "Filter Completed" pill appear selected:
     - Change fill to solid color (#48bb78ff) instead of light (#c6f6d5ff)
     - Change text fill to white (#ffffffff)
     - Change dot fill to white (#ffffffff)
     - Change count-bg fill to rgba white (#ffffff33)
     - Change count text to white (#ffffffff)
   - Also make "Filter Blocked" pill selected (same pattern with #e53e3eff base)
   - This shows multi-select state

4. In "Mill Production View - Filtered" frame:
   - Keep Completed and Blocked pills active (same as Frame 3)
   - In Production Cards group, visually indicate filtering:
     - Add opacity 0.3 to any card that would be "Mixing" or "Pending" state
     - Or add a semi-transparent overlay rectangle to those cards
  </action>
  <verify>
    <automated>grep -c '"name": "Mill Production View' designs/mill-production.pen | grep -q '^4$' && echo "PASS: 4 frames exist"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"name": "Mill Production View' returns exactly 4
    - grep '"name": "Mill Production View - Default"' returns 1 match
    - grep '"name": "Mill Production View - Hover"' returns 1 match
    - grep '"name": "Mill Production View - Active"' returns 1 match
    - grep '"name": "Mill Production View - Filtered"' returns 1 match
    - grep '"x": 15495' exists (Hover frame position)
    - grep '"x": 17495' exists (Active frame position)
    - grep '"x": 19495' exists (Filtered frame position)
  </acceptance_criteria>
  <done>Four frames showing default, hover, active multi-select, and filtered result states</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User approves filter pill design</name>
  <what-built>
Status filter pills design in mill-production.pen with 4 interaction states:
1. Default - all pills inactive, showing count badges
2. Hover - stroke highlight on hovered pill
3. Active - 2 pills selected (Completed + Blocked), showing multi-select
4. Filtered - same as active, with non-matching cards dimmed
  </what-built>
  <how-to-verify>
1. Open designs/mill-production.pen in Penpot (https://design.penpot.app)
2. Navigate to "Mill Production View - Default" frame
3. Verify filter pills appear left-aligned above mill columns
4. Check pill styling: rounded corners, colored backgrounds, count badges
5. Check color mapping:
   - Completed = green (#c6f6d5 bg, #2f855a text)
   - Mixing = yellow (#fefcbf bg, #975a16 text)
   - Blocked = red (#fed7d7 bg, #c53030 text)
   - Pending = gray (#edf2f7 bg, #718096 text)
6. Navigate to "Hover" frame - verify stroke highlight
7. Navigate to "Active" frame - verify selected pills have solid backgrounds
8. Navigate to "Filtered" frame - verify non-matching cards are dimmed
9. Confirm design matches your vision before Phase 8 implementation
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Phase 7, or describe changes needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Design phase - no code execution or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | N/A | .pen file | accept | Design file only, no executable code. No security implications. |
</threat_model>

<verification>
Design verification:
1. mill-production.pen contains "Status Filters" frame
2. 4 filter pills present: Completed, Mixing, Blocked, Pending
3. Each pill has count badge
4. 4 interaction frames exist (Default, Hover, Active, Filtered)
5. Colors match globals.css design tokens
</verification>

<success_criteria>
- mill-production.pen updated with status filter pills
- 4 frames showing interaction states (default, hover, active, filtered)
- User has reviewed and approved design in Penpot
- Ready to proceed to Phase 7 (Data Infrastructure)
</success_criteria>

<output>
After completion, create `.planning/phases/06-design/06-01-SUMMARY.md`
</output>
