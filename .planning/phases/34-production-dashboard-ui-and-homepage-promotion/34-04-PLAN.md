---
phase: 34
plan: 04
type: tdd
wave: 2
depends_on: [34-01]
files_modified:
  - src/lib/production-derivations.ts
  - src/lib/__tests__/production-derivations.test.ts
  - src/components/ProductionCard.tsx
  - src/components/ProductionCard.test.tsx
  - src/components/MillColumn.tsx
  - src/components/MillColumn.test.tsx
autonomous: true
requirements: [PROD-02, PROD-07, PROD-08]
tags: [card, column, next-up, in-progress, tdd, pure-functions]
must_haves:
  truths:
    - "Pure helpers groupOrdersByState, computeColumnWeights, filterOrders are exported from src/lib/production-derivations.ts and are 100% covered by tests with no React imports (PROD-02, PROD-04, PROD-07)"
    - "ProductionCard renders order_number, customer, formatted weight (parseFloat → toLocaleString), product, delivery_time; left status border per UI-SPEC §4 (PROD-02)"
    - "ProductionCard with isNextUp prop renders the 'Next Up' teal pill above the order number (PROD-07)"
    - "ProductionCard with isInProgress prop renders the 8px animated pulse dot beside the order number (PROD-08)"
    - "ProductionCard is keyboard-accessible (role='button', tabIndex=0, Enter and Space activate onClick) — Card.tsx pattern reused"
    - "MillColumn renders mill-line header (24px bold), 'completed / total lbs' sub-label, and StateSection sub-blocks in the Completed → Mixing → Blocked → Pending visual order (UI-SPEC §3)"
    - "MillColumn skips StateSections that have zero orders (returns null) but renders the column header even when totally empty (PROD-02)"
    - "MillColumn passes isNextUp=true ONLY to the topmost Pending order and isInProgress=true to every Mixing order — verified by RTL queries"
    - "Weight rendering uses parseFloat(weightLbs).toLocaleString() — never raw String.toLocaleString() (Pitfall 6)"
  artifacts:
    - path: "src/lib/production-derivations.ts"
      provides: "Pure helpers: groupOrdersByState, computeColumnWeights, filterOrders, isOrderNextUp"
      exports: ["groupOrdersByState", "computeColumnWeights", "filterOrders", "isOrderNextUp"]
    - path: "src/lib/__tests__/production-derivations.test.ts"
      provides: "Pure-function unit tests (red/green TDD)"
    - path: "src/components/ProductionCard.tsx"
      provides: "Clickable DB-shape order card (replaces MillProductionUI's DemoOrder-typed version)"
    - path: "src/components/MillColumn.tsx"
      provides: "Per-line column composition; consumes ProductionCard"
  key_links:
    - from: "src/components/MillColumn.tsx"
      to: "src/components/ProductionCard.tsx"
      via: "JSX render"
      pattern: "ProductionCard"
    - from: "src/components/MillColumn.tsx"
      to: "src/lib/production-derivations.ts"
      via: "import groupOrdersByState, computeColumnWeights"
      pattern: "from ['\"]@/lib/production-derivations['\"]"
---

<objective>
Wave 2 part B: ship the per-card and per-column rendering primitives, plus extract every pure derivation (grouping, weight aggregation, search filtering, next-up determination) into `src/lib/production-derivations.ts` so the dashboard wrapper (plan 05) becomes a thin client component. TDD discipline applied to the pure helpers (RED → GREEN) and to the visible-behavior contracts on the card + column (RTL).

Purpose: the next-up indicator, in-progress badge, and weight formatting are exactly the kind of small pure logic the TDD heuristic catches. The card + column visual contract is locked in UI-SPEC §3 and §4 — RTL verifies structure without becoming a pixel test.

Output: four artifacts; two pure-function test bundles and two component test bundles; all green; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md
@src/components/MillProductionUI.tsx
@src/components/ui/Card.tsx
@src/db/schema/orders.ts

<interfaces>
Helper signatures (after GREEN — these are the contract):

```
// src/lib/production-derivations.ts
import type { ProductionOrder, ProductionState, MillLine } from '@/db/schema/orders';

export function groupOrdersByState(
  orders: ProductionOrder[]
): Record<ProductionState, ProductionOrder[]>;

export function computeColumnWeights(
  orders: ProductionOrder[]
): { completed: number; total: number };

export function filterOrders(
  orders: ProductionOrder[],
  status: ProductionState[],
  q: string
): ProductionOrder[]; // empty status[] = show all; q is trimmed+lowercased; substring match on customer + product

export function isOrderNextUp(
  order: ProductionOrder,
  pendingOrdersInColumn: ProductionOrder[]
): boolean; // true only if order === pendingOrdersInColumn[0]
```

From src/db/schema/orders.ts:
- `ProductionOrder` — Drizzle `$inferSelect`; weight_lbs is TS `string` (Pitfall 6).
- Field names per Drizzle TS conventions: confirm via reading the schema file (camelCase: `orderNumber`, `weightLbs`, `customerName` or `customer`, `product`, `deliveryTime`, `millLine`, `state`, `version`, `textureType` etc.) — use whatever the schema declares as the source of truth.

From src/components/ui/Card.tsx (keyboard-accessible card pattern):
- `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter/Space.

From MillProductionUI.tsx (visual prior art, do NOT import):
- STATE_ORDER for grouping (Pending, Mixing, Completed, Blocked).
- STATE_COLORS map (border, header, bg-22) keyed on ProductionState — replicate locally as local consts in ProductionCard.tsx / MillColumn.tsx per D-01 (no code sharing).
- formatWeight helper (already takes a number) — replicate locally.
</interfaces>
</context>

<feature>
  <name>Pure derivations + ProductionCard + MillColumn — DB-shape order rendering primitives</name>
  <files>src/lib/production-derivations.ts, src/lib/__tests__/production-derivations.test.ts, src/components/ProductionCard.tsx, src/components/ProductionCard.test.tsx, src/components/MillColumn.tsx, src/components/MillColumn.test.tsx</files>
  <behavior>
    See per-task <behavior> blocks below.
  </behavior>
  <implementation>
    See per-task <action> blocks below.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): Pure derivations — groupOrdersByState, computeColumnWeights, filterOrders, isOrderNextUp</name>
  <files>
    src/lib/production-derivations.ts,
    src/lib/__tests__/production-derivations.test.ts
  </files>
  <read_first>
    - src/db/schema/orders.ts (canonical field names — orderNumber, weightLbs, customer, product, etc.; verify exact spellings before writing tests)
    - src/components/MillProductionUI.tsx lines 211-237 (existing demo's filter + grouping idioms — visual prior art, not imported)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/ProductionDashboard.tsx (NEW)" — see filter pipeline + ordersByMill snippets
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 6: weight_lbs rendered without parseFloat" and §"Pitfall 11: ?status= array literal — empty string vs missing key"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-07
  </read_first>
  <behavior>
    Pure tests — no React render. Use fixture orders constructed inline (no DB).

    groupOrdersByState:
    - Test 1 (RED): Empty input returns `{ Pending: [], Mixing: [], Completed: [], Blocked: [] }` (all four keys present, all empty).
    - Test 2 (RED): Input with one of each state returns the input grouped by exactly one element per bucket.
    - Test 3 (RED): Order within each bucket follows input order (stable grouping).

    computeColumnWeights:
    - Test 4 (RED): Empty input → `{ completed: 0, total: 0 }`.
    - Test 5 (RED): Two orders, one Completed weight `"1500.50"` (string per Drizzle numeric), one Mixing weight `"2000.00"`. Returns `{ completed: 1500.5, total: 3500.5 }`. CRITICAL: function MUST parseFloat — never raw string concat or `String.toLocaleString` (Pitfall 6).
    - Test 6 (RED): Order with weightLbs = `"0"` is included; one with `null` (if schema permits) is treated as 0. (Confirm nullability from `src/db/schema/orders.ts` — if `weightLbs` is non-null, drop this case.)

    filterOrders:
    - Test 7 (RED): Empty status array `status=[]` and empty `q=''` returns the input unchanged (D-07 "empty filter = show all", Pitfall 11).
    - Test 8 (RED): `status=['Pending']` returns ONLY orders with `state === 'Pending'`.
    - Test 9 (RED): `status=['Pending','Mixing']` returns Pending + Mixing.
    - Test 10 (RED): `q='acme'` matches orders whose customer or product field contains 'acme' (case-insensitive). Trim + lowercase happens inside `filterOrders` (D-05).
    - Test 11 (RED): `q='  ACME  '` (leading/trailing whitespace + uppercase) matches the same as `'acme'`.
    - Test 12 (RED): Filter chain order — status applied first, then q substring on the survivors.

    isOrderNextUp:
    - Test 13 (RED): Order is the first in its column's Pending list → returns `true`.
    - Test 14 (RED): Order is NOT first in Pending list → `false`.
    - Test 15 (RED): Order is not Pending at all → `false`.
    - Test 16 (RED): Empty Pending list (zero matches) → no order is "next up" → returns `false`.

    All ~16 tests fail before implementation (file does not exist; imports fail OR exported stubs are noop). After implementation, all pass.
  </behavior>
  <action>
    Create `src/lib/__tests__/production-derivations.test.ts` FIRST with the 16 cases above. Use plain object literals shaped like `ProductionOrder` — read the schema file to align field names exactly. Where a field is needed but irrelevant (e.g., `id`, `version`, `clerkUserId`), use placeholder strings/numbers consistent with the schema type. Commit RED.

    Then create `src/lib/production-derivations.ts`:
    - No `'use client'` — pure module, usable from both client and RSC contexts (no DB, no browser API).
    - Imports: types only from `@/db/schema/orders`.
    - Define STATE_ORDER constant matching `src/lib/search-params.ts` ordering (Pending, Mixing, Completed, Blocked) — do NOT re-export from search-params.ts (search-params.ts is `'server-only'` adjacent and we want this file usable from client components).
    - Implementations:
      - `groupOrdersByState(orders)` — `STATE_ORDER.reduce((acc, state) => { acc[state] = orders.filter(o => o.state === state); return acc; }, { Pending: [], Mixing: [], Completed: [], Blocked: [] } as Record<ProductionState, ProductionOrder[]>);`
      - `computeColumnWeights(orders)` — `const total = orders.reduce((s, o) => s + parseFloat(o.weightLbs || '0'), 0); const completed = orders.filter(o => o.state === 'Completed').reduce((s, o) => s + parseFloat(o.weightLbs || '0'), 0); return { completed, total };`
      - `filterOrders(orders, status, q)` — first by status (empty = pass through), then by trimmed+lowercased substring match on customer + product.
      - `isOrderNextUp(order, pendingOrdersInColumn)` — `order.state === 'Pending' && pendingOrdersInColumn.length > 0 && pendingOrdersInColumn[0].id === order.id`.

    Commit GREEN. Run `npm test -- --testPathPattern=production-derivations.test.ts` and confirm all 16 cases pass.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=production-derivations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "parseFloat" src/lib/production-derivations.ts` returns ≥ 2 (computeColumnWeights uses it twice).
    - source-assertion: `grep -c "toLowerCase\\(\\)" src/lib/production-derivations.ts` returns ≥ 1 (filterOrders does case-insensitive match).
    - source-assertion: `grep -c "trim\\(\\)" src/lib/production-derivations.ts` returns ≥ 1 (filterOrders trims q).
    - source-assertion: `grep -c "'use client'" src/lib/production-derivations.ts` returns 0 (pure module — no React/client directive).
    - source-assertion: `grep -c "react\|next/" src/lib/production-derivations.ts` returns 0 (no React or Next imports).
    - source-assertion: `grep -E "export function (groupOrdersByState|computeColumnWeights|filterOrders|isOrderNextUp)" src/lib/production-derivations.ts | wc -l` returns 4.
    - behavior-assertion: all 16 pure tests pass with zero skips.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Pure derivations live in `src/lib/production-derivations.ts` with full TDD coverage; Pitfall 6 mitigated at the helper level so neither MillColumn nor ProductionCard can accidentally regress to raw `String.toLocaleString()`; ~16 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED→GREEN): ProductionCard — DB-shape clickable card with next-up + in-progress + a11y</name>
  <files>
    src/components/ProductionCard.tsx,
    src/components/ProductionCard.test.tsx
  </files>
  <read_first>
    - src/components/MillProductionUI.tsx lines 74-99 (visual prior art ProductionCard block — copy structure, change type)
    - src/components/ui/Card.tsx (role="button" + tabIndex + onKeyDown idiom)
    - src/db/schema/orders.ts (ProductionOrder field names — verify before mapping JSX)
    - src/app/globals.css (status border tokens — `--status-{state}-border`)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/ProductionCard.tsx (NEW)" — verbatim shape source
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §4 "Production Card"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 6: weight_lbs rendered without parseFloat"
  </read_first>
  <behavior>
    Tests use RTL with a fixture order (`orderNumber: 'ORD-001'`, `customer: 'Acme Feed'`, `weightLbs: '1500.50'`, `product: 'Layer Mash'`, `deliveryTime: 'Mar 5, 2026 10am'`, `state: 'Pending'`, `millLine: 'Premix'`).

    - Test 1 (RED): Renders `getByText('ORD-001')` and `getByText('Acme Feed')`.
    - Test 2 (RED): Renders weight as `'1,500.5 lbs • Layer Mash'` (or equivalent — assert the formatted string contains "1,500.5" and "Layer Mash"). Pitfall 6 — pass test only if `parseFloat(weightLbs).toLocaleString()` is used.
    - Test 3 (RED): Renders delivery_time text `'Delivery: Mar 5, 2026 10am'`.
    - Test 4 (RED): With `isNextUp={true}`, the rendered output contains a "Next Up" badge (`getByText('Next Up')`). With `isNextUp={false}`, no such badge.
    - Test 5 (RED): With `isInProgress={true}` and `state='Mixing'`, the rendered output contains an element with `aria-label="In progress"` AND class `animate-pulse`. With `isInProgress={false}`, no such element.
    - Test 6 (RED): The card root has `role="button"` and `tabIndex={0}`.
    - Test 7 (RED): Click on the card fires `onClick` callback. Use `userEvent.click()`; assert `mockOnClick` called once.
    - Test 8 (RED): Press Enter on focused card fires `onClick`. Press Space fires `onClick`. (Two separate userEvent.keyboard calls.)
    - Test 9 (RED): Left-border element has inline style `backgroundColor` corresponding to the state's `--status-{state}-border` token. Assert by querying the element and checking `style.backgroundColor` contains `var(--status-` OR a hex value if Tailwind has resolved it; the safest assertion is on the `style` attribute's literal substring `var(--status-pending-border)` or equivalent. Use the local STATE_COLORS map (copied from MillProductionUI per D-01) so the test can reach the same color.
  </behavior>
  <action>
    Create `src/components/ProductionCard.test.tsx` FIRST with the 9 cases (RED commit).

    Then create `src/components/ProductionCard.tsx` per PATTERNS.md §"src/components/ProductionCard.tsx (NEW)":
    - Top: `'use client';` (because of onClick + onKeyDown).
    - Imports: `type { ProductionOrder, ProductionState } from '@/db/schema/orders';`.
    - Define local `STATE_COLORS: Record<ProductionState, { border: string; header: string }>` — copy the four entries from `MillProductionUI.tsx` lines 18-65 verbatim (visual prior art, D-01 explicitly OK).
    - Define local `formatWeight(n: number): string` — copy from MillProductionUI.tsx lines 67-72 (takes a `number`, returns `'1,500.5'`-style locale string).
    - Component shape per PATTERNS.md excerpt: outer `<div role="button" tabIndex={0} onClick={onClick} onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onClick(); } }} className="shadow-card relative overflow-hidden rounded-r-xl bg-[var(--bg-card)] cursor-pointer transition-opacity hover:opacity-95 active:scale-[0.98]">` containing:
      - Inner left-border absolute div with `style={{ backgroundColor: STATE_COLORS[order.state].border }}`.
      - Inner content `<div className="py-2.5 pr-4 pl-5">` with the four content rows per UI-SPEC §4:
        - If `isNextUp`: `<span className="inline-block px-1.5 py-0.5 rounded text-[10px] font-bold text-white bg-[var(--primary)] mb-1">Next Up</span>`
        - Order number row: `<div className="flex items-center gap-1.5"><p>{order.orderNumber}</p>{isInProgress && <span aria-label="In progress" className="h-2 w-2 rounded-full bg-[var(--status-mixing-header)] animate-pulse" />}</div>`
        - Customer: `<p>{order.customer}</p>`
        - Weight + product: `<p>{formatWeight(parseFloat(order.weightLbs))} lbs &bull; {order.product}</p>`
        - Delivery: `<p>Delivery: {order.deliveryTime}</p>`
    - Props: `{ order: ProductionOrder; isNextUp?: boolean; isInProgress?: boolean; onClick: () => void }`.
    - Verify the EXACT field names from the schema (`order.orderNumber` vs `order.order_number`, `order.deliveryTime` vs `order.delivery_time` — Drizzle TS infers camelCase; double-check before writing).

    Run tests — confirm 9 pass. Commit GREEN.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="ProductionCard\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "role=\"button\"" src/components/ProductionCard.tsx` returns ≥ 1.
    - source-assertion: `grep -c "tabIndex={0}\|tabIndex=\"0\"" src/components/ProductionCard.tsx` returns ≥ 1.
    - source-assertion: `grep -c "onKeyDown" src/components/ProductionCard.tsx` returns ≥ 1.
    - source-assertion: `grep -c "parseFloat\\(order\\.weightLbs\\)\|parseFloat(order.weightLbs" src/components/ProductionCard.tsx` returns ≥ 1 (Pitfall 6).
    - source-assertion: `grep -c "Next Up" src/components/ProductionCard.tsx` returns ≥ 1.
    - source-assertion: `grep -c "aria-label=\"In progress\"" src/components/ProductionCard.tsx` returns ≥ 1.
    - source-assertion: `grep -c "animate-pulse" src/components/ProductionCard.tsx` returns ≥ 1.
    - behavior-assertion: all 9 RTL tests pass.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    ProductionCard renders DB-shape orders with Pitfall-6-safe weight, isNextUp pill, isInProgress dot, full keyboard a11y; 9 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (RED→GREEN): MillColumn — per-line composition consuming ProductionCard + pure derivations</name>
  <files>
    src/components/MillColumn.tsx,
    src/components/MillColumn.test.tsx
  </files>
  <read_first>
    - src/components/MillProductionUI.tsx lines 101-179 (StateSection + MillColumn visual prior art)
    - src/components/ProductionCard.tsx (created in Task 2 above)
    - src/lib/production-derivations.ts (created in Task 1 above — uses groupOrdersByState, computeColumnWeights, isOrderNextUp)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/MillColumn.tsx (NEW)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §3 "Mill Columns" + §"Copywriting Contract" (empty column "No orders")
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md PROD-07, PROD-08
  </read_first>
  <behavior>
    Fixture: build an array of ~6 orders across Premix line in mixed states (2 Pending, 1 Mixing, 2 Completed, 1 Blocked).

    - Test 1 (RED): `<MillColumn millLine="Premix" orders={fixture} onOrderClick={mockClick} />` renders text "Premix" (24px bold heading).
    - Test 2 (RED): The sub-label renders `<completedWeight> / <totalWeight> lbs`, with values matching `computeColumnWeights(fixture)`. Assert via partial string match on `getByText` (e.g., expect to find `"/ "` and `" lbs"` in the same text node).
    - Test 3 (RED): State sections render in the order Completed → Mixing → Blocked → Pending (UI-SPEC §3 ordering). Assert by querying all state-section heading nodes and confirming their text order.
    - Test 4 (RED): A state with zero orders does NOT render its section header (returns null per MillProductionUI line 111 pattern). For the fixture above, all four sections appear; for a fixture missing all Blocked + Mixing orders, only Completed + Pending sections appear.
    - Test 5 (RED): The TOPMOST Pending order receives `isNextUp={true}` (verified by querying `getAllByText('Next Up')` returning exactly 1 in the column, attached to the first Pending card's DOM ancestor).
    - Test 6 (RED): EVERY Mixing order receives `isInProgress={true}` (verified by `getAllByLabelText('In progress')` count equal to Mixing-order count in fixture).
    - Test 7 (RED): With `orders={[]}` (empty input), the column header still renders (`getByText('Premix')`), the weight sub-label shows `'0 / 0 lbs'` (or similar zero state), and an "No orders" empty-state text appears centered in the column. Copywriting per UI-SPEC.
    - Test 8 (RED): Clicking any ProductionCard fires `onOrderClick(orderId)` with the correct id. Verify via mocking `onOrderClick` and clicking a specific fixture card.
  </behavior>
  <action>
    Create `src/components/MillColumn.test.tsx` FIRST with the 8 cases (RED).

    Then create `src/components/MillColumn.tsx`:
    - `'use client';` at top.
    - Imports: `import { groupOrdersByState, computeColumnWeights, isOrderNextUp } from '@/lib/production-derivations';`; `import ProductionCard from '@/components/ProductionCard';`; type imports from `@/db/schema/orders`.
    - Define local visual ordering constant `COLUMN_STATE_ORDER: readonly ProductionState[] = ['Completed', 'Mixing', 'Blocked', 'Pending'] as const;` (UI-SPEC §3 visual order — different from parsing STATE_ORDER which is the literal-validation order).
    - Define local `formatWeight(n: number): string` — same shape as ProductionCard (or import it from a shared util; if you choose to extract `formatWeight` into `src/lib/production-derivations.ts` instead, do so under Task 1 — your call. Adding it post-hoc here is fine as long as both files stay consistent).
    - Props: `{ millLine: MillLine; orders: ProductionOrder[]; onOrderClick: (orderId: string) => void; }`.
    - Body:
      - `const grouped = groupOrdersByState(orders);`
      - `const { completed, total } = computeColumnWeights(orders);`
      - Render outer `<div className="flex flex-1 flex-col gap-5">` containing:
        - Header: `<div><h2 className="text-primary text-2xl font-bold">{millLine}</h2><p className="text-muted mt-1 text-base font-semibold">{formatWeight(completed)} / {formatWeight(total)} lbs</p></div>`.
        - If `orders.length === 0`: render `<p className="text-center text-sm text-[var(--text-secondary)]">No orders</p>` and return.
        - Else: `<div className="flex flex-col gap-6">{COLUMN_STATE_ORDER.map((state) => { const list = grouped[state]; if (list.length === 0) return null; return <StateSection key={state} state={state} orders={list} pendingOrdersInColumn={grouped.Pending} onOrderClick={onOrderClick} />; })}</div>`.
    - Define inline (or extract) `StateSection({ state, orders, pendingOrdersInColumn, onOrderClick })` that renders a state heading + `flex flex-col gap-3` of `<ProductionCard key={order.id} order={order} isNextUp={isOrderNextUp(order, pendingOrdersInColumn)} isInProgress={order.state === 'Mixing'} onClick={() => onOrderClick(order.id)} />`.

    Run tests — confirm 8 pass. Commit GREEN.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="MillColumn\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "groupOrdersByState\|computeColumnWeights\|isOrderNextUp" src/components/MillColumn.tsx` returns ≥ 3 (all three helpers imported and used).
    - source-assertion: `grep -c "ProductionCard" src/components/MillColumn.tsx` returns ≥ 2 (import + JSX usage).
    - source-assertion: `grep -E "'Completed'.*'Mixing'.*'Blocked'.*'Pending'" src/components/MillColumn.tsx | wc -l` returns ≥ 1 (visual order constant present).
    - source-assertion: `grep -c "No orders" src/components/MillColumn.tsx` returns ≥ 1 (empty state).
    - source-assertion: `grep -c "isNextUp" src/components/MillColumn.tsx` returns ≥ 1.
    - source-assertion: `grep -c "isInProgress" src/components/MillColumn.tsx` returns ≥ 1.
    - source-assertion: `grep -c "state === 'Mixing'" src/components/MillColumn.tsx` returns ≥ 1 (deriving isInProgress).
    - behavior-assertion: all 8 RTL tests pass.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    MillColumn renders header + weight sub-label + ordered state sections + empty state; next-up + in-progress flags wire to ProductionCard correctly; 8 tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component → user click | Card click fires `onClick` callback supplied by parent (plan 05 ProductionDashboard); the parent translates to nuqs `setQuery({ order: id })`. No direct DB or auth surface here. |
| Display weight value | Drizzle `numeric` column returns TS `string`; consumers MUST parseFloat (Pitfall 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | T — Tampering | weight_lbs string-vs-number rendering (Pitfall 6) | mitigate | `computeColumnWeights` and `ProductionCard` BOTH use `parseFloat(weightLbs || '0')`. Acceptance criteria grep enforces this. Pure helper has dedicated unit tests for the string→number path (Test 5). |
| T-34-04-02 | I — Information Disclosure | ProductionCard renders customer + product + delivery_time | accept | Same trust posture as the dashboard page (auth-only via `auth()` in plan 07). All visible operator-internal fields. No PII beyond customer name, which is intentionally visible to mill operators. |
| T-34-04-03 | E — Elevation of Privilege | Card click only emits an orderId — no role check | accept | Click only sets URL state (`?order=<id>`). Subsequent drawer fetch (plan 06 / 07) re-enforces auth gate via page RSC + `getOrderById` parameterised query. |
| T-34-04-04 | T — Tampering | `isOrderNextUp` derives from in-memory list | accept | Pure function over already-fetched orders. Returning `true` for the wrong order is a UX bug, not a security issue. Test 13-16 enforce correctness. |
| T-34-04-05 | D — Denial of Service | groupOrdersByState scans the orders array four times (one filter per state) | accept | v2.0 row volume is ≤33 seeded + low daily import (CONTEXT.md). Quadratic-scaling is irrelevant at this size. Phase 35 may re-evaluate if KPI computations scale further. |
| T-34-04-06 | R — Repudiation | Cards do not write or audit | accept | Pure rendering layer; auditing happens at the action layer (Phase 33). |
</threat_model>

<verification>
- `npm test -- --testPathPattern="(production-derivations|ProductionCard|MillColumn)\.test\.tsx?$"` — all ~33 tests pass.
- `npx tsc --noEmit` — clean.
- `grep -rn "toLocaleString" src/components/ProductionCard.tsx src/components/MillColumn.tsx src/lib/production-derivations.ts` shows toLocaleString called only after parseFloat (manual scan; OK if there are 0 raw `weightLbs.toLocaleString` strings).
- `grep -c "from '@/lib/production-derivations'" src/components/MillColumn.tsx` returns ≥ 1.
</verification>

<success_criteria>
- PROD-02 (three-column board structure), PROD-07 (next-up indicator), PROD-08 (in-progress badge) all implemented at the card + column level — composition into the dashboard happens in plan 05.
- D-07 (client-side substring filter) implemented as a pure function inside `production-derivations.ts`, ready for the dashboard to wire to nuqs `q` state.
- Pitfall 6 (weight_lbs string vs number) eliminated at every render site via the helper + parseFloat discipline.
</success_criteria>

<output>
After completion, create `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-04-SUMMARY.md` summarizing:
- Four exported pure helpers and their signatures
- ProductionCard + MillColumn composition diagram (text)
- Test counts (pure: 16; card: 9; column: 8 — total ~33)
- Note for plan 05: the dashboard wrapper will `import { filterOrders, groupOrdersByState } from '@/lib/production-derivations'` and pass the per-mill-line `orders` arrays to `<MillColumn />`. The dashboard wrapper owns the nuqs hooks; MillColumn is presentational.
</output>
