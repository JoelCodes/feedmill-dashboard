---
phase: 34
plan: 07
type: execute
wave: 4
depends_on: [34-01, 34-02, 34-03, 34-04, 34-05, 34-06]
files_modified:
  - src/app/page.tsx
  - src/app/page.test.tsx
  - src/app/import/page.tsx
  - src/app/import/__tests__/page.test.tsx
  - src/components/ImportFlow.tsx
  - src/components/ImportFlow.test.tsx
  - src/components/ImportHistoryTable.tsx
  - src/components/ImportHistoryTable.test.tsx
  - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md
autonomous: false
requirements: [PROD-01, PROD-02, PROD-05, PROD-10]
tags: [page-rsc, import-flow, integration, uat, inherited-gap-02]
user_setup:
  - service: clerk
    why: "UAT verification of mill_operator role flow"
    env_vars:
      - name: CLERK_SECRET_KEY
        source: ".env.local — already present per Phase 31"
    dashboard_config:
      - task: "Ensure `mill_operator` test user exists with roles=['mill_operator'] in Clerk Dashboard publicMetadata"
        location: "Clerk Dashboard → Users → demo+mill_operator user → publicMetadata"
must_haves:
  truths:
    - "GET / (authenticated mill_operator) renders the ProductionDashboard composed with live DB orders, drawerOrder/drawerEvents when ?order= is present, and canEdit=true (PROD-01, PROD-02, D-09, D-25)"
    - "GET / (unauthenticated) redirects to /sign-in (AUTH inherited from middleware + page-level auth() defense-in-depth)"
    - "src/app/page.tsx is an async RSC with `export const dynamic = 'force-dynamic'` (PROD-01)"
    - "src/app/page.tsx parses searchParams via searchParamsCache.parse(searchParams) and fetches getOrderById + getOrderEvents in parallel with getProductionOrders when ?order= is set (D-09)"
    - "GET /import (authenticated) renders ImportFlow + ImportHistoryTable wrapped in DashboardLayout; getImportBatches({ limit: 10 }) supplies the history (D-15, D-16)"
    - "GET /import (unauthenticated) redirects to /sign-in"
    - "ImportFlow has three phases: entry (drop zone + file picker), preview (table + per-row Skip/Overwrite radios), committed (summary). Drag-drop AND file picker both supported (D-15, UI-SPEC Claude's Discretion)"
    - "ImportFlow client-side file-size guard rejects >2MB files BEFORE submit with the locked UI-SPEC message 'File exceeds 2 MB limit. Please upload a smaller file.' (D-17)"
    - "Per-row Skip/Overwrite radio default is Skip; no batch 'set all' toggle (D-18)"
    - "ImportHistoryTable shows 10 most recent batches with columns File Name, Rows, Imported By, Date in 'MMM D, YYYY h:mm a' format; empty state 'No imports yet' (D-16, UI-SPEC §8)"
    - "Inherited UAT entry 'Inherited: GAP-02 revalidateTag end-to-end (from Phase 33)' is recorded in 34-HUMAN-UAT.md with the two-tab observation protocol (D-26)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Production homepage RSC composing ProductionDashboard with live data"
    - path: "src/app/import/page.tsx"
      provides: "Bulk import RSC composing ImportFlow + ImportHistoryTable"
    - path: "src/components/ImportFlow.tsx"
      provides: "Three-phase client wrapper (entry → preview → committed) for XLSX bulk import"
    - path: "src/components/ImportHistoryTable.tsx"
      provides: "Presentation of ImportBatch[] from getImportBatches"
    - path: ".planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md"
      provides: "Phase 34 manual UAT contract + Inherited GAP-02 entry"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/search-params.ts"
      via: "await searchParamsCache.parse(searchParams)"
      pattern: "searchParamsCache\\.parse"
    - from: "src/app/page.tsx"
      to: "src/db/queries/orders.ts + src/db/queries/events.ts"
      via: "Promise.all([getProductionOrders(), getOrderById(order), getOrderEvents(order)])"
      pattern: "Promise\\.all"
    - from: "src/app/page.tsx"
      to: "src/components/ProductionDashboard.tsx"
      via: "JSX with full prop set"
      pattern: "<ProductionDashboard"
    - from: "src/app/import/page.tsx"
      to: "src/db/queries/imports.ts"
      via: "await getImportBatches({ limit: 10 })"
      pattern: "getImportBatches"
    - from: "src/components/ImportFlow.tsx"
      to: "src/actions/import.ts"
      via: "previewImportAction + commitImportAction"
      pattern: "previewImportAction\|commitImportAction"
---

<objective>
Wave 4: assemble the page-RSC layer and finalize the bulk-import surface. This plan rewrites `src/app/page.tsx` to fetch orders + (conditionally) drawer data and render `<ProductionDashboard>`, ships the new `/import` route as an async RSC, builds the three-phase `ImportFlow` client wrapper, builds the `ImportHistoryTable`, and writes the Phase 34 manual UAT contract including the explicit Inherited GAP-02 step (D-26 / RESEARCH.md Pitfall 8).

Purpose: every primitive shipped in plans 01-06 lands at a real URL endpoint here. PROD-01 (homepage promotion), the `/import` route (D-15), and the inherited GAP-02 closure are the gating deliverables that mark v2.0 Mill Production MVP as user-visible.

Output: two page RSCs, two new client components, two integration test bundles, one human UAT contract. This plan is `autonomous: false` because the final task is a `checkpoint:human-verify` running the manual UAT (including Inherited GAP-02 two-tab observation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md
@.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md
@src/app/page.tsx
@src/app/page.test.tsx
@src/app/demo/mill-production/page.tsx
@src/db/queries/orders.ts
@src/db/queries/events.ts
@src/db/queries/imports.ts
@src/actions/import.ts
@src/lib/import-constants.ts
@src/lib/auth.ts
@src/lib/search-params.ts
@src/components/DashboardLayout.tsx
@src/components/ProductionDashboard.tsx

<interfaces>
Page RSC signatures (Next.js 16 — async, force-dynamic):

```
// src/app/page.tsx
export const dynamic = 'force-dynamic';

export default async function HomePage({
  searchParams,
}: { searchParams: Promise<SearchParams> }): Promise<JSX.Element>;
```

`SearchParams` from 'nuqs/server' (re-exported by nuqs).

Server action signatures (from src/actions/import.ts — Phase 33 contract):
- `previewImportAction(formData: FormData): Promise<PreviewResult>`
- `commitImportAction(formData: FormData, decisions: ImportDecisions): Promise<CommitResult>`
- `MAX_IMPORT_BYTES` constant from `src/lib/import-constants.ts` (~2,097,152 — verify).

`getImportBatches({ limit })` from `src/db/queries/imports.ts` (plan 03) — returns `ImportBatch[]`.

ImportBatch shape (from src/db/schema/imports.ts):
```
{ id, fileName, rowCount, importedBy, importedAt: Date }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/app/page.tsx — async RSC composing ProductionDashboard with live data</name>
  <files>
    src/app/page.tsx,
    src/app/page.test.tsx
  </files>
  <read_first>
    - src/app/page.tsx (current transitional stub from plan 01)
    - src/app/demo/mill-production/page.tsx (RSC + service-call analog)
    - src/lib/search-params.ts (searchParamsCache — plan 01)
    - src/db/queries/orders.ts (getProductionOrders + getOrderById)
    - src/db/queries/events.ts (getOrderEvents)
    - src/lib/auth.ts (checkRole signature)
    - src/components/ProductionDashboard.tsx (props contract — orders, canEdit, drawerOrder, drawerEvents)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/app/page.tsx (REWRITE — async RSC, force-dynamic)" — verbatim shape
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 2: searchParams not awaited in RSC"
  </read_first>
  <behavior>
    RSC unit tests (existing pattern: mock the query layer + Clerk auth, render the page component, assert behavior).

    - Test 1: With unauthenticated session (`auth()` mock returns `{ userId: null }`), the page calls `redirect('/sign-in')` exactly once. (Use the existing redirect-spy pattern from plan-01 transitional test.)
    - Test 2: With authenticated session (`auth()` returns `{ userId: 'user_1' }`) and `checkRole('mill_operator')` returning true, no redirect; the page renders `<ProductionDashboard>` with `orders` (from `getProductionOrders`) and `canEdit=true`.
    - Test 3: With authenticated session but `checkRole('mill_operator')` returning false, the page still renders (no redirect — D-02), and `<ProductionDashboard>` receives `canEdit=false`.
    - Test 4: With `searchParams = Promise.resolve({})`, `getOrderById` and `getOrderEvents` are NOT called (since `?order=` is absent → empty default).
    - Test 5: With `searchParams = Promise.resolve({ order: 'ord_1' })`, `getOrderById('ord_1')` and `getOrderEvents('ord_1')` are called exactly once each, and their results are passed to `<ProductionDashboard>` as `drawerOrder` and `drawerEvents`.
    - Test 6: When `getOrderById('ord_stale')` returns `null` (stale id), the page does NOT throw; `<ProductionDashboard>` receives `drawerOrder={null}`, `drawerEvents={[]}` (Pitfall 7 — drawer null-state UI).
    - Test 7 (Pitfall 2): The page awaits `searchParamsCache.parse(searchParams)` (where `searchParams` is `Promise<SearchParams>`). Confirm by reading the source: `grep -c "await searchParamsCache\\.parse" src/app/page.tsx` returns exactly 1.
    - Test 8: `export const dynamic = 'force-dynamic'` is present.
  </behavior>
  <action>
    Rewrite `src/app/page.tsx` per 34-PATTERNS.md §"src/app/page.tsx (REWRITE — async RSC, force-dynamic)" and 34-RESEARCH.md §Pattern 1:

    - File start (NO blank line before): `import { auth } from '@clerk/nextjs/server';`
    - Continue imports: `redirect` from 'next/navigation'; `checkRole` from '@/lib/auth'; `searchParamsCache` from '@/lib/search-params'; `getProductionOrders, getOrderById` from '@/db/queries/orders'; `getOrderEvents` from '@/db/queries/events'; default `DashboardLayout` from '@/components/DashboardLayout'; default `ProductionDashboard` from '@/components/ProductionDashboard'; `type { SearchParams } from 'nuqs/server'`.
    - `export const dynamic = 'force-dynamic';`
    - Default async function with signature `async function HomePage({ searchParams }: { searchParams: Promise<SearchParams> })`:
      - `const { userId } = await auth(); if (!userId) redirect('/sign-in');`
      - `const canEdit = await checkRole('mill_operator');`
      - `const { order } = await searchParamsCache.parse(searchParams);`
      - `const [orders, drawerOrder, drawerEvents] = await Promise.all([getProductionOrders(), order ? getOrderById(order) : Promise.resolve(null), order ? getOrderEvents(order) : Promise.resolve([])]);`
      - Return `<DashboardLayout><ProductionDashboard orders={orders} canEdit={canEdit} drawerOrder={drawerOrder} drawerEvents={drawerEvents} /></DashboardLayout>`.
    - Place a JSDoc comment block before the default export citing PROD-01, D-02, D-09, D-25, and Pitfall 7 (null drawer order).
    - DELETE any leftover "transitional placeholder" text and the "Dashboard placeholder — wired in plan 07" string from plan 01.

    Rewrite `src/app/page.test.tsx`:
    - Replace the plan-01 placeholder assertions with the 8 cases listed in <behavior>.
    - Mock `@clerk/nextjs/server` `auth()`, `next/navigation` `redirect`, `@/lib/auth` `checkRole`, `@/db/queries/orders` (getProductionOrders + getOrderById), `@/db/queries/events` (getOrderEvents), `@/components/ProductionDashboard` (capture the props passed in).
    - Use the existing RSC test harness — call the default async function with explicit `searchParams: Promise.resolve({...})` and assert on the result via `prettyDOM` or by snapshot.

    Run `npm test -- --testPathPattern="src/app/__tests__/page\.test|src/app/page\.test"` and confirm all 8 tests pass.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="app/page\.test\.(ts|tsx)$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "export const dynamic = 'force-dynamic'" src/app/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "searchParamsCache\\.parse" src/app/page.tsx` returns exactly 1; preceded by `await`.
    - source-assertion: `grep -c "Promise\\.all" src/app/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "getProductionOrders\\(\\)" src/app/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "getOrderById\\(order\\)" src/app/page.tsx` returns ≥ 1.
    - source-assertion: `grep -c "getOrderEvents\\(order\\)" src/app/page.tsx` returns ≥ 1.
    - source-assertion: `grep -c "<ProductionDashboard" src/app/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "redirect\\('/sign-in'\\)" src/app/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "Dashboard placeholder" src/app/page.tsx` returns 0 (transitional text removed).
    - source-assertion: `grep -c "MillReadOnlyStub" src/app/page.tsx` returns 0 (already deleted in plan 01).
    - behavior-assertion: all 8 RSC tests pass.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `/` is a fully live, DB-backed, async RSC composing `<ProductionDashboard>` with live orders + optional drawer data; 8 RSC tests green; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: ImportHistoryTable + ImportFlow + /import page RSC</name>
  <files>
    src/components/ImportHistoryTable.tsx,
    src/components/ImportHistoryTable.test.tsx,
    src/components/ImportFlow.tsx,
    src/components/ImportFlow.test.tsx,
    src/app/import/page.tsx,
    src/app/import/__tests__/page.test.tsx
  </files>
  <read_first>
    - src/actions/import.ts (previewImportAction + commitImportAction signatures + return shapes)
    - src/lib/import-constants.ts (MAX_IMPORT_BYTES)
    - src/db/queries/imports.ts (getImportBatches from plan 03)
    - src/db/schema/imports.ts (ImportBatch shape: fileName, rowCount, importedBy, importedAt)
    - src/components/OrdersTable.tsx / OrdersTableContent.tsx (existing tabular display analog)
    - src/components/ui/Button.tsx + src/components/ui/Textarea.tsx + src/components/ui/Input.tsx (reuse for preview controls)
    - src/components/DashboardLayout.tsx (layout wrapper)
    - src/lib/auth.ts (checkRole)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/ImportFlow.tsx (NEW)" and §"src/components/ImportHistoryTable.tsx (NEW)" and §"src/app/import/page.tsx (NEW)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §8 "Import Page (`/import`)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-15, D-16, D-17, D-18
  </read_first>
  <behavior>
    ImportHistoryTable:
    - Test 1: With `batches=[]`, renders the empty state "No imports yet" (UI-SPEC copywriting); table body has zero data rows.
    - Test 2: With three batches, renders the heading "Recent Imports" + four column headers (File Name, Rows, Imported By, Date) + three data rows.
    - Test 3: Date rendering uses `Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit', hour12: true })` — assert by formatting a known date (e.g., `new Date('2026-05-14T14:34:00Z')`) and querying for the rendered text.
    - Test 4: Renders exactly 10 rows when given a batches array of length > 10 — page RSC will pass `limit=10` so this is a safety check at the component level (just render whatever is passed; don't slice — the slicing is the query's job).

    ImportFlow:
    - Test 5 (entry phase): Renders the drop zone with primary text "Drop your Excel file here" and secondary "or click to browse — .xlsx only, max 2 MB". Renders an `<input type="file" accept=".xlsx">` (file picker).
    - Test 6 (file-size guard — D-17): Simulate `onChange` with a `File` whose size is `MAX_IMPORT_BYTES + 1`; expect: error message "File exceeds 2 MB limit. Please upload a smaller file." appears AND `previewImportAction` is NOT called.
    - Test 7 (drag-drop): Fire a `drop` event on the drop zone with a valid `File`; expect `previewImportAction` is called with a FormData containing the file.
    - Test 8 (preview phase): With `previewImportAction` mock returning a successful preview (rows + summary + duplicates), the UI transitions to the preview phase: summary bar visible, preview table with the rows, per-row Skip/Overwrite radio for duplicates only (default Skip), Commit + Cancel buttons.
    - Test 9 (per-row Skip default — D-18): For a preview row marked as duplicate, the Skip radio is checked by default; the Overwrite radio is unchecked. Toggling Overwrite updates local state.
    - Test 10 (commit flow): Click Commit → `commitImportAction(formData, decisions)` is called with decisions reflecting the radio state. On success → flow transitions to a "committed" summary; the UI shows a confirmation.
    - Test 11 (cancel from preview): Click Cancel → flow returns to entry phase; preview state cleared.
    - Test 12 (commit failure): `commitImportAction` returns `{ ok: false, code: 'validation', message: '...' }` → render the error inline; do NOT transition to committed phase.

    /import page RSC:
    - Test 13: Unauthenticated → `redirect('/sign-in')`.
    - Test 14: Authenticated → renders `<DashboardLayout>` wrapping `<ImportFlow batches={...} canEdit={...} />`. `getImportBatches({ limit: 10 })` is called exactly once.
    - Test 15: `canEdit` from `checkRole('mill_operator')` is threaded into `<ImportFlow>` as a prop (so non-operators see read-only history without the drop zone — see plan-recommended D-25 carry).
  </behavior>
  <action>
    Create `src/components/ImportHistoryTable.tsx` per PATTERNS.md §"src/components/ImportHistoryTable.tsx (NEW)" and UI-SPEC §8 "Import history table":
    - `'use client'` is NOT required (purely presentational); but if any interactive element appears, mark it. Recommend keeping it as a server component (no event handlers).
    - Props: `{ batches: ImportBatch[] }`.
    - JSX: heading `<h3 className="text-base font-bold text-[var(--text-primary)]">Recent Imports</h3>`; if `batches.length === 0`, render `<p className="text-center text-sm text-[var(--text-secondary)]">No imports yet</p>`. Else a `<table>` with header row + body row per batch.
    - Date format helper: `function formatBatchDate(d: Date): string { return new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit', hour12: true }).format(d); }`.
    - JSDoc citing D-16 + UI-SPEC §8.

    Create `src/components/ImportFlow.tsx` (`'use client'`):
    - Props: `{ batches: ImportBatch[]; canEdit: boolean; }`.
    - State: `const [phase, setPhase] = useState<'entry' | 'preview' | 'committed'>('entry');`; `const [error, setError] = useState<string | null>(null);`; `const [preview, setPreview] = useState<PreviewResult | null>(null);`; `const [rowDecisions, setRowDecisions] = useState<Record<number, 'skip' | 'overwrite'>>({});`; `const [dragOver, setDragOver] = useState(false);`; `const [isPending, setIsPending] = useState(false);`.
    - `MAX_IMPORT_BYTES` imported from `@/lib/import-constants`.
    - File-select handler: validates size, builds FormData, calls `previewImportAction(formData)`, sets phase based on result. Encapsulate as `async function onFileSelect(file: File)`.
    - Drag handlers per PATTERNS.md §"src/components/ImportFlow.tsx" lines around drag-drop pattern.
    - Entry phase: dashed border box with `<input type="file" accept=".xlsx">`; on `onChange`: `onFileSelect(e.target.files![0])`. Drag handlers on the same wrapper. Error message inline below.
    - Preview phase: summary bar (`<preview.summary.rowCount> rows · <preview.summary.totalWeight> lbs total · <preview.summary.duplicateCount> duplicates`), preview table (Row #, Document Number, Customer, Product, Weight, Status), per-row Skip/Overwrite radio for `row.status === 'duplicate'`. Commit + Cancel buttons.
    - Commit handler builds the `decisions` object from `rowDecisions`, calls `commitImportAction(formData, decisions)`, sets `phase = 'committed'` on success.
    - Committed phase: simple confirmation card with a "Back to import" button that resets state to entry.
    - Conditional behavior on `canEdit === false`: entry phase shows the drop zone disabled (and a notice "Read-only mode — sign in as mill_operator to import"); preview + committed phases never reached.
    - Mount `<ImportHistoryTable batches={batches} />` below the active phase, separated by `<hr className="my-6 border-[var(--divider)]">`.

    Create `src/app/import/page.tsx` per PATTERNS.md §"src/app/import/page.tsx (NEW)":
    - `export const dynamic = 'force-dynamic';`
    - Imports: `auth`, `redirect`, `checkRole`, `getImportBatches`, `DashboardLayout`, `ImportFlow`.
    - Body: `const { userId } = await auth(); if (!userId) redirect('/sign-in'); const canEdit = await checkRole('mill_operator'); const batches = await getImportBatches({ limit: 10 }); return <DashboardLayout><ImportFlow batches={batches} canEdit={canEdit} /></DashboardLayout>;`
    - JSDoc citing D-15, D-16.

    Create `src/app/import/__tests__/page.test.tsx` with tests 13-15. Use the same RSC test harness as `src/app/page.test.tsx` (mock the query + auth modules; call the page function).

    Add a `src/app/import/__tests__/` directory if missing.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(ImportHistoryTable|ImportFlow|app/import/.*page)\.test\.(ts|tsx)$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "No imports yet" src/components/ImportHistoryTable.tsx` returns ≥ 1.
    - source-assertion: `grep -c "Recent Imports" src/components/ImportHistoryTable.tsx` returns ≥ 1.
    - source-assertion: `grep -c "Intl\\.DateTimeFormat" src/components/ImportHistoryTable.tsx` returns ≥ 1.
    - source-assertion: `grep -c "MAX_IMPORT_BYTES" src/components/ImportFlow.tsx` returns ≥ 1.
    - source-assertion: `grep -c "File exceeds 2 MB limit" src/components/ImportFlow.tsx` returns ≥ 1 (locked copywriting).
    - source-assertion: `grep -c "Drop your Excel file here" src/components/ImportFlow.tsx` returns ≥ 1.
    - source-assertion: `grep -c "previewImportAction\\b" src/components/ImportFlow.tsx` returns ≥ 1.
    - source-assertion: `grep -c "commitImportAction\\b" src/components/ImportFlow.tsx` returns ≥ 1.
    - source-assertion: `grep -c "accept=\".xlsx\"" src/components/ImportFlow.tsx` returns ≥ 1.
    - source-assertion: `grep -c "'skip'" src/components/ImportFlow.tsx` returns ≥ 1 (D-18 default).
    - source-assertion: `grep -c "force-dynamic" src/app/import/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "getImportBatches\\(\\s*\\{\\s*limit:\\s*10" src/app/import/page.tsx` returns ≥ 1.
    - source-assertion: `grep -c "redirect\\('/sign-in'\\)" src/app/import/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "<ImportFlow" src/app/import/page.tsx` returns exactly 1.
    - source-assertion: `grep -c "<DashboardLayout" src/app/import/page.tsx` returns ≥ 1.
    - behavior-assertion: all 15 tests pass across the four test bundles.
    - CLI output: `npx tsc --noEmit` exits 0.
    - CLI output: `next build` (or `npm run build`) succeeds without Edge-bundle errors.
  </acceptance_criteria>
  <done>
    `/import` route lives + ImportFlow handles entry/preview/committed phases with D-17 size guard, D-18 Skip default, drag-drop + picker; ImportHistoryTable renders 10 most recent batches with locked date format; 15 tests green; build green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT incl. Inherited GAP-02 — two-tab revalidateTag observation</name>
  <files>.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md (Inherited GAP-02 closure protocol)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-26
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 8: Forgetting the revalidateTag('import-batches') patch"
    - .planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md (existing Test #2 marked deferred_to_phase_34)
  </read_first>
  <what-built>
    Plans 01-06 + Tasks 1-2 of this plan have shipped the full Phase 34 surface:
    - `/` renders ProductionDashboard with live DB orders, URL-synced filters/search, polling, blocked band, drawer with transitions
    - `/import` renders ImportFlow with drop-zone + preview + commit + history
    - Sidebar shows Dashboard + Import
    - D-21 cross-phase patch invalidates `import-batches` tag
    - All Jest tests green; tsc clean; build green

    This checkpoint runs the manual UAT contract before phase completion. The UAT covers:
    1. Auth gate on `/` (unauthenticated → /sign-in)
    2. Three-column live dashboard render (PROD-01, PROD-02)
    3. URL-synced filter + search survives reload (PROD-03, PROD-04)
    4. Drawer opens on card click, shows fields + timeline (PROD-05)
    5. Blocked alert band + next-up indicator + in-progress badge (PROD-06, PROD-07, PROD-08)
    6. 30s polling + last-updated chip + manual refresh (PROD-09, PROD-11)
    7. Loading skeletons per column (PROD-10)
    8. Sidebar production nav (D-24)
    9. Bulk import end-to-end (entry → preview → commit → history)
    10. Transition flow (Pending → Mixing → Completed; Block + Resume)
    11. **Inherited GAP-02:** Two-tab revalidateTag observation (D-26)
  </what-built>
  <how-to-verify>
    Create `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md` with this exact frontmatter (replace the placeholder result entries during the live UAT):

    ```
    ---
    phase: 34-production-dashboard-ui-and-homepage-promotion
    type: human-uat
    created: <current date>
    status: pending
    inherited_from: 33-server-actions-queries-and-bulk-import (GAP-02)
    ---
    ```

    Body sections:
    - Preconditions (operator running `npm run dev`; seed data present; mill_operator user signed in)
    - Tests numbered T1..T11 in the order listed under <what-built>, each with: title, steps (numbered), pass criteria, fail criteria, observed result placeholder.
    - For T11 (Inherited GAP-02): copy the entire "Inherited Test Step (MUST appear in Phase 34's UAT)" body verbatim from `.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md`, retitled `Inherited: GAP-02 revalidateTag end-to-end (from Phase 33)`.

    Then the operator runs the UAT manually:
    1. `npm run dev` and visit `http://localhost:3000/` while signed OUT — confirm redirect to `/sign-in`.
    2. Sign in as the mill_operator test user — confirm dashboard renders three columns with live data.
    3. Click a Pending filter pill → URL becomes `?status=Pending`; reload page; filter survives.
    4. Type a customer name fragment in the search box; URL gets `?q=...`; matching cards remain.
    5. Click an order card → drawer opens; URL gets `?order=...`; close drawer (ESC, X button, backdrop click) — each clears the URL.
    6. Confirm blocked alert band appears only when blocked orders exist; click an entry → drawer opens for that order.
    7. Watch the LastUpdatedChip count up for ~35 seconds; observe a `router.refresh` happen at ~30s (devtools Network tab shows RSC payload reload).
    8. Click the manual refresh icon — chip resets to "Updated 0s ago" + spinner during refresh.
    9. Open the BlockReasonModal: try to confirm empty (button disabled) → type a reason → confirm; drawer timeline shows the new Blocked event.
    10. Visit `/import` — confirm Sidebar's "Import" item is active. Try drag-dropping a file > 2MB → error appears.
    11. Upload a valid Book1.xlsx fixture (use the harness file from Phase 33 plan 07) → preview shows; toggle one Skip→Overwrite; click Commit → ImportHistoryTable refreshes (D-21 verification).
    12. **Inherited GAP-02:** open two tabs of `/`. In Tab B, transition a Pending order to Mixing. WITHOUT refreshing, watch Tab A — within ≤30s the card moves to the Mixing column (polling-driven). Pass criterion per 34-INHERITED-UAT.md.

    Record each test as `pass` / `fail` with operator initials + date in the placeholder rows.

    After UAT completion (assuming pass):
    - Amend `.planning/phases/33-server-actions-queries-and-bulk-import/33-HUMAN-UAT.md` Test #2: change `result: deferred_to_phase_34` to `result: closed_in_phase_34 (pass, <date>, <Phase 34 verification commit>)`. Flip frontmatter `status: gaps_flagged` → `gaps_closed` (assuming GAP-01 and GAP-03 are already closed by plans 33-08 and 33-07).
    - Commit `34-HUMAN-UAT.md` and the `33-HUMAN-UAT.md` amendment.

    If any test fails: do NOT close the checkpoint. Capture the failure mode, file a follow-up gap in `34-VERIFICATION.md`, and surface the failure as a Phase 34 gap-closure candidate.
  </how-to-verify>
  <action>
    Manual operator step (no autonomous Claude work). Follow the protocol described in <how-to-verify>. Create 34-HUMAN-UAT.md, run all 12 tests against `npm run dev`, record results inline. On full pass: amend 33-HUMAN-UAT.md Test #2 to closed_in_phase_34 and commit both files together. On any fail: do NOT mark this task done; open a gap-closure entry in 34-VERIFICATION.md and re-spawn the planner with `--gaps`.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md && grep -c "Inherited: GAP-02" .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md | grep -E '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-HUMAN-UAT.md` exists with valid frontmatter (`phase`, `type: human-uat`, `created`, `status`, `inherited_from`).
    - source-assertion: `grep -c "Inherited: GAP-02 revalidateTag end-to-end (from Phase 33)" 34-HUMAN-UAT.md` returns ≥ 1 (D-26 mandatory title).
    - source-assertion: 34-HUMAN-UAT.md contains 12 numbered test entries (T1..T12) each with the operator's pass/fail result inline.
    - cross-file: 33-HUMAN-UAT.md Test #2 is amended from `result: deferred_to_phase_34` to `result: closed_in_phase_34 (pass, <date>, <commit>)` IF all 12 Phase 34 tests pass.
    - behavior-assertion: every test recorded `pass` is reproducible against `npm run dev` by an independent operator following the same steps.
  </acceptance_criteria>
  <done>
    All 12 tests recorded in 34-HUMAN-UAT.md (with at least 11 of them — Inherited GAP-02 being the 12th — marked `pass`); 33-HUMAN-UAT.md Test #2 amendment committed in the same commit as 34-HUMAN-UAT.md; phase ready for verification + retrospective.
  </done>
  <resume-signal>Type "UAT passed" once all 12 tests are recorded as pass in 34-HUMAN-UAT.md AND the 33-HUMAN-UAT.md amendment is committed. If any test failed, type "UAT failed: <test number>: <observation>" and the planner will re-open with a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /, /import RSC | `auth()` redirect to `/sign-in` is defense-in-depth on top of `auth.protect()` middleware. Open-redirect surface: `redirect('/sign-in')` is a hardcoded relative path — no user-controlled component. |
| Browser → bulk import file upload | File parsed server-side by `previewImportAction` (Phase 33 — already gated by `requireRole`). Plan 07 only adds UI binding + client-side size guard. |
| URL `?order=<id>` → getOrderById | Drizzle parameterised query; stale id returns null (Pitfall 7). Plan 06 drawer renders empty state. |
| RSC fetch fan-out (Promise.all) | All three queries are `'server-only'`; cannot be called from client. Tag-string contract for cache invalidation already verified in plans 01 + 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07-01 | S — Spoofing | Page-level auth gate | mitigate | `auth()` (Clerk) in both `/` and `/import` RSC bodies, before any DB call. Redirect to `/sign-in` if `userId` is null. |
| T-34-07-02 | T — Tampering | Open-redirect via callbackUrl on `/sign-in` | mitigate | `redirect('/sign-in')` is a hardcoded path. There is NO `callbackUrl` param crafted in this plan — Clerk's own callback flow handles return-to-origin via session cookies, not query strings. If a future plan adds a callbackUrl, it MUST validate against a whitelist (NextAuth-style). For this phase, no surface. |
| T-34-07-03 | T — Tampering | `?order=<crafted-id>` URL → getOrderById | mitigate | Drizzle uses parameterised SQL; no injection surface. Stale/unknown ids return null; Pitfall 7 renders empty state without throwing or leaking diagnostics. |
| T-34-07-04 | R — Repudiation | Import audit chain | mitigate | `commitImportAction` writes an `import_batches` row before returning (Phase 33). UI does not write directly — `import_batches` IS the audit trail. |
| T-34-07-05 | I — Information Disclosure | ImportHistoryTable shows imported_by Clerk user IDs + file names | accept | Operator-internal metadata. File names may contain customer references but are not PII. DATA-05 lazy-sync deferred (CONTEXT.md). |
| T-34-07-06 | I — Information Disclosure | Stored XSS via fileName / note rendering | mitigate | All rendering uses React text interpolation (`{batch.fileName}`, `{event.note}`). React escapes by default; no `dangerouslySetInnerHTML`. |
| T-34-07-07 | D — Denial of Service | Oversized XLSX upload | mitigate | 3-layer guard per Phase 33: (1) `next.config.ts` `serverActions.bodySizeLimit: '2mb'`; (2) server action checks file.size in `previewImportAction` and `commitImportAction`; (3) client-side guard in `ImportFlow` (Task 2 Test 6) rejects > MAX_IMPORT_BYTES before submit. Defense-in-depth at three layers. |
| T-34-07-08 | E — Elevation of Privilege | Non-mill_operator triggers import | mitigate | `previewImportAction` + `commitImportAction` enforce `requireRole('mill_operator')` (Phase 33). Plan 07 ALSO hides the drop zone when `canEdit === false` (D-25 carry — Task 2). Defense-in-depth at both layers. |
| T-34-07-09 | T — Tampering | Cross-phase tag drift (Pitfall 8) | mitigate | Plan 01's grep gate on `revalidateTag\(['"]import-batches['"]` (returns exactly 2) prevents reverts. Plan 03 grep gate on the query side (cache key + tag both `'import-batches'`). Inherited GAP-02 UAT (Task 3) confirms the wire end-to-end. |
| T-34-07-10 | I — Information Disclosure | URL `?status=`, `?q=`, `?order=` in browser history | accept | Same posture as T-34-01-04. No PII; operator-internal. |
</threat_model>

<verification>
- `npm test` — full Jest suite green (every plan's tests + integration).
- `npx tsc --noEmit` — clean.
- `npm run build` — production build succeeds; no Edge-bundle errors; no `unstable_cache` deprecation surprises (RESEARCH.md Open Question 3 — note any warnings in the SUMMARY).
- Playwright `mill-operator-smoke.spec.ts` (Phase 31 existing) green against the new dashboard render.
- `grep -c "'production-orders'" src/actions/ src/db/queries/ | wc -l` ≥ 6 (action + query side, both tags present — sanity verification per 34-INHERITED-UAT.md diagnostic).
- `grep -c "'import-batches'" src/actions/ src/db/queries/ | wc -l` ≥ 3 (action × 2 success/degraded + query × 1).
- Manual UAT recorded in `34-HUMAN-UAT.md` with all 12 tests `pass` (including Inherited GAP-02).
- `33-HUMAN-UAT.md` Test #2 amended to `closed_in_phase_34`.
</verification>

<success_criteria>
- PROD-01: `/` is async RSC, `force-dynamic`, live DB-backed.
- PROD-02: three-column board renders from `getProductionOrders`.
- PROD-05: drawer renders order fields + transition history when `?order=` is present.
- PROD-10: per-column Suspense skeletons appear during `router.refresh`.
- D-15, D-16, D-17, D-18: bulk import lives at `/import`, history at the bottom, 2 MB guard at three layers, Skip default.
- D-21 + D-26: cross-phase patch verified by both unit test (plan 01) and end-to-end UAT (this plan, Task 3 Inherited GAP-02).
- All five ROADMAP Phase-34 success criteria fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-07-SUMMARY.md` summarizing:
- Files added/rewritten in this plan
- Test counts (page.tsx 8, ImportHistoryTable 4, ImportFlow 8, /import page 3 = 23 unit; +12 UAT tests in 34-HUMAN-UAT.md)
- Inherited GAP-02 closure status (pass/fail + commit hash)
- `33-HUMAN-UAT.md` amendment commit hash
- Any deviations from PATTERNS.md / RESEARCH.md (expected: none)
- Build output notes (Edge bundle clean? `unstable_cache` warnings?)
</output>
