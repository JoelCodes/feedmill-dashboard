---
phase: 34
plan: 06
type: execute
wave: 3
depends_on: [34-01, 34-03, 34-04, 34-05]
files_modified:
  - src/components/ProductionDrawer.tsx
  - src/components/ProductionDrawer.test.tsx
  - src/components/TransitionButtons.tsx
  - src/components/TransitionButtons.test.tsx
  - src/components/BlockReasonModal.tsx
  - src/components/BlockReasonModal.test.tsx
  - src/components/DrawerCloseHandlers.tsx
  - src/components/DrawerCloseHandlers.test.tsx
  - src/components/ProductionDashboard.tsx
autonomous: true
requirements: [PROD-05]
tags: [drawer, transitions, modal, radix-dialog]
must_haves:
  truths:
    - "Clicking an order card opens the order's detail drawer; the drawer shows order fields and a transition history timeline"
    - "If the drawer is opened for a stale or unknown order id, the operator sees an explicit 'Order not found' empty state with a Close button, not a crash or blank screen"
    - "Operator can close the drawer with the Close button, the ESC key, or by clicking the backdrop — each clears the order from the URL"
    - "D-10, D-11, D-12: The buttons shown to a mill_operator depend on the order state — D-10: all four transition actions live in the drawer ONLY (cards are click-targets, no inline transition controls); Pending shows Start Mixing; Mixing shows Complete Order (D-11: single-click, no confirm modal) + Block Order; Blocked shows Resume to Mixing + Resume to Pending (D-12: split button, primary=Mixing secondary=Pending, bound to resumeFromBlocked toState parameter); Completed shows no transition buttons"
    - "D-25: A read-only viewer (canEdit=false) sees the drawer body and timeline but no transition buttons — canEdit prop is server-computed via checkRole('mill_operator') and threaded into the drawer; only mill_operator users can write (server actions enforce the role gate; UI hides buttons in read-only mode)"
    - "After a successful transition, the drawer reflects the new state and timeline entry without a hard reload"
    - "D-14: If the operator's transition is rejected because another operator has already changed the order, the drawer shows the inline red banner with the exact locked message 'Order was modified by another user. Please refresh.' AND auto-calls router.refresh() to re-fetch fresh state (drawer-level, not card-level; no modal, no toast-only)"
    - "D-13: Block Order opens a modal dialog with a required textarea — Confirm calls blockOrder(orderId, version, reason); Confirm is disabled client-side until trim().length > 0; whitespace-only blocked client-side; server still validates (returns { code: 'validation' } if empty slips through); on success the modal closes and the drawer timeline refreshes to show the new Blocked event"
    - "Modal validation errors from the server are shown inline next to the textarea (operator can fix and retry without losing the rest of the drawer state)"
    - "Opening the Block modal does NOT cause the drawer to close on backdrop click or ESC (the modal owns those keys while open)"
  artifacts:
    - path: "src/components/ProductionDrawer.tsx"
      provides: "Client component rendering order details + timeline + transition buttons (see <context_amendment> below — drawer owns modalOpen state for BlockReasonModal)"
    - path: "src/components/TransitionButtons.tsx"
      provides: "Client wrapper for the four transition actions via useActionState"
      exports: ["StartMixingButton", "CompleteOrderButton", "BlockOrderTrigger", "ResumeButton", "default function TransitionButtons"]
    - path: "src/components/BlockReasonModal.tsx"
      provides: "Radix Dialog modal capturing block reason; calls blockOrder server action"
    - path: "src/components/DrawerCloseHandlers.tsx"
      provides: "Client child binding ESC + backdrop click → clear ?order="
  key_links:
    - from: "src/components/TransitionButtons.tsx"
      to: "@/actions/transitions"
      via: "useActionState closure wrapping transitionToMixing/completeOrder/blockOrder/resumeFromBlocked"
      pattern: "from ['\"]@/actions/transitions['\"]"
    - from: "src/components/BlockReasonModal.tsx"
      to: "@radix-ui/react-dialog"
      via: "Dialog.Root + Dialog.Portal + Dialog.Overlay + Dialog.Content"
      pattern: "@radix-ui/react-dialog"
    - from: "src/components/ProductionDrawer.tsx"
      to: "src/components/TransitionButtons.tsx"
      via: "JSX (conditional on canEdit)"
      pattern: "<TransitionButtons"
    - from: "src/components/ProductionDashboard.tsx"
      to: "src/components/ProductionDrawer.tsx"
      via: "JSX (conditional on drawerOrder || order URL state)"
      pattern: "<ProductionDrawer"
---

<context_amendment>
CONTEXT.md describes ProductionDrawer as a server component in <domain> and as planner-discretion in Build-time risks. Phase 34 elects client component because the drawer must own modal-open state for BlockReasonModal (Radix Dialog requires client boundary). Server-side data is loaded by the parent RSC and passed as props.
</context_amendment>

<objective>
Ship the order-details drawer, the four transition buttons, and the block-reason modal. Wire them into the dashboard wrapper (plan 05) by replacing the drawer-pass-through TODO with a real `<ProductionDrawer>` render. This plan delivers the entire mutation-affordance surface for the dashboard.

Purpose: D-08 through D-14 + D-25 + Pitfall 4 + Pitfall 7 + Pitfall 10 all converge here. Phase 33 server actions are consumed exactly as exported (no signature changes). The locked conflict message is rendered verbatim.

Output: four new components + one edit to ProductionDashboard; full unit-test coverage for transition buttons (RED→GREEN by mocking server actions), drawer render (RTL), modal interactions (RTL + Radix), and close handlers (RTL keyboard + click).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md
@src/actions/transitions.ts
@src/components/OrderDetails.tsx
@src/components/ui/Button.tsx
@src/components/ui/Textarea.tsx
@src/components/ui/StatusBadge.tsx
@src/components/ui/Timeline.tsx
@src/db/schema/orders.ts
@src/db/schema/events.ts

<interfaces>
From src/actions/transitions.ts:
- `export async function transitionToMixing(orderId: string, version: number): Promise<TransitionResult>`
- `export async function completeOrder(orderId: string, version: number): Promise<TransitionResult>`
- `export async function blockOrder(orderId: string, version: number, reason: string): Promise<TransitionResult>`
- `export async function resumeFromBlocked(orderId: string, version: number, toState: 'Mixing' | 'Pending'): Promise<TransitionResult>`
- `export type TransitionResult = { ok: true } | { ok: false; code: 'conflict' | 'unauthorized' | 'validation' | 'not_found' | 'server'; message: string };`
- LOCKED constant (from line 59): `CONFLICT_MESSAGE = 'Order was modified by another user. Please refresh.'`

Drawer prop signature (LOCKED):
```
type DrawerProps = {
  order: ProductionOrder | null;
  events: OrderEvent[];
  canEdit: boolean;
};
```

ProductionDashboard edit: replace the plan-05 unused-prop TODO with:
- Render `<ProductionDrawer order={drawerOrder} events={drawerEvents} canEdit={canEdit} />` conditionally on `order !== ''` (the nuqs `order` URL state) — actually, since the page RSC fetches drawerOrder/drawerEvents based on `?order=`, the simplest rule is: render the drawer whenever `order !== ''` URL state is set. If the page passed `drawerOrder === null` for a stale id, the drawer renders the "Order not found" state.
- Also wrap the drawer in `<Suspense fallback={<DrawerSkeleton />}>` (D-23).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransitionButtons — four useActionState-bound transition buttons + conflict banner</name>
  <files>
    src/components/TransitionButtons.tsx,
    src/components/TransitionButtons.test.tsx
  </files>
  <read_first>
    - src/actions/transitions.ts (signatures, TransitionResult, CONFLICT_MESSAGE)
    - src/components/ui/Button.tsx (variant + loading prop)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/TransitionButtons.tsx (NEW)" (verbatim pattern source)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pattern 3: useActionState Wiring" and §"Pitfall 10: useActionState form action vs direct invocation"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-10, D-11, D-12, D-14, D-25
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §5 "Transition buttons" + Copywriting Contract
  </read_first>
  <behavior>
    Tests mock `@/actions/transitions` (jest.mock) so each action becomes a `jest.fn()` returning a `Promise<TransitionResult>` we control. Mock `next/navigation` `useRouter()` to capture `mockRefresh`.

    - Test 1 (Pending state — D-11): With order in Pending state, the rendered output contains exactly ONE button labeled "Start Mixing" (no Complete, no Block). Click → mocked `transitionToMixing` is called with `(orderId, version)`.
    - Test 2 (Mixing state): Renders exactly TWO buttons: "Complete Order" (primary) + "Block Order" (destructive). Click Complete → `completeOrder(orderId, version)` called. Click Block → opens BlockReasonModal (asserted by `onOpenBlock` callback prop being invoked OR by querying for the modal — see Task 3).
    - Test 3 (Blocked state — D-12): Renders TWO buttons: "Resume to Mixing" (primary) + "Resume to Pending" (secondary). Click first → `resumeFromBlocked(orderId, version, 'Mixing')`. Click second → `resumeFromBlocked(orderId, version, 'Pending')`.
    - Test 4 (Completed state): Renders ZERO transition buttons.
    - Test 5 (conflict UX — D-14): Mock `transitionToMixing` to return `{ ok: false, code: 'conflict', message: 'Order was modified by another user. Please refresh.' }`. Click Start Mixing; await microtasks; assert the rendered output now contains the EXACT locked text `'Order was modified by another user. Please refresh.'` AND `mockRefresh` was called exactly once.
    - Test 6 (locked-message verbatim): The string `'Order was modified by another user. Please refresh.'` appears character-for-character in the conflict banner. (Snapshot-style equality on the rendered banner text.)
    - Test 7 (loading state): During the in-flight action call, the button has `aria-busy="true"` or otherwise indicates pending (depending on the Button component's `loading` prop wiring). Use `act` + a pending promise to assert.
    - Test 8 (canEdit not directly tested here): TransitionButtons receives `canEdit` from its parent (ProductionDrawer) — TransitionButtons itself does NOT check canEdit; the parent decides whether to render. Confirm by reading the component and asserting `grep -c "canEdit" src/components/TransitionButtons.tsx` returns 0.
  </behavior>
  <action>
    Create `src/components/TransitionButtons.tsx` per PATTERNS.md §"src/components/TransitionButtons.tsx (NEW)" — but expand from the `StartMixingButton` example into a full multi-button component.

    Structure recommendation:
    - `'use client';` at top.
    - Imports: `useActionState, useEffect` from 'react'; `useRouter` from 'next/navigation'; `transitionToMixing, completeOrder, blockOrder, resumeFromBlocked, type TransitionResult` from '@/actions/transitions'; `Button` from '@/components/ui/Button'; type `ProductionOrder` from '@/db/schema/orders'.
    - LOCKED constant: `const CONFLICT_MESSAGE = 'Order was modified by another user. Please refresh.';` — match `src/actions/transitions.ts` line 59 verbatim (RESEARCH.md confirms this must be character-for-character).
    - Helper subcomponents `StartMixingButton`, `CompleteOrderButton`, `ResumeButton({ toState: 'Mixing' | 'Pending' })`. Each uses `useActionState<TransitionResult | null, FormData>` with a closure wrapper per Pitfall 10:
      `const [state, formAction, isPending] = useActionState<TransitionResult | null, FormData>(async (_prev) => transitionToMixing(orderId, version), null);`
    - `useEffect(() => { if (state?.ok === false && state.code === 'conflict') router.refresh(); }, [state, router]);`
    - JSX for each button: `<form action={formAction}><Button variant={...} loading={isPending} className="w-full">{label}</Button>{state?.ok === false && state.code === 'conflict' && <p role="alert" className="mt-2 rounded border-l-4 border-[var(--error)] bg-[var(--error-light)] p-2 text-sm text-[var(--error-dark)]">{state.message}</p>}</form>`.
    - Block button is NOT bound to useActionState directly — it is a trigger that calls a parent-supplied `onBlockClick` callback (the modal is rendered by the drawer, not by TransitionButtons). Signature: `BlockOrderTrigger({ onClick })` — renders a `<Button variant="destructive">Block Order</Button>` that calls the prop.
    - Default export: `function TransitionButtons({ order, onBlockClick }: { order: ProductionOrder; onBlockClick: () => void; }): JSX.Element` — switches on `order.state` and renders the appropriate subset:
      - 'Pending' → `<StartMixingButton orderId={order.id} version={order.version} />`
      - 'Mixing' → `<CompleteOrderButton orderId={order.id} version={order.version} /> <BlockOrderTrigger onClick={onBlockClick} />`
      - 'Blocked' → `<ResumeButton orderId={order.id} version={order.version} toState="Mixing" /> <ResumeButton orderId={order.id} version={order.version} toState="Pending" />`
      - 'Completed' → `null`
    - Layout per UI-SPEC §5: `<div className="flex gap-3">{buttons}</div>` — full-width each via `w-full` on the inner Button.
    - JSDoc on the file head citing D-10/D-11/D-12/D-14/D-25 + UI-SPEC §5.

    Tests as enumerated. Use `jest.mock('@/actions/transitions', () => ({ transitionToMixing: jest.fn(), completeOrder: jest.fn(), blockOrder: jest.fn(), resumeFromBlocked: jest.fn() }))` per existing test conventions. Use `userEvent.click` + `waitFor` for the conflict banner appearance.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="TransitionButtons\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "useActionState" src/components/TransitionButtons.tsx` returns ≥ 3 (Start Mixing + Complete + at least one Resume; Block is a trigger not a useActionState).
    - source-assertion: `grep -c "Order was modified by another user\\. Please refresh\\." src/components/TransitionButtons.tsx` returns ≥ 1 (locked message present verbatim).
    - source-assertion: `grep -c "router\\.refresh\\(\\)" src/components/TransitionButtons.tsx` returns ≥ 1 (D-14 auto-refresh).
    - source-assertion: `grep -c "Start Mixing\|Complete Order\|Block Order\|Resume to Mixing\|Resume to Pending" src/components/TransitionButtons.tsx` returns ≥ 5 (all five UI-SPEC labels).
    - source-assertion: `grep -c "canEdit" src/components/TransitionButtons.tsx` returns 0 (canEdit check lives in the parent drawer).
    - source-assertion: `grep -c "code === 'conflict'" src/components/TransitionButtons.tsx` returns ≥ 1.
    - source-assertion: `grep -c "transitionToMixing\|completeOrder\|resumeFromBlocked" src/components/TransitionButtons.tsx` returns ≥ 3 (three actions bound; Block is dispatched via the modal).
    - behavior-assertion: `npm test -- --testPathPattern="TransitionButtons\.test\.tsx$"` passes all 7 cases.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Four transition buttons wired via useActionState + closure wrapper (Pitfall 10); conflict banner renders the locked message and auto-refreshes (D-14); Block button is a trigger callback (modal handled by drawer); seven tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BlockReasonModal — Radix Dialog wrapping required textarea</name>
  <files>
    src/components/BlockReasonModal.tsx,
    src/components/BlockReasonModal.test.tsx
  </files>
  <read_first>
    - src/actions/transitions.ts (blockOrder signature; TransitionResult shape)
    - src/components/ui/Textarea.tsx (label + required + error + aria-invalid)
    - src/components/ui/Button.tsx (variant, disabled, loading props)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/BlockReasonModal.tsx (NEW)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pattern 4: Radix Dialog (Block Reason) Stacked on Top of Drawer"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §6 "Block Reason Modal"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-13
  </read_first>
  <behavior>
    Tests use RTL + `userEvent`. Mock `@/actions/transitions` `blockOrder` as `jest.fn()` returning a controllable Promise.

    - Test 1: With `open={false}`, no modal content is in the DOM. With `open={true}`, the modal title "Block Order" and the textarea and the two buttons render.
    - Test 2: Textarea has `aria-required="true"` (or `required` attribute) and label text "Reason (required)".
    - Test 3: When textarea is empty (trim length 0), the Confirm button is disabled (`disabled` attribute OR `aria-disabled="true"`).
    - Test 4: Type "missing premix corn" → Confirm button becomes enabled.
    - Test 5: Type whitespace-only "   " → Confirm stays disabled (D-13 — `reason.trim().length === 0` guard).
    - Test 6: With a valid reason typed, click Confirm → `blockOrder(orderId, version, reason)` is called once with the correct args.
    - Test 7: When `blockOrder` resolves with `{ ok: true }`, the `onClose` callback is invoked exactly once and the textarea is cleared.
    - Test 8: When `blockOrder` resolves with `{ ok: false, code: 'validation', message: 'Reason is required' }`, the textarea shows the error message via its built-in `error` prop (assert `getByText('Reason is required')` is associated with the textarea).
    - Test 9: Click Cancel → `onClose` is invoked; `blockOrder` is NOT called.
    - Test 10: Pressing ESC inside the modal closes it (Radix Dialog default `onOpenChange(false)` path) — assert `onClose` invoked.
  </behavior>
  <action>
    Create `src/components/BlockReasonModal.tsx` per PATTERNS.md §"src/components/BlockReasonModal.tsx" and RESEARCH.md §Pattern 4. Concrete shape:

    - `'use client';`
    - Imports: `* as Dialog from '@radix-ui/react-dialog'`; `useActionState, useState` from 'react'; `blockOrder, type TransitionResult` from '@/actions/transitions'; `Textarea` from '@/components/ui/Textarea'; `Button` from '@/components/ui/Button'.
    - Props: `{ orderId: string; version: number; open: boolean; onClose: () => void; }`.
    - State: `const [reason, setReason] = useState('');`. `const [state, formAction, isPending] = useActionState<TransitionResult | null, FormData>(async () => { const result = await blockOrder(orderId, version, reason); if (result.ok) { onClose(); setReason(''); } return result; }, null);`.
    - JSX: `<Dialog.Root open={open} onOpenChange={(o) => !o && onClose()}><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-black/50" /><Dialog.Content className="fixed left-1/2 top-1/2 z-50 -translate-x-1/2 -translate-y-1/2 w-full max-w-[480px] rounded-[var(--radius-lg)] bg-[var(--bg-card)] p-6 shadow-lg"><Dialog.Title className="text-base font-bold text-[var(--text-primary)]">Block Order</Dialog.Title><form action={formAction} className="mt-4 flex flex-col gap-4"><Textarea label="Reason (required)" required value={reason} onChange={(e) => setReason(e.target.value)} placeholder="Describe the issue..." error={state?.ok === false && state.code === 'validation' ? state.message : undefined} /><div className="flex justify-end gap-3"><Button type="button" variant="secondary" onClick={onClose}>Cancel</Button><Button type="submit" variant="destructive" disabled={reason.trim().length === 0} loading={isPending}>Confirm Block</Button></div></form></Dialog.Content></Dialog.Portal></Dialog.Root>`.
    - JSDoc citing D-13 + UI-SPEC §6.

    Tests as enumerated. For Radix Dialog testing, no special harness is needed beyond RTL — Radix renders into `document.body` via `Dialog.Portal`, which jsdom supports. Use `screen.getByRole` to find the elements rather than `container.querySelector`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="BlockReasonModal\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "@radix-ui/react-dialog" src/components/BlockReasonModal.tsx` returns ≥ 1.
    - source-assertion: `grep -c "Dialog\\.Portal\|Dialog\\.Root\|Dialog\\.Content\|Dialog\\.Overlay\|Dialog\\.Title" src/components/BlockReasonModal.tsx` returns ≥ 5 (all five primitives used).
    - source-assertion: `grep -c "Reason (required)" src/components/BlockReasonModal.tsx` returns exactly 1 (UI-SPEC §6 label).
    - source-assertion: `grep -c "Confirm Block" src/components/BlockReasonModal.tsx` returns exactly 1.
    - source-assertion: `grep -c "reason\\.trim\\(\\)\\.length === 0\|reason\\.trim\\(\\)\\.length<=0\|!reason\\.trim\\(\\)\\.length" src/components/BlockReasonModal.tsx` returns ≥ 1.
    - source-assertion: `grep -c "blockOrder" src/components/BlockReasonModal.tsx` returns ≥ 1.
    - source-assertion: `grep -c "onOpenChange" src/components/BlockReasonModal.tsx` returns ≥ 1 (ESC + backdrop close routes through this).
    - behavior-assertion: all 10 RTL tests pass.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    BlockReasonModal renders Radix Dialog with required textarea, client-side trim guard, server-side validation error wiring; 10 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DrawerCloseHandlers + ProductionDrawer + dashboard integration</name>
  <files>
    src/components/DrawerCloseHandlers.tsx,
    src/components/DrawerCloseHandlers.test.tsx,
    src/components/ProductionDrawer.tsx,
    src/components/ProductionDrawer.test.tsx,
    src/components/ProductionDashboard.tsx
  </files>
  <read_first>
    - src/components/OrderDetails.tsx (header + timeline + field-row idioms — role-match analog)
    - src/components/ui/Timeline.tsx (Intl.DateTimeFormat 'MMM D, YYYY h:mm a' pattern, lines 57-66)
    - src/components/ui/StatusBadge.tsx (extended in plan 01 — now supports ProductionState)
    - src/components/DrawerSkeleton.tsx (plan 03 — used by Suspense fallback)
    - src/components/TransitionButtons.tsx (created in Task 1 above)
    - src/components/BlockReasonModal.tsx (created in Task 2 above)
    - src/components/ProductionDashboard.tsx (plan 05 — has the drawer pass-through TODO to remove)
    - src/db/schema/events.ts (OrderEvent shape: id, orderId, fromState, toState, changedBy, changedAt, note)
    - src/db/schema/orders.ts (ProductionOrder shape)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/ProductionDrawer.tsx (NEW — server component, slide-over)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §5 "Order Details Drawer"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-08, D-09, D-25
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 4: Drawer outside-click closing on modal click" and §"Pitfall 7: ?order=<stale-id> rendering"
  </read_first>
  <behavior>
    DrawerCloseHandlers:
    - Test 1: Rendering `<DrawerCloseHandlers />` mounts a key listener for ESC. Pressing ESC calls the `onClose` prop (asserted via mock).
    - Test 2: Pressing keys OTHER than ESC does NOT call `onClose`.
    - Test 3: When `modalOpen` prop is true, ESC press does NOT call `onClose` (Pitfall 4 — modal owns ESC; drawer must defer). NOTE: this requires the drawer to thread `modalOpen` state down. If the drawer wraps both modal and close-handlers within a state-bearing client wrapper, the wiring is natural.
    - Test 4 (backdrop click): The backdrop element bound to the close handler (NOT a document-level listener) fires onClose when clicked.

    ProductionDrawer (server component — no event listeners; tests render with RTL just to assert structure):
    - Test 5: With `order={null}`, renders the "Order not found" empty state (Pitfall 7) with 14px text and a Close button.
    - Test 6: With a valid order + events, renders the order number + customer in the header (`'ORD-001 - Acme Feed'` per UI-SPEC §5 — adjust to exact spec).
    - Test 7: Renders the order fields: Document Number, Customer, Product, Weight (formatted with `parseFloat().toLocaleString()` per Pitfall 6), Delivery Date, Mill Line, Formula Type (texture_type — render `—` when null per UI-SPEC §5), Blocker Note (only when state is Blocked).
    - Test 8: Renders the events timeline in chronological order (matches the order passed in; Phase-33 `getOrderEvents` returns DESC by changedAt — verify against schema if needed). For each event: `from → to` line, formatted date (`Intl.DateTimeFormat` 'MMM D, YYYY h:mm a'), `changedBy` clerk user ID, optional `note`.
    - Test 9: When `canEdit === true` and order state is not Completed, `<TransitionButtons>` is rendered.
    - Test 10: When `canEdit === false`, `<TransitionButtons>` is NOT rendered (D-25 read-only mode hides the buttons).
    - Test 11: The drawer container has width class `w-[480px]` (UI-SPEC §5).
    - Test 12: The backdrop element has `bg-black/30` per UI-SPEC §5 + Interaction States table.

    ProductionDashboard integration:
    - Test 13: With `drawerOrder={null}` AND `?order=''`, no drawer is rendered.
    - Test 14: With `drawerOrder={validOrder}` AND `?order='ord_1'`, the drawer renders inside `<Suspense fallback={<DrawerSkeleton />}>`.
    - Test 15: When the drawer's Close button (or backdrop click, or ESC) fires onClose, the URL state clears `?order=` via nuqs setter.
  </behavior>
  <action>
    Create `src/components/DrawerCloseHandlers.tsx` (`'use client'`):
    - Props: `{ onClose: () => void; modalOpen: boolean; }`.
    - Body: `useEffect(() => { if (modalOpen) return; const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, [modalOpen, onClose]);`.
    - Returns `null` — purely behavioral, no JSX.

    Create `src/components/ProductionDrawer.tsx` as a SERVER component (no 'use client' directive at the top — D-09 says drawer renders server-side; client-side wrappers handle interactivity). BUT: the drawer must thread a modal-open state and a close callback to its client children. The cleanest decomposition:

    - `ProductionDrawer` (server): receives `{ order, events, canEdit }`; renders fields + timeline (pure structure); composes a client wrapper `<DrawerInteractiveShell order={order} canEdit={canEdit}>{fields+timeline}</DrawerInteractiveShell>` that owns the modalOpen state, the close handlers, the BlockReasonModal mount, the TransitionButtons render with onBlockClick. The interactive shell handles outside-click on the backdrop, ESC via DrawerCloseHandlers, etc.

    Or, simpler: make `ProductionDrawer` itself a client component (`'use client'`) since D-09 only locks the page-RSC fetch contract, not the drawer's own client/server nature. The PATTERNS.md and CONTEXT.md leave the planner discretion ("two options" per RESEARCH.md §Build-time risks). Choose **client component** for plan 06 to simplify the close-handler + modalOpen state — the data is still fetched by the page RSC (D-09 preserved). Document this choice in JSDoc.

    Recommended structure (single client component):
    - `'use client'`
    - Imports: `useState` from 'react'; `useQueryStates, parseAsString` from 'nuqs'; `Button` from '@/components/ui/Button'; `StatusBadge` (extended in plan 01) from '@/components/ui/StatusBadge'; `TransitionButtons` from '@/components/TransitionButtons'; `BlockReasonModal` from '@/components/BlockReasonModal'; `DrawerCloseHandlers` from '@/components/DrawerCloseHandlers'; `X` icon from `lucide-react`; types from schema.
    - Props: `{ order: ProductionOrder | null; events: OrderEvent[]; canEdit: boolean; }`.
    - State: `const [modalOpen, setModalOpen] = useState(false);`. `const [, setQuery] = useQueryStates({ order: parseAsString.withDefault('') });`. `const handleClose = () => setQuery({ order: '' });`.
    - Null-branch (Pitfall 7): if `order === null`, render `<div className="fixed right-0 top-0 z-40 flex h-full w-[480px] flex-col items-center justify-center bg-[var(--bg-card)] p-6 shadow-xl"><DrawerCloseHandlers onClose={handleClose} modalOpen={false} /><p className="text-sm text-[var(--text-secondary)]">Order not found</p><Button variant="ghost" size="sm" onClick={handleClose}>Close</Button></div>` plus an adjacent backdrop `<div onClick={handleClose} className="fixed inset-0 z-30 bg-black/30" />`.
    - Valid-order render: backdrop (same as above) + drawer body container `<aside className="fixed right-0 top-0 z-40 h-full w-[480px] overflow-y-auto bg-[var(--bg-card)] shadow-xl transition-transform">` containing:
      - `<DrawerCloseHandlers onClose={handleClose} modalOpen={modalOpen} />` (handles ESC, gated by modalOpen)
      - Header (close X button top-right, order number, customer, StatusBadge for `order.state`)
      - Divider
      - Fields section: 7 label+value rows (Document Number, Customer, Product, Weight — `parseFloat(order.weightLbs).toLocaleString() + ' lbs'`, Delivery, Mill Line, Formula Type — render `order.textureType ?? '—'`, Blocker Note — render only when `order.state === 'Blocked'` and event with a note exists; planner picks display source — likely the most recent Blocked event's note).
      - Divider
      - Timeline section: heading "Transition History" + map `events` (chronological — page RSC fetches `getOrderEvents` which returns DESC; reverse to ascending if UI-SPEC §5 requires bottom-to-top order; verify UI-SPEC). For each event: dot + 'from → to' + Intl.DateTimeFormat date + changedBy + optional note. Date format: `new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit', hour12: true }).format(new Date(event.changedAt))` — matches `src/components/ui/Timeline.tsx` lines 57-66.
      - Transition buttons (conditional): `{canEdit && order.state !== 'Completed' && <TransitionButtons order={order} onBlockClick={() => setModalOpen(true)} />}`.
      - Modal mount: `<BlockReasonModal orderId={order.id} version={order.version} open={modalOpen} onClose={() => setModalOpen(false)} />`.
    - JSDoc citing D-08, D-09, D-13, D-25; note the planner choice of client-component shape for state simplicity.

    Edit `src/components/ProductionDashboard.tsx`: replace the "TODO plan 06" pass-through with an actual conditional render at the END of the JSX tree (so the drawer overlays everything):
    `{order && <Suspense fallback={<DrawerSkeleton />}><ProductionDrawer order={drawerOrder} events={drawerEvents} canEdit={canEdit} /></Suspense>}`
    where `order` is the nuqs URL state and `drawerOrder` / `drawerEvents` are the props from the page RSC. Add the `import DrawerSkeleton from '@/components/DrawerSkeleton';` import. Drop the `void canEdit; void drawerOrder; void drawerEvents;` TODO line(s) from plan 05.

    Tests: 4 for DrawerCloseHandlers, 8 for ProductionDrawer, 3 for the dashboard integration (15 total). Mock `next/navigation`, `nuqs` (via NuqsTestingAdapter), `@/actions/transitions` as needed.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(DrawerCloseHandlers|ProductionDrawer|ProductionDashboard)\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "Order not found" src/components/ProductionDrawer.tsx` returns ≥ 1 (Pitfall 7).
    - source-assertion: `grep -c "w-\\[480px\\]" src/components/ProductionDrawer.tsx` returns ≥ 1 (UI-SPEC §5 width).
    - source-assertion: `grep -c "bg-black/30" src/components/ProductionDrawer.tsx` returns ≥ 1 (backdrop UI-SPEC).
    - source-assertion: `grep -c "<TransitionButtons" src/components/ProductionDrawer.tsx` returns ≥ 1.
    - source-assertion: `grep -c "<BlockReasonModal" src/components/ProductionDrawer.tsx` returns ≥ 1.
    - source-assertion: `grep -c "canEdit &&\|canEdit\\s*\\?" src/components/ProductionDrawer.tsx` returns ≥ 1 (D-25 conditional render).
    - source-assertion: `grep -c "parseFloat(order\\.weightLbs)\|parseFloat\\(order\\.weightLbs" src/components/ProductionDrawer.tsx` returns ≥ 1 (Pitfall 6).
    - source-assertion: `grep -c "Intl\\.DateTimeFormat" src/components/ProductionDrawer.tsx` returns ≥ 1 (timeline date format).
    - source-assertion: `grep -c "Transition History" src/components/ProductionDrawer.tsx` returns ≥ 1 (UI-SPEC §5 heading).
    - source-assertion: `grep -c "addEventListener" src/components/DrawerCloseHandlers.tsx` returns ≥ 1.
    - source-assertion: `grep -c "modalOpen" src/components/DrawerCloseHandlers.tsx` returns ≥ 1 (Pitfall 4 gating).
    - source-assertion: `grep -c "<ProductionDrawer" src/components/ProductionDashboard.tsx` returns exactly 1.
    - source-assertion: `grep -c "<DrawerSkeleton" src/components/ProductionDashboard.tsx` returns ≥ 1 (Suspense fallback).
    - source-assertion: `grep -c "void canEdit\|void drawerOrder\|void drawerEvents" src/components/ProductionDashboard.tsx` returns 0 (TODO removed).
    - behavior-assertion: `npm test -- --testPathPattern="(DrawerCloseHandlers|ProductionDrawer|ProductionDashboard)\.test\.tsx$"` passes the 15 tests (regression suite includes plan-05 cases).
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Drawer renders order details + timeline + transition buttons (conditional on canEdit); BlockReasonModal mounts inside drawer; DrawerCloseHandlers handles ESC with modal-open gating (Pitfall 4); ProductionDashboard now renders the drawer via Suspense; 15 tests green; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → transitions server actions | Phase 33 actions own `requireRole('mill_operator')` (unchanged). Plan 06 only adds the UI binding. |
| Browser → blockOrder reason payload | User-controlled free text. Server action already validates non-empty (Phase 33 D-13 unit tests). |
| Modal Portal → drawer click handler | Pitfall 4 — drawer close listener gated by `modalOpen` state. |
| Drawer note rendering | order_events.note + blocker note are user-supplied strings; React escapes by default (no dangerouslySetInnerHTML). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-01 | S — Spoofing | Transition actions bound by useActionState | accept | Phase 33's `requireRole('mill_operator')` runs server-side. UI cannot spoof a role; calling the action without the role returns `{ code: 'unauthorized' }` (handled by action's `requireRole` which throws NEXT_REDIRECT — drawer code path never sees a stale call). |
| T-34-06-02 | T — Tampering | Block reason free text → blockOrder | mitigate | Zod schema in Phase 33's blockOrder rejects empty strings → `{ code: 'validation' }`. Client trim guard (Test 5) provides UX defense-in-depth. React escapes the reason when rendering it back in the timeline note. |
| T-34-06-03 | R — Repudiation | Every transition writes `order_events` row | mitigate | Already covered by Phase 33 (TRANS-05). Plan 06 does not write to `order_events` itself; the action writes the row before returning. |
| T-34-06-04 | I — Information Disclosure | Stored XSS via order.textureType / note fields | mitigate | All three text-rendering sites use React children interpolation (e.g., `{order.textureType ?? '—'}`, `{event.note}`). React escapes HTML by default. NO `dangerouslySetInnerHTML` anywhere in this plan. |
| T-34-06-05 | D — Denial of Service | Click-spamming a transition button | mitigate | Button `loading={isPending}` disables the button during in-flight; `useActionState`'s pending semantics prevent double-submission. Test 7 verifies. |
| T-34-06-06 | E — Elevation of Privilege | TransitionButtons rendered when `canEdit === false` would let unauthorized users trigger actions | mitigate | Drawer wrapper conditionally renders `<TransitionButtons>` ONLY when `canEdit === true` (Task 3 Test 10). Even if a malicious user injected the buttons via devtools, the server action's `requireRole` rejects via NEXT_REDIRECT. Defense-in-depth at both layers. |
| T-34-06-07 | T — Tampering | Drawer outside-click closing while modal is open | mitigate | DrawerCloseHandlers gates ESC and click logic on `modalOpen` state (Pitfall 4, Tests 3-4). Modal's ESC closes modal, NOT drawer. |
| T-34-06-08 | I — Information Disclosure | Stale `?order=<id>` URL renders empty state, not throw | mitigate | Drawer null-branch renders "Order not found" without exposing diagnostic information (Pitfall 7 / Test 5). |
| T-34-06-09 | I — Information Disclosure | changedBy field renders raw Clerk user ID in timeline | accept | Operator-internal; Clerk user IDs are opaque tokens (`user_XXX`), not PII. DATA-05 lazy-sync deferred per CONTEXT.md "Claude's Discretion". |
| T-34-06-IDOR | T — Tampering (IDOR on transition actions) | TransitionButtons → transitions server actions | mitigate | IDOR — orderId tampering on transitions: an authenticated `mill_operator` submits a transition action targeting an `orderId` they should not modify (e.g., an order belonging to a different mill line, or an order they did not see in their UI). Mitigation: server action reads `version` and applies state-machine guards; Phase 33's optimistic-concurrency guard rejects stale/invalid transitions; future enhancement may add per-line authorization. Severity per ASVS L1: MEDIUM. |
| T-34-06-CSRF | S — Spoofing (CSRF on transition / block actions) | TransitionButtons + BlockReasonModal server-action submissions | mitigate | CSRF on transition / block actions: a malicious site posts to the transition server action on behalf of a logged-in operator. Mitigation: Next.js 16 server actions enforce same-origin via the framework's built-in origin check; cookies are SameSite=Lax (verify via `src/lib/auth.ts` and Clerk middleware session-cookie configuration). Severity: LOW (framework-mitigated). |
| T-34-06-10 | T — Tampering (locked-string drift) | Conflict banner text could drift | mitigate | Acceptance criteria grep for the EXACT character-for-character string. Plan-07 UAT inherits this verification (GAP-02 closure). |
</threat_model>

<verification>
- `npm test -- --testPathPattern="(TransitionButtons|BlockReasonModal|DrawerCloseHandlers|ProductionDrawer|ProductionDashboard)\.test\.tsx$"` — all green (~42 tests across the four bundles plus plan-05 regression).
- `npx tsc --noEmit` — clean.
- `grep -rn "Order was modified by another user. Please refresh." src/` returns hits in both `src/actions/transitions.ts` (Phase 33 source of truth) AND `src/components/TransitionButtons.tsx` (verbatim copy). The text matches character-for-character.
- `grep -c "<ProductionDrawer" src/components/ProductionDashboard.tsx` returns exactly 1 (replaces the plan-05 TODO).
</verification>

<success_criteria>
- D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-25 all wired end-to-end with passing tests.
- Pitfall 4 (modal-on-drawer close conflict), Pitfall 7 (stale order id), Pitfall 10 (useActionState closure wrapper) all explicitly mitigated.
- Locked conflict message rendered character-for-character at the only place it's user-visible.
- PROD-05 (order details panel with transition history) implemented; transition buttons gated by canEdit per D-25.
- Phase 33 server actions consumed as-is — no signature changes; no role-gate changes; no `revalidateTag` calls from the UI (action layer owns it).
</success_criteria>

<output>
After completion, create `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-06-SUMMARY.md` summarizing:
- Component tree: ProductionDashboard → ProductionDrawer → { DrawerCloseHandlers, TransitionButtons → BlockReasonModal }
- Decision: ProductionDrawer is a 'use client' component (planner discretion, documented inline)
- Test counts (TransitionButtons 7, BlockReasonModal 10, DrawerCloseHandlers 4, ProductionDrawer 8, dashboard integration 3 — ~32 new tests in this plan; plus the 11 plan-05 tests remain green = ~43 across these files)
- Confirmation that the locked conflict message is verbatim in both files
- Notes for plan 07 (page RSC will fetch drawerOrder + drawerEvents via getOrderById / getOrderEvents when ?order= is present)
</output>
