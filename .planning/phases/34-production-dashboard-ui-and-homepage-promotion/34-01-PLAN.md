---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.tsx
  - src/lib/search-params.ts
  - src/lib/__tests__/search-params.test.ts
  - src/components/ui/StatusBadge.tsx
  - src/components/ui/StatusBadge.test.tsx
  - src/components/Sidebar.tsx
  - src/components/Sidebar.test.tsx
  - src/components/Header.tsx
  - src/components/Header.test.tsx
  - src/actions/import.ts
  - src/actions/__tests__/import.test.ts
  - src/components/MillReadOnlyStub.tsx
  - src/app/page.tsx
  - src/app/page.test.tsx
  - src/lib/auth.ts
autonomous: true
requirements: [PROD-01, PROD-03, PROD-04, PROD-05, PROD-06]
tags: [nuqs, radix-dialog, sidebar, header, foundation]
must_haves:
  truths:
    - "D-04, D-05, D-06: Dashboard URL state survives a hard reload (status filter pills, search query, and drawer-open order id are preserved)"
    - "D-06: Status filter pills, search input, and order drawer all respect the same URL contract — sharing or bookmarking a filtered view works"
    - "D-03: Production status badges display the correct color and label for Pending / Mixing / Completed / Blocked orders (StatusBadge is reused as-is from src/components/ui/ and extended with new state cases — the component itself is not cloned)"
    - "D-24: The Sidebar lists Dashboard and Import as the only two production destinations — no 'Coming Soon' placeholder is shown"
    - "The Header page title reads 'Dashboard' on `/` and 'Import' on `/import`"
    - "D-21: Committing a bulk import refreshes both the production board AND the import history list within the same cache cycle (the operator does not need to hard-reload to see the new batch) — commitImportAction is patched to invalidate both `production-orders` and `import-batches` tags"
    - "Visiting `/` after Phase 34 foundation lands does not 500 or render the prior MillReadOnlyStub placeholder — a clean transitional page renders pending plan 07 wiring"
  artifacts:
    - path: "src/lib/search-params.ts"
      provides: "STATE_ORDER constant + searchParamsCache for ?status, ?q, ?order"
      exports: ["STATE_ORDER", "searchParamsCache"]
    - path: "src/lib/__tests__/search-params.test.ts"
      provides: "TDD red/green coverage of parsers (D-04 unknown-value drop, D-05 default, D-06 default)"
    - path: "src/app/layout.tsx"
      provides: "Root layout with NuqsAdapter wrapping {children}"
      contains: "NuqsAdapter"
    - path: "src/components/ui/StatusBadge.tsx"
      provides: "StatusBadge accepting both OrderStatus and ProductionState"
    - path: "src/components/Sidebar.tsx"
      provides: "productionNavItems = [Dashboard, Import]; isActive('/', ...) exact-match preserved"
    - path: "src/components/Header.tsx"
      provides: "getPageTitle entries for '/' and '/import'"
    - path: "src/actions/import.ts"
      provides: "commitImportAction patched with revalidateTag('import-batches','max')"
  key_links:
    - from: "src/app/layout.tsx"
      to: "nuqs/adapters/next/app"
      via: "<NuqsAdapter>{children}</NuqsAdapter>"
      pattern: "NuqsAdapter"
    - from: "src/lib/search-params.ts"
      to: "src/db/schema/orders.ts"
      via: "type ProductionState import"
      pattern: "import.*ProductionState.*@/db/schema/orders"
    - from: "src/actions/import.ts"
      to: "next/cache"
      via: "revalidateTag('import-batches','max')"
      pattern: "revalidateTag\\(['\"]import-batches['\"]"
    - from: "src/components/Sidebar.tsx"
      to: "/import"
      via: "productionNavItems entry"
      pattern: "href:\\s*[\"']/import[\"']"
---

<!-- Note: 17 files reflects source+test pairs + lockfile + deletion across the foundation surface; logical units ≈ 8. -->

<objective>
Phase-34 foundation: install nuqs + Radix Dialog, wire the NuqsAdapter in the root layout, ship the shared `searchParamsCache` and `STATE_ORDER` constant, extend `StatusBadge` for the production state set, edit `Sidebar` + `Header` for the new production nav, patch `commitImportAction` with the `revalidateTag('import-batches')` invalidation (D-21), and delete `MillReadOnlyStub.tsx` while leaving `src/app/page.tsx` in a clean transitional state (the full `ProductionDashboard` wiring lands in plan 07).

Purpose: every downstream plan in Phase 34 imports from `src/lib/search-params.ts`, expects `NuqsAdapter` to be mounted, and assumes StatusBadge can render production states. Without this foundation each later plan would either reimplement these primitives or fail at runtime ("nuqs requires an adapter").

Output: all foundation artifacts above, with TDD coverage for the URL-state parsers and the D-21 cache-tag patch, plus a green build that no longer references `MillReadOnlyStub`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md
@.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md
@.planning/phases/33-server-actions-queries-and-bulk-import/34-INHERITED-UAT.md
@src/app/layout.tsx
@src/app/page.tsx
@src/components/Sidebar.tsx
@src/components/Header.tsx
@src/components/ui/StatusBadge.tsx
@src/actions/import.ts
@src/db/schema/orders.ts
@src/types/clerk.d.ts

<interfaces>
<!-- Type definitions executors will rely on (extracted from existing files). -->

From src/db/schema/orders.ts (canonical, do NOT redeclare):
- `ProductionState = 'Pending' | 'Mixing' | 'Completed' | 'Blocked'`
- `MillLine = 'Premix' | 'Excel' | 'CGM'`
- `ProductionOrder = typeof productionOrders.$inferSelect` — note `weight_lbs: string` (numeric column)

From src/types/clerk.d.ts:
- `Role = 'demo' | 'admin' | 'user' | 'mill_operator'`

From src/actions/import.ts (current contract, do NOT change function signature):
- `export async function commitImportAction(formData: FormData, decisions: ImportDecisions): Promise<CommitResult>`
- Already calls `revalidateTag('production-orders', 'max')` in two places (success path ~line 808, degraded-success ~line 793). D-21 patch adds the second tag in BOTH places.

From src/components/ui/StatusBadge.tsx (current export):
- `STATUS_CONFIG: Record<OrderStatus, StatusConfig>` — to be extended into a union/lookup that ALSO covers `ProductionState`. Two approaches available; see action below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps + wire NuqsAdapter + create search-params lib (TDD red/green for parsers)</name>
  <files>
    package.json,
    package-lock.json,
    src/app/layout.tsx,
    src/lib/search-params.ts,
    src/lib/__tests__/search-params.test.ts
  </files>
  <read_first>
    - src/app/layout.tsx (current shape — ClerkProvider > ThemeProvider > {children})
    - src/db/schema/orders.ts (ProductionState union — source of truth for STATE_ORDER literals)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/app/layout.tsx (MODIFY)" and §"src/lib/search-params.ts (NEW)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pattern 1: nuqs Server-Side Parse" and §"Pitfall 1: nuqs NuqsAdapter missing"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-04, D-05, D-06
  </read_first>
  <behavior>
    - Test 1 (RED): `searchParamsCache.parse({ status: 'Pending,Mixing' })` resolves to `{ status: ['Pending','Mixing'], q: '', order: '' }`.
    - Test 2 (RED): `searchParamsCache.parse({ status: 'Pending,BadValue,Mixing' })` silently drops `BadValue`; returns `{ status: ['Pending','Mixing'], q: '', order: '' }` (D-04 unknown-value drop).
    - Test 3 (RED): missing key `status` → returns `status: []` via `.withDefault([])`.
    - Test 4 (RED): `q` default is `''`; `q: 'ACME'` round-trips as `'ACME'` (parsing does NOT trim/lowercase — that's a render-time concern per D-05).
    - Test 5 (RED): `order` default is `''`; presence of any string preserved verbatim.
    - Test 6 (RED): `STATE_ORDER` deep-equals `['Pending','Mixing','Completed','Blocked']` and is `readonly` (assertion via TypeScript `as const satisfies readonly ProductionState[]`).
    - After implementation (GREEN): all six tests pass.
  </behavior>
  <action>
    Run `npm install nuqs@2.8.9 @radix-ui/react-dialog@1.1.15` (versions verified in RESEARCH.md §Standard Stack). Commit both `package.json` and `package-lock.json`.

    In `src/app/layout.tsx`: add `import { NuqsAdapter } from 'nuqs/adapters/next/app'` at the top of the imports block. Inside the JSX, wrap `{children}` with `<NuqsAdapter>{children}</NuqsAdapter>` directly inside `<ThemeProvider>` (so the nesting is ClerkProvider > ThemeProvider > NuqsAdapter > {children}). Do not move ClerkProvider; do not move ThemeProvider. Do not change `afterSignOutUrl` or `suppressHydrationWarning`.

    Create `src/lib/search-params.ts` exporting:
    - `STATE_ORDER` — `as const satisfies readonly ProductionState[]` tuple in the exact order `['Pending','Mixing','Completed','Blocked']`. ProductionState imported from `@/db/schema/orders`. Add a JSDoc citing D-04 and noting that filter-pill display order is determined per UI-SPEC §1 by the renderer (parsing is set-semantics so order is irrelevant for the parser).
    - `searchParamsCache` — built via `createSearchParamsCache({ status: parseAsArrayOf(parseAsStringLiteral(STATE_ORDER)).withDefault([]), q: parseAsString.withDefault(''), order: parseAsString.withDefault('') })`. Imports come from `nuqs/server`.

    Create `src/lib/__tests__/search-params.test.ts` writing the six tests in <behavior> BEFORE implementation (RED commit). Use the existing Jest harness. Mock-free; pure unit tests. After tests are red, implement `src/lib/search-params.ts`. Confirm GREEN by `npm test -- --testPathPattern=search-params.test.ts`.

    No fenced code blocks in this action; copy the verbatim snippets from 34-PATTERNS.md §"src/lib/search-params.ts (NEW)" and 34-RESEARCH.md §Pattern 1 if needed.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=search-params.test.ts && npm run typecheck 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -n "from 'nuqs/adapters/next/app'" src/app/layout.tsx` returns exactly 1 match.
    - source-assertion: `grep -v '^//' src/app/layout.tsx | grep -c "<NuqsAdapter>"` returns exactly 1 (one open tag, ignoring comments).
    - source-assertion: `grep -n "STATE_ORDER" src/lib/search-params.ts` shows an `export const STATE_ORDER` declaration; `grep -c "'Pending'" src/lib/search-params.ts` returns ≥ 1 and `'Mixing'`, `'Completed'`, `'Blocked'` each return ≥ 1.
    - source-assertion: `grep -c "searchParamsCache" src/lib/search-params.ts` returns ≥ 1; `grep -c "createSearchParamsCache" src/lib/search-params.ts` returns exactly 1.
    - source-assertion: `grep -c "parseAsArrayOf" src/lib/search-params.ts` returns exactly 1; `grep -c "parseAsStringLiteral" src/lib/search-params.ts` returns exactly 1.
    - behavior-assertion: `npm test -- --testPathPattern=search-params.test.ts` passes all six behaviors above with no skips.
    - test-command: `cat package.json | grep -E '"nuqs"\s*:\s*"2.8.9"'` returns a hit (exact pinned version).
    - test-command: `cat package.json | grep -E '"@radix-ui/react-dialog"\s*:\s*"1.1.15"'` returns a hit.
  </acceptance_criteria>
  <done>
    nuqs + Radix Dialog installed at locked versions; NuqsAdapter mounted; search-params module exports STATE_ORDER and searchParamsCache with six green unit tests; build (`npx tsc --noEmit`) passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend StatusBadge for ProductionState + Sidebar/Header production nav</name>
  <files>
    src/components/ui/StatusBadge.tsx,
    src/components/ui/StatusBadge.test.tsx,
    src/components/Sidebar.tsx,
    src/components/Sidebar.test.tsx,
    src/components/Header.tsx,
    src/components/Header.test.tsx
  </files>
  <read_first>
    - src/components/ui/StatusBadge.tsx (current STATUS_CONFIG keyed on OrderStatus)
    - src/components/Sidebar.tsx (current productionNavItems + isActive helper, lines 19-32)
    - src/components/Header.tsx (current getPageTitle helper, lines 23-34)
    - src/db/schema/orders.ts (ProductionState union)
    - src/app/globals.css (status-{state}-{token} tokens; do not invent new ones)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/components/Sidebar.tsx" and §"src/components/Header.tsx"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-UI-SPEC.md §"Sidebar Update Contract"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 5: Sidebar prefix-match collision"
  </read_first>
  <behavior>
    StatusBadge:
    - Test 1 (RED): `<StatusBadge status="Mixing" />` renders without throwing; `getByRole('status')` (or container) contains the literal text "Mixing".
    - Test 2 (RED): `<StatusBadge status="Blocked" />` renders the blocked variant — assert presence of CSS class or style referencing `--status-blocked-` token or `bg-error-light` class.
    - Test 3 (RED): `<StatusBadge status="Pending" />` and `<StatusBadge status="Completed" />` both render their literal label text.
    - Test 4 (regression): existing `OrderStatus` cases (`'Producing'`, `'Ready'`, `'In Transit'`, `'Complete'`, original `'Pending'`) still render. Keep prior tests green.

    Sidebar:
    - Test 5 (RED): when production branch is shown (path not under `/demo/`), `getByRole('link', { name: /Dashboard/i })` has `href="/"` and `getByRole('link', { name: /Import/i })` has `href="/import"`.
    - Test 6 (RED): no element with the literal text "Coming Soon" appears in production branch render.
    - Test 7 (Pitfall 5 regression): with pathname `/import`, the Dashboard link does NOT carry the active style (assert via the `isActive` discriminator class), confirming `/` is exact-match-only.

    Header:
    - Test 8 (RED): rendered with `usePathname` mocked to `'/'`, the page title text is "Dashboard".
    - Test 9 (RED): rendered with `usePathname` mocked to `'/import'`, the page title text is "Import".
    - Test 10 (RED): rendered with `usePathname` mocked to `'/import?foo=bar'` (next/navigation strips query in usePathname; mock returns `/import`), title is still "Import" (covers prefix-match).
    - Test 11 (regression): existing `/demo/orders` returns 'Orders' (and other demo routes return their existing titles). Keep prior tests green.
  </behavior>
  <action>
    Extend `src/components/ui/StatusBadge.tsx` to accept the union `OrderStatus | ProductionState` (or define a new `BadgeState` union covering both) without losing existing OrderStatus support. Add four entries to STATUS_CONFIG: `'Mixing'`, `'Completed'`, `'Blocked'`, and re-mapping `'Pending'` (already present — keep its existing config unless ProductionState semantics conflict; they don't, since both use the same pending color). Use the status tokens from globals.css per UI-SPEC §"Color":
    - Mixing → bg `bg-warning-light`, text `text-warning`, dot `bg-warning`, countBg `bg-[var(--status-mixing-bg-22)]`, label `'Mixing'`.
    - Completed → bg `bg-success-light`, text `text-success-dark`, dot `bg-success-dark`, countBg `bg-[var(--status-completed-bg-22)]`, label `'Completed'`.
    - Blocked → bg `bg-error-light`, text `text-error-dark`, dot `bg-error`, countBg `bg-[var(--status-blocked-bg-22)]`, label `'Blocked'`.
    The existing `'Pending'` config (pending-light / text-secondary / pending dot) already matches UI-SPEC for ProductionState 'Pending' — no change.

    Update Props type so `status: OrderStatus | ProductionState` (or a single shared union). Import ProductionState from `@/db/schema/orders`.

    Add tests 1-4 to `src/components/ui/StatusBadge.test.tsx`. Keep all existing test cases passing (regression).

    In `src/components/Sidebar.tsx` lines 19-21: replace `productionNavItems` with the two-entry array per UI-SPEC and PATTERNS.md. Import `Upload` icon from `lucide-react` alongside the existing imports. Do NOT touch `isActive` (Pitfall 5).

    In `src/components/Sidebar.test.tsx`: add tests 5-7. Use the existing test harness pattern from prior cases (mock `usePathname` from `next/navigation`).

    In `src/components/Header.tsx` `getPageTitle`: add the two production cases — exact `if (path === '/') return 'Dashboard';` (place after demo checks per PATTERNS.md ordering) and `if (path.startsWith('/import')) return 'Import';`. Preserve the existing fallback `return 'Dashboard';`. Keep all demo + settings cases as-is.

    In `src/components/Header.test.tsx`: add tests 8-10 and confirm existing tests still pass.

    Implementation order: RED commit (tests fail) → GREEN commit (implementation + tests pass) → no REFACTOR needed.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(StatusBadge|Sidebar|Header)\.test\.tsx$"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "'Mixing'" src/components/ui/StatusBadge.tsx` returns ≥ 1, similarly for 'Completed' and 'Blocked'.
    - source-assertion: `grep -c "Coming Soon" src/components/Sidebar.tsx` returns 0.
    - source-assertion: `grep -E "href:\s*[\"']/import[\"']" src/components/Sidebar.tsx` returns exactly 1 match.
    - source-assertion: `grep -E "href:\s*[\"']/[\"']" src/components/Sidebar.tsx | grep -v '/demo' | grep -v '/import'` shows the `href: "/"` Dashboard entry.
    - source-assertion: `grep -E "return 'Dashboard'" src/components/Header.tsx` shows the exact-match `'/'` branch; `grep -E "/import.*return 'Import'" src/components/Header.tsx` matches the new branch.
    - source-assertion: `grep -n "pathname === \"/\"" src/components/Sidebar.tsx` still shows the exact-match guard (Pitfall 5 — must not be removed).
    - behavior-assertion: `npm test -- --testPathPattern="(StatusBadge|Sidebar|Header)\.test\.tsx$"` passes all listed cases.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    StatusBadge handles both OrderStatus and ProductionState; Sidebar shows Dashboard+Import (no "Coming Soon"); Header titles are correct for `/` and `/import`; eleven tests are green and prior regression tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-21 commitImportAction revalidateTag patch + MillReadOnlyStub deletion + transitional page.tsx</name>
  <files>
    src/actions/import.ts,
    src/actions/__tests__/import.test.ts,
    src/components/MillReadOnlyStub.tsx,
    src/app/page.tsx,
    src/app/page.test.tsx,
    src/lib/auth.ts
  </files>
  <read_first>
    - src/actions/import.ts (existing revalidateTag('production-orders','max') call sites ~line 793 and ~line 808)
    - src/actions/__tests__/import.test.ts (existing revalidateTag spy pattern — Phase 33 already mocks next/cache)
    - src/app/page.tsx (current Phase 31 stub — auth + checkRole + MillReadOnlyStub)
    - src/app/page.test.tsx (existing tests for the Phase 31 stub)
    - src/components/MillReadOnlyStub.tsx (will be deleted)
    - src/lib/auth.ts line ~80 (JSDoc example referencing MillReadOnlyStub — must be updated)
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-PATTERNS.md §"src/actions/import.ts (MODIFY — single-line patch, D-21)" and §"src/components/MillReadOnlyStub.tsx (DELETE)"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-RESEARCH.md §"Pitfall 8: Forgetting the revalidateTag('import-batches') patch"
    - .planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-CONTEXT.md D-21
  </read_first>
  <behavior>
    - Test 1 (RED): mocking `revalidateTag`, when `commitImportAction` resolves successfully it has been called with BOTH `('production-orders','max')` AND `('import-batches','max')`. Use `expect(revalidateTag).toHaveBeenCalledWith('production-orders','max')` and `expect(revalidateTag).toHaveBeenCalledWith('import-batches','max')`.
    - Test 2 (RED): in the degraded-success branch (batch-insert audit row failure but row inserts succeeded), both tag calls still fire. Trigger via the existing fault-injection harness in the Phase 33 import test or, if not present, add a fresh test that mocks db.insert on `importBatches` to throw while the production_orders inserts succeed.
    - Test 3 (regression): unchanged failure paths (validation failure before any insert) do NOT call `revalidateTag('import-batches','max')` (and continue not calling `revalidateTag('production-orders','max')`).
  </behavior>
  <action>
    Patch `src/actions/import.ts` at the two existing `revalidateTag('production-orders','max')` invocation sites (success path ~line 808 and degraded-success path ~line 793 — verify exact line numbers via grep before editing). Immediately after each existing call, add `revalidateTag('import-batches', 'max');` on a new line inside the same try-block. Preserve the existing console.error catch handlers verbatim. Do NOT touch the action's signature, role-guard, parse logic, or DB operations.

    Add tests 1-3 to `src/actions/__tests__/import.test.ts`. The existing suite already mocks `next/cache` — reuse that mock and add `toHaveBeenCalledWith('import-batches','max')` assertions to the existing success-path test plus a new test for the degraded path. Confirm RED (assertion fails before patch) → GREEN (passes after patch).

    Run `grep -rn "MillReadOnlyStub" src/` to enumerate remaining references. Expected hits: `src/app/page.tsx`, `src/app/page.test.tsx`, `src/lib/auth.ts` (JSDoc only). Remove all of them:
    - `src/components/MillReadOnlyStub.tsx` — delete the file (use `git rm` semantics via Bash `rm` — git status will pick it up). If a test file `src/components/MillReadOnlyStub.test.tsx` exists, delete it too.
    - `src/app/page.tsx` — rewrite as a transitional stub that retains the auth+checkRole shape from Phase 31 but renders a minimal placeholder `<main>` element with the text "Dashboard placeholder — wired in plan 07". This intentional intermediate keeps the build green between Wave 1 and Wave 4 without committing to ProductionDashboard wiring yet. Imports retained: `auth`, `redirect`, `checkRole`, `DashboardLayout`. Drop the `<MillReadOnlyStub />` import and render.
    - `src/app/page.test.tsx` — update to reflect the new transitional shape: keep the auth-redirect test (unauth → `/sign-in`), keep the `canEdit` prop-flow test if applicable, replace any assertion about `MillReadOnlyStub` rendering with an assertion that the placeholder text "Dashboard placeholder" appears. Add a TODO comment referencing plan 07 ("This test is rewritten in 34-07-PLAN.md to assert ProductionDashboard rendering.").
    - `src/lib/auth.ts` line ~80 JSDoc — update the example to reference `ProductionDashboard` (forward reference is fine; this is a comment-only edit) OR remove the example entirely. Do NOT change any runtime exports.

    After edits: `grep -rn "MillReadOnlyStub" src/` must return zero matches.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(actions/__tests__/import|app/page)\.test\.(ts|tsx)$" && grep -rn "MillReadOnlyStub" src/ | wc -l | grep -E '^\s*0\s*$'</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "revalidateTag\(['\"]import-batches['\"]" src/actions/import.ts` returns exactly 2 (success path + degraded path).
    - source-assertion: `grep -c "revalidateTag\(['\"]production-orders['\"]" src/actions/import.ts` is unchanged from pre-patch (≥ 2; do NOT remove existing calls).
    - source-assertion: `ls src/components/MillReadOnlyStub.tsx 2>/dev/null` returns nothing (file deleted).
    - source-assertion: `grep -rn 'MillReadOnlyStub' src/` returns 0 matches.
    - source-assertion: `grep -E "Dashboard placeholder" src/app/page.tsx` returns exactly 1 match (transitional stub text).
    - source-assertion: `grep -c "import.*auth.*@clerk/nextjs/server" src/app/page.tsx` returns exactly 1 (auth flow preserved).
    - behavior-assertion: `npm test -- --testPathPattern="actions/__tests__/import\.test\.ts$"` shows the new D-21 tests green.
    - behavior-assertion: `npm test -- --testPathPattern="app/page\.test\.tsx$"` passes with the transitional-stub assertions.
    - CLI output: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `commitImportAction` invalidates both `production-orders` and `import-batches` on both success and degraded-success paths; D-21 unit tests green; `MillReadOnlyStub` deleted everywhere; `src/app/page.tsx` is a clean transitional stub awaiting plan 07; full Jest suite passes; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /, /import RSC | Unauth users hit `auth.protect()` in middleware + `auth()` in page; defense-in-depth redirect to `/sign-in` |
| Client form → commitImportAction | Already protected via `requireRole('mill_operator')` (Phase 33). D-21 patch does not alter the auth gate. |
| Sidebar nav → /import | Operator-internal route; same auth posture as `/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | S — Spoofing | NuqsAdapter session/context boundary | accept | NuqsAdapter is a context provider only; no auth surface. Clerk session is established by ClerkProvider (outermost). No spoofing path introduced. |
| T-34-01-02 | T — Tampering | `?status=`, `?q=`, `?order=` URL params | mitigate | `parseAsStringLiteral(STATE_ORDER)` drops unknown literals silently (D-04). `?q=` is plain string used only for client-side substring match — no SQL injection surface (D-07 explicitly forbids server-side ILIKE in v2.0). `?order=<id>` is fed to `getOrderById(id)` which uses parameterized Drizzle queries; stale IDs render an empty state, do not throw (Pitfall 7). |
| T-34-01-03 | R — Repudiation | `revalidateTag('import-batches')` patch | mitigate | The patched action still owns the `requireRole` gate (line ~529) + audit row in `import_batches`. New tag invalidation does NOT bypass any audit step; it ONLY adds cache refresh. |
| T-34-01-04 | I — Information Disclosure | URL params (`?status=Pending,Mixing`, `?q=acme`, `?order=ord_123`) | accept | All three are operator-internal metadata. Status pills and search terms are not PII. Order IDs are opaque internal identifiers; exposing them to the URL is required for shareable dashboard links and bookmarkable drawer state. Documented in CONTEXT.md security commentary. |
| T-34-01-05 | D — Denial of Service | Parser cache / repeated `searchParamsCache.parse` calls | accept | `createSearchParamsCache` uses React `cache()` internally; per-request memoization. No external network call. |
| T-34-01-06 | E — Elevation of Privilege | StatusBadge config extension | accept | StatusBadge is pure presentation — no auth or data fetching path. Extension purely adds visual variants. |
| T-34-01-07 | T — Tampering | Sidebar prefix-match collision (Pitfall 5) | mitigate | Explicit Sidebar.test.tsx case (Test 7 in Task 2) asserts `isActive('/', '/import') === false`. Future refactor that breaks the exact-match guard surfaces as a failing test in CI. |
| T-34-01-08 | I — Information Disclosure | Transitional `src/app/page.tsx` placeholder text | accept | Placeholder is the literal string "Dashboard placeholder — wired in plan 07". Contains no PII, no order data. The auth gate (`auth()` + `redirect('/sign-in')`) still runs, so unauth users never see the placeholder. |
</threat_model>

<verification>
- `npm test` — full Jest suite green (all three task test bundles + regression).
- `npx tsc --noEmit` — TypeScript clean.
- `grep -rn "MillReadOnlyStub" src/` — returns 0 matches.
- `grep -c "revalidateTag\(['\"]import-batches['\"]" src/actions/import.ts` — returns exactly 2.
- `cat package.json | grep -E '"nuqs":\s*"2\.8\.9"'` — exact pinned version.
- `cat package.json | grep -E '"@radix-ui/react-dialog":\s*"1\.1\.15"'` — exact pinned version.
- Manual: `npm run dev`, visit `/` while signed in, confirm placeholder renders and console.error is empty (no nuqs adapter warning).
</verification>

<success_criteria>
- All four locked decisions implemented: D-04 (status URL param via parseAsStringLiteral), D-05 (q URL param), D-06 (order URL param), D-21 (commitImportAction patches both tags), D-24 (sidebar items), and the UI-SPEC Sidebar Update Contract.
- Foundation primitives available to downstream plans: `STATE_ORDER`, `searchParamsCache`, extended `StatusBadge`, updated `Sidebar`, updated `Header`, working `NuqsAdapter`.
- D-21 cross-phase patch surfaced as an explicit task (not folded silently) — RESEARCH.md Pitfall 8 mitigation satisfied.
- `MillReadOnlyStub` removed cleanly with no lingering imports.
- Build is green between Wave 1 and Wave 4 thanks to the transitional `src/app/page.tsx` stub.
</success_criteria>

<output>
After completion, create `.planning/phases/34-production-dashboard-ui-and-homepage-promotion/34-01-SUMMARY.md` summarizing:
- Installed dep versions (nuqs 2.8.9, @radix-ui/react-dialog 1.1.15) + lockfile commit hash
- Created files (`src/lib/search-params.ts`, test) and their export shapes
- D-21 patch sites in `src/actions/import.ts` (exact line numbers post-patch)
- Files deleted (`MillReadOnlyStub.tsx`) and references swept
- Any deviations from PATTERNS.md or RESEARCH.md (expected: none)
- Test counts: search-params (6), StatusBadge additions, Sidebar additions, Header additions, import.test D-21 (2-3 cases)
</output>
</content>
</invoke>