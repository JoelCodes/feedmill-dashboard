---
phase: 05-header
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/app/page.tsx
  - src/components/OrdersTable.tsx
  - src/components/Header.tsx
  - src/components/NotificationDropdown.tsx
autonomous: true
requirements: [HEADER-01, HEADER-02]
gap_closure: true
gaps_addressed:
  - "Search not wired to table filtering"
  - "Notification read indicators don't update"

must_haves:
  truths:
    - "Typing in header search box filters the orders table"
    - "Clicking a notification in dropdown removes its blue dot indicator"
    - "Badge count and blue dot indicators use the same source of truth (readNotificationIds)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Search state management and wiring"
      contains: "headerSearchTerm"
    - path: "src/components/OrdersTable.tsx"
      provides: "External search prop support"
      contains: "externalSearchTerm"
    - path: "src/components/NotificationDropdown.tsx"
      provides: "Read state from localStorage via prop"
      contains: "readNotificationIds"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/Header.tsx"
      via: "onSearch callback prop"
      pattern: "onSearch=\\{setHeaderSearchTerm\\}"
    - from: "src/app/page.tsx"
      to: "src/components/OrdersTable.tsx"
      via: "externalSearchTerm prop"
      pattern: "externalSearchTerm=\\{headerSearchTerm\\}"
    - from: "src/components/Header.tsx"
      to: "src/components/NotificationDropdown.tsx"
      via: "readNotificationIds prop"
      pattern: "readNotificationIds=\\{readNotificationIds\\}"
---

<objective>
Close verification gaps for Phase 05 Header by wiring the existing search and notification infrastructure to their consumers.

Purpose: The Header component has functional search (with debouncing) and notification read tracking (with localStorage), but neither is connected to the components that need them. This plan completes the data flow.

Output: Fully functional header search that filters the orders table, and notification read indicators that update when users click notifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-header/05-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/Header.tsx (existing):
```typescript
interface HeaderProps {
  onSearch?: (query: string) => void;
}
// Header already calls onSearch(debouncedSearchTerm) when prop is provided (lines 46-50)
```

From src/components/OrdersTable.tsx (existing):
```typescript
interface OrdersTableProps {
  selectedOrderId: string | null;
  onSelectOrder: (id: string) => void;
}
// Has internal searchTerm state (line 20) and debouncedSearch (line 21)
// Uses debouncedSearch in filteredOrders useMemo (lines 70-76)
```

From src/components/NotificationDropdown.tsx (existing):
```typescript
interface NotificationDropdownProps {
  notifications: Notification[];
  isOpen: boolean;
  onClose: () => void;
  onMarkAsRead: (id: string) => void;
  onClearAll: () => void;
}
// Currently checks notification.isRead (lines 76, 81) - NEEDS CHANGE
```

From src/components/Header.tsx (existing state):
```typescript
// readNotificationIds is already tracked via useLocalStorage (lines 35-38)
const [readNotificationIds, setReadNotificationIds] = useLocalStorage<string[]>(
  'notifications-read',
  []
);
// Badge count already uses readNotificationIds (lines 53-55)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire header search to Dashboard and OrdersTable</name>
  <files>src/app/page.tsx, src/components/OrdersTable.tsx</files>
  <read_first>
    - src/app/page.tsx (current Dashboard implementation)
    - src/components/OrdersTable.tsx (current OrdersTableProps interface and search logic)
    - src/components/Header.tsx (verify onSearch callback signature)
  </read_first>
  <action>
**In src/app/page.tsx:**
1. Add useState import if not present
2. Add state for header search: `const [headerSearchTerm, setHeaderSearchTerm] = useState('');`
3. Pass onSearch prop to Header: `<Header onSearch={setHeaderSearchTerm} />`
4. Pass externalSearchTerm prop to OrdersTable: `<OrdersTable ... externalSearchTerm={headerSearchTerm} />`

**In src/components/OrdersTable.tsx:**
1. Add `externalSearchTerm?: string` to OrdersTableProps interface
2. Destructure `externalSearchTerm` from props in component function
3. Modify filteredOrders useMemo:
   - Replace `if (debouncedSearch)` with `const activeSearch = externalSearchTerm || debouncedSearch;`
   - Use `if (activeSearch)` for filtering
   - Update the filter logic to use `activeSearch` instead of `debouncedSearch`
4. Apply same change to statusCounts useMemo (lines 97-104)
5. Apply same change to hasChangesCount useMemo (lines 121-127)
6. Update highlightMatch calls in table rows to use `externalSearchTerm || debouncedSearch` for highlighting

This allows the table's internal search bar to still work when no external search is provided, while giving priority to the header search when present.
  </action>
  <verify>
    <automated>grep -n "externalSearchTerm" src/components/OrdersTable.tsx && grep -n "onSearch={setHeaderSearchTerm}" src/app/page.tsx && grep -n "externalSearchTerm={headerSearchTerm}" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/OrdersTable.tsx contains `externalSearchTerm?: string` in OrdersTableProps
    - src/components/OrdersTable.tsx contains `const activeSearch = externalSearchTerm || debouncedSearch`
    - src/app/page.tsx contains `useState('')` for headerSearchTerm
    - src/app/page.tsx contains `onSearch={setHeaderSearchTerm}`
    - src/app/page.tsx contains `externalSearchTerm={headerSearchTerm}`
    - TypeScript compiles without errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Header search input filters OrdersTable. Typing in header search shows filtered results in table with highlights.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification read state to NotificationDropdown</name>
  <files>src/components/Header.tsx, src/components/NotificationDropdown.tsx</files>
  <read_first>
    - src/components/Header.tsx (current NotificationDropdown usage, readNotificationIds state)
    - src/components/NotificationDropdown.tsx (current interface and isRead logic at lines 76, 81)
  </read_first>
  <action>
**In src/components/NotificationDropdown.tsx:**
1. Add `readNotificationIds: string[]` to NotificationDropdownProps interface
2. Destructure `readNotificationIds` in component function
3. Replace line 76 `{!notification.isRead && (` with `{!readNotificationIds.includes(notification.id) && (`
4. Replace line 81 `{notification.isRead && (` with `{readNotificationIds.includes(notification.id) && (`

**In src/components/Header.tsx:**
1. Add `readNotificationIds={readNotificationIds}` prop to NotificationDropdown component (around line 122-128)

The NotificationDropdown now uses the same localStorage-backed readNotificationIds array that the badge count uses, ensuring consistent read/unread state.
  </action>
  <verify>
    <automated>grep -n "readNotificationIds: string\[\]" src/components/NotificationDropdown.tsx && grep -n "readNotificationIds.includes" src/components/NotificationDropdown.tsx && grep -n "readNotificationIds={readNotificationIds}" src/components/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NotificationDropdown.tsx contains `readNotificationIds: string[]` in interface
    - src/components/NotificationDropdown.tsx contains `readNotificationIds.includes(notification.id)` (2 occurrences)
    - src/components/NotificationDropdown.tsx does NOT contain `notification.isRead` (grep returns no matches)
    - src/components/Header.tsx contains `readNotificationIds={readNotificationIds}` prop on NotificationDropdown
    - TypeScript compiles without errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Clicking a notification in dropdown removes its blue dot. Badge count and blue dots are consistent - both use readNotificationIds from localStorage.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all gaps closed with build and manual check</name>
  <files>None (verification only)</files>
  <read_first>
    - src/app/page.tsx (verify final state)
    - src/components/OrdersTable.tsx (verify final state)
    - src/components/NotificationDropdown.tsx (verify final state)
  </read_first>
  <action>
1. Run TypeScript check: `npx tsc --noEmit`
2. Run build: `npm run build`
3. Run lint: `npm run lint`
4. Verify search wiring by checking file contents:
   - page.tsx has headerSearchTerm state
   - page.tsx passes onSearch to Header
   - page.tsx passes externalSearchTerm to OrdersTable
   - OrdersTable uses externalSearchTerm in filtering
5. Verify notification wiring by checking file contents:
   - NotificationDropdown interface has readNotificationIds
   - NotificationDropdown uses readNotificationIds.includes() for blue dot logic
   - Header passes readNotificationIds to NotificationDropdown
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0 and outputs "Compiled successfully"
    - `npm run lint` exits with code 0
    - No TypeScript errors related to missing props or type mismatches
  </acceptance_criteria>
  <done>All Phase 05 verification gaps closed. Search and notification read state are fully wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan only wires existing client-side state between components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | localStorage readNotificationIds | accept | User can only tamper with their own read state; no security boundary crossed |
| T-05-04-02 | Information Disclosure | search term in state | accept | Search term is user input, displayed in UI, no sensitive data |

No new security controls required - this plan only completes wiring of existing safe patterns.
</threat_model>

<verification>
## Phase Verification

After completing all tasks:

1. **Search Integration:**
   - Start dev server: `npm run dev`
   - Navigate to Dashboard (/)
   - Type "Valley" in header search box
   - Verify: OrdersTable shows only orders with "Valley" in customer or product column
   - Verify: Matches are highlighted in table

2. **Notification Read State:**
   - Click bell icon to open notification dropdown
   - Note which notifications have blue dots (unread)
   - Click on an unread notification
   - Verify: Blue dot disappears immediately
   - Verify: Badge count decrements
   - Refresh page
   - Verify: Same notification still shows as read (no blue dot)

3. **Build Verification:**
   - `npm run build` succeeds
   - `npm run lint` passes
   - `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Header search filters OrdersTable (typing "Valley" shows only Valley farms)
- Notification blue dots update when clicked (same source of truth as badge)
- TypeScript, build, and lint all pass
- No regressions to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/05-header/05-04-SUMMARY.md`
</output>
