---
phase: 22-auth-page-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - designs/page-layout.pen
autonomous: false
requirements: []

must_haves:
  truths:
    - "Header user area design exists with avatar (32px) + name display"
    - "Hover state shows subtle background highlight on user area trigger"
    - "Dropdown menu design shows name/email stacked + Sign Out button"
    - "Light theme variant uses $bg-card (#ffffff) and $text-primary (#2d3748)"
    - "Dark theme variant uses $bg-card (#2d3748) and $text-primary (#e2e8f0)"
  artifacts:
    - path: "designs/page-layout.pen"
      provides: "Header user area component designs"
      contains: "HeaderUserArea"
  key_links:
    - from: "designs/page-layout.pen"
      to: "designs/component-library.pen"
      via: "Token references ($primary, $bg-card, etc.)"
      pattern: "\\$[a-z-]+"
---

<objective>
Design the header user area component in Pencil.dev's page-layout.pen file.

Purpose: Create visual design specification for the authenticated user display that Phase 23 will implement using Clerk's UserButton component.

Output: Updated page-layout.pen with:
- HeaderUserArea/Default reusable component (avatar 32px + name)
- HeaderUserArea/Hover state variant
- HeaderUserArea/DropdownOpen state with dropdown menu
- Light and dark theme variants for all states
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-auth-page-design/22-CONTEXT.md
@.planning/phases/22-auth-page-design/22-RESEARCH.md
@.planning/phases/22-auth-page-design/22-UI-SPEC.md
@designs/page-layout.pen
@designs/component-library.pen

<interfaces>
<!-- Design tokens from component-library.pen variables section -->
Available tokens for use in designs:
- Colors: $primary, $primary-dark, $primary-hover, $bg-card, $bg-page, $divider
- Colors: $text-primary, $text-secondary, $text-white
- Colors: $success, $warning, $error, $info (and their -light variants)
- Spacing: $space-1 (4px), $space-2 (8px), $space-3 (12px), $space-4 (16px), $space-5 (24px), $space-6 (32px)
- Radius: $radius-sm (6px), $radius-md (8px), $radius-lg (12px), $radius-xl (16px)

Theme variants structure (from component-library.pen):
```json
"themes": { "mode": ["light", "dark"] },
"variables": {
  "token-name": {
    "type": "color",
    "value": [
      { "value": "#hexcode", "theme": { "mode": "light" } },
      { "value": "#hexcode", "theme": { "mode": "dark" } }
    ]
  }
}
```

Existing Header structure (from page-layout.pen lines 542-614):
- "Header Right" frame at x=OPrqP with gap: 16, alignItems: center
- Contains: Search (200x40), settingsIcon (14x14), bellIcon (14x14)
- User area goes AFTER bellIcon per D-01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable HeaderUserArea components with all states</name>
  <files>designs/page-layout.pen</files>
  <read_first>
    - designs/page-layout.pen (target file - see current structure)
    - designs/component-library.pen (reference for reusable component patterns and token syntax)
    - .planning/phases/22-auth-page-design/22-UI-SPEC.md (typography, spacing, color specifications)
  </read_first>
  <action>
Update designs/page-layout.pen to add header user area components. The file must be updated to:

1. **Upgrade version** from "2.8" to "2.11" to support theme variants and variables (match component-library.pen)

2. **Add variables section** at root level (after "children" array) with theme-conditional tokens. Copy the relevant variables from component-library.pen:
   - bg-card, bg-page, divider
   - text-primary, text-secondary, text-white
   - primary, primary-hover
   - radius-md, radius-lg
   - space-1, space-2, space-4

3. **Add themes section** at root level:
   ```json
   "themes": { "mode": ["light", "dark"] }
   ```

4. **Create reusable HeaderUserArea/Default component** in the children array (before the page-layout frame) with these exact specifications:
   - type: "frame"
   - id: generate unique 5-char ID
   - name: "HeaderUserArea/Default"
   - reusable: true
   - width: 120
   - height: 40
   - fill: "$bg-card"
   - cornerRadius: "$radius-md"
   - layout: "horizontal" (NOT "none")
   - gap: "$space-2" (8px)
   - padding: "$space-2" (8px)
   - alignItems: "center"
   - Children:
     a) Avatar frame:
        - type: "frame"
        - id: unique
        - name: "Avatar"
        - width: 32
        - height: 32
        - fill: "$primary"
        - cornerRadius: 9999 (full circle)
        - layout: "none"
        - children: text element with "JD" initials (fallback display)
          - type: "text"
          - content: "JD"
          - fill: "$text-white"
          - fontSize: 12
          - fontWeight: "600"
          - x: 8, y: 9 (centered in 32px circle)
     b) Display name text:
        - type: "text"
        - id: unique
        - name: "DisplayName"
        - content: "John Doe"
        - fill: "$text-primary"
        - fontSize: 13
        - fontWeight: "500"
        - fontFamily: "Helvetica"
        - lineHeight: 1.5

5. **Create HeaderUserArea/Hover component** as a ref with fill override:
   - type: "ref"
   - id: unique
   - name: "HeaderUserArea/Hover"
   - ref: [id of HeaderUserArea/Default]
   - fill: "$bg-page" (subtle background change for hover state)

6. **Create UserDropdownMenu component** (reusable):
   - type: "frame"
   - id: unique
   - name: "UserDropdownMenu"
   - reusable: true
   - width: 200
   - fill: "$bg-card"
   - cornerRadius: "$radius-lg" (12px)
   - layout: "vertical"
   - gap: 0
   - effect: shadow (x:0, y:4, blur:12, color:"#00000026")
   - Children:
     a) User info section frame:
        - width: "fill_container"
        - layout: "vertical"
        - gap: "$space-1" (4px)
        - padding: "$space-4" (16px)
        - children:
          - text "John Doe" (fill: $text-primary, fontSize: 13, fontWeight: 500)
          - text "john.doe@example.com" (fill: $text-secondary, fontSize: 11, fontWeight: 400)
     b) Divider rectangle:
        - width: "fill_container"
        - height: 1
        - fill: "$divider"
     c) Sign Out button frame:
        - width: "fill_container"
        - height: 40
        - layout: "horizontal"
        - alignItems: "center"
        - padding: "$space-4"
        - children:
          - text "Sign Out" (fill: $text-primary, fontSize: 13, fontWeight: 500)

7. **Create HeaderUserArea/DropdownOpen composite** showing trigger + dropdown:
   - type: "frame"
   - id: unique
   - name: "HeaderUserArea/DropdownOpen"
   - reusable: true
   - layout: "none" (absolute positioning)
   - width: 200
   - height: 150 (enough to show trigger + dropdown with gap)
   - children:
     a) ref to HeaderUserArea/Default at x:80, y:0 (aligned right within frame)
     b) ref to UserDropdownMenu at x:0, y:48 (positioned below trigger with 8px gap)

8. **Add instances to a new "Header User Area" artboard** for documentation:
   Create a new frame as a child of root children:
   - type: "frame"
   - name: "Header User Area - Light Mode"
   - theme: { "mode": "light" }
   - width: 800
   - height: 400
   - x: 0, y: 1300 (below existing page-layout frame)
   - fill: "$bg-page"
   - layout: "none"
   - children: section title text + instances of all three states

9. **Create dark mode documentation artboard**:
   - Same structure as light mode artboard
   - name: "Header User Area - Dark Mode"
   - theme: { "mode": "dark" }
   - x: 900, y: 1300

The JSON must be valid. Use unique IDs (5-6 alphanumeric characters) for each new element.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('designs/page-layout.pen'));console.log('version:',j.version);console.log('themes:',JSON.stringify(j.themes));console.log('variables count:',Object.keys(j.variables||{}).length);const names=[];const scan=(arr)=>arr.forEach(c=>{if(c.name)names.push(c.name);if(c.children)scan(c.children)});scan(j.children);console.log('HeaderUserArea components:',names.filter(n=>n.includes('HeaderUserArea')||n.includes('UserDropdown')).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"version": "2.11"' designs/page-layout.pen returns 1
    - grep -c '"HeaderUserArea/Default"' designs/page-layout.pen returns at least 1
    - grep -c '"HeaderUserArea/Hover"' designs/page-layout.pen returns at least 1
    - grep -c '"HeaderUserArea/DropdownOpen"' designs/page-layout.pen returns at least 1
    - grep -c '"UserDropdownMenu"' designs/page-layout.pen returns at least 1
    - grep -c '"themes"' designs/page-layout.pen returns 1
    - grep -c '"variables"' designs/page-layout.pen returns 1
    - grep -c '"reusable": true' designs/page-layout.pen returns at least 4 (new components)
    - grep -c '"Sign Out"' designs/page-layout.pen returns at least 1
    - node -e validates JSON parses without error
  </acceptance_criteria>
  <done>
    - page-layout.pen updated to version 2.11 with themes and variables
    - HeaderUserArea/Default component exists with 32px avatar + name
    - HeaderUserArea/Hover component exists with $bg-page fill override
    - HeaderUserArea/DropdownOpen composite shows trigger + dropdown menu
    - UserDropdownMenu component contains user info + divider + Sign Out
    - Light mode documentation artboard shows all three states
    - Dark mode documentation artboard shows all three states
    - All color values use token references ($primary, $bg-card, etc.)
    - JSON is valid and can be opened in Pencil.dev
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification in Pencil.dev</name>
  <what-built>Header user area component designs with three states (default, hover, dropdown open) and both light/dark theme variants in page-layout.pen</what-built>
  <how-to-verify>
1. Open Pencil.dev application
2. Open file: designs/page-layout.pen
3. Navigate to the new "Header User Area - Light Mode" artboard (below the main page layout)
4. Verify the following design elements:

**Default State (HeaderUserArea/Default):**
- [ ] Avatar is 32px circle with teal/blue primary color
- [ ] Avatar displays "JD" initials in white
- [ ] "John Doe" name displays to the right of avatar
- [ ] 8px gap between avatar and name
- [ ] Card-colored background (#ffffff in light mode)
- [ ] Rounded corners (8px)

**Hover State (HeaderUserArea/Hover):**
- [ ] Same layout as default
- [ ] Background color changed to page background (subtle highlight)

**Dropdown Open State (HeaderUserArea/DropdownOpen):**
- [ ] User trigger at top right
- [ ] Dropdown menu positioned below with 8px gap
- [ ] Dropdown shows "John Doe" name
- [ ] Dropdown shows "john.doe@example.com" email in secondary color
- [ ] Divider line between user info and Sign Out
- [ ] "Sign Out" text at bottom of dropdown
- [ ] Dropdown has shadow effect

5. Navigate to "Header User Area - Dark Mode" artboard
6. Verify all components render correctly with dark theme colors:
- [ ] Background is dark (#2d3748)
- [ ] Text is light (#e2e8f0)
- [ ] Primary color adapts (blue instead of teal)

7. Confirm overall visual quality matches existing component-library.pen style
  </how-to-verify>
  <resume-signal>Type "approved" if design matches specifications, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design files | Local .pen files - no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | Design file | accept | Design file contains only visual specifications, no secrets or PII |

Note: This is a design-only phase. No code execution, no authentication logic, no data processing. Security concerns are minimal.
</threat_model>

<verification>
- [ ] page-layout.pen version upgraded to 2.11
- [ ] themes section added with light/dark mode support
- [ ] variables section added with required design tokens
- [ ] HeaderUserArea/Default component with avatar + name
- [ ] HeaderUserArea/Hover component with hover state
- [ ] HeaderUserArea/DropdownOpen composite
- [ ] UserDropdownMenu component with user info + Sign Out
- [ ] Light mode documentation artboard
- [ ] Dark mode documentation artboard
- [ ] All tokens use $token-name syntax (no hardcoded colors in new components)
- [ ] JSON is valid (parseable by Node.js)
- [ ] Visual verification passed in Pencil.dev app
</verification>

<success_criteria>
- Phase 22 design specifications complete and ready for Phase 23 implementation
- Header user area component designed with 3 states (default, hover, dropdown open)
- Both light and dark theme variants designed
- Design uses existing design system tokens
- Design file committed and version controlled
</success_criteria>

<output>
After completion, create `.planning/phases/22-auth-page-design/22-01-SUMMARY.md`
</output>
