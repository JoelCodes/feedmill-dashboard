---
phase: 17-component-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ThemeToggle.tsx
  - src/components/ui/ThemeToggle.test.tsx
autonomous: true
requirements: [COMP-04]

must_haves:
  truths:
    - "User can click Light option to switch to light theme"
    - "User can click Dark option to switch to dark theme"
    - "User can click System option to use system preference"
    - "Active theme option shows visual active state"
  artifacts:
    - path: "src/components/ui/ThemeToggle.tsx"
      provides: "Theme toggle component using useTheme() hook"
      exports: ["default"]
    - path: "src/components/ui/ThemeToggle.test.tsx"
      provides: "Test coverage for theme switching"
  key_links:
    - from: "src/components/ui/ThemeToggle.tsx"
      to: "next-themes"
      via: "useTheme() hook"
      pattern: "import.*useTheme.*from.*next-themes"
---

<objective>
Create the ThemeToggle component that allows users to switch between light, dark, and system themes. Uses the useTheme() hook from next-themes.

Purpose: Enable users to control the application theme from Settings or any page.
Output: `src/components/ui/ThemeToggle.tsx` with test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-component-library/17-RESEARCH.md
@.planning/phases/17-component-library/17-UI-SPEC.md
@.planning/phases/17-component-library/17-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From next-themes useTheme() hook:
```typescript
const { theme, setTheme, resolvedTheme } = useTheme();
// theme: "light" | "dark" | "system"
// setTheme: (theme: string) => void
// resolvedTheme: "light" | "dark" (actual theme when "system" active)
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Token references from globals.css:
- Primary: `--primary`
- Text: `--text-white`, `--text-secondary`
- Background: `--bg-page`
- Border: `--divider`, `--radius-md`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ThemeToggle component with tests</name>
  <files>src/components/ui/ThemeToggle.tsx, src/components/ui/ThemeToggle.test.tsx</files>
  <read_first>
    - src/components/ThemeProvider.test.tsx (existing theme test pattern)
    - src/lib/utils.ts (cn utility)
    - src/app/globals.css (token values)
    - .planning/phases/17-component-library/17-UI-SPEC.md (exact specifications)
  </read_first>
  <behavior>
    - Test 1: ThemeToggle renders three options (Light, Dark, System)
    - Test 2: Light option calls setTheme("light") on click
    - Test 3: Dark option calls setTheme("dark") on click
    - Test 4: System option calls setTheme("system") on click
    - Test 5: Active option has aria-checked="true"
    - Test 6: Container has role="radiogroup" with aria-label
    - Test 7: Each option has role="radio"
  </behavior>
  <action>
1. Create test file first at `src/components/ui/ThemeToggle.test.tsx`:
   - Mock next-themes: jest.mock("next-themes", () => ({ useTheme: jest.fn() }))
   - Import render, screen, fireEvent from @testing-library/react
   - Import { useTheme } from "next-themes" (mocked)
   - Write tests for all 7 behaviors

2. Create ThemeToggle component at `src/components/ui/ThemeToggle.tsx`:
   - Add "use client" directive at top (required for useTheme hook)
   - Import { useTheme } from "next-themes"
   - Import { Sun, Moon, Monitor } from "lucide-react"
   - Import { cn } from "@/lib/utils"

3. Define theme options array:
   ```typescript
   const options = [
     { value: "light", label: "Light", icon: Sun },
     { value: "dark", label: "Dark", icon: Moon },
     { value: "system", label: "System", icon: Monitor },
   ];
   ```

4. Implement ThemeToggle function component:
   - Call useTheme() to get { theme, setTheme }
   - Return div with:
     - role="radiogroup"
     - aria-label="Theme selection"
     - className="inline-flex border border-[var(--divider)] rounded-[var(--radius-md)] overflow-hidden"
   - Map over options to render buttons:
     - key={option.value}
     - role="radio"
     - aria-checked={theme === option.value}
     - onClick={() => setTheme(option.value)}
     - className with conditional active state:
       - Active: "bg-[var(--primary)] text-[var(--text-white)] font-semibold"
       - Inactive: "bg-transparent text-[var(--text-secondary)] hover:bg-[var(--bg-page)]"
     - Border-right on all except last: "border-r border-[var(--divider)]"
     - Padding: "px-4 py-2"
     - Icon: <Icon className="h-4 w-4" />
     - Label: <span>{option.label}</span>

5. Export: export default ThemeToggle
  </action>
  <verify>
    <automated>npm test -- ThemeToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "use client" src/components/ui/ThemeToggle.tsx returns >= 1
    - grep -c "useTheme" src/components/ui/ThemeToggle.tsx returns >= 1
    - grep -c "setTheme" src/components/ui/ThemeToggle.tsx returns >= 1
    - grep -c 'role="radiogroup"' src/components/ui/ThemeToggle.tsx returns >= 1
    - grep -c 'role="radio"' src/components/ui/ThemeToggle.tsx returns >= 1
    - grep -c "aria-checked" src/components/ui/ThemeToggle.tsx returns >= 1
    - npm test -- ThemeToggle.test.tsx passes with 0 failures
  </acceptance_criteria>
  <done>ThemeToggle component switches themes on click, shows active state, has proper ARIA attributes, all 7 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme value | String value from next-themes, limited to predefined options |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | Theme value | accept | next-themes validates theme values internally |
| T-17-04 | Information Disclosure | Theme preference | accept | Theme preference is non-sensitive user preference |
</threat_model>

<verification>
- All 7 ThemeToggle tests pass
- Clicking Light/Dark/System calls setTheme with correct value
- Active option shows visual active state
- ARIA attributes correctly applied
</verification>

<success_criteria>
- ThemeToggle component exists at src/components/ui/ThemeToggle.tsx
- Tests exist at src/components/ui/ThemeToggle.test.tsx
- npm test -- ThemeToggle.test.tsx passes
- Component uses "use client" directive
- useTheme() hook used for theme state
</success_criteria>

<output>
After completion, create `.planning/phases/17-component-library/17-02-SUMMARY.md`
</output>
