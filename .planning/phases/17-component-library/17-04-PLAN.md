---
phase: 17-component-library
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/components/ui/Card.tsx
  - src/components/ui/Card.test.tsx
autonomous: true
requirements: [COMP-03]

must_haves:
  truths:
    - "Card renders with compound pattern (Card.Header, Card.Content, Card.Footer)"
    - "Card.Header renders with bottom border and proper typography"
    - "Card.Content renders as flex-1 growing section"
    - "Card.Footer renders with top border and flex-end alignment"
    - "Card becomes clickable with hover state when onClick provided"
  artifacts:
    - path: "src/components/ui/Card.tsx"
      provides: "Card compound component with dot notation"
      exports: ["default"]
    - path: "src/components/ui/Card.test.tsx"
      provides: "Test coverage for Card compound pattern"
  key_links:
    - from: "src/components/ui/Card.tsx"
      to: "src/lib/utils.ts"
      via: "cn() utility import"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Create the Card compound component using dot notation pattern (D-06). Card.Header, Card.Content, and Card.Footer provide flexible composition. Card supports clickable variant when onClick provided.

Purpose: Provide a flexible container primitive for content grouping with consistent styling.
Output: `src/components/ui/Card.tsx` with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-component-library/17-RESEARCH.md
@.planning/phases/17-component-library/17-UI-SPEC.md
@.planning/phases/17-component-library/17-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

From class-variance-authority:
```typescript
import { cva, type VariantProps } from "class-variance-authority";
```

Token references from globals.css:
- Background: `--bg-card`
- Border: `--divider`, `--radius-xl`
- Shadow: `--shadow-card`, `--shadow-sm`
- Text: `--text-primary`
- Spacing: `--space-4` (16px), `--space-2` (8px)

Compound pattern from 17-RESEARCH.md:
```typescript
Card.Header = CardHeader;
Card.Content = CardContent;
Card.Footer = CardFooter;
export default Card;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Card compound component with tests</name>
  <files>src/components/ui/Card.tsx, src/components/ui/Card.test.tsx</files>
  <read_first>
    - src/lib/utils.ts (cn utility)
    - src/app/globals.css (token values)
    - .planning/phases/17-component-library/17-UI-SPEC.md (Card specifications section 3.5)
    - .planning/phases/17-component-library/17-RESEARCH.md (Compound component pattern)
  </read_first>
  <behavior>
    - Test 1: Card renders children
    - Test 2: Card.Header renders with border-b border-[var(--divider)]
    - Test 3: Card.Content renders with flex-1 class
    - Test 4: Card.Footer renders with border-t border-[var(--divider)] and justify-end
    - Test 5: Card has default variant with shadow-[var(--shadow-card)]
    - Test 6: Card has elevated variant with shadow-[var(--shadow-sm)]
    - Test 7: Card is clickable when onClick provided (cursor-pointer, hover state)
    - Test 8: Clickable Card has role="button" and tabIndex={0}
    - Test 9: Card.Header renders h3 with text-lg font-semibold
    - Test 10: Card compound pattern works (Card.Header, Card.Content, Card.Footer accessible)
  </behavior>
  <action>
1. Create test file first at `src/components/ui/Card.test.tsx`:
   - Import render, screen, fireEvent from @testing-library/react
   - Import Card from ./Card
   - Write failing tests for all 10 behaviors
   - Test compound pattern: render(<Card><Card.Header>Title</Card.Header></Card>)

2. Create Card component at `src/components/ui/Card.tsx`:
   - Import { cva, type VariantProps } from "class-variance-authority"
   - Import { cn } from "@/lib/utils"

3. Define cardVariants using cva():
   - Base: "bg-[var(--bg-card)] border border-[var(--divider)] rounded-[var(--radius-xl)] overflow-hidden"
   - variant.default: "shadow-[var(--shadow-card)]"
   - variant.elevated: "shadow-[var(--shadow-sm)]"
   - defaultVariants: { variant: "default" }

4. Define CardProps interface:
   ```typescript
   interface CardProps
     extends React.HTMLAttributes<HTMLDivElement>,
       VariantProps<typeof cardVariants> {
     children: React.ReactNode;
   }
   ```

5. Implement Card function:
   - Destructure variant, className, children, onClick, ...props
   - Determine if clickable: const isClickable = !!onClick
   - Add clickable classes when onClick present:
     - "cursor-pointer hover:opacity-95 transition-opacity active:scale-[0.98]"
   - Return div with:
     - className={cn(cardVariants({ variant }), isClickable && "cursor-pointer...", className)}
     - role={isClickable ? "button" : undefined}
     - tabIndex={isClickable ? 0 : undefined}
     - onClick={onClick}
     - {children}

6. Define CardHeaderProps, CardContentProps, CardFooterProps interfaces:
   ```typescript
   interface CardHeaderProps extends React.HTMLAttributes<HTMLDivElement> {
     children: React.ReactNode;
   }
   // Same for Content and Footer
   ```

7. Implement CardHeader function:
   - Return div with:
     - className={cn("px-4 py-4 border-b border-[var(--divider)]", className)}
     - h3 child with: className="text-lg font-semibold text-[var(--text-primary)]"
     - {children}

8. Implement CardContent function:
   - Return div with:
     - className={cn("px-4 py-4 flex-1", className)}
     - {children}

9. Implement CardFooter function:
   - Return div with:
     - className={cn("px-4 py-4 border-t border-[var(--divider)] flex justify-end gap-2", className)}
     - {children}

10. Attach sub-components and export:
    ```typescript
    Card.Header = CardHeader;
    Card.Content = CardContent;
    Card.Footer = CardFooter;
    export default Card;
    ```
  </action>
  <verify>
    <automated>npm test -- Card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Card.Header = CardHeader" src/components/ui/Card.tsx returns >= 1
    - grep -c "Card.Content = CardContent" src/components/ui/Card.tsx returns >= 1
    - grep -c "Card.Footer = CardFooter" src/components/ui/Card.tsx returns >= 1
    - grep -c "cardVariants" src/components/ui/Card.tsx returns >= 1
    - grep -c 'role.*button' src/components/ui/Card.tsx returns >= 1
    - grep -c "tabIndex" src/components/ui/Card.tsx returns >= 1
    - npm test -- Card.test.tsx passes with 0 failures
  </acceptance_criteria>
  <done>Card compound component with dot notation pattern, default and elevated variants, clickable support, all 10 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card children | React nodes passed as children |
| onClick handler | Function prop from parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | Card children | accept | React sanitizes JSX children by default |
| T-17-08 | Repudiation | onClick action | accept | Component is presentational, action logging is parent's responsibility |
</threat_model>

<verification>
- All 10 Card tests pass
- Compound pattern works (Card.Header, Card.Content, Card.Footer)
- Default and elevated variants render correctly
- Clickable Card has proper accessibility attributes
</verification>

<success_criteria>
- Card component exists at src/components/ui/Card.tsx
- Tests exist at src/components/ui/Card.test.tsx
- npm test -- Card.test.tsx passes
- Dot notation pattern implemented (Card.Header, Card.Content, Card.Footer)
- CVA used for variant definitions
</success_criteria>

<output>
After completion, create `.planning/phases/17-component-library/17-04-SUMMARY.md`
</output>
